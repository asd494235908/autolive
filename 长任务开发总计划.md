# AutoLive 长任务开发总计划

> 音画统一时间轴与同步协调专项（2026-08-30，代码及聚焦测试完成、待用户真实媒体验收）：Rust 以 `playback_generation + source_path + loop_index + source_duration_ms` 统一标识媒体段，严格分离跨循环 presentation PTS 与单文件 source-local PTS，mpv 只接收合法 source-local PTS。播放态循环边界执行 `Seek → SetPause(false)`，暂停态执行 `SetPause(true) → Seek`；无效/越界音频时钟只停用纠偏并恢复基础速度，不触发 GPU/CPU4 降级。React 视频同步采用单 IPC 在途和 latest-wins pending，busy 按 `100/250/500ms` 有界退避，result_unknown 先查状态，stale 刷新最新 desired；Rust 已接入稳定错误分类与健康 GPU/CPU4 的 Original 防覆盖。同步命令的 mpv `IpcTimeout` 返回 result_unknown 并保留会话，`IpcDisconnected`、断管和进程退出仍沿既有单向降级。本轮未运行 1080p 真实媒体、多文件、1080p60、4K 或长稳，不能宣称无卡顿或音画同步指标已保证。

> 普通声音候选 prepare 调度专项（2026-08-30，代码及聚焦检查完成、待用户真实媒体验收）：校验、路径/滤镜准备与候选启动已移出 Tauri 主事件线程，同时保留现有单飞、取消、超时和候选所有权。该项只调整调度位置，不改变声音 DSP、参数语义、AAC/M4A 候选、N/N+1 提交、PortAudio、插话、固定话术或混音；真实联合媒体流畅性仍待用户验收。

> 播放声音连续性专项（2026-08-30，代码及非媒体自动化完成、待用户实测）：已消除“每 250ms 用 2 秒一次的陈旧 mpv PTS 回拉 WebView 原声”的错误时钟环。带音轨视频的前端源时间轴改由连续原声元素发布；普通漂移不再硬 seek 或调节原声倍速，只有显式 seek、真实循环/结束边界才对齐。自愈重载真实主时钟，原生后端停止/失败后 WebView 兼容画面会重新加载并跟随原声位置。Rust 既有 Original 失败终态维持 `Source + Failed + PID=None`，意图性 stop/suspend 维持 `Stopped`。PortAudio 可听 PTS 闭环及普通声音 DSP、候选、插话、固定话术、混音均未改变；本批按用户要求不运行媒体，现场连续性仍由用户复验。

> 视频处理开关同会话修复（2026-08-30，代码完成、待用户实测）：当前实现已将逻辑 `Source/Original` 与 mpv 物理启动模式分离。在同一源、同一 playback generation、同一宿主 HWND、进程存活且物理模式仍匹配当前 fallback floor 时，关闭视频处理由 runtime actor 在现有 mpv 上写入 GPU 中性 shader 或 CPU4 四项中性值并清除视频周期计划；再次开启后复用同一 PID 提交后续周期快照，不再仅因开关切换执行 surface suspend、重新 seek 或重建视频表面。首次建立会话、进程不存在/退出/丢失、换源或播放代次变化、宿主 HWND 变化、物理模式与 fallback floor 不匹配，以及导致会话失效的 GPU、设备或 IPC 故障降级、显式停止或关窗，仍允许先回收旧进程再创建或重启；普通周期更新不允许重启。GPU 自动周期当前仍仅准入 `79/83`（61 项 shader + 18 项受限 PTS 调度），剩余 4 项继续 fail-closed，CPU4 仍为 `4/83`。本批未运行真实视频，未实施或验收 4K，也不代表无卡顿、长稳或跨 GPU 验收通过；本修复未修改普通声音 DSP、声音候选、PortAudio、插话、固定话术或混音，最终效果和开关流畅性待用户实测确认。

> 2026-08-28 最高优先级覆盖：桌面视频进入 Phase 6I，唯一实施入口为 [`mpv/libplacebo 实时 GPU 主链实施方案`](./docs/superpowers/plans/2026-08-28-mpv-libplacebo实时GPU主链实施方案.md)。视频改为常驻 mpv `gpu-next/libplacebo` GPU83 实时主链，周期只原子更新参数；失败后按会话单向降级到 libavfilter CPU4，再失败保持 Original。CPU4 只执行亮度、对比度、饱和度和色相。旧 MSE、视频 period、逐周期 FFmpeg 重编码和“mpv 仅预留”方案已删除，不再作为当前任务上下文、入口或验收。

> 本文件是当前项目后续开发的唯一长任务执行依据。所有开发任务必须先阅读本文件、`AGENTS.md`、`产品需求文档.md`、`系统架构总览.md`、`管理系统架构.md`、`桌面客户端架构.md` 和对应接口契约，再开始修改代码。
>
> 当前版本范围声明（2026-08-26）：本版本不开发实时话术幻化。实时 speech-to-speech、VAD/ASR/LLM/TTS、候选音轨实时换轨和模型租约直连从当前阶段、当前验收和当前发布范围移除；旧阶段条目仅保留历史审计或后续版本规划。当前播放基线是最多 100 项、支持文件选择/系统拖放导入以及成员原子 CRUD 的本地有序播放池；单一窗口以一个活动媒体源执行 `A→B→…→A`，单项池自循环，播放池只存在当前进程且不是生成版本队列。登录安全批次不开发注册，用户继续由管理员创建。
>
> TS 视频与纯音频统一播放批次（2026-08-26，代码完成、实机待验收）：实施入口为 [`docs/superpowers/plans/2026-08-26-TS视频与纯音频统一播放实施计划.md`](./docs/superpowers/plans/2026-08-26-TS视频与纯音频统一播放实施计划.md)。播放池已扩展为视频/纯音频混排，增加 `.mp3/.wav/.m4a/.aac/.ogg/.flac`；纯音频忽略 `attached_pic`、最终窗口纯黑且视频处理/PiP 不适用，普通声音/插话/固定话术保留。TS/M2TS 使用应用缓存快速封装或必要 H.264/AAC 转码，并经 `.partial`、FFprobe 校验和原子提交；源文件不修改，失败/取消保留旧池。Rust/UI 自动化已通过，Windows 实机证据完成前不得标记为已发布。

## 1. 文档状态

- 状态：进行中
- 2026-08-31：Windows 测试包继续固定使用测试控制面、`tauri.test.conf.json`、`GpAutoLive Test` 应用标识、独立 `target-test-package`/`package-test` 目录和 `--no-sign`，但 Tauri/Rust 改为从 `target-test-package/release` 构建与归档，不再传入 `--debug`。这样测试 EXE 使用 Release GUI 子系统，启动时不再附带调试控制台窗口；该变化不等于正式签名、正式供应晋级或生产配置放行，并覆盖本文此前“独立 debug target”的当前构建口径。
- 2026-08-30：当前优先级收窄为“功能与测试打包环境可用”。GPU 周期维持已接通的 `79/83`，两项颜色和两项历史参数继续 fail-closed，不为凑数进入生产；CPU4 仍严格四项。PortAudio 可听 PTS 在视频读取时按快照年龄与实际播放速率外推，乱序同步观测在身份切换前被拒绝；未改变普通声音 DSP、候选、插话、固定话术或混音。测试构建使用独立 debug target、`GpAutoLive Test` 应用标识和 `package-test` 输出，NSIS、portable ZIP、portable/隔离安装启动均通过，并固定连接 `http://101.96.208.132:9090`。本批没有播放用户媒体、没有 4K、30 分钟长稳、完整显卡矩阵或正式发布准入，后续由用户使用指定视频和音频做功能复验。
- 2026-08-29：完成重复 `get time-pos` 请求 12 超时的专用视频宿主代码修复和代码总监整改，待用户开发端实测。`final-effect` 不再把 WebView 顶层 HWND 直接交给 mpv；新增最小 Windows child HWND 封装，所有创建/resize/销毁在 Tauri 主事件线程执行，Original/GPU/CPU4 只消费经过存活、父子关系、可见性和非零客户区校验的句柄。窗口返回严格 `video_host_ready`，前端未就绪不提交 `start_playback`。mpv 固定暂停启动，IPC/VO/首帧时间线握手后才恢复权威播放状态；未放宽 `5s/500ms` 超时。复核后又禁止已经超时的排队主线程任务迟到改变宿主状态，并补齐新 crate 的许可证与 unsafe 隔离元数据。Rust lib `408/408`、命令 `83/83`、宿主/窗口契约 `7/7`、前端视频运行时 `28/28`、就绪契约 `2/2`、TypeScript 与全目标检查通过。本批不启动开发端、不运行媒体、不修改声音功能、不实施 4K，不能据此宣称无卡顿验收完成。
- 2026-08-29：按用户指定文件完成一次 Phase 6 桌面基础播放复验。`D:\xz\8d020eb133350a74bbc4daec1f33bbc1.mp4` 经 FFprobe 确认为 HEVC Main `720×1280@30fps`、AAC LC 单声道 `44100Hz`、时长 `72.3s`；最终效果窗口观察到连续运动画面，系统中只有一个绑定目标 HWND、读取该精确源路径的受管 mpv，关闭应用后 mpv 与 Tauri 开发进程无残留。报告为 `artifacts/mpv-phase6-native-surface-gate-20260829-v2.json`。该结果只勾选 Original 基础原生表面与退出清理，不覆盖 EOF/重开、GPU83、GPU→CPU4、PortAudio 音画同步、1080p60 P99 或长稳；后续不重复播放该文件。
- 2026-08-29：Phase 6I/Phase 6 原生表面生命周期非媒体接线已收口。每个活动 mpv 进程都记录实际绑定的 final-effect HWND，GPU/CPU4/Original 重用必须精确匹配，换窗时回收旧进程并重建 `--wid`。关窗改为 suspend 并保留当前单向降级 floor，不写 stop tombstone；同一 generation 可在重开后绑定新 HWND。`CloseRequested/Destroyed` 每窗口实例最多执行一次既有清理，命令层不再重复触发。这只是纯状态/所有权门禁，未运行 mpv、FFmpeg、FFprobe 或视频，未改动音频处理语义，未实施 4K；真实原生表面、黑屏、EOF 和重开恢复仍待明确授权的 1080p 实机门禁。
- 2026-08-29：Phase 6I/Phase 7 发布资源门禁继续收口。Windows release/custom 固定九项资源白名单，Node 准备门与 Rust 直构建门同时锁定内部清单精确对象、固定值和文件哈希，额外组件/文件声明也 fail-closed；随包 D3DCompiler 回退因文件身份不符且 Windows 10/11 已有 System32 D3DCompiler 47 而移除。固定构建源码审计确认 Git 版 mpv 声明 25 个直接依赖，FFmpeg 还有更大传递依赖集合；多数依赖使用默认/可移动分支，`mpv-packaging` 未固定提交，libplacebo 带 `dirty`，资产没有最终链接清单、SBOM、完整提交集合或对应源码包。仓库已补 Copyright、GPL/LGPL、FFmpeg 完整提交引用和扩展 SOURCE，但不伪造 `THIRD-PARTY-NOTICES.md`；内部清单新增三项审核事实，当前为 `blocked/false/false`，只有审核状态批准、对应源码完整、第三方通知完成三者同时成立才允许准备和 release/custom 构建。正式发布须取得供应方完整对应源码包，或替换为本地/CI 完全固定输入的可复现构建。该批未运行媒体、未修改任何音频功能、未实施 4K。
- 2026-08-29：Phase 6I/Phase 7A 可复现构建边界冻结为最小视频专用 mpv：保留 cplayer、本地文件、D3D11/D3D11VA、Vulkan、libplacebo、shaderc、SPIRV-Cross 和必要色彩链；`libmpv=false`，生产继续固定 `--audio=no`，WASAPI/OpenAL/SDL 音频输出及网络、Lua/JavaScript、DVD/Bluray、归档、VapourSynth、gamepad、Sixel、字幕扩展不进入依赖闭包，不修改 PortAudio 或任何声音功能。本机当前已有 Rust/Git/Python 和 Windows SDK，但缺 Clang/LLVM、CMake、Meson、Ninja、NASM、VS 2022，WSL 没有 Linux 发行版，因此没有开始编译或下载。优先使用用户确认后的本机 Docker Desktop、摘要固定交叉构建镜像和断网构建阶段；输出必须含完整依赖锁、SBOM、许可证/版权、补丁、日志和对应源码归档，禁止服务器构建。
- 2026-08-29：Phase 7A-1 可复现构建输入锁和静态门禁完成。`desktop/third_party/mpv/reproducible-build-lock.json` 区分 Git 完整提交与不可移动的 HTTPS 归档，固定 Windows MSVC 目标、源码/补丁/工具链缓存、构建镜像摘要、Windows SDK/UCRT、确定性环境、最小视频能力、FFmpeg 禁网、PE 动态依赖允许列表和必需输出角色；缓存清单必须与所有输入双向相等。代码总监复核后删除锁内自证字段，将结果改名为派生的 `blocked/metadata_complete`，并补上 Meson/FFmpeg 固定允许集合，重复、相反开关或未知参数全部拒绝。当前锁派生为 `blocked`，完整传递依赖、构建器/脚本和确定性参数仍待本机构建阶段补齐；该状态不能通过 `--require-metadata-complete`。`metadata_complete` 也不证明输入文件或供应链完整，真实文件复算和语义闭包仍由后续门禁负责。此批只做静态契约与测试，没有启动 Docker、下载、编译或媒体测试，也没有修改 PortAudio、音频候选、混音、插话、固定话术或音频参数。
- 2026-08-29：Phase 7A-1-2 构建输入字节匹配门禁完成。每个源码、工具链和补丁缓存必须记录正整数 `size_bytes` 与 SHA-256，固定 `build-inputs/` 文件集合和 `cache_inventory` 双向相等且总声明量不超过 32 GiB；Dockerfile、脚本、cross file、配置及被调用模块统一进入 `recipe_inventory`，与固定 `build/` 实际文件双向相等。有界遍历拒绝符号链接/目录联接越界、夹带和缺失，通过同一文件句柄复核大小、哈希及读取期间身份。元数据未完成时不触碰不存在缓存，全部通过也只输出 `input_bytes_match_lock/admitted=false`，不把同锁自洽误写成可信来源。当前真实缓存尚未物化，仍为 `blocked`；本批未启动 Docker、下载、编译或媒体，也未修改任何音频功能。
- 2026-08-29：Phase 7A-2-1 构建后报告结构与文件证据门禁完成。独立校验器固定输入锁字节哈希和 `phase7a_supply_candidate` 作用域，逐项核对配置、构建环境、动态 DLL、12 个产物角色与格式，并把 `build-evidence/` 文件集合与报告双向比较后流式复算大小和 SHA-256；夹带、符号链接/目录联接越界、路径越界、超限锁/报告、篡改或报告自带 admitted 字段均拒绝。文件层通过只标记 `file_evidence_verified`，语义交叉验证前固定 `admitted=false`。真实报告和证据目录尚未生成，默认状态为 `blocked/reportPresent=false`；依赖锁、Meson introspection、PE 导入表和两种 SBOM 的内容级交叉验证留在 Phase 7A-2-2。该批未启动 Docker、未下载/编译、未运行媒体，也未修改任何音频功能。
- 2026-08-29：用户已授权本机 Phase 7A Docker、固定镜像下载和长时间构建。Docker Desktop 已恢复，摘要固定的官方 LLVM Ubuntu 24.04 镜像已拉取；镜像内 Clang/clang-cl/lld-link、CMake、Ninja 和 Python 可用，Meson、NASM、pkg-config、llvm-rc/llvm-lib 仍须离线锁定。mpv、FFmpeg、libplacebo 三个固定提交归档已缓存并复算 SHA-256。代码总监审计确认 MSVC CRT/STL 不能隐含在 Windows SDK/UCRT 中，锁契约新增独立强制 `msvc-toolset` 输入并通过相关静态测试。当前仍在物化 shaderc/Vulkan 完整传递源码、MSVC sysroot、FFmpeg 静态能力闭包、PE 允许列表和断网实编，Phase 7A/7B 继续未完成。
- 2026-08-29：Phase 7A-2-2 已完成一次 locked cold-build 供应候选。固定锁 SHA-256 为 `5ee096c7f4da718cc8b74f2006d23ef39820e0ead92266bc41da1e69dad09135`；容器 `autolive-phase7a-5ee096c7f4da-65761fadfff7` 以 `0:0`、`--network none`、只读根文件系统和本轮唯一 local `volume-nocopy` `/work` 正常退出（ExitCode `0`），输入只读 bind、输出可写 bind。15 项证据覆盖三项 PE 运行产物、依赖锁/构建参数、完整 Meson introspection、PE 导入、CycloneDX/SPDX、许可证/版权、四项补丁、构建日志和对应源码；代码总监整改后的独立 verifier 还逐组件证明对应源码完整包含锁定原始源码、按序补丁和七个显式 vendoring 目标，并锁定 Docker 构建上下文、镜像摘要、entrypoint/command 与挂载事实，最终返回 `admitted=true`、`semanticEvidenceVerified=true`、claim `one_locked_cold_build_candidate`，随后事务发布。该勾选不代表 bit-for-bit 可复现、Phase 7B 法律/发布准入、正式发布或 1080p 实机验收；所有 Docker 容器、volume、镜像及原输出均保留，本批没有运行媒体或修改音频功能。此前同日条目中的 `blocked` 是阶段时点记录，现以本条为 Phase 7A 权威状态。
- 2026-08-29：Phase 7B 首轮候选矩阵失败且未晋级资源。根因证据为 FFmpeg 未启用 GPL 导致 CPU4 `eq` 缺失、`Enabled hwaccels` 为空导致 D3D11 请求实际软件解码，以及 AMD 驱动对 libplacebo 扩展 surface capabilities 查询返回 `VK_ERROR_UNKNOWN`。整改配方显式启用 GPL、D3D11VA 和 AV1/H.264/HEVC/MPEG-2/VC-1/VP9/WMV3 的两套 D3D11VA hwaccel，FFmpeg 许可证身份固定为 `GPL-2.0-or-later`，并增加只对该特定 Vulkan 错误使用基础 surface 查询的 libplacebo 补丁。`c51c5749...` 构建因许可证身份不一致主动中止；`68ff115a...` 构建在约 37 分钟时因宿主 Docker/WSL 引擎管道消失以 Exit `255` 中断，两次失败容器与 volume 都保留。当前锁 `cff165702cadadb3bcb220c90466e46933a40f47796b4cbd0c08fe4d352a5352` 把构建并发固定为 `2`，容器内存与 memory-swap 固定为 `4 GiB`；元数据和 26 项输入字节门禁通过，新的全新卷断网冷构建正在执行。Phase 7B 技术矩阵已收紧为 GPU/CPU4 都必须提供全屏精确 `1920×1080` OSD 表面证据；新候选通过前不再执行媒体门禁，也不替换开发或发布资源。
- 2026-08-29：Phase 3A v6 只执行一次整改后真实门禁并通过。D3D11/Vulkan 均使用精确 `1920×1080` 源、解码输出和实际 OSD 表面；61 个产品场景、11 个 runtime 场景、默认中性、同 PID、PTS 连续、零新增丢帧和更新期无重复编译全部通过。D3D11 GPU P99 `4.26ms`、更新 P99 `17.42ms`，Vulkan分别为 `0.89ms`、`17.09ms`。报告生成时生产仍为 `20/83`，只有生产 shader、Rust 映射、固定哈希和 Phase 3C 剩余历史字段原子提升及静态复核完成后才可改写为生产 `61/83`；4K 未实施。
- 2026-08-29：Phase 3A v6 后的生产提升已完成。生产 shader、Rust capability、固定哈希和 Phase 3C 审计身份作为同一变更收口，当前生产为 `61/83`，归一化 shader SHA-256 为 `3e08e25507a063d405807184f4eaf51e5da77e03df72acba322e4a44e01b6f9f`；Phase 1/3A/3C 静态契约 `76/76`，Phase 3C 仍为 `not_admitted`。剩余 19 项调度、2 项颜色管理和 1 项跨帧历史继续不可用，不能宣称完整 GPU83；候选文件未进入发布包，4K 未实施。
- 2026-08-29：Phase 5 生产控制接线已完成并经代码总监整改。PortAudio 输出线程以 25Hz 发布 generation/audio epoch/source/loop 隔离的可听时钟，包含源锚点、回调累计帧、实际采样率、输出延迟、播放状态和 `audio.playback_speed`；视频 runtime 只消费同身份且不超过 160ms 的快照，严格读取 mpv pause/seeking/cache 状态。同步控制保持 `20/60/80ms` 与 `±1%/±2%`，最终 speed 为“实际音频速率 × 视频调度速度 × 同步修正”，由 `0.25–4.0` 值类型限制；恢复硬 seek 只由视频控制器创建。普通声音 DSP、N/N+1、插话、固定话术和混音语义未改变。Rust 库 `404/404`、观察/同步/降级契约和 `cargo check --all-targets` 已通过；真实联合媒体与 30 分钟长稳仍待验收。
- 2026-08-29：Phase 6I/Phase 2 补齐 Windows Job Object 退出兜底。`ManagedMpvProcess` 通过 Windows-only、精确锁定的 `win32job 2.0.3` 在 mpv 启动前创建 `KILL_ON_JOB_CLOSE` Job、启动后立即绑定；创建失败不启动，绑定失败执行进程树/单进程终止和 `wait` 后 fail-closed。正常 IPC quit 仍先有界等待，超时关闭 Job 后保留 `taskkill/Child::kill` 末级兜底，正常退出也关闭 Job 清理遗留后代；项目没有放宽 `unsafe_code = "forbid"`。纯进程树夹具通过绑定后握手和 PID+启动时间验证父、后代随 Job 关闭一起退出，RAII 覆盖断言失败清理；`cargo fmt --check`、`cargo check --all-targets`、进程树精确测试 `1/1` 和后端测试 `22/22` 通过，夹具目录、测试进程与 mpv/FFmpeg 进程收尾均为 0。未运行媒体，音频与生产 shader 未修改；真实 mpv 崩溃和安装包退出仍待阶段实机门禁。
- 2026-08-29：Phase 6I/Phase 2–3B 补齐用户 seek/循环的 mpv 新 epoch 对齐。`SyncRealtimeVideoRendererRequestDto.position_ms` 现在进入唯一 runtime actor；相同 generation/clock epoch/loop 的普通同步不 seek，只有 clock epoch 前进或 loop 单步前进发送一次 `seek absolute+exact`。暂停+seek 固定 `pause → seek`，恢复+seek 固定 `seek → unpause`；旧 generation、倒退 epoch/loop、跨多轮和已知源时长越界位置均 fail-closed。runtime 测试 `16/16`、独立契约 `4/4`、命令接线契约 `1/1` 与全目标检查通过；未运行媒体，普通周期硬 seek 仍明确禁止，真实呈现待阶段实机门禁。
- 2026-08-28：桌面内部开发版与测试包连接已部署的公网测试控制面 `http://101.96.208.132:9090`。`pnpm tauri:dev` 默认注入该地址与 `VITE_CONTROL_PLANE_ENV=test` 并加载测试 Tauri 配置，`tauri:build:test` 固定使用同一测试地址；开发启动允许环境变量覆盖默认值，但覆盖不能绕过客户端 URL 门禁或所选 Tauri capability 的 origin 白名单。该公网明文 HTTP 地址仅供内部开发/测试，不是生产服务；正式构建仍须显式注入 HTTPS 地址并使用生产 capability。
- 2026-08-28：插话目录受支持媒体文件上限从 `256` 调整为 `1000`，第 `1001` 个仍整体拒绝；随机插话抽屉状态摘要展示已保存快照的受支持文件数量与上限。不改扫描白名单、路径规范化、`4096` 字节单路径或 `512 KiB` 累计路径边界，不触发全量桌面打包。
- 2026-08-28（当前最高优先级）：进入 Phase 6I。先完成随包 mpv 的 D3D11/Vulkan、完整 libplacebo shader、动态 GPU83 参数、libavfilter CPU4 和 PortAudio 音画同步技术门禁，再依次接通常驻播放器、GPU83、CPU4/Original、前端切换和 Windows 多硬件长稳。全部验收完成前不得宣称 GPU83、无卡顿或音画同步已交付。
- 2026-08-28：Phase 6I 当前最高画面范围收窄为 1080p，4K 延后。最终代码总监报告覆盖 D3D11/Vulkan 的 1080p 24/25/30/50/60fps、每路径 200 次同 PID 动态参数更新、`vo-passes` 帧预算、逐帧丢帧时间线，以及 CPU4 1080p60 的 160 次四字段快照；全部通过，丢帧增量为零。报告为 `artifacts/mpv-phase1-code-director-audit-20260828-v8-1080p.json`。发布法律材料、跨硬件运行时性能降级和长稳仍未完成，当前结论不得扩张到所有硬件或 4K。
- 2026-08-28：Phase 6I 下一版本执行顺序冻结为 `GPU83 Phase 3A 单帧/同帧合成 → 3B mpv PTS 调度 → 3C 历史帧门禁 → Phase 4 生产单向降级 → Phase 5 PortAudio/mpv 同步 → 联合长稳`。不并行改三条主链；音频仅接只读可听时钟与同步 epoch，不改普通声音 DSP、候选、插话、固定话术或混音。正常周期不得切后端；设备丢失按权威 PTS 有界重建，不承诺硬件失效时绝对零停顿。
- 2026-08-28：Phase 6I/3A 候选继续实施并完成本批代码总监整改。现有 20 项基线候选修复 hue 矩阵、饱和度 luma 与无几何变化时裁剪回混；独立 fragment 候选已覆盖图像修复、UV 直定位最多十槽且每像素最多一次 SDF 的抽象几何脸、按归一化 X/Y 区域执行固定九点二维 tent 采样并保留 alpha 的局部模糊、沿用旧 FFmpeg `0%=2px` 硬填充且非零值映射 `2–16px` 有界宽度和百分比混合的边缘填充、直接在浮点 RGB 上执行红/蓝反向偏移且保持绿色/alpha 的通道偏移、目标/核心频率与 12 段固定频带组成的 21 字段共享空间调制，以及随机图形 4 项、共享挂件偏移和同帧 PIP 4 项组成的 9 字段叠加 pass。同帧随机图形使用 8×4 直接槽位、每像素固定 O(1)；PIP 修复旋转边界后只在命中区域增加一次同帧采样。短门控候选使用动态 `WHEN`，空间调制因固定 libplacebo 无法解析超长表达式改为固定预编译 pass 内部中性早退。PIP 时间轴解锁仍由 Phase 3C 实现，颜色管理契约不完整时继续不可用。候选只位于 `desktop/tools/shader-candidates/` 且不打包，生产仍为 `20/83`。当前 fail-closed 契约为 9 文件、61 项产品参数加 11 项 runtime 选项：55 个非布尔产品字段使用完整 72 键 `baseline/low/active` 三档，6 个布尔开关使用 `baseline/active` 二态；low 可证明广泛的一码值低感知响应，active 仍执行明显差异阈值，频率与动态均衡阈值不要求响应幅度伪单调。11 个 runtime 与 1 个全激活组合使用两档，共 73 个逻辑场景；另加 1 个 seed/PIP 预期相同隔离场景，共 203 张同 PTS 场景截图。隐藏门禁每个后端只启动一个零 shader mpv，会话内先截取无 shader 参考帧，动态追加固定 9 文件后以 72 键全激活快照截取独立预热帧，确保动态 `WHEN` pass 的首次编译/渲染位于日志基线之前，再恢复全默认并截取中性对照帧；三张 setup 截图保持同 PTS，只有无 shader/全默认两张进入中性分析。隐藏窗口固定 geometry，取消默认 90% 自动缩小、边框、视频缩放、HiDPI 窗口缩放和自动窗口重设；实际 OSD 表面仍须精确 `1920×1080`。P99 只在全默认和同一份 72 键全激活快照间交替，并要求非空同 PID和至少 100 份相邻不重复的 `vo-passes` 统计快照；官方契约允许逐帧改变 pass 数量，且每个 pass 的 `count` 是其自身样本数，因此门禁只比较有序 `index/desc/count/samples` 快照身份。VO/decoder 丢帧增长或计数重置直接失败，失败 catch 同时保留有界 stdout/stderr。静态 Node 门禁为 `70/70`；非精确目标表面上原本通过的计时证据降为 `unverified`，原本 `failed/unverified` 状态保持不变；Phase 1/3A 只有在最终状态通过且全部 GPU 源与实际表面分辨率匹配时才填写最高 `1920×1080`，否则为 `null`。一次性 v3 隐藏报告 `artifacts/mpv-phase3a-candidates-20260828-v3.json` 状态为失败：D3D11 的源和解码输出是 `1920×1080`，旧窗口实际表面为 `1777×1000`，所以该表面的 P99 `8.779ms`、118 个有效样本、同 PID、零新增丢帧、无重复编译和默认中性通过不能充当 1080p 证据；74 个场景通过 51 个、失败 23 个，Vulkan/WinVK 未建立 VO。报告后已静态修复 19 项门禁假阴性与 4 项真实弱响应：图像修复移除额外 `0.35` 衰减但保持固定五采样，`wave_level` 和 `runtime_frame_inner_active` 从 1 提升为有界 4 码值；整改后没有再次运行视频。下一次复验仍需明确授权，双后端全部通过前不得提升 production capability、最高分辨率或无卡顿结论。
- 2026-08-28：Phase 6I/3A 用户单次授权的 v4 真实门禁已执行且失败，报告为 `artifacts/mpv-phase3a-candidates-20260828-v4.json`。D3D11 与 Vulkan/WinVK 本轮均成功建立 `gpu-next`，保持同 PID、默认中性、无重复 shader 编译；但 `window-minimized=yes` 将两后端实际表面压成 `160×28`，因此全部 P99 只作诊断，最高验证分辨率仍为空。D3D11 丢帧计数发生一次重置并继续 fail-closed；剩余场景为 D3D11 的 frame-inner，以及 Vulkan 的 overlay-offset 和 frame-inner。静态整改已移除最小化参数及会按工作区缩小固定 `geometry` 的 `autofit-larger=100%x100%`，保留 `focus-on=never + show-in-taskbar=no` 的非抢焦点受控窗口；overlay 位移改为非强度单调响应门禁，frame-inner 提升为固定 16 码值。整改后的静态门禁通过，尚未再次运行视频；候选不打包，生产 shader、`20/83` capability、CPU4 和音频均未修改。
- 2026-08-29：Phase 6I/3A 计数重置与实机前静态门禁已按根因收口，旧估算预算已从当前上下文删除；门禁只生成一个固定 `15s/900 帧` 的 `1920×1080@60` 样本。setup/probe 后，每次更新同时记录 `time-pos/full` 与 Node 单调时钟，要求 PTS 严格推进、无 loop/reset、相对单调时钟新增停顿不超过一个 60fps 帧，且末尾实际 PTS 必须证明样本仍剩至少 `2s`，不再用估算时延宣称余量。61 项产品场景固定同上下文单变量，low 与 active 必须可区分；frame-inner 的 `2×2/8×8` 掩码经 `1920×1080 → 320×180` area 纯数学验证仍有 `56.25%` 像素跨两码值阈值。真实门禁逐项核对 Rust 20 个 `AVAILABLE` shader option 与生产 shader，等量交换 capability 也 fail-closed；7 文件静态集 `81/81`。本项未运行 mpv、FFmpeg、FFprobe、视频或音频，未实施 4K；候选未生产提升，当前仍为 `20/83`，完整 1080p60 双后端实机门禁仍需新的明确授权。
- 2026-08-29：Phase 3A v5 真实 `1920×1080@60fps` 双后端门禁已按新授权执行但失败。D3D11/Vulkan 同 PID、零新增丢帧，默认中性、无重复编译和 PTS 连续性通过，诊断 GPU P99 分别为 `6.322ms/0.787ms`；Windows 工作区把普通无边框客户区限制为 `1834×1032`，且 6 项反向强度/周期/离散/低档量化场景未通过，所以最高通过分辨率仍为空、生产保持 `20/83`。已静态改为 Phase 3A 专用全屏客户区，并修正 PIP 透明度、局部模糊、核心频率、grain、动态阈值和空间维度的可测门禁语义，定向测试 `65/65`；整改后尚未再次运行媒体，报告为 [`artifacts/mpv-phase3a-candidates-20260829-v5-1080p.json`](./artifacts/mpv-phase3a-candidates-20260829-v5-1080p.json)，4K 不在范围。
- 2026-08-28：Phase 6I/3A 真实门禁增加防重复硬门禁。每次执行必须显式携带 `--confirm-real-media-gate` 和新的 `--report` 路径；缺任一项在样本生成前以退出码 2 拒绝。参数校验后、调用 `runPhase3aCandidateGate` 前以 Node `open('wx')` 原子预占新报告文件，同名路径会在任何样本生成或媒体启动前拒绝，完成后由同一句柄写入结果。无媒体拒绝路径已验证不启动 mpv/FFmpeg，完整静态测试 `109/109`。
- 2026-08-28：Phase 6I/3A 历史证据纠偏：v2 总状态为 `failed`，但旧版报告错误保留 `maximumValidatedResolution=1920x1080`；该字段明确作废，不得引用为 1080p 通过证据。v3/v4 及当前门禁在失败时均为 `null`，既有报告保持不可覆盖。
- 2026-08-28：Phase 6I/3B 第一至五批完成纯 Rust 媒体 PTS/epoch 调度核心、受限 mpv 观察/速度原语、唯一运行时 actor、11 项 runtime shader 候选、计划级随机图形 seed 及受 capability 保护的有序命令批次。调度器以播放代次、sequence、seed、字段和源帧索引确定性输出帧率/基础视频速度、帧内/帧间/概率门控、切片、局部模糊间隔、高光脉冲、PIP 像素扰动、异步旋转及平滑状态；首次与新 epoch 必须携带启动/换源/seek/循环边界，暂停 PTS 偷跑和旧 generation/epoch/sequence 均 fail-closed 且不污染状态，同一源帧的 PIP 偏移与观察采样频率无关，同 PTS 的新参数序列立即应用并阻止旧序列回写。调度输入已与音频参数解耦，内部/外部为 `11/11`、`9/9`。mpv 原语只允许读取 `time-pos/full`、`estimated-vf-fps` 和向 `speed` 写入有限 `0.5–1.5`，一次观察只有两个有 deadline 的持久 IPC 请求；backend `18/18`、外部契约 `6/6`。第三批由容量 8、空闲 `40ms` 至多一次 tick 的 Rust actor 独占 mpv、IPC、调度器与计划状态，逐条观察命令 deadline 为 `15ms`，epoch 内冻结首次有效 FPS、相同速度不重复写，连续 3 次观察/速度失败才单向降级；React 透传已有 `clock_epoch/loop_index`，Rust 拒绝旧值、倒退值和跨越多轮循环。第四、五批把 11 项 runtime 选项按 scheduler 7、local blur 1、同帧叠加 3 本地声明到未打包候选；默认全 0，最多固定三次当前帧采样，不用片段随机数，不读墙钟/PTS/历史/音频、不含循环并保持 alpha。随机图形 seed 从调用方计划 seed、`session_id`、`playback_generation`、`sequence` 和独立字段 ID 派生为 `0..=16777215` 的精确整数，同一计划跨帧稳定；候选拆成高低 12 位后只参与随机图形六个固定 hash，不进入 PIP。Rust 对启用调度组固定生成全部 11 键，关闭门显式写 0；actor 按完整 shader 快照后最终 speed 的顺序各发送至多一次并去重，默认参数保持原生产快照逐字节不变。runtime 再次拒绝含未接入活动字段的计划，因此当前 capability 下动态批次不可达，生产仍只写中性 `1.0`。该批次当时尚未完成 GPU→CPU4 会话切换；现由下一条 2026-08-29 Phase 4 代码接线记录覆盖。Phase 3A 候选实机晋级和 Phase 5 PortAudio 闭环仍未完成；生产仍为 `20/83`。
- 2026-08-29：Phase 6I/Phase 4 完成生产代码接线与代码总监整改。单一 runtime actor 现按 `Gpu(D3D11) → Gpu(Vulkan) → Cpu4 → Original` 会话内单向降级；prepare/Original 在释放播放转换锁前签发 operation lease，revision、最新 generation、stop tombstone 与权威 cursor 在同一互斥状态内原子更新，stop/reserve Barrier 竞争、晚到旧 cursor 和混合 cursor 均有负向测试。处理关闭时主动进入 Original 不消耗失败降级 floor，真正失败到 Original 才成为同代次终点；后续同代次重新开启处理可回到既有 floor。prepare 的真实分流统一使用可测试的 `gpu_prepare_disposition`；下一级 GPU 已启动时不会沿用旧进程退出结果再次重启。每次转换先保留权威 PTS、回收唯一旧进程再启动下一模式，状态公开完整降级轨迹、四维身份、转换时间与恢复 PTS。CPU4 任一 activation 都固定报告 83 项、4 项支持、79 项未执行，无计划路径也不发布空报告。前端修复 Original backend epoch 恢复，确定性错误立即停止，进程/IPC 暂态错误按 `1/2/4/8/15s` 有界退避。`vo-passes` 只表示 GPU pass P99，同时接入 `frame-drop-count`、`decoder-frame-drop-count`、`mistimed-frame-count`、`vo-delayed-frame-count`，连续 3 个健康窗口异常才降级；它不等于真实呈现间隔 P99。Rust/前端非媒体专项通过，代码总监最终复核为 P0/P1/P2 均 0；本批没有启动视频、没有修改音频、没有实施 4K。Phase 4 实机恢复 `≤80ms`、真实呈现 P99、无黑屏/双进程和 30 分钟长稳仍待一次明确授权的 1080p 门禁；生产 GPU capability 仍为 `20/83`。Phase 5 仅完成纯控制律 `±1%/±2%` 收紧和唯一速度合成入口；既有 PortAudio 快照缺少 generation/audio epoch/source/loop/单调采样身份且约 4Hz，在“任何音频功能不能改”的当前约束下生产输入继续 fail-closed，真实同步未开始。
- 2026-08-27：普通声音 Worker 取消后最多等待 `3s`，超时保留槽位并拒绝同领域重复 Worker；当前版本同时从 Tauri 注册与窗口权限移除 speech-to-speech、audio variant candidate 和 `direct_model_chat`。普通声音、插话与固定话术权限保持。
- 2026-08-27：修复音视频周期调度恢复。实时视频 commit 与外层 apply 任意交错时，完成路径必须主动排空 latest-wins pending，不能由 React busy 展示状态承担互斥或唤醒。声音候选最终失败后保持当前 N，按 `1/2/4/8/15s` 最多五次自动重建新候选，连续第六次失败才等待人工重试；busy、暂停、旧代次不消耗预算，成功/人工重试/停止/换源/身份变化清零。播放中修改声音预设、多轨或周期范围不得清除 processing/ready N+1 的前端 owner，只替换尚未准备的 N+2；行为测试必须使用可控时钟和确定同步点，不再只检查源码结构。
- 2026-08-27：修复容器时长晚于音轨终点时普通声音候选被 `apad` 补零并误报静音。导入探测新增相对容器时间轴的有效音轨区间；React 计划器和 Rust 命令在 Worker 启动前双重跳过完全无交集窗口，部分相交保留，多项池等待换源、单项池按源时长周期绕回。稳定跳过不写失败状态、不显示错误、不进入退避；缺少可靠元数据时继续旧校验链。
- 2026-08-27：修复合法多预设参数组合偶发生成完整时长数字静音。保留单源 `asplit` 为 `1–4` 条效果支路并等权合并的路线，在源入口、支路和辅助输入进入各级 `amix` 前统一连续零基时间戳，有限主声与无限噪声/循环环境声按最短输入结束；末尾补齐不再承担时间轴纠偏，静音检测和周期级有限退避继续作为提交门禁。
- 版本：V2.99
- 更新时间：2026-08-27
- 适用范围：Go 后端、React 管理系统、Rust/Tauri 桌面客户端、本地媒体 Worker
- 2026-08-26：登录安全主体实现、本地自动化和 Docker PostgreSQL 16 真库验证已完成，实施记录见 [`docs/superpowers/plans/2026-08-26-服务端与桌面端登录安全加固实施计划.md`](./docs/superpowers/plans/2026-08-26-服务端与桌面端登录安全加固实施计划.md)。本批次不开发注册；已实现 `admin/desktop` audience 隔离、15 分钟 Access、30 天 Refresh family 重放检测、按 Refresh 注销、防枚举/持久化限流、Argon2id 渐进迁移、Rust 独占 Refresh、取消保存密码和 Tauri 窗口权限隔离。Windows Credential Manager、正式 HTTPS/DNS/TLS、多主机、Race 和确认后的未使用代码清理待完成。
- 2026-08-26：TS/M2TS 稳定播放与纯音频统一媒体池代码及自动化已完成，范围和验收以独立计划为准；Windows WebView、硬件与真实媒体样本验收仍按实际进度更新，不提前声明发布完成。
- 2026-08-26：修复 WebView 插话未压低普通声音处理后主轨的问题。最终效果窗口把视频原声、处理后 A/B 音轨和兼容候选主音轨统一汇入共享 `mainProgramGain`，插话绕过该节点；开始/结束按现有 attack/release 配置执行线性压低和恢复。PortAudio 样本域 duck 保持不变，新增前端回归契约；Windows 实机听感仍待验收。
- 2026-08-26：插话新配置默认将主轨 duck 深度设为 `-60dB`，WebView 与 PortAudio 都把该下限转换为增益 `0`，实现插话期间默认真正静音主节目；已有显式保存值不迁移。仅执行相关 UI/Rust 目标测试和类型检查，不触发桌面全量打包。
- 2026-08-26（历史完成事实，已由下一条 2026-08-27 口径覆盖）：主页“插话声音预设”曾增加逐参数值变化反馈，本段实际预设改变后，仅格式化显示值变化的参数行复用 A/B 代次与约 `900ms` 单次高亮；相关 UI 回归和类型检查当时已通过。当前不再执行该动画。
- 2026-08-27（当前实施口径，覆盖全部参数变化高亮历史）：主页所有媒体参数卡和“插话声音预设”参数行改为静态显示，保留逐卡强调色、参数值和状态文字，删除 A/B 闪烁代次、光晕/发光阴影与关键帧。原始诊断采样仍保持最多 `4Hz`，主波形可直接按该节拍绘制；React 诊断摘要最多每秒提交一次。参数面板、分区、单卡和插话参数行必须使用稳定输入与渲染边界，未变化的参数卡不得被诊断消息、播放时钟或其他无关主页状态带动重渲染。上一条 2026-08-26 完成事实保留为历史，不再作为当前验收。
- 2026-08-26：修复主页插话周期事实源与小窗口保存入口。主页“插话声音周期”改为显示已保存的 `interval_min_ms / interval_max_ms`，不再误读声音预设变化周期，半秒值保留小数；随机插话抽屉在高度不超过 `720px` 时于顶部状态概览显示保存按钮，常规高度继续使用 footer。相关 UI 回归与 TypeScript 检查作为本批验收，不触发桌面全量打包。
- 2026-08-27（历史实施入口，视频部分由 Phase 6H 覆盖）：[`N/N+1 音视频临时文件滚动处理实施计划`](./docs/superpowers/plans/2026-08-26-N1-N2音视频临时文件滚动处理实施计划.md) 继续约束声音 A/B 和可复用的进程/缓存生命周期；其中视频 A/B、迟到追赶和视频 N/N+1 文件切换不再作为当前目标。
- 2026-08-25：Windows 测试包门禁收口：构建前只清理受约束的 NSIS bundle 输出目录，不清理 `target/release`；portable 归档必须恰好检测到一个本轮当前 EXE，零个或多个均失败。NSIS 与 portable/ZIP 都固定携带 `ambient/low-level-room-tone.wav` 和 `ambient/LICENSE.txt`；portable 归档缺少任一文件即失败，并且只复制固定白名单，禁止递归带入其他 `ambient` 文件。
- 2026-08-25：修复生产 `snapshot` 读源升级多产品控制面后的历史客户端兼容回归：兼容窗口内登录缺失 product 时默认进入 `autolive`；旧快照中缺失 product 的设备也仅按历史默认解释为 `autolive`，Profile 与设备审计复用同一兼容规则；已认证为 `autolive` 的历史客户端心跳省略 product 时使用会话产品，避免登录直接 400、资料误报 403、失败心跳因审计再次失败变成 503 或旧客户端心跳持续收到 400。切换 normalized 后，失败激活/心跳审计若引用尚未创建的设备，保留 `target_id` 追踪事实但将 `device_id` 外键置空，避免审计外键失败把原业务错误放大成 503；已存在设备仍保留完整外键。`douyin_desktop` 心跳缺失 product、显式跨产品、无效产品和非设备审计目标继续严格拒绝。本修复不恢复或伪造旧激活码归属，不新增数据库迁移。
- 2026-08-25（历史环境声口径，后续由最终澄清收口）：桌面视频和全声音自动变换落地时允许未选择用户素材时解析随包环境声；最终口径明确为随包内置真实环境声音频，并要求与用户素材一样完成验证。其余完整视频快照、22×35 预设、PortAudio/WebView 插话及手工发布门禁事实保持不变。
- 2026-08-25（已被下一条最终澄清覆盖）：上一轮曾要求 `ambient_sound_mix_percent > 0` 时必须由用户选择本地素材并废止随包来源；该要求已撤回。零值不加载、输入验证和失败原声回退要求继续有效。
- 2026-08-25：环境声来源最终澄清为 `ambient_sound_mix_percent = 0` 时不加载第二输入；值大于 `0` 时默认解析随包内置真实环境声，用户本地素材可选且优先覆盖。两种来源均执行路径、格式、可读性和 FFmpeg 解码验证，失败保持原声并报可恢复错误。
- 2026-08-25：修复视频低感知链路的显示与离散放大边界：最终效果窗口只显示 FFmpeg 处理缓存或明确的源视频回退，不再对视频元素叠加 CSS 色彩/模糊/几何伪效果；整数像素位移在自动模式保持稳定方向，滤镜强度和帧概率不得被取整、固定最小档或错误采样放大。自动化契约完成后，原片/处理结果客观帧差与正常观看主观低感知仍是联合发布门禁。
- 2026-08-24：本地 Docker 验收批次：在 Apple Silicon 本机以 `linux/amd64` 构建 `autolive-api:local-20260824-r1` 与 `autolive-admin:local-20260824`，使用本地 PostgreSQL 16 完成 migration 24、normalized backfill 和 cutover preflight；Compose 迁移容器 `Exited(0)`、API/Admin `Healthy`，首页、health、livez、readyz、metrics、登录、管理员授权/RBAC、错误密码和退出登录链路通过，审计 Outbox 无 pending。真实运行发现 PostgreSQL `lib/pq` 在审计 Outbox 派发中复用未关闭的 rows 结果集会返回 `unexpected Parse response 'C'`，已调整为先关闭 rows 再更新状态，并新增真实 PostgreSQL 回归测试；同时补齐激活集成夹具的显式 `autolive` 产品字段。生产服务器、真实 HTTPS/DNS/TLS、镜像签名/回滚和桌面端 Windows 实机仍未在本轮验证。
- 当前阶段：服务端既有阶段继续收口；桌面媒体进入 Phase 6I。桌面导入、播放池和单窗口基线保留，视频迁移到常驻 mpv `gpu-next/libplacebo` GPU83 主链；普通声音继续使用独立 N/N+1/N+2 和 PortAudio。Phase 6F–6H 的视频 A/B、周期文件、MSE 和 5 项 Vulkan 路线均已废止。实时话术幻化本版本不开发。
- 2026-08-22：采用分层双 PRD：[`douyin-desktop 接入 autoLive 多产品控制面`](./tasks/prd-douyin-desktop-control-plane-integration.md) 以四阶段负责 product 硬隔离、Profile 新鲜度、模型认证和固定 OpenAPI/E2E；[`服务端商业化与生产就绪扩展 PRD`](./tasks/prd-server-commercial-production-readiness.md) 扩展为十一阶段，负责产品席位、管理查询/React、支付、密码重置、激活码安全、可信制品、公共配置、错误反馈和生产运维。当前仅完成文档规划，未修改代码、OpenAPI、迁移或部署，不并入当前已完成状态。
- 2026-08-21：服务端密钥接口与桌面控制面 HTTP 联调：临时 memory 控制面实际完成登录→`POST /api/v1/admin/model-pool` 写入带 API Key 的模型账号→创建激活码→设备激活→`POST /api/v1/client/model-leases` 申请租约→释放租约；账号创建响应、账号列表响应和租约响应均确认不含 API Key，`secret_configured=true`、`proxy_mode=direct_lease`、`direct_base_url` 与服务端配置一致。该联调不代表桌面端已完成供应商模型直连：当前租约契约只返回受限地址和生命周期，不返回 API Key 或短期供应商凭证；本版本仍不实现实时话术/LLM 调用，服务端 `APP_SECRET_ENCRYPTION_KEY` 始终留在 Go 服务端。
- 2026-08-21：桌面端 V2.90 启动认证接线批次：新增稳定设备标识、控制面会话状态机和启动恢复，首次登录/Refresh/退出、激活码绑定与设备状态门禁接入主窗口 HashRouter；未登录路径统一重定向到 `#/login`，已登录访问 `#/login` 返回主页；Refresh Token 仅经 Tauri `keyring` 命令写入系统凭据存储，Access Token 只留在 React 内存；登录后的设备运行信息按 30 秒 Heartbeat 上报，瞬态网络失败只保留每用户/设备一条有界 outbox；生产构建实际加载控制面客户端并强制 `VITE_CONTROL_PLANE_BASE_URL` 使用 HTTPS。补齐 TypeScript、UI 契约、设备标识和会话状态测试；服务端 `APP_SECRET_ENCRYPTION_KEY` 不进入桌面端。Windows 原生安装、真实 HTTPS/DNS/TLS、钥匙串不可用实机提示和长时间心跳/断网恢复仍需发布环境验收。
- 2026-08-21：桌面端测试控制面联调配置：本地开发默认和测试安装包统一使用 `http://101.96.208.132:9090`，新增仅测试使用的 `tauri.test.conf.json`/`test-control-plane` capability、`desktop/ui/.env.example` 和 `tauri:build:test`；控制面客户端继续自动拼接 `/api/v1`，登录路径为 `/api/v1/auth/login`。生产默认 capability、HTTPS 门禁和正式构建命令不变，真实 HTTPS/DNS/TLS 和正式域名替换仍待发布环境验证。
- 2026-08-21：桌面测试包本机可构建：运行资源树生成器在支持的平台自动推断目标三元组，`tauri:build:test` 不再要求手工设置 `AUTOLIVE_TARGET_TRIPLE`；测试包仍固定使用公网测试控制面，正式包配置不变。
- 2026-08-24：桌面登录与账号激活授权批次：`#/login` 只保留账号和密码，登录后自动请求 Go 按创建激活码时绑定的账号和设备额度完成当前设备绑定；客户端不再输入、保存或提交激活码，并尽力清理旧 Keychain 激活码项。服务端增加账号归属和逐设备绑定事实，同设备重登不重复占额，管理员解绑释放额度、禁用保留额度；管理端创建激活码时选择账号并设置 `1～100` 台最多登录设备数。
- 2026-08-23：历史桌面门禁批次曾把账号、密码和可选激活码合并到 `#/login`；该交互已由 2026-08-24 的账号密码登录与自动设备绑定替代。同期播放池增加带文字的常驻导入按钮、空状态主按钮和已有素材替换按钮，三处复用同一探测链路。Windows 10/11 x64 NSIS 改用 WebView2 离线安装器；便携 ZIP 明确只作内部测试，正式发布仍待 Authenticode 证书和干净系统冒烟。
- 2026-08-21：服务端 V2.66 提交结果收口批次：激活码、用户/授权、设备激活/心跳/生命周期、模型租约/用量、审计追加/Outbox、Session 轮换、保留清理、分页读事务和模型账号/Secret 的全部 PostgreSQL `tx.Commit()` 出口统一使用 `postgresCommitError`，Context 取消/截止与 `COMMIT_OUTCOME_UNKNOWN` 分离，HTTP 统一返回 `503/COMMIT_OUTCOME_UNKNOWN`，避免未知结果时误删 staged Secret；新增激活码 SQLMock 提交分类测试。当前源码镜像 `autolive-api:20260821-v266`（digest `sha256:d288c72cd52c0c931a5b2cfd8c760c83e1725c23044529c694edfe46eda71c85`）已在本地 PostgreSQL 16 完成迁移→回填→normalized readyz/metrics→SIGTERM clean shutdown，带 `postgres_integration` 的 store 全量测试通过；真实连接断开/提交后连接丢失、跨存储 Secret 补偿、供应商权威额度、容量告警规则、归档、镜像签名和 HTTPS 线上证据仍待完成。
- 2026-08-21：服务端 BE-006/BE-024/BE-020 故障矩阵批次：新增 PostgreSQL Session 设备冲突和持有 normalized advisory lock 时取消等待的真实集成测试，观察 `pg_locks` 后验证 `context.Canceled` 及业务/会话/幂等/审计零残留；本地 Docker PostgreSQL 16 下 `go test -tags=postgres_integration ./internal/store -count=1` 全量通过。强制连接断开、Commit 未知结果和进程外 Secret 故障仍待完成。
- 2026-08-21：服务端 BE-006/BE-020/BE-024 提交前连接故障批次：新增真实 PostgreSQL `pg_terminate_backend` 集成测试，在 normalized advisory lock 等待已由 `pg_locks` 观测后强制断开等待后端；请求有界返回连接失败且不误判 `COMMIT_OUTCOME_UNKNOWN`，激活码、设备、Session、幂等和审计无残留。本地 Docker PostgreSQL 16 目标故障切片 2/2 通过；提交后连接丢失、Secret 进程外故障和跨存储补偿仍待完成。
- 2026-08-21：服务端 BE-006/BE-020 提交后响应丢失故障批次：新增测试专用 PostgreSQL TCP 代理，在转发 `COMMIT` 的 `CommandComplete` 后丢弃连接，模拟已提交但客户端未收到 `ReadyForQuery`；真实旁路连接确认激活码、设备、Session、幂等和审计 Outbox 已持久化，客户端返回 `COMMIT_OUTCOME_UNKNOWN` 且不执行补偿删除。本地 Docker PostgreSQL 16 目标测试及 Store 全量集成通过；Secret 进程外故障、跨存储补偿和快照退出仍待完成。
- 2026-08-21：服务端 BE-007/Phase 6 HTTP 优雅关闭批次：新增 `TestServerGracefulShutdownWaitsForInFlightRequest`，真实 `http.Server` 在受控在途请求期间不会提前完成 `Shutdown`，释放请求后在预算内结束，监听关闭后新请求失败；既有 Compose/CI readyz→SIGTERM→clean-exit 证据继续保留。数据库、后台任务、指标导出器和关闭超时深层故障矩阵仍待完成。
- 2026-08-21：服务端 BE-015 跨存储 Secret 补偿批次：兼容 Memory/快照轮换在业务状态切换事务中预写退休引用到持久化 `PendingSecretCleanup` 队列，删除成功后移除，删除失败或提交未知时保留；新增 `SecretReferenceCleaner`，按 cutoff、批次和保护引用集合执行参数化有界删除，Memory/SQLMock/真实 PostgreSQL 均验证不会误删当前或新鲜引用。Secret Store 进程外恢复、旧密钥归档和快照退出仍待完成。
- 2026-08-21：服务端 V2.71 验证收口批次：快照回归确认 `PendingSecretCleanup` 队列跨序列化保留且 SecretRef/激活码明文继续脱敏；Go 全量测试、vet、build、govulncheck、PostgreSQL 集成测试和临时 Docker PostgreSQL 清理均完成；当前代码对应 `autolive-api:20260821-v271` 镜像构建成功（image ID `sha256:45cda764b8a80cc51a2133bfbc212a7f43a2ea608a9cdfd1fae8a9322de8f342`）。Race Detector 仍由 Linux/amd64 CI 提供证据，Secret 进程外恢复、旧密钥归档、规范化快照退出和生产 HTTPS 仍是后续任务。
- 2026-08-21：服务端 V2.72 规范化快照退出第一切片：normalized 页面读取、状态事务和保留清理只依赖 `normalized_backfill_state=completed`，不再读取或更新 `control_plane_state`；删除旧快照后的真实 PostgreSQL 16 集成测试通过。当前代码镜像 `autolive-api:20260821-v272` 构建成功（image ID `sha256:51bdff4d8443f6737f869f1a9621b36a02ffa42d65fc644a7e0cdff580e4d592`）。旧快照仍保留为一次性回填输入和显式 snapshot 兼容模式，完整领域 Repository 收口、开关删除和生产切换仍待完成。
- 2026-08-21：服务端 V2.73 模型账号测试领域事务批次：新增 `ModelPoolTestRepository`，将 normalized 连通性测试拆成无网络短事务准备、事务外受限 `/models` 探测和短事务结果提交；测试结果、active/cooldown 状态、幂等记录和可选审计 Outbox 原子提交，幂等重放校验账号归属，缺少领域能力时 fail-closed 不回退快照。新增 SQLMock、服务路由和真实 PostgreSQL 16 回放/cooldown 集成测试；`go test`/`vet`/`build`/`govulncheck` 与 tagged store 全量通过。镜像 `autolive-api:20260821-v273`（image ID `sha256:459beff7beed501c552da8c65e34bed019d923d7ed5dd98c32dec6892954e49b`，约 41.1 MB）构建成功。完整领域 Repository、Secret 进程外恢复、供应商权威额度和生产发布证据仍待完成。
- 2026-08-21：服务端 V2.74 认证读路径领域事务批次：新增 `UserCredentialReader`、`UserReader` 和 `AdminCredentialRepository`；normalized 登录按用户名、Refresh 按用户 ID、管理员初始化/就绪/密码轮换均直接访问规范化 PostgreSQL，密码哈希仅在服务层短暂比对，缺少领域能力时 fail-closed；新增 SQLMock、服务层和真实 PostgreSQL 16 集成测试，覆盖管理员幂等回放。镜像 `autolive-api:20260821-v274`（image ID `sha256:57ab0af6674a0ee5df201fd4ec1271b79f763a47b570272149b6828e81aa9203`，约 41.1 MB）构建成功。完整领域退出、Secret 进程外恢复、供应商权威额度和生产发布证据仍待完成。
- 2026-08-21：服务端 V2.75 规范化设备读取与用户写入 fail-closed 批次：新增 `DeviceReader`/`postgres_device_reader.go`，normalized `GetDevice` 与客户端 `/profile` 直接读取规范化设备/用户表并校验设备归属；用户创建、编辑、禁用、密码重置缺少领域 Repository 时不再回退快照。新增 SQLMock、服务层和真实 PostgreSQL 16 设备读取集成测试；镜像 `autolive-api:20260821-v275`（image ID `sha256:e908ec06b72fb2cdae7cddd0d6539b4930f4d5711b5770a05cc4a4d59b0dc315`，约 41.1 MB）完成迁移→回填→normalized readyz→SIGTERM clean shutdown。完整快照退出、其余领域读路径、Secret 进程外恢复、供应商权威额度和生产发布证据仍待完成。
- 2026-08-21：服务端 V2.76 激活码分页读路径批次：新增 `ActivationPageReader`/`postgres_activation_reader.go`，normalized `GET /api/v1/admin/activation-codes` 使用参数化 `COUNT + LIMIT/OFFSET` 有界 SQL，状态按服务端时钟派生过期结果，响应不含明文激活码；Memory/快照模式保留回退，服务层避免快照 PostgreSQL 误走 normalized-only reader。新增 SQLMock、服务层和真实 PostgreSQL 16 过期/活动/脱敏集成测试；镜像 `autolive-api:20260821-v276`（image ID `sha256:232d5b6160540bc38f0ec2b638ba7670ff39016ecb47e41e4e026773614abddf`，约 41.2 MB）完成迁移→回填→normalized readyz/metrics→SIGTERM clean shutdown。本轮 Go test/vet/build/govulncheck 通过，Windows/386 Race 仍由 Linux/amd64 CI 验证；完整快照退出、Secret 进程外恢复、供应商权威额度、生产 HTTPS、容量告警/归档仍待完成。
- 2026-08-21：服务端 V2.77 用户授权摘要 normalized 读路径批次：新增 `UserAuthorizationSummaryReader`，normalized 用户授权摘要在有界事务内直接聚合规范化 users/devices/model_leases/user_authorization_policies/model_usage_records，不再回读 `control_plane_state`；缺少领域能力时服务层 fail-closed，Memory/快照保留兼容回退。新增 SQLMock、服务层和真实 PostgreSQL 16 集成测试，覆盖策略、设备/租约计数、UTC 日切和无 lease 用量排除；镜像 `autolive-api:20260821-v277`（image ID `sha256:070a68a5e71db04aa9cb93c7d41d3f875e482264d1ad4e5fd6792a22c797cef4`，约 41.2 MB）完成迁移→回填→normalized readyz/metrics→SIGTERM clean shutdown，Go test/vet/build/govulncheck 与 tagged store 全量通过。Windows/386 Race、文档引用脚本（当前环境缺少 bash）、模型池健康调度器剩余完整列表读路径、Secret 进程外恢复/归档、供应商权威额度、容量告警/归档、生产 HTTPS 和镜像签名/回滚仍待完成。
- 2026-08-21：服务端 V2.78 查询/观测/后台读路径批次：模型调用摘要增加 provider/model/user/device/request_id、RFC3339 时间窗口和创建时间排序筛选，Memory/normalized PostgreSQL/HTTP/OpenAPI/管理端 URL 状态一致；新增连接池饱和度比例与阈值指标，Memory 不伪造数据库池指标；模型池健康调度器在 normalized PostgreSQL 使用有界账号分页，不再加载完整快照，缺失分页领域能力时 fail-closed，`ListModelPoolAccounts` normalized 路径改为分段分页并对无进展 fail-fast。新增服务/HTTP/SQLMock/契约回归，管理端与桌面端 typecheck/test/build/api:check 通过；Go test/vet/build/govulncheck、Docker PostgreSQL 16 tagged store 全量通过。镜像 `autolive-api:20260821-v278`（image ID `sha256:c45426386cbc45523258c83965074509800a5fb23f6e9d512f8c13332b83dcf2`，约 41.2 MB）完成迁移→回填→normalized readyz/metrics→SIGTERM clean shutdown。Windows/386 Race、真实规模 `EXPLAIN (ANALYZE, BUFFERS)`、Secret 进程外恢复/归档、供应商权威额度、容量告警规则、生产 HTTPS 和镜像签名/回滚仍待完成。
- 2026-08-21：服务端 V2.79 信息盘点：对照专项长任务、PRD、架构/迁移约束、OpenAPI、backend 入口和本地 Docker PostgreSQL 16 证据，确认基础控制面、normalized 事务第一切片、分页/筛选、审计 Outbox、保留清理、指标、限流、readyz、Go 1.26.6 和 Compose 优雅退出已落地；当前未完成集中在 normalized 正式切换/快照退出、Secret 进程外恢复与旧密钥归档、数据库/后台任务/指标 collector 故障矩阵、真实 HTTPS/DNS/TLS/发布回滚、供应商权威额度与短期凭证、Provider 边界、真实数据量 EXPLAIN/Benchmark、容量告警/归档和全领域审计恢复。另发现健康调度器首批分页后再过滤可能延后可探测账号，需专用候选过滤/游标查询；实时话术、旧媒体任务和服务端模型正文代理继续作为延期项，不进入当前版本。
- 2026-08-21：服务端 V2.80 BE-016 健康候选读取收口：新增 `ModelPoolHealthPageReader`，normalized PostgreSQL 在 SQL 层先过滤 disabled、缺失 Secret、cooldown 和日额度耗尽账号，再按 ID 有界返回候选；服务层缺少专用 Reader 时 fail-closed，避免不可探测账号占满首批分页。新增服务/SQLMock/真实 PostgreSQL 16 集成测试；Go test/vet/build/govulncheck、tagged store、管理端/桌面端 typecheck/test/build/api:check、OpenAPI/Compose/HTTPS/发布门禁通过。镜像 `autolive-api:20260821-v280` digest 为 `sha256:840219d4d4fef8126f6aa4b6bd2a12550c8f60cb300067ed1ba887b6da4a873a`，真实 PostgreSQL 迁移→回填→normalized readyz/metrics→SIGTERM clean shutdown 冒烟通过。Windows/386 Race、normalized 正式切换、Secret 进程外恢复/旧密钥归档、数据库与 collector 故障矩阵、生产 HTTPS/DNS/TLS、供应商权威额度、真实规模性能/容量告警、审计归档、镜像签名/回滚仍待完成。
- 2026-08-21：服务端 V2.81 normalized 切换准入批次：新增只读 `autolive-verify-normalized-cutover`，要求显式确认并校验迁移版本、`normalized_backfill_state=completed`、关键规范化表存在/可读和模型账号 Secret 引用完整性；失败不修改数据库，Dockerfile 提供该工具，PostgreSQL CI 在回填后执行。新增命令单元测试、SQLMock 和真实 Docker PostgreSQL 16 迁移→回填→准入检查证据；镜像 `autolive-api:20260821-v281` image ID `sha256:a5ae6ae18c7f0006a97a6c8610fc78f685ff2b9aaccc860f458c65c35068e1ac`。该检查不等于生产正式切换，快照退出、Secret 进程外恢复/旧密钥归档、数据库关闭矩阵、真实 HTTPS/TLS、供应商权威额度、真实规模性能/容量告警、审计归档、镜像签名/回滚仍待完成。
- 2026-08-21：服务端 V2.82 normalized 生产配置门禁批次：`APP_DEPLOYMENT_ENV=production` 拒绝 `APP_MODEL_READ_SOURCE=snapshot`，生产示例配置改为 `normalized`；未完成回填时 readiness 继续 fail-closed。镜像 `autolive-api:20260821-v282` image ID `sha256:57d54b169f1e47e4514a5b8009eaf90b37fdc871436adbd41f76e892258fcaac`，本地 PostgreSQL 16 与真实 Compose 已验证生产快照拒绝、迁移容器 `Exited(0)`→回填→准入→normalized API `Healthy`/readyz/metrics→SIGTERM clean shutdown。正式切换 Runbook、快照路径删除、Secret/数据库故障矩阵、真实 HTTPS/TLS、供应商额度、规模性能、审计归档、镜像签名/回滚仍待完成。
- 2026-08-21：服务端 V2.83 BE-005/BE-018 管理租约详情 normalized 读路径批次：新增 `ModelLeaseDetailReader`，PostgreSQL 使用显式回填门禁和参数化 SQL 读取脱敏租约详情，过期状态由服务端时钟派生；normalized 服务缺少该领域读接口时 fail-closed，不再通过 `StateOperation` 读取 legacy snapshot。SQLMock、服务层和真实 Docker PostgreSQL 16 集成测试通过；其他快照回退路径、正式切换 Runbook、Secret/数据库故障矩阵、HTTPS/TLS、供应商额度、规模性能、审计归档、镜像签名/回滚仍待完成。
- 2026-08-21：服务端 V2.84 normalized 回退与 Secret 恢复切片：用户/设备/激活码/审计/用量/租约/模型账号列表在缺少规范化分页 Reader 时统一 fail-closed，避免兼容 `StateOperation` 全量 materialize；新增 normalized 专用 staged Secret 清理事务，使用 advisory lock、`NOT EXISTS(model_accounts.secret_ref)`、cutoff、batch 和 `FOR UPDATE OF ... SKIP LOCKED` 删除孤儿 rotation 密文，绕过 `loadNormalized`/`syncReferenceRows`。SQLMock、服务层、真实 PostgreSQL 16 重启式 active/orphan/重复清理测试通过；外部 Secret/数据库断连、提交未知结果、正式切换 Runbook、HTTPS/TLS、供应商额度、规模性能、审计归档、镜像签名/回滚仍待完成。
- 2026-08-21：V2.84 Compose 证据补录：显式传入 `APP_STORAGE_MODE=postgres` 与 `APP_MODEL_READ_SOURCE=normalized` 后，真实 PostgreSQL 16 完成 migration 21→backfill，迁移容器 `Exited(0)`、API `Healthy`、`readyz=200`；容器 `/metrics` 首段实际出现 `autolive_audit_write_failures_total`、`autolive_database_idle_connections`、`autolive_database_in_use_connections`、`autolive_database_max_open_connections`、`autolive_database_pool_saturated`、`autolive_database_pool_saturation_ratio`、`autolive_database_wait_count_total`、`autolive_http_request_duration_seconds`，响应含 `autolive_http_requests_total` 和 `go_info{version="go1.26.6"} 1`。SIGTERM 后容器退出码 `0`、`OOMKilled=false`，日志出现 `shutdown signal received` 与 `http server stopped cleanly`；临时数据库/网络已清理，镜像 `autolive-api:20260821-v284` digest `sha256:089a97bccbefe3312668ed483e25ebaea776094a1e2cdc7d004dfc558be8a5b68` 保留。上述证据不覆盖 Secret 外部进程断连、提交未知、跨存储补偿或深层关闭故障矩阵。
- 2026-08-21：服务端 V2.85 BE-005/BE-024 设备事务 fail-closed 批次：normalized 激活、心跳、禁用、解绑缺少专用领域 Repository 时显式失败，不再回退 `StateOperation`；无会话凭证的兼容激活/心跳入口在 normalized 模式也拒绝快照写入，必须使用带会话绑定的入口；新增服务层六入口回归。Go 全量 test/vet/build/govulncheck、Docker PostgreSQL 16 migration→backfill→`postgres_integration` 全量、normalized `readyz`/metrics 和 SIGTERM clean shutdown 通过；镜像 `autolive-api:20260821-v285` digest `sha256:84583199be9f9cc08955b6eeade49cd7201198d1200350b8521f09e93670ffa0`。其余 normalized 写路径收口、Secret 外部故障/提交未知/跨存储补偿、正式切换 Runbook、HTTPS/TLS、供应商额度、规模性能、审计归档、镜像签名/回滚仍待完成。
- 2026-08-21：服务端 V2.86 BE-005/BE-017 租约事务 fail-closed 批次：normalized 租约创建、续期、客户端释放和管理员回收缺少专用 `ModelLeaseCreator`/`ModelLeaseRepository` 时显式失败，不再回退 `StateOperation`；新增服务层四入口回归。Go 全量 test/vet/build/govulncheck、Docker PostgreSQL 16 migration→backfill→`postgres_integration` 全量、normalized `readyz`/metrics 和 SIGTERM clean shutdown 通过；镜像 `autolive-api:20260821-v286` digest `sha256:6d8eb1a5aa628741a8dac3ef8a85df41f993b228d79d5864fe1ee6ee50f6808a`。模型账号/Secret、用量、激活码等其余 normalized 写路径收口、Secret 外部故障/提交未知/跨存储补偿、正式切换 Runbook、HTTPS/TLS、供应商额度、规模性能、审计归档、镜像签名/回滚仍待完成。
- 2026-08-21：服务端 V2.87 BE-005/BE-014/BE-017 normalized 写路径 fail-closed 批次：模型账号创建/禁用/编辑、激活码创建/作废、客户端调用摘要缺少专用领域 Repository 时显式失败，不再回退 `StateOperation`；新增服务层入口回归。Go 全量 test/vet/build/govulncheck、Docker PostgreSQL 16 migration→backfill→`postgres_integration` 全量、normalized `readyz`/metrics 和 SIGTERM clean shutdown 通过；镜像 `autolive-api:20260821-v287` digest `sha256:e6aebe180a072e3f541812d791925a6e82dd4dd10395364c6a9aad262863af0c`。Secret 轮换前置快照读取、其余 normalized 写路径、Secret 外部故障/提交未知/跨存储补偿、正式切换 Runbook、HTTPS/TLS、供应商额度、规模性能、审计归档、镜像签名/回滚仍待完成。
- 2026-08-21：服务端 V2.88 BE-002/BE-005/BE-015 normalized 回退收口批次：`RecordAudit`、`UpdateUserAuthorization`、历史 `EnsureLocalAdmin` 在缺少专用能力时 fail-closed；新增 `ModelPoolSecretRotationPreparer`，PostgreSQL 短只读事务读取账号、活动 SecretRef 和幂等事实，Secret 轮换不再先执行 `StateOperation`，且缺少准备器/事务型 Rotator 时不进入 staged 兼容路径。新增服务层/SQLMock 回归；Go test/vet/build/govulncheck 与 Docker PostgreSQL 16 migration→backfill→cutover preflight→`postgres_integration` 全量、normalized readyz/metrics、SIGTERM clean shutdown 通过。镜像 `autolive-api:20260821-v288` digest `sha256:f5657940af548655120105f245f252ee5d517aabe79a89f1d77372c0566fff8d`。严格 cutover preflight 在集成测试前执行；Secret 外部断连、提交未知/跨存储补偿/旧密钥归档、正式切换 Runbook、HTTPS/TLS、供应商额度、规模性能、审计归档、镜像签名/回滚仍待完成。
- 2026-08-21：服务端 V2.89 BE-006/BE-015/BE-020 Secret 轮换故障矩阵批次：真实 PostgreSQL 16 测试代理在已确认 `COMMIT` 后丢弃响应，轮换返回 `COMMIT_OUTCOME_UNKNOWN`，旁路核对确认新 active SecretRef/密文、测试结果、幂等记录和审计 Outbox 已持久化；重建 Repository/SecretStore 后原幂等键重放返回已提交结果且无重复事实。另在 normalized advisory lock 等待期间用 `pg_terminate_backend` 断开后端，轮换返回有界连接错误而非未知提交，旧密文保留且无 staged/test/idempotency/outbox 残留。新增 `TestPostgresNormalizedSecretRotationCommitResponseLossIsRecoverable`、`TestPostgresNormalizedSecretRotationConnectionTerminationBeforeCommitLeavesNoResidue`；本地 Docker PostgreSQL 16 migration→backfill→目标测试 `2/2 PASS`，临时容器已清理。该证据不等于外部 Secret 进程宕机、跨存储补偿、旧密钥归档或正式 normalized 切换完成。
- 2026-08-21：V2.89 Docker 运行验收：镜像 `autolive-api:20260821-v289` 构建成功，digest `sha256:abdbfe9ccea4a854d5031aa24fba6bcbff9c1ab89b1fe5d83c56e21bbf5648ba`；真实 PostgreSQL 16 migration→backfill→cutover preflight→normalized API 通过，`readyz=200`、`/metrics=200`，含 `autolive_database_pool_saturation_ratio`、`autolive_http_requests_total`、`go_info`；SIGTERM 后 API 退出码 `0`、`OOMKilled=false`，日志为 `shutdown signal received`→`http server stopped cleanly`，临时容器/网络清理完成。首次脚本关闭日志正则读取失败，直接读取容器日志复跑通过，未计为代码失败。
- 2026-08-21：服务端 BE-026/BE-007/BE-023 本地容器验收批次：backend/admin `linux/amd64` 镜像构建成功；真实 PostgreSQL 16 完成迁移 0021 与 normalized backfill，`/api/v1/readyz`、`/metrics` 返回 200；真实 Compose 证明 migrate `Exited(0)`→API `Healthy`→管理端代理 readyz，SIGTERM 后 clean shutdown。临时容器清理，验收镜像标签保留；签名、回滚清单、生产 HTTPS 和 Linux/amd64 Race 仍待完成。
- 2026-08-21：服务端 BE-014 PostgreSQL 边界验收批次：新增过期/作废激活码拒绝、重复核销和重启式幂等指纹冲突集成测试，验证不会残留设备、会话或幂等记录，最终只保留一个核销终态；本地 Docker PostgreSQL 16 已执行通过，CI PostgreSQL 16 继续执行真实路径。
- 2026-08-21：服务端 BE-016/BE-017 PostgreSQL 租约生命周期批次：新增懒过期释放并发槽位、跳过冷却/禁用账号、冷却到期恢复和释放租约恢复槽位集成测试；本地 Docker PostgreSQL 16 已执行通过，CI PostgreSQL 16 继续执行真实路径。
- 2026-08-21：服务端 BE-018/BE-021 查询索引批次：新增前向迁移 0021，为租约按用户/设备/provider+model 和审计按操作者/设备/动作/资源筛选补齐复合索引；新增 PostgreSQL `EXPLAIN` 计划测试，真实数据量 `EXPLAIN (ANALYZE, BUFFERS)` 与容量评估仍待发布环境复核。
- 2026-08-21：服务端 BE-002/BE-005 规范化回填准入批次：新增迁移 0020 `normalized_backfill_state` 与 `autolive_require_normalized_backfill_completed()`；显式回填工具在锁定 pending 标记、复制快照、逐表校验后同事务切换 completed，完成后拒绝重复回填。normalized 读、写、清理和运维聚合入口均 fail-closed，CI PostgreSQL 作业按“迁移→回填→集成测试”顺序执行；生产 Compose 仍默认 snapshot，真实发布切换和故障矩阵仍待环境证据。
- 2026-08-21：服务端 BE-007/BE-022 迁移指标发布批次：`autolive-migrate` 可选输出原子 Prometheus text exposition 快照，固定暴露结果状态、耗时、超时预算和迁移版本，不包含 URL、Secret 或完整错误；发布失败不伪装迁移成功，未配置时保持原行为。真实 textfile collector 挂载/权限/重启消费和迁移故障矩阵仍待环境验收。
- 2026-08-21：服务端 BE-008 DTO 契约门禁批次：`backend/internal/httpapi/contract_test.go` 新增显式 Go DTO→OpenAPI schema 对照，递归校验 JSON 字段集合、required、`omitempty` 与指针 nullable 语义；首次运行发现并修复 Logout `refresh_token`、激活码 `max_devices`、Direct LLM 可选统计字段和响应 schema 名漂移。
- 2026-08-21：服务端 BE-008 错误响应批次：OpenAPI 共享错误 response 组件新增 400/401/403/404/405/409/429/500/502/503/504 示例；契约测试验证示例字段和共享错误码存在性，并通过真实 Router/错误映射覆盖上述运行时状态。全量操作级状态声明、生成类型差异、Linux/amd64 Race/镜像冒烟和本机真实 PostgreSQL 仍待后续环境证据。
- 2026-08-21：服务端 BE-008 操作级错误声明批次：OpenAPI 40 个 HTTP 操作全部显式声明 405/408/500 通用 response；新增 `TestOpenAPIOperationsDeclareCommonRuntimeErrorStatuses` 逐项校验状态码到共享 component 的引用，YAML 重新解析通过。生成类型差异、Linux/amd64 Race/镜像冒烟和本机真实 PostgreSQL 仍待后续环境证据。
- 2026-08-21：服务端 BE-023/BE-007 发布与关闭证据批次：后端 Dockerfile 的 Go/Alpine 基础镜像固定 manifest digest；BuildKit CI 同时导出 Docker/OCI 产物并启用 SBOM/Provenance，记录 canonical digest、镜像内容摘要并上传 metadata artifact；新增静态发布门禁和 server readyz→SIGTERM→clean-exit 冒烟。镜像签名、迁移版本/回滚清单、在途事务和真实关闭资源释放仍待后续环境证据。
- 2026-08-21：服务端 BE-020 激活码并发验收批次：新增两个独立 normalized PostgreSQL Repository 并发核销同一激活码的集成测试，验证 advisory lock/行锁只产生一个成功者并保持设备、会话和激活码终态一致；本地 Docker PostgreSQL 16 已执行通过，CI PostgreSQL 16 继续执行。
- 2026-08-21：服务端 BE-017/BE-020 租约并发验收批次：新增两个独立 normalized PostgreSQL Repository 在同一账号 `concurrency_limit=1` 下并发申请租约的集成测试，验证只有一个活动租约；本地 Docker PostgreSQL 16 已执行通过，CI PostgreSQL 16 继续执行。
- 2026-08-21：服务端 BE-008 生成类型门禁批次：管理端锁定 `openapi-typescript@7.13.0`，提交 `openapi.generated.ts`，手写错误 DTO 从生成 components 派生；新增 `api:generate/api:check` 与 CI `frontend-contract` 作业，统一验证 OpenAPI 生成一致性、管理端类型、测试和构建。
- 2026-08-21：服务端 BE-008 生成类型消费扩展批次：桌面端同样锁定 `openapi-typescript@7.13.0`，生成 `desktop/ui/src/api/openapi.generated.ts`，控制面关键 DTO 从生成 components 派生；管理端资源/请求/响应 DTO 统一改为生成类型别名，两个前端均纳入 `api:check`。
- 2026-08-21：服务端 BE-017/BE-020 UTC 日额度验收批次：新增 PostgreSQL normalized 跨 UTC 日切集成测试，验证 `exhausted` 恢复、日用量窗口归零和新租约可申请；本地 Docker PostgreSQL 16 已执行通过，CI PostgreSQL 16 继续执行。
- 2026-08-21：桌面依赖安全修复批次：Vite 从 5.4.21 升级至 6.4.3，修复 Windows `server.fs.deny` 高危漏洞，desktop `pnpm audit --audit-level high`、类型检查、177 项测试和构建通过。
- 2026-08-21：服务端 BE-021/BE-022 保留清理边界补强：Memory 控制面三类记录、normalized PostgreSQL 幂等/模型测试/审计三类记录及 SQL `auth_sessions` 均统一 cutoff、1～1000 批次上限、Context 取消和确定性旧到新删除；SQL 使用参数化 CTE/锁定批次并保持快照读源拒绝语义，SQLMock/Memory 回归覆盖成功、取消、错误和边界。
- 2026-08-21：服务端 BE-019 审计可靠投递批次：新增迁移 0019 `audit_outbox`；normalized PostgreSQL 先以 request_id 去重键持久化脱敏审计，再由有界 Dispatcher 幂等写入 `audit_logs`，失败保留 pending/attempts/next_attempt_at 并接入 Retention Scheduler 重试；Memory/快照保留兼容直接审计。SQLMock 覆盖成功、取消和投递失败回退，服务层与调度器回归通过。业务状态与审计同事务、提交未知结果、真实 PostgreSQL 故障矩阵、容量告警和归档仍待完成。
- 2026-08-21：服务端 BE-006/BE-019/BE-024 审计同事务首批：normalized PostgreSQL 激活/心跳/设备禁用/解绑事务新增可选成功审计事件，在同一事务提交设备、会话绑定或撤销、幂等记录和 `audit_outbox`；HTTP 路由传入已校验 request_id/设备目标，后置审计使用 dedupe_key 避免重复；Outbox 写失败回滚对应业务状态。新增 SQLMock 成功入队/失败回滚和服务层传递回归；其他业务域审计同事务、提交未知结果、真实 PostgreSQL 故障矩阵和归档仍待完成。
- 2026-08-21：服务端 BE-006/BE-017/BE-019 租约审计同事务批次：normalized PostgreSQL 租约创建、续租、客户端释放和管理员回收事务新增可选成功审计事件，与租约状态、幂等记录一起写入 `audit_outbox`；HTTP 路由传入租约目标和 request_id，重复请求不重复产生审计事实；新增四类 SQLMock 和服务层传递回归。客户端调用摘要、模型账号/Secret 轮换、提交未知结果、真实 PostgreSQL 故障矩阵和归档仍待完成。
- 2026-08-21：服务端 BE-017/BE-019 客户端调用摘要审计同事务批次：normalized PostgreSQL 在调用摘要、账号日额度状态、幂等记录和 `audit_outbox` 同一事务提交，可选成功事件在 HTTP 路由传入；重复调用按调用 ID/request_id 去重且不重复产生审计事实，新增 SQLMock 与服务层传递回归。模型账号/Secret 轮换审计、提交未知结果、真实 PostgreSQL 故障矩阵和归档仍待完成。
- 2026-08-21：服务端 BE-015/BE-019 Secret 轮换事务批次：新增 `ModelPoolSecretRotator`，normalized PostgreSQL 通过 `TransactionalSecretWriter.PutTx` 将候选密文、账号引用、模型测试结果、幂等记录、旧密文删除和成功审计 Outbox 放入同一事务；Memory/快照继续使用 staged fallback。真实连接中断/提交未知结果、旧密钥归档、Secret 故障矩阵和发布证据仍待完成。
- 2026-08-21：服务端 BE-015/BE-020 Secret 轮换真实 PostgreSQL 验收批次：新增带 `postgres_integration` 标签的提交后重启读取、幂等重放、旧密文删除、审计 Outbox 持久化和 staged ciphertext 事务回滚测试；本地 Docker PostgreSQL 16 与 CI PostgreSQL 16 均执行真实路径。真实连接中断/提交未知结果、进程外 Secret 故障注入、旧密钥归档和 Memory/快照跨进程补偿仍待完成。
- 2026-08-21：服务端 BE-009 桌面控制面发布边界批次：移除硬编码私网 HTTP 地址；开发构建仅回退到 `127.0.0.1`，生产构建必须显式注入 HTTPS `VITE_CONTROL_PLANE_BASE_URL`，缺少时拒绝启动；Tauri capability、桌面架构文档和契约测试同步更新。真实证书、DNS、Nginx reload、HTTPS 冒烟和正式域名 capability 替换仍待发布环境执行。
- 2026-08-21：服务端 BE-024/BE-006 会话绑定同事务批次：新增可选 `TransactionalSessionBinder` Repository 边界；PostgreSQL 在同一事务锁定并校验 `auth_sessions`、写入 `device_bound_at`，再提交激活码核销或心跳/幂等状态，业务错误统一回滚绑定与业务写入；HTTP SQL 路径自动使用同事务，Memory/兼容路径保留精确补偿。新增 SQLMock、服务层 runner 和 PostgreSQL 集成故障回滚测试；Secret Store/审计 Outbox 原子性、真实 CI 连接中断、容量告警与归档仍待完成。
- 2026-08-21：服务端 BE-005/BE-024 激活核销领域事务批次：normalized PostgreSQL 新增 `TransactionalDeviceActivator`，在同一事务锁定会话、用户、设备和激活码，完成一次性核销、设备注册/解绑后重激活、幂等记录与 `device_bound_at` 绑定；幂等键沿用 `control-plane-state` + 完整激活作用域，兼容旧快照同步记录。新增 SQLMock 成功提交和服务层 normalized 路由测试；心跳/禁用/解绑、模型账号/租约/用量/审计其余领域写入、真实 PostgreSQL 并发/重启/连接故障和审计 Outbox 仍待完成。
- 2026-08-21：服务端 BE-005/BE-024 心跳领域事务批次：normalized PostgreSQL 新增 `TransactionalHeartbeatRecorder`，在同一事务锁定会话、用户和设备，写入心跳指标/播放状态/最后心跳、幂等记录并绑定 `device_bound_at`；兼容旧快照/Memory runner，幂等键沿用 `control-plane-state` + 完整心跳作用域。新增 SQLMock 成功提交和服务层 normalized 路由测试；设备禁用/解绑、模型账号/租约/用量/审计其余领域写入、真实 PostgreSQL 故障矩阵和审计 Outbox仍待完成。
- 2026-08-21：服务端 BE-005/BE-013/BE-024 设备生命周期事务批次：normalized PostgreSQL 新增 `DeviceLifecycleRepository`，在同一事务更新禁用/待激活状态、释放活动模型租约、撤销设备会话并写入幂等记录；HTTP normalized 路径跳过重复 Session Store 撤销，Memory/快照继续保留兼容 StateOperation 和补偿。新增 SQLMock 与服务路由回归；模型/租约/用量/审计其余领域写入、真实 PostgreSQL 连接故障和审计 Outbox 仍待完成。
- 2026-08-21：服务端 BE-005/BE-017/BE-024 租约生命周期事务批次：normalized PostgreSQL 新增 `ModelLeaseRepository`，将客户端续租、客户端释放和管理员回收拆为独立事务，锁定用户/设备/租约，处理过期派生、归属校验、活动账号状态、条件状态更新和 `control-plane-state` 幂等记录；租约创建暂保留兼容 StateOperation，待纳入 SecretStore 可用性、账号选型、用户门禁和租约插入的同一事务。新增三类 SQLMock、取消路径和服务层规范化路由回归；真实 PostgreSQL 并发/连接故障、供应商硬额度、用量与审计写入仍待完成。
- 2026-08-21：服务端 BE-005/BE-017 客户端调用摘要事务批次：normalized PostgreSQL 新增 `ModelUsageRepository`，在同一事务锁定用户、设备、租约和模型账号，按 `(lease_id, client_call_id)` 核对重复摘要，写入 `model_usage_records` 与幂等记录，执行用户记录用量门禁并在账号日额度达到阈值时更新 `exhausted`；摘要只接受 `client_reported`，不保存模型正文或 finish reason。新增 SQLMock、取消和服务层 normalized 路由回归；真实 PostgreSQL 唯一竞争/连接故障、模型账号/租约创建和审计写入仍待完成。
- 2026-08-21：服务端 BE-005/BE-025 模型账号生命周期事务批次：normalized PostgreSQL 新增 `ModelPoolRepository`，将账号编辑/禁用放入同一事务，锁定账号行，先派生过期租约，再校验活动租约冲突/并发上限，写入账号状态、URL、优先级、日额度、并发额度和幂等记录，并返回账号摘要；账号创建的事务型 SecretStore 边界在后续批次补齐，密钥轮换原子性未改变。新增 SQLMock、服务层规范化路由和状态不变量回归；真实 PostgreSQL 连接故障、租约创建和审计写入仍待完成。
- 2026-08-21：服务端 BE-019 审计规范化追加批次：normalized PostgreSQL 新增 `AuditRepository`，直接追加已脱敏的主体、设备、动作、资源、结果、状态码、错误码、request_id 和时间，不再为单条审计读取/重写整份快照；HTTP fail-closed 与 `AUDIT_UNAVAILABLE` 语义保持不变。新增 SQLMock、取消和服务层规范化路由回归；业务状态与审计同事务/可靠 Outbox、重复投递、未知提交和归档仍待完成。
- 2026-08-21：服务端 BE-005/BE-025 模型账号创建事务批次：新增独立 `ModelPoolAccountCreator` 与 `TransactionalSecretWriter`；normalized PostgreSQL 在同一事务写入幂等记录、模型账号行和 AES-GCM 密文，非事务型 SecretStore 明确拒绝，避免账号与密钥出现隐式部分成功；Memory/快照保留兼容创建路径，新增 SQLMock、事务型 SecretStore 和服务层路由回归。密钥轮换故障补偿、审计 Outbox/业务同事务和真实 PostgreSQL 连接故障仍待完成。
- 2026-08-21：服务端 BE-005/BE-017 租约创建事务批次：新增独立 `ModelLeaseCreator`；normalized PostgreSQL 在 advisory lock 事务内锁定用户/设备/授权策略和候选账号，执行 allowlist、服务端日额度、账号优先级/日额度/活动租约并发、过期派生及 SecretStore 可用性检查后插入 `model_leases`，幂等记录与租约同事务提交；Memory/快照保留兼容路径，新增 SQLMock、allowlist、取消和服务层路由回归。供应商权威额度、跨实例压力/连接故障和未知提交仍待真实 PostgreSQL 验收。
- 2026-08-21：服务端 BE-019 审计 fail-closed 批次：API 写请求使用内存响应缓冲，先持久化审计再提交响应；审计写失败统一返回 `503/AUDIT_UNAVAILABLE`，不泄露原业务成功响应，并以同幂等键重试恢复；新增统一错误码文档、OpenAPI 描述和回归测试。业务状态与审计的同事务/可靠 Outbox、重复投递、未知提交结果和归档仍待完成。
- 2026-08-21：服务端 BE-019 客户端重试批次：管理端 `apiClient` 与桌面端控制面客户端在写请求携带 `Idempotency-Key` 且收到 `503/AUDIT_UNAVAILABLE` 时各自动重试一次，复用原请求体和幂等键，新增管理端/桌面端契约回归；业务状态与审计同事务、可靠 Outbox、未知提交结果和归档仍待完成。
- 2026-08-21：服务端专项文档 V2.44 增加“当前仍未关闭”摘要和 BE-005/BE-017/BE-019/BE-025 账号/租约/调用摘要/审计事务批次：P0 聚焦规范化唯一事实源、跨存储原子性、真实故障/关闭与 HTTPS 发布证据；normalized 用户/授权/激活码/设备/会话、模型账号编辑/禁用、租约续期/释放/管理员回收、客户端调用摘要和审计追加已通过最小领域 Repository 直接写规范化表/幂等表，账号创建、租约创建、Secret/密钥轮换原子性和审计 Outbox/业务同事务仍未完成。P1 聚焦额度、租约并发、Secret/审计 Outbox 和真实 PostgreSQL 矩阵；P2 聚焦容量告警、细分运维指标、归档、SBOM 与镜像签名。已完成的 Go 基线、SSRF、管理员启动、状态不变量和限流不再列为实现缺口。
- 2026-08-21：服务端 BE-007 超时观测批次：健康探测与保留清理将 `context deadline` 和主动取消分别记录为固定低基数 `timeout`/`cancelled`；迁移命令成功/失败日志增加 `duration_ms` 与预算字段；新增服务层、HTTP `/metrics` 和迁移命令回归。真实 SIGTERM/在途事务、数据库关闭冒烟及迁移指标文件真实 collector 消费仍待完成。
- 2026-08-21：服务端 BE-007/BE-022 迁移指标导出批次：迁移命令新增可选 `APP_MIGRATION_METRICS_FILE`，使用官方 Prometheus client_golang 生成固定低基数 text exposition 快照（结果 status、耗时、超时预算、目标 migration version），同目录临时文件刷盘后原子重命名，未配置时保持既有行为；已配置但指标发布失败会明确记录并以非零退出，Compose 仅透传配置。真实 textfile collector 挂载、权限、重启和故障矩阵仍待环境验收。
- 2026-08-21：服务端 BE-008 错误状态门禁批次：契约测试从 Go `NewError` 调用提取错误码及 HTTP 状态，与 `接口契约/错误码.md` 逐项校验，同时保留 OpenAPI 路由、方法和本地 `$ref` 检查；DTO required/nullable、错误响应示例和真实运行时状态矩阵仍待完成。
- 2026-08-21：服务端 BE-005 用户/激活码领域 Repository 扩展批次：`UserRepository` 已覆盖 normalized PostgreSQL 用户创建、编辑、密码重置和禁用，新增独立 `UserAuthorizationRepository` 写入 allowlist/软额度策略及 `ActivationRepository` 创建/作废一次性激活码；事务使用既有 advisory lock，幂等记录与用户/策略/激活码行同事务提交，覆盖重复回放、指纹冲突、用户名唯一冲突、最后管理员保护、本地管理员禁用保护、密码哈希更新、策略 JSONB 更新和激活码明文不落库。服务层仅在 normalized 读源启用这些路径，快照/Memory 保留兼容回退；激活码核销/设备绑定、设备、模型、租约、用量、审计领域写入、Secret/审计原子性和快照退出仍待完成。
- 2026-08-21：服务端 BE-015 staged secret 恢复批次：新增 `StagedSecretCleaner`，SQL Secret Store 仅扫描 `model-account/%/rotation_%`、更新时间超过 1 小时且不在当前模型账号 SecretRef 集合中的候选，使用 `LIMIT + FOR UPDATE SKIP LOCKED` 有界删除；ControlPlane 先读取并排序当前引用集合，Retention Scheduler 接入固定低基数 `staged_secrets` 数据集，Memory Secret Store 保持无持久化回收回退；新增 SQLMock、服务层保护集合和调度器测试。跨 Secret Store/Repository 原子提交、旧密钥故障补偿和真实 PostgreSQL 故障注入仍待完成。
- 2026-08-21：服务端 BE-011/BE-017 用户策略门禁批次：新增迁移 0016 `user_authorization_policies`、用户模型 allowlist 与服务端已接收用量门禁；管理员可通过幂等 `PATCH /api/v1/admin/users/{user_id}/authorization` 配置，授权摘要和管理端用户抽屉同步展示，模型租约申请/调用摘要写入前执行策略校验。门禁只基于服务端已接收的 `client_reported` 记录，不是供应商权威账单，也不做未来用量预占；跨实例原子额度、供应商短期凭证和专用指标仍待完成。
- 2026-08-21：服务端 BE-007 预算切片：迁移命令新增独立 `APP_MIGRATION_TIMEOUT`（默认 30s），Compose 迁移服务传递该变量；API 启动的 PostgreSQL Ping、迁移版本检查和管理员引导统一使用 `APP_REQUEST_TIMEOUT`，配置与命令测试覆盖非法/非正预算。迁移耗时指标、后台任务超时细分和完整关闭编排仍待完成。
- 2026-08-21：服务端 BE-022 运维聚合指标批次：`/metrics` 新增模型账号固定状态计数、未过期活动租约数、UTC 日用量 Token 总数和已配置用户授权策略数；Memory 与 normalized PostgreSQL 均提供有界聚合读取，指标不带账号/用户/租约 ID，并以 HTTP、Memory、SQLMock 回归覆盖。随后补齐健康探测运行/探测/跳过/耗时的固定低基数事件指标，未知标签统一为 `unknown`；供应商权威额度、跨实例租约以及迁移/后台超时细分指标仍待完成。
- 2026-08-21：服务端 BE-016 后台健康探测批次：新增由服务器进程拥有的 `ModelPoolHealthProbeScheduler`，默认 5 分钟周期，可配置每轮账号数、并发数、探测超时、失败退避/抖动，并在优雅关闭时取消和等待；`cooldown/exhausted/disabled` 账号不会重复探测，成功会清除退避状态。健康事件通过服务遥测边界接入 Prometheus 固定状态指标，调度器不携带账号、URL、错误全文或秘密；跨实例 advisory lock 已在后续批次接入，供应商权威额度和真实 PostgreSQL 长时故障矩阵仍待完成。
- 2026-08-21：服务端 BE-021/BE-022 保留清理批次：会话、幂等记录、模型测试结果和审计日志支持 cutoff、1～1000 批次上限、取消和确定性旧到新删除；normalized PostgreSQL 采用参数化 `CTE + LIMIT + FOR UPDATE SKIP LOCKED`，在 advisory lock 事务中同步快照，快照读源明确拒绝清理；新增 `APP_RETENTION_CLEANUP_INTERVAL/TIMEOUT/BATCH` 与四类 TTL，服务启动/关闭接线后台调度器，单类失败继续处理其他数据集，并以固定低基数指标记录运行、数据集结果和删除数量；0017 已补齐首轮保留/筛选索引，容量告警、归档和真实数据量 `EXPLAIN` 复核仍待完成。
- 2026-08-21：服务端 BE-020 真实 PostgreSQL 集成批次：CI 为 PostgreSQL 16 注入 `TEST_POSTGRES_URL`，normalized round-trip 改为重新构造 Repository 后读取，新增外键约束失败回滚、双实例同幂等键竞争和 SQL Session Store 重启/Refresh 单次消费测试；本地缺少数据库 URL 时按预期跳过，真实执行留给 CI service。
- 2026-08-21：服务端 Phase 6 关闭预算批次：API 默认 `APP_SHUTDOWN_TIMEOUT` 调整为 45 秒，Compose 的迁移/API/管理容器统一声明 `AUTOLIVE_STOP_GRACE_PERIOD`（默认 60 秒），与请求和后台清理/健康任务预算保持余量；Compose YAML、配置回归通过，真实 SIGTERM/在途事务冒烟仍待发布环境执行。
- 2026-08-21：服务端 Phase 6 关闭路径修复：HTTP 异常、Shutdown 超时、后台任务停止失败和最终服务错误分支在非零退出前显式关闭数据库运行时，避免 `os.Exit` 跳过 deferred close；真实 SIGTERM/在途事务冒烟仍待发布环境执行。
- 2026-08-21：服务端 BE-021/BE-018 索引迁移批次：新增前向迁移 0017，为会话过期/撤销、幂等/测试结果/审计保留清理、模型账号/租约筛选和调用摘要查询增加可重复执行索引；迁移目录门禁更新至 0017，未改历史迁移或数据。
- 2026-08-21：服务端 BE-015 staged secret 并发修复：密钥轮换每次尝试使用随机唯一 staged reference；提交事务校验 SecretRef 未被其他幂等键切换，只有实际提交事务的竞争者回收旧密钥，重复幂等或提交失败方只回收自己的 staged secret；新增提交故障并发和不同幂等键竞争回归，进程崩溃后的孤立 staged secret 清理仍待完成。
- 2026-08-21：服务端 BE-016 跨实例健康调度锁批次：新增 Repository advisory-lock 边界；PostgreSQL 在专用连接上执行 `pg_try_advisory_lock`，锁持有期间覆盖整轮探测并在取消/关闭路径调用 `pg_advisory_unlock` 后归还连接，锁忙时跳过本轮并记录固定低基数 `distributed_lock` 指标；Memory/单进程保留无锁回退，新增 SQLMock、调度器忙锁和 PostgreSQL 双 Repository 集成回归。
- 2026-08-21：服务端 BE-022/BE-019 指标批次：限流命中新增按固定策略名的 `autolive_http_rate_limited_total`，审计写失败新增无标签 `autolive_audit_write_failures_total`；Router 将注入的 `slog.Logger` 传入审计中间件，生产路径不再绕过应用 Logger 使用全局 Logger。后续聚合指标已另列最新执行记录；健康事件、供应商额度、跨实例租约、审计 Outbox/fail-closed 与保留清理仍待完成。
- 2026-08-21：服务端 BE-014 并发核销批次：MemoryStore 并发读改写回归证明一次性激活码只成功绑定一个设备，失败请求返回 `ACTIVATION_CODE_USED`，成功请求可用原幂等键重放；激活/作废后清除内存明文，列表继续只返回脱敏字段。真实 PostgreSQL 并发、重启恢复和完整核销审计仍待完成。
- 2026-08-21：服务端 BE-019 激活审计补强：客户端激活/心跳由已验证会话绑定补齐设备目标 ID，HTTP 回归确认激活审计包含设备资源且不含激活码正文；Outbox/fail-closed 和真实 PostgreSQL 故障证据仍待完成。
- 2026-08-20：服务端 BE-019 继续收口审计语义：统一错误出口会填充 `error_code`，登录成功/失败按 `POST /api/v1/auth/login` 记录 actor、结果和状态码，新增回归测试验证密码不会进入审计；可靠 Outbox、筛选和指标仍待完成。
- 2026-08-20：服务端 BE-022/BE-007 落地可观测性首切片：引入 `github.com/prometheus/client_golang v1.20.5`，为每个 Router 使用独立 registry，新增 `/metrics` 的低基数 HTTP 请求量、状态码、延迟、在途请求和 PostgreSQL `sql.DBStats` 连接池指标；事务、账号健康、租约、额度、审计失败和限流专用指标仍待补齐。
- 2026-08-20：服务端 BE-003 收口明文模型出站准入：模型连通性测试默认拒绝 `http://`，仅在应用启动配置 `APP_ALLOW_INSECURE_HTTP=true` 时允许受控开发/联调；新增 `MODEL_POOL_CONNECTIVITY_INSECURE_HTTP` 测试结果错误码，并将配置透传到 Go 控制面和生产 Router。
- 2026-08-20：服务端 BE-011 落地用户授权/软额度摘要第一切片：新增 `GET /api/v1/admin/users/{user_id}/authorization-summary` 和管理端用户抽屉摘要，展示设备、活动租约、活动模型账号与客户端自报日用量；明确当前没有用户级供应商硬额度，不把客户端上报伪装成权威计费。
- 2026-08-20：服务端 BE-007 补齐数据库预算第一切片：`APP_REQUEST_TIMEOUT` 透传到 PostgreSQL Repository 与 normalized backfill 的事务/语句 Context，上游取消优先，数据库驱动取消统一回译为 Context 错误；SQL Session Store/Secret Store 独立预算和优雅关闭等待仍待完成。
- 2026-08-20：服务端 BE-007 后续切片补齐 SQL Session Store 与加密 Secret Store 的独立有界操作预算，并增加超时回归测试；迁移/后台任务预算、细分指标和优雅关闭等待仍待完成。
- 2026-08-20：服务端 BE-011/BE-018 分页第一切片：用户、设备和按用户设备列表将分页参数传入服务层，normalized PostgreSQL 读源使用有界 `COUNT + LIMIT/OFFSET` 查询并执行规范化覆盖检查；内存/快照模式保留兼容回退，模型账号、租约、用量和审计筛选分页仍待迁移。
- 2026-08-20：服务端 BE-018 扩展分页第一切片：调用摘要和审计列表同样接入 normalized PostgreSQL 有界分页，保留结构化错误/结果字段和稳定 `created_at,id` 排序；模型账号、租约管理端查询以及筛选排序白名单仍待迁移。
- 2026-08-21：服务端 BE-018 继续扩展分页第一切片：模型账号管理列表接入 `page/page_size`，normalized PostgreSQL 以有界 SQL 查询派生活动租约、UTC 日用量和最近测试结果，并同步 OpenAPI、管理端 Ant Design 分页和回归测试；租约管理端查询已在后续批次接通，筛选排序白名单另见最新执行记录。
- 2026-08-21：服务端 BE-018 增加租约查询第一切片：新增脱敏管理员租约列表 API、normalized PostgreSQL `COUNT + LIMIT/OFFSET` 查询、活动/过期状态派生和管理端 `/model-leases` 路由；租约详情、筛选排序白名单、并发与故障验收仍待完成。
- 2026-08-21：服务端 BE-018 增加租约筛选/排序第一切片：`/api/v1/admin/model-leases` 支持状态、供应商、模型、用户、设备、账号筛选及到期/状态/供应商模型四种白名单排序；normalized PostgreSQL 以参数化 SQL 执行，内存/快照回退保持同一语义，管理端筛选控件、OpenAPI 和回归测试同步；租约详情/回收、完整列表筛选和索引策略仍待完成。
- 2026-08-21：服务端 BE-019 增加审计查询第一切片：`/api/v1/admin/audit-logs` 支持操作者、设备、动作、目标类型、结果、错误码、request_id、RFC3339 时间窗口筛选及创建时间升降序；normalized PostgreSQL 使用参数化 SQL，内存/快照回退与管理端筛选控件、OpenAPI、回归测试同步；审计 Outbox、保留清理、专用指标和完整索引策略仍待完成。
- 2026-08-21：服务端 BE-018 增加租约详情/管理员回收第一切片：新增脱敏 `GET /api/v1/admin/model-leases/{lease_id}` 和幂等 `POST /api/v1/admin/model-leases/{lease_id}/reclaim`，补齐创建/回收时间、管理端详情与回收控件、OpenAPI、审计和回归测试；跨存储故障注入、完整索引和更广列表筛选仍待完成。
- 2026-08-20：PCM 恢复/N+1 竞态收口：恢复全程增加独立原子占位，N+1 prepare、普通同步和无匹配旧候选 cancel 不再使恢复 token 失效；source/revision superseded、覆盖不足及更新播放控制抢占产生的 candidate/start stale 统一返回结构化可重试状态，保留 current、PortAudio 流和偏好，前端按最新快照串行重试，成功后只清除旧 PortAudio 回退提示且不重复包装错误。声音 profile 实际变化同步递增流 revision。目标 Windows 设备长时间运行验收仍待完成。
- 2026-08-20：循环边界静音/错轨收口：最终效果窗口在后端确认新循环序号后按新绝对媒体时间显式重锚 PortAudio；实时 PCM 解码不再让单个 `-stream_loop -1` FFmpeg 长驻跨越源文件边界，改为有限输入 EOF 后等待旧进程退出并从 0ms 顺序重启，避免源文件整数倍位置停产以及下一轮沿用上一轮时间轴。Windows 声卡 50 轮实机听感仍待验收。
- 2026-08-20：循环/静音恢复补强：最终效果窗口对 `timeupdate`/`ended` 使用同一单飞提交门禁，防止本地绝对媒体时钟一次推进两轮；物理环缓和旧轨都枯竭时，已预热候选允许从静音淡入接管。重复播放命令按目标状态幂等跳过，结构化 Tauri 错误保留真实原因。
- 2026-08-20：轮换恢复所有权收口：声音周期是否继续走 PortAudio 候选链路改为依据“用户仍偏好 PortAudio 且硬件为 `active`”，不再依据瞬时 `running`。PCM 停产恢复期间主窗口不直接应用下一 profile、不递增 revision；自然循环重锚保留跨循环的普通 N+1 候选；旧 candidate cancel 与 prepare 串行并按 ID 匹配；交叉淡化等待期间继续供给 current PCM；持续 callback 欠载按生产侧故障触发恢复。上述规则共同避免单选和组合轮换连续使候选过期、耗空环缓并形成静音；最终效果窗口仍负责串行恢复，WebView 负责回退出声。Windows 声卡 50 轮实机听感仍待验收。
- 2026-08-20：服务端专项执行批次已记录在 [`服务端未完成功能长任务开发计划.md`](./服务端未完成功能长任务开发计划.md)：完成可控测试时钟、SSRF 出站校验、会话写入错误传播、模型账号禁用不变量、live/ready、请求级超时、设备播放状态持久化、用户编辑/密码重置、按用户设备查询、设备授权/软额度摘要、设备详情/在线派生、禁用解绑后的设备会话与租约联动、激活码脱敏核销详情、统一前后端错误/请求 ID 契约、桌面 HashRouter，以及登录/Refresh/模型测试/密钥轮换的单实例有界限流；Go 模块语言版本保持 `1.22`，构建工具链固定 `1.26.6`。规范化 PostgreSQL 唯一事实源、HTTPS 发布、限流指标、用户级硬额度/模型授权和激活码并发核销仍未关闭。
- 2026-08-20：服务端 Phase 4 新增模型账号密钥轮换闭环：管理端提供换密钥表单，Go 通过受限 `/models` 校验候选密钥后写入 Secret Store，并按 `Idempotency-Key` 记录测试结果；失败保留旧密钥、重复请求不重复探测。跨 Secret Store/Repository 原子提交、旧密钥回收、自动健康/冷却和真实 PostgreSQL 故障补偿仍未关闭，详见 [`服务端未完成功能长任务开发计划.md`](./服务端未完成功能长任务开发计划.md)。
- 2026-08-20：服务端 Phase 1 收口管理员启动边界并补齐高风险接口限流：PostgreSQL 首次引导拒绝缺失或占位管理员配置并写入 bcrypt 哈希；本地管理员专用密码轮换接口要求幂等键、成功后撤销全部管理员会话，轮换后可省略静态管理员密码环境变量；启动失败时 API 主进程非零退出，`/readyz` 校验持久化管理员启用状态和凭证哈希；`golang.org/x/time/rate` 按客户端 IP 对登录、Refresh、模型测试和密钥轮换返回 `429/RATE_LIMITED`。限流指标和 HTTPS 发布仍待完成。
- 2026-08-20：服务端补充 OpenAPI 路由门禁：`backend/internal/httpapi/contract_test.go` 使用 `gopkg.in/yaml.v3` 读取唯一契约并逐个请求真实 Router，任何声明路径/方法落到 404/405 时 Go 测试失败；DTO、错误码和数据库规范化事实源仍待后续批次。
- 2026-08-20：OpenAPI 契约门禁扩展为本地 `$ref` 完整性检查，paths 与 components 中的引用必须指向已声明组件；前后端继续统一 `application/json`、`X-Request-Id`、`request_id`、`code/message/details` 包络，DTO 字段语义和响应示例仍列为后续收口项。
- 2026-08-20：服务端 BE-008 收口一批响应漂移：解绑设备改用不携带用户归属的独立响应，模型账号创建 `status` 写入 OpenAPI，请求 ID 明确为合法值校验后透传、非法值由服务端生成；新增解绑 HTTP 契约回归测试。
- 2026-08-20：服务端 BE-019 落地审计语义第一切片：0015 持久化 `outcome/status_code/error_code`，已认证写请求按路径记录目标类型和资源 ID，管理端展示结果与状态码；登录审计、可靠 Outbox、筛选和指标仍待完成。
- 2026-08-20：服务端 Phase 2 开始落地 0012 前向迁移：在保留历史事实的前提下，为非空 `(lease_id, client_call_id)` 建立数据库唯一索引；迁移遇到历史重复摘要会明确失败，待发布前核对，不静默合并。
- 2026-08-20：服务端 BE-009 完成 HTTPS 发布门禁切片：生产配置强制 `APP_PUBLIC_BASE_URL=https://...` 并拒绝不安全 HTTP，Compose 管理端 HTTP hop 绑定回环地址，新增宿主机 Nginx 80→443/TLS 模板、Tauri 明确 HTTPS capability 和 `tools/check-https-publish.mjs`；真实证书/DNS、代理加载和 PostgreSQL HTTPS 冒烟仍待发布环境验证。
- 2026-08-20：服务端 Phase 2 落地规范化读源第一切片：新增 0013 迁移补齐设备展示字段和模型测试结果表；PostgreSQL `APP_MODEL_READ_SOURCE=normalized` 在事务内读取用户、设备、激活码、模型账号、租约、用量、模型测试、幂等和审计表，并用事务级 advisory lock 保护现有读改写边界。快照回填、按领域 Repository、SQL 分页和真实跨进程 PostgreSQL 验收仍未关闭。
- 2026-08-20：服务端补齐规范化切换前的一次性回填工具 `backend/cmd/backfill-normalized`：显式确认、迁移版本校验、事务级 advisory lock、快照读取、规范化 upsert 和逐表数量校验；工具不接入 API 启动自动流程，失败回滚后仍保持 snapshot 读源。
- 2026-08-20：服务端 BE-024 收口会话绑定假成功：激活和心跳先执行受保护会话绑定，再提交业务状态；业务失败仅清理本请求新建的绑定，SQL `UpdateDeviceID`/`ClearDeviceID` 检查影响行数并受 2 秒有界补偿上下文保护；新增激活/心跳失败与补偿回归测试。跨存储同事务或真实 PostgreSQL 故障注入仍待完成。
- 2026-08-20：服务端 BE-016 落地模型账号健康状态第一切片：失败/超时连通性测试进入持久化 5 分钟 `cooldown`，成功测试恢复，新的 UTC 日按服务端时钟解除自动 `exhausted`；后台健康探测、失败计数和严格供应商额度仍未关闭，详见服务端专项计划。
- 2026-08-20：服务端补充真实 PostgreSQL 验证入口：CI 增加 PostgreSQL 16 service，运行全部迁移并执行 `postgres_integration` normalized round-trip，验证规范化写入、重启式读取、幂等记录恢复和迁移版本门禁；双实例并发、故障注入和额度矩阵仍待后续。
- 2026-08-20（历史方案，其设备入口口径已由 2026-08-26 最新设计覆盖）：当时恢复 PortAudio Host API 的 `ASIO` 可见筛选入口；当前 UI 在右栏话术功能上方基于真实枚举提供 Host API 筛选、输出设备选择、`128–2048 KiB` 内存缓冲和显式应用，并分开展示编辑值、已应用配置和运行状态。当前随包 DLL 未启用 ASIO，未枚举时不得伪造可用；默认系统/WASAPI 出口及 WebView 回退规则不变。
- 代码策略：本轮允许清理代码；数据库历史迁移文件不删除、不回滚
- 唯一依据：本文件与根目录当前有效架构/接口文档；`docs/superpowers/` 中标记为废止的旧设计只用于审计

## 2. 当前产品基线

### 2.1 当前目标

用户登录并激活 Rust 桌面端后，可从 `.mp4`、`.mov`、`.mkv`、`.avi`、`.webm`、`.m4v`、`.ts`、`.m2ts`、`.flv`、`.wmv`、`.3gp` 中选择或系统拖入本地源视频。新增候选按返回顺序逐项探测，只有全部成功才原子整批替换、追加或替换单项；既有条目可原子重排、删除和清空。取消、超限、重复路径或任一失败均保留旧池且不提交部分结果。成功编辑停止当前资源，非空池首项 `Ready` 后由用户点击播放，在一个持续存在的窗口、一个活动媒体源内执行 `A→B→…→A`，单项池自循环。桌面端当前保留两个互相独立的处理开关，并新增两个待实施输出能力：

1. 视频处理：控制正式 `VideoEffectParams`/`AdvancedEffectParams`。已映射基础画面效果进入运行态；视觉频段、扰动、挂件、切片、检测和修复等参考图参数全部进入正式 backlog，未接入前在参数面板显示“正式需求待实现”，语义尚未确认时显示“待确认”。
2. 声音处理：控制普通声音处理和音频分析。
3. 实时话术幻化（后续版本，不纳入当前）：输入本地文件或受控实时流，通过 speech-to-speech 的 VAD/ASR/LLM/TTS 生成候选音轨，并在安全边界替换当前播放音轨。本版本不提供该入口。
4. ZLMediaKit RTMP/RTMPS 直推（正式需求·待实施）：用户输入完整发布地址，默认输出画面和声音且可只选一轨；桌面端直接读取活动源和最终 PCM，通过独立受管 FFmpeg 发布链使用 GPU83 静态快照与硬件 H.264 编码，不捕获窗口、不经过 Go。
5. AkVirtualCamera GPU 辅助虚拟摄像头（正式需求·待实施）：只捕获最终效果 HWND，WGC/D3D11 在 GPU 上缩放并转 YUY2 `1280×720@30fps`，一次有界回读后由独立 GPL sidecar 交给 DirectShow；明确 `zero_copy=false`，Windows 11 MF 保持实验未启用。

整批源媒体通过实际容器、音视频流、可读性和元数据探测后，首项保持 `Ready`；用户点击主页“播放”才打开或聚焦唯一最终效果窗口。媒体效果由 N/N+1/N+2 候选文件链处理；候选失败、超时、过期或不同步时继续当前 N 或源媒体，不得暂停、静音、黑屏或重新弹出窗口。

桌面主界面按用户参考图重做为深色高密度三栏，但只复刻视觉结构和信息层级：`36px` 顶栏；`1728×1044` 同视口下为 `320px / minmax(0, 1fr) / 272px`、`12px` 外边距/栏间距；DOM/键盘顺序为素材 → 视频/参数 → 普通声音/输出；三栏独立滚动并支持窄屏降级。六张顶部状态卡只显示播放、循环、视频处理、普通声音处理、源规格和最终效果窗口的真实状态。

### 2.2 明确不做

- 不生成 N 个离线视频或话术版本。
- 不建立生成版本队列或批量 MP4 合成；本地多视频顺序只消费用户整批选择的源文件，不得复用旧版本队列语义。
- 不保留 `prepare → transcribe → rewrite → tts → align → mux` 作为当前产品业务链路。
- 除用户指定 ZLMediaKit 的 RTMP/RTMPS 直接发布外，不接 OBS、抖音/快手账号、平台自动发布或多平台推流。
- 不做运行时 Chromaprint/landmark 重复检测。
- 不写入或采集物理摄像头，不捕获桌面/第三方窗口，不提供虚拟麦克风、虚拟摄像头 4K/60fps/多实例或端到端 GPU 零拷贝承诺。
- 不做服务端模型代理；本版本也不启动实时话术模型链路，Go 不转发实时话术正文、视频或完整音频。
- 不保留 `speech_preview_task` 单段旧试听链路。
- 当前已实现 UI 不展示实时话术幻化、speech-to-speech、旧报告/变体 MP4 产物、内容指纹、OBS、平台推流或检测规避指标；ZLMediaKit RTMP/RTMPS 卡片只在 Phase 6K 实施时按真实状态加入。`65Hz–20000Hz` 视觉频段、像素/帧率扰动等参考图参数属于正式需求，但只有真实映射后才能进入运行态。

### 2.3 必须保留

- 用户登录、Refresh、退出、权限和审计。
- 设备激活、激活码、设备基础信息、心跳和离线播放。
- 桌面端显式记住/清除账号和密码：账号只作普通偏好，密码只进系统 Keychain/Credential Manager；桌面端不输入、保存或提交激活码并尽力清除旧凭据，自动填充不自动提交，工作台入口继续由有效会话和当前设备账号授权成功共同门禁。
- Go 模型账号登记、OpenAI-compatible 账号连通性测试、Secret Store、单账号短期直连租约和调用摘要。
- Rust 直连模型能力仅作后续版本预留；本版本不使用实时话术模型租约，完整 API Key 不进入客户端。
- 当前进程内最多 100 项的本地有序播放池、文件选择/系统拖放和成员原子 CRUD、单窗口/一个活动源、暂停、继续、停止、`A→B→…→A`、单项自循环，以及受支持格式本地媒体的整批探测/完整文件哈希。本批允许视频与纯音频混排，不得退回仅视频队列或为音频新建第二套窗口/播放器状态机。
- 视频处理和普通声音处理两个当前独立开关；实时话术幻化不属于本版本入口。
- 参考图中的声音、视频、高级视觉、挂件和切片参数均属于当前正式产品需求；旧独立报告任务、媒体鲁棒性/内容指纹对比和平台检测规避结论不属于当前产品。旧 `research_*` 类型、报告 Worker 和 IPC 已删除，禁止重新引入。

## 3. 旧 PRD 代码清理边界

### 3.1 清理原则

- 先删除业务入口，再删除领域类型和实现，再删除测试和无用依赖。
- 删除前必须用 `rg` 检查生产引用、测试引用、Tauri 注册和前端路由。
- 历史 SQL 迁移 `backend/migrations/0001_核心控制面.up.sql` 至 `0009_修正阶段事件唯一键.up.sql` 保留，不修改历史内容。
- `variant_tasks`、`variant_task_events` 和旧模型请求预占表仅作为历史迁移兼容结构保留；新业务代码不得创建、更新或依赖，也不擅自删除已部署数据库中的历史数据。
- 不删除当前仍被登录、激活、模型账号、租约、Keychain、Secret Store 或单窗口播放使用的公共能力；已移除的旧报告代码禁止成为当前产品依赖。

### 3.2 必须删除的旧后端代码

涉及文件范围：

- `backend/internal/service/model_proxy.go`
- `backend/internal/service/model_proxy_test.go`
- `backend/internal/service/model_proxy_postgres_integration_test.go`
- `backend/internal/service/model_proxy_recovery_test.go`
- `backend/internal/service/variant_task_lease_test.go`
- `backend/internal/controlplane/types.go` 中的旧 LLM Chat、变体任务、版本计划、阶段事件和旧产物类型
- `backend/internal/controlplane/errors.go` 中仅服务旧媒体任务/服务端代理的错误码
- `backend/internal/service/controlplane.go` 中的变体任务、版本阶段、旧 rewrite 租约绑定、版本数量计算和旧阶段辅助函数
- `backend/internal/httpapi/controlplane.go` 中的变体任务、旧 LLM Chat、旧阶段事件和旧媒体任务路由
- `backend/internal/store/memory.go` 中的 `Tasks`、`ModelProxyRequests`、阶段事件指纹、版本事件指纹及旧模型预占状态
- `backend/internal/store/postgres_repository.go` 中面向旧任务影子表、旧代理请求和版本事件的写入逻辑
- 仅验证上述旧业务的 Go 单元测试和集成测试

不得直接删除当前模型账号、租约和用量基础结构；它们要迁移为“账号测试 + direct_lease + Rust 调用摘要”。

### 3.3 必须删除的旧 Rust 代码

删除旧阶段和版本生成链：

- `desktop/src-tauri/src/align_worker.rs`
- `desktop/src-tauri/src/transcribe_worker.rs`
- `desktop/src-tauri/src/tts_worker.rs`
- `desktop/src-tauri/src/stage_pipeline.rs`
- `desktop/src-tauri/src/variant_manifest.rs`
- `desktop/src-tauri/src/variant_pipeline.rs`
- `desktop/src-tauri/src/variant_plan.rs`
- `desktop/src-tauri/src/speech_preview.rs`
- `desktop/src-tauri/src/continuous_strategy.rs` 中只服务生成版本下一项预加载/版本队列的逻辑；不得误删当前 `source_media_pool` 的用户源文件顺序推进
- `desktop/src-tauri/src/media_engine.rs` 中只服务旧版本产物命名/队列的逻辑；当前播放池条目的本地重编码、缓存提交和哈希校验必须保留
- `desktop/src-tauri/src/commands.rs` 中的 `speech_preview_task`、`cancel_speech_preview_task`、`prepare_variant_task`、`transcribe_variant_task`、`tts_variant_task`、`align_variant_task`、`mux_variant_task`、`render_variant_task`、旧阶段取消、版本脚本和版本队列命令
- `desktop/src-tauri/src/main.rs` 与 `lib.rs` 对应模块注册和 Tauri handler 注册

保留并重构：`playback`、`media_library`、媒体处理/哈希、`auth_session`、设备信息、Keychain 和单窗口命令。正式参数模块使用 `media_effect_params`；旧 `research_params`、`research_worker` 及相关 IPC 已删除，禁止重新引入。`playback/queue.rs` 如果只服务生成版本队列则应删除或收口；当前用户源文件有序池必须使用实际快照字段 `source_media_pool`、`source_media_index`、`playback_pool_cycle`、`playback_generation`，不得重新引入生成版本队列接口。

### 3.4 必须删除的旧 React/管理端代码

- 删除 `admin-web/src/features/variant-tasks/VariantTasksPage.tsx`。
- 从 `admin-web/src/app/router.tsx`、`admin-web/src/components/AppLayout.tsx` 删除变体任务入口、图标和路由。
- 从 `admin-web/src/types/api.ts` 删除 `VariantTask`、版本、阶段事件、旧媒体任务响应类型。
- 删除只服务变体任务的前端测试和无用导入。
- `desktop/ui/src/App.tsx` 删除旧 PRD 的任务创建、N 计算、版本脚本、批量 rewrite/TTS/align/mux、版本队列、旧租约绑定和 speech preview UI。
- `desktop/ui/src/controlPlaneClient.ts` 删除旧媒体任务和服务端模型代理调用；保留登录、设备、心跳、租约生命周期，并新增后续 direct call summary 调用位置。

### 3.5 数据库处理

- 保留全部历史 migration 文件，保证已有数据库可以升级和审计。
- 新代码不得创建、更新或依赖 `variant_tasks`、`variant_task_events`。
- 新代码不得以服务端模型代理请求表作为模型调用事实源。
- 后续新增迁移应为当前模型账号测试、直连租约、客户端调用摘要和审计服务；迁移必须单独评审。

## 4. 长任务阶段

### Phase 0：旧 PRD 代码清理与基线恢复

目标：删除旧 PRD 的媒体任务、N 版本、服务端代理、旧预览和版本队列入口，三端恢复可编译、可测试的最小骨架。

主要任务：

- [x] 删除 Go 旧任务/阶段/代理类型、服务、路由、测试和内存状态。
- [x] 删除 Rust 旧 Worker、旧预览命令、旧版本脚本、版本队列命令和 Tauri 注册。
- [x] 删除管理端变体任务页面、路由和类型。
- [x] 删除桌面端旧 PRD 任务编排、N 计算、版本队列、speech preview UI 和无调用的任务互斥辅助模块。
- [x] 更新错误码、OpenAPI、共享类型和数据库说明，使其只描述当前能力。
- [x] 删除由清理暴露出的未使用依赖、导入、类型、配置和测试脚本入口。

退出标准：

- `rg` 不再发现生产代码中的 `VariantTask`、`ProxyLLMChat`、`speech_preview_task`、`prepare_variant_task`、`set_playback_queue`、`append_playback_queue` 等旧入口。
- 后端、Rust 核心、桌面端 UI、管理端均能完成各自基础检查。
- 历史 migration 和历史文档仍可追溯，但不再被当前路由或服务调用。

### Phase 1：Go 控制面当前模型闭环

目标：实现 Go 账号登记、账号连通性测试、短期单账号直连租约、调用摘要和审计，不转发模型内容。

主要任务：

- [x] 新增 `POST /api/v1/admin/model-pool/{account_id}/test` 的服务层和 HTTP 路由；使用 Secret Store 调用 OpenAI-compatible `/models`，不返回密钥和完整响应。
- [x] 固定 `ModelLease.proxy_mode=direct_lease`，返回账号允许的 `direct_base_url`；短期 `direct_access_token` 需要供应商支持凭证委托，当前未伪造或泄露完整 API Key。
- [x] 新增 `POST /api/v1/client/llm/call-records`，记录 `client_call_id`、租约、provider/model、usage、延迟、状态和错误摘要，并支持幂等。
- [x] 新增 `GET /api/v1/admin/model-usage`，为管理端提供调用摘要分页查询。
- [x] Secret Store 只由 Go 读取完整 API Key；响应、日志和审计不得写入密钥或模型正文。
- [x] 将账号健康、并发、额度和租约状态与 direct call summary 建立一致性测试。
- [x] 更新 PostgreSQL 规范化读写计划，账号活动租约数和当日已用 token 已同步到规范化模型账号字段；旧媒体任务和模型请求预占只保留为历史迁移兼容结构。

退出标准：管理员可以测试账号；客户端可以获得单账号短期租约；Go 已提供幂等调用摘要接口，待 Rust 直连凭证委托和可信租约注入完成后再验收“直连调用后自动上报摘要”；Go 不存在模型正文代理路径。

### Phase 2：Rust 单源单窗口播放核心

> 本节保留此前单源核心的历史完成事实；当前最多 100 项播放池增量以 Phase 5A 为准，不把旧勾选项改写为新功能已完成。

目标：播放器只维护一个 `SourceMedia` 和一个窗口，播放结束回到 0 秒并递增 `loop_index`。

主要任务：

- [x] 删除多项播放队列、下一项等待和版本切换状态。
- [x] 建立单源播放快照：源媒体、播放轮次、播放状态、视频/普通声音处理开关、当前音频来源、现有 MP4/音频哈希和回退原因；通用源文件哈希及条件 MP4 专用字段仍需随多格式 IPC 实现迁移。
- [x] 首项完成可读性/元数据探测后保持 `Ready`，用户点击播放才开始原声播放；哈希异步计算，处理提交前强制完成。
- [ ] 保留同一窗口的播放、暂停、继续、停止、断网继续和窗口关闭行为；播放控制与单实例窗口已实现，断网继续和窗口关闭手工验收待补。
- [x] Rust IPC 只暴露当前文档定义的单源播放命令。

退出标准：几分钟、30 分钟和 1 小时素材可在单窗口中循环；`.mp4`、`.mov`、`.mkv`、`.avi`、`.webm`、`.m4v`、`.ts`、`.m2ts`、`.flv`、`.wmv`、`.3gp` 逐一通过导入、探测并验证直放能力或明确错误链路；不创建第二窗口、不生成下一项、不等待旧任务。

### Phase 3：声音处理、插话与固定话术

目标：普通声音处理与视频效果写入同一 N/N+1 候选文件，并完成插话叠加和固定话术系统朗读；实时话术幻化不在本版本实施。

主要任务：

- [x] 为普通声音处理建立独立开关、参数版本和当前音轨配置接口；普通音频执行 FFmpeg 基础/频域/第二输入/干湿滤镜、Signalsmith Stretch 音高/共振峰、realFFT/rustdct MFCC 与滚动 RMS SNR 处理，Web Audio 只展示 Rust 最终混音只读诊断；独立压缩因没有正式字段显示 `unavailable` 并保持原音轨。
- [ ] 普通声音周期迁移到 Phase 6F 的统一 N/N+1 文件候选；复用绝对媒体时间、generation/revision 和候选失败继续当前媒体的有效约束。
- [ ] （后续版本，不纳入当前）为 speech-to-speech Worker 建立本地受控输入、输出、超时、取消和能力探测契约；历史代码和适配器仅作迁移审计，本版本不启动。
- [x] 废止并删除本地固定话术 voice clone 链路：移除 `AUTOLIVE_VOICE_CLONE_WORKER`、Python/模型依赖、Rust IPC/状态和语音运行资源；固定话术改为 WebView2 本地 `speechSynthesis`，只使用 `localService` 系统声音，前端完成文本校验、BroadcastChannel 状态、静音租约、取消和恢复。Windows release EXE 的本地中文朗读烟测仍是 Phase 6 发布门禁。
- [x] 收口随机插话独立音轨与双出口：普通声音和随机插话引用同一份 22 套精确 35 字段预设定义，并各自持有选择状态和运行快照；默认随机池为 `p01–p20`，`p21/p22` 只有显式勾选或固定选择后参与。插话目录复用统一媒体白名单，支持 6 种音频和 11 种视频容器；视频只映射音频流，PortAudio 先生成静音结束/暂停时钟缓存再从原文件音轨输出，视频无音轨或缓存失败时跳过本次并保持主轨。PortAudio/FFmpeg 维持一个处理图、一个受管任务、一个硬件输出和最多四条实际支路；WebView 使用同一预设快照和环境声解析结果生成短期处理缓存，音频来源生成、校验或播放失败时可明确回退该段原声。自然结束释放当前缓存，停止、切换素材、出口切换、窗口卸载和启动过期清理均取消任务并删除缓存；不预生成 22 套组合，不引入实时话术幻化。Windows 双出口连续播放和异常退出残留清理仍列入手工门禁。
- [x] 统一插话时间参数边界：触发间隔在 UI 中以秒显示/输入，范围 `0.5–60s`、默认 `8–13s`，提交和快照回显换算到 `interval_min_ms/interval_max_ms=500–60000ms`；`ducking_attack_ms`、`ducking_release_ms` 分别接受 `0–1000ms`、`0–3000ms`，默认 `50/250ms`，`0` 分别立即压低/恢复主轨。
- [ ] 将 `audio_variation_period_min_ms/max_ms` 独立扩展为 `1000–600000ms`，按当前插话媒体时间在同一文件内切换声音预设；WebView 与 PortAudio 均限制为 current + pending，失败保留 current。主页显示当前随机文件基本名称，并把独立插话音量放入“插话声音周期”卡片；播放控制媒体音量只作用于主媒体。
- [ ] （后续版本，不纳入当前）Rust 使用具备供应商凭证委托的 direct lease 直连 LLM；本版本不发起实时话术模型调用。
- [ ] （后续版本，不纳入当前）设计实时话术候选缓冲层、候选门禁和沿用/改写约束；本版本不生成或切换话术候选。

退出标准：普通声音处理不阻塞首帧；普通声音与随机插话逐轮使用同一完整预设定义。`p01–p20` 是默认低感知池，`p21/p22` 只显式参与。插话文件随机周期与预设变化周期分别验收；30 分钟文件固定 10 分钟预设周期时只随机一次文件，并在 `0/10/20` 分钟应用预设，暂停期间边界冻结。媒体音量和插话音量互不影响；WebView/PortAudio 候选失败均保持当前可听输出。自然结束、停止、切换素材、出口切换、卸载和过期清理按所有权释放缓存与任务。本版本不验收实时话术换轨。

### Phase 4：视频处理与参数面板

目标：视频处理开关、声音处理开关与当前播放对象解耦；正式媒体参数分级接入当前产品链路，未接入字段不伪造运行态。

主要任务：

- [ ] FFprobe 只负责媒体导入探测，FFmpeg 只保留普通声音候选等既有职责；视频导入和参数周期都不再生成 `.partial/.ready`、重编码产物或统一音视频文件候选。Windows 原生验收和完整参数映射按唯一 mpv/libplacebo 方案执行。
- [x] 保留并整理当前已接入的亮度、饱和度、对比度、色相、锐化、模糊、噪点、细节增强、动态裁剪、像素扰动、X/Y 空间偏移和缩放等基础视频参数。
- [ ] 裁剪边缘平滑、帧率/帧内/帧间扰动、色彩空间转换、波频、通道/空间偏移、帧扰动概率、随机几何、抽象几何脸、同源画中画、切片调度和局部模糊等正式字段已建模；只有进入生产 mpv shader/调度且通过逐字段门禁的字段才可勾选完成。旧打包 FFmpeg 滤镜证据不再算当前生产接入证据。
- [ ] 固定 `65Hz–20000Hz` 视频空间频段、目标/核心频率、动态均衡、边缘柔化、源帧率锁定、源片段最小长度、边缘羽化和变换平滑仍按 Phase 3A/3B 候选与调度门禁逐项晋级；画中画时间轴解锁属于 Phase 3C，当前明确 `not_admitted`，不得标记完成。
- [ ] 61 项 shader 与 18 项受限 PTS 调度已进入当前开发生产链 `79/83`；2 项颜色候选等待帧级原子提交、完整开关语义和 HDR/驱动验证，`slice_min_length_ms` 与画中画时间轴解锁两项跨帧历史保持未接入。79 项仍须完成 1080p60 周期更新、联合媒体和长稳门禁。
- [ ] 普通声音参数契约保留，执行出口改为 FFmpeg → Signalsmith Stretch → realFFT/rustdct → 独立声音候选文件；未接入字段继续保持 `unavailable`。
- [x] 将波形、频谱、响度、噪声底、MFCC、SNR 和 LPC F1/F2/F3 接入最终混音只读诊断；频谱扰动、频谱盲区和 6kHz 以上高频扰动进入真实 FFmpeg 链，`current_formant_hz` 只读投影 F1，旧报告任务不纳入参数面板。
- [ ] 视频自动低感知模式每轮只生成完整 `VideoEffectParams + AdvancedEffectParams` 参数快照；生产由受管 mpv 原子更新，不重新编码、不生成视频产物。画中画、挂件、随机图形和抽象图形只有完成 shader/历史纹理准入后才能参与生产随机化；当前以 capability 和真实帧证据证明生效，不能只改 CSS、UI 数值或复用旧 FFmpeg `applied_fields`。
- [x] 收口最终视频显示与微量映射边界：处理缓存和源视频回退都不在 `<video>` 上追加 CSS 色彩/模糊/位移/缩放，布局 CSS 只填充单一媒体表面；整数像素位移在自动模式固定为稳定方向，避免跨周期正负取整跳变；滤镜强度和帧概率不再经取整、固定下限或错误整数采样放大，底层无法忠实表达时保持中性/旁路或使用明确精度策略。
- [x] 声音预设收口为 22 套、每套精确 35 个预设可写字段；播放速度、高质量音高、共振峰偏移、MFCC 偏移/维度、SNR 浮动、频谱盲区、干湿比、环境声混合和编码字段都参加真实处理。“预设可写”不等于“用户可手动编辑”；上述 9 项继续参加普通声音周期随机化。`random_change_period_ms` 为预设外全局调度结果，`current_formant_hz` 为只读诊断；目标 SNR 的 `null` 表示自动。
- [x] 收口环境声双来源门禁：`ambient_sound_mix_percent = 0` 时不加载第二输入；值大于 `0` 时用户本地素材可选且优先，未选择时使用随包内置真实环境声。两种来源均完成路径、格式、可读性和 FFmpeg 解码验证，失败保持原声并返回可恢复错误；普通声音、随机插话和 WebView/PortAudio 契约测试覆盖该边界。

退出标准：关闭任何一个开关不影响其他链路；声音面板能够区分配置、只读诊断和 `disabled/configured/runtime/unavailable` 状态；媒体处理失败时保持当前可用视频/音轨并显示结构化原因。

### Phase 5：React 管理端和 Rust 桌面端当前 UI

目标：界面只呈现当前产品，不再展示旧 PRD 的任务、N 版本、阶段进度和模型代理。

主要任务：

- [x] 管理端完成用户、设备、激活码、模型账号测试、租约和调用摘要页面的当前最小闭环。
- [x] 桌面端接入控制面启动门禁：设备标识、账号密码登录/Refresh、账号激活授权自动设备绑定、系统钥匙串 Refresh Token、退出清理和登录后 Heartbeat/outbox；服务端加密主密钥和模型 API Key 不进入桌面端。
- [x] 历史能力：接入记住账号密码，账号仅写 LocalStore、密码写系统 Keychain/Credential Manager；该能力将由 Phase 6B 的“记住账号并保持登录”替换，不作为目标态继续保留。
- [x] 门禁表单只保留账号和密码；登录后查询当前设备，必要时由服务端按该账号有效授权自动绑定，失败保持登录页并展示稳定原因，服务端未确认 Ready 时不进入工作台。
- [x] 收口凭据反馈生命周期：保存账号密码和清除凭据的成功提示 `4` 秒自动消失，新操作清理旧成功提示，旧计时器以提示代际保护且在组件卸载时取消；警告/错误不自动隐藏。Phase 6B 保留提示生命周期，但删除密码保存语义。
- [ ] 在 Phase 6B 完成后，于 Windows 真机验证 Credential Manager 的 Refresh Token 写入/轮换/重启恢复/删除、旧密码凭据清理和凭据服务拒绝权限提示；不得再验证或恢复密码持久化。
- [ ] 收紧工作台双门禁：Refresh 恢复、登录后跳转、深链、后退、直接 Hash 和冷启动都必须同时验证有效会话与当前 `user_id + device_id` 账号授权成功；历史缓存和已记住信息不得放行。覆盖账号无授权、授权过期、设备额度已满、绑定冲突、设备未激活/禁用及网络无法确认，并保留稳定错误码。
- [x] 桌面端完成素材导入、单窗口播放、视频/普通声音处理开关、播放状态、当前音频来源、异步哈希和回退原因的当前最小闭环。
- [x] 画面 Card 不提供手动“应用处理”入口；视频处理由独立开关启用，开启后按既有随机周期自动生成完整参数快照并在受控媒体边界应用，不改变处理链、参数、失败回退或重试语义。
- [ ] （后续版本，不纳入当前）话术实时幻化展示 Worker 能力状态/原因、候选槽、决策、换轨结果和失败原因；当前 UI 不展示该能力。
- [x] 历史实现已使用 Ant Design 完成默认值读取、参数卡/诊断/周期状态只读展示、Rust 校验和两个具名声音分组；当时将分组整体作为正式编辑入口的口径已由 2026-08-25 最新决策覆盖。
- [x] 历史基线已用全量正式参数卡覆盖 Rust 三个参数结构，并完成紧凑分段进度、三态与编辑权限分离及旧闪动死代码清理；当时主页两个具名声音配置区可编辑的口径已废止。
- [x] 按 2026-08-25 最新决策移除 MFCC 偏移、MFCC 维度、SNR 浮动、频谱盲区、干湿比、环境声混合、高质量音高、播放速度和共振峰偏移的全部手动输入与高级声音抽屉重复入口，保留只读展示；9 项继续由普通声音预设/周期随机化更新，更新后刷新展示并复用既有范围/组合校验、N+1 候选准备与提交、失败回退和运行状态链路。
- [x] 历史布局已将参数内容区调整为约 `880px` 居中和双主栏，左栏依次为普通视频、高级视觉，右栏为普通声音；普通卡当时采用目标宽度约 `270–290px`，并完成紧凑高度、至少 `12px` 说明、三列固定视觉频段、随机分类色及显示值变化约 `290ms` 高亮。聚焦契约测试 `12/12`、桌面 UI 全量测试 `241/241` 与 TypeScript 类型检查已通过；该完成事实保留，但固定/目标卡宽验收已由下一项最新响应式任务覆盖。
- [x] 历史约 `880px` 居中实现已完成自动列数响应式与新卡片内容：外层双主栏、极窄单栏、无底部说明、固定视觉频段三列、分类色和三态规则均已落地；聚焦契约测试 `13/13`、桌面 UI 全量测试 `242/242`、TypeScript 类型检查和本地生产构建已通过。该完成事实保留，但约 `880px` 居中宽度与当时未明确的卡片高度已由下一项最新满宽任务覆盖。
- [x] 参数内容区已改为填满桌面中栏实际可用宽度，并删除约 `880px`、`860–900px` 及其他最大宽度上限，避免宽中栏两侧出现大面积空白；栏内自动网格随窗口放大/缩小增加或减少列数，外层继续使用左侧普通视频与高级视觉、右侧普通声音双主栏，极窄时按 DOM 顺序降为单栏，全程不横向裁剪。普通参数卡固定为统一 `80px` 高度，固定视觉频段权重卡固定为独立 `196px` 高度并跨满所属栏；无底部说明、三列完整显示、本项完成时的分类色/约 `290ms` 高亮/减少动态效果、三态和正式参数事实规则均保持；分类色已由下一项逐卡颜色决策覆盖。聚焦契约测试 `13/13`、桌面 UI 全量测试 `242/242`、TypeScript 类型检查和本地生产构建已通过，未扩展实时话术幻化。
- [x] 历史完成事实：参数面板颜色分配已改为逐卡/逐独立模块，每张参数卡或独立参数模块分别从参考图五色色板分配自己的颜色；五色可循环复用，分配顺序尽量避免视觉相邻项同色，单次挂载期内逐卡映射稳定。当时显示 `value` 变化使用约 `290ms` 高亮，聚焦契约测试 `13/13`、桌面 UI 全量测试 `242/242`、TypeScript 类型检查和本地生产构建已通过。颜色分配事实继续有效，历史动效已由 2026-08-27 主页性能口径覆盖；满宽、自动网格、普通卡 `80px`、频段卡 `196px`、无底部说明与三态规则不变，不扩展实时话术幻化。
- [x] 主参数区已同时展示视频周期与声音周期两个真实状态模块：两者分别显示配置范围、真实变化次数并按各自计划计算进度；声音状态标签读取真实 `audio processing status`。当时实现过的联动目标已由 2026-08-27 决策废止，后续只验收两套独立计划。
- [x] 历史完成事实：参数显示 `value` 变化后的逐卡颜色高亮曾从约 `290ms` 调整为约 `900ms`，卡片伪元素持有边框/光晕并对透明度执行动画；当时聚焦参数面板测试 `13/13`、桌面 UI 全量测试 `243/243`、桌面布局契约测试 `11/11`、TypeScript 类型检查和本地生产构建均通过。该实现已由 2026-08-27 主页性能口径覆盖，不再作为当前验收。
- [x] 按 2026-08-27 主页性能口径移除媒体参数卡与插话预设参数行的上一值签名、A/B 闪烁代次、动画类、发光伪元素和关键帧；保留静态逐卡强调色、当前值与状态文字。原始诊断和主波形保持最多 `4Hz`，进入 React 状态树的诊断摘要最多每秒一次；参数面板、分区、单卡和插话参数行以稳定输入隔离诊断消息、播放时钟及其他无关主页更新。本轮按用户要求暂不执行相关 UI 测试、diff、构建和 TypeScript 类型检查，自动化与实机性能对照仍保留在后续验收项。
- [x] PortAudio 继续以真实设备枚举为事实源；当前 UI 在右栏话术功能上方提供 Host API 筛选、输出设备选择、`128–2048 KiB` 内存缓冲和显式应用，编辑草稿与已应用/运行状态分离，且不伪造 ASIO 可用能力。
- [x] 核心播放、媒体处理和缓存操作的 loading、error、disabled、unavailable 状态可解释且不伪造成功；历史研究页面不属于当前产品入口。
- [x] 完成多格式导入 UI 与 `probe_local_video` FFprobe 探测 IPC；通用异步哈希 IPC、自动兼容预览和各格式实机播放验收仍待后续阶段。
- [x] 播放池标题区增加带文字的常驻“导入视频”，空状态增加“选择视频文件”主按钮，已有素材增加“导入/替换视频”；三个入口复用现有 `importVideo()`，不新增第二套文件选择或探测逻辑。
- [x] 移除桌面 UI 独立“运行资源”卡片及 FFmpeg/FFprobe 安装、取消、选择目录、清理资源按钮；后台资源准备、安装包打包和内部能力探测继续保留，失败通过导入/处理错误提示。
- [x] 按参考图映射完成桌面主界面深色高密度三栏：`36px` 顶栏，`1728×1044` 下 `320px / 1fr / 272px` 三列和 `12px` 边距/栏距，三栏独立滚动，DOM/键盘顺序为素材 → 视频/参数 → 普通声音/输出；`≤1199px` 时外层工作区按同一顺序重排，使 `960px` 最小窗口能够命中窄屏规则。
- [x] 将顶部概览收敛为六张真实状态卡；左栏保持单源素材会话和完整播放控制；中栏承载音频/视频/高级全量正式参数面板；右栏承载最终效果窗口、普通声音实际出口/诊断和真实处理链，插话与固定话术使用紧凑入口/抽屉。
- [x] 删除当前桌面 UI 中与实时话术幻化、speech-to-speech、旧报告/变体 MP4、内容指纹、OBS/RTMP 和检测规避有关的入口、文案和伪状态。
- [x] 已重新展示全部正式媒体参数的能力状态：已接入、正式需求待实现、待确认都可发现并显示原因，三态只表达算法能力，不决定编辑权限；参数卡、诊断和周期状态不提供用户输入。主页声音列两个具名分组当时提供正式编辑入口的历史实现已完成，但该编辑口径已由上方 2026-08-25 最新任务覆盖；显示值继续由默认值、现有调度和媒体引擎状态更新，目标 SNR 的 `null` 显示“自动”。
- [x] 统一高级声音、随机插话和固定话术三个抽屉：桌面目标宽度分别为 `760px/560px/560px` 且不超过 `100vw`；采用深色中性主题、双行标题/真实状态摘要、卡片分区、正文独立滚动和固定底部主操作；表单标签持续可见，窄屏字段单列，删除用户预设必须确认。
- [x] 修复工作台账号/退出、激活有效期和会话提示 fixed 层覆盖声音抽屉：页面状态层降到 Ant Design 弹层之下，三个功能抽屉使用公开属性明确固定头尾、正文滚动、自动聚焦和 Esc 关闭；不修改随机插话保存、播放或错误处理逻辑。
- [ ] 手工验证三个声音抽屉的桌面/窄屏宽度、滚动与固定区域、键盘焦点、真实状态摘要、持续标签、单列降级和删除预设确认，并确认普通声音/插话/本地系统固定话术边界未被实时话术幻化或旧报告入口扩展；正式媒体参数按三态展示。
- [ ] 手工验证插话文件抽屉打开后账号/退出、激活有效期和会话提示全部位于遮罩后方，标题/关闭/正文/底部无遮挡；验证长正文滚动、窄屏单列、背景不可点、Tab 焦点约束、Esc 关闭和焦点归还。
- [x] 已删除无可见消费方的旧参数卡闪动 token、预设参与闪动、CSS 动画和对应测试；该清理事实保留。当前逐卡颜色显示值反馈必须以最小新状态实现，不复用旧 token；`audio_stream_params/audio_stream_variants` 继续服务于已提交普通声音支路的独立状态区，不是全量参数面板的唯一事实源，也不得从数值反推预设。
- [ ] 手工验证主参数区同屏显示视频周期和声音周期两个状态模块：分别核对配置范围、真实变化次数、各自计划目标时间、周期长度和进度；核对声音标签与真实 `audio processing status` 一致。确认不存在联动目标或区间求交，修改任一周期不会重建另一领域计划；处理关闭、暂停、未启用或计划缺失时进度归零且非活动。
- [ ] 手工验证全量参数主面板滚动、键盘和系统缩放：参数内容区填满桌面中栏实际可用宽度，不受约 `880px`、`860–900px` 或其他最大宽度限制，宽中栏两侧没有由参数容器上限造成的大面积空白；外层双主栏保持“左：普通视频 + 高级视觉 / 右：普通声音”。每个主栏内部卡网格根据实际可用宽度自动决定列数并自然换行，不限制每排数量；逐步放大与缩小窗口时验证列数同步增加或减少，极窄时外层双主栏按 DOM 顺序降为单列，全程无固定列数、固定/目标 `270–290px` 卡宽和横向裁剪。验证普通参数卡固定为统一 `80px` 高度，只显示参数名、当前值/无手柄紧凑分段进度和“已接入 / 正式需求待实现 / 待确认”三态标签，完全不存在底部范围、单位、接入情况或待实现原因说明句；固定视觉频段权重卡固定为独立 `196px` 高度、跨满所属栏、三列完整且无裁剪。覆盖 `1728×1044`、`1280×800`、`1200×760`、`960×680` 及 Windows `125%/150%`，确认满宽、自动列数、自然换行、外层单列降级、普通卡统一高度、频段卡跨栏、进度不裁剪且不提前满格；验证范围、默认值和能力状态契约仍参与进度、校验及媒体边界。验证每张参数卡或独立参数模块从参考图五色色板获得自己的静态强调色，不按普通视频、高级视觉、普通声音大分类固定色；允许五色循环复用，分配顺序尽量避免视觉相邻项同色，且单次挂载期间逐卡映射稳定。参数卡和插话预设参数行的值变化只立即更新内容，不出现 A/B 闪烁类、光晕/发光伪元素或关键帧；颜色不表达算法状态，三态不决定编辑权限。验证只有实际消费值变化的卡片重新渲染，原始诊断、播放时钟和其他无关主页状态不带动全量参数卡。同时验证上述 9 项不存在滑块、输入框、步进器、`onChange` 或抽屉重复入口；普通声音预设/周期随机化改变其值后刷新展示，并继续通过既有校验、N+1 候选准备/提交、失败回退和运行状态更新。另验证原始诊断和主波形保持最多 `4Hz`，React 诊断摘要最多每秒提交一次，诊断停止后按既有新鲜度边界切换，页面隐藏/通道关闭/卸载无残留定时器或订阅；并验证调度/引擎状态更新、错误反馈、真实效果听感/画面，以及凭据成功提示 `4` 秒消失、新操作清理、旧计时器不清后续提示、警告/错误常驻及卸载清理。
- [ ] 手工验证随机插话在 WebView 与 PortAudio 实际出口都可听：普通声音和插话引用同一份 22 套精确 35 字段预设定义，默认随机池为 `p01–p20`，`p21/p22` 只有显式选择后参与；目标 SNR 显示自动，环境声来源显示用户素材、随包内置真实环境声或零值未加载。逐一覆盖 6 种音频与 11 种视频扩展名，并覆盖含音轨、无音轨和损坏/不可解码视频；确认视频只产生声音，不显示画面、不进入视频处理，失败时保持主轨且无残留 duck。验证 `ambient_sound_mix_percent=0` 时不加载第二输入；非零时分别覆盖未选择用户素材而使用随包来源，以及用户素材优先覆盖。两种来源都覆盖路径/资源越界、格式不支持、不可读和 FFmpeg 解码失败，所有失败保持原声并返回可恢复原因。PortAudio 覆盖固定单轨、随机单轨、最多四支路单图混合、同流 duck 以及视频来源静音时钟缓存；WebView 覆盖短期处理缓存、音频来源明确原声回退和无双播。覆盖暂停/继续、自然结束、停止、配置变化、切换素材、出口切换、窗口卸载、固定话术抢占及异常退出遗留缓存清理，确认任务、缓存、asset scope 和 duck 均按所有权释放。
- [ ] 收口并验证随机插话保存边界：有效配置只提交一次并进入同一 playback snapshot，只标记下一段应用新固定选择或重新抽样，不调用 `StopInterlude`，不使当前 generation 失效；当前插话继续使用启动时快照。两种模式都保存并校验 `audio_preset_ids`，目录、范围、池为空/重复/非法项、asset scope 和锁损坏仍拒绝保存并保留当前插话，React 展示 Tauri 结构化 `message`。
- [ ] 验证随机插话触发间隔 UI 仅显示/输入秒，`0.5s/60s` 边界及 `8s/13s` 默认值准确映射为毫秒 IPC；验证 duck 攻击/释放 `0ms` 的立即压低/恢复语义、`1000ms/3000ms` 上界，以及 Rust 对越界值和间隔倒序的稳定拒绝。

退出标准：管理端和桌面端不出现旧任务/版本/代理或实时话术入口；桌面门禁、布局、三个声音抽屉和全量参数面板通过既有验收。主页声音列的两个具名分组不整体标为编辑区，上述 9 项只读展示且高级声音抽屉不重复输入；普通声音与随机插话共用 22×35 预设事实源，默认池为 `p01–p20`，`p21/p22` 仅显式参与。9 项继续参加普通声音预设/周期随机化，变化后刷新展示并通过既有校验和运行链路。随机插话通过目录递归、固定/随机选择、最多四支路、PortAudio 单图单任务同流输出、WebView 短期处理缓存与明确原声回退、用户素材优先/随包真实环境声默认的双来源验证、抢占/失败回退和有界资源清理检查；`ambient_sound_mix_percent=0` 不加载第二输入，非零来源验证失败时保持原声并报可恢复错误。周期到期或配置变化只影响下一段，不预生成组合或常驻多任务。

本轮参数面板退出标准以当前满宽、自动网格、卡片内容、逐卡静态颜色和稳定渲染决策为准：正式字段必须全量可发现，左侧为普通视频与高级视觉、右侧为普通声音；参数卡、诊断和周期状态只读，主页声音列的“音高、变速与共振峰”“特征、信噪比与空间混合”不整体定义为编辑区，上述 9 项只读展示。普通卡固定 `80px`，固定视觉频段卡固定 `196px` 并跨满所属栏；极窄时按 DOM 顺序单列，不固定列数或卡宽且不横向裁剪。三态只表达算法能力，显示值由默认值、普通声音预设、周期随机化和正式快照更新；逐卡颜色映射稳定且只作静态强调，值变化不执行闪烁、光晕、发光阴影或关键帧。未变化的参数卡不因诊断消息、播放时钟和其他无关主页状态重渲染；原始诊断与主波形最多 `4Hz`，React 诊断摘要最多每秒一次。随机化结果不得绕过既有校验、候选准备/提交和运行状态链路；已接入字段必须产生真实输出，Rust 拒绝未接入非默认值。本标准不引入实时话术幻化。

周期状态模块退出标准：主参数区必须同时显示视频周期与声音周期，各自显示配置范围、真实变化次数，并以各自真实计划的目标时间和周期长度计算进度；声音状态标签使用真实 `audio processing status`。两者始终独立推进，不存在联动目标。处理关闭、播放暂停、周期未启用或计划缺失时进度为 `0` 且非活动。

### Phase 5A：本地统一媒体池增量

目标：在不恢复生成版本队列的前提下，将当前进程播放状态扩展为最多 100 项、允许视频和纯音频混排的用户本地源文件有序池，始终复用一个最终效果窗口和一个活动媒体源。本批实施细节见 [`docs/superpowers/plans/2026-08-26-TS视频与纯音频统一播放实施计划.md`](./docs/superpowers/plans/2026-08-26-TS视频与纯音频统一播放实施计划.md)。

主要任务：

- [ ] 验收文件选择/系统拖放整批替换与追加、单项替换、拖拽和键盘重排、单项删除及清空；新增候选按返回顺序逐项探测，取消、超限、重复路径或任一项失败必须保留旧池且不提交部分成功项，仍不提供目录导入或路径/探测元数据手工编辑。
- [ ] 验收 `source_media_pool`、`source_media_index`、`playback_pool_cycle`、`playback_generation` 与 `complete_playback_item`：多项池执行 `A→B→…→A` 并在换源时递增 generation、按新媒体重建绝对时钟；单项池自循环、递增池循环轮次并保持 generation；回归覆盖 `ended/timeupdate/pause` 的不同触发顺序，重复完成事件不得跳项，自然结束产生的内部 `pause` 不得把后端改成 `paused` 或取消完成提交，且首项结束仍须推进 `A→B`。
- [ ] 验收换源时旧媒体、FFmpeg/PortAudio 候选、插话和监听器有界取消/释放；新源从 `0ms` 建立媒体时钟，不延续上一源的 PCM 或候选。
- [ ] 确认播放池、路径、索引、池循环和 generation 只在当前桌面进程内，不写入 LocalStore、不跨重启恢复、不上传 Go，也不创建服务端媒体任务或生成版本队列。
- [x] 实现统一媒体探测 DTO 与白名单：视频保持 11 种格式，纯音频增加 `.mp3`、`.wav`、`.m4a`、`.aac`、`.ogg`、`.flac`；FFprobe 忽略 `attached_pic`，按真实流返回 `media_kind`，并补充 `playback_reference`、`compatibility_mode` 的 Rust/UI 边界测试。2026-08-29 收窄新导入契约：成功项固定 `playback_reference=source_path`、`compatibility_mode=direct`，导入不再执行整段 FFmpeg 视频转码；历史枚举值仅为兼容读取保留。
- [x] 实现 TS/M2TS 兼容缓存：兼容组合优先快速封装，不可靠编码使用现有 H.264 编码器降级和 AAC 转码；输出经 `.partial.mp4`、FFprobe 校验和原子提交，源文件只读。任务具有所有者、总超时、取消、Join 和受限缓存清理，失败或取消不改变旧池。
- [x] 实现纯音频最终窗口纯黑：继续复用主 HTML 媒体时钟，不显示封面、波形、占位图或上一视频残帧；视频处理状态为 `not_applicable` 且 PiP 禁用，普通声音处理、插话、固定话术、音量、静音及播放控制保持现有规则。

退出标准：整批导入、追加、单项替换、重排、删除和清空均可原子提交，成功编辑后非空池首项为 `Ready`、空池为无源状态；视频/纯音频混排可稳定执行 `A→B→…→A`，单项池可持续自循环。取消和失败不破坏旧池，TS/M2TS 不修改源文件且无无引用暂存残留，纯音频保持纯黑但声音链可用，重复结束事件不跳项，换源不重建窗口且始终只有一个活动媒体源；Rust/UI 测试、Windows 手工播放和资源清理证据齐全。

### Phase 6：真实 Worker、性能和稳定性验收

目标：在真实本地环境验证连续播放和资源回收。

主要任务：

- [ ] （后续版本，不纳入当前）配置并验证 Hugging Face `speech-to-speech` 或兼容本地 Worker。
- [ ] （后续版本，不纳入当前）验证中文 ASR、LLM 改写、TTS 和候选音频同步。
- [ ] 验证短视频、几分钟、30 分钟、1 小时视频的首帧和循环连续性，并逐一覆盖 `.mp4`、`.mov`、`.mkv`、`.avi`、`.webm`、`.m4v`、`.flv`、`.wmv`、`.3gp` 的探测、直放能力或明确错误路径；TS/M2TS 另覆盖 H.264/AAC 快速封装、MPEG-2/MP2 等必要转码、无音轨、损坏文件、取消和缓存提交/清理。
- [ ] 在 Windows release 逐一验证 `.mp3`、`.wav`、`.m4a`、`.aac`、`.ogg`、`.flac`，并覆盖带 `attached_pic` 音频、损坏文件及视频 → 音频 → 视频连续两轮混排；纯音频期间必须纯黑、无封面/上一帧/PiP/视频 Worker，普通声音、插话、固定话术和播放控制仍可用。
- [ ] 验证本地媒体 Worker 超时、取消、崩溃和网络断开时的回退；实时话术 Worker 与模型租约回退属于后续版本，不纳入当前验收。
- [x] 建立 FFmpeg/媒体引擎子进程的本地受控执行、超时、取消和回收契约；focused 测试已覆盖参数数组、取消清理、输出冲突、成功产物提交和未映射参数拒绝；三平台资源解析、打包前检查和标准资源名已接入，macOS 安装包资源已验收。仍需在 Windows 原生安装环境验证能力探测和长视频资源占用；不进入推流路径。
- [x] 统一 Windows release 的 FFmpeg/FFprobe 子进程入口：能力探测、素材探测、处理、普通声音、插话和 EOF 重启全部通过共享命令构造器设置 `CREATE_NO_WINDOW`，且 spawn/stderr/退出码/超时/取消错误继续传播。普通声音周期候选使用 `max(3, playback_speed + 1)`、1 秒初始突发的有界 CatchUp 和“prepare 已用时间 + 130–750ms 提交安全尾部”的动态覆盖水位，容量为 `5130–5750ms`；覆盖不足保持 current 并可重试，`1500ms` 真断粮检测及一次恢复/失败回退不变。
- [x] Windows 10/11 x64 NSIS 安装包切换为 WebView2 `offlineInstaller`，并继续内置 FFmpeg/FFprobe、PortAudio 和运行资源清单；正式跨机交付只认 NSIS，便携 ZIP 只作内部测试。测试构建开始前只清理受约束的 NSIS bundle 输出目录，不清理 `target/release`；portable 归档阶段必须恰好检测到一个本轮当前 EXE，零个或多个均失败，避免旧安装器或旧可执行文件混入。NSIS 与 portable/ZIP 都固定包含 `ambient/low-level-room-tone.wav` 和 `ambient/LICENSE.txt`；portable 归档缺少任一文件即失败，且只复制这两项白名单，禁止递归复制其他 `ambient` 文件。
- [ ] 使用受信任 Authenticode 证书签名 Windows 安装器和主 EXE；证书与私钥不得进入仓库。签名后在干净 Windows 10/11 x64 系统离线安装/卸载，并完成登录、激活、导入、播放和最终效果窗口冒烟。
- [ ] 原有未完成媒体运行时验收统一迁移到 Phase 6F，按 N/N+1 文件、单候选 FFmpeg、A/B 切换和清理契约执行。

退出标准：本地播放连续，失败可解释，资源可回收，无多窗口、视频项无意外黑屏、纯音频项按契约纯黑、无空音轨和无隐式旧任务；后续媒体候选与清理验收以 Phase 6F 为准。

### Phase 6A：桌面媒体运行时稳定性治理

实施入口：[`tasks/desktop-media-runtime-stability-implementation.md`](./tasks/desktop-media-runtime-stability-implementation.md)。本段是 2026-08-25 的历史稳定性治理记录；其中 PortAudio、frame/sample 单位、缓冲背压、诊断节拍、声音 FFmpeg 与 IPC 边界仍有效，视频周期整段重编码和画中画隐藏解码已由 2026-08-28 mpv/libplacebo 主链替代，不再作为当前实现或验收入口。

- [x] 写入实施边界、分组、验收阈值和回滚方案。
- [x] 完成 PortAudio/缓冲/诊断修复与回归测试。
- [x] 完成 FFmpeg/视频渲染修复与回归测试。
- [x] 完成 WebView/画中画/类型边界修复与回归测试。
- [x] 经用户确认后执行主线程代码总监复核并修正，清理仅限本次范围内确认无用的代码、导入和调试项。
- [x] 完成 Rust、桌面 UI 自动化门禁和本地 Windows NSIS/portable 测试包构建。
- [x] 在最终效果窗口实现媒体时钟健康状态机：以 `playback_generation + source` 和真实 `currentTime` 为事实，排除暂停、seek、切源、metadata 装载及循环边界宽限后连续约 `1500ms` 无推进即发布停滞；主窗口冻结双周期真实进度并显示“播放时钟停滞/恢复中”，不得用墙钟补偿。
- [x] 实现同源代次最多一次的受控恢复：重载同一媒体、恢复已保存位置并续播，异步结果按源代次丢弃迟到完成；成功后重新对齐 PortAudio，失败后显式展示时钟失败、保留 Rust 用户播放意图并继续本地续播尝试，不反向暂停且不无限 reload。
- [x] 保持视频 latest-wins、受控媒体边界和单飞提交，硬件编码继续优先；为 `libopenh264` 回退增加固定 2 个视频编码线程的有界限制。
- [x] 删除旧逐周期视频 Worker 及其 FFmpeg `out_time_us` 进度协议；当前视频进度只来自受管 mpv 的真实媒体时钟。
- [ ] 继续细分等待边界、渲染、校验、切换、完成、取消、失败阶段，并按需向诊断界面暴露输出字节和最近推进时间。
- [x] 增加独立于最长总超时的有限无进展 watchdog；`.partial` 输出连续 120 秒不增长时终止并有界 Join 子进程、清理临时文件、保留当前媒体并返回可重试错误；文件首次出现、增长或因重写而缩小时均刷新推进时间，并覆盖真实推进与真实停滞回归。
- [ ] Phase 6F 重新验收播放中开启/关闭普通声音处理：开关只更新下一候选，当前 N 持续播放；A/B 槽内部 `pause/load/release` 不得调用后端用户暂停。
- [x] 增加声音出口切换回归：覆盖开启、关闭、PortAudio 启动/预热/同步/恢复失败和内部媒体暂停事件，确认画面时钟持续推进，只有主页显式暂停/停止改变 Rust 播放状态；Windows 本机真实 `.ts` 素材已验证开启时 `00:03→00:25`、关闭并跨循环后继续到 `00:14`，无时钟停滞告警。
- [ ] 使用 Windows 高负载真实素材分别验证硬件编码与 `libopenh264` 回退，确认 WebView 播放和 PortAudio 获得资源余量，并完成长时间稳定性验收。
- [ ] 在目标 Windows 10/11 x64 新机与真实声卡/驱动完成 30 分钟 CPU/RSS、缓冲水位和跨 EOF 长稳验证。

### Phase 6F：N/N+1/N+2 有界音视频临时片段滚动处理（历史基线；视频部分由 Phase 6H 覆盖）

实施入口：[`docs/superpowers/plans/2026-08-26-N1-N2音视频临时文件滚动处理实施计划.md`](./docs/superpowers/plans/2026-08-26-N1-N2音视频临时文件滚动处理实施计划.md)。

覆盖说明：本阶段已完成事实保留用于迁移审计；声音 A/B、进程监督和缓存生命周期仍可复用。视频 A/B、迟到追赶、视频 N/N+1 文件切换及全局单候选 FFmpeg 不再作为目标验收。

- [x] 同步 PRD、系统架构、桌面架构、参数契约、长计划和独立实施计划，明确视频与普通声音为两条独立 N/N+1/N+2 文件链。
- [x] Phase 0：失败测试覆盖 N+1 提前准备、到期只提交、N+2 无执行资源和 N+2 原样晋升；自动周期不再在到期时启动整文件处理。
- [x] Phase 1：视频与声音候选都接收带完整身份、源起点、输出长度和有效覆盖范围的有界窗口请求，分别写入 MP4 与 AAC/M4A，并复用取消、watchdog、有界 stderr、进程树终止和线程 Join。
- [x] Phase 2：视频与声音 `MediaCycleQueue` 在各自初始化和晋升时立即 prepare N+1；各自 N+2 只保存参数，禁止合并最早目标。
- [x] Phase 3：视频与声音各自到期只提交 `ready + preloaded + identity-valid` 候选；对应 A/B 切槽成功后才更新该领域参数、次数并滚动该队列，迟到或失败不阻塞另一链。
- [x] Phase 3 补强：候选错过有效窗口或返回失败时，先废弃对应四元身份并清理文件，再跳过旧计划、推进 N+2；禁止重复渲染同一过期 sequence。
- [x] Phase 3 补强：普通声音 FFmpeg 正常退出、线程 Join 且 `.partial` 已通过 FFprobe 后，若 `volumedetect` 暂缺有效 `max_volume`，同一声音 Worker 对同一暂存产物最多自动重试内容探测 `2` 次（总尝试最多 `3` 次），共享原 deadline/取消令牌且不重新编码 N+1。stderr 保留末尾摘要；静音、取消、超时、参数错误、FFmpeg 非零退出及其他不可读错误不重试。恢复只占声音域、保留当前声音 N，不改变视频 N/N+1/N+2；耗尽后返回可解释失败并清理暂存。
- [ ] Phase 4：A/B 相对/绝对时间对齐、单项循环读取、播放池条目边界换代、旧文件释放确认和失败候选删除已实施；独立 session/generation 目录及文件数硬上限仍待完成。
- [x] Phase 5：保持 NVENC → AMF → QSV → Media Foundation → 软件编码真实探测；软件线程上限为 `min(8, max(1, logical_cores - 1))`，慢机器不增加并发。
- [ ] Phase 6：覆盖用户输入的 `1–60s`、相等区间、音视频不同周期同时运行、暂停/停滞冻结、换源、seek、停止、关闭、崩溃恢复和清理重试；页面 UI 不改。
- [ ] 完成 Windows 10/11 x64 的 NVIDIA、AMD、Intel 和无独显软件路径测试；每类至少 30 分钟、100 次周期变化，验证 CPU/GPU/RSS、句柄、子进程、线程和临时文件无持续增长。
- [ ] 肉眼验收 1 分钟素材按用户设置区间多次变化，画面参数和声音参数均真实变化，正常观看无可感知卡顿。
### Phase 6B：服务端与桌面端登录安全加固

实施入口：[`docs/superpowers/plans/2026-08-26-服务端与桌面端登录安全加固实施计划.md`](./docs/superpowers/plans/2026-08-26-服务端与桌面端登录安全加固实施计划.md)。本阶段只加固管理员创建用户后的管理端/桌面端登录，不开发注册、MFA/WebAuthn、邮箱找回或设备密钥。

- [x] 同步 `产品需求文档.md`、`系统架构总览.md`、`数据库迁移与密钥存储约束.md`、本计划和独立实施计划；明确当前态与目标态，未把未实施能力标记为已完成。
- [x] 更新 OpenAPI、共享类型和错误码：管理端/桌面端独立登录入口，`admin/desktop` audience，Refresh/Logout 新语义及兼容窗口。
- [x] 新增前向迁移：`auth_sessions` 增加 audience、token family、generation、轮换/撤销事实和约束；新增 PostgreSQL 登录限流桶。旧会话统一标记 `legacy` 并撤销，要求重新登录。
- [x] 实现密码哈希边界：新建/重置/换密使用 Argon2id PHC，旧 bcrypt 成功登录后渐进升级；新增密码 15～128 Unicode 字符，登录继续兼容既有凭证，并限制 Argon2 并发内存。
- [x] 实现服务端登录分流与路由 audience 校验：桌面拒绝管理员账号，管理路由拒绝 `desktop`，客户端路由拒绝 `admin`，Refresh 不得改变 user/product/device/audience。
- [x] 实现防账号枚举、预生成 dummy hash、可信代理解析和“账号摘要 + 来源 IP”双维度限流；账号/IP 桶在 PostgreSQL 共享，进程内限流只保留瞬时保护。
- [x] 将 Access Token 收紧到 15 分钟；Refresh Token 保持 30 天绝对期限并启用 family 重放检测，并发轮换只能成功一次。Logout 改为按 Refresh 哈希幂等撤销，Access 过期时仍可注销。
- [x] 将桌面认证秘密所有权迁入 Rust：Refresh 的保存、读取、轮换、恢复和删除不再返回 WebView；删除保存/读取登录密码 IPC 与 UI，升级时尽力清理旧密码和激活码凭据。
- [x] 拆分 Tauri `main`/`final-effect` capability，并在认证命令内校验窗口标签；正式 CSP 去除开发 origin，发布域名仍由发布环境验收。
- [x] 完成 Go 单元/SQLMock、React 契约/类型/测试、Rust fmt/Clippy/全目标测试。
- [x] 使用本地 Docker PostgreSQL 16 完成迁移/Store 真库集成、normalized 回填与读源启动、双 Repository 登录限流竞争，以及 audience/Refresh 重放/Logout/重启限流 HTTP 冒烟。
- [ ] 完成 Go Race、Windows Credential Manager、正式 HTTPS/DNS/TLS、多主机部署和桌面端到端冒烟；这些发布环境证据缺失时不得把 Phase 6B 整体标记完成。
- [x] 子任务完成后由主线程执行第一轮代码总监复核，已修复会话撤销与 Refresh 轮换并发锁边界。
- [ ] 只在用户确认后删除本次变更暴露的未使用代码、旧凭据命令、构建残留和测试夹具，并同步最终清理结果。

退出标准：管理端与桌面会话无法跨 audience 使用；桌面 WebView 永远拿不到密码或 Refresh Token；Access 过期仍能注销；Refresh 重放撤销整个 family；不存在用户与其他凭证失败无法通过外部语义直接区分；跨实例限流、Argon2id 渐进迁移、Windows Keychain 和窗口权限隔离均有自动化与手工证据。

### Phase 7：最终代码总监复核

- [ ] 需求覆盖复核：当前 PRD 每个目标都有实现或明确延期。
- [ ] 依赖复核：删除未使用代码、依赖、导入、配置和生成物。
- [ ] 接口复核：OpenAPI、Go DTO、Rust IPC、React 类型一致。
- [ ] 安全复核：API Key、租约凭证、Refresh Token、用户密码、激活码和音视频内容不泄露；`admin/desktop` audience 不能越界，Refresh family 重放与注销语义正确，账号偏好与 Rust 独占系统凭据分层，桌面不保存密码、Refresh 不进入 WebView，主窗口/最终效果窗口权限与工作台双门禁不可绕过。
- [ ] 性能复核：首帧不等待哈希/模型/处理缓存，长视频无无界缓存。
- [ ] 视频显示复核：最终窗口禁止 CSS Filter/Transform 视频叠加；完整参数按 Phase 6I 进入受管 mpv `gpu-next/libplacebo` 主链。确认单次快照、逐字段输出证据、D3D11→Vulkan→CPU4→Original 单向降级、真实 PTS/呈现门禁以及自动化与手工证据一致；当前最高验收 1080p，4K 延后；不得恢复逐周期 FFmpeg/MSE 视频路线。
- [x] 桌面内存复核：探测/控制面/模型/历史 Worker/资源清单响应均有实际字节上限；插话与缓存目录扫描不收集无界集合；媒体监听器、定时器和后台 Worker 具有对称清理及退出门禁。
- [ ] 历史边界复核：旧 migration 和文档只用于审计，不被当前入口调用。

### Phase 8：多产品控制面与商业生产专项（P0 基线与商业 Phase 2 已部分交付）

目标：共享控制面的产品硬隔离与商业 Phase 2 RBAC 已完成服务端/管理端自动化交付；后续继续按独立拆分 PRD 补齐 Profile、商业事实、可信制品和生产治理能力。

P0 执行入口：[`tasks/prd-douyin-desktop-control-plane-integration.md`](./tasks/prd-douyin-desktop-control-plane-integration.md)，四个阶段依次为产品硬隔离、Profile/离线授权、模型认证、固定 OpenAPI/跨仓库 E2E；设计见 [`多产品控制面与 douyin-desktop 接入设计`](./docs/superpowers/specs/2026-08-22-douyin-desktop-接入-autolive-多产品控制面设计.md)。

P1/P2 执行入口：[`tasks/prd-server-commercial-production-readiness.md`](./tasks/prd-server-commercial-production-readiness.md)，阶段上下文与十一个阶段文件位于 [`tasks/prd-server-commercial-production-readiness/`](./tasks/prd-server-commercial-production-readiness/)；设计见 [`服务端商业化与生产能力补齐设计`](./docs/superpowers/specs/2026-08-21-服务端商业化与生产能力补齐设计.md)。

共享执行顺序：商业 Phase 1 契约基线 → P0 Phase 1 与商业 Phase 2 协同交付同一权限模型 → P0 Phase 2～4 → 商业 Phase 3～11。权限目录/角色生命周期/React 通用壳归商业 Phase 2，product 范围语义与跨产品拒绝归 P0 Phase 1，不创建两套角色表或 API。

- [x] P0 Phase 1：产品注册表、全局用户产品成员、产品会话、数据约束和产品范围 RBAC；真实 PostgreSQL 回放仍待 `TEST_POSTGRES_URL`。
- [ ] P0 Phase 2：Profile 服务端时间、6 小时复核、24 小时签名离线授权和时钟回拨处理。
- [ ] P0 Phase 3：douyin-desktop BYOK 首发，以及经供应商验证后才开放的可撤销短期凭证。
- [ ] P0 Phase 4：固定 autoLive commit/digest 的 OpenAPI 三件套和跨仓库主/负向 E2E。

- [ ] Phase 1：锁定 OpenAPI、领域边界、威胁模型、迁移顺序和供应商适配器边界。
- [x] Phase 2：实现固定权限目录、自定义角色、用户多角色、服务端鉴权和 React 权限化管理壳；浏览器手工冒烟和真实 PostgreSQL 仍待补。
- [ ] Phase 3：实现激活码独立加密表、明文二次查看、绑定槽位、容量修改、设备切换和 React 安全管理交互。
- [ ] Phase 4：实现按产品的套餐/订单/订阅/设备席位商业事实，有效订阅开放全部产品功能。
- [ ] Phase 5：补齐订阅、订单、设备席位、设备和套餐版本管理查询/筛选及 React 管理页。
- [ ] Phase 6～7：实现微信支付 Native、密码重置投递 Worker 与对应管理页。
- [ ] Phase 8～11：实现产品范围私有对象存储/可信下载、product/environment/namespace JSON Schema、双重脱敏错误报告/最小反馈、数据保留/备份恢复/OpenTelemetry/CI 签名与 Provenance，并补齐管理/运维页。

开始条件：Phase 2 已获用户明确授权并完成；后续商业阶段仍需单独授权。实施范围为 Go 服务端与 React 管理端，Rust/Tauri 消费端另立任务；服务器仍只接收本地或 CI 构建并通过验证的发布产物。

## 5. 每阶段执行规则

1. 每个阶段开始前重新读取本文件和对应架构/接口文档。
2. 修改前用 `rg` 查找所有生产引用、测试引用、路由、Tauri 注册和依赖。
3. 先写或更新失败测试，再写最小实现；删除代码时先删除调用方，再删除实现，最后清理测试和依赖。
4. 每个阶段完成后运行对应的 Go、Rust、React、文档或手工媒体检查。
5. 主线程必须对所有子任务进行代码总监复核，重点检查误删当前能力、残留旧入口、错误码漂移、未使用依赖和安全边界。
6. 未经用户确认，不删除数据库历史迁移、不做数据回滚、不新增推流能力。
7. 长任务执行中发现当前文档与实现冲突时，先更新本文件和相关契约，再继续代码修改。

## 6. 验收命令

```bash
cd backend && go test ./...
cd backend && go test -race ./...
cd backend && go vet ./...
cd backend && go build ./...

cd desktop/src-tauri && cargo fmt --check
cd desktop/src-tauri && cargo test --workspace
cd desktop/src-tauri && cargo clippy --workspace --all-targets -- -D warnings

cd admin-web && pnpm test
cd admin-web && pnpm exec tsc --noEmit
cd admin-web && pnpm run build

cd desktop/ui && pnpm test
cd desktop/ui && pnpm exec tsc --noEmit
cd desktop/ui && pnpm run build

bash tools/check-document-references.sh
bash tools/check-parameter-contract.sh
ruby -e 'require "yaml"; YAML.load_file("接口契约/openapi.yaml"); puts "OpenAPI 解析通过"'
```

本次服务端 Phase 2 交付实际执行 Go 与 `admin-web` 门禁、文档引用和格式检查；按用户范围未执行 `desktop/`、Rust/Tauri 或桌面端管理契约验证。

### Phase 6I：mpv/libplacebo GPU83 实时主链（当前视频唯一实施重点）

实施入口：[`docs/superpowers/plans/2026-08-28-mpv-libplacebo实时GPU主链实施方案.md`](./docs/superpowers/plans/2026-08-28-mpv-libplacebo实时GPU主链实施方案.md)。旧视频文件链和 MSE 阶段已从当前计划删除，不再传递实施决策。

- [x] 建立唯一方案并删除两份旧视频方案。
- [x] 完成里程碑 1：受管 mpv 持久 JSON IPC、受控 hook、精确 GPU83 映射、单条 shader 参数命令和正式周期入口；本机 D3D11 hook 编译通过，旧 MSE 不再由成功正式周期进入。
- [x] 删除旧视频 MSE/SourceBuffer、period/分块 IPC、逐周期 FFmpeg 视频 Worker、权限和对应测试；普通声音候选、PortAudio、插话、固定话术和一次性视频导入兼容不变。
- [x] 完成受管 mpv Original 的代码接线及指定文件基础复验：处理关闭时复用同一 runtime actor、无 shader 启动规格、D3D11→Vulkan、持久 IPC、Job Object 与 seek/loop/pause 同步；source 状态携带 PID 和真实原生输出信息，失败保留 WebView 兼容画面且不污染 GPU→CPU4 降级状态。指定 HEVC 文件已确认最终原生表面连续运动与退出无残留；换源、EOF、关窗重开和 1080p60 性能仍待实机门禁。
- [x] 完成 Phase 4 四级生产状态机与非媒体门禁：D3D11→Vulkan→CPU4→Original 单向 floor、启动 lease/stop tombstone、seek/loop 取消、完整 demotion history、CPU4 精确 `83/4/79` 状态和前端确定性错误停止重试均已接线；真实 1080p 恢复误差、呈现 P99、黑屏/单进程及长稳仍未验收。
- [x] 完成受管 Original 专项代码总监整改：GPU 失败不再通过完整停止重置单向降级；loop 快照追平后才占用尝试键；Source 进程退出和同步失败可回写 UI；重复 ensure 拒绝旧 generation/epoch/loop；GPU/Source 活动态缺少合法 PID 时 fail-closed。整改只做非媒体验证，未触碰音频与 4K。
- [x] 完成 Phase 6 EOF/表面仲裁静态闭环：固定 `keep-open=yes` 且禁止 `loop-file=inf`，严格解析布尔 EOF 并绑定 generation/backend epoch/clock epoch/loop，边界清零；EOF 成立后停止 PTS/FPS/帧预算健康采样，暂停态不发布或消费 EOF；final-effect 仅在 active PID 与身份匹配时释放可见 WebView video，单项循环跨轮次等待新状态时保持原生表面，EOF 以既有幂等命令单飞推进。指定文件只通过基础原生表面，不标记 EOF、Phase 6 完成或无卡顿。
- [x] 完成统一媒体段时间轴的代码与聚焦测试：身份固定为 `playback_generation + source_path + loop_index + source_duration_ms`，presentation/source-local PTS 分离，mpv 只接收合法源内 PTS；播放态边界 `Seek → SetPause(false)`、暂停态边界 `SetPause(true) → Seek`。无效或越界音频时钟只停用纠偏，不触发视频降级。该勾选不包含真实媒体、1080p60 或长稳验收。
- [x] 完成前端视频同步 latest-wins 和 Rust 稳定错误合同的代码与聚焦测试：单 IPC 在途，busy `100/250/500ms` 有界退避，result_unknown 先查状态，stale 刷新最新 desired，迟到 Original 不覆盖健康 GPU/CPU4。同步确认 `IpcTimeout` 保留会话并返回 result_unknown；`IpcDisconnected` 等断链继续原降级。该勾选不包含现场周期、无卡顿或多文件验收。
- [x] 普通声音候选 prepare 已移出 Tauri 主线程：只迁移校验、准备和候选启动的调度位置，保留既有单飞、取消、超时、DSP、候选格式、提交和播放语义；代码及聚焦检查完成，但不得作为真实声音不卡顿证据。
- [x] 完成 Phase 6 运行事实状态整改：GPU83/CPU4 首帧后只读取 mpv `hwdec-current` 作为实际解码器，`no` 映射为软件解码，属性不可用、未知值和错误响应 fail-closed；无滤镜 Original 必须在 `vo-configured=true` 后取得有效视频 PTS 与源 FPS 才发布 Active，随后最佳努力读取解码属性，失败明确显示“未报告（Original）”，不按 D3D11/Vulkan 推断硬解器。主界面展示 GPU pass P99、VO/decoder 丢帧、mistimed、delayed 与连续帧预算异常窗口；视频周期提交漂移不冒充 PortAudio 音画同步漂移，GPU pass P99 不冒充真实呈现间隔 P99。本项不构成无卡顿或音画同步验收。
- [x] 2026-08-29 指定 HEVC `720×1280@30fps` 文件的基础 Original 原生表面复验通过，报告为 [`artifacts/mpv-phase6-native-surface-gate-20260829-v2.json`](./artifacts/mpv-phase6-native-surface-gate-20260829-v2.json)：连续运动画面、单一受管 mpv、精确源路径/目标 HWND 和退出无残留均已核对。本次没有开启视频处理或 PortAudio，不覆盖 EOF/重开、GPU83、GPU→CPU4、1080p60 P99 或长稳，后续不重复播放该文件。
- [ ] 2026-08-29 首轮 1080p60 无音轨 Phase 6 门禁仍保留为失败：原 `15ms` Windows 命名管道观察预算连续超时并结束健康 Original，报告为 [`artifacts/mpv-phase6-native-surface-gate-20260829-v1.json`](./artifacts/mpv-phase6-native-surface-gate-20260829-v1.json)。当前单命令/整 tick 预算为 `250ms/350ms`；单次暂态时钟超时只忽略本轮，连续 3 轮失联进入既有降级或 Original 失败；每秒帧健康读取另有独立三次失败门禁，不被普通 PTS 成功清零。指定竖屏文件的基础通过不能替代 1080p60、EOF、重开和无黑屏复验。
- [x] 完成 Phase 2/4 stderr 故障接线与状态矩阵：复用有界脱敏尾缓冲，严格识别 shader/hook、VO/图形 API 和设备丢失致命日志，启动阶段拒绝、运行阶段进入唯一单向降级 actor，Original 终态失败；音频错误和正常 error-diffusion 描述不误触发。状态机覆盖启动失败、shader 失败、设备丢失、断管、CPU4 过载、Original 恢复和并发 stop/seek；真实硬件故障注入仍待实机。
- [x] 完成 Phase 3C 非媒体准入审计与代码总监整改：锁定外部 mpv JSON IPC、主 Tauri `unsafe_code=forbid`、libmpv 资产/依赖/资源声明、生产 shader/20 项 AVAILABLE 身份、15 项 HISTORY 和画中画时间轴组合条件；专项测试 `11/11`，累计反例 `47/47`，P0/P1/P2 均为 0。该项只表示实机前静态阻断清零，报告仍为 `not_admitted`、`phase3cImplemented=false`。
- [ ] 将当前开发接线 `79/83` 完成实机验收并扩展为完整 `83/83`；2 项颜色与 2 项跨帧历史必须在取得各自原子性和真实输出证据前保持 `Unavailable`。
- [ ] 完成生产级 mpv/libplacebo 资源、完整 shader、D3D11/Vulkan/CPU4 真实短样本门禁。
- [ ] 接入单一常驻 mpv 会话、有界双向 JSON IPC、命令超时、stderr 尾缓冲和对称退出；其中 Original/GPU 共享 actor、Windows Job Object、EOF/表面仲裁静态接线和指定文件的 Original 基础最终表面/退出清理已通过，真实 mpv 换源/EOF 时序、关窗重开、GPU/CPU4 恢复与安装包退出仍待实机门禁。
- [ ] 完成 GPU83 单次快照更新、逐字段证据、CPU4/Original 会话级降级和真实呈现状态。
- [x] PortAudio 可听 PTS 到 Rust/mpv 的源内 PTS 闭环代码及聚焦测试完成；正常校正不得持续硬 seek，跨循环 presentation PTS 不得进入 mpv。
- [ ] 以真实 PortAudio/mpv 媒体完成 1080p 联合音画同步、设备恢复和 30 分钟长稳门禁。
- [ ] 通过 NVIDIA、AMD、Intel、CPU-only、200 周期和 30 分钟长稳验收。

退出标准：视频入口唯一为受管 mpv；GPU83 逐字段真实生效，CPU 严格只有 4 项；周期只更新参数且无播放卡顿；音画最大偏差 `≤80ms`、P99 `≤40ms`。

### Phase 6J：麦克风插话与插话文件入口（正式需求·待实施）

实施入口：[`docs/superpowers/plans/2026-08-30-麦克风插话与插话文件入口实施方案.md`](./docs/superpowers/plans/2026-08-30-麦克风插话与插话文件入口实施方案.md)。本阶段仅处理本地麦克风门控和现有文件插话的用户界面命名，不恢复实时话术幻化。

- [ ] 先将所有用户可见的“随机插话”入口、标题、提示和无障碍名称统一改为“插话文件”；保持现有目录扫描、随机文件选择、预设、duck、持久化和兼容 IPC 语义不变。
- [ ] 新增独立“麦克风插话”配置与状态；用户显式开启后才申请麦克风权限和打开输入设备，关闭后立即停止采集并有界释放资源。
- [ ] 复用 PortAudio 建立单一全双工音频所有权；捕获回调只写固定帧有界缓冲，DSP 工作线程使用 SpeexDSP 执行 AEC → 降噪 → AGC → VAD，最终输出回调只读已准备 PCM 和原子门控状态。
- [ ] 建立 `Idle / MaybeSpeaking / Speaking / Hangover / Error` 状态机和优先级 `麦克风插话 > 固定话术 > 插话文件 > 普通声音/原媒体`；进入 `Speaking` 后将所有低优先级声音的最终数字增益设为 `0`，只混入麦克风，退出尾音保持后短释放恢复。
- [ ] 补齐权限拒绝、无输入设备、设备拔出、AEC 参考缺失、DSP 失败、队列溢出、暂停、停止、切源和退出的超时、取消、Join、结构化错误与 fail-open 恢复；不得落盘、上传或记录麦克风 PCM。
- [ ] 完成 Rust 单元/集成测试、React 状态和无障碍测试，以及 Windows 真实扬声器/耳机、默认设备切换、设备拔插、长时间说停交替和进程退出无残留验收。

退出标准：用户界面不再出现“随机插话”；麦克风未授权或故障不影响原媒体播放；用户说话期间最终输出只包含麦克风，视频画面和媒体时钟连续；恢复无突发爆音；音频不落盘、不上传；所有资源在关闭、停止和退出后有界回收。

### Phase 6K：ZLMediaKit RTMP/RTMPS GPU 直推（正式需求·待实施）

实施入口：[`docs/superpowers/plans/2026-08-30-ZLMediaKit-RTMP-GPU直推实施方案.md`](./docs/superpowers/plans/2026-08-30-ZLMediaKit-RTMP-GPU直推实施方案.md)。本阶段只处理用户指定 ZLMediaKit 的直接发布，不接入 OBS、平台账号、自动开播或多平台分发。

- [ ] 先建立 `RtmpOutputConfig`、地址/轨道/范围校验、脱敏规则和失败测试；完整地址只允许 `rtmp://`/`rtmps://` 且第一阶段不持久化，不接受自由 FFmpeg 参数。
- [ ] 新增主窗口 RTMP 输出卡片，默认勾选画面和声音且至少一轨启用；草稿与活动配置分离，展示真实状态、发布时间、实际编码器、输出轨道、码率、重试次数和脱敏错误。
- [ ] 新增 Rust `RtmpOutputManager`，独占 session generation、attempt、FFmpeg 进程、管道、取消、状态和 Join；所有子进程纳入 Windows Job Object，停止、换源、失败和退出无残留。
- [ ] 视频直接读取当前活动源，第一阶段复用启动时静态 GPU83 快照和既有硬件编码器探测，按 `NVENC → AMF → QSV → Media Foundation → OpenH264` 单向降级；不捕获桌面、最终效果窗口或 HWND。
- [ ] 在最终 PCM 总线增加有界 `RtmpAudioSink`，输出 AAC-LC `48kHz` 双声道；不能阻塞 PortAudio 回调。普通声音处理和插话应进入推流声音，固定话术在 PCM 接入前必须显示明确限制。
- [ ] 第一阶段换源采用受控停止并重新发布，视频参数使用启动时静态快照；UI 不得宣称跨条目无断流或逐周期 GPU83 已接通。网络暂态故障按 `1/2/4/8/15s` 最多五次重试，确定性错误不重试。
- [ ] 完成 Rust/React 自动化、FFmpeg 资源/命令权限检查，以及真实 ZLMediaKit 上音画双轨、仅画面、仅声音、GPU/CPU 回退、断网/拒绝/恢复、停止/退出和 30 分钟长稳门禁。

退出标准：三种轨道组合均能以 H.264/AAC 直接发布到用户指定 ZLMediaKit；本地播放和最终效果窗口不被推流故障改变；状态公开实际编码器和脱敏原因；地址不泄露；停止、换源、失败和退出后 FFmpeg、线程、句柄、管道和重试任务全部有界回收。第一阶段限制必须可见，未完成固定话术 PCM、动态 GPU83 或跨源连续发布时不得宣称支持。

### Phase 6L：AkVirtualCamera GPU 虚拟摄像头（正式需求·待实施）

实施入口：[`docs/superpowers/plans/2026-08-30-AkVirtualCamera-GPU虚拟摄像头实施方案.md`](./docs/superpowers/plans/2026-08-30-AkVirtualCamera-GPU虚拟摄像头实施方案.md)。本阶段只处理本应用最终效果画面到本地虚拟摄像头，不进入 Go，不写物理摄像头，也不改变 RTMP、声音或播放事实。

- [ ] Phase 0 先固定 AkVirtualCamera 提交并完成 GPL-3.0 法律审核、完整对应源码/补丁/构建脚本、SBOM 和独立 sidecar 边界；修复非回环/无认证 IPC、共享内存 ACL 和 NV12 平面边界风险。任何阻断未解除不得进入正式产品接线。
- [ ] 在本机分别构建和签名 x86/x64 DirectShow 组件，创建稳定 `GpAutoLive Camera`；Windows 10/11 均以 DirectShow 为默认，Windows 11 MF 保持实验未启用，禁止使用伪 `mfsensorgroup.dll`。
- [ ] 完成独立 GPU 技术门禁：最终效果 HWND → WGC `ID3D11Texture2D` → 非 WARP D3D11 Video Processor 缩放/BGRA→YUY2 → 三槽 staging 一次回读 → AkVirtualCamera direct mode；首版固定 `1280×720@30fps`、`zero_copy=false`。
- [ ] 新增单一 `VirtualCameraOutputManager`、容量 1 latest-wins、generation、取消、deadline、Job Object、黑帧和 Join；不得启动第二 mpv、二次解码、CPU 转换、GDI 截图或反压本地播放。
- [ ] 新增最小安装/修复/启停/状态 UI 与固定白名单 IPC；用户在下游应用选择 `GpAutoLive Camera`，产品不枚举物理摄像头作为输出目标。
- [ ] 完成 Win10/11、x86/x64、Intel/NVIDIA/AMD、Camera/Chrome/Edge/Teams/Zoom/Discord/OBS、DPI/多显示器/锁屏/休眠/崩溃/卸载和 2 小时 720p30 矩阵；公开 adapter LUID、回读 P50/P99、投递/丢帧和客户端事实。

退出标准：DirectShow 下游能选择 `GpAutoLive Camera` 且画面与最终效果一致；GPU 捕获/缩放/转换和一次 CPU 边界回读均有真实证据，暂停/停止/纯音频/锁屏两帧内黑屏；本地播放不受故障影响，停止/退出/卸载无残留。MF、NV12、1080p 或零拷贝未各自通过新门禁时不得宣称支持。

## 7. 变更记录

- 2026-08-30：新增 Phase 6L，虚拟摄像头统一采用 AkVirtualCamera。首版冻结为 Windows 10/11 DirectShow、YUY2 `1280×720@30fps`；WGC/D3D11 在 GPU 上处理后一次有界回读到独立 GPL sidecar，明确 `zero_copy=false`。GPL、安全、NV12、MF、签名和兼容矩阵均为前置硬门禁。
- 2026-08-30：新增 Phase 6K 和 ZLMediaKit RTMP/RTMPS GPU 直推专项方案。桌面端直接读取活动源和最终 PCM，默认发布音画双轨且可只选一轨，使用受管 FFmpeg/libplacebo、GPU 优先 H.264 编码和有界重试；不捕获窗口、不经过 Go、不接入 OBS。第一阶段明确采用换源重发、静态 GPU83 快照，并保留固定话术不进入推流声音的可见限制。
- 2026-08-30：新增 Phase 6J 和专项实施方案，确定“随机插话”用户入口改名为“插话文件”，并规划 PortAudio + SpeexDSP 的本地麦克风插话。唯一允许的 VAD 仅作非语义说话门控；说话时由最终混音数字静音所有低优先级声音，只保留麦克风。本阶段尚未实施，不包含 ASR、LLM、TTS、speech-to-speech、录音或上传。
- 2026-08-28：新增 Phase 6I，常驻 mpv `gpu-next/libplacebo` GPU83 实时主链整体替代 Phase 6G/6H 的视频 FFmpeg/MSE 方案；旧两份专项方案已删除。声音 N/N+1、PortAudio、独立周期、源媒体只读和受管资源原则继续有效。
- 2026-08-27：根据开发截图修复 N active 被下一轮 prepare 降为 available、已计数参数未立即广播到最终窗口，以及 PortAudio 被前端门禁强制旁路的问题；默认设备显式可选，回退可重试且错误真实显示。Rust 实时链 15/15、前端关联 66/66、PortAudio 本机真实设备枚举 1/1 与本地生产构建通过，开发版画面主观变化和实际听音仍待手工验收。
- 2026-08-27：修复声音和画面两个处理开关同时开启时的三层竞态：上一帧 React 状态覆盖另一域、同钟参数 state/ref 覆盖、Rust 跨域清 current/pending。Rust 核心 261/261、前端聚焦 69/69、TypeScript/本地生产构建及 Rust 格式检查通过；开发进程因 Rust 更新重启，双开实机播放仍待重新导入后验收。
- 2026-08-26（TS 视频与纯音频统一播放，代码完成、实机待验收）：Phase 5A 已扩展为最多 100 项的视频/纯音频统一媒体池，增加六种音频格式、`attached_pic` 忽略、纯音频黑屏及视频处理/PiP 不适用规则；TS/M2TS 已实现快速封装/必要转码、`.partial`、FFprobe 校验、原子提交和取消/缓存清理任务。Rust/UI 自动化已通过，Windows WebView 与真实媒体样本仍待验收，当前不声明整体发布完成。
- 2026-08-26：完成 Phase 6B 登录安全主体实现、本地自动化和 Docker PostgreSQL 16 真库验收；`admin/desktop` audience、Access 15 分钟、Refresh 30 天 family 重放检测、按 Refresh 幂等注销、防枚举与 PostgreSQL 限流、Argon2id 渐进迁移、桌面不保存密码、Rust 独占 Refresh 和 Tauri 窗口隔离已落地。真库迁移/Store 集成、normalized 启动、双 Repository 限流竞争及认证 HTTP 冒烟通过；注册、MFA、邮箱找回和设备密钥不在本次范围，Windows 凭证库、正式 HTTPS/DNS/TLS、多主机与 Race 仍待环境验收。
- 2026-08-25：完成 Phase 6A 代码总监复核。补齐 PortAudio 环缓连续帧借用与 `250ms` 状态节拍、FFmpeg 单请求总 deadline、画中画/Web Audio/页面轮询的对称生命周期和 IPC 运行时校验；从最终效果 UI 删除本版本禁止启用的实时话术能力查询及 `250ms` Worker 调度。PortAudio `27/27`、Rust workspace `374/374`、桌面 UI `282/282`、类型检查、fmt、全目标 Clippy、前端/Rust build 与本地 Windows 测试包构建均通过；全新 Windows 机器与真实驱动 30 分钟长稳仍待验收。
- 2026-08-25：新增桌面媒体运行时稳定性 Phase 6A 与独立实施方案，覆盖 PortAudio 流指针所有权、frame/sample 单位、缓冲背压、诊断节拍、FFmpeg 共享超时/错误分类、视频周期整段重编码、画中画隐藏解码、Web Audio 双运行和 IPC 类型边界；当前进入并行实施，未扩展实时话术、推流或服务端范围。
- 2026-08-25：补齐 Windows NSIS 与 portable/ZIP 测试包门禁：构建前只清理受约束的 NSIS bundle 输出目录，不清理 `target/release`；portable 归档必须恰好检测到一个本轮当前 EXE，零个或多个均失败；固定包含 `ambient/low-level-room-tone.wav`、`ambient/LICENSE.txt`，缺任一项必须失败，禁止递归复制额外 `ambient` 文件。
- 2026-08-25（环境声来源最终澄清）：`ambient_sound_mix_percent = 0` 时不加载第二输入；值大于 `0` 时默认解析随包内置真实环境声，用户本地素材可选且优先覆盖。两种来源都执行路径、格式、可读性和 FFmpeg 解码验证，失败保持原声并报可恢复错误；Phase 4 实现项按最终状态完成。
- 2026-08-25（已被“环境声来源最终澄清”覆盖）：上一轮曾要求环境声非零时必须由用户选择本地素材，并废止随包来源；该要求已撤回。零值不加载、完整输入验证和失败原声回退要求继续有效。
- 2026-08-25（视频低感知根因修复）：最终效果窗口只显示经校验的 FFmpeg 处理缓存或明确的源视频回退，移除视频元素上的二次 CSS 色彩/模糊/几何伪效果；整数像素位移在自动模式保持稳定方向，滤镜强度和帧概率不得被固定最小档或错误采样放大。Phase 6/7 保留客观帧差与正常观看主观低感知联合门禁。
- 2026-08-25（最终效果窗口与候选时钟）：最终效果窗口作为客户观看表面只渲染媒体，不显示播放、空源、禁用、音频候选、通信或窗口尺寸错误；诊断通过跨窗口通道保留在主操作窗口。普通声音候选严格限制为 `current < target <= current + 60000ms`，同 generation/loop 的迟到同步时钟重锚到 Rust 最新位置，过期候选按暂态 stale 从最新时钟重建或推进，保持当前媒体和旧轨。
- 2026-08-25（声音参数只读随机化）：新增当前 Phase 5 待办，移除 MFCC 偏移、MFCC 维度、SNR 浮动、频谱盲区、干湿比、环境声混合、高质量音高、播放速度和共振峰偏移的手动输入及高级声音抽屉重复入口，仅保留展示；9 项继续参加普通声音预设与周期随机化，随机后刷新显示并复用既有校验、候选准备/提交、失败回退和运行状态链路。历史“两个具名编辑区可编辑”完成项只保留为已被覆盖的实现事实。
- 2026-08-25（历史环境声口径，后续由同日“环境声来源最终澄清”收口）：视频与全声音预设自动变换落地时允许未选择用户素材时解析随包环境声；最终口径明确为随包内置真实环境声音频，且必须与用户本地素材一样通过完整验证。其余完整视频/高级快照、22×35 预设、随机插话双出口和缓存清理事实保持不变；实时话术幻化继续排除。
- 2026-08-24（正式参数完整接入）：完成当前正式模型中剩余视频空间频段、高级视觉、环境声/干湿、MFCC/SNR 和 LPC 诊断等执行边界；新增 `realfft 3.5.0`、`rustdct 0.7.1` 并由 `Cargo.lock` 固定。剩余开发边界收窄为外部检测模型、外部挂件素材、冻结回退策略和 OCR 待核字段，不扩展实时话术幻化。
- 2026-08-24（历史逐卡高亮动效完成事实，已由 2026-08-27 当前实施口径覆盖）：参数显示 `value` 变化后曾按该卡自己的颜色执行一次约 `900ms` 高亮，并在 `prefers-reduced-motion: reduce` 下禁用；当时实现与自动化验证已完成。该事实保留，当前已取消参数变化闪烁、光晕、发光阴影和关键帧。
- 2026-08-24（历史双周期状态决策，联动部分已废止）：主参数区同时显示视频周期与声音周期、各自展示真实进度与次数的规则继续有效；当时允许共享联动目标的口径已由 Phase 6H 的独立周期规则覆盖。
- 2026-08-24（现行逐卡静态颜色分配；历史动效已由 2026-08-27 当前实施口径覆盖）：每张参数卡或独立参数模块分别从参考图五色色板分配自己的颜色，不再按普通视频、高级视觉、普通声音大分类固定一种颜色；五色可循环复用，分配顺序尽量避免视觉相邻项同色，且单次挂载期内逐卡映射稳定。历史 `290ms/900ms` 高亮事实保留，当前颜色仅作静态强调。算法状态仍由三态文字表达，颜色不表达算法状态；满宽、自动网格、`80px`/`196px` 高度和无底部说明规则不变，也不扩展实时话术幻化。
- 2026-08-24（当前最新满宽决策）：参数内容区填满桌面中栏实际可用宽度，删除约 `880px`、`860–900px` 及其他最大宽度上限；栏内自动网格随窗口增减列数，外层双主栏与极窄单栏降级保持。普通参数卡固定为 `80px` 高，固定视觉频段权重卡固定为 `196px` 高并跨满所属栏；无底部说明、逐卡静态颜色、三态及正式参数事实规则不变。该决策覆盖此前约 `880px` 居中宽度口径，不扩展实时话术幻化。
- 2026-08-24（现行卡片内容决策；高度由上一条明确）：普通参数卡只显示参数名、当前值/进度和三态标签，删除底部范围、单位、接入情况及待实现原因说明句；固定视觉频段权重卡跨满所属栏且三列内容必须完整、不得裁剪。范围、默认值和能力状态事实仍保留在正式契约、校验与媒体边界中。该决策覆盖此前“说明文字至少 `12px` 且显示状态原因”的卡内展示口径，不扩展实时话术幻化。
- 2026-08-24（现行自动网格布局；旧宽度子句已由第一条覆盖）：参数预览使用填满中栏可用宽度的外层双主栏，左栏为普通视频与高级视觉、右栏为普通声音；每个主栏内部卡网格根据实际可用宽度自动决定列数并自然换行，不限制每排数量。极窄时外层双主栏按 DOM 顺序降为单列，全程不设置固定列数或卡宽且不得横向裁剪。逐卡静态颜色和三态规则保持不变；参数变化动效已由 2026-08-27 当前实施口径取消。该布局决策覆盖下一条“每栏两卡、整行最多四卡、先固定降栏内单列”口径，不扩展实时话术幻化。
- 2026-08-24（历史宽度口径，已由同日最新满宽决策覆盖）：参数内容区此前以约 `880px` 为桌面目标宽度，并允许在 `860–900px` 内取整后居中；该最大宽度和由此产生的中栏两侧留白不再属于当前验收。
- 2026-08-24（历史卡内说明口径，已由同日最新卡片内容决策覆盖）：普通卡当时保持紧凑且显示至少 `12px` 的范围、接入情况或待实现原因说明；该说明句与内容自适应高度要求不再作为当前验收，参数范围、默认值和能力状态事实本身仍保留。
- 2026-08-24（历史响应式澄清，已由同日最新自动网格决策覆盖）：参数预览继续使用约 `880px` 居中的外层双主栏，左栏为普通视频与高级视觉、右栏为普通声音；宽窗下每个主栏内部卡网格一排两张，参数区同一横排最多四张。空间不足时先把栏内卡网格降为单列，参数区进一步变窄时再把外层双主栏按 DOM 顺序降为单列，全程伸缩且不得横向裁剪。紧凑高度、至少 `12px` 说明、三列频段、分类色与约 `290ms` 反馈、减少动态效果和三态规则保持不变。该澄清覆盖下一条“每卡占满整栏、每横排左右两张”口径，不扩展实时话术幻化。
- 2026-08-24（历史响应式口径，已由同日最新澄清覆盖）：参数预览继续使用约 `880px` 居中的双主栏，左栏为普通视频与高级视觉、右栏为普通声音；可用宽度足够时两栏等分伸缩、卡片填满所属栏且每横排左右两张，窄窗按 DOM 顺序降为单列，不设置固定卡宽且不得横向裁剪。紧凑高度、至少 `12px` 说明、三列频段、分类色与约 `290ms` 反馈、减少动态效果和三态规则保持不变。该决策覆盖下一条固定/目标 `270–290px` 卡宽口径，不扩展实时话术幻化。
- 2026-08-24（历史布局，已由同日最新响应式澄清覆盖）：参数预览内容区改为约 `880px` 居中的双主栏，左栏合并普通视频与高级视觉、右栏为普通声音；普通卡目标宽度约 `270–290px`、保持紧凑且说明文字至少 `12px`，固定视觉频段权重按三列展示。三个大分类从《媒体参数范围与默认值》的参考图五色色板随机且不重复分配颜色并在挂载期稳定；挂载后显示 `value` 实际变化按所属分类色执行一次约 `290ms` 高亮，初始挂载、相同值、仅重排和普通重渲染不触发，减少动态效果时禁用。能力状态固定为“已接入 / 正式需求待实现 / 待确认”且不等于编辑权限。该决策覆盖下方同日“三分区同页三栏/删除闪烁”和旧 `720ms` 参数卡反馈口径，不扩展实时话术幻化。
- 2026-08-24：随机插话时间参数契约调整为 UI 以秒输入 `0.5–60s`、默认 `8–13s`，IPC/Rust 仍使用 `interval_min_ms/interval_max_ms` 和 `500–60000ms`；duck 攻击/释放允许 `0`，范围分别为 `0–1000ms`、`0–3000ms`，`0` 表示立即压低/恢复，默认值保持 `50/250ms`。Phase 3 增加前后端换算、边界和即时过渡验收。
- 2026-08-25：播放池范围增加系统文件拖放、追加、单项替换、拖拽/键盘重排、删除和清空；新增候选整批探测后原子提交，任何失败保持旧池。成功编辑停止当前资源并重置为非空池首项 `Ready` 或空池无源状态；路径和探测元数据不可手工编辑。本决策覆盖 2026-08-24“不提供追加、重排或删除”的限制，其余单窗口、单活动源、内存态和非生成队列边界不变。
- 2026-08-24（历史基线，已由 2026-08-25 播放池 CRUD 范围覆盖）：当前播放基线更新为当前进程内最多 `100` 项的本地有序播放池。一次多选按文件对话框返回顺序逐项探测，只有全部成功才原子替换旧池；取消、超限或任一失败保留旧池，不提交部分成功项，也不提供追加、重排或删除。首项 `Ready` 后由用户点击播放，在单一最终效果窗口、一个活动媒体源内执行 `A→B→…→A`，单项池自循环。快照沿用实际字段 `source_media_pool`、`source_media_index`、`playback_pool_cycle`、`playback_generation`；播放池不持久化、不上传，不创建服务端媒体任务或生成版本队列。历史 Phase 2 单源完成事实保持原样，新增 Phase 5A 承接当前增量验收。
- 2026-08-24：修复 React 管理端登录未提交必填 `product` 导致的 400；登录表单按当前固定管理端产品提交 `product: autolive`，新增回归测试并通过本地 Docker 登录验证。
- 2026-08-21：加入服务端商业化与生产就绪 Phase 8 入口和拆分 PRD；仅规划、不实施。
- 2026-08-22：Phase 8 改为分层双 PRD，加入多产品 P0 四阶段并将商业生产计划扩展为 11 阶段；仅规划、不实施。
- 2026-08-22：Phase 8 专项先纳入 React 管理页和“自定义角色 + 固定权限点”，后由同日分层双 PRD 决策继续扩展；Phase 2 已按独立任务完成服务端与管理端交付，其他阶段仍按规划推进。
- 2026-08-22：Phase 2 自动化验证记录：Go 全量、Race、vet、build，管理端契约/类型/测试/构建，文档引用和格式检查通过；真实 PostgreSQL 因 `TEST_POSTGRES_URL` 未设置跳过，桌面端按范围未验证。
- 2026-08-24（历史布局，已由同日后续决策覆盖）：参数主面板进一步收紧卡片内边距和分段尺寸，修复窄卡分段越界；使用 Ant Design 青、橙、紫、粉、黄主题 token 区分分类/分组，三态文字仍独立表达算法能力。外层工作区新增 `≤1199px` 可达重排断点，覆盖 Tauri `960px` 最小窗口。
- 2026-08-24（历史参数卡布局；其主页声音编辑口径已由 2026-08-25 最新只读随机化决策覆盖）：当时完成普通视频、普通声音、高级视觉的参数卡与无手柄分段进度；当时主页两个具名声音配置区可编辑的口径不再属于当前验收。
- 2026-08-24（历史 Phase 4 状态，已由同日正式参数完整接入覆盖）：当时保留固定视觉频段、音频分析/第二输入和平滑/锁定为后续边界；现已完成这些正式模型字段，后续仅保留外部模型、外部素材和 OCR 待核项。
- 2026-08-24：参考图参数全部正式化，当前参数类型/IPC 统一为 `MediaEffectParams`、`AudioEffectParams`、`VideoEffectParams`、`AdvancedEffectParams`、`get_default_media_effect_params`、`validate_media_effect_params`；重开未映射参数的实现计划并按三态管理。旧 `research_*` 类型、报告 Worker 和 IPC 已删除且禁止重新引入；实时话术幻化、OBS/RTMP 和平台检测规避仍不在范围。
- 2026-08-23：完成桌面端内存管理风险修复计划：按风险边界加入 512 KiB 探测输出、1 MiB 控制面/历史 Worker、2 MiB 模型响应、256 KiB 运行资源清单上限；插话目录和缓存目录改为有界/流式扫描；最终效果窗口清理监听器与定时器；退出统一取消媒体、运行资源和历史兼容 Worker，并在共享 3 秒预算内等待后台 Worker，晚到任务由退出门禁拒绝。
- 2026-08-13：用户确认删除旧 PRD 相关代码，包括 `speech_preview_task`；建立本长任务计划。
- 2026-08-13（历史方案）：曾确定单源单窗口循环、三个独立开关、Go 管理账号/租约、Rust 直连模型和实时音频候选换轨；实时话术范围已由 2026-08-19 决策移出当前版本。
- 2026-08-13：完成旧 PRD 代码清理；Go 补齐 OpenAI-compatible 账号测试、直连调用摘要、用量查询和第 0010 号摘要字段迁移，当前进入 Phase 1 后续验证。
- 2026-08-20：新增第 0011 号前向迁移，保存激活码首次核销用户归属并回填已有设备核销关系；不修改 0001–0010 历史迁移。
- 2026-08-13：补齐号池健康/并发/额度派生状态、PostgreSQL 模型账号活动与当日用量同步、管理端连通性测试和调用摘要页面；Rust 接入单源播放快照、异步哈希状态、候选音频同步门禁契约。
- 2026-08-13：删除候选音频旧的直接替换入口，统一为暂存/提交/丢弃三步状态机；主窗口与最终效果窗口共享播放状态，补齐同窗口播放控制、异步哈希刷新和候选音频目标时长/锁定话术门禁。
- 2026-08-13：补齐普通声音处理配置契约 `AudioProcessingProfile` 和 `set_audio_processing_profile`；复用音频参数范围校验，快照增加参数版本/执行状态，未配置 DSP Worker 时保持原音轨并报告 `unavailable`。
- 2026-08-13：新增本地 speech-to-speech Worker 受控执行适配器：通过 `AUTOLIVE_SPEECH_TO_SPEECH_WORKER` 配置可执行文件，能力探测使用 `--capabilities-json`，任务输入/输出使用 JSON 文件和 `.partial` 原子提交；支持参数数组、超时、取消、子进程回收和失败清理。桌面端新增真实能力状态展示；尚未接入 Hugging Face 真实模型、直连凭证和媒体音轨切换。
- 2026-08-13：新增按播放代际、循环轮次和片段上下文启动/取消 speech-to-speech Worker 的 Rust/Tauri 调度入口；Worker 结果经过上下文、锁定字段、时长、音频格式和 SHA-256 校验后进入候选槽，未通过时回退上一条可用音轨。真实 Hugging Face 适配桥、TTS 候选产物和播放器媒体音轨热替换仍未完成。
- 2026-08-14：接入本地 Hugging Face `speech-to-speech` WebSocket 适配器入口、受控环境依赖说明、片段安全余量调度和到 `start_at_ms` 边界才提交候选；适配器将返回音频转换为当前源采样率/声道/目标时长并计算 WAV SHA-256。当前机器未安装 `websockets`，未配置真实 `speech-to-speech serve`，因此仅通过语法、能力不可用回退和 Rust/React 契约验证，未宣称实机无缝换轨完成。
- 2026-08-14：Rust 新增 OpenAI-compatible `direct_model_chat` 本地命令和真实 HTTP 请求解析，使用租约描述地址，强制委托凭证和短期过期时间，不提供独立 URL 覆盖字段，不记录模型正文；可信租约注入和 Go 供应商凭证委托尚未完成，命令在无凭证时明确返回不可用。
- 2026-08-14（历史状态）：修正实时幻化与普通声音处理顺序：实时候选不再被误认为已写入基础 MP4；当时最终效果窗口仅对已实现的增益/响度路径报告 `runtime`，高级 DSP 参数报告 `unavailable` 并保持上一条可用音轨；该阶段性边界已由 2026-08-17 的实时预览方案更新覆盖。
- 2026-08-14：补齐视频/声音处理状态的 `disabled/unavailable` 区分、Tauri 最终效果窗口 capability、完整音频参数字段（输入/输出增益、采样率、输出码率）和声音 profile 提交；参数面板继续只使用 Ant Design，不覆盖组件库样式，真实 DSP Worker 仍保持明确不可用回退。
- 2026-08-14：代码总监复核收敛直连调用文档状态，明确 Go 调用摘要接口已提供但 Rust 自动上报、可信租约注入和供应商凭证委托尚未接通；清理无调用的直连 URL 辅助代码，并将 Worker 成功契约测试的超时预算与生产硬超时解耦，避免全仓并行测试的偶发启动抖动被误判为 Worker 超时。
- 2026-08-14（历史记录）：完成旧 PRD 残留复核与清理收口；其中历史研究 Worker 标识和研究报告闭环不属于当前产品，遗留代码仅用于迁移审计。历史数据库迁移按安全约束保留，不删除已部署数据；新代码不得读写旧任务/预占表。
- 2026-08-14（历史记录）：补齐媒体基础滤镜映射与桌面端状态；波形/频谱诊断保留，MFCC、SNR、共振峰、65Hz～20000Hz 视觉调制和研究报告不再纳入当前产品。
- 2026-08-14：接入跨平台 FFmpeg 资源打包入口和运行时资源校验。当前实现采用 Tauri bundle 内置资源优先、已校验应用数据目录下载/安装兜底；正式发布前仍需许可证复核、macOS 签名公证、Windows 原生运行和安装包验收。
- 2026-08-17：更新并落地文档契约：源视频导入扩展为 `.mp4`、`.mov`、`.mkv`、`.avi`、`.webm`、`.m4v`、`.ts`、`.m2ts`、`.flv`、`.wmv`、`.3gp`；桌面端已使用多格式选择器和 `probe_local_video` FFprobe 探测，通用源/产物 SHA-256 与自动兼容预览仍待后续；补齐普通声音处理和 `disabled/configured/runtime/unavailable` 状态；实时音频幻化参数不扩展；桌面 UI 取消独立“运行资源”卡片和安装/取消/目录选择/清理资源按钮，后台 FFmpeg/FFprobe 资源准备能力保留。本地声音/视频研究分析从当前产品范围移除。
- 2026-08-17（历史状态，已由 2026-08-24 正式参数完整接入覆盖）：当时只接入 21 项 FFmpeg 声音参数，环境声素材混合、干湿比和高级分析仍为 `unavailable`；现已补齐真实第二输入、湿声支路和混音后分析/处理，配置保存仍不等于 `ready`。
- 2026-08-20：普通源同步与 PCM 恢复统一持有覆盖创建、追赶和提交全程的原子占位；N+1 prepare 在占位期间退避而不推进音轨 token，修复“候选追赶期间收到更新切换请求”被误报为音频源不可用。用户主动暂停或停止仍可取消同步，前端不再把该预期取消显示成硬回退。
- 2026-08-20：按用户参考图建立桌面 UI 深色高密度三栏重做阶段：固定 `1728×1044` 视觉基线、六张真实状态卡、基础视频参数可见白名单和右栏声音/输出职责；视觉复刻不继承参考图的实时话术、研究/检测规避、内容指纹或 OBS/RTMP 功能，并增加截图、启动壳、键盘/缩放/窄屏验收。
- 2026-08-20：桌面 UI 同尺寸对照通过后，将参数契约门禁从历史研究/实时话术字段改为当前基础视频白名单、普通声音参数和范围声明校验，避免已退出本版本的字段反向阻塞文档收口。
- 2026-08-20：统一高级声音、随机插话和固定话术抽屉的 `760px/560px/560px` 宽度上限、深色中性层级、双行标题/真实状态摘要、卡片分区、正文滚动和固定底部主操作；补充持续标签、窄屏单列和删除预设确认，保持三条现有声音链路边界不变。
- 2026-08-21（历史参数卡反馈口径，已由 2026-08-24 当前后续决策覆盖）：补充参数卡/声音预设只响应真实输出变化的实现与验收任务，相同值和未提交候选不反馈；新增随机插话 WebView/PortAudio 双出口任务，要求 PortAudio 混入唯一输出流并持续 duck，补齐目录/文件校验、暂停停止、固定话术抢占、失败回退、状态原因和资源清理。本版本继续排除实时话术幻化与研究能力。
- 2026-08-21（历史方案，已由 2026-08-26 登录安全目标覆盖）：曾新增桌面记住账号/密码/激活码任务；激活码表单和凭据持久化已由 2026-08-24 的账号密码登录与服务端自动设备绑定替代，登录密码持久化又将在 Phase 6B 删除。目标态只保存账号偏好，并由 Rust 独占 Refresh Token 保持登录；工作台仍执行有效 `desktop` 会话 + 当前设备账号授权双门禁。
- 2026-08-21：修复工作台 fixed 会话状态层压住随机插话抽屉的问题：账号/退出、激活有效期和会话提示降回页面层级，Ant Design 抽屉以公开属性保持遮罩、固定头尾、正文滚动与键盘行为；补充静态层级契约和桌面/窄屏焦点验收，随机插话业务逻辑不变。
- 2026-08-21：补充随机插话配置校验和结构化错误展示；当前生效语义已由 2026-08-24 收口为“保存只更新 playback snapshot，下一段应用新固定选择或重新抽样，当前活动插话不中断且不调用 `StopInterlude`”。两种模式都保存并校验 `audio_preset_ids`，目录/范围/唯一性、asset scope 和锁损坏继续拒绝保存并保留当前插话。
- 2026-08-21（历史参数卡反馈口径，已由 2026-08-24 当前后续决策覆盖）：收口两类 UI 反馈：参数卡/声音预设仍以真实输出变化为唯一触发，单次约 `720ms` 并继承各 box 自身强调色，减少动态效果时禁用；凭据成功提示 `4` 秒自动消失，新操作和卸载清理旧计时器，代际保护避免误清后续提示，警告/错误不自动隐藏。
- 2026-08-21（历史参数卡反馈口径，已由 2026-08-24 当前后续决策覆盖）：把“普通声音 · 参数状态”和“声音预设池 · 当前选择”区块容器纳入既有真实输出变化反馈，分别以实际输出参数集合和实际参与预设集合为事实源；沿用约 `720ms`、自身强调色及减少动态效果禁用，相同集合、轮询、预热和失败不反馈。
- 2026-08-21：完成 Windows release 媒体子进程后台化和周期声音 EOF 连续性修复：FFmpeg/FFprobe 统一设置 `CREATE_NO_WINDOW` 且不吞错误；Scheduled 候选仍以 `750ms` 就绪，成为 current 后保留有界 `5130ms` PCM 水位，`1500ms` 真断粮检测、一次恢复和失败回退不变，并新增 Windows 安装包持续循环验收。
- 2026-08-21（历史参数卡反馈口径，已由 2026-08-24 当前后续决策覆盖）：完成普通声音实际参数事实源收口：Rust `PlaybackSnapshot`/DTO 暴露已提交 `audio_stream_params`、`audio_stream_variants`，运行态按有效混合支路为每字段生成顺序无关的多重集合签名；真实值或支路增删驱动对应参数卡与区块约 `720ms` 的自身色反馈，等权重排、相同快照、预热、失败和非运行态不反馈。预设 ID/权重未进入本轮 Rust 契约，继续沿用现有参与确认状态。
- 2026-08-24：修复普通声音参数反馈投影回归：真实 `AudioEffectParams` DTO 合法包含 `natural_voice_mode` 和可选 `voice_library_id`，前端改为只投影并校验反馈实际消费的数值字段，不再通过 `Object.values(...)` 全量数值判定丢弃有效支路；参数卡与“普通声音 · 参数状态”区块的手工验收仍未完成。
- 2026-08-24（历史插话选择模型，处理出口已由 2026-08-25 当前边界覆盖）：建立固定/随机选择、最多四支路、单图和受管任务边界；当前默认随机池、22×35 共享预设、环境声解析、WebView 短期处理缓存和缓存生命周期以 2026-08-25 记录为准。
- 2026-08-24：收窄基础视频 X/Y 空间偏移的自动周期预览附加幅度，由 `±2 px` 调整为 `±0.5 px`；继续保留 `-4–+4 px` 手动调节范围、`0 px` 默认值和挂件模块独立画面偏移边界。
- 2026-08-24：修复普通声音 `3–5s` 短周期偶发整轮静音：N+1 周期候选使用 `max(3, playback_speed + 1)`、1 秒初始突发的有界 CatchUp，按 prepare 已用时间动态扩大覆盖水位，未覆盖目标时保持 current 并返回可重试状态；即时插话继续使用固定窗口 RealTime。新增生产调用契约和 3 秒边界水位回归测试；用户 MP4 的合法四支路连续 30 轮 3 秒压力测试全部就绪，声音周期参数范围及默认值不变。
