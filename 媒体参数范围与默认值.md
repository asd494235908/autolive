# 媒体参数范围与默认值

## 1. 文档目的

本文是 Rust 桌面端、React 参数面板和本地媒体 Worker 共用的参数契约，解决“参数属于哪个模块、单位是什么、默认值是什么、允许调到什么范围”的歧义。

当前媒体 Worker 已映射基础视频 `亮度/对比度/饱和度/色相/模糊/锐化/细节增强/噪点/动态裁剪/像素扰动/空间偏移/缩放` 和基础音频 `输入增益/输出增益/响度/显式采样率/源采样率可用时的音高微移` 到实际 FFmpeg 滤镜。其他研究参数仍可编辑和校验，但在算法映射完成前，启用非默认值会被 Worker 明确拒绝，不会静默忽略或伪造已生效。

这些范围是首版 UI 和任务校验的参考范围，不代表所有媒体算法的理论极限。具体媒体库通过适配器映射到本契约，不能把底层库的裸参数直接暴露给 UI。

竞品截图中的数值（例如 `0.11 st`、`4%`、`35.5 dB`、`1.08`）按“运行时当前值/测量值”理解，不直接作为本项目默认值。配置参数、实时测量值、资源统计和倒计时必须在数据模型中分开。

所有数值参数必须同时保存：

- `value`：当前值
- `unit`：单位
- `min` / `max`：允许范围
- `default_source`：`source_metadata`、`system_default` 或 `user_profile`
- `enabled`：是否启用
- `algorithm_version`：处理算法版本

默认策略是“先读取源素材，再允许微调”：源视频的时长、分辨率、帧率、色域、音频采样率、声道数和码率从媒体元数据读取；未启用的实验参数默认不改变源素材。

## 2. 播放状态参数

当前首版播放口径：导入一个源视频后在同一个窗口持续循环，不生成离线视频版本、不建立版本队列。

| 参数 | 范围/枚举 | 单位 | 默认值 | 说明 |
|---|---|---:|---:|---|
| 源素材时长 | `> 0` | ms | 自动读取 | 使用媒体探测结果，不允许手工覆盖为不一致值 |
| 源视频循环 | 开/关 | — | 开 | 同一窗口从源视频 0 秒继续播放 |
| 播放轮次 | `0–无限` | 轮 | 自动记录 | 每次源视频重新开始时递增 |
| 首项原声立即播放 | 开/关 | — | 开 | 源视频通过可读性校验后立即播放 |

源素材可以是几分钟、30 分钟或 1 小时；播放器只记录实际播放轮次，不根据时长计算版本数量。

## 3. 音频模块参数

### 3.0 独立功能开关

| 开关 | 字段 | 默认值 | 行为 |
|---|---|---|---|
| 声音处理 | `audio_processing_enabled` | 关 | 控制普通音频效果；关闭时保持源音轨 |
| 实时幻化 | `realtime_audio_variant_enabled` | 关 | 独立控制 speech-to-speech 话术决策、实时变体和当前播放音轨替换 |

实时幻化关闭时不启动 speech-to-speech Worker，不生成候选音轨；声音处理开关和实时幻化开关互不隐式开启或关闭。

### 3.1 基础与自然真人模式

| 参数 | 参考范围 | 单位 | 默认值 |
|---|---:|---:|---:|
| 自然真人模式 | `original` / `natural_dynamic` | 枚举 | `original` |
| 随机变声周期 | `0.5–60` | s | `5` |
| 下一次变声周期 | `0–周期` | s | 只读倒计时 |
| 音高微移 | `-2–+2` | semitone | `0` |
| 输入增益 | `-6–+6` | dB | `0` |
| 输出增益 | `-6–+6` | dB | `0` |
| 响度调整 | `-6–+6` | dB | `0` |
| 干湿比 | `0–100` | % | `0` |
| 轻混响湿声 | `0–20` | % | `0` |
| 环境底噪电平 | `-60–-20` | dBFS | 自动读取/关闭 |
| 采样率 | `source` / `44100` / `48000` | Hz | `source` |
| 输出音频码率 | `64–320` | kbps | 自动读取 |

### 3.2 研究分析参数

| 参数 | 参考范围 | 单位 | 默认值 | 说明 |
|---|---:|---:|---:|---|
| MFCC 维度 | `1–40` | 阶 | `13` | 仅用于分析/实验，不直接显示为音色百分比 |
| MFCC 偏移 | `-20–+20` | % | `0` | 相对偏移；实际值保留原始 MFCC 报告 |
| SNR 浮动 | `-6–+6` | dB | `0` | 与源素材估计值比较 |
| 当前 SNR | `0–60` | dB | 自动测量 | 只读诊断值；不能与 SNR 浮动幅度混用 |
| 共振峰偏移 | `-5–+5` | % | `0` | 超出范围需人工确认 |
| 当前共振峰 | `20–10000` | Hz | 自动测量 | 只读诊断值；截图中的 7261Hz 属于当前测量值 |
| 滤波 Q 值 | `0.3–10` | 无量纲 | `1` | 由滤波器适配器映射 |
| 相位扰动 | `-20–+20` | % | `0` |
| 颤音频率 | `3–8` | Hz | `5` |
| 颤音深度 | `0–3` | % | `0` |
| 频谱扰动 | `0–10` | % | `0` |
| 频谱盲区宽度 | `0–5` | % | `0` |
| 音色库 | 已安装音色 ID | — | `source` | 不在客户端硬编码供应商密钥 |
| 可用音色数量 | `0–1,000,000` | 个 | 自动统计 | 只读资源统计；截图中的 17.5 万+ 属于展示示例 |

“下一次变声”不是配置参数，而是运行时状态：保存 `period_ms`、`next_change_at`、`countdown_ms` 和当前随机种子；UI 显示倒计时和进度条，任务暂停、取消或切换媒体时必须停止计时器。

MFCC 的维度、采样率、FFT 窗口和 hop length 属于分析配置；工程实现应使用成熟音频分析库，不自行实现 MFCC。MFCC 维度本身不是“音色变化百分比”。

## 4. 视频模块参数

### 4.1 画面效果与空间/时间扰动

| 参数 | 参考范围 | 单位 | 默认值 |
|---|---:|---:|---:|
| 亮度 | `-100–+100` | % | `0` |
| 对比度 | `0–200` | % | `100` |
| 饱和度 | `0–200` | % | `100` |
| 色相旋转 | `-180–+180` | ° | `0` |
| 模糊半径 | `0–8` | px | `0` |
| 锐化 | `0–100` | % | `0` |
| 噪点强度 | `0–8` | % | `0` |
| 细节增强 | `0–50` | % | `0` |
| 裁剪边缘平滑度 | `0–1` | 无量纲 | `0.5` |
| X/Y 空间偏移 | `-4–+4` | px | `0` |
| 像素级扰动 | `0–2` | px | `0` |
| 帧内微扰幅度 | `0–2` | px | `0` |
| 帧间微扰概率 | `0–20` | % | `0` |
| 动态裁剪 | `0–4` | %/边 | `0` |
| 帧率微扰频率 | `0.01–2` | Hz | `0.1` |
| 帧率微扰幅度 | `0–2` | fps | `0` |
| 色域转换 | `source` / `BT.601` / `BT.709` / `sRGB` | — | `source` |
| 色域转换强度 | `0–100` | % | `0` |
| 像素级缩放 | `0.95–1.05` | 倍 | `1.0` |

只要启用了上述任意视频参数，输出 MP4 必须经过解码、处理和重新编码；首版不使用视频流 `copy`。即使没有启用参数，研究产物也应保留处理链和编码策略记录。

### 4.2 65Hz～20000Hz 视觉调制实验

这组参数归属**视频模块**，名称为“视觉调制频率”，用于画面纹理、粒子、空间噪声或时间调制实验；它不是音频频段控制，也不改变音频采样率。

| 参数 | 参考范围 | 单位 | 默认值 |
|---|---:|---:|---:|
| 目标频率 | `65–20000` | Hz | 关闭 |
| 核心频率 | `65–20000` | Hz | 跟随目标频率 |
| 频段权重 | `0.5–1.5` | 倍 | `1.0` |
| 波频强度 | `0–1` | 归一化 | `0` |
| 波频颗粒数 | `1–100` | 个 | `20` |
| 波频电平 | `0–1` | 归一化 | `0` |
| 动态均衡阈值 | `0–20` | 归一化刻度 | `10` |
| 通道偏移 | `-10–+10` | % | `0` |
| 空间维度 | `1–3` | 维 | `2` |
| 空间 X/Y 偏移 | `-10–+10` | px | `0` |
| 帧扰动概率 | `0–20` | % | `0` |

首版固定频段列表为：`65、92、131、188、267、381、544、777、1110、1585、2263、20000 Hz`。每个频段保存一个独立的 `band_weight`，例如 `65Hz: 1.08`；`核心频率`和`目标频率`仍是独立字段。截图中的 `1.08`、`0.93` 等值应显示为倍数，不能显示成百分比或 dB。

视频帧序列存在采样上限：有效时间调制频率不能超过实际处理帧率的一半。UI 必须同时显示 `target_frequency` 和 `effective_frequency`；例如 30 fps 输出的有效时间调制上限约为 15 Hz，65Hz～20000Hz 的数值不能声称在普通 30/60 fps MP4 中都能被忠实还原。超过上限时应降级、标记混叠或改为纹理/空间频率实验。

## 5. 挂件与切片参数

| 参数 | 参考范围 | 单位 | 默认值 |
|---|---:|---:|---:|
| 随机图形透明度 | `0–50` | % | `0` |
| 随机图形大小 | `1–64` | px | `4` |
| 抽象人脸数量 | `0–10` | 个 | `0` |
| 抽象人脸大小 | `1–10` | %/画面宽度 | `2` |
| 抽象人脸透明度 | `0–30` | % | `0` |
| 画面偏移 | `-10–+10` | px | `0` |
| 切片长度 | `0.5–10` | s | `5` |
| 切片最小长度 | `1–60` | s | `10` |
| 切片触发间隔 | `5–120` | s | `15` |

“切片长度”表示一次挂件/实验效果的持续时间；“切片最小长度”表示可被选择的源片段下限，二者不是同一个字段。切片触发间隔必须大于等于切片长度，参数校验不能只检查单字段范围。

## 6. 话术幻化与连续优先参数

本节只定义当前播放音轨的实时话术幻化参数。它处理正在播放视频的音频，不生成新的 MP4。

| 参数 | 参考范围 | 单位 | 默认值 |
|---|---:|---:|---:|
| 单段气口 | `100–300` | ms | `180` |
| 音频变速 | `0.95–1.05` | 倍 | `1.0` |
| 实时安全余量 | `0.5–10` | s | `2` |
| 话术生成失败回退 | `原声` / `上一候选` / `保持当前音轨` | — | `原声` |

模型或 speech-to-speech Worker 处理不及时时，当前视频继续播放并保持当前音轨；只有候选完成同步和安全门禁后才允许替换当前音轨。

## 7. SHA-256 阶段哈希契约

对 MP4 产物，哈希对象是**整个 MP4 文件的字节流**，不是抽样帧、视频流、音频流或文件名。JSON、WAV 等非 MP4 中间产物也计算完整字节流 SHA-256，但字段名称使用 `artifact_sha256`，不能伪装成 MP4 哈希。

每个阶段执行以下顺序：

1. 阶段开始前，对每个输入产物计算完整字节流 SHA-256；如果输入是 MP4，同时记录 `input_mp4_sha256`。
2. 阶段执行并写入临时产物，不覆盖原文件。
3. 产物完成并通过容器/媒体可读性校验后，对整个输出文件计算 `output_artifact_sha256`；如果输出是 MP4，同时记录 `output_mp4_sha256`。
4. 将阶段、文件路径摘要、文件大小、输入哈希、输出哈希、算法版本、参数快照和随机种子写入产物清单。

界面字段约定：

- `original_sha256`：源 MP4 第一次进入流水线前的固定哈希，对应研究报告 `source_mp4_sha256`。
- `current_sha256`：当前选中阶段产物的完整 MP4 哈希；研究阶段另区分 `input_mp4_sha256` 与 `current_mp4_sha256`。
- `stage_input_sha256` / `stage_output_sha256`：阶段级完整产物哈希；MP4 另记录对应的 `*_mp4_sha256`。

## 8. 参考依据

- 媒体滤镜和参数适配优先基于 [FFmpeg Filters Documentation](https://ffmpeg.org/ffmpeg-filters.html) 等成熟媒体库；项目不自行实现编解码器、滤镜和播放内核。
- MFCC 参数定义参考 [librosa.feature.mfcc](https://librosa.org/doc/latest/generated/librosa.feature.mfcc.html)；具体维度、采样率和 FFT 配置由本项目参数契约约束。
- SHA-256 使用 [NIST FIPS 180-4 Secure Hash Standard](https://csrc.nist.gov/pubs/fips/180/4/final) 作为算法依据。
