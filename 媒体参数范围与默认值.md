# 媒体参数范围与默认值

> 当前视频参数执行契约（2026-08-28，本文最高优先级）：除水平/垂直翻转外的 83 个视频 UI 参数以一份快照原子进入 mpv `gpu-next/libplacebo` 实时 GPU 主链；像素和合成参数由 libplacebo shader 执行，真实帧率类参数由 mpv 播放调度执行。GPU83 失败时按会话单向降级到 libavfilter CPU4，CPU4 只接受 `brightness_percent`、`contrast_percent`、`saturation_percent`、`hue_rotation_degrees`，再失败保持 Original。周期更新不生成视频 period、重编码文件或 MSE 数据。

> 唯一实施入口：[`mpv/libplacebo 实时 GPU 主链实施方案`](./docs/superpowers/plans/2026-08-28-mpv-libplacebo实时GPU主链实施方案.md)。本文后续 MSE、SourceBuffer、FFmpeg 视频 Worker、period-ready 和逐周期 `.partial/.ready` 描述均为历史视频链记录，不属于当前参数执行契约；声音候选和其他非视频临时文件规则继续有效。

> 视频输入边界（2026-08-29）：新导入只通过 FFprobe 做有界容器与真实流探测，mpv 直接读取只读 `source_path`；成功项固定 `playback_reference=source_path`、`compatibility_mode=direct`。导入不得整段转码或生成兼容引用；历史字段只作向后读取，也不作为 GPU83/CPU4 生效证据。
>
> 当前实现状态（更新至 2026-08-30）：mpv 持久 IPC、受控 hook、精确 83 项映射和正式周期入口已接线。Phase 3A v6 已在 D3D11/Vulkan 的精确 `1920×1080@60fps` 表面完成 61 项产品参数和 11 项 runtime 门控验证；第二阶段把 18 项具有真实消费点的 PTS 调度字段接入开发生产链，自动周期现准入 `79/83`。两项颜色候选已定义为 libplacebo 感知色域映射，但外部 JSON IPC 尚不能与 shader 快照证明帧级原子提交，开关关闭语义及 HDR/驱动颜色合同也未完成验证；两项跨帧历史缺少历史纹理，四项均为 `Unavailable`。CPU4 四字段和会话级单向降级代码已接线；GPU 重建先重放最后活动 shader 快照再恢复播放。PortAudio 可听时钟与 mpv 同步控制代码已接线且不改变既有声音处理语义。Phase 4/5/6 的真实联合媒体与长稳仍待验收；兼容 Original 继续作为兜底，旧 MSE/FFmpeg 视频链不得进入正式周期。
>
> GPU83 生效必须同时满足：83 项映射完整、生产 mpv 显式加载受控 shader、完整快照命令成功、下一真实帧已呈现、stderr 无 shader/VO 禁用错误、逐字段及组合输出证据通过。CPU4 只允许四字段，Original 不应用任何视频效果。
>
> 下一版本执行口径（2026-08-28）：63 项按单帧/同帧合成、mpv PTS 调度、跨帧历史三组补齐，字段只有在中性、低感知、明显对照、组合和真实呈现证据全部通过后才能从 `Unavailable` 改为可用。mpv user shader 的同帧 `SAVE/BIND` 不等于跨帧历史；独立画中画时间轴缺少真实历史帧时必须继续拒绝。音画同步只消费 PortAudio 已有可听 PTS，不修改任何声音参数或候选算法；4K 不在本版本。
>
> Phase 3A 候选状态（2026-08-28）：现有 20 项的 hue/luma/裁剪回混修复，以及图像修复两字段、抽象几何脸三字段、局部模糊启用/区域/半径三字段、边缘填充启用/羽化两字段、通道偏移一字段、21 字段共享空间调制 pass 和 9 字段同帧叠加 pass 已进入 `desktop/tools/shader-candidates/`。同帧叠加覆盖随机图形启用/数量/透明度/大小、共享挂件偏移和画中画启用/比例/透明度/旋转：随机图形按 8×4 网格直接定位至多 32 个确定性伪随机槽，每像素固定 O(1)；画中画仅在旋转区域内重采样同一当前帧，最多增加一次纹理采样。Phase 3B 的 PIP runtime X/Y jitter 与计划级随机图形 seed 已作为本地候选参数加入该 pass：PIP 只平移最多 ±4px，24 位 seed 只改变随机图形的位置、形状和颜色，两者互不影响；时间轴解锁仍属 Phase 3C。抽象脸候选以 UV 直定位最多十槽并限制每像素最多一次 SDF；局部模糊候选按归一化 X/Y 百分比定义区域，使用九点二维 tent 核、边缘羽化且保留 alpha；边缘填充候选沿用旧 FFmpeg 契约，`0%` 执行 2px 硬填充，非零值映射为 `2–16px` 有界宽度与百分比混合，内部像素不增加采样；通道偏移候选把 `-10–10%` 直接归一化为浮点 RGB 的红/蓝反向偏移，不保留旧 8-bit 抖动，绿色和 alpha 不变。共享空间调制覆盖目标/核心频率、波形、动态均衡、空间维度、频率空间 XY 和 12 个固定频段；`0Hz` 是可选频率 `None` 哨兵，有效频率对数映射到 `1–32` 个画面空间周期，第三维使用同帧径向分量，不读取 PTS、历史或音频频谱。当前候选契约为 9 文件、61 项产品参数加 11 项 runtime 选项；6 个布尔产品开关使用完整 72 键 `baseline/active` 二态，55 个其余产品参数使用 `baseline/low/active` 三档，low 可用广泛的一码值响应证明低感知效果，active 仍执行明显差异阈值，频率与动态均衡阈值不要求响应幅度伪单调。11 项 runtime 和 1 个全激活组合使用两档，共 73 个逻辑场景，另有 1 个 seed/PIP 预期相同隔离场景，总计 203 张同 PTS 场景截图。单后端门禁固定只用一个 mpv 进程：先取无 shader 参考帧，再动态追加 9 个 shader，以 72 键全激活快照截取独立预热帧，确保动态 `WHEN` pass 在日志基线前完成首次编译/渲染，最后恢复全默认并截取中性对照帧；三帧保持同 PTS，只有无 shader/全默认两帧进入中性分析。性能阶段只在全默认与 72 键全激活组合间交替。隐藏窗口固定 geometry，并取消默认 90% 自动缩小、边框、视频缩放、HiDPI 窗口缩放和自动窗口重设；实际 OSD 表面仍须精确 `1920×1080`。`vo-passes` 的 pass 数量可变，单个 pass `count` 不是共享帧号；P99 只跳过相邻完全相同的有序 `index/desc/count/samples` 统计快照，并要求至少 100 份相邻不重复样本，不再要求所有 count 相等或单调。PID、P99、丢帧增长/重置和重复编译全部 fail-closed，失败同时保留 stdout/stderr。相关静态 Node 门禁为 `70/70`；非精确目标表面上原本通过的计时只保留诊断数据并标记 `unverified`，原本失败状态保持失败；Phase 1/3A 报告仅在最终通过且全部 GPU 源与实际表面分辨率匹配时填写最高 `1920×1080`，否则为 `null`；候选不打包、不由生产加载。最新隐藏报告 `artifacts/mpv-phase3a-candidates-20260828-v3.json` 仍为失败证据：D3D11 的源和解码输出为 `1920×1080`，旧窗口实际表面却为 `1777×1000`，该表面的 P99 `8.779ms`、118 份有效样本、同 PID、零新增丢帧、无重复编译和默认中性通过均不能外推为 1080p；74 个场景中 51 个通过、23 个失败，Vulkan 仍不可用。v3 后已静态修正 19 项门禁假阴性和 4 项真实弱响应：图像修复移除额外 `0.35` 混合衰减但仍固定五采样，`wave_level` 与 `runtime_frame_inner_active` 最大响应从 1 提升为有界 4 码值；尚未再次实机复验。生产能力继续为 `20/83`，生产 shader 固定哈希不变。色彩空间转换缺少明确源/目标 primaries、TRC、范围与输出标签，继续“正式需求·待实现/未接入”；不得用 RGB 上的固定矩阵色偏冒充完整颜色管理。

> Phase 3A v4 复验覆盖（2026-08-28，覆盖上段 v3 的“最新报告”口径）：[`artifacts/mpv-phase3a-candidates-20260828-v4.json`](./artifacts/mpv-phase3a-candidates-20260828-v4.json) 仍为失败证据。D3D11 与 Vulkan/WinVK 都已建立 `gpu-next`、保持同 PID、默认中性且无重复编译，但最小化窗口令两者实际 OSD 表面只有 `160×28`，原始 P99 只能诊断；D3D11 还发生丢帧计数重置，且两个后端合计剩余 frame-inner/overlay-offset 场景失败。整改后门禁不再最小化窗口，并移除会按工作区缩小固定 `geometry` 的 `autofit-larger=100%x100%`，保留不抢焦点、不进任务栏的受控窗口；overlay 位移不再要求差异幅度单调，稀疏 frame-inner 固定提升到 16 码值。整改后的静态门禁通过，尚未再次运行视频；生产仍为 `20/83`，生产 shader 固定哈希不变，音频未修改。

> Phase 3A v4 后样本整改（2026-08-28）：v4 的 D3D11 丢帧计数重置来自 `0.75s` 样本在约 `2s` 更新阶段跨越无限循环边界；门禁继续把任何重置判为失败，不增加豁免。候选门禁现在只生成一个 4 秒 `1920×1080@60` 专用样本，为 120 次更新预留额外 120 帧，Phase 1 五档样本不受影响。完整静态测试 `106/106`；尚未再次运行视频，生产与音频均未修改。

> Phase 3A 样本报告证据（2026-08-28）：后续报告必须直接记录 4 秒/240 帧样本、120 次/2 秒更新窗口和 120 帧/2 秒余量；余量不足 1 秒时启动前失败。完整静态测试 `107/107`，本项仍未运行视频。

> Phase 3A 防重复实机门禁（2026-08-28）：CLI 必须逐次提供 `--confirm-real-media-gate` 和新的 `--report` 路径；缺确认或缺报告路径时退出码为 2，且不生成样本、不创建报告、不启动媒体。参数校验后、门禁运行前以 Node `open('wx')` 原子预占新报告文件，同名路径会在任何样本生成或媒体启动前拒绝，测试结果由同一句柄写入；已有报告不能覆盖。完整静态测试 `109/109`。

> Phase 3A 历史证据纠偏（2026-08-28）：v2 报告总状态为 `failed`，其旧版 `maximumValidatedResolution=1920x1080` 与总状态冲突并明确无效，不得用作 1080p 通过或生产提升依据。v3/v4 和当前门禁均在失败时输出 `null`；历史报告保持不可改写，仅作局部诊断。

> Phase 3B/5 调度与同步状态（更新至 2026-08-30）：纯 Rust 媒体 PTS/epoch 调度器和 11 项 runtime shader 选项已存在，18 个产品调度字段已通过受限 `ScheduledParameter` 接入；颜色两项继续受原子颜色合同门禁保护；`slice_min_length_ms` 与画中画独立时间轴继续受历史纹理门禁保护。mpv 观察固定为 PTS/FPS/pause/seeking/cache 五个属性；PortAudio 可听时钟提供实际 `audio.playback_speed`。唯一 speed 合成入口接受音频 `0.5–2.0`、调度 `0.5–1.5` 和同步 `0.98–1.02`，结果由 `0.25–4.0` 值类型限制。当前开发能力为 `79/83`；Phase 5 代码接线已完成但真实联合媒体门禁尚未通过。

> 普通声音候选边界补充（2026-08-27）：参数快照只对与源媒体有效音轨 `[audio_start_ms, audio_end_ms)` 相交的候选窗口执行。完全位于容器无声头尾的周期静默跳过，不计为参数失败或静音内容失败；部分相交仍执行完整参数链。该规则与插话的 `ducking_depth_db` 无关，插话只在实际播放期间调节主节目增益。
>
> 多支路声音时间轴补充（2026-08-27）：普通声音仍由单一源音轨 `asplit` 为 `1–4` 条预设效果支路并等权 `amix` 为一条输出，不改为单预设路线。源入口、每条效果支路和环境声辅助输入在混合前必须归一为从零开始的连续音频时间戳；有限主声与湿声、生成噪声或循环环境声的混合按最短有限输入结束。末尾 `apad/atrim` 只补齐候选覆盖时长，不能掩盖上游空输出；最终产物继续执行 `volumedetect` 静音门禁。

## 1. 文档目的

本文是 Rust 桌面端、React 参数面板和本地媒体 Worker 共用的参数契约，解决“参数属于哪个模块、单位是什么、默认值是什么、允许调到什么范围”的歧义。

当前正式模型中语义已经确认且已有映射的参数，不等于已经通过新的视频周期准入门禁。视频侧除水平、垂直翻转外的 `83` 项是目标原子快照；任何一项缺少可执行映射、组合校验或输出验证时，整份处理结果都不能宣称完整生效，必须按目标时限回原视频。65–20000Hz 明确是视频空间频段，按对数映射到每幅画面 1–32 个空间周期，不进入音频频谱。音频侧除原有 FFmpeg 和 Signalsmith Stretch 链外，已接入自然动态包络、本地音色 ID 轻量 EQ、真实环境声第二输入、干湿支路、频谱盲区、realFFT/rustdct MFCC 分析与重建、滚动 RMS SNR 处理以及 LPC F1/F2/F3 诊断。仍标记“正式需求待实现/待确认”的条目不能用近似滤镜冒充能力。

当前唯一正式类型和命令为：

- `MediaEffectParams`：聚合 `audio: AudioEffectParams`、`video: VideoEffectParams`、`advanced: AdvancedEffectParams`。
- `get_default_media_effect_params`：读取正式默认值。
- `validate_media_effect_params`：只校验结构、有限值、范围和组合；返回成功不代表媒体 Worker 已映射算法。
- 禁止新增或继续使用 `research_*`、`*ResearchParams`、旧默认值/校验 IPC。旧报告 Worker/IPC 已移除，历史文档和迁移不作为当前契约。

视频参数只有同时满足“生产 mpv 已加载 shader、完整快照命令成功、下一真实帧已呈现、stderr 无禁用错误、输出证据通过”时才可显示为已生效。普通声音预览展示当前 N 音轨、N+1 候选阶段和运行状态；Web Audio 不执行普通声音效果，只提供播放、duck 和诊断。

这些范围是首版 UI 和任务校验的参考范围，不代表所有媒体算法的理论极限。具体媒体库通过适配器映射到本契约，不能把底层库的裸参数直接暴露给 UI。

竞品截图中的数值（例如 `0.11 st`、`4%`、`35.5 dB`、`1.08`）按“运行时当前值/测量值”理解，不直接作为本项目默认值。配置参数、只读诊断值、资源统计、倒计时和运行时生效状态必须在数据模型中分开。

所有数值参数必须同时保存：

- `value`：当前值
- `unit`：单位
- `min` / `max`：允许范围
- `default_source`：`source_metadata`、`system_default` 或 `user_profile`
- `enabled`：是否启用
- `algorithm_version`：处理算法版本

默认策略是“先读取播放池当前项，再允许微调”：当前项的时长、分辨率、帧率、色域、音频采样率、声道数和码率从媒体元数据读取；未启用的参数默认不改变对应源素材。

## 2. 播放状态参数

当前首版播放口径：文件选择或系统文件拖放按返回顺序形成最多 `100` 项的本地有序播放池。新增候选全部探测成功后才原子整批替换、追加或替换单项；既有条目可重排、删除或清空。取消、超限、重复路径或任一探测失败均保持旧池并显示错误，不提交部分成功项。任何成功编辑都停止当前资源；非空新池首项进入 `Ready` 后等待用户点击“播放”，空池回到无源状态。同一个最终效果窗口按 `A→B→…→A` 顺序循环，单项池自循环，任意时刻只有一个活动媒体源。播放池只存在当前进程，不生成离线视频版本、不建立生成版本队列，也不创建服务端媒体任务。

播放池允许视频与纯音频混排。纯音频项不创建 mpv 视频会话；视频处理和画中画状态为 `not_applicable`，普通声音周期、插话、固定话术和顺序循环继续可用。视频与纯音频互相切换时必须建立新的播放代际并完整释放上一媒体类型的进程、流、句柄和缓存所有权。

本地源视频允许的容器/扩展名为：`.mp4`、`.mov`、`.mkv`、`.avi`、`.webm`、`.m4v`、`.ts`、`.m2ts`、`.flv`、`.wmv`、`.3gp`。扩展名只用于文件选择白名单，是否可导入以 FFprobe 对容器、真实视频流、音频流和时长的探测结果为准，是否可播放再由生产 mpv 真实启动确认。`source_path` 始终只读；导入失败或取消保持旧池，播放失败如实报错，不准备一次性兼容视频。

| 参数 | 范围/枚举 | 单位 | 默认值 | 说明 |
|---|---|---:|---:|---|
| 播放池容量 | `0–100` | 项 | `0` | 新增候选按文件选择或系统拖放返回顺序保存；全部探测成功才原子替换、追加或替换单项，既有条目可原子重排、删除或清空，不允许部分提交 |
| 当前项素材时长 | `> 0` | ms | 自动读取 | 使用当前项媒体探测结果，不允许手工覆盖为不一致值 |
| 当前索引 | 空池为占位 `0`，非空池为 `0–pool_length-1` | 项 | `0` | 快照字段 `source_media_index`；空池时没有活动媒体源且该值只作占位，非空池时表示当前项在 `source_media_pool` 中的位置，项结束时单飞推进，末项回 `0` |
| 播放池循环轮次 | `0–无限` | 轮 | `0` | 快照字段 `playback_pool_cycle`；末项回到首项时递增，单项池每次回到自身时也递增 |
| 播放代际 | `0–无限` | 代 | 运行时自动记录 | 快照字段 `playback_generation`；播放池实际编辑或多项池切换到另一个活动媒体源时递增并按新媒体从 `0ms` 重建绝对时钟，单项池自循环保持该值 |
| 首项原声播放 | 开/关 | — | 开 | 新池整批探测成功后首项保持 `Ready`；用户点击主页“播放”才打开/聚焦最终效果窗口并开始播放 |

池内每项素材可以是几分钟、30 分钟或 1 小时；播放器只记录当前索引、实际池循环轮次和播放代际，不根据时长计算版本数量。

### 2.1 RTMP 输出参数（正式需求·待实施）

RTMP/RTMPS 输出配置使用独立 `RtmpOutputConfig`，不并入 `MediaEffectParams`，也不改变本地播放参数的事实源。用户输入的是完整 ZLMediaKit 发布地址；默认同时输出画面和声音，可以只选择其中一种，但至少选择一轨。第一阶段不持久化完整地址，不接受自由格式 FFmpeg 参数，所有白名单与范围必须在 React 和 Rust 信任边界重复校验。

| 参数 | 字段 | 范围/枚举 | 默认值 | 说明 |
|---|---|---|---|---|
| 完整发布地址 | `target_url` | UTF-8 `1–2048` 字节；仅 `rtmp://`、`rtmps://` | 空 | 必须有 host 和发布路径，不含控制字符、CR/LF 或首尾空白；日志、错误和状态只返回脱敏地址 |
| 输出画面 | `video_enabled` | 开/关 | 开 | 关闭时不启动视频解码、GPU 处理和视频编码 |
| 输出声音 | `audio_enabled` | 开/关 | 开 | 与画面至少开启一项；声音取最终 PCM 总线并编码 AAC-LC |
| 输出分辨率 | `width` / `height` | 第一阶段白名单 `1280×720`、`1920×1080` | `1920×1080` | 仅画面开启时生效；4K 不在第一阶段范围 |
| 输出帧率 | `fps` | 白名单 `25/30/50/60` | `30` | `50/60` 只有通过目标 GPU、网络和 ZLMediaKit 门禁后才可启用 |
| 视频码率 | `video_bitrate_kbps` | `500–50000` | `6000` | kbps；第一阶段 H.264、`yuv420p`、低延迟，UI 不暴露编码器裸参数 |
| 音频码率 | `audio_bitrate_kbps` | `64–320` | `160` | kbps；固定 AAC-LC、`48kHz`、双声道 |
| GOP | 内部固定策略 | `1–4` | `2` | 秒；第一阶段不开放 UI 编辑 |
| 重试次数 | 内部固定策略 | `0–5` | `5` | 仅网络类暂态错误使用 `1/2/4/8/15s` 退避；用户停止、换地址或退出立即取消 |

运行中锁定地址、轨道组合和编码规格；用户修改草稿不能改变活动会话，只有停止并重新开始成功后才提交。第一阶段换源会受控停止并重新发布，视频只使用推流启动时取得的静态 GPU83 快照；固定话术 PCM 尚未进入 Rust 最终总线，因此必须明确显示“不进入当前推流音轨”。这些限制在完成后续专项前不得标记为已接入。

### 2.2 AkVirtualCamera 虚拟摄像头输出参数（正式需求·待实施）

虚拟摄像头输出配置独立为 `VirtualCameraOutputConfig`，不得并入 83 项 `MediaEffectParams`，也不参与视频/声音随机周期：

| 字段 | 类型/范围 | 默认值 | 说明 |
|---|---|---|---|
| `enabled` | `bool` | `false` | 用户显式开启；未安装或 GPU 门禁失败时拒绝 |
| `device_id` | 固定内部值 | `GpAutoLiveCamera0` | 不允许前端自由填写 |
| `display_name` | 固定只读值 | `GpAutoLive Camera` | 用户在下游应用中选择 |
| `width` / `height` | 固定组合 | `1280 / 720` | 首版只有 720p；1080p 另过门禁 |
| `fps` | 固定值 | `30` | 首版不开放 60fps |
| `pixel_format` | 固定值 | `YUY2` | AkVirtualCamera direct mode；NV12 修复内存边界前禁止生产 |
| `idle_frame_policy` | 固定值 | `black` | 暂停、停止、纯音频、锁屏和无有效帧两帧内黑屏 |

运行状态只读字段至少包括 `source_effect_backend`、`capture_backend=windows_graphics_capture_d3d11`、`transform_backend=d3d11_video_processor`、`transport_backend=akvcam_mmap_cpu`、`zero_copy=false`、adapter LUID/name、回读 P50/P99、captured/sent/dropped frames、consumer count 和错误码。字段可保存不等于链路已实施；当前统一为“正式需求·待实施/未接入”。

资源上限固定为一个 WGC session、一套 D3D11 转换资源、三个 staging 帧槽、一个容量 1 latest-wins 投递槽和一个独立 GPL sidecar。队列满时只丢弃虚拟摄像头旧帧并计数，不得反压 mpv 或创建第二播放器/解码链。

## 3. 音频模块参数

### 3.0 独立功能开关

| 开关 | 字段 | 默认值 | 行为 |
|---|---|---|---|
| 声音处理 | `audio_processing_enabled` | 关 | 控制普通音频效果；关闭时保持源音轨 |
| 实时幻化（后续版本） | `realtime_audio_variant_enabled` | 关 | 预留兼容字段；本版本不提供入口、不启动 speech-to-speech、不生成实时变体 |

本版本不开发实时话术幻化，`realtime_audio_variant_enabled` 不进入当前 UI、处理链或验收。代码中如仍读取该字段，必须保持关闭/不可用语义，不得启动 speech-to-speech Worker、生成候选音轨或申请模型租约。

普通声音处理参数和只读诊断使用两组独立元数据：

- `parameter_role`：`configurable` 表示字段可由默认值、预设或调度写入处理配置并进入校验链，不等于用户拥有手动编辑权限；`readonly_diagnostic` 表示只读测量或资源统计，后者不得提交为处理参数。参数卡继续只读；主页声音参数列不得把“音高、变速与共振峰”“特征、信噪比与空间混合”整体标为编辑区。MFCC 偏移、MFCC 维度、SNR 浮动、频谱盲区、干湿比、环境声混合、高质量音高（`pitch_shift_semitones`）、播放速度和共振峰偏移这 9 项只读展示，不得提供手动输入。
- `execution_status`：`disabled` 表示对应开关关闭或参数未启用；`configured` 表示值已通过校验并保存，但不代表 DSP 已执行；`runtime` 表示当前媒体链已确认实际生效；`unavailable` 表示当前 Worker/分析器未接入或能力不可用，必须保持原音轨或已确认的可用结果。

UI 必须分别展示配置值、只读诊断值和 `execution_status`。任何只完成字段保存、范围校验或面板展示的参数都不得标记为 `runtime`。本节不扩展第 6 节的实时音频幻化参数。

### 3.1 基础与自然真人模式

本表中的“配置”表示参数契约角色，不表示用户可编辑。音频参数由默认值、未被本决策收为只读的既有配置控件或预设/周期调度产生，经校验后交给 FFmpeg 与混音后 Signalsmith Stretch 处理链；普通参数卡只展示当前值。上述 9 项只能由默认值、普通声音预设或周期随机化产生，随机化后刷新展示，并继续进入既有校验、N+1 候选准备/提交和运行状态链路。`random_change_period_ms` 是每轮从全局声音周期区间抽出的调度结果（校验范围 `1000–60000ms`），不是 22 套声音预设中的第 36 个字段；当前 UI 的声音周期新配置默认从 `3000–5000ms` 随机取值，视频周期默认从 `5000–8000ms` 随机取值，既有有效本地保存值保持不变。

界面展示分为两层：能力状态列出候选渲染链已支持的声音参数；预览状态列出用户配置、当前 N 音轨、N+1 阶段、真实周期、下次变化、编码进度、PID、线程预算、文件数量和当前参数。22 套预设逐套精确包含 35 个可写效果字段：p01–p20 为默认低感知随机池，p21、p22 为明显可听验收预设且仅在用户显式勾选或固定选择后参与；普通声音与随机插话引用同一预设事实源。随机插话的 WebView 路径可继续播放独立短期处理缓存。

| 参数 | 参考范围 | 单位 | 默认值 | 角色 | 当前生效状态 |
|---|---:|---:|---:|---|---|
| 自然真人模式 | `original` / `natural_dynamic` | 枚举 | `original` | 配置 | `original=disabled`；`natural_dynamic` 进入按变化周期运行的缓慢响度包络，执行时为 `runtime` |
| 声音随机周期 | `1–60` | s 媒体时间 | `3–5` 随机取值 | 配置 | 默认独立调度；提前保存 N+1/N+2 参数，仅 N+1 可创建 FFmpeg 候选；已有有效本地值不迁移 |
| 视频随机周期 | `1–60` | s 媒体时间 | `5–8` 随机取值 | 配置 | `5–8` 是新配置默认值；最小值和最大值独立编辑，越界时只收敛当前编辑端点、不得交换或改写另一端点；每轮在闭区间重新随机，相等则固定；按真实媒体时钟准备 N+1 并在目标边界切换，暂停/停滞冻结 |
| 下一次随机周期 | `0–周期` | s 媒体时间 | 只读倒计时 | 只读运行状态 | 由最终播放窗口真实绝对媒体时钟计算，暂停或时钟停滞时冻结；定时器/墙钟不是目标事实源 |
| 高质量音高（音高微移） | `-2–+2` | semitone | `0` | 配置 | UI 只读；由普通声音预设/周期随机化写入，Signalsmith Stretch 在 FFmpeg 多支路混音后执行并保持音频时长；不得修改视频 `playbackRate` |
| 共振峰偏移 | `-5–+5` | % | `0` | 配置 | UI 只读；由普通声音预设/周期随机化写入，Signalsmith Stretch 与音高共用混音后处理器；执行时为 `runtime`，当前不提供独立“自动共振峰保持”开关 |
| 输入增益 | `-6–+6` | dB | `0` | 配置 | 已映射媒体 Worker，执行时为 `runtime` |
| 输出增益 | `-6–+6` | dB | `0` | 配置 | 已映射媒体 Worker，执行时为 `runtime` |
| 响度调整 | `-6–+6` | dB | `0` | 配置 | 已映射媒体 Worker，执行时为 `runtime` |
| 播放速度 | `0.5–2.0` | 倍 | `1.0` | 配置 | UI 只读；已映射 FFmpeg，流式处理时与视频 `playbackRate` 共用同一倍速，并由每轮主预设参与周期变化 |
| 低/中/高频均衡 | `-12–+12` | dB/频段 | `0` | 配置 | 已映射 FFmpeg，普通声音处理链生效时为 `ready` |
| 降噪强度 | `0–100` | % | `0` | 配置 | 已映射 FFmpeg `afftdn`，处理执行时为 `runtime` |
| 环境声混合 | `0–100` | % | `0` | 配置 | UI 只读且参加预设/周期随机化；值为 `0` 时不加载第二输入，值大于 `0` 时用户本地素材可选且优先，否则使用随包内置真实环境声；最终来源验证成功后才循环解码并进入总线 `amix` |
| 淡入时长 | `0–10` | s | `0` | 配置 | 已映射 FFmpeg，普通声音处理链生效时为 `ready` |
| 淡出时长 | `0–10` | s | `0` | 配置 | N/N+1 候选是有限输入，可在 FFmpeg 中安全映射淡出；不得引入无界反向缓存 |
| 干湿比 | `0–100` | % | `0` | 配置 | UI 只读且参加预设/周期随机化；已建立完整处理声的短反射湿声支路，并按百分比与干声 `amix` |
| 轻混响湿声 | `0–20` | % | `0` | 配置 | 已映射 FFmpeg `aecho`，流式处理运行时为 `runtime` |
| 环境底噪电平 | `-60–-20` | dBFS | `-40` | 配置 | 与环境噪声比例一起映射 FFmpeg `anoisesrc/amix` |
| 频谱扰动 | `0–10` | % | `0` | 配置 | 已映射 FFmpeg `afftfilt` 幅度扰动 |
| 高频扰动开关 | 开/关 | — | 关 | 配置 | 开启且强度非零时只作用于约 6kHz 以上频段 |
| 高频扰动间隔 | `0.5–60` | s | `12` | 配置 | FFmpeg 时间表达式周期门控 |
| 高频扰动强度/电平 | `0–20 / -60–0` | `% / dB` | `0 / -32` | 配置 | 已映射 FFmpeg `afftfilt`，执行时为 `runtime` |
| 采样率 | `source` / `44100` / `48000` | Hz | `source` | 配置 | 已映射媒体 Worker，显式转换执行时为 `runtime` |
| 输出音频码率 | `64–320` | kbps | 自动读取 | 配置 | 仅保存/校验时为 `configured`；编码器实际采用后才为 `runtime` |

当前不提供独立动态压缩参数。环境声素材混合只有在真实第二输入完成校验并进入当前 FFmpeg 图后才能报告 `runtime`；干湿比、降噪、相位、颤音和环境噪声也只有实际链成功运行时才能标记为 `runtime`，不能仅因字段存在或默认值为零而标记为已生效。

### 3.2 补充音频参数的真实执行语义

`natural_voice_mode=natural_dynamic` 使用本轮 `random_change_period_ms` 驱动 1.2% 的缓慢本地响度包络；`voice_library_id` 经 FNV-1a 稳定映射到内建轻量 EQ 参数，不下载或推断话术模型。`ambient_sound_mix_percent = 0` 时不创建 `[1:a:0]`。值大于 `0` 时，Rust 优先规范化用户可选的本地素材；用户未选择时解析随包内置真实环境声音频。用户素材必须位于授权路径，随包素材必须位于受控资源目录；两种来源均要求可读、格式属于 mp3/wav/m4a/aac/ogg/flac，并通过 FFmpeg 实际解码验证后才作为 `[1:a:0]`。任一验证失败时保持原声并报告可恢复错误。`dry_wet_percent` 使用独立短反射湿声支路。`spectrum_blind_spot_percent` 以 8kHz 为当前明确中心，带宽为 `20000 × percent / 100` Hz 的 `bandreject`。

`mfcc_shift_percent` 使用 1024 点 STFT、40 个 Mel 滤组、DCT-II/DCT-III 和原相位重建，`mfcc_dimensions` 控制 1–40 阶实际分析/重建维数；固定算法延迟由有界 FIFO 承接。`snr_target_db/snr_variation_db` 在混音后滚动测量 RMS，只在目标 SNR 低于当前测量值时注入达到目标所需的增量噪声；加噪不能改善 SNR，因此更高目标保持旁路。候选编码前的最终混音 PCM 同时产生 realFFT MFCC、滚动噪声底/SNR 和 LPC F1/F2/F3；`current_formant_hz` 只读投影 F1，非空值不得作为效果请求提交。独立报告任务、报告导出和旧 Worker 不恢复。

`snr_target_db` 默认 `null`，UI 必须显示“自动”，表示读取当前源与混音总线基线后不强制固定目标。p01–p20 中的 MFCC 偏移/维数、SNR 浮动、频谱盲区、干湿比、环境声混合、高质量音高、播放速度、共振峰偏移及其余预设可写字段都使用非中性低值；p21、p22 也包含相同的完整 35 字段，只提高可听强度。“预设可写”不开放上述 9 项的用户输入。预设完整性按精确字段集合校验，禁止缺字段后静默继承默认值。

### 3.3 普通声音处理的候选文件执行语义

普通声音预设或周期随机化提交参数快照后，Rust 校验范围、组合和环境声来源，并将 audio revision 纳入下一份独立声音候选。FFmpeg 解码当前源媒体音轨；`ambient_sound_mix_percent = 0` 时不加载辅助输入，非零时按“用户本地素材优先、随包内置真实环境声默认”解析第二输入。基础滤镜与最多四条支路在同一图内混合，Signalsmith Stretch 统一执行高质量音高与共振峰偏移，realFFT/rustdct 与滚动 RMS 处理 MFCC/SNR，最终写入独立 AAC/M4A，不再封装进视频候选 MP4。

N+1 候选只有在 FFmpeg 正常退出、FFprobe 校验可解码流/时长/时间戳/音轨并原子提交为 `.ready` 后才可预载。任何来源验证、Signalsmith、PCM 有限值、编码、封装或校验失败都继续当前 N 或源媒体，不静音、不切空文件，也不把 `configured` 标记成 `runtime`。只有 A/B 槽完成切换后才更新已生效参数、预设身份和变化次数。

普通声音 `.partial` 已通过 FFprobe 后，仅当 `volumedetect` 暂未返回有效 `max_volume` 时，允许对同一暂存产物自动重试内容探测 `2` 次（总尝试最多 `3` 次），共享原超时/取消预算且不重复编码 N+1。stderr 保留末尾摘要；静音、取消、超时、参数错误、FFmpeg 非零退出及其他不可读错误不重试。恢复期间保持当前声音 N，不更新参数/预设/变化次数，并与视频周期完全分域；耗尽后进入明确 `failed` 并清理暂存。

上述限制只约束 Worker 内对同一 `.partial` 的重复探测。候选已经返回最终失败并完成清理后，前端周期调度器允许在保持当前声音 N 的前提下按 `1/2/4/8/15s` 有界退避，最多五次重建全新的 N+1/N+2；连续第六次失败后才锁存为等待人工重试。busy 不计入预算，成功、人工重试、停止、换源或播放身份变化清零；暂停和旧代次不启动重建。

声音和视频周期都以用户保存的 `1–60s` 区间为输入，每轮从各自闭区间独立随机，相等区间固定。两者拥有独立的参数计划、目标媒体时间、候选产物和提交计数，不合并为同一文件，也不提供联动模式。每个领域的 N+2 只保存参数、目标媒体时间和随机种子，不创建文件或 FFmpeg。候选过期、取消或延后都不形成结果队列，只保留各自最新计划。

| 参数组 | 实际执行位置 | 生效状态 |
|---|---|---|
| 基础增益、响度、EQ、滤波、速度、淡入淡出、混响、降噪、环境声、干湿比、采样率、码率 | FFmpeg 有限候选输入 | `configured/rendering/ready/runtime/failed` |
| 高质量音高、共振峰偏移 | FFmpeg 混音后由 Signalsmith 处理，再进入候选编码 | `configured/rendering/ready/runtime/failed` |
| MFCC 偏移/维度、目标 SNR/浮动 | 混音后 PCM 处理器，再进入候选编码 | `configured/rendering/ready/runtime/failed` |
| 波形、频谱、响度、噪声底、MFCC、SNR、LPC F1/F2/F3 | 已编码候选的处理前分析快照及当前媒体只读诊断 | `ready/runtime/unavailable` |
| 当前共振峰 | 只读投影 F1；禁止作为效果请求提交 | `runtime/unavailable` |

### 3.4 视频周期与声音候选的资源上限契约

| 项目 | 范围/上限 | 默认 | 说明 |
|---|---:|---:|---|
| 视频周期原视频 | 最多 1 个 `.ready` | 1 | 在重处理前准备；预测或实际迟到时作为该周期确定的回退结果 |
| mpv 视频会话 | 最多 1 个进程、1 条持久 IPC | 0 | 一个所有者、有界队列、命令 deadline、Job Object 和 stderr 尾缓冲 |
| 声音当前 N | 最多 1 | 1 | 当前出声并提供独立声音候选状态 |
| 声音候选 N+1 | 最多 1 个 `.partial` 或 `.ready` | 0 | 独立 AAC/M4A A/B 候选，不封装进视频候选 |
| 视频 N+2 / 声音 N+2 | 各 0 个文件/进程 | 各参数 1 份 | 分别只保存参数、种子和目标时间 |
| 待删文件 | 每个领域最多 1 | 0 | Windows 文件占用时有界退避；达到上限时不得继续扩大暂存集合 |
| 候选 Worker | 视频最多 1＋声音最多 1 | 0 | 两个领域可并行；领域内部单飞，配置变化采用 latest-wins |
| 软件编码线程 | `1–8` | `min(8, max(1, logical_cores - 1))` | 至少预留一个逻辑核给播放器和 UI |
| stderr 尾缓冲 | 有界 | 64 KiB | 只保留诊断尾部，不累积完整输出 |
| 删除重试 | 有界 | 5 次 | 指数退避；退出或下次启动继续清理 |
| 声音候选探测重试 | `0–2` 次 | 2 次 | 仅 FFmpeg 退出后的 `.partial` 探测失败；每次重试前清理失败暂存，不影响视频周期 |

受管进程必须进入 Windows Job Object 并启用 `KILL_ON_JOB_CLOSE`。mpv 所有者拥有取消令牌、子进程句柄、持久 IPC reader/writer、命令超时和 stderr 尾缓冲；声音 Worker 继续拥有其 FFmpeg/FFprobe 候选生命周期。停止、换源或关闭时必须停止派发、取消、有界等待、终止进程树兜底、Join 线程并关闭句柄。

临时文件使用应用数据目录下的 session/generation/candidate 路径。正常关闭同步清理当前会话；进程崩溃后由下次启动扫描无活动租约且超过宽限期的 session。源文件永不删除，清理器必须验证规范化路径位于受管根目录内。
### 3.5 桌面端全量正式参数面板

桌面端中栏统一使用 `MediaParameterPanels`。参数内容容器必须填满桌面中栏的实际可用宽度，不设置约 `880px`、`860–900px` 或其他固定 `max-width`，不得因居中宽度上限在中栏两侧留下大面积空白。容器外层保持等分伸缩的双主栏：左栏依次承载“普通视频”和“高级视觉”，右栏承载“普通声音”；每个主栏内部的参数卡网格根据该栏实际可用宽度自动决定列数并自然换行，不限制每排卡片数量，也不设置固定列数。参数区极窄时，外层双主栏按“普通视频 → 高级视觉 → 普通声音”的 DOM 顺序降为单列。这里的双主栏只描述参数区内部信息结构，不改变桌面工作区外层的素材/参数/输出三栏。面板定义仍必须与 Rust `VideoEffectParams`、`AdvancedEffectParams`、`AudioEffectParams` 逐字段相等，不能漏字段、重复字段或重新引入 `research_*`。

普通参数卡统一使用紧凑单行布局；卡片宽度由所属主栏内部的自动列数网格和实际可用空间共同决定，窗口缩放时自然换行，不设置固定列数或固定/目标 `270–290px` 卡宽。卡内只保留参数名称、当前值或无手柄显式紧凑分段进度，以及“已接入 / 正式需求待实现 / 待确认”三态标签；不得渲染“范围……；已接入/正式需求待实现原因……”等底部说明句。范围、单位、默认值和能力状态仍以本文件各参数表与状态清单为正式事实，并继续参与进度归一化、边界校验和媒体实现判断，隐藏说明句不改变这些定义。完整进度分段必须落在卡片正文宽度内，禁止通过 `overflow` 裁剪制造无溢出的假象。固定视觉频段权重继续属于唯一正式字段 `advanced.band_weights`，但界面必须按 `advanced.band_weights.<frequency_hz>` 路径拆成 12 个独立紧凑参数行，并完整展示固定频率和当前权重；不得伪造成 12 个新的 DTO/Rust 字段。

参考图提取色板是本参数面板逐卡颜色的唯一颜色契约：青蓝 `#38B8F8`、橙 `#F89838`、紫 `#C888F8`、粉紫 `#E878F8`、黄 `#F8C818`。每次参数面板挂载时，为每张参数卡或独立参数模块分别从该五色色板分配自己的颜色，不再为“普通视频 / 高级视觉 / 普通声音”三个大分类各固定一种颜色。五色允许循环复用，分配顺序应尽量避免视觉上相邻的卡片或模块同色；每卡颜色映射在该次挂载生命周期内保持稳定，参数更新、重新渲染和排序不得重新抽色。颜色只用于逐卡识别和更新反馈，不表达算法能力或算法状态。

全量参数卡和周期状态模块保持只读；主页声音参数列可承载“音高、变速与共振峰”和“特征、信噪比与空间混合”两个具名展示/配置分组，但不得把分组整体定义为编辑区。上述 9 项与其他参数一样只读展示，高级声音抽屉不再保留其重复输入；它们继续由普通声音预设和周期随机化更新，更新后刷新显示并进入既有校验与运行链路。目标 SNR 为 `null` 时必须可见显示“自动”；只读诊断、能力三态和当前运行值仍不得由输入控件伪造。

参数卡在挂载后只要该卡接收的显示 `value` 实际变化，就使用该卡自己的稳定颜色执行一次约 `900ms` 的高亮反馈：先短暂增强，再平滑淡出，避免约 `290ms` 瞬时闪动难以感知。初始挂载、相同值、仅重排和没有改变 `value` 的普通重新渲染不得触发。该规则以 `MediaParameterPanels` 已有显示值为事实，不额外假设全量面板存在 `runtime`、预热或失败状态属性。主页“插话声音预设”按最终效果窗口上报的本段实际预设逐项比较格式化显示值，并复用同一 A/B 动画类；只有值变化的参数行高亮。`prefers-reduced-motion: reduce` 下必须禁用高亮并保持静态可读状态。算法状态仍以“已接入 / 正式需求待实现 / 待确认”三态文字为唯一表达，逐卡颜色不得表达或替代算法状态。`audio_stream_params`、`audio_stream_variants` 继续服务于已提交普通声音支路的独立状态区，但不得被描述为全量参数面板的唯一事实源。该反馈不复用此前已删除的无可见消费方闪动 token，也不得基于 UI 值推断“已生效”或预设身份。处理失败必须展示 Rust 返回的明确原因，未接入的非默认值由 Rust 媒体边界拒绝，不能在前端静默归零。

主参数区必须同时显示视频周期和声音周期两个只读状态模块，不得仅以视频周期代表两条链路。两个模块都显示用户保存的 `1–60s` 区间、当前真实变化次数、本轮实际随机周期和下次变化媒体时间；相等区间显示固定周期。进度来自最终效果窗口真实媒体时钟，暂停、seek、加载或已确认停滞时冻结；无真实下一计划时为 `0` 且保持非活动。展示层不得新增随机采样或使用前端倒计时生成虚假进度。两类状态共同读取 N/N+1 候选阶段和当前已应用参数；只有候选实际切换后才递增对应变化次数。画面 Card 不提供手动“应用处理”入口。

视频与声音状态始终使用各自的真实计划、周期长度和变化次数推进，不提供共享目标或联动区间求交。视频或声音处理关闭、播放暂停、对应周期未启用或尚无真实下一计划时，相应模块的进度为 `0` 且保持非活动，次数按现有调度器状态显示。该状态投影不改变既有周期范围、各自的 N/N+1/N+2 调度、候选准备提交或任何后端算法。

最终效果窗口的真实绝对媒体位置是两条周期共同的进度事实源；Rust `playback_state=playing` 只表达期望播放，不能替代位置推进证据。排除暂停、seek、切源、metadata 装载和受控循环边界宽限后，连续约 `1500ms` 无真实推进必须标记 `media_clock_stalled`，两条周期冻结在最后真实位置。同一 `playback_generation + source` 最多自动恢复一次，恢复成功后重新建立真实时钟；失败必须显示明确原因并退出伪 `playing`。禁止用墙钟、轮询次数或动画补走进度。

### 3.6 插话音轨选择参数与双出口契约

| 参数 | 范围/允许值 | 单位 | 默认值 | 说明 |
|---|---:|---:|---:|---|
| 启用随机插话 | `true / false` | 布尔 | `false` | 启用前目录必须通过校验 |
| 插话目录 | 存在且可读的本地目录 | 路径 | 未配置 | 作为扫描根目录，规范化后递归发现任意层级子目录中的可用插话媒体文件，且至少存在一个 |
| 插话目录集合上限 | 文件 `<=1000`；单路径 `<=4096` UTF-8 字节；全部路径累计 `<=512 KiB` | 数量/字节 | — | 流式扫描；任一上限超出时拒绝整个配置，不保留部分结果；随机插话抽屉展示已保存快照的实际数量/1000 |
| 支持文件扩展名 | 音频：`mp3 / wav / m4a / aac / ogg / flac`；视频：`mp4 / mov / mkv / avi / webm / m4v / ts / m2ts / flv / wmv / 3gp` | 枚举 | — | 视频只选择并解码音频流，忽略视频、字幕和数据流；每次播放前仍须校验规范化路径、普通文件、可读性和可解码音轨 |
| 插话音轨选择模式 | `fixed / random` | 枚举 | `random` | IPC `audio_selection_mode`；`fixed` 固定单轨且仅在运行时不消费已保存、已校验的随机预设池、多轨和变化策略，`random` 从所选池抽样 |
| 插话固定预设 | `p01–p22` | 枚举 | `p01` | IPC `audio_fixed_preset_id`；只在 `audio_selection_mode=fixed` 时作为唯一实际音轨 |
| 插话随机声音参数值预设池 | `p01–p22` 中 `1–22` 个唯一项，可全选 `22` 项 | 枚举集合 | `p01–p20` | IPC `audio_preset_ids`；普通声音与随机插话共用 22 套精确 35 字段定义，但分别保存选择状态；`p21/p22` 只有显式选择后参与 |
| 插话多轨合一开关 | `true / false` | 布尔 | `false` | IPC `audio_mix_enabled`；所有模式都保存并校验，`random` 运行时关闭则每次抽取一项、开启则按支路上下限抽取并合一；`fixed` 运行时不消费该值 |
| 插话随机支路下限 | `1–4` 且不大于上限 | 条 | `1` | IPC `audio_mix_pick_min`；所有模式都保存并校验，只在 `audio_selection_mode=random && audio_mix_enabled=true` 时决定本次最少抽取数 |
| 插话随机支路上限 | `1–4` 且不小于下限 | 条 | `2` | IPC `audio_mix_pick_max`；所有模式都保存并校验，实际支路数不得超过预设池中的可选项数；`fixed` 运行时不消费该值 |
| 插话预设变化策略 | `each_playback / periodic` | 枚举 | `periodic` | IPC `audio_variation_mode` 为兼容字段；当前 UI 和运行时统一使用 `periodic`，旧 `each_playback` 快照也按统一周期语义执行。`fixed` 运行时不消费该值 |
| 插话预设变化周期下限/上限 | `1000–600000` 且下限不大于上限 | ms | `8000 / 15000` | IPC `audio_variation_period_min_ms / audio_variation_period_max_ms`；范围独立于普通声音 `random_change_period_ms=1000–60000ms`。`random` 在文件开始时先抽样一次；文件不足一个周期时只使用该次预设，长文件到达媒体时间边界后保持同一文件和当前播放位置继续切换声音预设 |
| 触发间隔下限/上限（UI） | `0.5–60` 且下限不大于上限 | s | `8 / 13` | UI 以秒显示和输入；提交/回显边界换算为 IPC/Rust `interval_min_ms/interval_max_ms`，其范围仍为 `500–60000ms`。主页进度按本轮等待起点到已抽样触发点独立计算为 `0%–100%`，不得复用预设段进度；触发时到达 `100%`，文件结束后归零并开始下一轮。暂停、固定话术抢占或停止期间不推进；存在活动插话时恢复当前插话，否则继续现有调度 |
| 插话音量 | IPC `-60–+12`；UI `0–100` | dB / % | `0dB / 100%` | 作用于插话轨，在主页“插话声音周期”卡片独立控制；界面只显示和输入整数 `0%–100%`，其中 `0%=-60dB`、`100%=0dB`，历史正增益值回显为 `100%`，当前 UI 不生成正增益；IPC 与持久化仍使用 dB；不跟随播放控制中的主媒体音量或主媒体静音，预设切换前后保持当前值 |
| 主轨 duck 深度 | `-60–0` | dB | `-60` | 插话实际出声期间作用于主轨；`-60` 按增益 `0` 处理，为真正静音 |
| duck 攻击 | `0–1000` | ms | `50` | 插话进入时的衰减包络；`0` 表示立即压低到目标增益 |
| duck 释放 | `0–3000` | ms | `250` | 插话结束、失败、暂停或抢占时恢复主轨；`0` 表示立即恢复 |

UI 的秒制只适用于插话触发间隔，不修改运行态和 IPC 的毫秒事实源。React 必须在显示/输入边界执行秒与毫秒换算；Tauri/Rust 继续对 `interval_min_ms/interval_max_ms` 执行 `500–60000ms` 和下限不大于上限的信任边界校验。`ducking_attack_ms/ducking_release_ms` 始终以毫秒传输，`0` 是合法且有明确即时语义的值，不能被前端空值回退、默认值合并或运行时包络逻辑改写为旧的非零下限。

WebView 为实际出口时，Rust 以当前预设和同一环境声解析结果生成短期处理音频缓存，最终效果窗口播放该缓存并 duck WebView 主轨；音频文件处理失败时可明确回退插话原声，不影响主轨。PortAudio 接管时，`fixed` 只应用 `audio_fixed_preset_id` 对应的一项参数；`random` 单轨或最多四支路多轨合一进入唯一 `audio_cycle_output`。`periodic` 为当前段保留一个 current 和最多一个 pending 候选，按实际插话媒体位置在边界切换；准备或切换失败继续使用 current，不静音、不换文件、不回到文件开头。输入为视频容器时两条出口都只消费音频流。短期缓存和受管任务按本次插话 generation 持有，当前文件结束、失败、停止、切换素材或窗口卸载时释放；不得预生成整池组合或创建常驻多任务。

插话选择模式、固定预设、随机预设池、`audio_mix_enabled/audio_mix_pick_min/audio_mix_pick_max` 和周期状态与普通声音分别保存，但引用同一份 22 套精确 35 字段预设定义。默认 `audio_selection_mode=random`、`audio_fixed_preset_id=p01`、随机池 `p01–p20`、`audio_mix_enabled=false`、`audio_mix_pick_min=1`、`audio_mix_pick_max=2`、`audio_variation_mode=periodic`，预设变化周期默认 `8000–15000ms`；`p21/p22` 只有显式勾选或固定选择后参与。随机模式在每个文件开始时抽样一次，并在长文件内按媒体时间周期切换；保存的新预设池或周期配置从下一个边界消费，不重启当前文件。Tauri 校验成功后的全部随机插话输入以 `autolive.interlude-config.v1` 保存为本机非敏感偏好，重新登录或重启后再次通过 Tauri 校验并恢复；损坏、越界或目录失效的值不得直接进入运行快照。该链路不启动 speech-to-speech、VAD/ASR/LLM/TTS、模型租约或任何实时话术幻化能力。

暂停会挂起当前插话、文件随机计时和预设媒体时间计划，继续后从当前位置恢复；墙钟流逝不得提前跨越预设边界。插话音量允许在活动期间独立更新，主媒体音量和静音只作用于主轨；duck 仍只在插话实际出声期间作用于主轨。停止播放、切换素材或窗口卸载会取消当前与 pending 任务，并在既有超时预算内停止 FFmpeg、清空有界 PCM 和 Join 受管任务。固定话术优先级高于随机插话。目录、预设、滤镜、加载、解码、候选或输出失败均不得静音或阻断主轨；预设候选失败继续当前预设，文件级致命失败结束本次插话并恢复 duck。运行状态必须包含当前文件基本名称、当前预设、文件累计触发次数 `file_cycle`、当前文件 1 基预设段号 `preset_segment`、媒体位置、下一边界和具体失败原因，不得暴露完整本地路径。主页文件触发次数与当前文件内预设切换次数必须独立；同一文件切换预设不得增加 `file_cycle`，新文件成功开始时下方切换次数从 `0` 重新计算。

## 4. 视频模块参数

### 4.1 正式参数与目标执行边界

下表继续是正式范围和默认值契约，但 mpv/libplacebo 正式接线与验收前均不得标记“已实现并生效”。展示卡保持只读，参数值必须来自当前已提交快照，不得用参考图示例值替代。

| 参数 | 参考范围 | 单位 | 默认值 |
|---|---:|---:|---:|
| 亮度 | `-100–+100` | % | `0` |
| 对比度 | `0–200` | % | `100` |
| 饱和度 | `0–200` | % | `100` |
| 色相旋转 | `-180–+180` | ° | `0` |
| 模糊半径 | `0–8` | px | `0` |
| 锐化 | `0–100` | % | `0` |
| 噪点强度 | `0–8` | % | `0` |
| 细节增强 | `0–50` | % | `0` |
| X/Y 空间偏移 | `-4–+4` | px | `0` |
| 动态裁剪 | `0–4` | %/边 | `0` |
| 像素扰动 | `0–2` | px | `0` |
| 像素级缩放 | `95–105` | `%` | `100` |
| 高光/阴影 | `-100–+100` | `%` | `0/0` |
| 暗角 | `0–100` | `%` | `0` |
| 色彩空间转换强度 | `0–100` | `%` | `0`（开关关） |
| 图像修复强度 | `0–100` | `%` | `0`（开关关） |
| 画面旋转 | `-180–+180` | ° | `0` |
| 水平/垂直翻转 | 开/关 | — | 关/关 |
| 帧率扰动 | `0–2` / `0.01–2` / `0–2` | `% / Hz / fps` | `0 / 0.1 / 0` |
| 帧内/帧间扰动 | `0–2 / 0–20` | `%` | `0 / 0` |

启用视频参数后，mpv 负责解码并由 libplacebo 实时处理/呈现，周期边界不重新编码或产生 MP4。UI 可把 X/Y 偏移显示为两个独立控件，但它们仍属于同一空间偏移参数组。

### 4.2 正式视频与高级参数：GPU83 目标执行分类

除水平/垂直翻转外的 83 项必须逐项分类为 `libplacebo 单帧/multi-pass`、`libplacebo 合成/历史纹理`或 `mpv 帧调度`。未能表达对应语义的字段必须标记“正式需求·待实现/未接入”，不得以旧 FFmpeg 映射冒充 mpv/libplacebo 已生效。

12 个 `65–20000Hz` 固定频段明确属于视频空间频段，不是声音频段；它们继续投影为 12 个独立显示行。只有 12 项分别通过生产 shader 加载、单字段输出证据和真实呈现门禁后才能标记“已接入”。

2026-08-25 历史口径，已被 2026-08-26 目标覆盖：视频处理由画面 Card 的独立开关启用；开启后，每个周期仍自动生成一份完整 `VideoEffectParams + AdvancedEffectParams` 快照，但到点只把最新快照标记为待应用，不立即提交整段 FFmpeg 重编码；多次到期按 latest-wins 合并。真实渲染只在首次开启、换源或同一条目新播放轮次边界单飞提交，暂停后恢复不属于首次开启，画面 Card 不提供手动“应用处理”入口。最终效果窗口只显示成功校验并切换的处理缓存，处理关闭或失败时明确回退源视频，两种路径都不得在视频元素上追加 CSS 色彩、模糊、位移或缩放伪效果。`horizontal_flip_enabled`、`vertical_flip_enabled` 保留兼容字段但固定不进入自动池。其余已有真实执行路径的字段按“独立连续参数 / 开关及非零从属强度 / 从属调度参数”参加快照；当前 FFmpeg 基础 X/Y 位移只有整数像素精度，自动快照固定使用 `(+0.5px, -0.5px)`，使实际 `(+1px, -1px)` 方向跨周期稳定，不在 `-1px/+1px` 间跳变。画中画、随机图形、抽象几何挂件、局部模糊、边缘填充、高光扰动和异步旋转保持启用并使用非零低感知值。`picture_in_picture_opacity_percent` 在画中画支路先转为 alpha 再执行 overlay；自动值必须非零且以难以察觉为目标，默认模型中的 `100%` 仅用于兼容既有手动配置。`applied_fields`、输出帧变化和缓存成功切换共同证明快照已经真实参加变换；字段存在、随机值非零、待应用状态或 CSS 变化不能替代该证据。

当前目标口径（2026-08-28）：视频每轮按真实媒体时钟生成唯一计划，并以除水平/垂直翻转外的 `83` 个 UI 参数项组成原子快照。快照只通过一次 `glsl-shader-opts` 更新进入常驻 mpv；GPU83 失败时会话级单向降级到 CPU4，CPU4 只执行亮度、对比度、饱和度和色相，再失败保持 Original。声音继续维护独立 N/N+1 文件链，两类周期始终独立，但共享同一源时间轴。

实时直接映射的自动低感知/明显可见对照区间为：亮度 `±0.1–0.35% / ±2–4%`，对比度与饱和度相对 `100%` 的偏移 `±0.1–0.3% / ±3–6%`，色相 `±0.05–0.2° / ±2–5°`，模糊 `0.01–0.05px / 0.5–2px`，缩放偏移 `±0.05–0.15% / ±0.5–1.5%`，位移 `0.5px / 1–3px`，旋转 `±0.01–0.04° / ±0.2–0.8°`。右侧“明显可见”只是固定测试素材的验收对照，不是跨素材、屏幕和观看距离的人眼阈值承诺。

自动画面周期必须为除水平/垂直翻转外全部 `83` 项提供真实执行证据；任一项未实现时整体不得报告 GPU83 完整生效。CPU4 永远不接收其余 79 项。

资源保护改为受管 mpv 会话和帧预算：D3D11/Vulkan 必须使用与生产一致的完整 shader 和真实短样本探测，不能用编码器名称、`vo-configured` 或字段数量推断 GPU83 可用。

2026-08-25 历史口径，已被 2026-08-26 目标覆盖：低感知数值必须按本契约量级进入滤镜；亚像素/小数参数不能无条件取整成更大的离散值，任意非零值不能触发与幅度无关的固定最小滤镜强度，概率参数不能因整数取模或下限截断而提高实际命中率。底层不支持所需精度时应保持中性/旁路或采用明确的连续表达，不能静默放大。自动化范围/映射测试只保证契约边界；发布仍需对同一素材保留原片/处理结果客观帧差，并在正常观看下确认变化低感知，二者缺一不可。

当前发布验收以实时效果为准：同一素材每轮参数变化需有真实非零帧差，自动低感知档在正常观看下不应明显可辨；另使用明显可见对照档证明同一映射链路真实改变画面。连续播放不得出现跳帧、黑帧、闪烁、明显时间轴回退或音画中断；自动化测试与 Windows 10/11 x64 实机观察均必须通过。

## 5. 挂件与切片参数

本节参数均为正式 `AdvancedEffectParams` 需求，并已进入统一参数模型；旧 FFmpeg 路径曾覆盖其中部分效果，但不构成当前 mpv/GPU83 已接入证据。当前 mpv 开发生产链承认 capability 表中的 `79/83`：61 项 shader 加 18 项 PTS 调度。两项颜色和两项历史仍按各自门禁推进；`slice_min_length_ms` 在画中画时间轴解锁时约束可见源片段窗口，不代表创建离线切片文件或独立素材选择器。抽象人脸只生成几何标记，不进行真人脸检测、识别或避让。

| 参数 | 参考范围 | 单位 | 默认值 |
|---|---:|---:|---:|
| 随机图形透明度 | `0–50` | % | `0` |
| 随机图形大小 | `1–64` | px | `4` |
| 抽象人脸数量 | `0–10` | 个 | `0` |
| 抽象人脸大小 | `1–10` | %/画面宽度 | `2` |
| 抽象人脸透明度 | `0–30` | % | `0` |
| 画面偏移 | `-10–+10` | px | `0` |
| 切片长度 | `0.5–10` | s | `5` |
| 切片最小长度 | `1–60` | s | `10` |
| 切片触发间隔 | `5–120` | s | `15` |
| 随机图形开关/数量 | 开/关；`1–32` | —/个 | 关/`4` |
| 画中画比例/透明度/旋转/扰动 | `10–50 / 0–100 / -15–+15 / 0–4` | `% / % / ° / px` | `24 / 100 / 0 / 0` |
| 局部模糊区域/半径/间隔 | `5–50 / 0.1–16 / 0.5–60` | `% / px / s` | `20 / 2 / 10` |
| 异步旋转最小/最大角度 | `-15–+15` 且最小≤最大 | ° | `-1 / +1` |

“切片长度”表示一次可见叠加效果的持续时间；“切片最小长度”表示可被选择的源片段下限，二者不是同一个字段。切片触发间隔必须大于等于切片长度，参数校验不能只检查单字段范围。

## 6. 话术幻化与连续优先参数

本节仅保留后续版本的实时话术幻化参数草案，不属于当前版本参数契约。本版本不处理正在播放视频的实时话术音频，不生成候选音轨，也不执行实时换轨。

| 参数 | 参考范围 | 单位 | 默认值 |
|---|---:|---:|---:|
| 单段气口 | `100–300` | ms | `180` |
| 音频变速 | `0.95–1.05` | 倍 | `1.0` |
| 实时安全余量 | `0.5–10` | s | `2` |
| 话术生成失败回退 | `原声` / `上一候选` / `保持当前音轨` | — | `原声` |

后续版本如恢复该能力，模型或 speech-to-speech Worker 处理不及时时应保持当前音轨；本版本不启动 Worker，当前视频直接按源音频/普通声音处理链播放。

## 7. 媒体处理 SHA-256 契约

对 MP4 产物，哈希对象是**整个 MP4 文件的字节流**，不是抽样帧、视频流、音频流或文件名。JSON、WAV 等非 MP4 中间产物也计算完整字节流 SHA-256，但字段名称使用 `artifact_sha256`，不能伪装成 MP4 哈希。

每个阶段执行以下顺序：

1. 阶段开始前，对每个输入产物计算完整字节流 SHA-256；如果输入是 MP4，同时记录 `input_mp4_sha256`。
2. 阶段执行并写入临时产物，不覆盖原文件。
3. 产物完成并通过容器/媒体可读性校验后，对整个输出文件计算 `output_artifact_sha256`；如果输出是 MP4，同时记录 `output_mp4_sha256`。
4. 将阶段、文件路径摘要、文件大小、输入哈希、输出哈希和参数快照写入产物清单。

界面字段约定：

- `source_artifact_sha256`：任意受支持源视频第一次进入流水线前的完整文件字节流哈希；源文件为 MP4 时可同时记录 `source_mp4_sha256`。
- `current_artifact_sha256`：当前选中阶段产物的完整文件字节流哈希；当前产物为 MP4 时可同时记录 `current_mp4_sha256`。
- `stage_input_sha256` / `stage_output_sha256`：阶段级完整产物哈希；输入或输出为 MP4 时，另记录对应的 `*_mp4_sha256`。

非 MP4 源视频不得把自身哈希写入 `source_mp4_sha256`、`input_mp4_sha256` 或其他 MP4 专用字段；只有流水线实际产生 MP4 产物后，才能记录对应 MP4 哈希。

## 8. 参考依据

- 媒体滤镜和参数适配优先基于 [FFmpeg Filters Documentation](https://ffmpeg.org/ffmpeg-filters.html) 等成熟媒体库；项目不自行实现编解码器、滤镜和播放内核。
- 参考图字段、当前三态能力和依赖选型建议见 [参考图参数对比与正式化清单](./参考图参数对比与正式化清单.md)。
- SHA-256 使用 [NIST FIPS 180-4 Secure Hash Standard](https://csrc.nist.gov/pubs/fips/180/4/final) 作为算法依据。

## 9. 变更记录

- 2026-08-30：新增独立 `VirtualCameraOutputConfig` 契约，冻结 AkVirtualCamera 首版为 YUY2 `1280×720@30fps`、黑帧空闲策略、非 WARP D3D11 GPU 捕获/转换和一次 CPU 边界回读；明确 `zero_copy=false`，不并入 83 项媒体效果参数。
- 2026-08-30：新增独立 `RtmpOutputConfig` 正式待实施契约，定义完整 RTMP/RTMPS 地址、默认音画双轨、单轨选择、720p/1080p、25/30/50/60fps、H.264/AAC 默认码率、GOP 和有界重试。配置不并入媒体效果参数，第一阶段不持久化完整地址、不开放自由 FFmpeg 参数，并明确换源重发、静态 GPU83 快照和固定话术不进入推流声音的限制。
- 2026-08-27（历史视频口径，已被 2026-08-28 mpv/libplacebo 决策整体覆盖）：单 MSE、SourceBuffer、视频 period、视频 Worker 和 `.partial/.ready` 不再定义当前参数生效条件。
- 2026-08-26（真实媒体时钟部分继续有效；视频渲染部分为 2026-08-25 历史口径，已被 2026-08-26 目标覆盖）：双周期继续使用最终窗口真实绝对媒体时钟；排除合法过渡后约 `1500ms` 无推进即停滞，同源代次最多恢复一次，失败退出伪 `playing`，禁止墙钟假进度。原视频 latest-wins、受控边界和整文件单飞只作历史审计。实时话术范围不变。
- 2026-08-25 历史口径，已被 2026-08-26 目标覆盖（画面 Card 自动处理入口）：移除手动“应用处理”入口继续有效；原每周期快照在首次开启、换源或新播放轮次边界执行整文件重编码的方式只作历史审计。
- 2026-08-25（环境声来源最终澄清）：`ambient_sound_mix_percent = 0` 时不加载第二输入；值大于 `0` 时默认解析随包内置真实环境声，用户本地素材可选且优先覆盖。两种来源都必须通过路径、格式、可读性和 FFmpeg 解码验证，失败保持原声并报可恢复错误。
- 2026-08-25（已被“环境声来源最终澄清”覆盖）：上一轮曾要求环境声非零时必须由用户选择本地素材，并废止随包来源；该要求现已撤回。零值不加载、完整输入验证和失败原声回退要求继续有效。
- 2026-08-25（声音参数只读随机化）：明确 `configurable`/“配置”只表示可进入参数与校验链，不等于用户可编辑。MFCC 偏移、MFCC 维度、SNR 浮动、频谱盲区、干湿比、环境声混合、高质量音高、播放速度和共振峰偏移只读展示，不在主页或高级声音抽屉提供手动输入；9 项继续参加普通声音预设与周期随机化，变化后刷新展示并复用既有校验、候选准备/提交、失败回退和运行状态链路。
- 2026-08-25 历史口径，已被 2026-08-26 目标覆盖（视频部分）：低感知视频叠加的运行时有效 alpha 曾统一下限为 `2/255`；自动采样的 PIP、随机图形和抽象脸透明度为 `0.8–1.2%`，PIP 独立时间轴必须与 overlay enable 窗口相交。自动低强度 `image_repair` 使用随包可用的 `atadenoise=s=5`，显式高强度继续使用 `nlmeans`；随包 FFmpeg 未编译 `hqdn3d`，不得生成该滤镜。声音部分继续有效：p01–p20 不再经过固定 `-16 LUFS` 归一化，所有增益/响度只按预设相对源素材应用；Signalsmith 合法空 PCM 为“尚未产出”，不得调用 MFCC/SNR 或触发硬回退。
- 2026-08-25（历史环境声口径，后续由同日“环境声来源最终澄清”收口）：视频与全声音自动变换落地时允许未选择用户素材时解析随包环境声；最终声音口径明确为随包内置真实环境声音频，且必须与用户本地素材一样通过完整验证。22×35 声音预设、WebView 插话短期缓存和失败原声回退继续有效；旧视频完整快照属于 2026-08-25 历史口径，已被 2026-08-26 目标覆盖。

- 2026-08-26（当前最新频段展示决策）：固定视觉频段权重不再使用单个 `196px` 跨栏汇总卡；界面按 `advanced.band_weights.<frequency_hz>` 拆成 12 个独立紧凑参数行。Rust/DTO 仍只有 `AdvancedEffectParams.band_weights` 一个 Map 字段，本次只改变视图投影，不改变数据结构、范围、默认值和媒体执行语义。该决策覆盖 2026-08-24 的频段汇总卡展示口径。
- 2026-08-25：参数契约静态检查同步当前正式产品口径：像素扰动、帧率扰动和帧内/帧间扰动作为已接入视频参数参与必填校验，`65–20000Hz` 继续按视频空间频段校验；只拒绝已废弃的“研究参数/实验参数”命名，不再把这些正式字段本身判为违规。

- 2026-08-24（正式参数完整接入）：明确 `65–20000Hz` 为视频空间频段并接入 12 段权重、目标/核心频率和动态均衡；补齐视频边缘柔化、源帧率锁定、画中画独立时间轴、源片段最小长度、边缘羽化和变换平滑。音频补齐自然动态、本地音色 ID、真实环境声第二输入、干湿支路、频谱盲区、MFCC 重建、SNR 处理及 MFCC/SNR/LPC 共振峰诊断；新增 `realfft 3.5.0` 与 `rustdct 0.7.1` 并由 `Cargo.lock` 固定。当前正式模型中语义已确认的字段不再标记“正式需求待实现”。
- 2026-08-24（当前最新逐卡高亮动效决策）：参数显示 `value` 变化后按该卡自己的颜色执行一次约 `900ms` 高亮，视觉过程为短暂增强后平滑淡出；`prefers-reduced-motion: reduce` 下完全禁用。该决策覆盖下方约 `290ms` 瞬时闪动时长口径，只调整逐卡颜色反馈动效，不改变触发事实源、逐卡颜色分配、布局、参数范围、状态或实时话术边界。
- 2026-08-24（历史双周期状态决策，联动部分已废止）：主参数区同时显示视频周期与声音周期两个状态模块，各自展示配置范围、真实变化次数，并以各自真实计划计算进度的规则继续有效；当时允许两条状态共用联动目标的口径已被 2026-08-27 独立周期决策覆盖。
- 2026-08-24（逐卡颜色分配；频段卡形态已由 2026-08-26 覆盖）：每张参数卡或独立参数模块分别从参考图五色色板分配自己的颜色，不再按普通视频、高级视觉、普通声音大分类固定一种颜色；五色可循环复用，分配顺序尽量避免视觉相邻项同色，且单次挂载期内逐卡映射稳定。当时显示 `value` 变化使用约 `290ms` 高亮，该时长现已由上方约 `900ms` 短暂增强并平滑淡出决策覆盖。算法状态仍由三态文字表达，颜色不表达算法状态。该决策覆盖下方按三个大分类固定一种颜色的历史口径，不扩展实时话术幻化。
- 2026-08-24（历史满宽与高度决策，频段卡形态已由 2026-08-26 覆盖）：参数内容区填满桌面中栏实际可用宽度，删除约 `880px`、`860–900px` 及其他最大宽度上限；栏内自动网格随窗口增减列数，外层双主栏与极窄单栏降级保持。当时普通参数卡固定为 `80px` 高，固定视觉频段权重卡固定为 `196px` 高并跨满所属栏；无底部说明、逐卡颜色反馈、三态及正式参数事实规则不变。该决策覆盖此前约 `880px` 居中宽度口径，但不扩展实时话术幻化。
- 2026-08-24（历史卡片内容决策，频段卡形态已由 2026-08-26 覆盖）：普通参数卡只显示参数名、当前值/进度和三态标签，删除底部范围、单位、接入情况及待实现原因说明句；当时固定视觉频段权重卡跨满所属栏且三列内容必须完整、不得裁剪。范围、默认值和能力状态事实仍保留在本契约、校验与媒体边界中。该决策覆盖此前“说明文字至少 `12px` 且显示状态原因”的卡内展示口径，但不扩展实时话术幻化。
- 2026-08-24（现行自动网格布局；旧宽度子句已由第一条覆盖）：参数预览使用填满中栏可用宽度的外层双主栏，左栏为普通视频与高级视觉、右栏为普通声音；每个主栏内部卡网格根据实际可用宽度自动决定列数并自然换行，不限制每排数量。极窄时外层双主栏按 DOM 顺序降为单列，全程不设置固定列数或卡宽且不得横向裁剪。逐卡颜色、减少动态效果和三态规则保持不变；动效时长以最上方约 `900ms` 决策为准。该布局决策覆盖下一条“每栏两卡、整行最多四卡、先固定降栏内单列”口径，但不扩展实时话术幻化。
- 2026-08-24（历史宽度口径，已由同日最新满宽决策覆盖）：参数内容区此前以约 `880px` 为桌面目标宽度，并允许在 `860–900px` 内取整后居中；该最大宽度和由此产生的中栏两侧留白不再属于当前验收。
- 2026-08-24（历史卡内说明口径，已由同日最新卡片内容决策覆盖）：普通卡当时保持紧凑且显示至少 `12px` 的范围、接入情况或待实现原因说明；该说明句与内容自适应高度要求不再作为当前验收，参数范围、默认值和能力状态事实本身仍保留。
- 2026-08-24（历史响应式澄清，已由同日最新自动网格决策覆盖）：参数预览继续使用约 `880px` 居中的外层双主栏，左栏为普通视频与高级视觉、右栏为普通声音；宽窗下每个主栏内部卡网格一排两张，参数区同一横排最多四张。空间不足时先把栏内卡网格降为单列，参数区进一步变窄时再把外层双主栏按 DOM 顺序降为单列，全程伸缩且不得横向裁剪。紧凑高度、至少 `12px` 说明、三列频段、分类色与约 `290ms` 反馈、减少动态效果和三态规则保持不变。该澄清覆盖下一条“每卡占满整栏、每横排左右两张”口径，但不扩展实时话术幻化。
- 2026-08-24（历史响应式口径，已由同日最新澄清覆盖）：参数预览继续使用约 `880px` 居中的双主栏，左栏为普通视频与高级视觉、右栏为普通声音；可用宽度足够时两栏等分伸缩、卡片填满所属栏且每横排左右两张，窄窗按 DOM 顺序降为单列，不设置固定卡宽且不得横向裁剪。紧凑高度、至少 `12px` 说明、三列频段、分类色与约 `290ms` 反馈、减少动态效果和三态规则保持不变。该决策覆盖下一条固定/目标 `270–290px` 卡宽口径，但不扩展实时话术幻化。
- 2026-08-24（历史布局，已由同日最新响应式澄清覆盖）：参数预览改为约 `880px` 居中的双主栏，左栏为普通视频与高级视觉、右栏为普通声音；普通卡目标宽度约 `270–290px`、说明文字至少 `12px`，固定视觉频段权重按三列展示。三个大分类从第 3.5 节参考图提取色板随机且不重复分配颜色，并在单次挂载期保持稳定；挂载后显示 `value` 实际变化时按所属分类色执行一次约 `290ms` 高亮，初始挂载、相同值、仅重排和普通重渲染不触发，减少动态效果时禁用。该决策覆盖下方同日“三分区同页三栏/删除闪烁”和旧 `720ms` 参数卡反馈口径，但不改写当时的实现和清理事实。
- 2026-08-24：随机插话触发间隔的 UI 契约改为 `0.5–60s`、默认 `8–13s`；IPC/Rust 字段保持 `interval_min_ms/interval_max_ms`、范围 `500–60000ms`。`ducking_attack_ms` 和 `ducking_release_ms` 的下限均改为 `0`，范围分别为 `0–1000ms`、`0–3000ms`，`0` 表示立即压低/立即恢复；默认值保持 `50/250ms`。
- 2026-08-25：播放状态契约增加系统文件拖放导入、追加、单项替换、拖拽/键盘重排、删除和清空；成功编辑停止当前资源并重置为非空池首项 `Ready` 或空池无源状态，失败不修改旧池。本决策覆盖 2026-08-24“不提供追加、重排或删除”的范围限制，不改变参数表中的最多 100 项、当前索引、池循环轮次、播放代际、当前进程内存和单窗口/单活动源语义。
- 2026-08-24（历史基线，已由 2026-08-25 播放池 CRUD 范围覆盖）：播放状态契约由单源循环更新为当前进程内最多 `100` 项的本地有序播放池：一次多选按文件对话框返回顺序整批探测，只有全部成功才原子替换旧池；取消、超限或任一失败保留旧池，不提交部分成功项，也不提供追加、重排或删除。新增当前索引、池循环轮次和每次换源递增的播放代际；同一最终效果窗口执行 `A→B→…→A`，单项池自循环，绝对媒体时钟按当前媒体代际重建。播放池不跨进程持久化、不创建服务端媒体任务或生成版本队列。
- 2026-08-24（历史参数卡布局；其主页声音编辑口径已由 2026-08-25 最新只读随机化决策覆盖）：当时完成三类参数卡、无手柄分段进度和三态展示；当时两个具名声音配置区可编辑的口径不再属于当前验收。
- 2026-08-24：新增全量正式参数面板，并把频谱/高频音频扰动、扩展视频效果、波频/通道/空间调制、随机几何/抽象几何脸、同源画中画、切片、局部模糊、边缘填充、高光扰动和异步旋转接入实际 FFmpeg 输出；仍缺算法语义、第二输入、外部模型或 OCR 确认的字段明确标记为“正式需求待实现/待确认”。
- 2026-08-24：参考图参数全部从旧“研究/实验”口径转为正式产品需求；统一 `MediaEffectParams`/`AudioEffectParams`/`VideoEffectParams`/`AdvancedEffectParams` 和新默认值/校验 IPC，按“已实现并生效/已有模型待接入/正式需求但缺失”管理。未恢复独立报告链路，也不扩展到实时话术幻化、OBS/RTMP 或平台检测规避。
- 2026-08-24（历史声音编辑入口；已由 2026-08-25 最新只读随机化决策覆盖）：引入 MIT Signalsmith Stretch，在 FFmpeg 多支路 `amix` 后统一处理 `pitch_shift_semitones` 与 `formant_shift_percent`；普通变速继续使用 FFmpeg `atempo`。当时 UI 提供三项手动入口；当前三项只读展示并由预设/周期随机化更新，多支路共享音高/共振峰，初始化或处理失败保持旧轨/回退 WebView 原声。
- 2026-08-24：将基础视频 X/Y 空间偏移的自动周期预览附加幅度由 `±2 px` 收窄为 `±0.5 px`；手动调节范围、默认值以及挂件模块的独立画面偏移保持不变。
- 2026-08-24（历史插话选择模型，出口由 2026-08-25 当前方案覆盖）：建立固定/随机选择、最多四支路、下一段生效和有界释放边界；当前默认池、共享预设、环境声解析、WebView 短期处理缓存及原声回退以 2026-08-25 记录为准。

- 2026-08-20（历史口径，已由 2026-08-24 取代）：曾将当前视频参数网格收敛为基础项；现参考图参数已全部正式化，但未映射字段仍不得显示为已生效。
- 2026-08-21（历史参数卡反馈口径，已由 2026-08-24 当前后续决策覆盖）：新增参数卡和声音预设的真实输出反馈契约，相同值与未提交候选不触发；补充随机插话目录/文件、间隔、音量和 duck 参数范围，并明确 WebView/PortAudio 双出口、唯一 PortAudio 输出流混音、固定话术抢占、暂停停止清理与失败回退。本版本仍不包含实时话术幻化或研究能力。
- 2026-08-23：为随机插话目录增加 256 个文件、4096 字节单路径和 512 KiB 累计路径硬上限，目录扫描保持流式，避免超大目录集合进入播放快照。
- 2026-08-21（历史参数卡反馈口径，已由 2026-08-24 当前后续决策覆盖）：细化参数卡和声音预设反馈为单次约 `720ms`，颜色继承各 box 自身强调色且不得统一绿色；匹配 `prefers-reduced-motion: reduce` 时完全禁用，真实输出变化与相同值静止规则不变。
- 2026-08-21（历史参数卡反馈口径，已由 2026-08-24 当前后续决策覆盖）：补充普通声音参数状态与声音预设池两个区块容器的反馈事实源，分别比较实际输出参数集合和实际参与预设集合；相同集合、轮询、预热与失败不触发，时长、强调色和减少动态效果规则沿用卡片契约。
- 2026-08-21：区分 Scheduled 候选 `750ms` 提交 readiness、提交为 current 后的 `5130ms` 有界 PCM 保留容量和 `1500ms` 真停产门槛；扩大水位只覆盖 FFmpeg EOF 重启，不改变一次恢复、失败回退或当前产品范围。
- 2026-08-24：普通声音 `3–5s` 周期候选改用 `max(3, playback_speed + 1)`、1 秒初始突发的 CatchUp 和随 prepare 已用时间增长的动态覆盖水位，容量调整为 `5000ms + 130–750ms` 提交安全尾部；覆盖不足时保留 current。该最小速率/突发组合使用用户 MP4 的合法四支路连续 30 轮通过；即时插话的固定窗口 RealTime 策略、用户参数范围和真停产门槛不变。
- 2026-08-21（历史参数卡反馈口径，已由 2026-08-24 当前后续决策覆盖）：明确 Rust `PlaybackSnapshot`/DTO 的 `audio_stream_params`、`audio_stream_variants` 是已提交普通声音参数的唯一事实源；仅在 runtime=true 时展开有效支路并按每字段顺序无关的多重集合签名反馈，真实值或支路增删触发，等权重排、相同快照、预热、失败和非运行态不触发。本轮 Rust 字段不包含预设 ID/权重，预设身份不得从参数反推。
