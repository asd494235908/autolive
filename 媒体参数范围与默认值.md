# 媒体参数范围与默认值

> 当前版本范围声明（2026-08-19）：本版本不开发实时话术幻化。`realtime_audio_variant_enabled`、speech-to-speech、VAD/ASR/LLM/TTS 和实时候选音轨换轨仅作历史/兼容字段或后续版本预留，不属于当前参数面板、处理链、验收或发布范围；当前参数契约只约束视频处理、普通声音处理、插话和固定话术朗读。

## 1. 文档目的

本文是 Rust 桌面端、React 参数面板和本地媒体 Worker 共用的参数契约，解决“参数属于哪个模块、单位是什么、默认值是什么、允许调到什么范围”的歧义。

当前媒体 Worker 已映射基础视频 `亮度/对比度/饱和度/色相/模糊/锐化/细节增强/噪点/动态裁剪/像素扰动/空间偏移/缩放` 和基础音频 `输入增益/输出增益/响度/低中高频 EQ/Q 值/音高微移/播放速度/淡入/淡出/轻混响/降噪/相位扰动/颤音/环境噪声/显式采样率/输出码率` 到实际 FFmpeg 滤镜。音高微移要求源采样率可用。研究分析参数不属于当前产品，不纳入当前 UI、播放链路或验收。

“已映射媒体 Worker”只表示 FFmpeg 能够执行该参数。普通声音预览展示 FFmpeg 流式处理、PortAudio/WebView 实际出口和当前运行状态；视频处理仍可生成视频缓存。Web Audio 不执行声音效果，只提供回退和诊断。

这些范围是首版 UI 和任务校验的参考范围，不代表所有媒体算法的理论极限。具体媒体库通过适配器映射到本契约，不能把底层库的裸参数直接暴露给 UI。

竞品截图中的数值（例如 `0.11 st`、`4%`、`35.5 dB`、`1.08`）按“运行时当前值/测量值”理解，不直接作为本项目默认值。配置参数、只读诊断值、资源统计、倒计时和运行时生效状态必须在数据模型中分开。

所有数值参数必须同时保存：

- `value`：当前值
- `unit`：单位
- `min` / `max`：允许范围
- `default_source`：`source_metadata`、`system_default` 或 `user_profile`
- `enabled`：是否启用
- `algorithm_version`：处理算法版本

默认策略是“先读取源素材，再允许微调”：源视频的时长、分辨率、帧率、色域、音频采样率、声道数和码率从媒体元数据读取；未启用的参数默认不改变源素材。

## 2. 播放状态参数

当前首版播放口径：导入一个源视频后在同一个窗口持续循环，不生成离线视频版本、不建立版本队列。

本地源视频允许的容器/扩展名为：`.mp4`、`.mov`、`.mkv`、`.avi`、`.webm`、`.m4v`、`.ts`、`.m2ts`、`.flv`、`.wmv`、`.3gp`。扩展名只用于文件选择白名单，最终是否可导入必须以 FFprobe 对容器、视频流、音频流和时长的实际探测结果为准；当前不承诺自动转码或兼容预览缓存，直放能力以目标 WebView/媒体引擎实际支持为准。

| 参数 | 范围/枚举 | 单位 | 默认值 | 说明 |
|---|---|---:|---:|---|
| 源素材时长 | `> 0` | ms | 自动读取 | 使用媒体探测结果，不允许手工覆盖为不一致值 |
| 源视频循环 | 开/关 | — | 开 | 同一窗口从源视频 0 秒继续播放 |
| 播放轮次 | `0–无限` | 轮 | 自动记录 | 每次源视频重新开始时递增 |
| 首项原声播放 | 开/关 | — | 开 | 导入探测成功后保持 `Ready`；用户点击主页“播放”才打开最终效果窗口并开始源视频播放 |

源素材可以是几分钟、30 分钟或 1 小时；播放器只记录实际播放轮次，不根据时长计算版本数量。

## 3. 音频模块参数

### 3.0 独立功能开关

| 开关 | 字段 | 默认值 | 行为 |
|---|---|---|---|
| 声音处理 | `audio_processing_enabled` | 关 | 控制普通音频效果；关闭时保持源音轨 |
| 实时幻化（后续版本） | `realtime_audio_variant_enabled` | 关 | 预留兼容字段；本版本不提供入口、不启动 speech-to-speech、不生成实时变体 |

本版本不开发实时话术幻化，`realtime_audio_variant_enabled` 不进入当前 UI、处理链或验收。代码中如仍读取该字段，必须保持关闭/不可用语义，不得启动 speech-to-speech Worker、生成候选音轨或申请模型租约。

普通声音处理参数和只读诊断使用两组独立元数据：

- `parameter_role`：`configurable` 表示用户配置，`readonly_diagnostic` 表示只读测量或资源统计，后者不得提交为处理参数。
- `execution_status`：`disabled` 表示对应开关关闭或参数未启用；`configured` 表示值已通过校验并保存，但不代表 DSP 已执行；`runtime` 表示当前媒体链已确认实际生效；`unavailable` 表示当前 Worker/分析器未接入或能力不可用，必须保持原音轨或已确认的可用结果。

UI 必须分别展示配置值、只读诊断值和 `execution_status`。任何只完成字段保存、范围校验或面板展示的参数都不得标记为 `runtime`。本节不扩展第 6 节的实时音频幻化参数。

### 3.1 基础与自然真人模式

本表中的“配置”表示参数契约角色。音频参数由用户编辑后交给 FFmpeg。Rust 仍保留 `random_change_period_ms` 作为兼容的单值参数（默认 `4000ms`，校验范围 `1000–60000ms`）；当前 UI 已拆分为独立的声音/视频随机周期调度，两者都限制在 `1000–60000ms`，默认分别从 `8000–15000ms` 随机取值，不能再把该旧字段解释为“仅视频”或用它驱动音频效果。

界面展示分为两层：能力状态列出 FFmpeg 音频流/媒体输出链已支持的 21 项声音参数；预览状态列出用户配置、流式处理状态、PortAudio/WebView 实际出口和视频缓存状态。普通声音处理的 PortAudio 主路径直接从源视频音频流输出双声道 f32 PCM，不把处理后音频 MP4 作为播放前置条件；视频处理缓存只保存视频结果并复制源音频供 WebView 回退。运行时默认随机选择集只包含第 1–20 项，第 21 项不参与运行，p22“明显可辨识（手动）”仅在显式选择时参与抽样和持久化。Web Audio 只做回退、音量控制和只读波形/频谱诊断，不能作为 PortAudio 的 PCM IPC 执行器。

| 参数 | 参考范围 | 单位 | 默认值 | 角色 | 当前生效状态 |
|---|---:|---:|---:|---|---|
| 自然真人模式 | `original` / `natural_dynamic` | 枚举 | `original` | 配置 | `original=disabled`；`natural_dynamic=unavailable` |
| 声音随机周期 | `1–60` | s | `8–15` 随机取值 | 配置 | 由 UI 独立调度；对应随机 DSP 未接入时为 `unavailable` |
| 视频随机周期 | `1–60` | s | `8–15` 随机取值 | 配置 | 由 UI 独立调度，不改变音频参数 |
| 下一次随机周期 | `0–周期` | s | 只读倒计时 | 只读运行状态 | 对应调度运行时为 `runtime`，否则 `disabled/unavailable` |
| 音高微移 | `-2–+2` | semitone | `0` | 配置 | 源采样率可用且媒体 Worker 执行时为 `runtime`；FFmpeg 链保持音频时长，不得修改视频 `playbackRate` |
| 输入增益 | `-6–+6` | dB | `0` | 配置 | 已映射媒体 Worker，执行时为 `runtime` |
| 输出增益 | `-6–+6` | dB | `0` | 配置 | 已映射媒体 Worker，执行时为 `runtime` |
| 响度调整 | `-6–+6` | dB | `0` | 配置 | 已映射媒体 Worker，执行时为 `runtime` |
| 播放速度 | `0.5–2.0` | 倍 | `1.0` | 配置 | 已映射 FFmpeg；流式处理时与视频 `playbackRate` 共用同一倍速，不参与周期调度 |
| 低/中/高频均衡 | `-12–+12` | dB/频段 | `0` | 配置 | 已映射 FFmpeg，处理缓存生效时为 `ready` |
| 降噪强度 | `0–100` | % | `0` | 配置 | 已映射 FFmpeg `afftdn`，处理执行时为 `runtime` |
| 环境声混合 | `0–100` | % | `0` | 配置 | `unavailable`；需要外部环境音素材 |
| 淡入时长 | `0–10` | s | `0` | 配置 | 已映射 FFmpeg，处理缓存生效时为 `ready` |
| 淡出时长 | `0–10` | s | `0` | 配置 | 离线有限输入映射 FFmpeg；PortAudio 无限流实时路径跳过 `areverse`，避免无界缓存 |
| 干湿比 | `0–100` | % | `0` | 配置 | `unavailable` |
| 轻混响湿声 | `0–20` | % | `0` | 配置 | 已映射 FFmpeg `aecho`，流式处理运行时为 `runtime` |
| 环境底噪电平 | `-60–-20` | dBFS | `-40` | 配置 | 与环境噪声比例一起映射 FFmpeg `anoisesrc/amix` |
| 采样率 | `source` / `44100` / `48000` | Hz | `source` | 配置 | 已映射媒体 Worker，显式转换执行时为 `runtime` |
| 输出音频码率 | `64–320` | kbps | 自动读取 | 配置 | 仅保存/校验时为 `configured`；编码器实际采用后才为 `runtime` |

当前不提供可执行的动态压缩参数；压缩、环境声素材混合和干湿比保持 `unavailable`。降噪、相位、颤音和环境噪声已使用 FFmpeg 原生滤镜，不能仅因字段存在或默认值为零而标记为 `ready`。

### 3.2 非当前范围的研究参数

MFCC、SNR、共振峰、频谱扰动、音色库及研究报告字段不属于当前产品，不纳入当前 UI、普通声音处理或实时音频幻化链路。仓库中如仍保留对应 Rust 参数、Worker 或 IPC，只用于迁移审计；新代码不得依赖或扩展这些字段。

### 3.3 普通声音处理的 FFmpeg 参数预览

该预览属于普通声音处理链路，与本版本不开发的实时音频幻化完全隔离。声音处理开关关闭时不启动 FFmpeg 音频处理；当前任何参数状态都不得触发 speech-to-speech、VAD、ASR、LLM、TTS 或候选音轨生成。

用户修改参数后点击“应用音频参数”，Rust 保存流式音频配置并让 PortAudio 候选解码器直接调用 FFmpeg 音频滤镜；候选预热完成后在播放安全边界切换。开启或重新配置 PortAudio 时先以 callback 静音暂停态启动硬件流，视频继续使用 WebView 原声；设备启动、FFmpeg 预热和源同步在 Tauri blocking worker 上执行。后端取得实际输出延迟后，按“UI 观测位置 + 预热耗时 × 当前播放倍速 + 旧环缓待播时长 + 实际输出延迟”裁剪候选 PCM，只原子预填 50ms，成功后才放行 callback 并静音 WebView。失败或候选身份过期时保持 WebView/旧轨。PortAudio 默认关闭，开启且可用时才作为普通声音处理出口；关闭、不可用或运行失败时回退 WebView 源视频音频。视频处理开启时，视频仍经过 FFmpeg 生成缓存，但缓存音频只复制源音轨，不承担普通声音效果。实时变体音轨不再套用普通声音滤镜。检测到流式解码失败时关闭 PortAudio 并回退 WebView；不再因临时音频 MP4 不可读而阻断普通声音播放。音高链保持时长且不能改动画面时钟；只有播放速度同时驱动 FFmpeg `atempo` 和视频 `playbackRate`。Web Audio 只保留回退和观测。

| 参数 | 处理方式 | 预览状态 |
|---|---|---|
| 增益、响度、三段 EQ | FFmpeg `volume/equalizer` | `configured/processing/ready/failed` |
| 音高、播放速度 | FFmpeg `asetrate/aresample/atempo` | `configured/processing/ready/failed` |
| 淡入、淡出、轻混响 | FFmpeg `afade/aecho`；实时淡出不使用 `areverse` | `configured/processing/ready/failed` |
| 降噪、相位、颤音 | FFmpeg `afftdn/aphaser/vibrato` | `configured/processing/ready/failed` |
| 环境噪声比例与电平 | FFmpeg `anoisesrc/amix` | `configured/processing/ready/failed` |
| 采样率、输出码率 | FFmpeg `aresample` 与运行时采样率配置；视频缓存音频仍复制源 AAC | `configured/processing/runtime/failed` |
| 环境声素材混合、干湿比、压缩及其他未接入 DSP 字段 | 尚无稳定映射或缺少外部输入 | `unavailable` |

UI 展示全部已支持参数、当前配置、处理进度、当前运行出口和失败回退原因。声音/视频随机周期分别调度，不能在 Web Audio 中执行声音效果；当前产品不提供“低可感知”研究参数或检测规避能力。

### 3.4 PortAudio 内存缓冲契约

| 参数 | 范围 | 单位 | 默认值 | 说明 |
|---|---:|---|---:|---|
| 应用侧 SPSC PCM 内存缓冲 | `128–2048` | KiB | `1024` | 用户可编辑；表示实际分配的 f32 环形缓冲字节容量，不是硬件回调帧数 |
| PortAudio 硬件回调 | 内部参数 | frame | `256` | 与内存缓冲独立，当前不作为用户输入 |

应用侧环缓容量不等于播放延迟。普通实时播放采用独立的 50ms 目标水位，防止 FFmpeg 解码速度快于硬件消费时把整个容量填满；候选音轨原子提交也只写入固定 50ms，数秒追赶缓存继续留在候选线程内并在启用后按顺序补入，禁止一次填满物理环缓。UI 应同时展示环缓占用样本数、按实际采样率/声道换算的待播毫秒数和 PortAudio 实际输出延迟。

实时 PortAudio 解码使用 `-re -stream_loop -1`，输入不会自然结束，因此禁止在实时滤镜链使用需要等待 EOF 的 `areverse`。实时淡出参数仍可保存和校验，但当前安全行为是跳过反向淡出；`areverse → afade → areverse` 只允许用于有限输入的离线媒体处理。停止时必须确认 FFmpeg 子进程、stderr、解码线程和混音线程均已退出，不得静默放弃线程句柄；运行态同时记录当前/候选 FFmpeg PID 和任务数量，任务数最多为 1 个当前任务加 1 个候选任务。

当前轨与“已知未来切换点”的 Scheduled 候选都使用 `-re -stream_loop -1`。Scheduled 候选直接 seek 到目标前 250ms，只保留固定 750ms PCM 时间窗，提交时要求至少剩余 100ms，因此不需要在切换点无限追赶画面。首次启动、恢复和跳转后的 CatchUp 兼容路径仍使用受控 `-readrate max(2, playback_speed + 1) -readrate_initial_burst 0.5`，只用于追上当前时钟。两类实时链路均禁止 `areverse`。

本产品只提供波形、频谱和响度只读诊断；MFCC 等研究分析配置不属于当前参数契约。

## 4. 视频模块参数

### 4.1 画面效果与空间/时间扰动

| 参数 | 参考范围 | 单位 | 默认值 |
|---|---:|---:|---:|
| 亮度 | `-100–+100` | % | `0` |
| 对比度 | `0–200` | % | `100` |
| 饱和度 | `0–200` | % | `100` |
| 色相旋转 | `-180–+180` | ° | `0` |
| 模糊半径 | `0–8` | px | `0` |
| 锐化 | `0–100` | % | `0` |
| 噪点强度 | `0–8` | % | `0` |
| 细节增强 | `0–50` | % | `0` |
| 裁剪边缘平滑度 | `0–1` | 无量纲 | `0.5` |
| X/Y 空间偏移 | `-4–+4` | px | `0` |
| 像素级扰动 | `0–2` | px | `0` |
| 帧内微扰幅度 | `0–2` | px | `0` |
| 帧间微扰概率 | `0–20` | % | `0` |
| 动态裁剪 | `0–4` | %/边 | `0` |
| 帧率微扰频率 | `0.01–2` | Hz | `0.1` |
| 帧率微扰幅度 | `0–2` | fps | `0` |
| 色域转换 | `source` / `BT.601` / `BT.709` / `sRGB` | — | `source` |
| 色域转换强度 | `0–100` | % | `0` |
| 像素级缩放 | `0.95–1.05` | 倍 | `1.0` |

只要启用了上述任意视频参数，视频输出必须经过解码、处理和重新编码；首版不使用视频流 `copy`。如果输出产物为 MP4，则对整个 MP4 文件执行对应媒体处理哈希契约。

### 4.2 非当前范围的视觉研究参数

65Hz～20000Hz 视觉调制频段、实验性帧率/像素扰动、研究挂件和相关 `effective_frequency` 报告不属于当前产品，不纳入当前视频处理 UI、播放链路或验收。历史字段如仍存在，只用于迁移审计。

## 5. 挂件与切片参数

| 参数 | 参考范围 | 单位 | 默认值 |
|---|---:|---:|---:|
| 随机图形透明度 | `0–50` | % | `0` |
| 随机图形大小 | `1–64` | px | `4` |
| 抽象人脸数量 | `0–10` | 个 | `0` |
| 抽象人脸大小 | `1–10` | %/画面宽度 | `2` |
| 抽象人脸透明度 | `0–30` | % | `0` |
| 画面偏移 | `-10–+10` | px | `0` |
| 切片长度 | `0.5–10` | s | `5` |
| 切片最小长度 | `1–60` | s | `10` |
| 切片触发间隔 | `5–120` | s | `15` |

“切片长度”表示一次可见叠加效果的持续时间；“切片最小长度”表示可被选择的源片段下限，二者不是同一个字段。切片触发间隔必须大于等于切片长度，参数校验不能只检查单字段范围。

## 6. 话术幻化与连续优先参数

本节仅保留后续版本的实时话术幻化参数草案，不属于当前版本参数契约。本版本不处理正在播放视频的实时话术音频，不生成候选音轨，也不执行实时换轨。

| 参数 | 参考范围 | 单位 | 默认值 |
|---|---:|---:|---:|
| 单段气口 | `100–300` | ms | `180` |
| 音频变速 | `0.95–1.05` | 倍 | `1.0` |
| 实时安全余量 | `0.5–10` | s | `2` |
| 话术生成失败回退 | `原声` / `上一候选` / `保持当前音轨` | — | `原声` |

后续版本如恢复该能力，模型或 speech-to-speech Worker 处理不及时时应保持当前音轨；本版本不启动 Worker，当前视频直接按源音频/普通声音处理链播放。

## 7. 媒体处理 SHA-256 契约

对 MP4 产物，哈希对象是**整个 MP4 文件的字节流**，不是抽样帧、视频流、音频流或文件名。JSON、WAV 等非 MP4 中间产物也计算完整字节流 SHA-256，但字段名称使用 `artifact_sha256`，不能伪装成 MP4 哈希。

每个阶段执行以下顺序：

1. 阶段开始前，对每个输入产物计算完整字节流 SHA-256；如果输入是 MP4，同时记录 `input_mp4_sha256`。
2. 阶段执行并写入临时产物，不覆盖原文件。
3. 产物完成并通过容器/媒体可读性校验后，对整个输出文件计算 `output_artifact_sha256`；如果输出是 MP4，同时记录 `output_mp4_sha256`。
4. 将阶段、文件路径摘要、文件大小、输入哈希、输出哈希和参数快照写入产物清单。

界面字段约定：

- `source_artifact_sha256`：任意受支持源视频第一次进入流水线前的完整文件字节流哈希；源文件为 MP4 时可同时记录 `source_mp4_sha256`。
- `current_artifact_sha256`：当前选中阶段产物的完整文件字节流哈希；当前产物为 MP4 时可同时记录 `current_mp4_sha256`。
- `stage_input_sha256` / `stage_output_sha256`：阶段级完整产物哈希；输入或输出为 MP4 时，另记录对应的 `*_mp4_sha256`。

非 MP4 源视频不得把自身哈希写入 `source_mp4_sha256`、`input_mp4_sha256` 或其他 MP4 专用字段；只有流水线实际产生 MP4 产物后，才能记录对应 MP4 哈希。

## 8. 参考依据

- 媒体滤镜和参数适配优先基于 [FFmpeg Filters Documentation](https://ffmpeg.org/ffmpeg-filters.html) 等成熟媒体库；项目不自行实现编解码器、滤镜和播放内核。
- SHA-256 使用 [NIST FIPS 180-4 Secure Hash Standard](https://csrc.nist.gov/pubs/fips/180/4/final) 作为算法依据。
