# 媒体参数范围与默认值

## 1. 文档目的

本文是 Rust 桌面端、React 参数面板和本地媒体 Worker 共用的参数契约，解决“参数属于哪个模块、单位是什么、默认值是什么、允许调到什么范围”的歧义。

当前媒体 Worker 已映射基础视频 `亮度/对比度/饱和度/色相/模糊/锐化/细节增强/噪点/动态裁剪/像素扰动/空间偏移/缩放` 和基础音频 `输入增益/输出增益/响度/低中高频 EQ/Q 值/音高微移/播放速度/淡入/淡出/轻混响/降噪/相位扰动/颤音/环境噪声/显式采样率/输出码率` 到实际 FFmpeg 滤镜。音高微移要求源采样率可用。其他研究参数仍可编辑和校验，但在算法映射完成前，启用非默认值会被 Worker 明确拒绝，不会静默忽略或伪造已生效。

“已映射媒体 Worker”只表示 FFmpeg 媒体输出链能够执行该参数。Web Audio 不执行声音效果；音频预览只展示 FFmpeg 配置、处理进度和当前缓存生效状态。

这些范围是首版 UI 和任务校验的参考范围，不代表所有媒体算法的理论极限。具体媒体库通过适配器映射到本契约，不能把底层库的裸参数直接暴露给 UI。

竞品截图中的数值（例如 `0.11 st`、`4%`、`35.5 dB`、`1.08`）按“运行时当前值/测量值”理解，不直接作为本项目默认值。配置参数、只读诊断值、资源统计、倒计时和运行时生效状态必须在数据模型中分开。

所有数值参数必须同时保存：

- `value`：当前值
- `unit`：单位
- `min` / `max`：允许范围
- `default_source`：`source_metadata`、`system_default` 或 `user_profile`
- `enabled`：是否启用
- `algorithm_version`：处理算法版本

默认策略是“先读取源素材，再允许微调”：源视频的时长、分辨率、帧率、色域、音频采样率、声道数和码率从媒体元数据读取；未启用的实验参数默认不改变源素材。

## 2. 播放状态参数

当前首版播放口径：导入一个源视频后在同一个窗口持续循环，不生成离线视频版本、不建立版本队列。

本地源视频允许的容器/扩展名为：`.mp4`、`.mov`、`.mkv`、`.avi`、`.webm`、`.m4v`、`.ts`、`.m2ts`、`.flv`、`.wmv`、`.3gp`。扩展名只用于文件选择白名单，最终是否可导入必须以 FFprobe 对容器、视频流、音频流和时长的实际探测结果为准；当前不承诺自动转码或兼容预览缓存，直放能力以目标 WebView/媒体引擎实际支持为准。

| 参数 | 范围/枚举 | 单位 | 默认值 | 说明 |
|---|---|---:|---:|---|
| 源素材时长 | `> 0` | ms | 自动读取 | 使用媒体探测结果，不允许手工覆盖为不一致值 |
| 源视频循环 | 开/关 | — | 开 | 同一窗口从源视频 0 秒继续播放 |
| 播放轮次 | `0–无限` | 轮 | 自动记录 | 每次源视频重新开始时递增 |
| 首项原声立即播放 | 开/关 | — | 开 | 源视频通过可读性校验后立即播放 |

源素材可以是几分钟、30 分钟或 1 小时；播放器只记录实际播放轮次，不根据时长计算版本数量。

## 3. 音频模块参数

### 3.0 独立功能开关

| 开关 | 字段 | 默认值 | 行为 |
|---|---|---|---|
| 声音处理 | `audio_processing_enabled` | 关 | 控制普通音频效果；关闭时保持源音轨 |
| 实时幻化 | `realtime_audio_variant_enabled` | 关 | 独立控制 speech-to-speech 话术决策、实时变体和当前播放音轨替换 |

实时幻化关闭时不启动 speech-to-speech Worker，不生成候选音轨；声音处理开关和实时幻化开关互不隐式开启或关闭。

普通声音处理和研究分析参数使用两组独立元数据：

- `parameter_role`：`configurable` 表示用户配置，`readonly_diagnostic` 表示只读测量或资源统计，后者不得提交为处理参数。
- `execution_status`：`disabled` 表示对应开关关闭或参数未启用；`configured` 表示值已通过校验并保存，但不代表 DSP 已执行；`runtime` 表示当前媒体链已确认实际生效；`unavailable` 表示当前 Worker/分析器未接入或能力不可用，必须保持原音轨或已确认的可用结果。

UI 必须分别展示配置值、只读诊断值和 `execution_status`。任何只完成字段保存、范围校验或面板展示的参数都不得标记为 `runtime`。本节不扩展第 6 节的实时音频幻化参数。

### 3.1 基础与自然真人模式

本表中的“配置”表示参数契约角色。音频参数由用户编辑后交给 FFmpeg；`random_change_period_ms` 仅用于视频预览调度，不改变音频参数。

界面展示分为两层：能力状态列出 FFmpeg 媒体输出链已支持的 21 项声音参数；预览状态列出用户配置、FFmpeg 处理进度、当前处理缓存和 `ready/failed` 状态。Web Audio 只做只读波形/频谱诊断，不能作为声音效果执行器。

| 参数 | 参考范围 | 单位 | 默认值 | 角色 | 当前生效状态 |
|---|---:|---:|---:|---|---|
| 自然真人模式 | `original` / `natural_dynamic` | 枚举 | `original` | 配置 | `original=disabled`；`natural_dynamic=unavailable` |
| 随机变声周期 | `0.5–60` | s | `15` | 配置 | `configured`；对应随机 DSP 未接入时为 `unavailable` |
| 下一次变声周期 | `0–周期` | s | 只读倒计时 | 只读运行状态 | 调度运行时为 `runtime`，否则 `disabled/unavailable` |
| 音高微移 | `-2–+2` | semitone | `0` | 配置 | 源采样率可用且媒体 Worker 执行时为 `runtime` |
| 输入增益 | `-6–+6` | dB | `0` | 配置 | 已映射媒体 Worker，执行时为 `runtime` |
| 输出增益 | `-6–+6` | dB | `0` | 配置 | 已映射媒体 Worker，执行时为 `runtime` |
| 响度调整 | `-6–+6` | dB | `0` | 配置 | 已映射媒体 Worker，执行时为 `runtime` |
| 播放速度 | `0.5–2.0` | 倍 | `1.0` | 配置 | 已映射 FFmpeg，处理缓存生效时为 `ready`；不参与周期调度 |
| 低/中/高频均衡 | `-12–+12` | dB/频段 | `0` | 配置 | 已映射 FFmpeg，处理缓存生效时为 `ready` |
| 降噪强度 | `0–100` | % | `0` | 配置 | 已映射 FFmpeg `afftdn`，处理执行时为 `runtime` |
| 环境声混合 | `0–100` | % | `0` | 配置 | `unavailable`；需要外部环境音素材 |
| 淡入时长 | `0–10` | s | `0` | 配置 | 已映射 FFmpeg，处理缓存生效时为 `ready` |
| 淡出时长 | `0–10` | s | `0` | 配置 | 已映射 FFmpeg，处理缓存生效时为 `ready` |
| 干湿比 | `0–100` | % | `0` | 配置 | `unavailable` |
| 轻混响湿声 | `0–20` | % | `0` | 配置 | 已映射 FFmpeg `aecho`，处理缓存生效时为 `ready` |
| 环境底噪电平 | `-60–-20` | dBFS | `-40` | 配置 | 与环境噪声比例一起映射 FFmpeg `anoisesrc/amix` |
| 采样率 | `source` / `44100` / `48000` | Hz | `source` | 配置 | 已映射媒体 Worker，显式转换执行时为 `runtime` |
| 输出音频码率 | `64–320` | kbps | 自动读取 | 配置 | 仅保存/校验时为 `configured`；编码器实际采用后才为 `runtime` |

当前不提供可执行的动态压缩参数；压缩、环境声素材混合、干湿比以及 MFCC、SNR、共振峰、频谱扰动和音色库适配器均为 `unavailable`。降噪、相位、颤音和环境噪声已使用 FFmpeg 原生滤镜，不能仅因字段存在或默认值为零而标记为 `ready`。

### 3.2 研究分析参数

| 参数 | 参考范围 | 单位 | 默认值 | 角色 | 当前生效状态/说明 |
|---|---:|---:|---:|---|---|
| MFCC 维度 | `1–40` | 阶 | `13` | 配置 | `unavailable`；仅用于分析/实验，不直接显示为音色百分比 |
| MFCC 偏移 | `-20–+20` | % | `0` | 配置 | `unavailable`；相对偏移，实际值保留原始 MFCC 报告 |
| SNR 浮动 | `-6–+6` | dB | `0` | 配置 | `unavailable`；与源素材估计值比较 |
| 当前 SNR | `0–60` | dB | 自动测量 | 只读诊断 | 分析器接入前为 `unavailable`；不能与 SNR 浮动混用 |
| 共振峰偏移 | `-5–+5` | % | `0` | 配置 | `unavailable`；超出范围需人工确认 |
| 当前共振峰 | `20–10000` | Hz | 自动测量 | 只读诊断 | 分析器接入前为 `unavailable`；不能作为偏移量提交 |
| 滤波 Q 值 | `0.3–10` | 无量纲 | `1` | 配置 | 已映射 FFmpeg `equalizer` 的 Q 值，处理缓存生效时为 `ready` |
| 相位扰动 | `-20–+20` | % | `0` | 配置 | 已映射 FFmpeg `aphaser`，处理执行时为 `runtime` |
| 颤音频率 | `3–8` | Hz | `5` | 配置 | 深度非零时映射 FFmpeg `vibrato` |
| 颤音深度 | `0–3` | % | `0` | 配置 | 深度非零时映射 FFmpeg `vibrato` |
| 频谱扰动 | `0–10` | % | `0` | 配置 | `unavailable` |
| 频谱盲区宽度 | `0–5` | % | `0` | 配置 | `unavailable` |
| 音色库 | 已安装音色 ID | — | `source` | 配置 | 音色适配器未接入时为 `unavailable`；不硬编码供应商密钥 |
| 可用音色数量 | `0–1,000,000` | 个 | 自动统计 | 只读资源统计 | 音色库未接入时为 `unavailable`，不得伪造数量 |

### 3.3 普通声音处理的 FFmpeg 参数预览

该预览属于普通声音处理链路，与实时音频幻化完全独立。声音处理开关关闭时不启动 FFmpeg 音频处理；实时音频幻化开关的状态不改变普通声音参数是否进入 FFmpeg，也不触发 speech-to-speech、VAD、ASR、LLM、TTS 或候选音轨生成。

用户修改参数后点击“应用音频参数”，Rust 生成新的 `.partial.mp4`，调用 FFmpeg 音频滤镜和 AAC 编码，经过 FFprobe 可读性检查、整文件 SHA-256 和原子提交，再在播放安全边界切换。播放速度按配置处理但不参加周期调度；Web Audio 只保留只读观测。

| 参数 | 处理方式 | 预览状态 |
|---|---|---|
| 增益、响度、三段 EQ | FFmpeg `volume/equalizer` | `configured/processing/ready/failed` |
| 音高、播放速度 | FFmpeg `asetrate/aresample/atempo` | `configured/processing/ready/failed` |
| 淡入、淡出、轻混响 | FFmpeg `afade/aecho` | `configured/processing/ready/failed` |
| 降噪、相位、颤音 | FFmpeg `afftdn/aphaser/vibrato` | `configured/processing/ready/failed` |
| 环境噪声比例与电平 | FFmpeg `anoisesrc/amix` | `configured/processing/ready/failed` |
| 采样率、输出码率 | FFmpeg `aresample` 与 AAC `-b:a` | `configured/processing/ready/failed` |
| 环境声素材混合、干湿比、压缩、MFCC、共振峰、频谱扰动 | 尚无稳定映射或缺少外部输入 | `unavailable` |

UI 展示全部已支持参数、当前配置、处理进度、当前处理缓存和失败回退原因。随机周期仍可作为视频预览调度参数，但不再改变音频参数或在 Web Audio 中执行声音效果。所谓“低可感知”只用于研究参数的幅度控制，不能承诺对所有素材和分析器都不可检测，也不得用于平台检测规避。

MFCC 的维度、采样率、FFT 窗口和 hop length 属于分析配置；工程实现应使用成熟音频分析库，不自行实现 MFCC。MFCC 维度本身不是“音色变化百分比”。

## 4. 视频模块参数

### 4.1 画面效果与空间/时间扰动

| 参数 | 参考范围 | 单位 | 默认值 |
|---|---:|---:|---:|
| 亮度 | `-100–+100` | % | `0` |
| 对比度 | `0–200` | % | `100` |
| 饱和度 | `0–200` | % | `100` |
| 色相旋转 | `-180–+180` | ° | `0` |
| 模糊半径 | `0–8` | px | `0` |
| 锐化 | `0–100` | % | `0` |
| 噪点强度 | `0–8` | % | `0` |
| 细节增强 | `0–50` | % | `0` |
| 裁剪边缘平滑度 | `0–1` | 无量纲 | `0.5` |
| X/Y 空间偏移 | `-4–+4` | px | `0` |
| 像素级扰动 | `0–2` | px | `0` |
| 帧内微扰幅度 | `0–2` | px | `0` |
| 帧间微扰概率 | `0–20` | % | `0` |
| 动态裁剪 | `0–4` | %/边 | `0` |
| 帧率微扰频率 | `0.01–2` | Hz | `0.1` |
| 帧率微扰幅度 | `0–2` | fps | `0` |
| 色域转换 | `source` / `BT.601` / `BT.709` / `sRGB` | — | `source` |
| 色域转换强度 | `0–100` | % | `0` |
| 像素级缩放 | `0.95–1.05` | 倍 | `1.0` |

只要启用了上述任意视频参数，视频输出必须经过解码、处理和重新编码；首版不使用视频流 `copy`。如果输出产物为 MP4，则对整个 MP4 文件执行后续哈希契约；即使没有启用参数，研究产物也应保留处理链和编码策略记录。

### 4.2 65Hz～20000Hz 视觉调制实验

这组参数归属**视频模块**，名称为“视觉调制频率”，用于画面纹理、粒子、空间噪声或时间调制实验；它不是音频频段控制，也不改变音频采样率。

| 参数 | 参考范围 | 单位 | 默认值 |
|---|---:|---:|---:|
| 目标频率 | `65–20000` | Hz | 关闭 |
| 核心频率 | `65–20000` | Hz | 跟随目标频率 |
| 频段权重 | `0.5–1.5` | 倍 | `1.0` |
| 波频强度 | `0–1` | 归一化 | `0` |
| 波频颗粒数 | `1–100` | 个 | `20` |
| 波频电平 | `0–1` | 归一化 | `0` |
| 动态均衡阈值 | `0–20` | 归一化刻度 | `10` |
| 通道偏移 | `-10–+10` | % | `0` |
| 空间维度 | `1–3` | 维 | `2` |
| 空间 X/Y 偏移 | `-10–+10` | px | `0` |
| 帧扰动概率 | `0–20` | % | `0` |

首版固定频段列表为：`65、92、131、188、267、381、544、777、1110、1585、2263、20000 Hz`。每个频段保存一个独立的 `band_weight`，例如 `65Hz: 1.08`；`核心频率`和`目标频率`仍是独立字段。截图中的 `1.08`、`0.93` 等值应显示为倍数，不能显示成百分比或 dB。

视频帧序列存在采样上限：有效时间调制频率不能超过实际处理帧率的一半。UI 必须同时显示 `target_frequency` 和 `effective_frequency`；例如 30 fps 输出的有效时间调制上限约为 15 Hz，65Hz～20000Hz 的数值不能声称在普通 30/60 fps 视频中都能被忠实还原。超过上限时应降级、标记混叠或改为纹理/空间频率实验。

## 5. 挂件与切片参数

| 参数 | 参考范围 | 单位 | 默认值 |
|---|---:|---:|---:|
| 随机图形透明度 | `0–50` | % | `0` |
| 随机图形大小 | `1–64` | px | `4` |
| 抽象人脸数量 | `0–10` | 个 | `0` |
| 抽象人脸大小 | `1–10` | %/画面宽度 | `2` |
| 抽象人脸透明度 | `0–30` | % | `0` |
| 画面偏移 | `-10–+10` | px | `0` |
| 切片长度 | `0.5–10` | s | `5` |
| 切片最小长度 | `1–60` | s | `10` |
| 切片触发间隔 | `5–120` | s | `15` |

“切片长度”表示一次挂件/实验效果的持续时间；“切片最小长度”表示可被选择的源片段下限，二者不是同一个字段。切片触发间隔必须大于等于切片长度，参数校验不能只检查单字段范围。

## 6. 话术幻化与连续优先参数

本节只定义当前播放音轨的实时话术幻化参数。它处理正在播放视频的音频，不生成新的 MP4。

| 参数 | 参考范围 | 单位 | 默认值 |
|---|---:|---:|---:|
| 单段气口 | `100–300` | ms | `180` |
| 音频变速 | `0.95–1.05` | 倍 | `1.0` |
| 实时安全余量 | `0.5–10` | s | `2` |
| 话术生成失败回退 | `原声` / `上一候选` / `保持当前音轨` | — | `原声` |

模型或 speech-to-speech Worker 处理不及时时，当前视频继续播放并保持当前音轨；只有候选完成同步和安全门禁后才允许替换当前音轨。

## 7. SHA-256 阶段哈希契约

对 MP4 产物，哈希对象是**整个 MP4 文件的字节流**，不是抽样帧、视频流、音频流或文件名。JSON、WAV 等非 MP4 中间产物也计算完整字节流 SHA-256，但字段名称使用 `artifact_sha256`，不能伪装成 MP4 哈希。

每个阶段执行以下顺序：

1. 阶段开始前，对每个输入产物计算完整字节流 SHA-256；如果输入是 MP4，同时记录 `input_mp4_sha256`。
2. 阶段执行并写入临时产物，不覆盖原文件。
3. 产物完成并通过容器/媒体可读性校验后，对整个输出文件计算 `output_artifact_sha256`；如果输出是 MP4，同时记录 `output_mp4_sha256`。
4. 将阶段、文件路径摘要、文件大小、输入哈希、输出哈希、算法版本、参数快照和随机种子写入产物清单。

界面字段约定：

- `source_artifact_sha256`：任意受支持源视频第一次进入流水线前的完整文件字节流哈希；源文件为 MP4 时可同时记录 `source_mp4_sha256`。
- `current_artifact_sha256`：当前选中阶段产物的完整文件字节流哈希；当前产物为 MP4 时可同时记录 `current_mp4_sha256`。
- `stage_input_sha256` / `stage_output_sha256`：阶段级完整产物哈希；输入或输出为 MP4 时，另记录对应的 `*_mp4_sha256`。

非 MP4 源视频不得把自身哈希写入 `source_mp4_sha256`、`input_mp4_sha256` 或其他 MP4 专用字段；只有流水线实际产生 MP4 产物后，才能记录对应 MP4 哈希。

## 8. 参考依据

- 媒体滤镜和参数适配优先基于 [FFmpeg Filters Documentation](https://ffmpeg.org/ffmpeg-filters.html) 等成熟媒体库；项目不自行实现编解码器、滤镜和播放内核。
- MFCC 参数定义参考 [librosa.feature.mfcc](https://librosa.org/doc/latest/generated/librosa.feature.mfcc.html)；具体维度、采样率和 FFT 配置由本项目参数契约约束。
- SHA-256 使用 [NIST FIPS 180-4 Secure Hash Standard](https://csrc.nist.gov/pubs/fips/180/4/final) 作为算法依据。
