# 媒体参数范围与默认值

> 当前版本范围声明（2026-08-24）：本版本不开发实时话术幻化。`realtime_audio_variant_enabled`、speech-to-speech、VAD/ASR/LLM/TTS 和实时候选音轨换轨仅作历史/兼容字段或后续版本预留，不属于当前参数面板、处理链、验收或发布范围。参考图中的音频、视频、高级视觉、挂件、切片、检测和修复参数全部属于正式产品需求；未映射算法在当前参数面板统一标记“正式需求待实现”，语义尚未确认的字段标记“待确认”。

## 1. 文档目的

本文是 Rust 桌面端、React 参数面板和本地媒体 Worker 共用的参数契约，解决“参数属于哪个模块、单位是什么、默认值是什么、允许调到什么范围”的歧义。

当前正式模型中语义已经确认的媒体参数均已接入实际输出链。视频侧新增边缘柔化、源帧率 CFR 锁定，以及 65–20000Hz 视频空间频段权重、目标/核心频率、动态均衡阈值、画中画独立时间轴、源片段最小长度、边缘羽化和变换平滑；其中 65–20000Hz 明确是视频空间频段，按对数映射到每幅画面 1–32 个空间周期，不进入音频频谱。音频侧除原有 FFmpeg 和 Signalsmith Stretch 链外，已接入自然动态包络、本地音色 ID 轻量 EQ、真实环境声第二输入、干湿支路、频谱盲区、realFFT/rustdct MFCC 分析与重建、滚动 RMS SNR 处理以及 LPC F1/F2/F3 诊断。仍标记“正式需求待实现/待确认”的只剩尚无正式字段、依赖外部检测模型或 OCR 语义未确认的条目；不能用近似滤镜冒充这些能力。

当前唯一正式类型和命令为：

- `MediaEffectParams`：聚合 `audio: AudioEffectParams`、`video: VideoEffectParams`、`advanced: AdvancedEffectParams`。
- `get_default_media_effect_params`：读取正式默认值。
- `validate_media_effect_params`：只校验结构、有限值、范围和组合；返回成功不代表媒体 Worker 已映射算法。
- 禁止新增或继续使用 `research_*`、`*ResearchParams`、旧默认值/校验 IPC。旧报告 Worker/IPC 已移除，历史文档和迁移不作为当前契约。

“已映射媒体 Worker”只表示 FFmpeg 能够执行该参数。普通声音预览展示 FFmpeg 流式处理、PortAudio/WebView 实际出口和当前运行状态；视频处理仍可生成视频缓存。Web Audio 不执行声音效果，只提供回退和诊断。

这些范围是首版 UI 和任务校验的参考范围，不代表所有媒体算法的理论极限。具体媒体库通过适配器映射到本契约，不能把底层库的裸参数直接暴露给 UI。

竞品截图中的数值（例如 `0.11 st`、`4%`、`35.5 dB`、`1.08`）按“运行时当前值/测量值”理解，不直接作为本项目默认值。配置参数、只读诊断值、资源统计、倒计时和运行时生效状态必须在数据模型中分开。

所有数值参数必须同时保存：

- `value`：当前值
- `unit`：单位
- `min` / `max`：允许范围
- `default_source`：`source_metadata`、`system_default` 或 `user_profile`
- `enabled`：是否启用
- `algorithm_version`：处理算法版本

默认策略是“先读取播放池当前项，再允许微调”：当前项的时长、分辨率、帧率、色域、音频采样率、声道数和码率从媒体元数据读取；未启用的参数默认不改变对应源素材。

## 2. 播放状态参数

当前首版播放口径：一次多选按文件对话框返回顺序形成最多 `100` 项的本地有序播放池。只有全部候选探测成功才原子替换旧池；取消、超限或任一项失败均保持旧池并显示错误，不提交部分成功项，也不提供追加、重排或删除。首项进入 `Ready` 后等待用户点击“播放”，同一个最终效果窗口按 `A→B→…→A` 顺序循环，单项池自循环，任意时刻只有一个活动媒体源。播放池只存在当前进程，不生成离线视频版本、不建立生成版本队列，也不创建服务端媒体任务。

本地源视频允许的容器/扩展名为：`.mp4`、`.mov`、`.mkv`、`.avi`、`.webm`、`.m4v`、`.ts`、`.m2ts`、`.flv`、`.wmv`、`.3gp`。扩展名只用于文件选择白名单，最终是否可导入必须以 FFprobe 对容器、视频流、音频流和时长的实际探测结果为准；当前不承诺自动转码或兼容预览缓存，直放能力以目标 WebView/媒体引擎实际支持为准。

| 参数 | 范围/枚举 | 单位 | 默认值 | 说明 |
|---|---|---:|---:|---|
| 播放池容量 | `1–100` | 项 | 本次整批探测成功项数 | 按文件对话框返回顺序保存；整批成功才原子替换，不允许部分提交 |
| 当前项素材时长 | `> 0` | ms | 自动读取 | 使用当前项媒体探测结果，不允许手工覆盖为不一致值 |
| 当前索引 | `0–pool_length-1` | 项 | `0` | 快照字段 `source_media_index`；当前活动媒体源在 `source_media_pool` 中的位置，项结束时单飞推进，末项回 `0` |
| 播放池循环轮次 | `0–无限` | 轮 | `0` | 快照字段 `playback_pool_cycle`；末项回到首项时递增，单项池每次回到自身时也递增 |
| 播放代际 | `0–无限` | 代 | 运行时自动记录 | 快照字段 `playback_generation`；仅多项池切换到另一个活动媒体源时递增并按新媒体从 `0ms` 重建绝对时钟，单项池自循环保持该值 |
| 首项原声播放 | 开/关 | — | 开 | 新池整批探测成功后首项保持 `Ready`；用户点击主页“播放”才打开/聚焦最终效果窗口并开始播放 |

池内每项素材可以是几分钟、30 分钟或 1 小时；播放器只记录当前索引、实际池循环轮次和播放代际，不根据时长计算版本数量。

## 3. 音频模块参数

### 3.0 独立功能开关

| 开关 | 字段 | 默认值 | 行为 |
|---|---|---|---|
| 声音处理 | `audio_processing_enabled` | 关 | 控制普通音频效果；关闭时保持源音轨 |
| 实时幻化（后续版本） | `realtime_audio_variant_enabled` | 关 | 预留兼容字段；本版本不提供入口、不启动 speech-to-speech、不生成实时变体 |

本版本不开发实时话术幻化，`realtime_audio_variant_enabled` 不进入当前 UI、处理链或验收。代码中如仍读取该字段，必须保持关闭/不可用语义，不得启动 speech-to-speech Worker、生成候选音轨或申请模型租约。

普通声音处理参数和只读诊断使用两组独立元数据：

- `parameter_role`：`configurable` 表示字段可由默认值、预设或调度写入处理配置并进入校验链，不等于用户拥有手动编辑权限；`readonly_diagnostic` 表示只读测量或资源统计，后者不得提交为处理参数。参数卡继续只读；主页声音参数列不得把“音高、变速与共振峰”“特征、信噪比与空间混合”整体标为编辑区。MFCC 偏移、MFCC 维度、SNR 浮动、频谱盲区、干湿比、环境声混合、高质量音高（`pitch_shift_semitones`）、播放速度和共振峰偏移这 9 项只读展示，不得提供手动输入。
- `execution_status`：`disabled` 表示对应开关关闭或参数未启用；`configured` 表示值已通过校验并保存，但不代表 DSP 已执行；`runtime` 表示当前媒体链已确认实际生效；`unavailable` 表示当前 Worker/分析器未接入或能力不可用，必须保持原音轨或已确认的可用结果。

UI 必须分别展示配置值、只读诊断值和 `execution_status`。任何只完成字段保存、范围校验或面板展示的参数都不得标记为 `runtime`。本节不扩展第 6 节的实时音频幻化参数。

### 3.1 基础与自然真人模式

本表中的“配置”表示参数契约角色，不表示用户可编辑。音频参数由默认值、未被本决策收为只读的既有配置控件或预设/周期调度产生，经校验后交给 FFmpeg 与混音后 Signalsmith Stretch 处理链；普通参数卡只展示当前值。上述 9 项只能由默认值、普通声音预设或周期随机化产生，随机化后刷新展示，并继续进入既有校验、N+1 候选准备/提交和运行状态链路。`random_change_period_ms` 是每轮从全局声音周期区间抽出的调度结果（校验范围 `1000–60000ms`），不是 22 套声音预设中的第 36 个字段；当前 UI 的声音/视频周期默认都从 `8000–15000ms` 随机取值。

界面展示分为两层：能力状态列出本地音频流/媒体输出链已支持的声音参数；预览状态列出用户配置、流式处理状态、PortAudio/WebView 实际出口和视频缓存状态。普通声音处理的 PortAudio 主路径直接从源视频音频流输出双声道 f32 PCM，不把处理后音频 MP4 作为播放前置条件；视频处理缓存只保存视频结果并复制源音频供 WebView 回退。22 套预设逐套精确包含 35 个可写效果字段：p01–p20 为默认低感知随机池，p21、p22 为明显可听验收预设且仅在用户显式勾选或固定选择后参与；普通声音与随机插话引用同一预设事实源。Web Audio 不作为 PortAudio 的 PCM IPC 执行器；随机插话的 WebView 路径会播放短期处理缓存，缓存失败才明确回退插话原声。

| 参数 | 参考范围 | 单位 | 默认值 | 角色 | 当前生效状态 |
|---|---:|---:|---:|---|---|
| 自然真人模式 | `original` / `natural_dynamic` | 枚举 | `original` | 配置 | `original=disabled`；`natural_dynamic` 进入按变化周期运行的缓慢响度包络，执行时为 `runtime` |
| 声音随机周期 | `1–60` | s 媒体时间 | `8–15` 随机取值 | 配置 | 默认独立调度；提前保存 N+1/N+2 参数，仅 N+1 可创建 FFmpeg 候选 |
| 视频随机周期 | `1–60` | s 媒体时间 | `8–15` 随机取值 | 配置 | 默认独立调度；提前保存 N+1/N+2 参数，不改变音频参数 |
| 声音与视频联动周期 | 两区间交集 | s 媒体时间 | 关闭 | 配置 | 开启后共享同一绝对媒体目标；无交集时拒绝启用，不合并或覆盖原输入 |
| 下一次随机周期 | `0–周期` | s 媒体时间 | 只读倒计时 | 只读运行状态 | 由最终播放窗口绝对媒体时钟计算，暂停时冻结；定时器时间不是目标事实源 |
| 高质量音高（音高微移） | `-2–+2` | semitone | `0` | 配置 | UI 只读；由普通声音预设/周期随机化写入，Signalsmith Stretch 在 FFmpeg 多支路混音后执行并保持音频时长；不得修改视频 `playbackRate` |
| 共振峰偏移 | `-5–+5` | % | `0` | 配置 | UI 只读；由普通声音预设/周期随机化写入，Signalsmith Stretch 与音高共用混音后处理器；执行时为 `runtime`，当前不提供独立“自动共振峰保持”开关 |
| 输入增益 | `-6–+6` | dB | `0` | 配置 | 已映射媒体 Worker，执行时为 `runtime` |
| 输出增益 | `-6–+6` | dB | `0` | 配置 | 已映射媒体 Worker，执行时为 `runtime` |
| 响度调整 | `-6–+6` | dB | `0` | 配置 | 已映射媒体 Worker，执行时为 `runtime` |
| 播放速度 | `0.5–2.0` | 倍 | `1.0` | 配置 | UI 只读；已映射 FFmpeg，流式处理时与视频 `playbackRate` 共用同一倍速，并由每轮主预设参与周期变化 |
| 低/中/高频均衡 | `-12–+12` | dB/频段 | `0` | 配置 | 已映射 FFmpeg，处理缓存生效时为 `ready` |
| 降噪强度 | `0–100` | % | `0` | 配置 | 已映射 FFmpeg `afftdn`，处理执行时为 `runtime` |
| 环境声混合 | `0–100` | % | `0` | 配置 | UI 只读且参加预设/周期随机化；值为 `0` 时不加载第二输入，值大于 `0` 时用户本地素材可选且优先，否则使用随包内置真实环境声；最终来源验证成功后才循环解码并进入总线 `amix` |
| 淡入时长 | `0–10` | s | `0` | 配置 | 已映射 FFmpeg，处理缓存生效时为 `ready` |
| 淡出时长 | `0–10` | s | `0` | 配置 | 离线有限输入映射 FFmpeg；PortAudio 无限流实时路径跳过 `areverse`，避免无界缓存 |
| 干湿比 | `0–100` | % | `0` | 配置 | UI 只读且参加预设/周期随机化；已建立完整处理声的短反射湿声支路，并按百分比与干声 `amix` |
| 轻混响湿声 | `0–20` | % | `0` | 配置 | 已映射 FFmpeg `aecho`，流式处理运行时为 `runtime` |
| 环境底噪电平 | `-60–-20` | dBFS | `-40` | 配置 | 与环境噪声比例一起映射 FFmpeg `anoisesrc/amix` |
| 频谱扰动 | `0–10` | % | `0` | 配置 | 已映射 FFmpeg `afftfilt` 幅度扰动 |
| 高频扰动开关 | 开/关 | — | 关 | 配置 | 开启且强度非零时只作用于约 6kHz 以上频段 |
| 高频扰动间隔 | `0.5–60` | s | `12` | 配置 | FFmpeg 时间表达式周期门控 |
| 高频扰动强度/电平 | `0–20 / -60–0` | `% / dB` | `0 / -32` | 配置 | 已映射 FFmpeg `afftfilt`，执行时为 `runtime` |
| 采样率 | `source` / `44100` / `48000` | Hz | `source` | 配置 | 已映射媒体 Worker，显式转换执行时为 `runtime` |
| 输出音频码率 | `64–320` | kbps | 自动读取 | 配置 | 仅保存/校验时为 `configured`；编码器实际采用后才为 `runtime` |

当前不提供独立动态压缩参数。环境声素材混合只有在真实第二输入完成校验并进入当前 FFmpeg 图后才能报告 `runtime`；干湿比、降噪、相位、颤音和环境噪声也只有实际链成功运行时才能标记为 `runtime`，不能仅因字段存在或默认值为零而标记为已生效。

### 3.2 补充音频参数的真实执行语义

`natural_voice_mode=natural_dynamic` 使用本轮 `random_change_period_ms` 驱动 1.2% 的缓慢本地响度包络；`voice_library_id` 经 FNV-1a 稳定映射到内建轻量 EQ 参数，不下载或推断话术模型。`ambient_sound_mix_percent = 0` 时不创建 `[1:a:0]`。值大于 `0` 时，Rust 优先规范化用户可选的本地素材；用户未选择时解析随包内置真实环境声音频。用户素材必须位于授权路径，随包素材必须位于受控资源目录；两种来源均要求可读、格式属于 mp3/wav/m4a/aac/ogg/flac，并通过 FFmpeg 实际解码验证后才作为 `[1:a:0]`。任一验证失败时保持原声并报告可恢复错误。`dry_wet_percent` 使用独立短反射湿声支路。`spectrum_blind_spot_percent` 以 8kHz 为当前明确中心，带宽为 `20000 × percent / 100` Hz 的 `bandreject`。

`mfcc_shift_percent` 使用 1024 点 STFT、40 个 Mel 滤组、DCT-II/DCT-III 和原相位重建，`mfcc_dimensions` 控制 1–40 阶实际分析/重建维数；固定算法延迟由有界 FIFO 承接。`snr_target_db/snr_variation_db` 在混音后滚动测量 RMS，只在目标 SNR 低于当前测量值时注入达到目标所需的增量噪声；加噪不能改善 SNR，因此更高目标保持旁路。最终 PortAudio 混音 PCM 同时产生 realFFT MFCC、滚动噪声底/SNR 和 LPC F1/F2/F3；`current_formant_hz` 只读投影 F1，非空值不得作为效果请求提交。独立报告任务、报告导出和旧 Worker 不恢复。

`snr_target_db` 默认 `null`，UI 必须显示“自动”，表示读取当前源与混音总线基线后不强制固定目标。p01–p20 中的 MFCC 偏移/维数、SNR 浮动、频谱盲区、干湿比、环境声混合、高质量音高、播放速度、共振峰偏移及其余预设可写字段都使用非中性低值；p21、p22 也包含相同的完整 35 字段，只提高可听强度。“预设可写”不开放上述 9 项的用户输入。预设完整性按精确字段集合校验，禁止缺字段后静默继承默认值。

### 3.3 普通声音处理的本地 DSP 参数预览

该预览属于普通声音处理链路，与本版本不开发的实时音频幻化完全隔离。声音处理开关关闭时不启动 FFmpeg 音频处理或 Signalsmith Stretch PCM 处理；当前任何参数状态都不得触发 speech-to-speech、VAD、ASR、LLM、TTS 或候选话术音轨生成。

普通声音预设或周期随机化提交参数快照后，Rust 按既有入口保存经校验的流式音频配置：FFmpeg 解码真实源；`ambient_sound_mix_percent = 0` 时不加载辅助输入，值大于 `0` 时按“用户本地素材优先、随包内置真实环境声默认”解析最终来源，完成路径、格式、可读性和 FFmpeg 解码验证后才增加真实第二输入，再执行各支路滤镜/干湿混合并用 `amix` 合成双声道 f32 PCM。任一来源验证失败时保持原声并报告可恢复错误。随后 Signalsmith Stretch 统一执行高质量音高与共振峰偏移，realFFT/rustdct PCM 处理器执行 MFCC 重建与目标 SNR 增量噪声，最后进入候选缓冲、诊断分析和 PortAudio。多轨支路可以保留各自的增益/EQ/空间效果，但混音后 DSP、播放速度、采样率、码率与真实辅助输入字段必须继承本轮主预设；“共享”仍表示这些字段参加本轮变换，只是不在支路间分叉。上述 9 项的展示刷新不得绕过校验、候选准备/提交、失败回退或运行状态更新。

N+1 参数预选完成后立即启动唯一 Scheduled 候选，N+2 只保存参数，候选预热完成后在播放安全边界切换。声音处理开启且视频正在播放时自动尝试 PortAudio，不提供手动硬件出口开关；硬件流先以 callback 静音暂停态启动，视频继续使用 WebView 原声。设备启动、FFmpeg/Signalsmith 预热和源同步在 Tauri blocking worker 上执行。Signalsmith 的算法输入/输出延迟计入候选就绪水位；只有产生连续有效 PCM 并完成原有水位门禁后才标记 `runtime`。首轨取得实际输出延迟并裁剪到可听位置后，按“至少 200ms 且至少覆盖 8 次硬件 callback、最高 500ms、不得超过环缓容量”的动态水位原子预填，成功后才放行 callback 并静音 WebView。普通源启动/恢复/跳转和普通声音周期候选的 CatchUp 预热与提交使用同一单调时钟，提交前按最新画面、环缓和 DAC 延迟动态检查覆盖；不足时每 10ms 复核，最多持续 5 秒。周期候选从 prepare 起的所需覆盖等于“已用准备时间 + max(130ms, 当前动态播放水位)”，提交安全尾部最高 750ms，候选缓冲上限为 `5000ms + 提交安全尾部`（5130–5750ms）。周期换轨由唯一 `audio_cycle_output` 线程取得旧/新轨样本，原子追加完整 30ms 双向线性交叉淡化；候选至少保留 100ms 淡化后连续 PCM。未达到动态覆盖时保持 WebView/旧轨并返回可重试状态，不得按固定 750ms PCM 提前切换。即时插话继续使用固定窗口 RealTime 预热，只覆盖当前硬件水位。PortAudio 不可用或首轨 Signalsmith 初始化/处理失败时保持 WebView 源视频原声；运行中候选失败时保持旧轨，不静默退回旧 FFmpeg `asetrate` 变调。视频处理开启时，视频仍经过 FFmpeg 生成缓存，但缓存音频只复制源音轨，不承担普通声音效果。运行态不能只看 callback 次数：当前 mixer、音频时间轴和实际 PCM frame 必须同时有效；callback 补零不得标记为运行。有效 PCM 连续 1500ms 不推进仍是不可隐藏的真实停产，继续触发现有一次恢复，恢复失败才关闭 PortAudio 偏好并保持 WebView。Web Audio 只保留回退和观测。

普通源同步和 PCM 恢复共用独立占位保护全局音轨 token；N+1 prepare 和无匹配的旧候选 cancel 不能抢占。媒体身份或声音 revision 更新产生的旧同步候选标记为可重试 `audio_mixer_candidate_superseded`，追赶/启动被更新控制请求抢占产生的 `audio_mixer_candidate_stale/audio_mixer_start_stale` 也标记为可重试，二者都不等同于 PortAudio/FFmpeg 故障，也不得关闭硬件偏好；暂停、停止仍可立即取消，前端不显示音频源不可用。最新同步成功后只清除已解决的 PortAudio 回退提示。声音 profile 值实际变化时递增 `audio_stream_revision`，相同值重复提交保持 revision 不变。前后端通过 `reason_code/retryable` 区分暂态恢复与硬回退。

活动 PortAudio 的源同步只由最终效果窗口发起；主窗口仅创建或重启未激活的硬件出口。声音周期是否由 PortAudio 候选链路接管，以 `preferred_portaudio=true && hardware_state=active` 为准，不以瞬时 `running` 为准；恢复期间即使 `running=false`，主窗口也不得直接应用下一 profile 或递增声音 revision。普通声音处理的输入路径以播放池当前项源素材为准，不使用视频处理缓存路径。多项池切换到另一个源条目时必须递增 `playback_generation`，取消并有界等待旧媒体代际的同步/候选/生产任务，按新媒体从 `0ms` 重建绝对媒体时钟后重新绑定；不得把上一项候选带入下一项。单项池回到自身时保持 generation，递增 `playback_pool_cycle` 并继续同一媒体代际。旧 candidate 的取消命令必须等待 prepare 完成并按 ID 匹配，不能抢占新代际候选。硬件仍为 `active` 但尚无有效 PCM，或 callback 持续欠载时，WebView 保持或恢复当前项原声，最终效果窗口在冷却期内串行尝试普通源同步或生产者恢复。

播放项推进使用单飞提交门禁，并以 `(source_media_pool 当前条目, source_media_index, playback_generation)` 校验事件；`timeupdate` 与 `ended` 不能在同一物理边界重复推进索引、跳过条目或重复递增 `playback_pool_cycle`。PCM 恢复的健康路径使用 30ms 双向淡化；等待候选取得完整淡化样本期间继续从 current 向物理环缓续料。仅当旧轨 PCM 与物理环缓均为空时，已预热候选允许从静音执行 30ms 淡入接管。

| 参数 | 处理方式 | 预览状态 |
|---|---|---|
| 增益、响度、三段 EQ | FFmpeg `volume/equalizer` | `configured/processing/ready/failed` |
| 高质量音高、共振峰偏移 | Signalsmith Stretch 混音后 PCM 处理；音高补偿保持打开，UI 只读展示预设/周期随机化后的正式值 | `configured/processing/ready/failed` |
| 播放速度 | UI 只读展示预设/周期随机化后的值；FFmpeg `atempo` 执行并与视频 `playbackRate` 同步 | `configured/processing/ready/failed` |
| 淡入、淡出、轻混响 | FFmpeg `afade/aecho`；实时淡出不使用 `areverse` | `configured/processing/ready/failed` |
| 降噪、相位、颤音 | FFmpeg `afftdn/aphaser/vibrato` | `configured/processing/ready/failed` |
| 环境噪声比例与电平 | FFmpeg `anoisesrc/amix` | `configured/processing/ready/failed` |
| 频谱扰动、高频周期扰动 | FFmpeg `afftfilt`；高频扰动只作用于归一化总线约 6kHz 以上频段 | `configured/processing/ready/failed` |
| 采样率、输出码率 | FFmpeg `aresample` 与运行时采样率配置；视频缓存音频仍复制源 AAC | `configured/processing/runtime/failed` |
| 环境声素材混合、干湿比 | UI 只读展示随机化值；已校验的第二音频输入循环解码后进入 `amix`，完整处理声经短反射湿声支路按百分比与干声混合 | `configured/processing/ready/failed` |
| MFCC 偏移/维数、SNR 目标/波动、频谱盲区 | 其中 MFCC 偏移/维数、SNR 浮动和频谱盲区只读展示随机化值；混音后 realFFT/rustdct PCM 处理，仅在目标 SNR 低于实测值时注入增量噪声 | `configured/processing/ready/failed` |
| 独立压缩及尚无正式字段的 DSP | 当前模型没有可执行参数，不猜测映射 | `unavailable` |

UI 展示全部正式参数、当前能力分类、配置、处理进度、当前运行出口和失败回退原因。声音/视频随机周期分别调度，不能在 Web Audio 中执行声音效果；任何效果都不得包装为平台检测规避能力。

Signalsmith Stretch 上游采用 [MIT License](https://github.com/Signalsmith-Audio/signalsmith-stretch/blob/main/LICENSE.txt)。商业安装包无需购买 Rubber Band 专有授权，但发布物必须保留 Signalsmith Stretch 与 Signalsmith Linear 的版权及 MIT 许可文本；当前 Tauri 资源清单已携带两份许可证。MFCC 的 FFT 与 DCT 分别复用 `realfft 3.5.0`（MIT）和 `rustdct 0.7.1`（MIT OR Apache-2.0），不维护自写 FFT/DCT；两者是本地纯 Rust 计算依赖，不携带模型、不发起网络请求、不要求运行时下载，替代方案“自写 FFT/DCT”因重复造轮子和数值风险被拒绝，Essentia 因 AGPL/商业授权与体积边界未引入。三项依赖均由 Cargo 版本约束与 `Cargo.lock` 固定，在本地/CI 为目标平台构建，不得从运行时网络下载 DSP 库或把源码上传服务器构建。本轮已完成来源、许可证、锁文件、严格 Clippy 与测试审查；正式发布前仍必须执行依赖安全审计、release 二进制体积差异、C++ 工具链/静态链接、连续播放 CPU/内存、算法延迟与音质验收。移除 Signalsmith Stretch 时必须同时撤下非零音高/共振峰的运行态入口；移除 realfft/rustdct 时必须撤下非零 MFCC 重建和对应诊断，均不能用同名低质量近似仍显示“已接入”。

### 3.4 PortAudio 内存缓冲契约

| 参数 | 范围 | 单位 | 默认值 | 说明 |
|---|---:|---|---:|---|
| 应用侧 SPSC PCM 内存缓冲 | `128–2048` | KiB | `1024` | 用户可编辑；表示实际分配的 f32 环形缓冲字节容量，不是硬件回调帧数 |
| PortAudio 硬件回调 | 内部参数 | frame | `256` | 与内存缓冲独立，当前不作为用户输入 |
| PortAudio Host API 筛选 | `all` / `wasapi` / `asio` / `mme` / `dsound` / `wdmks` | 枚举 | `all` | `ASIO` 始终可见；仅在运行时枚举到 ASIO 输出设备时启用，当前随包运行库未启用 ASIO |
| 普通声音周期候选提交安全尾部 | `130–750` | ms PCM | 运行时动态 | `max(30ms crossfade + 100ms tail, 当前动态播放水位)`；prepare readiness 还要加上已经过去的准备时间 |
| 普通声音周期候选有界 PCM 容量 | `5130–5750` | ms PCM | 运行时动态 | `5000ms` 有界追赶上限 + `130–750ms` 提交安全尾部；用于追赶时钟和覆盖 EOF 重启，不一次性写满物理 SPSC 环缓 |
| PCM 真停产门槛 | `1500` | ms 无有效 frame | `1500` | 暂停时不观察；静音 PCM 仍计为有效 frame；触发一次生产者恢复，失败回退 WebView |

应用侧环缓容量不等于播放延迟。普通实时播放采用独立动态目标水位：`max(200ms 对应帧数, 8 × frames_per_buffer)`，上限为 500ms 且不得超过实际环缓容量；它用于覆盖 Windows 调度抖动并防止 FFmpeg 把 1024 KiB 默认容量全部填满。周期候选提交至少需要 130ms 连续新轨 PCM（30ms crossfade + 100ms post-fade tail），提交安全尾部取该值与当前动态播放水位的较大者、最高 750ms；prepare readiness 还必须加上已经过去的准备时间，内部队列因而有界保留 `5000ms + 提交安全尾部`（5130–5750ms）。只有完整 30ms 交叉淡化原子追加到物理环缓，后续 PCM 仍按动态水位供给。候选提交目标必须提前扣除环缓实际待播毫秒数和 PortAudio 硬件输出延迟；若后端判断尚未到点或 PCM 未覆盖动态水位，保留已预热候选和 current 并继续等待，不重新创建 FFmpeg。禁止把数秒候选缓存一次填满物理环缓。UI 同时展示环缓占用样本数、按实际采样率/声道换算的待播毫秒数、动态目标水位、PortAudio 实际输出延迟和 A/V 偏差。

实时 PortAudio 解码每次只处理一遍有限的当前项输入；FFmpeg 成功到达 EOF 后退出当前 FFmpeg 子进程。播放协调器单飞决定下一项或末项回首项：多项池切到另一个源时递增 `playback_generation`，有界停止并 Join 旧子进程/线程，再以新当前项 `0ms` 和新绝对时钟启动解码；单项池保持 generation、递增 `playback_pool_cycle`，从同一源 `0ms` 继续。实时滤镜链仍禁止使用需要缓存完整输入的 `areverse`。即时插话的 RealTime 生产速率按 `playback_speed × 1.1` 设置有界 `-readrate`；普通源启动/恢复/跳转使用 `max(2, playback_speed + 1)` 和 0.5 秒初始突发；普通声音周期候选独立使用 `max(3, playback_speed + 1)` 和 1 秒初始突发。周期候选提交后的 5130–5750ms 有界容量只覆盖当前项内追赶与受控恢复间隙。实时淡出参数仍可保存和校验，但当前安全行为是跳过反向淡出；`areverse → afade → areverse` 只允许用于有限输入的离线媒体处理。停止时必须确认 FFmpeg 子进程、stderr、解码线程和混音线程均已退出，不得静默放弃线程句柄；进程重启必须先等待上一进程退出，运行态任务数最多为 1 个当前任务加 1 个候选任务。

“已知未来切换点”的普通声音周期候选直接 seek 到目标前 250ms，并使用 `-readrate max(3, playback_speed + 1) -readrate_initial_burst 1.0` 追赶；prepare readiness 为“已用准备时间 + max(130ms, 当前动态播放水位)”，有界队列容量为 5130–5750ms。候选目标若达到或跨越当前项 EOF，则将其标记为当前媒体代际失效并保持 current 至项结束；不得从当前旧源 `0ms` 续接。播放协调器换源后再为新 `playback_generation` 建立候选。动态覆盖不足时保持 current，不提前切轨。提交安全尾部中 30ms 用于交叉淡化，淡化后至少还有 100ms 连续 PCM。普通源启动/恢复/跳转仍用 2x 最低速率和 0.5 秒突发，即时插话使用 `playback_speed × 1.1` 的 RealTime 固定窗口。三类实时链路均禁止 `areverse`。

本产品的只读诊断来自最终混音 PCM：除波形、频谱和响度外，还输出 1–40 维 MFCC、滚动噪声底与 SNR，以及 LPC 估计的 F1/F2/F3。`current_formant_hz` 只读投影 F1，不得作为配置提交；`formant_shift_percent` 则是 Signalsmith Stretch 已执行的独立配置。分析器样本不足或单次估计失败时返回不可用诊断但不阻塞播放，不能用默认静态数值冒充实测结果。

### 3.5 桌面端全量正式参数面板

桌面端中栏统一使用 `MediaParameterPanels`。参数内容容器必须填满桌面中栏的实际可用宽度，不设置约 `880px`、`860–900px` 或其他固定 `max-width`，不得因居中宽度上限在中栏两侧留下大面积空白。容器外层保持等分伸缩的双主栏：左栏依次承载“普通视频”和“高级视觉”，右栏承载“普通声音”；每个主栏内部的参数卡网格根据该栏实际可用宽度自动决定列数并自然换行，不限制每排卡片数量，也不设置固定列数。参数区极窄时，外层双主栏按“普通视频 → 高级视觉 → 普通声音”的 DOM 顺序降为单列。这里的双主栏只描述参数区内部信息结构，不改变桌面工作区外层的素材/参数/输出三栏。面板定义仍必须与 Rust `VideoEffectParams`、`AdvancedEffectParams`、`AudioEffectParams` 逐字段相等，不能漏字段、重复字段或重新引入 `research_*`。

普通参数卡统一使用 `80px` 固定紧凑高度；卡片宽度由所属主栏内部的自动列数网格和实际可用空间共同决定，窗口缩放时自然换行，不设置固定列数或固定/目标 `270–290px` 卡宽。卡内只保留参数名称、当前值或无手柄显式紧凑分段进度，以及“已接入 / 正式需求待实现 / 待确认”三态标签；不得渲染“范围……；已接入/正式需求待实现原因……”等底部说明句。范围、单位、默认值和能力状态仍以本文件各参数表与状态清单为正式事实，并继续参与进度归一化、边界校验和媒体实现判断，隐藏说明句不改变这些定义。完整进度分段必须落在卡片正文宽度内，禁止通过 `overflow` 裁剪制造无溢出的假象。固定视觉频段权重使用独立 `196px` 固定高度卡片并跨满所属栏，在卡内按三列完整排列频段名称和当前权重，并继续使用卡级三态标签；不得裁剪或以缺项换取统一高度。

参考图提取色板是本参数面板逐卡颜色的唯一颜色契约：青蓝 `#38B8F8`、橙 `#F89838`、紫 `#C888F8`、粉紫 `#E878F8`、黄 `#F8C818`。每次参数面板挂载时，为每张参数卡或独立参数模块分别从该五色色板分配自己的颜色，不再为“普通视频 / 高级视觉 / 普通声音”三个大分类各固定一种颜色。五色允许循环复用，分配顺序应尽量避免视觉上相邻的卡片或模块同色；每卡颜色映射在该次挂载生命周期内保持稳定，参数更新、重新渲染和排序不得重新抽色。颜色只用于逐卡识别和更新反馈，不表达算法能力或算法状态。

全量参数卡和周期状态模块保持只读；主页声音参数列可承载“音高、变速与共振峰”和“特征、信噪比与空间混合”两个具名展示/配置分组，但不得把分组整体定义为编辑区。上述 9 项与其他参数一样只读展示，高级声音抽屉不再保留其重复输入；它们继续由普通声音预设和周期随机化更新，更新后刷新显示并进入既有校验与运行链路。目标 SNR 为 `null` 时必须可见显示“自动”；只读诊断、能力三态和当前运行值仍不得由输入控件伪造。

参数卡在挂载后只要该卡接收的显示 `value` 实际变化，就使用该卡自己的稳定颜色执行一次约 `900ms` 的高亮反馈：先短暂增强，再平滑淡出，避免约 `290ms` 瞬时闪动难以感知。初始挂载、相同值、仅重排和没有改变 `value` 的普通重新渲染不得触发。该规则以 `MediaParameterPanels` 已有显示值为事实，不额外假设全量面板存在 `runtime`、预热或失败状态属性。`prefers-reduced-motion: reduce` 下必须禁用高亮并保持静态可读状态。算法状态仍以“已接入 / 正式需求待实现 / 待确认”三态文字为唯一表达，逐卡颜色不得表达或替代算法状态。`audio_stream_params`、`audio_stream_variants` 继续服务于已提交普通声音支路的独立状态区，但不得被描述为全量参数面板的唯一事实源。该反馈是本轮新交互，不复用此前已删除的无可见消费方闪动 token，也不得基于 UI 值推断“已生效”或预设身份。处理失败必须展示 Rust 返回的明确原因，未接入的非默认值由 Rust 媒体边界拒绝，不能在前端静默归零。

主参数区必须同时显示视频周期和声音周期两个只读状态模块，不得仅以视频周期代表两条链路。每个模块分别展示本文件和既有配置所定义的周期范围、当前真实变化次数，并以各自同一条 N+1 真实计划的目标时间和周期长度计算进度；无真实下一计划时进度为 `0` 且保持非活动。展示层不得新增随机采样、改变范围或使用前端倒计时生成虚假进度。声音周期状态标签直接读取现有真实 `audio processing status`，不能从视频周期、声音开关外观或进度动画推断。

独立模式下，视频与声音状态分别使用各自的真实计划、周期长度和变化次数推进；联动模式下，两条状态仍同时显示，但引用同一个真实联动目标。视频或声音处理关闭、播放暂停、对应周期未启用或尚无真实下一计划时，相应模块的进度为 `0` 且保持非活动，次数按现有调度器状态显示。该状态投影不改变既有周期范围、默认值、联动区间求交、N/N+1/N+2 调度、候选准备提交或任何后端算法。

### 3.6 插话音轨选择参数与双出口契约

| 参数 | 范围/允许值 | 单位 | 默认值 | 说明 |
|---|---:|---:|---:|---|
| 启用随机插话 | `true / false` | 布尔 | `false` | 启用前目录必须通过校验 |
| 插话目录 | 存在且可读的本地目录 | 路径 | 未配置 | 作为扫描根目录，规范化后递归发现任意层级子目录中的可用音频文件，且至少存在一个 |
| 插话目录集合上限 | 文件 `<=256`；单路径 `<=4096` UTF-8 字节；全部路径累计 `<=512 KiB` | 数量/字节 | — | 流式扫描；任一上限超出时拒绝整个配置，不保留部分结果 |
| 支持文件扩展名 | `mp3 / wav / m4a / aac / ogg / flac` | 枚举 | — | 每次播放前仍须校验规范化路径、普通文件和可读性 |
| 插话音轨选择模式 | `fixed / random` | 枚举 | `random` | IPC `audio_selection_mode`；`fixed` 固定单轨且仅在运行时不消费已保存、已校验的随机预设池、多轨和变化策略，`random` 从所选池抽样 |
| 插话固定预设 | `p01–p22` | 枚举 | `p01` | IPC `audio_fixed_preset_id`；只在 `audio_selection_mode=fixed` 时作为唯一实际音轨 |
| 插话随机声音参数值预设池 | `p01–p22` 中 `1–22` 个唯一项，可全选 `22` 项 | 枚举集合 | `p01–p20` | IPC `audio_preset_ids`；普通声音与随机插话共用 22 套精确 35 字段定义，但分别保存选择状态；`p21/p22` 只有显式选择后参与 |
| 插话多轨合一开关 | `true / false` | 布尔 | `false` | IPC `audio_mix_enabled`；所有模式都保存并校验，`random` 运行时关闭则每次抽取一项、开启则按支路上下限抽取并合一；`fixed` 运行时不消费该值 |
| 插话随机支路下限 | `1–4` 且不大于上限 | 条 | `1` | IPC `audio_mix_pick_min`；所有模式都保存并校验，只在 `audio_selection_mode=random && audio_mix_enabled=true` 时决定本次最少抽取数 |
| 插话随机支路上限 | `1–4` 且不小于下限 | 条 | `2` | IPC `audio_mix_pick_max`；所有模式都保存并校验，实际支路数不得超过预设池中的可选项数；`fixed` 运行时不消费该值 |
| 插话预设变化策略 | `each_playback / periodic` | 枚举 | `each_playback` | IPC `audio_variation_mode`；所有模式都保存并校验，`random` 运行时由 `each_playback` 在每段开始前重新抽样，或由 `periodic` 在周期到期时更新下一段待应用组；`fixed` 运行时不消费该值 |
| 插话预设随机周期下限/上限 | `1000–60000` 且下限不大于上限 | ms | `8000 / 15000` | IPC `audio_variation_period_min_ms / audio_variation_period_max_ms`；所有模式都保存并校验，仅 `random + periodic` 在运行时从区间内重新取值，周期到期只影响下一段插话，不中途重启、换组或切换当前音频 |
| 触发间隔下限/上限（UI） | `0.5–60` 且下限不大于上限 | s | `8 / 13` | UI 以秒显示和输入；提交/回显边界换算为 IPC/Rust `interval_min_ms/interval_max_ms`，其范围仍为 `500–60000ms`。暂停、固定话术抢占或停止期间不推进；存在活动插话时恢复当前插话，否则继续现有调度 |
| 插话音量 | `-60–+12` | dB | `0` | 作用于插话轨 |
| 主轨 duck 深度 | `-60–0` | dB | `-12` | 插话实际出声期间作用于主轨 |
| duck 攻击 | `0–1000` | ms | `50` | 插话进入时的衰减包络；`0` 表示立即压低到目标增益 |
| duck 释放 | `0–3000` | ms | `250` | 插话结束、失败、暂停或抢占时恢复主轨；`0` 表示立即恢复 |

UI 的秒制只适用于插话触发间隔，不修改运行态和 IPC 的毫秒事实源。React 必须在显示/输入边界执行秒与毫秒换算；Tauri/Rust 继续对 `interval_min_ms/interval_max_ms` 执行 `500–60000ms` 和下限不大于上限的信任边界校验。`ducking_attack_ms/ducking_release_ms` 始终以毫秒传输，`0` 是合法且有明确即时语义的值，不能被前端空值回退、默认值合并或运行时包络逻辑改写为旧的非零下限。

WebView 为实际出口时，Rust 以本段完整预设和同一环境声解析结果生成一份短期处理音频缓存，最终效果窗口播放该缓存并 duck WebView 主轨；处理失败时明确回退插话原声，不影响主轨。PortAudio 接管时，`fixed` 只应用 `audio_fixed_preset_id` 对应的一项参数；`random` 单轨应用本次从 `audio_preset_ids` 抽取的一项参数，随机多轨合一则在同一个 FFmpeg 滤镜图内 `asplit` 为 `1–4` 条有限支路、分别应用本次参数后以 `amix` 等权合成一条 PCM，再解码、重采样并匹配当前声道，混入唯一 `audio_cycle_output` 输出流并继续 duck 主轨。两条路径共用预设、参数校验和环境声来源。短期缓存按本次插话 generation 持有，当前段结束、失败、停止、切换素材或窗口卸载时删除；配置变化只影响下一段，不抢停或删除当前段缓存。无论随机池是否全选 22 项和本次支路数，都只允许一个受管插话任务、一个 FFmpeg 图、一个 PortAudio 输出和最多四条实际支路，不预生成 22 套组合缓存。

插话选择模式、固定预设、随机预设池、`audio_mix_enabled/audio_mix_pick_min/audio_mix_pick_max`、变化策略和周期状态与普通声音分别保存，但引用同一份 22 套精确 35 字段预设定义。默认 `audio_selection_mode=random`、`audio_fixed_preset_id=p01`、随机池 `p01–p20`、`audio_mix_enabled=false`、`audio_mix_pick_min=1`、`audio_mix_pick_max=2`、`audio_variation_mode=each_playback`，随机周期默认 `8000–15000ms`；`p21/p22` 只有显式勾选或固定选择后参与。无论模式为何，随机池都必须保存并校验为 `1–22` 个互不重复的合法项；`fixed` 运行时只消费固定预设，`random` 才消费随机池、多轨和变化策略。`periodic` 到期或用户保存配置时，当前活动插话保持启动时快照直至自然结束，下一段才应用新固定选择或重新抽样。该链路不启动 speech-to-speech、VAD/ASR/LLM/TTS、模型租约或任何实时话术幻化能力。

暂停会挂起当前插话与随机计时，继续后从当前插话位置恢复。配置变化只提交下一段使用的新配置并标记应用新固定选择或重新抽样，不使当前 generation 失效，不停止 FFmpeg，不清空当前 PCM，也不改变当前插话的预设组、音量或 duck 包络；当前插话自然结束后才释放其旧资源和旧目录精确授权。只有停止播放、切换素材或窗口卸载会主动取消当前插话任务，并在既有超时预算内停止 FFmpeg、清空有界 PCM 和 Join 受管任务。固定话术优先级高于随机插话：朗读前只挂起当前插话，固定话术结束、失败或取消后恢复主轨与被挂起的插话。目录缺失/不可读/无可用文件、固定预设无效、任一模式下预设池为空/重复/含非法项、随机选择、滤镜构建、单文件加载/解码、重采样、混音或输出失败均不得静音或阻断主轨；运行时致命失败结束并清理本次插话，必须恢复 duck，不得残留随机计时、解码器或常驻支路。状态须给出禁用、等待、暂停、固定话术抢占、WebView 原声播放、PortAudio 固定单轨/随机单轨/随机多轨混入或具体失败原因。该契约只覆盖普通声音、随机插话和本地系统固定话术，不引入实时话术幻化或已删除的旧报告链路。

## 4. 视频模块参数

### 4.1 已实现并生效的基础画面效果

下表参数已有 Worker 映射，展示卡保持只读；参数值必须来自当前自动快照/Worker 状态，不得用参考图中的示例值替代本契约默认值。视频只提供一个处理开关和一种自动低感知模式，不增加手动模式选择器。

| 参数 | 参考范围 | 单位 | 默认值 |
|---|---:|---:|---:|
| 亮度 | `-100–+100` | % | `0` |
| 对比度 | `0–200` | % | `100` |
| 饱和度 | `0–200` | % | `100` |
| 色相旋转 | `-180–+180` | ° | `0` |
| 模糊半径 | `0–8` | px | `0` |
| 锐化 | `0–100` | % | `0` |
| 噪点强度 | `0–8` | % | `0` |
| 细节增强 | `0–50` | % | `0` |
| X/Y 空间偏移 | `-4–+4` | px | `0` |
| 动态裁剪 | `0–4` | %/边 | `0` |
| 像素扰动 | `0–2` | px | `0` |
| 像素级缩放 | `95–105` | `%` | `100` |
| 高光/阴影 | `-100–+100` | `%` | `0/0` |
| 暗角 | `0–100` | `%` | `0` |
| 色彩空间转换强度 | `0–100` | `%` | `0`（开关关） |
| 图像修复强度 | `0–100` | `%` | `0`（开关关） |
| 画面旋转 | `-180–+180` | ° | `0` |
| 水平/垂直翻转 | 开/关 | — | 关/关 |
| 帧率扰动 | `0–2` / `0.01–2` / `0–2` | `% / Hz / fps` | `0 / 0.1 / 0` |
| 帧内/帧间扰动 | `0–2 / 0–20` | `%` | `0 / 0` |

只要启用了上述任意视频参数，视频输出必须经过解码、处理和重新编码；首版不使用视频流 `copy`。如果输出产物为 MP4，则对整个 MP4 文件执行对应媒体处理哈希契约。UI 可把 X/Y 偏移显示为两个独立控件，但它们仍属于同一空间偏移参数组。手动调节范围继续保持 `-4–+4 px`；自动周期预览在用户基线之上额外叠加的单轴幅度限制为 `±0.5 px`，不得误改挂件模块的独立“画面偏移”。

### 4.2 正式视频与高级参数：已接入与外部能力边界

已接入：裁剪边缘平滑、画面边缘柔化、源帧率 CFR 锁定、帧率扰动、帧内/帧间扰动、色彩空间转换、高光/阴影/暗角、红通道保护、图像修复、翻转/旋转；高级参数中的波频强度/电平/颗粒数、通道偏移、空间维度与频率空间 X/Y 偏移、帧扰动概率、随机几何挂件、抽象几何脸、同源画中画及独立延迟时间轴、切片显示调度与源片段最小长度、局部模糊、边缘填充/羽化、高光扰动、异步旋转和变换平滑也已进入实际输出。

12 个 `65–20000Hz` 固定频段明确属于视频空间频段，不是声音频段：运行时按对数映射到每幅画面 `1–32` 个空间周期，各权重参与载波调制，目标频率控制载波中心，核心频率控制相位，动态均衡阈值控制调制门限。参数预览继续按第 3.5 节使用跨满所属栏的独立 `196px` 固定高度卡片，以三列完整展示全部频段并标记“已接入”。仍待外部能力或产品定义的是冻结检测回退、主体/真人脸保护、特征相似度、不可见水印载荷及 OCR 待核字段；它们尚无当前正式字段或依赖外部模型，继续按《参考图参数对比与正式化清单》管理，不能猜测实现。

视频处理开启后，每个周期生成一份完整 `VideoEffectParams + AdvancedEffectParams` 自动快照并提交真实 FFmpeg 重新编码；最终效果窗口只显示该经校验的处理缓存，处理关闭或失败时明确回退源视频，两种路径都不得在视频元素上追加 CSS 色彩、模糊、位移或缩放伪效果。`horizontal_flip_enabled`、`vertical_flip_enabled` 保留兼容字段但固定不进入自动池。其余已有真实执行路径的字段按“独立连续参数 / 开关及非零从属强度 / 从属调度参数”参加快照；当前 FFmpeg 基础 X/Y 位移只有整数像素精度，自动快照固定使用 `(+0.5px, -0.5px)`，使实际 `(+1px, -1px)` 方向跨周期稳定，不在 `-1px/+1px` 间跳变。画中画、随机图形、抽象几何挂件、局部模糊、边缘填充、高光扰动和异步旋转保持启用并使用非零低感知值。`picture_in_picture_opacity_percent` 在画中画支路先转为 alpha 再执行 overlay；自动值必须非零且以难以察觉为目标，默认模型中的 `100%` 仅用于兼容既有手动配置。`applied_fields` 和输出帧变化是实际参加变换的证据，字段存在、随机值非零或 CSS 变化不能替代该证据。

低感知数值必须按本契约量级进入滤镜：亚像素/小数参数不能无条件取整成更大的离散值，任意非零值不能触发与幅度无关的固定最小滤镜强度，概率参数不能因整数取模或下限截断而提高实际命中率。底层不支持所需精度时应保持中性/旁路或采用明确的连续表达，不能静默放大。自动化范围/映射测试只保证契约边界；发布仍需对同一素材保留原片/处理结果客观帧差，并在正常观看下确认变化低感知，二者缺一不可。

## 5. 挂件与切片参数

本节参数均为正式 `AdvancedEffectParams` 需求。随机几何挂件、抽象几何脸标记、挂件偏移、切片长度/触发间隔、同源画中画、独立延迟时间轴、源片段最小长度和局部模糊均已接入；`slice_min_length_ms` 在画中画时间轴解锁时约束可见源片段窗口，不代表创建离线切片文件或独立素材选择器。抽象人脸只生成几何标记，不进行真人脸检测、识别或避让。

| 参数 | 参考范围 | 单位 | 默认值 |
|---|---:|---:|---:|
| 随机图形透明度 | `0–50` | % | `0` |
| 随机图形大小 | `1–64` | px | `4` |
| 抽象人脸数量 | `0–10` | 个 | `0` |
| 抽象人脸大小 | `1–10` | %/画面宽度 | `2` |
| 抽象人脸透明度 | `0–30` | % | `0` |
| 画面偏移 | `-10–+10` | px | `0` |
| 切片长度 | `0.5–10` | s | `5` |
| 切片最小长度 | `1–60` | s | `10` |
| 切片触发间隔 | `5–120` | s | `15` |
| 随机图形开关/数量 | 开/关；`1–32` | —/个 | 关/`4` |
| 画中画比例/透明度/旋转/扰动 | `10–50 / 0–100 / -15–+15 / 0–4` | `% / % / ° / px` | `24 / 100 / 0 / 0` |
| 局部模糊区域/半径/间隔 | `5–50 / 0.1–16 / 0.5–60` | `% / px / s` | `20 / 2 / 10` |
| 异步旋转最小/最大角度 | `-15–+15` 且最小≤最大 | ° | `-1 / +1` |

“切片长度”表示一次可见叠加效果的持续时间；“切片最小长度”表示可被选择的源片段下限，二者不是同一个字段。切片触发间隔必须大于等于切片长度，参数校验不能只检查单字段范围。

## 6. 话术幻化与连续优先参数

本节仅保留后续版本的实时话术幻化参数草案，不属于当前版本参数契约。本版本不处理正在播放视频的实时话术音频，不生成候选音轨，也不执行实时换轨。

| 参数 | 参考范围 | 单位 | 默认值 |
|---|---:|---:|---:|
| 单段气口 | `100–300` | ms | `180` |
| 音频变速 | `0.95–1.05` | 倍 | `1.0` |
| 实时安全余量 | `0.5–10` | s | `2` |
| 话术生成失败回退 | `原声` / `上一候选` / `保持当前音轨` | — | `原声` |

后续版本如恢复该能力，模型或 speech-to-speech Worker 处理不及时时应保持当前音轨；本版本不启动 Worker，当前视频直接按源音频/普通声音处理链播放。

## 7. 媒体处理 SHA-256 契约

对 MP4 产物，哈希对象是**整个 MP4 文件的字节流**，不是抽样帧、视频流、音频流或文件名。JSON、WAV 等非 MP4 中间产物也计算完整字节流 SHA-256，但字段名称使用 `artifact_sha256`，不能伪装成 MP4 哈希。

每个阶段执行以下顺序：

1. 阶段开始前，对每个输入产物计算完整字节流 SHA-256；如果输入是 MP4，同时记录 `input_mp4_sha256`。
2. 阶段执行并写入临时产物，不覆盖原文件。
3. 产物完成并通过容器/媒体可读性校验后，对整个输出文件计算 `output_artifact_sha256`；如果输出是 MP4，同时记录 `output_mp4_sha256`。
4. 将阶段、文件路径摘要、文件大小、输入哈希、输出哈希和参数快照写入产物清单。

界面字段约定：

- `source_artifact_sha256`：任意受支持源视频第一次进入流水线前的完整文件字节流哈希；源文件为 MP4 时可同时记录 `source_mp4_sha256`。
- `current_artifact_sha256`：当前选中阶段产物的完整文件字节流哈希；当前产物为 MP4 时可同时记录 `current_mp4_sha256`。
- `stage_input_sha256` / `stage_output_sha256`：阶段级完整产物哈希；输入或输出为 MP4 时，另记录对应的 `*_mp4_sha256`。

非 MP4 源视频不得把自身哈希写入 `source_mp4_sha256`、`input_mp4_sha256` 或其他 MP4 专用字段；只有流水线实际产生 MP4 产物后，才能记录对应 MP4 哈希。

## 8. 参考依据

- 媒体滤镜和参数适配优先基于 [FFmpeg Filters Documentation](https://ffmpeg.org/ffmpeg-filters.html) 等成熟媒体库；项目不自行实现编解码器、滤镜和播放内核。
- 参考图字段、当前三态能力和依赖选型建议见 [参考图参数对比与正式化清单](./参考图参数对比与正式化清单.md)。
- SHA-256 使用 [NIST FIPS 180-4 Secure Hash Standard](https://csrc.nist.gov/pubs/fips/180/4/final) 作为算法依据。

## 9. 变更记录

- 2026-08-25（环境声来源最终澄清）：`ambient_sound_mix_percent = 0` 时不加载第二输入；值大于 `0` 时默认解析随包内置真实环境声，用户本地素材可选且优先覆盖。两种来源都必须通过路径、格式、可读性和 FFmpeg 解码验证，失败保持原声并报可恢复错误。
- 2026-08-25（已被“环境声来源最终澄清”覆盖）：上一轮曾要求环境声非零时必须由用户选择本地素材，并废止随包来源；该要求现已撤回。零值不加载、完整输入验证和失败原声回退要求继续有效。
- 2026-08-25（视频低感知映射与显示）：最终效果窗口只显示经校验的 FFmpeg 处理缓存或明确的源视频回退，不在视频元素上二次叠加 CSS 色彩、模糊、位移或缩放；整数像素位移保持稳定方向，其他微量参数禁止通过取整、固定最小滤镜档或错误概率采样放大。客观帧差和正常观看主观低感知继续作为联合发布门禁。
- 2026-08-25（声音参数只读随机化）：明确 `configurable`/“配置”只表示可进入参数与校验链，不等于用户可编辑。MFCC 偏移、MFCC 维度、SNR 浮动、频谱盲区、干湿比、环境声混合、高质量音高、播放速度和共振峰偏移只读展示，不在主页或高级声音抽屉提供手动输入；9 项继续参加普通声音预设与周期随机化，变化后刷新展示并复用既有校验、候选准备/提交、失败回退和运行状态链路。
- 2026-08-25：低感知视频叠加的运行时有效 alpha 统一下限为 `2/255`；自动采样的 PIP、随机图形和抽象脸透明度为 `0.8–1.2%`，PIP 独立时间轴必须与 overlay enable 窗口相交。自动低强度 `image_repair` 使用随包可用的 `atadenoise=s=5`，显式高强度继续使用 `nlmeans`；随包 FFmpeg 未编译 `hqdn3d`，不得生成该滤镜。p01–p20 不再经过固定 `-16 LUFS` 归一化，所有增益/响度只按预设相对源素材应用；Signalsmith 合法空 PCM 为“尚未产出”，不得调用 MFCC/SNR 或触发硬回退。
- 2026-08-25（历史环境声口径，后续由同日“环境声来源最终澄清”收口）：视频与全声音自动变换落地时允许未选择用户素材时解析随包环境声；最终口径明确为随包内置真实环境声音频，且必须与用户本地素材一样通过完整验证。其余视频完整快照、22×35 预设、WebView 插话短期缓存和失败原声回退事实保持不变。

- 2026-08-25：参数契约静态检查同步当前正式产品口径：像素扰动、帧率扰动和帧内/帧间扰动作为已接入视频参数参与必填校验，`65–20000Hz` 继续按视频空间频段校验；只拒绝已废弃的“研究参数/实验参数”命名，不再把这些正式字段本身判为违规。

- 2026-08-24（正式参数完整接入）：明确 `65–20000Hz` 为视频空间频段并接入 12 段权重、目标/核心频率和动态均衡；补齐视频边缘柔化、源帧率锁定、画中画独立时间轴、源片段最小长度、边缘羽化和变换平滑。音频补齐自然动态、本地音色 ID、真实环境声第二输入、干湿支路、频谱盲区、MFCC 重建、SNR 处理及 MFCC/SNR/LPC 共振峰诊断；新增 `realfft 3.5.0` 与 `rustdct 0.7.1` 并由 `Cargo.lock` 固定。当前正式模型中语义已确认的字段不再标记“正式需求待实现”。
- 2026-08-24（当前最新逐卡高亮动效决策）：参数显示 `value` 变化后按该卡自己的颜色执行一次约 `900ms` 高亮，视觉过程为短暂增强后平滑淡出；`prefers-reduced-motion: reduce` 下完全禁用。该决策覆盖下方约 `290ms` 瞬时闪动时长口径，只调整逐卡颜色反馈动效，不改变触发事实源、逐卡颜色分配、布局、参数范围、状态或实时话术边界。
- 2026-08-24（当前最新双周期状态决策）：主参数区同时显示视频周期与声音周期两个状态模块，各自展示现有配置范围、真实变化次数，并以各自同一条真实 N+1 计划的目标时间和周期长度计算进度；声音状态标签读取真实 `audio processing status`。独立模式各自推进，联动模式仍分别显示两条状态但共用同一个真实联动目标；处理关闭、播放暂停、周期未启用或真实计划缺失时进度为 `0` 且保持非活动，次数沿用现有调度器状态。本决策仅改变状态投影，不修改调度、范围、默认值或后端算法；旧“主状态只显示视频周期”口径仅保留为已被本决策覆盖的历史，不扩展实时话术幻化。
- 2026-08-24（现行逐卡颜色分配；旧动效时长已由上方覆盖）：每张参数卡或独立参数模块分别从参考图五色色板分配自己的颜色，不再按普通视频、高级视觉、普通声音大分类固定一种颜色；五色可循环复用，分配顺序尽量避免视觉相邻项同色，且单次挂载期内逐卡映射稳定。当时显示 `value` 变化使用约 `290ms` 高亮，该时长现已由上方约 `900ms` 短暂增强并平滑淡出决策覆盖。算法状态仍由三态文字表达，颜色不表达算法状态。该决策覆盖下方按三个大分类固定一种颜色的历史口径，不改变满宽、自动网格、`80px`/`196px` 高度和无底部说明规则，也不扩展实时话术幻化。
- 2026-08-24（当前最新满宽决策）：参数内容区填满桌面中栏实际可用宽度，删除约 `880px`、`860–900px` 及其他最大宽度上限；栏内自动网格随窗口增减列数，外层双主栏与极窄单栏降级保持。普通参数卡固定为 `80px` 高，固定视觉频段权重卡固定为 `196px` 高并跨满所属栏；无底部说明、逐卡颜色反馈、三态及正式参数事实规则不变。该决策覆盖此前约 `880px` 居中宽度口径，但不扩展实时话术幻化。
- 2026-08-24（现行卡片内容决策；高度由上一条明确）：普通参数卡只显示参数名、当前值/进度和三态标签，删除底部范围、单位、接入情况及待实现原因说明句；固定视觉频段权重卡跨满所属栏且三列内容必须完整、不得裁剪。范围、默认值和能力状态事实仍保留在本契约、校验与媒体边界中。该决策覆盖此前“说明文字至少 `12px` 且显示状态原因”的卡内展示口径，但不扩展实时话术幻化。
- 2026-08-24（现行自动网格布局；旧宽度子句已由第一条覆盖）：参数预览使用填满中栏可用宽度的外层双主栏，左栏为普通视频与高级视觉、右栏为普通声音；每个主栏内部卡网格根据实际可用宽度自动决定列数并自然换行，不限制每排数量。极窄时外层双主栏按 DOM 顺序降为单列，全程不设置固定列数或卡宽且不得横向裁剪。逐卡颜色、减少动态效果和三态规则保持不变；动效时长以最上方约 `900ms` 决策为准。该布局决策覆盖下一条“每栏两卡、整行最多四卡、先固定降栏内单列”口径，但不扩展实时话术幻化。
- 2026-08-24（历史宽度口径，已由同日最新满宽决策覆盖）：参数内容区此前以约 `880px` 为桌面目标宽度，并允许在 `860–900px` 内取整后居中；该最大宽度和由此产生的中栏两侧留白不再属于当前验收。
- 2026-08-24（历史卡内说明口径，已由同日最新卡片内容决策覆盖）：普通卡当时保持紧凑且显示至少 `12px` 的范围、接入情况或待实现原因说明；该说明句与内容自适应高度要求不再作为当前验收，参数范围、默认值和能力状态事实本身仍保留。
- 2026-08-24（历史响应式澄清，已由同日最新自动网格决策覆盖）：参数预览继续使用约 `880px` 居中的外层双主栏，左栏为普通视频与高级视觉、右栏为普通声音；宽窗下每个主栏内部卡网格一排两张，参数区同一横排最多四张。空间不足时先把栏内卡网格降为单列，参数区进一步变窄时再把外层双主栏按 DOM 顺序降为单列，全程伸缩且不得横向裁剪。紧凑高度、至少 `12px` 说明、三列频段、分类色与约 `290ms` 反馈、减少动态效果和三态规则保持不变。该澄清覆盖下一条“每卡占满整栏、每横排左右两张”口径，但不扩展实时话术幻化。
- 2026-08-24（历史响应式口径，已由同日最新澄清覆盖）：参数预览继续使用约 `880px` 居中的双主栏，左栏为普通视频与高级视觉、右栏为普通声音；可用宽度足够时两栏等分伸缩、卡片填满所属栏且每横排左右两张，窄窗按 DOM 顺序降为单列，不设置固定卡宽且不得横向裁剪。紧凑高度、至少 `12px` 说明、三列频段、分类色与约 `290ms` 反馈、减少动态效果和三态规则保持不变。该决策覆盖下一条固定/目标 `270–290px` 卡宽口径，但不扩展实时话术幻化。
- 2026-08-24（历史布局，已由同日最新响应式澄清覆盖）：参数预览改为约 `880px` 居中的双主栏，左栏为普通视频与高级视觉、右栏为普通声音；普通卡目标宽度约 `270–290px`、说明文字至少 `12px`，固定视觉频段权重按三列展示。三个大分类从第 3.5 节参考图提取色板随机且不重复分配颜色，并在单次挂载期保持稳定；挂载后显示 `value` 实际变化时按所属分类色执行一次约 `290ms` 高亮，初始挂载、相同值、仅重排和普通重渲染不触发，减少动态效果时禁用。该决策覆盖下方同日“三分区同页三栏/删除闪烁”和旧 `720ms` 参数卡反馈口径，但不改写当时的实现和清理事实。
- 2026-08-24：随机插话触发间隔的 UI 契约改为 `0.5–60s`、默认 `8–13s`；IPC/Rust 字段保持 `interval_min_ms/interval_max_ms`、范围 `500–60000ms`。`ducking_attack_ms` 和 `ducking_release_ms` 的下限均改为 `0`，范围分别为 `0–1000ms`、`0–3000ms`，`0` 表示立即压低/立即恢复；默认值保持 `50/250ms`。
- 2026-08-24：播放状态契约由单源循环更新为当前进程内最多 `100` 项的本地有序播放池：一次多选按文件对话框返回顺序整批探测，只有全部成功才原子替换旧池；取消、超限或任一失败保留旧池，不提交部分成功项，也不提供追加、重排或删除。新增当前索引、池循环轮次和每次换源递增的播放代际；同一最终效果窗口执行 `A→B→…→A`，单项池自循环，绝对媒体时钟按当前媒体代际重建。播放池不跨进程持久化、不创建服务端媒体任务或生成版本队列。
- 2026-08-24（历史参数卡布局；其主页声音编辑口径已由 2026-08-25 最新只读随机化决策覆盖）：当时完成三类参数卡、无手柄分段进度和三态展示；当时两个具名声音配置区可编辑的口径不再属于当前验收。
- 2026-08-24：新增全量正式参数面板，并把频谱/高频音频扰动、扩展视频效果、波频/通道/空间调制、随机几何/抽象几何脸、同源画中画、切片、局部模糊、边缘填充、高光扰动和异步旋转接入实际 FFmpeg 输出；仍缺算法语义、第二输入、外部模型或 OCR 确认的字段明确标记为“正式需求待实现/待确认”。
- 2026-08-24：参考图参数全部从旧“研究/实验”口径转为正式产品需求；统一 `MediaEffectParams`/`AudioEffectParams`/`VideoEffectParams`/`AdvancedEffectParams` 和新默认值/校验 IPC，按“已实现并生效/已有模型待接入/正式需求但缺失”管理。未恢复独立报告链路，也不扩展到实时话术幻化、OBS/RTMP 或平台检测规避。
- 2026-08-24（历史声音编辑入口；已由 2026-08-25 最新只读随机化决策覆盖）：引入 MIT Signalsmith Stretch，在 FFmpeg 多支路 `amix` 后统一处理 `pitch_shift_semitones` 与 `formant_shift_percent`；普通变速继续使用 FFmpeg `atempo`。当时 UI 提供三项手动入口；当前三项只读展示并由预设/周期随机化更新，多支路共享音高/共振峰，初始化或处理失败保持旧轨/回退 WebView 原声。
- 2026-08-24：将基础视频 X/Y 空间偏移的自动周期预览附加幅度由 `±2 px` 收窄为 `±0.5 px`；手动调节范围、默认值以及挂件模块的独立画面偏移保持不变。
- 2026-08-24（历史插话选择模型，出口由 2026-08-25 当前方案覆盖）：建立固定/随机选择、最多四支路、下一段生效和有界释放边界；当前默认池、共享预设、环境声解析、WebView 短期处理缓存及原声回退以 2026-08-25 记录为准。

- 2026-08-20（历史口径，已由 2026-08-24 取代）：曾将当前视频参数网格收敛为基础项；现参考图参数已全部正式化，但未映射字段仍不得显示为已生效。
- 2026-08-21（历史参数卡反馈口径，已由 2026-08-24 当前后续决策覆盖）：新增参数卡和声音预设的真实输出反馈契约，相同值与未提交候选不触发；补充随机插话目录/文件、间隔、音量和 duck 参数范围，并明确 WebView/PortAudio 双出口、唯一 PortAudio 输出流混音、固定话术抢占、暂停停止清理与失败回退。本版本仍不包含实时话术幻化或研究能力。
- 2026-08-23：为随机插话目录增加 256 个文件、4096 字节单路径和 512 KiB 累计路径硬上限，目录扫描保持流式，避免超大目录集合进入播放快照。
- 2026-08-21（历史参数卡反馈口径，已由 2026-08-24 当前后续决策覆盖）：细化参数卡和声音预设反馈为单次约 `720ms`，颜色继承各 box 自身强调色且不得统一绿色；匹配 `prefers-reduced-motion: reduce` 时完全禁用，真实输出变化与相同值静止规则不变。
- 2026-08-21（历史参数卡反馈口径，已由 2026-08-24 当前后续决策覆盖）：补充普通声音参数状态与声音预设池两个区块容器的反馈事实源，分别比较实际输出参数集合和实际参与预设集合；相同集合、轮询、预热与失败不触发，时长、强调色和减少动态效果规则沿用卡片契约。
- 2026-08-21：区分 Scheduled 候选 `750ms` 提交 readiness、提交为 current 后的 `5130ms` 有界 PCM 保留容量和 `1500ms` 真停产门槛；扩大水位只覆盖 FFmpeg EOF 重启，不改变一次恢复、失败回退或当前产品范围。
- 2026-08-24：普通声音 `3–5s` 周期候选改用 `max(3, playback_speed + 1)`、1 秒初始突发的 CatchUp 和随 prepare 已用时间增长的动态覆盖水位，容量调整为 `5000ms + 130–750ms` 提交安全尾部；覆盖不足时保留 current。该最小速率/突发组合使用用户 MP4 的合法四支路连续 30 轮通过；即时插话的固定窗口 RealTime 策略、用户参数范围和真停产门槛不变。
- 2026-08-21（历史参数卡反馈口径，已由 2026-08-24 当前后续决策覆盖）：明确 Rust `PlaybackSnapshot`/DTO 的 `audio_stream_params`、`audio_stream_variants` 是已提交普通声音参数的唯一事实源；仅在 runtime=true 时展开有效支路并按每字段顺序无关的多重集合签名反馈，真实值或支路增删触发，等权重排、相同快照、预热、失败和非运行态不触发。本轮 Rust 字段不包含预设 ID/权重，预设身份不得从参数反推。
