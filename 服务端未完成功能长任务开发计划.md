# AutoLive 服务端未完成功能长任务开发计划

> 本文件是 [`长任务开发总计划.md`](./长任务开发总计划.md) 下的 Go 服务端专项执行子计划。商业化与生产就绪扩展还包含与服务端权限/业务 API 同阶段交付的 React 管理页；桌面端仍以总计划为准。执行时必须同时满足本文件和总计划，冲突时先按仓库事实更新文档，不得让实现迁就过期描述。
>
> 当前版本范围：不开发实时话术幻化，不恢复 speech-to-speech、VAD/ASR/LLM/TTS、候选话术音轨、旧媒体任务、服务端模型正文代理或请求预占链路。历史迁移中的相关表只保留审计兼容，不属于本计划的产品入口。

## 1. 文档状态

- 状态：执行中
- 版本：V2.91
- 审计日期：2026-08-22
- 审计分支：`dev-2.0`
- 审计对象：`backend/`、`backend/migrations/`、`admin-web/`、`deploy/`、[`接口契约/openapi.yaml`](./接口契约/openapi.yaml)、[`数据库迁移与密钥存储约束.md`](./数据库迁移与密钥存储约束.md)
- 上位依据：[`AGENTS.md`](./AGENTS.md)、[`产品需求文档.md`](./产品需求文档.md)、[`系统架构总览.md`](./系统架构总览.md)、[`管理系统架构.md`](./管理系统架构.md)、[`长任务开发总计划.md`](./长任务开发总计划.md)、[`开发约束/Go开发约束.md`](./开发约束/Go开发约束.md)
- 后续专项：[`tasks/prd-server-commercial-production-readiness.md`](./tasks/prd-server-commercial-production-readiness.md)；当前仅规划，未进入实现、OpenAPI、迁移或部署
- P0 接入专项：[`tasks/prd-douyin-desktop-control-plane-integration.md`](./tasks/prd-douyin-desktop-control-plane-integration.md)；当前仅规划，先于商业资源实施产品硬隔离、Profile 新鲜度、模型认证和固定契约/E2E
- 专项边界：服务端仍是业务与授权权威；执行范围包含 Go、PostgreSQL/Secret Store、服务端 Worker、供应商适配器、服务端 CI/运维和对应 React 管理页；不修改 Rust/Tauri 客户端业务
- 计划目标：把现有“开发期最小闭环”收口为可验证、可持久化、可安全发布的控制面，并补齐当前产品文档已要求但服务端尚未实现的功能。

## 2. 功能边界

### 2.1 本计划负责

1. 统一登录、Refresh Token 轮换、退出、会话撤销和管理员初始化。
2. 用户、设备、激活码的管理与状态闭环。
3. OpenAI-compatible 模型账号登记、密钥轮换、连通性测试、健康/冷却、租约和客户端调用摘要。
4. PostgreSQL 规范化持久化、幂等、事务、跨实例并发和 Secret Store。
5. 审计、结构化日志、指标、存活/就绪检查、配置、迁移和发布门禁。
6. OpenAPI、稳定错误码、Go DTO、管理端调用类型之间的契约一致性。
7. 后续专项中的管理员 RBAC、套餐/订单/订阅、微信支付、密码重置投递、激活码安全管理、可信制品、公共配置 Schema、生产治理和对应 React 管理页；这些能力须经用户另行授权后按拆分 PRD 实施。

### 2.2 本计划不负责

- 不上传、转发、分析或保存本地视频和完整音频。
- 不实现实时话术幻化，不启动 speech-to-speech/ASR/LLM/TTS Worker。
- 不恢复 `variant_tasks`、`variant_task_events`、`model_request_reservations` 的业务读写。
- 不实现服务端模型正文代理，不向桌面端下发长期 API Key 或完整号池。
- 不修改桌面播放器、FFmpeg/PortAudio 或当前媒体参数。
- 不删除或改写 0001～0010 历史迁移；新数据库变化只能新增前向迁移。
- 不在服务器构建源码；服务器只接收本地开发机或 CI 生成并验证的产物。

## 3. 审计结论

### 3.1 总结

当前 Go 服务端已经形成内存模式下的控制面最小闭环，也具备 PostgreSQL 会话、JSONB 快照、加密密钥存储和影子表写入能力；但还不能据此认定为完整生产控制面。最关键的差距如下：

1. PostgreSQL 仍保留 `control_plane_state` 快照过渡层，但 `APP_MODEL_READ_SOURCE=normalized` 已可在 PostgreSQL 上读取用户、设备、激活码、模型账号、租约、用量、模型测试、幂等和审计规范化表；当前通过事务级 advisory lock 保持服务层读改写串行，已提供显式快照回填工具，用户身份四条写路径、授权策略写路径、激活码创建/作废写路径、激活码核销+设备注册+会话绑定写路径、心跳+会话绑定写路径、设备禁用/解绑+租约释放+会话撤销写路径、租约创建/续期/释放/管理员回收写路径、客户端调用摘要写路径、模型账号创建/编辑/禁用写路径以及审计追加写路径已通过最小领域 Repository 直接写规范化表与幂等记录；模型账号创建要求 SecretStore 通过 `TransactionalSecretWriter` 在同一 SQL 事务写入密文，租约创建在同一事务完成用户/设备归属、授权 allowlist、服务端日额度、账号优先级/日额度/活动租约并发和 SecretStore 可用性检查后插入租约；激活码只保存哈希且重放不重新暴露明文；用户/设备/调用摘要/审计/模型账号/租约列表已增加 SQL `COUNT + LIMIT/OFFSET` 第一切片，租约列表已增加状态/供应商/模型/用户/设备/账号白名单筛选和排序第一切片，审计列表已增加结果/操作者/设备/动作/目标/request_id/时间窗口白名单筛选和排序第一切片，租约详情/回收第一切片已接通；审计 Outbox 已接入，且 normalized 激活/心跳/设备禁用/解绑/租约创建/续租/释放/管理员回收的业务状态、会话绑定或撤销、幂等记录和成功审计事件可在同一 SQL 事务持久化；密钥轮换/Secret 故障补偿、客户端调用摘要和其他领域审计同事务、完整按领域 Repository 和真实回填执行证据仍未完成，客户端调用摘要当前只接受 `client_reported`，不是供应商权威账单。迁移 0020 新增 `normalized_backfill_state` 显式完成标记，迁移初始为 `pending`；`autolive-backfill-normalized` 只有在快照复制和逐表校验成功的同一事务内才切换为 `completed`，normalized 读路径和写事务均拒绝缺失/未完成标记，现有数量覆盖检查继续拒绝不一致投影，历史快照表不删除。
2. 模型账号连通性测试已限制 `http:80`/`https:443`、私网/回环/链路本地/云元数据地址、DNS 解析结果、实际拨号和重定向；默认拒绝明文 HTTP，只有 `APP_ALLOW_INSECURE_HTTP=true` 才在受控开发/联调进程中放行；生产公共地址和模型目标仍强制 HTTPS，真实发布证书和网络联调仍待完成。
3. HTTP 集成测试已改用可控测试时钟并补齐状态断言，当前 `go test ./...` 全绿；Race Detector 仍需在支持的平台验证。
4. 心跳 DTO 已接收 `current_media_name` 和 `playback_state`，并已保存到 `DeviceSummary`、JSONB 状态、PostgreSQL 设备影子行和管理端列表；在线/离线阈值由服务端时间派生，设备详情接口和管理端详情抽屉已接通。
5. 用户编辑、密码重置和必要的启用/禁用状态转换已接入并会撤销相关用户会话；按用户查询设备和激活码脱敏核销详情已接通；用户授权摘要现在同时显示模型 allowlist 和服务端记录用量门禁，管理员可幂等更新策略，租约申请会校验 allowlist/门禁；供应商权威硬额度、未来用量预占和跨实例额度原子性仍未完成。
6. 模型账号已支持独立密钥轮换入口：新密钥会先走受限 `/models` 连通性校验，通过后写入 Secret Store，并以幂等键记录测试结果；每次轮换使用唯一 staged reference，竞争失败方不会删除成功方密钥；不同幂等键在提交前会校验 SecretRef 仍是探测时的旧值，避免后到请求覆盖已提交的新密钥。普通连通性测试在 normalized PostgreSQL 走 `ModelPoolTestRepository` 的准备/探测/提交三段边界，网络探测不持有事务锁，结果与 cooldown、幂等和可选审计同事务持久化，缺少领域能力时 fail-closed。新增按 `model-account/%/rotation_%` 前缀、更新时间和当前账号 SecretRef 保护集合执行的有界 staged secret 回收，固定保留 1 小时，进程崩溃后由后台调度恢复；跨 Secret Store 与业务 Repository 的真正原子提交、旧密钥故障补偿和真实 PostgreSQL 故障注入仍未完成。失败测试现在进入带 `cooldown_until` 的有界冷却，成功测试可恢复，新的 UTC 日会解除自动 `exhausted`。新增可配置的后台健康探测调度，具备每轮账号上限、并发上限、请求超时、取消等待、失败计数、确定性抖动退避；normalized PostgreSQL 通过专用健康 Reader 在 SQL 层先过滤 disabled、缺失 Secret、cooldown 和日额度耗尽账号，再按上限返回候选，避免首批分页被不可探测账号占满；PostgreSQL 使用会话级 advisory lock 防止多实例重复探测，Memory/单进程保留无锁回退；成功/失败/不健康/跳过/取消/耗时通过低基数 Prometheus 事件指标记录，进程关闭时等待探测退出。用户策略门禁只依据服务端已接收的客户端上报摘要，不能宣称为供应商权威计费、未来用量预占或严格供应商额度。
7. 审计中间件现在为已认证写请求记录目标类型/资源 ID、结果、HTTP 状态码和统一错误码，登录成功/失败也会记录；0015 已持久化这些字段，敏感请求体仍不入库。关键 API 写请求现在先缓冲响应，审计持久化失败统一返回 `503/AUDIT_UNAVAILABLE`，不向客户端提交原业务成功响应；管理端 `apiClient` 与桌面端控制面客户端在携带同一 `Idempotency-Key` 时各自动重试一次，避免未知响应下生成新业务写入；新增会话、孤立设备绑定、staged secret、幂等记录、模型测试结果和审计日志的有界批量清理接口，并接入按 TTL 配置运行的后台调度器：Memory 走确定性旧到新删除，normalized PostgreSQL 使用参数化 `CTE + LIMIT + FOR UPDATE SKIP LOCKED`，并在同一事务同步快照；会话绑定通过 0018 的 `device_bound_at` 和固定 5 分钟保护窗口提供崩溃后可恢复补偿，staged secret 只删除过期且不在当前账号引用集合中的 rotation 前缀记录，0017/0018/0021 已补齐首轮清理与管理筛选索引，0019 Outbox 提供审计 durable enqueue/retry，激活/心跳成功审计已进入业务事务，其他业务领域审计同事务、归档和提交未知结果仍未完成。
8. `/api/v1/health`/`/api/v1/livez` 只证明进程能响应；`/api/v1/readyz` 已检查管理员初始化、Repository 和 Secret Store，数据库迁移由启动阶段校验，HTTP 请求预算和 PostgreSQL Repository/规范化回填/SQL Session Store/Secret Store 的有界数据库预算已接入；迁移命令新增独立 `APP_MIGRATION_TIMEOUT`，并在成功/失败结构化日志记录 `duration_ms` 和预算；服务启动的 PostgreSQL Ping/版本检查/管理员引导使用 `APP_REQUEST_TIMEOUT`；登录、Refresh、模型测试和密钥轮换已按客户端 IP 做进程内有界限流并返回结构化 `429/RATE_LIMITED`，限流命中按固定策略名计入专用指标。新增基于官方 Prometheus Go 客户端的 `/metrics`，提供低基数 HTTP 请求量/延迟/在途请求、限流命中、审计写失败、PostgreSQL 连接池、模型账号状态、活动租约、UTC 日用量、已配置用户授权策略、健康探测事件，以及保留清理运行/数据集结果/删除数量指标；健康探测与保留清理现在区分 `timeout`/`cancelled`，避免把预算耗尽误报为主动取消；`APP_SHUTDOWN_TIMEOUT` 默认 45 秒、Compose `AUTOLIVE_STOP_GRACE_PERIOD` 默认 60 秒，CI 已增加真实 server 进程 SIGTERM/readyz/clean-exit 冒烟；迁移命令可选输出原子 text exposition 指标文件，未配置时保持原行为，已配置但发布失败会阻止成功退出；在途事务、指标文件真实 collector 消费/权限与重启故障矩阵、供应商额度和跨实例租约指标仍待补齐。
9. 大部分列表接口仍先加载完整快照再在内存分页；用户、设备、按用户设备、激活码、调用摘要、审计、模型账号和租约列表已在规范化 PostgreSQL 读源使用有界 SQL 分页，激活码列表只返回脱敏字段并按服务端时钟派生过期状态，租约列表和审计列表的筛选/排序已在 SQL 层使用固定白名单，模型账号页同时从租约、用量和最近测试结果派生展示摘要，管理员租约页提供脱敏列表、单租约详情和幂等回收，快照/内存模式保留兼容回退。会话过期/撤销、孤立设备绑定、staged secret、幂等记录、测试结果、审计已有可取消、1～1000 批次上限的清理接口，并由 `APP_RETENTION_*` TTL/间隔/超时配置驱动后台调度；0017/0018/0021 已补齐首轮清理/筛选索引，容量告警、归档和真实数据量复核仍待完成，数据量增长后仍存在无界内存、JSONB 体积和事务时延风险。
10. 当前业务容器仍使用内部 HTTP hop，数据库示例连接使用 `sslmode=disable`；本轮新增生产 HTTPS 配置校验、回环端口、宿主机反向代理模板和 Tauri HTTPS capability。真实证书、DNS、Nginx reload 和线上 PostgreSQL HTTPS 冒烟仍待发布环境验证。
11. 模型账号通用 `PATCH status=disabled` 已复用活动租约冲突检查；BE-025 的状态转换不变量已有回归测试，后续仍需将更多健康/额度状态转移集中到领域命令。
12. Logout、设备绑定、用户编辑/禁用/密码重置、设备禁用/解绑的 Session Store 写入错误已进入失败响应；PostgreSQL 路径的激活/心跳现在通过 `TransactionalSessionBinder` 将会话绑定、业务状态、幂等和规范化/快照写入放入同一事务，Memory/兼容实现保留有界补偿回退；0018 的 `device_bound_at` 和孤立绑定清理继续覆盖进程崩溃遗留，真实 PostgreSQL 连接断开/故障注入和审计 Outbox 仍待完成，设备禁用/解绑现在会撤销设备会话并释放活动租约。
13. OpenAPI 行为漂移本轮已继续收口：解绑设备改用不含用户归属的独立响应，模型账号创建 `status` 已写入契约，请求 ID 规则已明确为“合法值校验后透传，否则服务端生成”；契约测试现在还会从 Go `NewError` 调用解析 HTTP 状态并与错误码文档逐项比较，并按显式 Go DTO 映射校验字段集合、required/nullable 与 `omitempty` 语义。该门禁已发现并修复 Logout `refresh_token`、激活码 `max_devices`、Direct LLM 可选 token/延迟字段及响应 schema 名漂移；共享错误响应组件现在必须有可解析示例，运行时矩阵覆盖 400/401/403/404/405/408/409/429/500/502/503/504；40 个 OpenAPI HTTP 操作已显式声明通用 405/408/500 response，并由操作级契约测试逐项锁定；管理端生成类型由 `openapi-typescript --check` 和 CI `frontend-contract` 作业锁定。真实运行时状态与桌面端更广泛的生成类型消费仍需继续收口。
14. 模块语言版本仍保持 `go 1.22`，但本地模块工具链、Docker builder 和 CI 已固定为 Go `1.26.6`；`govulncheck@v1.7.0` 通过，Go `1.26.5` 的标准库漏洞不再作为构建基线。[Go 官方发布与支持策略](https://go.dev/doc/devel/release)
15. BE-009 已完成一个可执行的发布门禁切片：生产配置必须声明 `APP_PUBLIC_BASE_URL=https://...`、禁止 `APP_ALLOW_INSECURE_HTTP`，Compose 管理端 HTTP hop 只绑定回环地址，宿主机反向代理模板强制 80→443 跳转和 TLS，Tauri capability 保留明确 HTTPS origin。BE-023 现已固定后端 Docker 基础镜像 manifest digest，CI 构建同时导出 Docker/OCI 产物并启用 BuildKit SBOM/Provenance，记录镜像内容摘要并上传 artifact；真实证书、DNS、Nginx reload、签名和 PostgreSQL HTTPS 冒烟仍待发布环境验证。

> V2.51 口径修正：上文历史审计条目中“密钥轮换/其他领域审计同事务仍未完成”描述保留为当时记录；截至本版本，normalized PostgreSQL 的模型账号创建/编辑/禁用、客户端调用摘要和 Secret 轮换成功审计已分别进入领域事务或 `audit_outbox`，Secret 轮换通过 `ModelPoolSecretRotator` 与 `TransactionalSecretWriter.PutTx` 在同一事务切换引用、写入探测结果、删除旧密文并记录幂等事实。尚未关闭的是 Memory/快照 staged fallback、真实连接断开/提交未知结果、Secret 故障注入、旧密钥归档、容量告警和发布环境证据。

### 3.2 已完成能力与成熟度

| 能力 | 当前证据 | 成熟度结论 |
| --- | --- | --- |
| 登录、Refresh 轮换、Logout | `backend/internal/httpapi/auth.go`、`backend/internal/store/session_store.go` | 管理员首次引导会持久化 bcrypt 哈希，后续登录使用存储凭证；全局会话撤销和过期清理待补 |
| 用户列表、创建、禁用、编辑、密码重置、授权策略、认证读路径 | `backend/internal/service/controlplane.go`、`backend/internal/httpapi/controlplane.go`、`backend/internal/store/postgres_user_repository.go`、`backend/internal/store/postgres_admin_repository.go`、`backend/internal/store/postgres_user_authorization_repository.go`、迁移 0016 | 用户状态、最后管理员保护、授权摘要、模型 allowlist、服务端记录用量门禁和用户列表分页第一切片已完成；normalized 模式的用户写入、按用户名凭证读取、按 ID 用户读取、管理员初始化/就绪/密码轮换及授权策略均通过领域 Repository 直接访问规范化表，Refresh 不再回读快照；供应商权威硬额度、未来预占、语义化审计未完成 |
| 设备激活、列表、详情、心跳、禁用、解绑 | 同上、`backend/internal/store/postgres_repository.go`、`backend/internal/store/postgres_device_reader.go`、`backend/internal/store/postgres_device_activation_repository.go`、`backend/internal/store/postgres_device_heartbeat_repository.go`、`backend/internal/store/postgres_device_lifecycle_repository.go` | normalized 设备详情和客户端 profile 已通过 `DeviceReader` 直接读取规范化表；激活路径已在单事务内完成会话锁定、设备注册/重激活、激活码核销和幂等回放；心跳已在单事务写入设备指标、幂等和会话绑定；禁用/解绑已在单事务释放活动租约、撤销设备会话并更新设备状态，HTTP normalized 路径不再重复执行会话撤销；真实 PostgreSQL 故障注入和重激活审计未完成 |
| 激活码创建、分页列表、作废、一次核销 | `backend/internal/service/controlplane.go`、`backend/internal/store/postgres_activation_repository.go`、`backend/internal/store/postgres_activation_reader.go`、`backend/internal/store/postgres_device_activation_repository.go`、迁移 0011 | normalized 模式的创建/作废/核销均在同事务写入哈希、前缀、状态、核销归属和幂等记录，激活成功审计 Outbox 也在同事务入队，列表使用有界 `COUNT + LIMIT/OFFSET` SQL，过期状态按服务端时钟派生且不返回明文，首次创建响应才返回明文，重放不重新暴露；SQLMock 与 PostgreSQL 16 集成测试已覆盖分页参数、脱敏、过期/作废和核销提交边界；重启恢复、提交未知结果和语义化审计仍未完成 |
| 模型账号创建、编辑、禁用、连通性测试、密钥轮换 | `backend/internal/service/controlplane.go`、`backend/internal/service/model_pool_probe.go`、`backend/internal/service/model_pool_health_scheduler.go`、`backend/internal/store/postgres_model_pool_repository.go`、`backend/internal/store/postgres_model_pool_test_repository.go`、`backend/internal/store/postgres_model_secret_rotation_repository.go`、`backend/internal/store/sql_secret_store.go`、`backend/internal/store/secret_retention.go`、`backend/internal/httpapi/controlplane.go` | 基础闭环、SSRF 出站校验、“验证后切换”的幂等入口、有界 cooldown/UTC 日切恢复、normalized SQL 分页摘要、模型账号创建/编辑/禁用事务、连通性测试的“短事务准备→网络探测→短事务结果/cooldown/幂等/审计提交”、normalized Secret 轮换事务、带 PostgreSQL advisory lock 的后台探测调度和过期 staged secret 回收已完成；normalized 测试路径缺少领域 Repository 时 fail-closed，SecretRef 仅用于后续受限探测且不进入 API；真实连接故障、提交未知结果、旧密钥归档和跨存储补偿仍未完成 |
| 租约申请、续租、释放、管理员详情/回收 | 同上、`backend/internal/store/postgres_model_lease_repository.go` | normalized PostgreSQL 已将租约创建、续租、客户端释放和管理员回收拆为事务 Repository；创建锁定用户/设备/策略/账号，执行 allowlist、服务端日额度、账号优先级/日额度/活动租约并发和 SecretStore 可用性检查，续租/释放/回收保持行锁、过期派生、归属校验、账号状态检查和幂等回放；供应商硬额度、跨实例压力/故障和真实 PostgreSQL 证据未完成 |
| 客户端调用摘要和管理端用量列表 | 同上、迁移 0010、`backend/internal/store/postgres_repository.go`、`backend/internal/store/postgres_model_usage_repository.go` | normalized PostgreSQL 已将调用摘要写入、`(lease_id, client_call_id)` 幂等核对、用户记录用量门禁和账号日额度状态更新放入事务 Repository，并保留 `client_reported` 来源标记；管理端 SQL 分页第一切片已完成，供应商权威账单/硬额度仍不属于当前能力 |
| 审计完整性与写入保护 | `backend/internal/httpapi/audit.go`、`backend/internal/service/controlplane.go`、`backend/internal/store/postgres_audit_outbox_repository.go`、迁移 0015/0019 | 已记录主体/目标/结果/状态码/错误码/request_id，normalized PostgreSQL 通过 Outbox 先持久化脱敏事件并幂等投递，激活/心跳成功事件在业务事务内入队，敏感正文不入库；关键 API 写响应在审计持久化失败时 fail-closed 为 `503/AUDIT_UNAVAILABLE`，并保留指标与清理 | 其他业务状态与审计同事务、重复投递/提交未知结果真实验证和归档待补 |
| AES-256-GCM Secret Store | `backend/internal/store/sql_secret_store.go` | 基础加密存储已完成；与业务事务原子提交、轮换和故障补偿待补 |
| PostgreSQL 会话/密钥清理 | `backend/internal/store/session_store.go`、`backend/internal/store/retention_postgres.go`、`backend/internal/store/secret_retention.go`、`backend/internal/service/retention_cleanup_scheduler.go`、迁移 0018 | 可重启恢复；按用户/设备批量撤销已完成；会话过期/撤销、超过 5 分钟保护窗口的孤立设备绑定和超过 1 小时的 staged secret 已有参数化、有界、可取消清理接口，并由后台调度和固定低基数清理指标驱动；激活/心跳 SQL 会话绑定已与业务事务同成同败，Secret Store 原子提交、真实故障注入和容量告警待补 |
 | PostgreSQL 业务数据 | `backend/internal/store/postgres_repository.go`、`backend/internal/store/postgres_user_repository.go`、`backend/internal/store/postgres_user_authorization_repository.go`、`backend/internal/store/postgres_activation_repository.go`、`backend/internal/store/postgres_device_activation_repository.go`、`backend/internal/store/postgres_device_heartbeat_repository.go`、`backend/internal/store/postgres_device_lifecycle_repository.go`、`backend/internal/store/postgres_model_pool_repository.go`、`backend/internal/store/postgres_model_secret_rotation_repository.go`、`backend/internal/store/postgres_model_lease_repository.go`、`backend/internal/store/postgres_model_usage_repository.go`、`backend/internal/store/postgres_audit_repository.go`、`backend/internal/store/postgres_audit_outbox_repository.go`、迁移 0011～0022 | 快照默认、规范化读源和用户/设备/调用摘要/审计/模型账号/租约 SQL 分页第一切片已接通并受迁移覆盖检查和 0020 回填完成标记保护；用户身份/授权策略、激活码创建/作废、激活核销+设备+会话绑定、心跳+设备+会话绑定、设备禁用/解绑+租约释放+会话撤销、租约续期/释放/管理员回收、客户端调用摘要写入、模型账号创建/编辑/禁用、normalized Secret 轮换以及审计追加写入已按领域边界直接落规范化表并验证幂等、唯一冲突、用户归属、过期/状态约束、软额度、fail-closed 和明文不落库，激活/心跳/租约/客户端调用摘要/Secret 轮换成功审计 Outbox 与业务事务同提交，租约和审计查询支持固定白名单筛选/排序，模型账号页派生活动租约、UTC 日用量和最近测试结果，0017/0019/0021 已补齐首轮清理/筛选/Outbox 索引，0022 已补齐激活码容量与已绑定计数约束；真实故障矩阵、Secret/旧密钥补偿、完整领域退出和真实回填执行证据仍未完成 |
| 请求 ID、结构化请求日志、HTTP 超时和优雅关闭 | `backend/internal/httpapi/router.go`、`backend/cmd/server/main.go`、`backend/cmd/migrate/main.go`、`backend/internal/httpapi/timeout.go`、`backend/internal/httpapi/metrics.go`、`backend/internal/service/*scheduler.go` | 基础能力已完成；已增加 live/ready、请求级处理预算、PostgreSQL Repository/回填/SQL Session/Secret Store 有界预算、迁移成功/失败耗时日志与可选原子 text exposition、审计结果/错误码、用户/设备/调用摘要/审计分页和 `/metrics` HTTP/连接池/模型账号/租约/日用量/用户策略/健康探测/清理 timeout-cancelled 细分，异常退出路径已显式关闭存储；CI 已有 server readyz→SIGTERM→clean-exit 冒烟，在途事务/数据库关闭故障矩阵、指标文件真实 collector 消费/权限与重启验证、供应商额度和跨实例租约指标待补 |
| OpenAPI 路由覆盖 | `接口契约/openapi.yaml`、`backend/internal/httpapi/contract_test.go`、`admin-web/src/api/openapi.generated.ts`、`desktop/ui/src/api/openapi.generated.ts` 与当前路由 | 路径/方法、本地 `$ref`、Go 错误码文档存在性、HTTP 状态一致性、共享错误示例、操作级 405/408/500、显式 DTO required/nullable 和管理端/桌面端 `openapi-typescript --check` 生成一致性均已有自动门禁；真实运行时状态和未接入生成类型的边界仍待继续收口 |

### 3.3 本次验证基线

| 命令 | 结果 | 说明 |
| --- | --- | --- |
| `go env GOVERSION` | 通过 | `go1.26.6`；模块语言版本仍为 `go 1.22` |
| `cd backend && go test ./...` | 通过 | 采用注入测试时钟并补齐响应状态前置断言后，全量 Go 测试通过 |
| `cd backend && go vet ./...` | 通过 | 无输出 |
| `cd backend && go build ./...` | 通过 | 本地构建通过 |
| `cd backend && GOTOOLCHAIN=go1.26.6+auto go run golang.org/x/vuln/cmd/govulncheck@v1.7.0 ./...` | 通过 | Go 1.26.6；CI 固定同一工具链并使用 `govulncheck@v1.7.0` |
| `cd backend && go test ./internal/service ./internal/store ./internal/httpapi` | 通过 | 覆盖设备禁用释放租约、按用户/设备撤销会话、用户密码重置和管理端接口回归 |
| `cd backend && go test ./internal/service -run 'TestModelPoolHealthProbeScheduler' -count=1` | 通过 | 健康探测调度的并发/账号上限、失败退避、不健康结果、取消、生命周期和固定遥测标签回归 |
| `cd backend && go test ./internal/store -run 'TestPostgresRepositoryTryAdvisoryLock' -count=1` | 通过 | PostgreSQL 会话级 advisory lock 获取、锁忙跳过、释放归还连接和重复释放幂等回归 |
| `cd backend && go test ./internal/service -run 'TestModelPoolHealthProbeSchedulerSkipsWhenDistributedLockIsBusy' -count=1` | 通过 | 调度器锁忙时不探测、记录 `distributed_lock` 固定跳过标签并返回成功 |
| `cd backend && go test ./internal/service -run 'TestRetentionCleanupScheduler|TestCleanupStagedSecrets' -count=1` | 通过 | TTL cutoff、孤立设备绑定 5 分钟保护窗口、staged secret 1 小时保护窗口、批次、逐数据集隔离、snapshot 边界跳过、取消、生命周期和清理遥测回归 |
| `cd backend && go test ./internal/store ./internal/httpapi -run 'RetentionCleanup|DeviceBinding|StagedSecret' -count=1` | 通过 | Memory、会话 SQL、SQL Secret Store、normalized PostgreSQL 的过期/孤立绑定/staged secret cutoff、保护引用、批次上限、取消、SQL 错误和快照同步/拒绝回归 |
| `cd backend && go test ./internal/store ./internal/service -run 'SessionBinding|Transactional' -count=1` | 通过 | PostgreSQL Repository 会话锁定/用户归属/设备冲突、同事务绑定与状态提交/回滚，以及服务层激活/心跳 runner 回归 |
| `cd backend && go test ./internal/store -run 'TestPostgresRepository(ActivateDevice|RecordHeartbeat).*' -count=1` | 通过 | 激活/心跳业务事务内写入审计 Outbox、request_id 去重参数和 Outbox 写失败回滚 |
| `cd backend && go test ./internal/service -run 'TestNormalizedSessionBindingPassesSuccessAuditIntoTransaction' -count=1` | 通过 | normalized 服务层将激活/心跳成功审计事件传递到事务 Repository |
| `cd backend && go test ./internal/store -run 'TestPostgresRepository.*ModelLease.*' -count=1` | 通过 | 租约创建、续租、客户端释放和管理员回收事务内写入审计 Outbox |
| `cd backend && go test ./internal/service -run 'TestNormalizedModelLeasePassesSuccessAuditIntoTransaction' -count=1` | 通过 | normalized 服务层将四类租约成功审计事件传递到事务 Repository |
| `cd backend && go test ./internal/store -run 'TestPostgresRepositoryRecordDirectLLMCallUsesNormalizedTransaction' -count=1` | 通过 | 调用摘要、账号额度状态、幂等记录和成功审计 Outbox 同事务提交 |
| `cd backend && go test ./internal/service -run 'TestModelUsagePassesSuccessAuditIntoTransaction' -count=1` | 通过 | normalized 服务层将客户端调用摘要成功审计事件传递到事务 Repository |
| `cd backend && go test ./internal/store -run 'TestPostgresRepositoryRotateModelPoolAccountSecretUsesSharedTransaction' -count=1` | 通过 | normalized 密钥轮换将候选密文、账号引用、探测结果、旧密文和成功审计 Outbox 放入同一 SQL 事务 |
| `cd backend && go test ./internal/service -run 'TestRotateModelPoolAccountSecretUsesNormalizedTransactionalRepository' -count=1` | 通过 | 服务层在 normalized 读源下将密钥轮换路由到事务型 SecretRotator，并传递成功审计事件 |
| `cd backend && go test -count=1 -tags=postgres_integration ./internal/store -run 'TestPostgresNormalizedSecretRotation'` | 通过（本地 Docker PostgreSQL 16） | 验证 Secret 轮换提交后重启读取、幂等重放、旧密文删除、审计 Outbox 持久化和事务失败回滚 |
| `cd backend && go test ./internal/store ./internal/service ./internal/httpapi -run 'ModelPool.*Test|ConnectivityTestUsesNormalized' -count=1` | 通过 | SQLMock 与服务层验证模型账号测试采用准备/探测/提交领域边界、幂等键和成功审计传递 |
| `cd backend && go test -count=1 -tags=postgres_integration ./internal/store -run 'TestPostgresNormalizedModelPoolTestPersistsResultAndReplay'` | 通过（本地 Docker PostgreSQL 16） | 验证规范化模型测试结果持久化、失败 cooldown、SecretRef 读取边界和幂等回放 |
| `cd backend && go test -count=1 -tags=postgres_integration ./internal/store -run 'TestPostgresNormalizedActivationRedeemIsSingleWinnerAcrossRepositories'` | 通过（本地 Docker PostgreSQL 16） | 两个独立 Repository 并发核销同一激活码只有一个成功，失败者得到 `ErrActivationCodeAlreadyUsed`，设备/会话/激活码终态一致 |
| `cd backend && go test -count=1 -tags=postgres_integration ./internal/store -run 'TestPostgresNormalizedModelLeaseConcurrencyHasSingleWinnerAcrossRepositories'` | 通过（本地 Docker PostgreSQL 16） | 同一账号 `concurrency_limit=1` 下并发申请租约只有一个成功，最终活动租约数为 1 |
| `cd backend && go test -count=1 -v -tags=postgres_integration ./internal/store -run 'TestPostgresNormalizedModelDailyQuotaRecoversAfterUTCDayRollover'` | 通过（本地 Docker PostgreSQL 16） | UTC 日切后账号从 `exhausted` 恢复、日用量窗口归零并可重新申请租约 |
| `cd backend && go test ./internal/service -run 'TestModelPoolConnectivityTestRequiresExplicitInsecureHTTPOptIn' -count=1` | 通过 | 验证模型连通性测试默认拒绝明文 HTTP，错误码稳定且未发生出站连接 |
| `cd backend && go test ./internal/httpapi -run 'TestOpenAPI' -count=1` | 通过 | 真实 Router 路径/方法与 OpenAPI 本地 `$ref` 完整性门禁 |
| `cd backend && go test ./internal/httpapi -run 'TestOpenAPIMirroredDTOFieldsMatchGoJSONTags' -count=1` | 通过 | 显式 Go DTO 与 OpenAPI properties、required、omitempty 和 nullable 语义门禁 |
| `cd backend && go test ./internal/httpapi -run 'TestOpenAPIErrorResponseExamplesAndRuntimeStatusMatrix' -count=1` | 通过 | OpenAPI 错误响应示例、共享错误码存在性和 400/401/403/404/405/408/409/429/500/502/503/504 运行时矩阵 |
| `cd backend && go test ./internal/httpapi -run 'TestOpenAPIOperationsDeclareCommonRuntimeErrorStatuses' -count=1` | 通过 | 40 个 OpenAPI HTTP 操作逐项声明 405/408/500 通用错误 response，并锁定共享 component 引用 |
| `cd backend && go test ./internal/httpapi -run 'TestAuditTarget|TestMetrics' -count=1` | 通过 | 登录审计错误码、资源 ID 提取、低基数 HTTP 指标和 PostgreSQL 连接池指标回归 |
| `cd admin-web && pnpm api:check` | 通过 | `openapi-typescript@7.13.0` 生成的 `src/api/openapi.generated.ts` 与 OpenAPI 契约一致 |
| `cd desktop/ui && pnpm api:check` | 通过 | 桌面端生成的 `src/api/openapi.generated.ts` 与同一 OpenAPI 契约一致 |
| `cd admin-web && pnpm typecheck && pnpm test && pnpm build` | 通过 | 管理端生成类型适配、统一 HTTP 错误 DTO、路由和 UI 构建回归 |
| `cd desktop/ui && pnpm typecheck && pnpm test && pnpm build` | 通过 | 桌面端生成类型适配、HashRouter、控制面 JSON/error 契约和媒体 UI 回归；Node 测试 177/177 通过 |
| `cd admin-web && pnpm audit --audit-level high` | 通过 | 管理端依赖无已知高危漏洞 |
| `cd desktop/ui && pnpm audit --audit-level high` | 通过 | Vite 6.4.3 升级后桌面端依赖无已知高危漏洞 |
| `npx --yes js-yaml .github/workflows/backend-quality.yml` | 通过 | 后端 PostgreSQL、镜像、优雅关闭和前端契约 CI 工作流 YAML 可解析 |
| `cd backend && go test ./internal/httpapi -run 'TestAuditFailure|TestGoErrorCodesMatchErrorCodeDocument|TestOpenAPI' -count=1` | 通过 | 审计失败 fail-closed、`AUDIT_UNAVAILABLE` 错误码文档一致性和 OpenAPI 路由/引用门禁回归 |
| `cd backend && go test ./internal/service -run 'TestModelPoolHealthProbeSchedulerTelemetryRecords(Timeout|Cancellation)|TestRetentionCleanupSchedulerRecordsTimeout' -count=1` | 通过 | 健康探测与保留清理区分 `timeout`/`cancelled`，超时不再被归类为主动取消 |
| `cd backend && go test ./internal/httpapi -run 'TestMetricsExpose(HealthProbe|RetentionCleanup)Telemetry' -count=1` | 通过 | `/metrics` 接受固定 `timeout` 标签并继续拒绝未知/高基数标签 |
| `cd backend && go test ./cmd/migrate ./cmd/server -count=1` | 通过 | 迁移命令预算校验与服务器关闭/HTTP 超时配置回归 |
| `node --test tools/check-backend-release.test.mjs` | 通过 | 后端 Docker 基础镜像 digest、Docker/OCI 输出、SBOM/Provenance 和 metadata artifact 静态门禁 |
| `node tools/check-backend-release.mjs --dockerfile backend/Dockerfile --workflow .github/workflows/backend-quality.yml` | 通过 | 当前 Dockerfile 与 CI 发布声明满足可复现性检查 |
| CI `Graceful shutdown smoke` | 待 Linux/amd64 CI | 构建 server 后执行 readyz→SIGTERM→clean-exit；本机未模拟 Linux 容器运行环境 |
| `cd backend && go test ./internal/httpapi -run 'TestGoErrorCodesMatchErrorCodeDocument' -count=1` | 通过 | Go `NewError` HTTP 状态与错误码文档逐项一致，防止状态漂移 |
| `cd backend && go test ./internal/store -run 'TestPostgresRepositoryCreateUser' -count=1` | 通过 | normalized 用户 Repository 的幂等创建、重复请求回放、语义冲突和用户名唯一约束错误映射 |
| `cd backend && go test ./internal/service -run 'TestCreateUserUsesNormalizedUserRepository' -count=1` | 通过 | normalized 读源下服务层将用户创建路由到最小领域 Repository，并保持幂等键/指纹/密码哈希边界 |
| `cd backend && go test ./internal/store -run 'TestPostgresRepository(UpdateUser|ResetUserPassword)' -count=1` | 通过 | normalized 用户编辑/密码重置的 advisory lock、幂等事务、用户名唯一冲突和最后管理员保护 |
| `cd backend && go test ./internal/service -run 'TestUserMutationsUseNormalizedUserRepositoryWriter' -count=1` | 通过 | 服务层在 normalized 读源下将用户编辑和密码重置路由到领域 Repository，密码只以哈希进入边界 |
| `cd backend && go test ./internal/store -run 'TestPostgresRepositoryDisableUser' -count=1` | 通过 | normalized 用户禁用的 advisory lock、专用本地管理员错误、最后管理员保护和幂等状态写入 |
| `cd backend && go test ./internal/store -run 'TestPostgresRepositoryUpdateUserAuthorization' -count=1` | 通过 | normalized 用户授权策略的用户锁、幂等回放/冲突、JSONB 策略写入和同事务提交 |
| `cd backend && go test ./internal/service -run 'TestUpdateUserAuthorizationUsesNormalizedRepositoryWriter' -count=1` | 通过 | 服务层在 normalized 读源下将 allowlist/软额度策略写入路由到独立领域 Repository |
| `cd backend && go test ./internal/store -run 'TestPostgresRepositoryCreateActivationCode' -count=1` | 通过 | normalized 激活码创建的哈希落库、幂等回放/冲突和明文仅首次响应边界 |
| `cd backend && go test ./internal/service -run 'TestCreateActivationCodeUsesNormalizedRepositoryWriter' -count=1` | 通过 | 服务层在 normalized 读源下将激活码创建路由到领域 Repository，明文不进入持久化记录 |
| `cd backend && go test ./internal/store -run 'TestPostgresRepositoryRevokeActivationCode' -count=1` | 通过 | normalized 激活码作废的状态校验、幂等事务和明文清除边界 |
| `cd backend && go test ./internal/service -run 'TestRevokeActivationCodeUsesNormalizedRepositoryWriter' -count=1` | 通过 | 服务层在 normalized 读源下将激活码作废路由到领域 Repository |
| `admin-web`: `node --test src/api/client-contract.test.mjs` | 通过 | 管理端 API 客户端只对带幂等键的写请求按同一 key 重试一次 |
| `desktop/ui`: `node --test src/control-plane-client-contract.test.mjs` | 通过 | 桌面端控制面客户端保留同一幂等键处理 `503/AUDIT_UNAVAILABLE` |
| `cd backend && go test ./internal/store ./internal/httpapi -run 'OperationalMetrics|MetricsExposeLowCardinality' -count=1` | 通过 | Memory 与 normalized PostgreSQL 聚合查询，以及 `/metrics` 模型账号/活动租约/日用量/用户策略低基数输出回归 |
| `cd backend && go test ./internal/httpapi -run 'TestMetricsExposeHealthProbeTelemetry' -count=1` | 通过 | 健康探测运行/探测/跳过/耗时指标只接受固定状态，任意输入标签归一化为 `unknown` |
| `cd backend && go test ./internal/httpapi -run 'TestMetricsExposeRetentionCleanupTelemetry' -count=1` | 通过 | 清理运行/数据集状态/删除数量指标只接受固定数据集与状态，任意输入标签归一化为 `unknown` |
| `cd backend && go test ./internal/service -run 'TestRotateModelPoolAccountSecret' -count=1` | 通过 | 密钥轮换候选校验、幂等重放，以及并发提交故障下 staged secret 不互删回归 |
| `cd backend && go test ./internal/service -run 'TestRotateModelPoolAccountSecretDifferentIdempotencyKeysCannotOverwriteWinner' -count=1` | 通过 | 不同幂等键并发轮换的 SecretRef 冲突门禁、胜者密钥保留和旧密钥回收回归 |
| `cd backend && go test -count=1 -tags=postgres_integration ./internal/store` | 通过（本地 Docker PostgreSQL 16） | 执行 normalized 重启式读取、约束失败回滚、双实例幂等竞争、Session Store 重启/Refresh、清理、Secret、审计、故障取消和 0021 计划门禁 |
| `cd backend && go test -count=1 -tags=postgres_integration ./internal/store -run '^TestPostgresNormalizedActivation'` | 通过（本地 Docker PostgreSQL 16） | 验证过期/作废拒绝、重复核销、幂等指纹冲突、Session 冲突/取消等待和无设备/会话/幂等/审计残留 |
| `cd backend && go test ./internal/store ./internal/service -run 'TestPostgresRepositoryGet(Device|OwnedDevice)|TestGetClientProfile|TestNormalizedUserMutationFailsClosed' -count=1` | 通过 | normalized 设备详情/profile 直接读规范化表；缺失 UserRepository 时用户写路径 fail-closed |
| `cd backend && go test -count=1 -tags=postgres_integration ./internal/store -run 'TestPostgresNormalizedDeviceReaderUsesDeviceTables'` | 通过（本地 Docker PostgreSQL 16） | 迁移 0021→回填后验证设备详情、按用户归属读取和最近心跳选择，不读取快照 |
| `cd backend && go test ./internal/store ./internal/service ./internal/httpapi -run 'Activation(Page|CodesPage)' -count=1` | 通过 | 激活码 SQLMock、服务层分页路由和 HTTP 分页响应回归；normalized reader 只在规范化读源启用 |
| `cd backend && go test ./internal/httpapi -run 'TestAdminActivationCodeListEndpointPaginatesAndRedacts' -count=1` | 通过 | 管理端激活码分页响应包含总数/页码，列表 `plain_code` 为 null 且不泄露明文 |
| `cd backend && go test -count=1 -tags=postgres_integration ./internal/store -run 'TestPostgresNormalizedActivationPageReadsRedactedRows'` | 通过（本地 Docker PostgreSQL 16） | 验证 `COUNT + LIMIT/OFFSET`、过期状态派生、活动状态、明文不泄露和 `MaxDevices=1` |
| `docker build --pull --tag autolive-api:20260821-v275 --file backend/Dockerfile backend` | 通过 | linux/amd64 镜像 digest `sha256:e908ec06b72fb2cdae7cddd0d6539b4930f4d5711b5770a05cc4a4d59b0dc315` |
| `autolive-api:20260821-v275` 本地容器冒烟 | 通过 | 真实 PostgreSQL 迁移→normalized backfill→`/api/v1/readyz=200`→SIGTERM，日志含 `http server stopped cleanly` |
| `docker build --pull --tag autolive-api:20260821-v276 --file backend/Dockerfile backend` | 通过 | linux/amd64 镜像 digest `sha256:232d5b6160540bc38f0ec2b638ba7670ff39016ecb47e41e4e026773614abddf` |
| `autolive-api:20260821-v276` 本地容器冒烟 | 通过 | 真实 PostgreSQL 迁移→normalized backfill→`readyz=200`、`/metrics` 可抓取→SIGTERM，日志含 `shutdown signal received` 与 `http server stopped cleanly`；临时容器/网络已清理 |
| `cd backend && go test ./internal/store ./internal/service -run 'GetUserAuthorizationSummary|AuthorizationSummary' -count=1` | 通过 | SQLMock 与服务层验证 normalized 授权摘要聚合、fail-closed 和 Memory/快照兼容回退 |
| `cd backend && go test -count=1 -tags=postgres_integration ./internal/store -run 'TestPostgresNormalizedUserAuthorizationSummaryReadsDomainFacts'` | 通过（本地 Docker PostgreSQL 16） | 验证规范化 users/devices/model_leases/policy/usage 聚合、UTC 日切和无 lease 用量排除 |
| `docker build --pull --tag autolive-api:20260821-v277 --file backend/Dockerfile backend` | 通过 | linux/amd64 镜像 digest `sha256:070a68a5e71db04aa9cb93c7d41d3f875e482264d1ad4e5fd6792a22c797cef4`，约 41.2 MB |
| `autolive-api:20260821-v277` 本地容器冒烟 | 通过 | 真实 PostgreSQL 迁移→normalized backfill→`readyz=200`、`/metrics` 含请求指标→SIGTERM clean shutdown；临时容器/网络已清理 |
| `cd backend && go test ./internal/service ./internal/httpapi ./internal/store -run 'ModelUsage|ListModelUsage|PoolSaturation|ModelPoolHealthProbeScheduler' -count=1` | 通过 | 模型调用摘要筛选、连接池饱和度指标、normalized 健康调度器分页和缺失 Reader fail-closed 回归 |
| `admin-web`: `pnpm typecheck && pnpm test && pnpm build && pnpm api:check` | 通过 | 管理端模型调用摘要筛选使用 URL 查询状态，OpenAPI 生成类型无漂移 |
| `desktop/ui`: `pnpm typecheck && pnpm test && pnpm build && pnpm api:check` | 通过 | 桌面端契约、路由和生成类型门禁通过 |
| `docker build --pull --tag autolive-api:20260821-v278 --file backend/Dockerfile backend` | 通过 | linux/amd64 镜像 digest `sha256:c45426386cbc45523258c83965074509800a5fb23f6e9d512f8c13332b83dcf2`，约 41.2 MB |
| `autolive-api:20260821-v278` 本地容器冒烟 | 通过 | 真实 PostgreSQL 迁移→normalized backfill→`readyz=200`、`/metrics` 含请求量/连接池饱和度指标→SIGTERM clean shutdown；临时容器/网络已清理 |
| `cd backend && go test ./cmd/verify-normalized-cutover ./internal/store -run 'TestVerifyNormalizedCutover|TestValidateCutoverConfig' -count=1` | 通过 | 只读准入命令配置校验、normalized source、pending marker、关键表和 Secret 引用检查回归 |
| `cd backend && go run ./cmd/verify-normalized-cutover`（Docker PostgreSQL 16，`APP_NORMALIZED_CUTOVER_CONFIRM=YES`） | 通过 | 迁移→回填后准入检查通过，输出仅包含聚合表计数；命令不写数据库 |
| `docker build --pull --tag autolive-api:20260821-v281 --file backend/Dockerfile backend` | 通过 | linux/amd64 镜像 image ID `sha256:a5ae6ae18c7f0006a97a6c8610fc78f685ff2b9aaccc860f458c65c35068e1ac`，包含 `autolive-verify-normalized-cutover` |
| `autolive-api:20260821-v281` normalized API 本地容器冒烟 | 通过 | PostgreSQL 迁移→回填→API `readyz=200`、`/metrics` 请求/连接池指标→SIGTERM clean shutdown；临时容器/网络已清理 |
| `cd backend && go test ./internal/store ./internal/service -run 'TestPostgresRepositoryNormalizedModelPoolHealthPageFiltersBeforeLimit|TestModelPoolHealthProbeSchedulerUsesBoundedNormalizedReader|TestListModelPoolHealthAccountsFailsClosedWithoutNormalizedReader' -count=1` | 通过 | 健康专用 Reader 路由、缺失 Reader fail-closed 及 SQL 在 LIMIT 前过滤 disabled/Secret/cooldown/日额度耗尽账号 |
| `cd backend && go test -count=1 -tags=postgres_integration ./internal/store -run 'TestPostgresNormalizedModelPoolHealthReaderFiltersBeforeLimit'` | 通过（本地 Docker PostgreSQL 16） | 不可探测账号占满前缀时，normalized 查询仍返回后续可探测账号 |
| `docker build --pull --tag autolive-api:20260821-v280 --file backend/Dockerfile backend` | 通过 | linux/amd64 镜像 digest `sha256:840219d4d4fef8126f6aa4b6bd2a12550c8f60cb300067ed1ba887b6da4a873a` |
| `autolive-api:20260821-v280` 本地容器冒烟 | 通过 | 真实 PostgreSQL 迁移→normalized backfill→`readyz=200`、`/metrics` 请求/连接池指标→SIGTERM clean shutdown；临时容器/网络已清理 |
| `cd backend && go test -count=1 -tags=postgres_integration ./internal/store -run 'TestPostgresNormalizedModelLease(LazyExpiry|SkipsCooldown|Release)'` | 通过（本地 Docker PostgreSQL 16） | 验证懒过期释放并发槽位、冷却/禁用账号跳过、冷却到期恢复和释放租约后槽位恢复 |
| `cd backend && go test -count=1 -tags=postgres_integration ./internal/store -run 'TestPostgresManagementFilterIndexesAreVisibleToPlanner'` | 通过（本地 Docker PostgreSQL 16） | 验证 0021 复合筛选索引在 `EXPLAIN` 计划中可见；真实数据量 `EXPLAIN (ANALYZE, BUFFERS)` 仍需发布环境复核 |
| `cd backend && go test -count=1 -tags=postgres_integration ./internal/store -run 'TestPostgresNormalizedActivation(RejectsConflictingSessionBindingWithoutResidue|CancellationWhileWaitingForMutationLockLeavesNoResidue)'` | 通过（本地 Docker PostgreSQL 16） | 真实 `pg_locks` 等待取消和 Session 设备冲突均返回稳定错误且无业务/会话/幂等/审计残留 |
| `cd backend && go test ./migrations -count=1` | 通过 | 迁移目录版本已到 0021，并校验管理筛选复合索引和既有唯一约束/cooldown 字段安全片段 |
| `cd backend && go test ./cmd/backfill-normalized ./internal/store -count=1` | 通过 | 一次性规范化回填的显式确认、advisory lock、事务提交和逐表计数校验 |
| `cd backend && go test ./cmd/migrate -count=20` | 通过 | 迁移 text exposition 的成功/失败/超时/取消状态、敏感字段排除、原子替换和临时文件清理 |
| `cd backend && go test ./internal/store -run 'TestPostgres.*NormalizedBackfill|TestPostgresRetentionCleanupRejectsIncompleteNormalizedBackfill' -count=1` | 通过 | pending/缺失标记 fail-closed、completed 重跑拒绝、兼容写入和保留清理均受完成标记门禁 |
| `npx --yes js-yaml .github/workflows/backend-quality.yml` | 通过 | CI PostgreSQL 作业显式按迁移→回填→normalized 集成测试顺序执行 |
| `cd backend && go test ./internal/store -run 'Test.*Page' -count=1` | 通过 | 内存分页稳定排序、normalized PostgreSQL 覆盖检查、`COUNT + LIMIT/OFFSET` 用户/设备查询 |
| `cd backend && go test ./internal/store -run 'TestPostgresRepositoryNormalized(ModelPool|ModelLease|Usage|Audit)Page' -count=1` | 通过 | normalized 模型账号/租约派生摘要、调用摘要/审计分页和结构化字段回归 |
| `cd backend && go test ./internal/store ./internal/service ./internal/httpapi -run 'ModelLease.*(Page|Lifecycle)|ListModelLeasesPageWithOptions' -count=1` | 通过 | 租约状态/归属筛选、排序白名单、脱敏详情、管理员幂等回收、normalized SQL 参数绑定和 HTTP 400/200 回归 |
| `cd backend && go test ./internal/store ./internal/service ./internal/httpapi -run 'Audit.*(Page|Log)|ListAuditLogsPageWithOptions' -count=1` | 通过 | 审计结果/归属/时间窗口筛选、排序白名单、normalized SQL 参数绑定和 HTTP 400/200 回归 |
| `cd backend && go test -race ./...` | 未执行成功 | 当前环境为 `windows/386`，Go 报告 `-race is not supported on windows/386`；必须在 `amd64` CI 或本地环境补跑 |
| `desktop/ui`: `pnpm typecheck && pnpm build && pnpm test` | 通过 | 桌面端类型检查、构建通过，Node 测试 177/177 通过 |
| `admin-web`: `pnpm typecheck && pnpm build && pnpm test` | 通过 | 管理端类型检查、构建通过，Node 测试 6/6 通过；模型账号与租约列表使用分页 |
| `npx --yes js-yaml 接口契约/openapi.yaml` | 通过 | OpenAPI YAML 解析通过 |
| `npx --yes js-yaml .github/workflows/backend-quality.yml` | 通过 | CI 工作流 YAML 解析通过 |
| `docker compose --env-file deploy/.env.example -f deploy/docker-compose.yml config` | 通过 | Compose 插值和 `/api/v1/readyz` 健康检查配置通过；本地 Docker Desktop 已完成真实镜像/迁移/回填/normalized API 与 Compose 健康验收 |
| `docker build --platform linux/amd64 -t autolive-api:20260814-01 ./backend`、`docker build --platform linux/amd64 -t autolive-admin:20260814-01 ./admin-web` | 通过 | backend/admin 镜像构建成功；backend 镜像 digest 为 `sha256:a56b48f9171603b9769bbce081c629907ccfb4315da2c94eb64f2b2397979d5d` |
| `docker run ... autolive-migrate`、`autolive-backfill-normalized` + PostgreSQL 16 | 通过 | 迁移到 0021、回填 marker 为 `completed`，normalized `/readyz`/`/metrics` 返回 200 |
| `docker compose ... up -d`、`docker compose ... stop -t 60` | 通过 | migrate `Exited(0)` → API `Healthy` → 管理端代理 readyz 200；SIGTERM 日志含 `http server stopped cleanly` |
| `go test ./internal/config ./cmd/server` | 通过 | `APP_SHUTDOWN_TIMEOUT` 解析/默认值和服务器关闭预算回归通过 |
| `node tools/check-https-publish.mjs --env-file deploy/.env.example --reverse-proxy deploy/reverse-proxy/nginx.conf --capability desktop/src-tauri/capabilities/default.json --admin-nginx admin-web/nginx.conf` | 通过 | 生产 HTTPS 公共地址、回环管理端 hop、反向代理 TLS/308 和 Tauri HTTPS capability 门禁通过 |
| `cd desktop/ui && pnpm typecheck && pnpm test && pnpm build` | 通过 | 桌面端控制面 URL/JSON 契约、HashRouter、媒体 UI 回归 177/177，生产构建通过 |
| `cd desktop && cargo test --manifest-path src-tauri/Cargo.toml capabilities_allow_remote_control_plane_only -- --nocapture` | 通过 | Tauri capability 只允许开发回环 HTTP 和明确 HTTPS 模板域名 |
| `tools/check-document-references.sh` | 未执行成功 | 当前 Windows/WSL 环境没有 `/bin/bash`；OpenAPI、HTTPS 发布门禁和 Compose 检查已单独通过，文档引用脚本仍需 Linux/CI 或可用 Bash 环境复核 |

在 Phase 0 完成前，不能把“测试全绿”直接等同于“服务端已完成”；本轮只记录实际命令结果，不提前关闭未完成的阶段任务。

### 3.4 本轮执行记录（2026-08-20～2026-08-21）

本轮继续落地 Phase 2/3/4 的控制面闭环；第 4 节仍保留完整 backlog，已关闭项在这里标记，避免把局部完成误写成服务端整体完成：

#### V2.50 本批次补充事实

- 客户端调用摘要（`POST /api/v1/client/llm/call-records`）在 normalized PostgreSQL 路径接收可选成功审计事件；调用摘要、账号日额度状态、`control-plane-state` 幂等记录与 `audit_outbox` 在同一事务提交。
- 重放使用原 request ID 或 `(lease_id, client_call_id)` 事实核对，Outbox `dedupe_key` 防止重复审计；兼容 Memory/快照路径仍由统一审计中间件负责，未宣称跨存储原子性。
- 当前仍未关闭：模型账号/Secret 轮换审计同事务、提交未知结果、真实 PostgreSQL 连接/并发故障矩阵、容量告警、归档和 HTTPS 发布证据。

#### V2.51 本批次补充事实

- normalized PostgreSQL 新增 `ModelPoolSecretRotator`：候选密文、账号 `secret_ref` 切换、模型测试结果、幂等记录、旧密文删除和成功审计 Outbox 通过 `TransactionalSecretWriter.PutTx` 共享同一事务。
- 管理端密钥轮换路由在 normalized 能力存在时使用该事务边界；Memory/快照兼容路径仍使用 staged secret 和有界补偿，不能据此宣称所有存储模式均已原子。
- 当前仍未关闭：跨实例真实 PostgreSQL 连接断开/提交未知结果、Secret Store 故障注入、旧密钥归档策略、容量告警和发布环境 HTTPS 证据。

#### V2.52 本批次补充事实

- 新增 PostgreSQL 集成验收：`TestPostgresNormalizedSecretRotationPersistsAcrossRepositoryRestart` 验证 normalized Secret 轮换提交后重启 Repository 仍能读取新密文，旧密文已删除，幂等重放不重复切换，并且成功审计 Outbox 只保留一条。
- 新增 `TestPostgresNormalizedSecretRotationRollsBackStagedCiphertext`：在候选密文写入后注入非法探测时间，验证账号引用、Secret 密文、测试结果和幂等记录一起回滚，不留下 staged ciphertext。
- 当前仍未关闭：真实 PostgreSQL 连接断开/提交未知结果、Secret Store 进程外故障注入、旧密钥归档策略、Memory/快照 staged fallback 的跨进程恢复、容量告警和发布环境 HTTPS 证据。

#### V2.53 本批次补充事实

- 桌面控制面客户端不再固化 `192.168.100.213:18090` 私网 HTTP 地址；未配置 `VITE_CONTROL_PLANE_BASE_URL` 时只在 Vite 开发构建回退到 `http://127.0.0.1:18090`，生产构建缺少显式地址会直接拒绝启动。
- Tauri HTTP capability 与桌面架构文档同步为开发回环 HTTP + 明确 HTTPS 模板域名，继续禁止任意 URL 通配和旧私网地址。
- 当前仍未关闭：真实生产域名/证书/DNS/Nginx reload/HTTPS 冒烟；正式构建必须注入实际 `VITE_CONTROL_PLANE_BASE_URL` 并重新运行发布门禁。

#### V2.55 本批次补充事实

- BE-008 DTO 契约门禁已落地：`TestOpenAPIMirroredDTOFieldsMatchGoJSONTags` 使用显式 Go DTO→OpenAPI schema 映射，递归处理匿名嵌入字段，比较 JSON 字段集合和 required 集合，并对非 `omitempty` 指针字段要求 OpenAPI nullable。
- 门禁首次运行发现并修复四类真实漂移：Logout 请求的 `refresh_token` 改为可选；激活码创建的 `max_devices` 与服务端零值默认 1 语义对齐；Direct LLM 调用的 token/延迟统计字段改为可选；响应映射改为实际 `DirectLLMCallRecordResponse` schema。该批次没有放宽测试规则。
- BE-021/BE-022 保留清理边界补强：Memory 控制面三类记录、normalized PostgreSQL 幂等/模型测试/审计三类记录及 SQL `auth_sessions` 均统一 cutoff、1～1000 批次上限、Context 取消和确定性旧到新删除；SQL 使用参数化 CTE/锁定批次并保持快照读源拒绝语义，SQLMock/Memory 回归覆盖成功、取消、错误和边界。
- BE-008 错误响应批次：OpenAPI 共享错误 response 组件新增 400/401/403/404/405/409/429/500/502/503/504 示例；契约测试验证示例包含 `code/message/request_id` 且错误码存在于共享错误码文档，并通过真实 Router/错误映射覆盖 400、401、403、404、405、409、429、500、502、503、504。
- 当前仍未关闭：全量操作级状态声明和生成类型差异门禁；本地 Docker PostgreSQL 集成测试已全量通过，Race 仍由 Linux/amd64 CI 提供证据。

#### V2.56 本批次补充事实

- BE-008 操作级错误声明已落地：OpenAPI 的 40 个 HTTP 操作全部显式声明 `405 MethodNotAllowed`、`408 RequestTimeout` 和 `500 InternalServerError`，健康检查也保留同一通用错误包络；`TestOpenAPIOperationsDeclareCommonRuntimeErrorStatuses` 会逐个解析操作 response，并校验状态码到共享 component 的引用，新增操作缺声明会直接失败。
- OpenAPI YAML 已重新解析通过；此前已完成的共享错误示例、Go 错误码状态矩阵、DTO properties/required/nullable 和本地 `$ref` 门禁继续作为同一组契约检查。
- 当前仍未关闭：生成客户端类型与手写 DTO 的自动差异门禁；本地 Docker PostgreSQL 集成测试已全量通过，Race 仍由 Linux/amd64 CI 提供证据。

#### V2.57 本批次补充事实

- 新增 `TestPostgresNormalizedActivationRedeemIsSingleWinnerAcrossRepositories`：两个独立 normalized PostgreSQL Repository 使用不同会话、不同设备和不同幂等键同时核销同一激活码，验证事务级 advisory lock 与激活码行锁共同保证只有一个成功者，失败者稳定返回 `ErrActivationCodeAlreadyUsed`，最终只留下一个已激活设备、一个绑定会话和一条已使用激活码事实。
- 该测试不使用任意长睡眠，使用 barrier/channel 同步并发起点；由 `.github/workflows/backend-quality.yml` 的 PostgreSQL 16 集成作业执行。当前本机未设置 `TEST_POSTGRES_URL`，因此仅完成编译与跳过路径验证，真实数据库结果仍以 CI 为准。
- 新增 `TestPostgresNormalizedModelLeaseConcurrencyHasSingleWinnerAcrossRepositories`：两个独立 normalized PostgreSQL Repository 使用不同设备和不同幂等键，同时申请同一账号租约；真实数据库验证账号 `concurrency_limit=1` 只产生一个活动租约，失败者返回 `MODEL_POOL_UNAVAILABLE`，测试使用 channel barrier，不使用任意长睡眠。
- 该租约测试同样由 PostgreSQL 16 集成作业执行；当前本机未设置 `TEST_POSTGRES_URL`，仅完成编译与跳过路径验证。租约 UTC 日额度日切、连接断开/提交未知结果、Secret Store 进程外故障注入、激活码重启恢复与语义化审计证据仍未关闭。
- BE-023 发布可复现性切片已落地：`backend/Dockerfile` 的 Go/Alpine 基础镜像固定 Docker Hub manifest digest；CI 使用 BuildKit 同时导出 Docker/OCI 产物，启用 SBOM/Provenance，记录 BuildKit canonical digest、镜像内容 `image_id` 与产物 SHA-256，并上传 metadata artifact；`tools/check-backend-release.mjs` 及测试阻止摘要或构建声明回退。
- BE-007 优雅关闭证据已加入 CI：构建后的 server 进程通过 `/readyz` 后收到 SIGTERM，等待退出并检查 `http server stopped cleanly`；在途事务取消、后台任务/数据库关闭和迁移 textfile collector 的挂载/权限/重启仍需更深故障矩阵。

#### V2.58 本批次补充事实

- BE-008 生成类型门禁已落地：管理端新增 `openapi-typescript@7.13.0`（MIT，约 0.88 MB unpacked、仅开发依赖；实际调用点为 `admin-web` 的 `api:generate/api:check`，锁文件固定版本；生成产物 `src/api/openapi.generated.ts` 与手写适配层分离），CI 新增 `frontend-contract` 作业执行生成一致性、TypeScript、测试和构建。选择该库是因为它直接支持 OpenAPI 3.1 并提供官方 `--check`；不引入运行时请求客户端，避免重复已有 `fetch` 封装，后续若迁移到其他生成器可删除该开发依赖和生成文件。
- 管理端 `ApiErrorDetail`/`ApiErrorResponse` 已从生成的 OpenAPI components 派生，统一错误 DTO 不再维护第二份字段定义；桌面端继续通过已有控制面契约测试校验同一 JSON/error 包络。
- 租约真实 PostgreSQL 集成测试已补齐同账号 `concurrency_limit=1` 的双 Repository 单一成功者证据；本机无 `TEST_POSTGRES_URL` 时按预期跳过。UTC 日额度日切、连接断开/提交未知结果、Secret Store 进程外故障注入和镜像签名仍未关闭。

#### V2.59 本批次补充事实

- BE-008 生成类型消费已扩展到桌面端：`desktop/ui` 同样锁定 `openapi-typescript@7.13.0`，生成 `src/api/openapi.generated.ts`，并以 `api:check` 校验；控制面错误、设备、登录、激活、心跳、客户端资料、租约和调用摘要 DTO 从 OpenAPI components 派生，前后端继续共用同一 JSON/error/request_id 包络。
- 管理端 `src/types/api.ts` 已将资源、请求和响应 DTO 全部改为生成 components 的类型别名，保留 `ApiRequestOptions` 这类真正属于 fetch 适配层的本地类型，删除重复手写字段定义。
- BE-017/BE-020 新增 `TestPostgresNormalizedModelDailyQuotaRecoversAfterUTCDayRollover`：使用注入时钟从 UTC `23:59:59` 推进到次日 `00:00:00`，验证达到日额度后账号进入 `exhausted`、跨日恢复 `active`、新日用量从零开始并可重新申请租约；本机没有 `TEST_POSTGRES_URL` 时按预期跳过，CI PostgreSQL 16 执行真实数据库路径。
- 桌面端 Vite 从 5.4.21 升级到已修复安全版本 6.4.3，`pnpm audit --audit-level high` 通过；React 18、React Router 7、Tauri 构建和 177 项桌面测试通过。剩余 P0/P1 仍是生产 HTTPS 证书/DNS、连接断开/未知提交故障、Secret 补偿、规范化快照退出和供应商权威额度。

#### V2.61 本批次补充事实

- P0 规范化回填准入已补齐：迁移 0020 新增单例 `normalized_backfill_state`，迁移默认写入 `pending`；`BackfillNormalized` 在 advisory lock 下先锁定并校验标记，只允许 pending 状态执行快照复制、逐表计数校验和同事务切换 `completed`。
- normalized 完整读取、SQL 分页读取和 `/readyz` 使用的 Repository 状态加载会拒绝缺失或非 `completed` 标记；normalized 领域写事务通过数据库函数门禁拒绝未完成回填，既有快照数量覆盖检查继续拒绝规范化投影少于快照的状态。历史 `control_plane_state` 快照表没有删除。
- 回填工具对已完成标记直接拒绝重跑，不会把可能已经发生的 normalized-only 事实重新覆盖回快照旧数据；回滚只允许通过受控发布切回快照读源，不能把重复回填当作回滚操作。SQLMock 覆盖完成、pending 拒绝和 completed 重跑拒绝，迁移目录和命令测试通过。

#### V2.62 本批次补充事实

- 规范化完成标记门禁已扩展到兼容 `StateOperation` 写入、normalized 保留清理和运维聚合读取；这些入口在同一数据库事务中调用 `autolive_require_normalized_backfill_completed()`，缺失或 `pending` 时 fail-closed，不会绕过回填直接修改或清理规范化事实。
- PostgreSQL 集成作业在应用全部迁移后显式运行 `autolive-backfill-normalized`（`APP_NORMALIZED_BACKFILL_CONFIRM=YES`），再执行 normalized 集成测试；生产 Compose 仍默认 `snapshot`，不会把未完成的真实回填误当作发布切换。
- 迁移命令可选的 `APP_MIGRATION_METRICS_FILE` 已使用现有 Prometheus 客户端生成固定低基数 text exposition，并通过同目录临时文件、刷盘后原子重命名发布；指标文件发布失败会阻止迁移成功退出，未配置时保持既有行为。挂载目录权限、textfile collector 消费、重启恢复和真实故障矩阵仍待环境验收。
- 本批次历史验证记录：`go test ./...`、`go vet ./...`、`go build ./...`、`govulncheck@v1.7.0 ./...`、迁移命令重复回归、workflow YAML/Compose 解析和后端发布门禁通过；当时本机 `windows/386` 不支持 Race 且 Docker daemon 不可用，真实 PostgreSQL 仅由 CI PostgreSQL 16 作业执行。后续 V2.64/V2.65 已补充本地 Docker Desktop 与 PostgreSQL 16 证据。

#### V2.63 本批次补充事实

- BE-014 真实 PostgreSQL 集成切片新增过期、作废、重复核销和幂等冲突验收：过期/作废请求不会写入设备、会话或幂等记录；已提交核销只允许一个设备/会话终态；重启式 Repository 使用同一幂等键但不同指纹会返回 `IDEMPOTENCY_CONFLICT`。本机没有 `TEST_POSTGRES_URL` 时按设计跳过，CI PostgreSQL 16 执行真实路径。
- BE-016/BE-017 真实 PostgreSQL 集成切片新增租约懒过期复用并发槽位、禁用/冷却账号跳过及冷却到期恢复、释放租约后恢复并发槽位；测试同时校验 `released_at`、账号 `status/cooldown_until` 和旧租约终态。本机无数据库 URL 时按设计跳过，CI 执行真实路径。
- BE-018/BE-021 新增前向迁移 0021，为租约按用户/设备/provider+model 和审计按操作者/设备/动作/资源的常用筛选补齐复合索引；新增 PostgreSQL `EXPLAIN` 计划测试（关闭顺序/位图扫描后验证索引可见），真实数据量 `EXPLAIN (ANALYZE, BUFFERS)` 与容量评估仍待发布环境复核。
- 本批次未新增生产抽象；新增集成测试均复用现有隔离数据、事务 Repository 和 SQL Secret Store。迁移目录版本提升至 0021，生产仍需按“迁移→回填→集成测试”顺序执行。

#### V2.64 本批次补充事实

- BE-006/BE-024/BE-020 新增真实 PostgreSQL 故障切片：已绑定其他设备的 Session 激活返回内部错误 `ErrSessionDeviceBindingConflict`，不残留激活码核销、目标设备、幂等、审计 Outbox 或审计日志；独立事务持有 normalized advisory lock 时取消等待请求返回 `context.Canceled`，同样不残留业务/会话/幂等/审计事实。两项测试均观察真实 `pg_locks` 等待，不使用任意长睡眠。
- 本地 Docker PostgreSQL 16 已执行 0001～0021 迁移、`APP_NORMALIZED_BACKFILL_CONFIRM=YES` 回填并完成 `migration_version=21`、`normalized_backfill_state=completed` 校验；带 `postgres_integration` 的 `./internal/store` 全量测试通过，包含激活码、租约、额度日切、会话、清理、Secret、审计和 0021 计划门禁。强制连接断开、Commit 返回未知结果及进程外 Secret 故障仍未覆盖。
- BE-026/BE-007/BE-023 本地发布验收已补齐：backend/admin 的 `linux/amd64` 镜像构建成功；normalized API 的 `/api/v1/readyz` 与 `/metrics` 返回 200；真实 Compose 顺序为 migrate `Exited(0)` → API `Healthy` → 管理端代理 readyz 200；API 收到 SIGTERM 后记录 `shutdown signal received` 和 `http server stopped cleanly`。临时 PostgreSQL/Compose 容器已清理，验收镜像标签保留；镜像签名、迁移回滚清单和生产 HTTPS 仍待完成。

#### V2.65 本批次补充事实

- BE-018 查询筛选切片已完成：模型调用摘要管理接口新增 `provider`、`model`、`user_id`、`device_id`、`request_id`、RFC3339 `created_after/created_before` 和 `created_at_asc|created_at_desc`；Memory 与 normalized PostgreSQL 使用同一边界语义，SQL 只拼接固定列白名单，值、时间、LIMIT/OFFSET 全部参数化，管理端通过 URL 查询参数保留筛选和页码，OpenAPI 与管理端/桌面端生成类型已同步。剩余真实高数据量 `EXPLAIN (ANALYZE, BUFFERS)` 和其他列表筛选仍未关闭。
- BE-021/BE-022 容量观测切片已完成：对实现 `DBStats()` 的 PostgreSQL Repository 增加无高基数标签的 `autolive_database_pool_saturation_ratio` 与 `autolive_database_pool_saturated`，固定 0.8 饱和阈值；无限连接池和 Memory 仓储不伪造数据库饱和度，并有阈值、钳制、指标输出回归。告警规则、真实容量基线和归档策略仍未完成。
- BE-006/BE-020 提交结果切片已完成部分收口：`postgresCommitError` 将提交驱动错误区分为 Context 取消/截止或 `COMMIT_OUTCOME_UNKNOWN`；控制面快照 `Run` 在 COMMIT 发出后不再删除 staged Secret，normalized 模型账号创建、账号变更和 Secret 轮换保留候选密文，HTTP 统一返回 `503/COMMIT_OUTCOME_UNKNOWN`；同幂等键可继续核对，不能立即生成新写入。其他 normalized 领域提交、真实断线/提交后连接丢失和进程外 Secret 故障仍待真实 PostgreSQL 矩阵。
- 本批次新增/更新 SQLMock、Memory、Service、HTTP 契约和错误码回归；已通过 `go test ./...`、`go vet ./...`、`go build ./...` 的既有全量基线，新增切片定向测试通过。Docker Desktop 重新构建 `autolive-api:20260821-v265`（digest `sha256:73105900d2b8165a97d99dddda2a22d13c320fa789be9531704f13a3d4ef9fda`，约 41 MB），真实 PostgreSQL 16 再次完成迁移 0021→normalized backfill→normalized `/api/v1/readyz`/`/metrics`，新饱和度指标可见，SIGTERM 日志为 `shutdown signal received`→`http server stopped cleanly`；新增模型用量筛选尚未用带业务数据的容器请求单独验收。

#### V2.66 本批次补充事实

- BE-006/BE-020 提交结果语义已扩展到全部 PostgreSQL 事务出口：激活码、用户/授权、设备激活/心跳/生命周期、模型租约/用量、审计追加/Outbox、Session 轮换、保留清理、分页读事务和模型账号/Secret 事务均统一使用 `postgresCommitError`；驱动错误在 Context 取消/截止与 `COMMIT_OUTCOME_UNKNOWN` 之间分类，HTTP 通过 `503/COMMIT_OUTCOME_UNKNOWN` 提示客户端核对原幂等键，COMMIT 发出后不执行不安全补偿删除。
- 新增激活码 normalized SQLMock 回归，验证 COMMIT 驱动错误返回 `ErrCommitOutcomeUnknown`；真实 PostgreSQL 16 集成测试全量通过，包含激活码、租约、额度日切、Session、清理、Secret、审计、筛选索引和 advisory-lock 取消路径。该批次仍未模拟真实网络断开后“提交已落库但客户端失联”的数据库故障注入。
- Docker Desktop 构建 `autolive-api:20260821-v266`（digest `sha256:d288c72cd52c0c931a5b2cfd8c760c83e1725c23044529c694edfe46eda71c85`，约 41 MB）；真实 PostgreSQL 16 完成迁移 0021、normalized backfill、API normalized `/api/v1/readyz`/`/metrics` 和连接池饱和度指标检查，SIGTERM 后记录 `shutdown signal received` 与 `http server stopped cleanly`；临时容器已清理，镜像标签保留。

#### V2.67 本批次补充事实

- BE-006/BE-020/BE-024 真实连接故障切片已补齐：新增 `TestPostgresNormalizedActivationConnectionTerminationWhileWaitingLeavesNoResidue`，在独立 PostgreSQL 事务持有 normalized advisory lock、业务事务已由真实 `pg_locks` 观察到等待后，使用另一条管理连接调用 `pg_terminate_backend` 强制断开等待后端；请求在有界时间内返回连接失败，未误判为 `COMMIT_OUTCOME_UNKNOWN`，激活码、设备、Session、幂等记录、审计 Outbox 和审计日志均无残留。该证据覆盖提交前连接断开；“COMMIT 已发送但客户端丢失响应”仍由 SQLMock 分类和后续真实故障注入覆盖，不能合并宣称。
- 本地 Docker PostgreSQL 16 已执行该故障切片与既有取消等待切片，目标测试 `2/2 PASS`；临时数据库容器在测试后停止并清理。未新增生产依赖或连接杀手代码，测试仅在 `postgres_integration` 标签下编译。

#### V2.68 本批次补充事实

- BE-006/BE-020 提交后响应丢失故障切片已补齐：新增测试专用 PostgreSQL TCP 代理，读取服务端协议帧并在转发 `COMMIT` 的 `CommandComplete` 后关闭连接、丢弃 `ReadyForQuery`；Repository 通过代理执行 normalized 激活，真实旁路连接确认激活码已核销、设备已创建、Session 已绑定、幂等记录和审计 Outbox 已持久化，客户端返回稳定 `ErrCommitOutcomeUnknown`，没有执行补偿删除。
- 本地 Docker PostgreSQL 16 下提交后响应丢失目标测试和 `postgres_integration` Store 全量测试均通过；代理、连接终止和协议解析均只存在于集成测试文件，不进入生产二进制。该证据仍不等于跨 Secret Store 进程故障补偿或快照退出已完成。

#### V2.69 本批次补充事实

- BE-007/Phase 6 优雅关闭切片已补齐：`TestServerGracefulShutdownWaitsForInFlightRequest` 使用真实 `http.Server` 和受控在途请求验证 `Shutdown` 不会提前返回，释放请求后在预算内完成，并且关闭监听后新请求不再成功；已有 Compose/CI 的 readyz→SIGTERM→clean-exit 冒烟继续覆盖进程边界。
- 该切片只证明 HTTP 层停止接收新请求并等待在途请求，数据库连接关闭、后台调度器、指标导出器和关闭超时的深层故障矩阵仍未宣称完成。

#### V2.70 本批次补充事实

- BE-015 跨存储旧密钥补偿已补齐第一切片：兼容 Memory/快照轮换在业务状态切换事务中预写旧引用和排队时间到 `State.PendingSecretCleanup`，旧 Secret 删除成功后移除，删除失败或提交结果未知时保留；快照序列化保留该队列，normalized PostgreSQL 继续使用事务型 SecretStore 同事务删除旧密文。
- 保留清理新增 `SecretReferenceCleaner`，按固定 cutoff、1～1000 批次、保护引用集合和参数化 `DELETE ... RETURNING` 删除明确的退休引用；Memory、SQLMock 和真实 PostgreSQL 均验证旧引用可恢复删除、当前引用/新鲜引用不会误删。该批次仍未覆盖 Secret Store 进程外宕机后的跨进程恢复和旧密钥归档策略。

#### V2.71 本批次补充事实

- 补充快照持久化回归：`PendingSecretCleanup` 会随控制面快照保留排队时间和引用；快照仍会清除激活码明文和活动账号 `SecretRef`，恢复时按账号 ID 重建当前引用，未把退休引用误当作活动密钥。
- 最终复核通过 `go test ./...`、`go vet ./...`、`go build ./...`、`GOTOOLCHAIN=go1.26.6+auto go run golang.org/x/vuln/cmd/govulncheck@v1.7.0 ./...`、`go vet -tags=postgres_integration ./internal/store` 以及 Docker PostgreSQL 16 的迁移 0021→回填→规范化集成测试；当前代码对应镜像 `autolive-api:20260821-v271` 构建成功（image ID `sha256:45cda764b8a80cc51a2133bfbc212a7f43a2ea608a9cdfd1fae8a9322de8f342`，约 41.1 MB），临时数据库容器已停止并删除。`go test -race ./...` 仍因当前 `windows/386` 不支持 Race Detector，保留 Linux/amd64 CI 验证。

#### V2.72 本批次补充事实

- BE-002/BE-005 快照退出第一切片已完成：normalized 页面读取、状态事务和保留清理只校验 `normalized_backfill_state=completed`，不再读取或更新 `control_plane_state`；旧快照仅保留为一次性回填输入和显式 snapshot 兼容模式，完整领域 Repository 收口和删除 `APP_MODEL_READ_SOURCE` 仍未完成。
- 新增 `TestPostgresNormalizedRuntimeDoesNotDependOnLegacySnapshot`：本地 PostgreSQL 16 在回填完成后删除旧快照，normalized 分页、清理和状态事务仍通过；SQLMock 同步移除运行时快照查询/更新预期。临时数据库容器已停止并删除。
- 当前代码镜像 `autolive-api:20260821-v272` 构建成功，image ID 为 `sha256:51bdff4d8443f6737f869f1a9621b36a02ffa42d65fc644a7e0cdff580e4d592`，约 41.1 MB；本轮未将快照删除场景放入长期 Compose 数据库容器，临时资源已清理。

#### V2.73 本批次补充事实

- BE-016/BE-019 模型账号连通性测试已退出 normalized 快照写回：新增 `ModelPoolTestRepository`，准备事务只读取规范化账号、SecretRef 和幂等缓存，随后在事务外执行已有 SSRF/超时受限的 `/models` 探测，完成事务再原子写入 `model_pool_test_results`、账号 active/cooldown 状态、幂等记录和可选审计 Outbox；网络探测不会持有 SQL 锁。
- 同一幂等键重放直接返回持久化测试结果，指纹冲突 fail-closed；normalized 仓储缺失测试领域能力时不再静默回退快照。新增 SQLMock、服务层路由和真实 PostgreSQL 16 回放/cooldown 集成测试，验证 SecretRef 不进入 API 响应。
- 本轮验证：`go test ./... -count=1`、`go vet ./...`、`go build ./...`、`govulncheck@v1.7.0`、`go test -tags=postgres_integration ./internal/store -count=1` 均通过；Docker PostgreSQL 16 完成迁移 0021→normalized backfill→模型测试集成回归并清理临时容器。镜像 `autolive-api:20260821-v273` 构建成功，image ID `sha256:459beff7beed501c552da8c65e34bed019d923d7ed5dd98c32dec6892954e49b`，约 41.1 MB。`go test -race ./...` 仍由 Linux/amd64 CI 验证。

#### V2.74 本批次补充事实

- BE-004/BE-005 认证读路径已退出 normalized 快照：新增 `UserCredentialReader` 按用户名读取规范化用户与 bcrypt 哈希，新增 `UserReader` 按 ID 读取用户，登录和 Refresh 使用同一 PostgreSQL 规范化事实源；密码比对仍在服务层，哈希不进入 API、日志或持久化响应。
- normalized PostgreSQL 的管理员初始化、`/readyz` 管理员凭证检查和本地管理员密码轮换新增 `AdminCredentialRepository`，均通过短事务、advisory lock、幂等记录和明确状态校验直接访问 `users`；缺少领域能力时 fail-closed，不回退 `control_plane_state`。
- 新增 SQLMock、服务层和真实 PostgreSQL 16 集成测试，覆盖登录凭证读取、按 ID 用户读取、管理员创建/就绪/轮换/幂等回放；Docker 临时数据库已清理。镜像 `autolive-api:20260821-v274` 构建成功，image ID `sha256:57ab0af6674a0ee5df201fd4ec1271b79f763a47b570272149b6828e81aa9203`，约 41.1 MB。
- 本轮完整 `go test ./... -count=1`、`go vet ./...`、`go build ./...`、`govulncheck@v1.7.0` 通过；`go test -race ./...` 仍因当前 Windows/386 不支持 Race Detector，保留 Linux/amd64 CI 证据。完整快照退出、Secret 进程外恢复、供应商权威额度和生产发布证据仍未关闭。

#### V2.75 本批次补充事实

- BE-002/BE-005 规范化设备读路径第一切片：新增 `DeviceReader` 与 `postgres_device_reader.go`，`GetDevice` 和客户端 `/api/v1/client/profile` 在 normalized PostgreSQL 下直接读取 `devices`/`users`，支持按用户校验设备归属及无 device ID 时按最近心跳选择设备，不再回读 `control_plane_state`。
- normalized 用户创建、编辑、禁用和密码重置在缺少 `UserRepository` 时改为 fail-closed，返回明确的领域能力错误，不再静默落入快照 `StateOperation`；补充 SQLMock、服务层和真实 PostgreSQL 16 设备读取集成测试。
- 本轮验证：`go test ./... -count=1`、`go vet ./...`、`go build ./...`、`govulncheck@v1.7.0` 通过；本地 Docker PostgreSQL 16 完成迁移→回填→设备读取集成测试；镜像 `autolive-api:20260821-v275` 构建成功（image ID `sha256:e908ec06b72fb2cdae7cddd0d6539b4930f4d5711b5770a05cc4a4d59b0dc315`）。镜像运行时 normalized `readyz=200`，SIGTERM 日志出现 `shutdown signal received` 与 `http server stopped cleanly`。完整快照退出、其余领域读路径/Secret 进程外恢复、供应商权威额度和生产发布证据仍未关闭。

#### V2.76 本批次补充事实

- BE-018/BE-005 激活码读路径第一切片：新增 `ActivationPageReader` 与 `postgres_activation_reader.go`，normalized PostgreSQL 的 `GET /api/v1/admin/activation-codes` 使用 `COUNT + LIMIT/OFFSET` 有界参数化 SQL，状态按服务端时钟派生过期结果，响应只返回前缀、状态、时间和脱敏核销归属，不返回明文激活码。
- 服务层在 normalized PostgreSQL 使用领域分页能力；Memory 与快照模式保留兼容回退。快照 PostgreSQL 不会误走 normalized-only reader，页码/页大小仍受统一上限（页大小最大 200）和整数溢出保护约束。
- 新增 SQLMock、服务层、HTTP 路由和真实 PostgreSQL 16 集成测试，覆盖总数、分页参数、过期状态、活动状态、明文不泄露和 `MaxDevices=1`。本地 Docker PostgreSQL 完成迁移 0021→normalized backfill→集成测试；`autolive-api:20260821-v276` 构建成功（image ID `sha256:232d5b6160540bc38f0ec2b638ba7670ff39016ecb47e41e4e026773614abddf`，约 41.2 MB），真实容器验证 `readyz=200`、`/metrics` 可抓取和 SIGTERM clean shutdown。
- 本轮 `go test ./... -count=1`、`go vet ./...`、`go build ./...`、`govulncheck@v1.7.0` 和 Docker PostgreSQL 16 下 `go test -tags=postgres_integration ./internal/store -count=1` 全部通过；当前 Windows/386 仍不支持 `go test -race ./...`，由 Linux/amd64 CI 提供 Race 证据。完整快照退出、剩余领域读路径、Secret 进程外恢复、供应商权威额度、真实发布 HTTPS 与容量/归档证据仍未关闭。

#### V2.77 本批次补充事实

- BE-011/BE-005 授权摘要读路径已退出 normalized 快照回退：新增 `UserAuthorizationSummaryReader` 与 `postgres_user_authorization_repository.go`，normalized `GET /api/v1/admin/users/{user_id}/authorization-summary` 在同一有界只读事务中直接聚合 `users`、`devices`、`model_leases`、`user_authorization_policies` 和当日 `model_usage_records`；用量只统计服务端已接收的客户端摘要并保留 `client_reported_soft`/`server_recorded_usage_guard` 语义，不宣称供应商权威额度。
- 服务层在 normalized PostgreSQL 缺少该领域能力时 fail-closed，Memory/快照模式继续使用现有兼容状态；SQLMock、服务层和真实 PostgreSQL 16 集成测试覆盖策略、设备/租约计数、UTC 日切、无 lease 用量排除和提交/回滚边界。
- 本轮验证：`go test ./... -count=1`、`go vet ./...`、`go build ./...`、`govulncheck@v1.7.0`、Docker PostgreSQL 16 迁移→回填→`go test -tags=postgres_integration ./internal/store -count=1` 全部通过；Docker 镜像 `autolive-api:20260821-v277` 构建成功，image ID `sha256:070a68a5e71db04aa9cb93c7d41d3f875e482264d1ad4e5fd6792a22c797cef4`（约 41.2 MB），真实容器 `readyz=200`、`/metrics` 含请求指标、SIGTERM clean shutdown。`go test -race ./...` 仍因 Windows/386 不支持，由 Linux/amd64 CI 验证；文档引用脚本在当前 Windows 环境因缺少 `/bin/bash` 未执行。
- 本批次关闭的是授权摘要的快照回退风险；模型池健康调度器的 normalized 读取已在 V2.78 收口，但这仍不等于完整规范化领域退出。Secret Store 进程外恢复/旧密钥归档、供应商权威额度与未来预占、跨存储故障补偿、容量告警/归档、生产 HTTPS/DNS/证书和镜像签名/回滚清单仍是后续长任务。

#### V2.78 本批次补充事实

- BE-018 模型调用摘要查询已补齐受控筛选：provider、model、user_id、device_id、request_id、RFC3339 创建时间范围和 `created_at` 升降序；服务层统一校验长度、时间范围和排序白名单，PostgreSQL 使用参数化 SQL，Memory/快照保持相同结果语义，HTTP/OpenAPI、管理端 URL 状态和管理端生成类型已同步。
- BE-022 连接池观测增加 `autolive_database_pool_saturation_ratio` 与 `autolive_database_pool_saturated`；固定 0.8 饱和阈值、比例钳制到 0～1、无限池为 0，Memory 不伪造数据库连接池指标。
- BE-006/BE-020 PostgreSQL Commit 出口继续统一区分 Context 取消与 `COMMIT_OUTCOME_UNKNOWN`；提交已发出但结果未知时保留 staged Secret，不执行不安全删除。该批次仍未替代真实 Secret Store 进程外故障注入。
- BE-005/BE-016 模型池健康调度器在 normalized PostgreSQL 使用有界模型账号分页读取，不再通过完整快照 `StateOperation`；normalized 缺少 `ModelPoolPageReader` 时 fail-closed，Memory/快照继续使用兼容路径。`ListModelPoolAccounts` 在 normalized 模式也改为分段分页并对无进展读取 fail-fast。
- 本轮验证：Go `test/vet/build`、`govulncheck@v1.7.0`、PostgreSQL 16 tagged store 全量、管理端/桌面端 typecheck/test/build、OpenAPI 生成类型检查、Compose/HTTPS 门禁均通过；`autolive-api:20260821-v278` linux/amd64 镜像构建成功，image ID `sha256:c45426386cbc45523258c83965074509800a5fb23f6e9d512f8c13332b83dcf2`（约 41.2 MB），真实 PostgreSQL 迁移→回填→normalized `readyz=200`、`metrics` 请求量/连接池饱和度指标→SIGTERM clean shutdown 通过。Windows/386 Race 仍需 Linux/amd64 CI；模型调用筛选尚未在带业务数据的真实生产规模 PostgreSQL 上完成 `EXPLAIN (ANALYZE, BUFFERS)`。

#### V2.79 信息盘点与新长任务基线

- 本次盘点范围覆盖服务端专项计划、总计划、PRD、系统/管理系统架构、数据库与密钥约束、OpenAPI、`backend/` 入口和迁移 0001～0021，并以本地 Docker PostgreSQL 16 验收结果作为运行时证据。已确认的服务端能力包括：认证/Refresh/Logout、用户/设备/激活码控制面、模型账号测试/租约/调用摘要、normalized 领域写入第一切片、分页与筛选第一切片、审计 Outbox 第一切片、保留清理、Prometheus 指标、限流、readyz、Go 1.26.6 构建和 Compose 优雅退出。
- P0 未完成（阻止生产切换）：
  1. normalized PostgreSQL 的正式切换仍未完成。当前仍保留 `APP_MODEL_READ_SOURCE` 和 `control_plane_state` 兼容模式；需要生产数据回填前检查、切换/回滚 runbook、删除运行时快照写入路径，并证明所有业务读写与后台任务都不会回退快照。
  2. Secret Store 的进程外故障、进程崩溃恢复、跨 Secret Store/业务事务补偿、提交未知结果后的核对/归档仍未完成。当前只证明了 staged 队列、限量清理和 normalized 事务切片，不能等同于完整密钥生命周期。
  3. 数据库关闭深层故障矩阵仍缺：后台健康探测、保留清理、指标导出器、迁移 textfile collector、连接池关闭、在途事务超时/取消和重启后的恢复需要真实故障证据。
  4. 生产发布证据仍缺：真实证书/DNS、Nginx reload、网络 ACL、HTTPS 统一入口登录与核心业务冒烟、PostgreSQL TLS 联调；镜像签名、迁移版本/回滚清单和发布产物追溯也未闭环。
- P1 未完成（当前 PRD/控制面仍有功能或证据缺口）：
  1. 供应商权威额度、短期可撤销凭证、严格额度扣减和“活动租约数”与“供应商活动请求数”的语义尚未落地；`client_reported` 只能作为软额度，不能宣称精确计费。Provider 目前只是账号上的字符串维度，是否建立独立实体、CRUD、启停和引用约束尚未决策。
  2. 健康调度器虽然已改为 bounded page reader，但当前 normalized 页按账号 ID 取首批后再过滤 inactive/cooldown/missing-secret；当首批被不可探测账号占满时，可用账号会被延后。这需要专用的“可探测账号”过滤/游标查询和跨实例长时故障验收，不能只以“没有完整快照”作为完成标准。
  3. 审计 Outbox 的 Memory/快照兼容语义、全部领域的同事务覆盖、重复投递/归档/TTL 运行验证和真实连接故障矩阵仍不完整。
  4. 查询与容量证据仍不足：需要带代表性业务数据的 `EXPLAIN (ANALYZE, BUFFERS)`、Benchmark/pprof、连接池饱和告警阈值、保留归档策略及迁移指标文件 collector 的挂载/权限/重启验证。
  5. 激活码、租约、会话、用量的真实跨进程恢复与长期压力矩阵仍需持续执行；本地 Docker 已覆盖的提交前断开、提交后响应丢失、锁等待取消和并发边界不能替代生产规模测试。
- P2 未完成（规模化运维）：容量告警规则、数据归档/备份恢复演练、镜像签名与供应链策略、发布回滚演练、细分后台任务指标和完整生产观测接入。
- 明确不列入当前版本的延期项：实时话术幻化及 speech-to-speech/VAD/ASR/LLM/TTS、Rust 真实模型直连与供应商凭证委托、旧媒体任务/版本队列、服务端模型正文代理、RTMP/OBS 和本地研究分析。恢复这些能力前必须先更新 PRD、架构、OpenAPI、安全模型和独立验收计划。

V2.80 后续执行顺序：先完成 Secret/数据库故障与 normalized 切换退出，再完成真实 HTTPS/发布回滚证据；随后处理供应商额度与 Provider 边界、真实数据量性能基线、审计归档和容量告警。任何一项只有单元测试或 SQLMock 证据时，继续保持“部分关闭”，不得改为“已完成”。

#### V2.80 本批次补充事实

- BE-016 健康调度候选读取已收口：新增 `ModelPoolHealthPageReader`，normalized PostgreSQL 在 SQL 层先过滤 `disabled`、缺失 Secret、仍在 cooldown 和当日额度耗尽账号，再按 `id` 有界 `LIMIT` 返回候选；服务层缺少专用 Reader 时 fail-closed，避免不可探测账号占满首批分页而延后可用账号。Memory/快照继续保留兼容路径。
- 新增服务层、SQLMock、真实 PostgreSQL 16 集成测试，覆盖专用 Reader 路由、缺失 Reader 拒绝、过滤条件在 LIMIT 前生效及可用账号返回；未修改 HTTP/OpenAPI 契约，不引入新依赖。
- 本批次验证：Go `test/vet/build`、`govulncheck@v1.7.0`、PostgreSQL tagged store 全量、管理端/桌面端 typecheck/test/build/api:check、OpenAPI/Compose/HTTPS/后端发布门禁均通过；Docker 镜像 `autolive-api:20260821-v280` 构建成功，digest 为 `sha256:840219d4d4fef8126f6aa4b6bd2a12550c8f60cb300067ed1ba887b6da4a873a`，真实 PostgreSQL 迁移→回填→normalized readyz/metrics→SIGTERM clean shutdown 冒烟通过，临时容器/网络已清理。
- `go test -race ./...` 在当前 Windows/386 不支持，仍需 Linux/amd64 CI；真实生产证书/DNS/TLS、Secret 进程外恢复/旧密钥归档、供应商权威额度、真实规模性能/容量告警、审计归档、镜像签名/回滚和 normalized 正式切换仍未关闭。

#### V2.81 本批次补充事实

- 为 normalized 正式切换新增只读准入命令 `autolive-verify-normalized-cutover`：要求 PostgreSQL、`APP_MODEL_READ_SOURCE=normalized` 和显式 `APP_NORMALIZED_CUTOVER_CONFIRM=YES`，检查迁移版本、`normalized_backfill_state=completed`、关键规范化表存在且可读，并拒绝存在缺失 Secret 引用的模型账号；失败不修改数据库，输出不包含 URL、SecretRef、用户名或业务 payload。
- 新增 `PostgresRepository.VerifyNormalizedCutover`、命令单元测试和 SQLMock 回归；Dockerfile 同时构建并发布该只读检查工具，PostgreSQL CI 在回填后执行同一准入检查。
- 本轮本地 Docker PostgreSQL 16 证据：迁移成功、normalized backfill 成功、只读准入检查通过；同一镜像启动 normalized API 后 `readyz=200`、`/metrics` 含请求/连接池指标、SIGTERM clean shutdown 通过。镜像 `autolive-api:20260821-v281` 构建成功，image ID `sha256:a5ae6ae18c7f0006a97a6c8610fc78f685ff2b9aaccc860f458c65c35068e1ac`。Go 全量 test/vet/build/govulncheck、发布门禁和命令测试通过。
- 该切片只提供切换前阻断检查，不代表已经切换生产默认读源；快照退出、Secret 进程外恢复/旧密钥归档、数据库关闭矩阵、真实 HTTPS/TLS、供应商权威额度、真实规模性能/容量告警、审计归档、镜像签名/回滚仍未关闭。Race 仍需 Linux/amd64 CI。

#### V2.82 本批次补充事实

- normalized 正式切换增加生产配置安全门禁：`APP_DEPLOYMENT_ENV=production` 拒绝 `APP_MODEL_READ_SOURCE=snapshot`，要求生产进程显式使用 `normalized`；开发/测试环境仍可保留快照兼容路径。
- `deploy/.env.example` 已切换为生产 `APP_MODEL_READ_SOURCE=normalized`，并保留“迁移→一次性回填→只读准入检查→启动 API”的显式操作顺序；未完成回填时 API readiness 继续 fail-closed，不自动回退快照。
- 本地 Docker PostgreSQL 16 与真实生产 Compose 16 验收：生产快照配置被拒绝；Compose 迁移容器 `Exited(0)` 后执行回填和准入检查，normalized API `Healthy`、容器内 `readyz=200`、`/metrics` 可抓取，SIGTERM 日志出现 `shutdown signal received` 与 `http server stopped cleanly`。镜像 `autolive-api:20260821-v282` image ID `sha256:57d54b169f1e47e4514a5b8009eaf90b37fdc871436adbd41f76e892258fcaac`。
- 该切片只关闭“生产配置不能继续以快照启动”的安全缺口；正式切换 Runbook、快照运行时写入路径删除、Secret/数据库故障矩阵、真实 HTTPS/TLS、供应商额度、规模性能、审计归档、镜像签名和回滚仍未关闭。

#### V2.83 本批次补充事实

- BE-005/BE-018 管理租约详情已退出 normalized 快照回退：新增 `ModelLeaseDetailReader`，PostgreSQL 使用显式回填门禁和参数化单行 SQL 读取脱敏租约生命周期；过期状态按服务端时钟派生，不写回 `control_plane_state`。
- normalized 服务层在缺少该领域读接口时 fail-closed，新增服务层无 `StateOperation` 回退测试；SQLMock 覆盖有界查询、过期派生和错误边界；真实 Docker PostgreSQL 16 的迁移→回填→集成测试通过。
- 本切片未宣称完整快照退出：其他仍使用兼容 `StateOperation` 的领域路径、正式切换 Runbook、Secret/数据库故障矩阵、真实 HTTPS/TLS、供应商额度、规模性能、审计归档、镜像签名和回滚仍需继续收口。

#### V2.84 本批次补充事实

- normalized 列表服务边界已补齐 fail-closed：用户、设备、激活码、审计、用量、租约和模型账号的兼容列表入口在缺少规范化分页 Reader 时不再调用 `StateOperation`；新增回归验证 `Run` 不被调用。已有 PostgreSQL Reader 不改变 HTTP 契约和分页语义。
- normalized staged Secret 清理已增加专用 `NormalizedStagedSecretCleaner`：PostgreSQL 在同一短事务内取得 normalized advisory lock，使用 `NOT EXISTS (model_accounts.secret_ref)`、固定 rotation 前缀、cutoff、`LIMIT` 和 `FOR UPDATE OF ... SKIP LOCKED` 删除孤儿密文，绕过完整 `loadNormalized`/`syncReferenceRows` 兼容状态路径；缺少该能力时服务层 fail-closed，Memory/快照继续使用原有 `PendingSecretCleanup` 兼容回退。
- 新增 SQLMock、服务层和真实 PostgreSQL 16 重启式集成证据：重构 SecretStore/Repository 后，active rotation 密文保留、过期 orphan staged 密文删除，第二次清理返回 0；迁移 0021→backfill→定向 tagged 测试通过。该证据不等于 Secret 进程外断连、`pg_terminate_backend`、提交未知结果或所有跨存储补偿已完成。
- V2.84 本地真实 Compose 收口：迁移容器 `Exited(0)`，显式传入 `APP_STORAGE_MODE=postgres` 与 `APP_MODEL_READ_SOURCE=normalized` 后完成 migration 21→backfill completed→API `Healthy`；容器内 `readyz=200`。`/metrics` 实际首段包含 `autolive_audit_write_failures_total`、`autolive_database_idle_connections`、`autolive_database_in_use_connections`、`autolive_database_max_open_connections`、`autolive_database_pool_saturated`、`autolive_database_pool_saturation_ratio`、`autolive_database_wait_count_total`、`autolive_http_request_duration_seconds` 等固定低基数指标，响应同时包含 `autolive_http_requests_total` 与 `go_info{version="go1.26.6"} 1`；SIGTERM 后 API 容器退出码为 `0`、`OOMKilled=false`，日志出现 `shutdown signal received` 和 `http server stopped cleanly`。临时 PostgreSQL/网络已清理，镜像 `autolive-api:20260821-v284`（digest `sha256:089a97bccbefe3312668ed483e25ebaea776094a1e2cdc7d004dfc558be8a5b68`）保留。
- 本批次仍未关闭：剩余 normalized 写路径的领域接口 fail-closed、正式切换 Runbook/快照运行时路径删除、Secret/数据库深层故障矩阵、真实 HTTPS/TLS、供应商额度、规模性能、审计归档、镜像签名和回滚。

#### V2.85 本批次补充事实

- BE-005/BE-024 normalized 设备变更服务边界已收口：激活、心跳、禁用、解绑在缺少 `TransactionalDeviceActivator`、`TransactionalHeartbeatRecorder` 或 `DeviceLifecycleRepository` 时返回明确能力缺失错误，不再回退 `StateOperation`；取消/截止检查优先于能力检查，Memory/快照兼容路径保持不变。
- 新增服务层回归 `TestNormalizedDeviceMutationsFailClosedWithoutTransactionalRepositories`，覆盖无专用 Repository 时六类入口均不调用 `Run`；全量 Go test/vet/build/govulncheck 通过。
- 本地 Docker PostgreSQL 16 验收：migration→normalized backfill→`postgres_integration` 全量通过；镜像 `autolive-api:20260821-v285`（digest `sha256:84583199be9f9cc08955b6eeade49cd7201198d1200350b8521f09e93670ffa0`）启动 normalized API，`readyz=200`、`/metrics` 含数据库池/HTTP/审计指标，SIGTERM 后退出码 `0`、`OOMKilled=false`，日志出现 `shutdown signal received` 与 `http server stopped cleanly`；临时 PostgreSQL/网络已清理。
- 本批次仍未关闭：其余 normalized 写路径的领域接口 fail-closed、正式切换 Runbook/快照运行时路径删除、Secret 外部故障/提交未知/跨存储补偿/旧密钥归档、数据库与后台深层故障矩阵、真实 HTTPS/TLS、供应商额度、规模性能、审计归档、镜像签名和回滚。

#### V2.86 本批次补充事实

- BE-005/BE-017 normalized 租约写路径已收口：创建、续租、客户端释放和管理员回收在缺少 `ModelLeaseCreator` 或 `ModelLeaseRepository` 时明确失败，不再回退 `StateOperation`；PostgreSQL 现有领域事务实现和 Memory/快照兼容路径保持不变。
- 新增服务层回归 `TestNormalizedLeaseMutationsFailClosedWithoutDomainRepositories`，覆盖四类租约写入口均不调用 `Run`；全量 Go test/vet/build/govulncheck 通过。
- 本地 Docker PostgreSQL 16 验收：migration→normalized backfill→`postgres_integration` 全量通过；镜像 `autolive-api:20260821-v286`（digest `sha256:6d8eb1a5aa628741a8dac3ef8a85df41f993b228d79d5864fe1ee6ee50f6808a`）启动 normalized API，`readyz=200`、`/metrics` 含数据库池/HTTP/审计指标，SIGTERM 后退出码 `0`、`OOMKilled=false`，日志出现 `shutdown signal received` 与 `http server stopped cleanly`；临时 PostgreSQL/网络已清理。
- 本批次仍未关闭：模型账号/Secret、用量、激活码等其余 normalized 写路径的领域接口 fail-closed，正式切换 Runbook/快照运行时路径删除，Secret 外部故障/提交未知/跨存储补偿/旧密钥归档，数据库与后台深层故障矩阵，真实 HTTPS/TLS，供应商额度，规模性能，审计归档，镜像签名和回滚。

#### V2.87 本批次补充事实

- BE-005/BE-014/BE-017 normalized 写路径继续收口：模型账号创建/禁用/编辑、激活码创建/作废、客户端调用摘要在缺少 `ModelPoolAccountCreator`、`ModelPoolRepository`、`ActivationRepository` 或 `ModelUsageRepository` 时明确失败，不再回退 `StateOperation`；PostgreSQL 领域实现和 Memory/快照兼容路径保持不变。
- 新增服务层回归 `TestNormalizedModelAccountMutationsFailClosedWithoutDomainRepositories`、`TestNormalizedActivationMutationsFailClosedWithoutRepository`、`TestNormalizedUsageMutationFailsClosedWithoutRepository`，验证入口不调用 `Run`；全量 Go test/vet/build/govulncheck 通过。
- 本地 Docker PostgreSQL 16 验收：migration→normalized backfill→`postgres_integration` 全量通过；镜像 `autolive-api:20260821-v287`（digest `sha256:e6aebe180a072e3f541812d791925a6e82dd4dd10395364c6a9aad262863af0c`）启动 normalized API，`readyz=200`、`/metrics` 含数据库池/HTTP/审计指标，SIGTERM 后退出码 `0`、`OOMKilled=false`，日志出现 `shutdown signal received` 与 `http server stopped cleanly`；临时 PostgreSQL/网络已清理。第一次验收因端口映射瞬时 EOF 未建表，已通过主机连接重试复跑成功，不计入代码失败。
- 本批次仍未关闭：模型 Secret 轮换前置快照读取、其余 normalized 写路径、正式切换 Runbook/快照运行时路径删除、Secret 外部故障/提交未知/跨存储补偿/旧密钥归档、数据库与后台深层故障矩阵、真实 HTTPS/TLS、供应商额度、规模性能、审计归档、镜像签名和回滚。

#### V2.88 本批次补充事实

- BE-002/BE-005 normalized 兼容写入口继续收口：`RecordAudit`、`UpdateUserAuthorization` 和历史 `EnsureLocalAdmin` 在 normalized 模式缺少专用能力时直接 fail-closed，不再调用 `StateOperation`；管理员启动统一使用带持久化凭证的 `EnsureConfiguredAdmin`，Memory/快照兼容入口保持不变。
- BE-015 Secret 轮换前置读取已退出快照：新增 `ModelPoolSecretRotationPreparer`，PostgreSQL 在短只读事务内通过回填门禁读取账号、活动 SecretRef、账号派生摘要和幂等事实；服务层要求准备器与事务型 `ModelPoolSecretRotator` 同时存在，完成外部模型探测后提交，不再先执行完整 `withState`，也不再在 normalized 模式回退 staged `StateOperation`。
- 新增服务层回归 `TestNormalizedCompatibilityMutationsFailClosedWithoutDomainRepositories`、`TestNormalizedSecretRotationPreparationDoesNotUseStateOperation`，以及 SQLMock `TestPostgresRepositoryPreparesNormalizedModelSecretRotationWithoutSnapshot`；服务层/存储定向测试通过。
- 全量验证通过：`go test ./... -count=1`、`go vet ./...`、`go build ./...`、`govulncheck@v1.7.0`（无可调用漏洞）；本地 Docker PostgreSQL 16 完成 migration 0021→normalized backfill→cutover preflight→`postgres_integration` 全量测试，严格 cutover 检查在集成测试前执行以避免故障夹具遗留的缺失 Secret 引用影响准入检查。
- Docker 镜像 `autolive-api:20260821-v288` 构建成功，digest `sha256:f5657940af548655120105f245f252ee5d517aabe79a89f1d77372c0566fff8d`；normalized API `readyz=200`，`/metrics` 实际包含审计、数据库池饱和度、HTTP 延迟和 Go runtime 指标；SIGTERM 后退出码 `0`、`OOMKilled=false`，日志出现 `shutdown signal received` 与 `http server stopped cleanly`，临时 PostgreSQL/网络已清理。
- 本批次仍未关闭：normalized 正式切换 Runbook、其余运行时快照写入/删除和回滚证据；Secret 外部进程断连、提交未知结果核对、跨存储补偿和旧密钥归档；数据库/后台任务/指标 collector 深层故障矩阵；真实 HTTPS/DNS/TLS、供应商权威额度、规模性能/容量告警、全领域审计归档、镜像签名和可回滚发布清单。

#### V2.89 本批次补充事实

- BE-006/BE-015/BE-020 Secret 轮换故障矩阵新增真实 PostgreSQL 16 证据：提交响应在服务端已确认 `COMMIT` 后被测试代理丢弃时，normalized 轮换返回 `COMMIT_OUTCOME_UNKNOWN`，旁路核对确认新 active SecretRef/密文、测试结果、幂等记录和审计 Outbox 均已持久化；重建 Repository/SecretStore 后使用原幂等键重放返回已提交结果，不产生第二份事实。另在 normalized advisory lock 等待期间使用 `pg_terminate_backend` 强制断开，轮换返回有界连接错误而非未知提交，旧密文保留且候选、测试、幂等和 Outbox 均无残留。
- 新增 tagged 集成测试 `TestPostgresNormalizedSecretRotationCommitResponseLossIsRecoverable` 与 `TestPostgresNormalizedSecretRotationConnectionTerminationBeforeCommitLeavesNoResidue`，复用既有提交响应丢失代理和锁等待观测工具；生产代码未引入测试代理或外部依赖。
- 本地 Docker PostgreSQL 16 已执行 migration 0021→normalized backfill→上述两个故障测试，目标测试 `2/2 PASS`，临时容器已清理。该证据关闭的是 normalized SQL Secret 轮换的提交前连接断开与提交后响应丢失核对切片，不等于外部 Secret 进程宕机、跨存储补偿、旧密钥归档或正式 normalized 切换完成。
- 本批次镜像 `autolive-api:20260821-v289` 构建成功，digest 为 `sha256:abdbfe9ccea4a854d5031aa24fba6bcbff9c1ab89b1fe5d83c56e21bbf5648ba`；真实 Docker PostgreSQL 16 完成 migration→backfill→cutover preflight→normalized API，`readyz=200`、`/metrics=200`，实际抓取包含 `autolive_database_pool_saturation_ratio`、`autolive_http_requests_total` 和 `go_info`；SIGTERM 后 API 退出码 `0`、`OOMKilled=false`，日志出现 `shutdown signal received` 与 `http server stopped cleanly`，临时容器/网络已清理。第一次脚本仅因关闭日志正则读取方式未记录为通过，随后直接读取容器日志复跑通过。

- BE-001：已关闭。HTTP 激活码测试改用注入的固定测试时钟，移除过期绝对日期；`go test ./...` 已通过。
- BE-002：部分关闭。`APP_MODEL_READ_SOURCE=normalized` 现在仅允许 PostgreSQL，并由迁移 0013 提供规范化设备展示字段和模型测试结果表；Repository 已在事务内读取规范化领域表并以 advisory lock 串行读改写，不再静默回退快照。快照到规范化表回填、完整领域 Repository 和真实 PostgreSQL 跨进程证据仍待补齐。
- BE-008：部分关闭。服务端对合法 `X-Request-Id` 做长度/字符集校验并透传，非法值重新生成；解绑设备使用不含 `user_id` 的独立响应；模型账号创建 `status` 已加入 OpenAPI；桌面端和管理端统一使用 `application/json`、`X-Request-Id`、`request_id`、`code/message/details` 错误结构；契约测试从 OpenAPI 读取全部路径/方法并请求真实 Router，阻止声明路径落到 404/405，校验所有本地 `$ref` 指向已声明的 component，解析 `NewError` 的 HTTP 状态与错误码文档逐项比较，校验显式 Go DTO 的 properties/required/nullable 与 JSON tag 语义，并验证共享错误响应示例和 400/401/403/404/405/408/409/429/500/502/503/504 运行时矩阵；40 个操作已显式声明 405/408/500；管理端生成类型由 `openapi-typescript --check` 和 CI `frontend-contract` 作业锁定。真实运行时状态和桌面端生成类型消费仍待继续收口。
- BE-024：部分关闭。normalized PostgreSQL 激活路径已由 `TransactionalDeviceActivator`、心跳路径已由 `TransactionalHeartbeatRecorder`、设备禁用/解绑路径已由 `DeviceLifecycleRepository` 在同一事务锁定会话/设备并提交业务状态、幂等、租约释放和会话撤销；激活/心跳成功审计事件已随业务事务进入 Outbox；旧快照/Memory 保留 `TransactionalSessionBinder` 或 StateOperation 和精确有界补偿。新增 SQLMock、服务路由以及本地 Docker PostgreSQL Session 冲突/锁等待取消测试；强制连接断开、提交未知结果矩阵和其他业务审计同事务仍待完成。
- BE-025：已关闭。模型账号通用 PATCH 禁用路径复用活动租约冲突规则，并有回归测试。
- BE-003：已关闭。模型账号连通性测试现在在请求前、实际拨号和每次重定向前校验协议、端口、DNS 解析结果和 IP 分类；默认拒绝明文 HTTP，只有服务启动配置显式允许时放行，公开控制面构造器也保持该安全默认值；禁用代理，限制重定向与响应体大小，并要求 2xx 响应包含目标模型。测试覆盖回环/私网、私网重定向、非法 JSON、超大响应、模型不存在和未显式允许的 HTTP。
- BE-007：部分关闭。新增 `/api/v1/livez` 与 `/api/v1/readyz`，并通过 `APP_REQUEST_TIMEOUT` 将请求 Context 预算配置化；超时/取消保持结构化 JSON 错误，Dockerfile 和 Compose 健康检查统一使用 `/api/v1/readyz`。PostgreSQL Repository、规范化回填、SQL Session Store、Secret Store 和模型健康探测现在使用有界事务/语句/探测预算，超时统一返回 Context 错误；健康探测与保留清理遥测区分 `timeout`/`cancelled`，迁移命令独立预算并记录成功/失败 `duration_ms`；健康探测拥有取消路径并在优雅关闭时等待退出；`APP_SHUTDOWN_TIMEOUT` 与 Compose `AUTOLIVE_STOP_GRACE_PERIOD` 已配置对齐；迁移命令可选生成原子 text exposition 指标文件，指标文件发布失败会阻止迁移成功退出。CI 与本地 Docker 已验证真实 SIGTERM clean-exit；在途请求/数据库关闭深层故障及指标文件真实 collector 消费/权限与重启故障矩阵仍待补齐。
- BE-004：已关闭。PostgreSQL 首次引导拒绝缺失/占位管理员配置并写入 bcrypt 哈希；新增仅本地管理员可调用的 `POST /api/v1/admin/auth/change-password`，使用幂等键轮换持久化密码并撤销全部管理员会话；轮换后可省略静态管理员密码环境变量，启动前通过持久化管理员和 `/readyz` 门禁，初始化失败主进程非零退出。
- BE-010：部分关闭。已增加用户编辑、密码重置、启用/禁用状态转换、最后有效管理员保护和管理端表单；重置/禁用/编辑后会撤销该用户会话，模型 allowlist/服务端记录用量门禁已另在 BE-011/017 接通，供应商权威额度和语义化审计仍待补齐。
- BE-012：部分关闭。已增加服务端按最后心跳派生 `online`、设备详情接口、管理端详情抽屉和回归测试；设备禁用/解绑后的联动已接入，真实 PostgreSQL 事务和重激活验收仍待补齐。
- BE-013：部分关闭。normalized PostgreSQL 禁用/解绑已在设备生命周期事务中释放活动租约、撤销设备 Session 并更新状态，HTTP normalized 路径跳过重复撤销；Memory/快照仍由服务 StateOperation 后走 Session Store 补偿，真实存储故障和重激活验收仍缺失。
- BE-014：部分关闭。激活码列表现在返回脱敏前缀、核销用户、核销设备和核销时间，明文仅创建响应返回；激活/作废后内存状态也清除明文，normalized PostgreSQL 激活路径已将核销、设备注册和会话绑定置于单事务，新增 SQLMock、服务路由、内存并发和 PostgreSQL 过期/作废/重复核销/幂等冲突/Session 冲突与取消等待回归。真实 PostgreSQL 提交未知结果和语义化审计仍待补齐。
- BE-015：部分关闭。新增 `POST /api/v1/admin/model-pool/{account_id}/rotate-secret`，新密钥先通过受限模型列表校验，成功后写入 Secret Store；幂等重放不重复探测且失败保留旧密钥，staged reference 已按每次尝试随机唯一化，不同幂等键不能覆盖已提交的密钥，竞争失败方不会删除成功方密钥；新增按 rotation 前缀、更新时间和当前账号引用集合保护的有界 staged secret 回收。Secret Store 写入与业务幂等/测试结果仍未形成跨存储原子提交，旧密钥故障补偿和真实 PostgreSQL 故障注入待补齐。
- BE-016：部分关闭。失败/超时连通性测试进入持久化 5 分钟 `cooldown`，成功测试恢复，`exhausted` 在新的 UTC 日按同一服务端时钟恢复；新增 0014 `cooldown_until` 字段、OpenAPI/管理端类型和注入时钟回归测试。新增后台调度器和 `APP_MODEL_HEALTH_PROBE_*` 预算配置，支持每轮账号/并发上限、探测超时、取消、失败计数与确定性抖动退避，成功会清除退避状态；PostgreSQL 使用会话级 advisory lock 防止多实例重复探测，锁忙时跳过本轮并记录固定指标，供应商权威额度和更细故障矩阵仍待补齐。
- BE-019：部分关闭。0015 新增审计 `outcome/status_code/error_code` 字段，0019 新增审计 Outbox；已认证写请求和登录成功/失败按路径记录目标类型、资源 ID、结果、状态码和统一错误码，客户端激活/心跳通过已验证会话绑定补齐设备目标 ID，激活/心跳成功事件在业务事务内入 Outbox，管理端展示结果和状态码，敏感请求正文仍不入库；审计列表已支持结果/归属/动作/目标/request_id/时间窗口筛选和创建时间排序白名单，审计写失败已暴露低基数计数器且使用注入 Logger；关键 API 写响应采用缓冲后审计，失败统一 `503/AUDIT_UNAVAILABLE`，管理端/桌面端按同一幂等键各重试一次；审计记录已有有界保留清理接口。其他业务域同事务、未知提交结果处理和归档仍待补齐。
- BE-022：部分关闭。使用官方 Prometheus Go 客户端增加独立 registry 和 `/metrics`，已覆盖 HTTP 请求量/状态码/延迟/在途请求、PostgreSQL 连接池等待、限流命中、审计写失败，以及 `autolive_model_accounts{status=...}`、`autolive_model_leases_active`、`autolive_model_usage_tokens_today`、`autolive_user_authorization_policies_configured` 和 `autolive_model_pool_health_*` 等低基数聚合；迁移命令另提供可选原子 text exposition 文件，包含固定 `status`、耗时、超时预算和目标版本；标签只使用固定方法、路由模板、有限策略名、健康状态/跳过原因或无标签，部署时应由内网抓取或反向代理 ACL 限制指标端点，不将其作为公网业务入口；供应商额度、跨实例租约、后台任务细分指标、容量告警和迁移文件真实 collector 消费/权限故障矩阵仍待补齐。
- BE-026：已关闭。模块语言版本保持 `go 1.22`，本地/CI/Docker builder 固定 Go `1.26.6`；`go test`、`go vet`、`go build` 和 `govulncheck@v1.7.0` 通过，Linux/amd64 backend/admin 镜像已在本地 Docker Desktop 构建并完成迁移→回填→normalized readyz/metrics→Compose 健康与 SIGTERM clean shutdown 验收；Race 仍由 Linux/amd64 CI 执行。
- BE-027：已关闭。使用 `golang.org/x/time/rate` 增加进程内 token-bucket 限流，覆盖登录、Refresh、模型连通性测试、密钥轮换和管理员密码轮换。登录拆为单账号 burst 20/每 2 秒补 1 个与来源地址 burst 60/每秒补 1 个；模型测试/密钥轮换保持 burst 5、每 2 秒补 1 个，Refresh burst 10、每秒补 1 个，密码轮换 burst 2、每 30 秒补 1 个。登录另有 PostgreSQL 账号/来源地址持久摘要桶，分别从第 5/50 次失败起退避；全局最多保留 4096 个进程桶、空闲 10 分钟淘汰，返回 `Retry-After` 和 `429/RATE_LIMITED`。只有可信代理边界才解析转发地址，不宣称进程令牌桶本身是分布式限流。
- BE-024 执行批次：部分关闭。激活/心跳会话绑定前置；业务失败只补偿本请求新建的设备绑定，SQL 会话更新检查影响行数；normalized 激活/心跳成功审计已随业务事务写入 Outbox。新增真实 PostgreSQL Session 设备冲突与 advisory-lock 等待取消测试并验证零残留；强制连接断开、提交未知结果和其他业务域 Outbox/同事务方案仍待补齐。
- 管理端安全页：已落地 `/security` 路由和管理员密码轮换表单，使用统一 `apiClient`、幂等键和成功后会话清理；密码修改后回到登录页，503 会保留幂等键供原请求重试。
- HTTP 契约门禁：已新增 `backend/internal/httpapi/contract_test.go`，使用 `gopkg.in/yaml.v3` 读取 `接口契约/openapi.yaml`，逐个调用实际 Router 并拒绝 404/405；该检查随 `go test ./...` 执行。
- HTTP 契约门禁增强：同一测试同时遍历 paths 与 components，拒绝指向不存在 `#/components/*` 的本地 `$ref`；管理端、桌面端的 JSON 请求头与错误包络继续由各自契约测试覆盖。
- BE-005：部分关闭。新增 0012 调用摘要唯一索引和 0013 规范化字段/模型测试表；`normalized` 模式已具备规范化表读取、写入同步、事务级 advisory lock 和用户/设备 SQL 分页第一切片测试；用户创建、编辑、密码重置、禁用四条写路径、授权策略、激活码创建/作废、激活码核销+设备注册+会话绑定、心跳+设备+会话绑定以及设备禁用/解绑+租约释放+会话撤销已通过最小领域边界直接写规范化用户/策略/激活码/设备/会话/租约与幂等表，并覆盖回放、指纹冲突、用户名唯一冲突、最后管理员保护、密码更新、本地管理员错误、策略 JSONB 更新和明文不落库。模型/租约/用量/审计其余写入、快照回填、其余列表分页筛选、跨进程并发和故障注入仍未关闭。
- BE-011：部分关闭。新增 `GET /api/v1/admin/users/{user_id}/authorization-summary` 与幂等 `PATCH /api/v1/admin/users/{user_id}/authorization`；迁移 0016 保存用户模型 allowlist 和服务端记录用量门禁，管理端抽屉可配置，租约申请校验模型授权并返回稳定错误码。空 allowlist 表示允许所有已登记模型；供应商权威硬额度、未来预占和完整语义化审计仍待补齐。
- BE-018：部分关闭。用户、设备、按用户设备、调用摘要、审计、模型账号和租约列表已把 `page/page_size` 传入服务层；normalized PostgreSQL 通过覆盖检查后执行有界 `COUNT + LIMIT/OFFSET`，模型账号页派生活动租约、UTC 日用量和最近测试结果，管理员租约页返回脱敏归属/生命周期摘要；租约列表已在 SQL/内存回退统一支持状态、供应商、模型、用户、设备、账号筛选及到期/状态/供应商模型排序，新增单租约脱敏详情和管理员幂等回收，快照/内存模式保留兼容回退；0017/0021 已补齐首轮账号/租约/调用摘要/审计筛选索引，PostgreSQL 计划可见性测试已加入，其余列表筛选和真实数据量复核仍待补齐。
- 2026-08-21 租约查询批次：新增 `ModelLeaseAdminSummary`、内存/normalized PostgreSQL 有界分页和活动过期状态派生；新增 `GET /api/v1/admin/model-leases`、OpenAPI `ModelLeaseListResponse` 以及管理端 `/model-leases` 路由页面。租约详情/回收已在后续批次接通，并发故障验收仍保留在 backlog。
- 2026-08-21 租约筛选批次：在同一租约列表 API 上增加 `status/provider/model/user_id/device_id/account_id/sort` 白名单参数；normalized PostgreSQL 在 SQL 层绑定状态（含活动过期派生）、归属筛选和四种排序，内存/快照回退保持同一结果语义；管理端使用 Ant Design 状态/排序选择和搜索框，OpenAPI、SQLMock、服务层和 HTTP 400/200 回归已同步。租约详情/回收、索引策略和跨实例并发故障验收仍保留在 backlog。
- 2026-08-21 审计查询批次：在 `GET /api/v1/admin/audit-logs` 增加操作者、设备、动作、目标类型、结果、错误码、request_id、RFC3339 时间窗口和创建时间排序白名单；normalized PostgreSQL 使用参数化 SQL，内存/快照回退保持同一结果语义，管理端增加 Ant Design 筛选/排序控件，OpenAPI、SQLMock、服务层和 HTTP 400/200 回归已同步。审计 Outbox、保留清理和索引策略仍保留在 backlog。
- 2026-08-21 租约详情/回收批次：新增 `GET /api/v1/admin/model-leases/{lease_id}` 脱敏生命周期详情和带 `Idempotency-Key` 的 `POST /api/v1/admin/model-leases/{lease_id}/reclaim` 管理员回收；记录创建/回收时间，内存/快照/normalized PostgreSQL 读写保持一致，管理端详情 Modal、回收原因、OpenAPI、审计目标和服务/HTTP 回归已同步；跨存储事务故障注入和完整索引仍待补齐。
- 2026-08-21 BE-022/BE-019 指标批次：限流中间件新增按固定策略名计数的 `autolive_http_rate_limited_total`，审计写入失败新增无标签 `autolive_audit_write_failures_total`；Router 统一向审计中间件注入 `slog.Logger`，去除生产路径对全局 Logger 的依赖，并增加限流/指标回归测试。账号健康、租约、额度指标、审计 Outbox/fail-closed、保留清理和真实 PostgreSQL/跨实例验证仍待补齐。
- 2026-08-21 BE-020 集成测试批次：PostgreSQL 16 CI service 注入 `TEST_POSTGRES_URL`；normalized round-trip 在重新构造 Repository 后验证重启式读取，新增外键约束失败回滚、Session 冲突、advisory-lock 等待取消和清理边界测试，确认同一事务中的用户变更不会部分提交且取消不残留；本地 Docker PostgreSQL 16 全量 tagged store 通过，强制连接断开/提交未知结果和 Secret 进程外故障矩阵仍待补齐。
- 2026-08-21 BE-020 会话/跨实例集成批次：在 PostgreSQL 集成标签下新增 SQL Session Store 的创建→重新构造→Refresh Rotate→旧 Refresh 重放拒绝→再次重构读取新会话验证；另以两个独立 normalized Repository 实例并发竞争同一幂等键，验证 advisory lock 后只有一个用户创建者且另一事务观察已提交记录；测试使用唯一用户/哈希并按记录后用户清理。本机缺少 `TEST_POSTGRES_URL` 时按预期跳过，CI PostgreSQL 16 service 执行真实路径。
- 2026-08-21 Phase 6 关闭路径修复：服务已启动后 HTTP 异常、Shutdown 超时、后台任务停止失败和最终服务错误分支在非零退出前显式关闭数据库运行时，避免 `os.Exit` 跳过 deferred close；本地真实 Compose 已完成 migrate→healthy→SIGTERM clean shutdown，停止接收新请求/在途事务和数据库关闭深层故障矩阵仍待补齐。
- 2026-08-21 BE-021/BE-018 索引迁移批次：新增前向迁移 0017，为会话过期/用户设备撤销、幂等与测试结果保留清理、模型账号/租约筛选、调用摘要和审计 request_id 查询增加 `IF NOT EXISTS` 索引；迁移目录门禁已更新至 0017，未改历史迁移或数据。
- 2026-08-21 BE-015 staged secret 并发与恢复批次：密钥轮换每次尝试使用随机唯一 staged reference；提交事务校验 SecretRef 未被其他幂等键切换，只有实际提交事务的竞争者回收旧密钥，重复幂等或提交失败方只回收自己的 staged secret；新增 Secret Store 清理接口，后台按 1 小时保护窗口、rotation 前缀、批次上限和当前账号引用集合回收进程崩溃遗留候选，Memory 模式无持久化清理；新增 SQLMock、服务层保护集合和调度器回归。跨存储原子提交、旧密钥故障补偿和真实 PostgreSQL 故障注入仍待补齐。
- 2026-08-21 BE-016 跨实例健康调度锁批次：新增 Repository advisory-lock 边界；PostgreSQL 在专用连接上执行 `pg_try_advisory_lock`，锁持有期间覆盖整轮探测并在取消/关闭路径调用 `pg_advisory_unlock` 后归还连接，锁忙时跳过本轮并记录 `distributed_lock` 固定低基数标签；新增 SQLMock、调度器忙锁和 PostgreSQL 双 Repository 集成回归。供应商额度和长时故障矩阵仍待补齐。
- 2026-08-21 BE-014/BE-019 激活审计批次：`ActivateDevice` 在 MemoryStore 并发读改写下验证单次核销只产生一个设备，另一请求稳定返回 `ACTIVATION_CODE_USED`；成功请求沿用同一幂等键可稳定重放，激活/作废后清除内存记录中的明文激活码；HTTP 审计通过会话绑定补齐 `device` 目标 ID，且回归确认不记录激活码正文。真实 PostgreSQL 并发/重启恢复、跨存储会话绑定和完整核销审计仍待补齐。
- 2026-08-21 BE-016 后台健康探测批次：新增 `ModelPoolHealthProbeScheduler`，由服务器进程拥有并在启动/关闭时接线；默认 5 分钟周期，可通过 `APP_MODEL_HEALTH_PROBE_INTERVAL=0` 禁用，单轮最多 50 个账号、最多 2 个并发探测，单探测默认 10 秒预算，失败按账号计数并使用 80%～120% 确定性抖动退避；账号 `cooldown/exhausted/disabled` 不重复探测，成功清除退避。服务层、配置、启动关闭和 provider/secret-store 回归已通过，健康事件已接入固定低基数 `/metrics`；多实例分布式锁、供应商额度和真实 PostgreSQL 长时故障矩阵仍待补齐。
- 2026-08-21 BE-011/BE-017 用户策略门禁批次：新增迁移 0016 `user_authorization_policies`、用户模型 allowlist 和服务端记录用量门禁的 Go DTO/快照/normalized PostgreSQL 回填与读取；新增管理员授权策略 PATCH、摘要字段、OpenAPI/错误码/管理端表单，模型租约申请和调用摘要写入前执行主体策略校验，幂等重放保持稳定。门禁只使用服务端已接收的 `client_reported` 记录，不是供应商权威账单，不做未来用量预占；真实 PostgreSQL 并发额度、供应商短期凭证和专用额度指标仍待补齐。
- 2026-08-21 BE-007 预算切片：新增独立 `APP_MIGRATION_TIMEOUT`（默认 30s），迁移命令对正值预算做校验并将其用于 PostgreSQL Ping/迁移 Context；API 启动的 PostgreSQL Ping、schema 版本检查和管理员引导统一使用 `APP_REQUEST_TIMEOUT`，Compose 迁移服务已传递独立变量，配置/命令回归已覆盖零值和非法 duration。后台任务超时细分指标、迁移耗时指标和完整关闭编排仍待补齐。
- 2026-08-21 BE-022 运维聚合指标批次：`OperationalMetricsReader` 为 Memory/normalized PostgreSQL 提供固定状态维度的模型账号计数、未过期活动租约数、UTC 当日服务端记录用量 Token 总数和已配置用户授权策略数；`/metrics` 只输出低基数聚合，不携带账号/用户/租约 ID，覆盖 Memory、normalized SQLMock 和 HTTP 抓取回归。后续健康探测遥测新增运行/探测/跳过/耗时固定标签及恶意输入归一化回归；供应商权威额度、跨实例租约以及迁移/后台超时细分指标仍待补齐。
- 2026-08-21 BE-016/BE-022 健康事件遥测批次：健康探测调度器通过服务边界上报每轮 `completed/failed/timeout/cancelled`、探测 `succeeded/failed/unhealthy/timeout/cancelled`、跳过原因和运行耗时；HTTP `/metrics` 注册固定低基数 `autolive_model_pool_health_runs_total`、`run_duration_seconds`、`probes_total`、`skips_total`，未知或恶意标签统一收敛到 `unknown`，不接受账号、URL、错误文本或密钥；新增 `distributed_lock` 跳过原因和超时标签，跨实例锁已接入，供应商额度和真实 PostgreSQL 长时故障矩阵仍待补齐。
- 2026-08-21 BE-021/BE-022 保留清理批次：新增 `RetentionCleanupRequest` 的 cutoff 与 1～1000 批次约束；Memory 支持幂等记录、模型测试结果、审计日志和内存会话的可取消旧到新清理（遗留 State 的幂等创建时间按首次观测时间索引），SQL Session Store 支持过期/撤销会话清理，normalized PostgreSQL 使用参数化 `CTE + LIMIT + FOR UPDATE SKIP LOCKED`，在 advisory lock 事务中同步 `control_plane_state` 快照；新增 `APP_RETENTION_CLEANUP_*` 和四类 TTL 配置，服务启动/关闭接线后台调度器，逐类清理失败会继续处理其他数据集，快照边界错误只记录调试日志；新增固定低基数运行/数据集结果/删除数量 Prometheus 指标，0017 补齐首轮清理索引。容量告警、归档和真实数据量 `EXPLAIN` 复核仍待补齐。
- 2026-08-21 BE-024/BE-006 同事务绑定批次：新增 `TransactionalSessionBinder` 可选 Repository 边界；PostgreSQL 在同一事务内锁定并校验 `auth_sessions`、写入 `device_bound_at`，再执行激活码核销或心跳/幂等状态，任一业务错误统一回滚会话绑定和业务状态；HTTP 在 SQL Repository 上自动走同事务路径，Memory/兼容路径保留精确补偿。新增 Repository SQLMock、服务层 runner 和 PostgreSQL 集成故障回滚回归；Secret Store/审计 Outbox 原子性、真实 CI 连接中断矩阵、容量告警和归档仍待补齐。
- 2026-08-21 BE-019 审计 fail-closed 批次：API 写请求使用内存响应缓冲，先持久化审计再提交响应；审计写失败统一返回 `503/AUDIT_UNAVAILABLE`，不泄露原业务成功响应；管理端 `apiClient` 与桌面端控制面客户端携带同一幂等键各重试一次，避免失败响应后生成新 key 重复提交；新增统一错误码文档、OpenAPI 描述、前后端契约测试和审计失败门禁回归。业务状态与审计的同事务/可靠 Outbox、重复投递、未知提交结果和归档仍待完成。
- 2026-08-21 BE-007 超时观测批次：健康探测和保留清理将 Context deadline 与主动取消映射为固定 `timeout`/`cancelled` 指标状态；模型探测自身预算耗尽也记录 `timeout`，不写入取消语义；迁移命令成功/失败结构化日志增加 `duration_ms` 与 `timeout` 字段。新增服务层和 `/metrics` 回归；迁移指标文件导出另有独立批次，真实 SIGTERM/在途事务/数据库关闭冒烟和 textfile collector 消费仍待补齐。
- 2026-08-21 BE-007/BE-022 迁移指标导出批次：新增可选 `APP_MIGRATION_METRICS_FILE`；迁移命令使用官方 Prometheus client_golang 生成 text exposition 快照，固定输出 `autolive_migration_result{status}`、`autolive_migration_duration_seconds`、`autolive_migration_timeout_seconds` 和 `autolive_migration_version`，不写入 URL、错误全文或 Secret；同目录临时文件刷盘后原子重命名，未配置时完全保持原行为。迁移成功但指标发布失败会记录明确错误并以非零退出，Compose 仅透传配置，真实 textfile collector 挂载、权限、重启和故障矩阵仍待环境验收。
- 2026-08-21 BE-008 错误状态门禁批次：契约测试继续覆盖 OpenAPI 路由/方法和本地 `$ref`，并从 Go `NewError` AST 调用提取错误码及 HTTP 状态，与 `接口契约/错误码.md` 逐项比对；新增状态漂移回归，客户端统一 JSON 包络保持不变。DTO required/nullable、错误响应示例和真实运行时状态矩阵仍待补齐。
- 2026-08-21 BE-005 领域 Repository 第一写入批次：新增最小 `UserRepository`/`UserCreateRecord` 边界；normalized PostgreSQL 用户创建在同一事务写入 `idempotency_records` 与 `users`，使用数据库唯一约束防重，重复指纹回放原用户，指纹不一致返回 `IDEMPOTENCY_CONFLICT`，用户名唯一冲突映射 `USERNAME_ALREADY_EXISTS`；服务层仅在 normalized 读源且能力存在时走该路径，Memory/快照保留兼容 StateOperation。SQLMock 与服务层回归已通过，其余领域写入、Secret/审计同事务和快照退出仍待补齐。
- 2026-08-21 BE-005 用户写入扩展批次：`UserRepository` 新增用户编辑、密码重置和禁用入口；normalized PostgreSQL 在 advisory lock 保护下锁定用户行，幂等记录与 `users` 更新同事务提交，用户名唯一冲突、最后管理员保护、本地管理员禁止禁用、局部失败和密码哈希写入均有稳定边界；服务层仅在 normalized 读源且能力存在时走该路径，Memory/快照保留兼容 StateOperation。其余用户授权、设备/激活码/模型/租约/用量/审计写入、Secret/审计同事务和快照退出仍待补齐。
- 2026-08-21 BE-005 用户授权 Repository 批次：新增独立 `UserAuthorizationRepository`；normalized PostgreSQL 在 advisory lock 下锁定用户，幂等记录与 `user_authorization_policies` JSONB/软额度更新同事务提交，重复指纹回放已有策略，指纹冲突返回 `IDEMPOTENCY_CONFLICT`；服务层只在 normalized 读源且能力存在时走该路径，仍明确客户端记录用量不是供应商权威额度。
- 2026-08-21 BE-005 激活码 Repository 批次：新增独立 `ActivationRepository`；normalized PostgreSQL 在 advisory lock 下写入激活码哈希、脱敏前缀、状态、过期时间和幂等记录，并支持作废状态转换；首次响应才携带随机明文，重放只读取元数据且不重新暴露明文；服务层仅在 normalized 读源且能力存在时走该路径，激活码核销/设备会话绑定仍保留后续同事务任务。
- 2026-08-21 BE-005/BE-024 激活核销同事务批次：新增 `TransactionalDeviceActivator` 与 `DeviceActivationRecord`；normalized PostgreSQL 在同一事务锁定会话、用户、设备和激活码，执行一次性核销、设备注册/解绑后重激活、幂等记录和 `device_bound_at` 会话绑定，旧快照/Memory 保留兼容 runner；幂等作用域沿用 `control-plane-state` + 完整激活作用域键，避免读源切换后重放漂移。新增 SQLMock 成功提交和服务层规范化路由回归；真实 PostgreSQL 并发核销、重启恢复、连接断开/未知提交和完整审计仍待完成。
- 2026-08-21 BE-005/BE-024 心跳领域事务批次：新增 `TransactionalHeartbeatRecorder` 与 `DeviceHeartbeatRecord`；normalized PostgreSQL 在同一事务锁定会话、用户和设备，校验设备归属/启用状态，写入资源指标、播放状态、最后心跳、幂等记录并绑定 `device_bound_at`；幂等键沿用 `control-plane-state` + 完整心跳作用域，旧快照/Memory 保留兼容 runner。新增 SQLMock 成功提交和服务层 normalized 路由回归；设备禁用/解绑、真实 PostgreSQL 故障矩阵和审计 Outbox 仍待完成。
- 2026-08-21 BE-005/BE-013/BE-024 设备生命周期事务批次：新增 `DeviceLifecycleRepository`；normalized PostgreSQL 在同一事务锁定设备，写入禁用或解绑状态、释放活动模型租约、撤销设备会话并记录幂等；HTTP normalized 路径跳过重复的 Session Store 撤销，Memory/快照继续保留兼容 StateOperation 和有界补偿。新增禁用/解绑 SQLMock 与服务路由回归；真实 PostgreSQL 连接断开/未知提交、跨实例并发和审计 Outbox 仍待完成。
- 2026-08-21 BE-005/BE-017/BE-024 租约生命周期事务批次：新增 `ModelLeaseRepository` 与 `postgres_model_lease_repository.go`；normalized PostgreSQL 将客户端续租、客户端释放和管理员回收拆为独立事务，锁定用户/设备/租约（管理员回收锁定租约），在事务内处理过期状态派生、设备归属、活动账号状态、条件状态更新和 `control-plane-state` 幂等记录；创建租约仍保留在兼容 StateOperation，待把 SecretStore 可用性、账号选型、用户门禁和租约插入纳入同一事务。新增三类 SQLMock、取消路径和服务层规范化路由回归；真实 PostgreSQL 并发/连接断开/未知提交、供应商硬额度、用量与审计 Repository 仍待完成。
- 2026-08-21 BE-005/BE-017 客户端调用摘要事务批次：新增 `ModelUsageRepository` 与 `postgres_model_usage_repository.go`；normalized PostgreSQL 在同一事务锁定用户、设备、租约和模型账号，校验租约归属/provider/model，按 `(lease_id, client_call_id)` 读取并核对已有摘要，写入 `model_usage_records` 与 `control-plane-state` 幂等记录，执行用户记录用量门禁并在账号日额度达到阈值时更新 `exhausted`；摘要只接受 `client_reported`，不保存模型正文或 finish reason。新增 SQLMock 成功提交、取消和服务层规范化路由回归；真实 PostgreSQL 唯一约束竞争/连接断开/未知提交、模型账号/租约创建和审计写入仍待完成。
- 2026-08-21 BE-005/BE-025 模型账号生命周期事务批次：新增 `ModelPoolRepository` 与 `postgres_model_pool_repository.go`；normalized PostgreSQL 将账号编辑/禁用放入同一事务，锁定账号行，先派生并过期该账号活动租约，再校验活动租约冲突/并发上限，写入账号状态、URL、优先级、日额度、并发额度和幂等记录，并返回活动租约、UTC 日用量和最近测试结果摘要；账号创建的事务型 SecretStore 边界在下一批次补齐，密钥轮换原子性未改变。新增 SQLMock、服务层规范化路由和状态不变量回归；真实 PostgreSQL 连接断开/未知提交、租约创建和审计写入仍待完成。
- 2026-08-21 BE-005/BE-025 模型账号创建事务批次：新增独立 `ModelPoolAccountCreator`；normalized PostgreSQL 在同一事务写入 `idempotency_records`、`model_accounts` 和通过 `TransactionalSecretWriter` 加密的 `model_account_secrets`，非事务型 SecretStore 明确返回 `SECRET_STORE_UNAVAILABLE`，避免把账号行与密钥写入拆成隐式补偿；Memory/快照保留兼容路径，新增 SQLMock、事务型 SecretStore 和服务层 normalized 路由回归。密钥轮换跨存储原子提交、旧密钥故障补偿、真实 PostgreSQL 连接断开/未知提交仍待完成。
- 2026-08-21 BE-005/BE-017 租约创建事务批次：新增独立 `ModelLeaseCreator`；normalized PostgreSQL 在 advisory lock 事务内锁定用户和设备，回放幂等键，读取并锁定用户授权策略，执行模型 allowlist、服务端日额度、账号优先级/日额度/活动租约并发和过期派生，读取 SecretStore 验证密钥可用性后插入规范化 `model_leases`；Memory/快照保留兼容路径，新增 SQLMock、allowlist、取消和服务层 normalized 路由回归。供应商权威额度、跨实例压力/连接故障和未知提交仍待真实 PostgreSQL 验收。
- 2026-08-21 BE-019 审计可靠投递批次：新增迁移 0019 `audit_outbox`，normalized PostgreSQL 先以 request_id 去重键持久化脱敏审计事件，再由有界 Dispatcher 投递 `audit_logs`；投递失败递增 attempts、保留 pending/next_attempt_at/有界错误，重复投递使用确定性审计 ID 幂等，Retention Scheduler 接入定时重试；Memory/快照仍保留兼容直接审计路径。新增 Outbox SQLMock（成功、取消、投递失败回退）、服务层路由和调度器回归；业务状态与审计同事务、提交未知结果、真实 PostgreSQL 故障矩阵和归档仍待完成。
- 2026-08-21 BE-006/BE-019/BE-024 审计同事务首批：`DeviceActivationRecord`、`DeviceHeartbeatRecord` 与 `DeviceMutationRecord` 接受可选成功审计事件；normalized PostgreSQL 在激活/心跳/设备禁用/解绑同一事务内写入设备、会话绑定或撤销、幂等记录和 `audit_outbox`，HTTP 路由传入已校验的 request_id/设备目标，后置审计通过唯一 dedupe_key 重放不重复；审计 Outbox 失败会回滚对应业务事务。新增激活/心跳/生命周期成功入队、激活 Outbox 失败回滚和服务层传递测试；其他业务域、提交未知结果与真实 PostgreSQL 故障矩阵仍待完成。
- 2026-08-21 BE-006/BE-017/BE-019 租约审计同事务批次：`ModelLeaseCreateRecord`、`ModelLeaseRenewRecord`、`ModelLeaseReleaseRecord` 与 `ModelLeaseReclaimRecord` 接受可选成功审计事件；normalized PostgreSQL 在租约创建、续租、客户端释放和管理员回收事务中与幂等/租约状态一起写入 `audit_outbox`，HTTP 路由传入租约目标和 request_id，重复请求不重复产生审计事实；新增四类 SQLMock 与服务层传递回归。模型账号/Secret 轮换、提交未知结果和真实 PostgreSQL 故障矩阵仍待完成。
- 2026-08-21 BE-017/BE-019 客户端调用摘要审计同事务批次：`ModelUsageWriteRecord` 接受可选成功审计事件；normalized PostgreSQL 在调用摘要、账号日额度状态、幂等记录和 `audit_outbox` 同一事务提交，重复调用按 `(lease_id, client_call_id)` 或同一 request_id 幂等且不重复产生审计事实；新增 SQLMock 与服务层传递回归。模型账号/Secret 轮换审计、提交未知结果和真实 PostgreSQL 故障矩阵仍待完成。
- 2026-08-21 BE-015/BE-019 Secret 轮换事务批次：新增 `ModelPoolSecretRotator`；normalized PostgreSQL 使用 `TransactionalSecretWriter.PutTx` 在同一事务写入候选密文、切换账号引用、记录探测结果、幂等事实、删除旧密文并写入成功审计 Outbox，HTTP 路由传入轮换目标和 request_id；Memory/快照继续保留 staged fallback。真实 PostgreSQL 连接故障/提交未知结果和 Secret Store 故障矩阵仍待完成。
- BE-007 执行批次：部分关闭。`PostgresRepository`、normalized backfill、SQL Session Store 和加密 Secret Store 均新增有界 operation timeout，服务启动统一透传 `APP_REQUEST_TIMEOUT`；SQL mock 回归覆盖构造器拒绝零预算、数据库语句超时和稳定 `context deadline exceeded`。本批次新增健康探测/保留清理 `timeout` 与 `cancelled` 固定指标状态，以及迁移成功/失败 `duration_ms` 结构化日志；迁移 Prometheus 导出、真实 SIGTERM/在途事务和数据库关闭冒烟仍待补齐。
- Phase 3 批次：部分关闭。新增用户 PATCH 编辑、密码重置、状态转换、最后管理员保护和按用户会话撤销；新增按用户设备查询、授权/软额度摘要、设备详情、服务端 `online` 派生、设备禁用/解绑会话与租约联动、激活码脱敏核销详情、用户模型 allowlist/记录用量门禁、管理端详情列及 OpenAPI/类型同步。供应商权威硬额度、未来预占、激活码并发核销和真实 PostgreSQL 原子性仍未完成。
- Phase 4 批次：部分关闭。新增模型密钥轮换 API、管理端换密钥表单、候选密钥验证和幂等测试记录；模型账号健康状态支持有界 cooldown、UTC 日切恢复和带 PostgreSQL advisory lock 的后台探测调度；跨 Secret Store/Repository 原子性、旧密钥回收和严格额度语义仍未完成。
- Phase 1 批次：部分关闭。管理员配置校验、持久化 bcrypt 引导、本地管理员专用密码轮换、统一存储凭证登录、PostgreSQL 启动前引导/持久化凭证失败退出、readiness 凭证检查和高风险接口进程内限流已落地；本轮新增生产 HTTPS 配置/反向代理/桌面 capability 门禁，但真实证书、DNS、Nginx reload 和线上冒烟仍未完成。
- 桌面端路由：已落地。Tauri 主窗口使用 `HashRouter`，提供主页、设置、状态和未知路由回退；最终效果单实例窗口不进入主窗口路由。
- 验证：后端 `go test ./...`、`go vet ./...`、`go build ./...`、Go 1.26.6 `govulncheck@v1.7.0` 通过；管理端/桌面端类型检查、构建和测试通过；OpenAPI YAML 与 Compose 配置解析通过；本地 Docker Desktop 已完成 backend/admin 镜像、PostgreSQL 16 迁移→回填→normalized readyz/metrics、真实 Compose 健康和 SIGTERM clean shutdown；当前环境 `windows/386` 不支持 `go test -race ./...`，Race 仍由 Linux/amd64 CI 完成。

本轮未声称完成 BE-005/009 或任何 P1/P2；BE-004 与 BE-027 已按本轮验收关闭。BE-009 仅关闭“配置与发布检查切片”，真实发布验收仍需环境证据。V2.66～V2.69 已覆盖 PostgreSQL 提交结果、连接断开、响应丢失和 HTTP 在途请求关闭，V2.70 补充兼容模式旧密钥删除失败的持久化队列与有界重试清理，V2.71 补充快照队列持久化回归和最终验证记录；这些证据仍没有把规范化快照退出、Secret 进程外补偿、旧密钥归档或供应商权威额度写成完成。下一执行批次优先处理快照退出、真实数据量计划和 HTTPS 真实冒烟。

V2.72 口径更新：normalized 运行时已经不再依赖旧快照，当前剩余的是完整领域 Repository 收口、删除兼容开关、Secret 进程外恢复、真实额度与生产发布证据；不能把本切片误写成整个 Phase 2 已完成。

## 4. 未完成功能清单

优先级定义：

- P0：阻止生产发布、安全边界或事实源正确性的缺口。
- P1：当前 PRD 已要求，但不阻止只读开发联调的功能缺口。
- P2：规模化运维、体验或长期治理能力。

### 4.1 当前仍未关闭（V2.91，2026-08-22）

以下项目仍有实现或真实环境证据缺口，不能按“测试通过”宣称完成：

- P0：BE-002/BE-005 normalized 正式切换、快照退出和生产回滚（生产配置已禁止 snapshot，已审计的列表、租约详情、设备激活/心跳/禁用/解绑、租约生命周期、模型账号、激活码、调用摘要、审计、授权和 Secret 轮换前置读取回退已收口，但正式切换 Runbook、其余运行时快照写入删除和回滚证据尚未完成）；BE-006/BE-015/BE-019/BE-024 的 Secret/审计跨存储补偿与完整生命周期（normalized SQL 轮换的提交前断开、提交后响应丢失核对和同库 staged 清理已有真实切片，但外部进程断连、跨存储补偿、旧密钥归档和全部领域未知提交仍未完成）；BE-007 在途事务、数据库关闭、后台任务和指标 collector 故障矩阵；BE-009 真实证书、DNS、Nginx reload、HTTPS 与 PostgreSQL TLS 线上冒烟；BE-023 镜像签名和可回滚发布清单。
- P1：BE-011/BE-016/BE-017 的供应商权威额度、短期凭证、严格扣减和长期跨实例矩阵（健康候选过滤已在 V2.80 收口）；BE-018/BE-021/BE-022 的真实数据量 `EXPLAIN (ANALYZE, BUFFERS)`、Benchmark/pprof、容量告警和归档；BE-019 全领域审计同事务/Outbox 的 Memory/快照兼容、重复投递和归档验证；Provider 是否独立实体的产品决策。
- P2：数据备份恢复演练、细分后台任务指标、迁移 textfile collector 生产接入、供应链签名/回滚追溯和完整发布观测。

BE-001、BE-003、BE-004、BE-025、BE-026、BE-027 已达到本地代码验收，但仍需按表中环境条件维护 CI/发布证据；它们不属于当前实现缺口。

| ID | 优先级 | 未完成项 | 当前事实 | 完成标准 |
| --- | --- | --- | --- | --- |
| BE-001 | P0 | 恢复可重复测试基线 | HTTP 测试依赖过期绝对时间 | 注入可控时钟；测试不依赖执行日期；`go test ./...` 全绿 |
| BE-002 | P0 | 纠正文档与存储事实 | 已有规范化读源第一切片，但仍依赖一次性回填和快照过渡层 | normalized 仅允许 PostgreSQL；回填、真实规范化查询、领域事务和集成测试必须证明后才能移除快照过渡层 |
| BE-003 | P0 | 模型测试出站 SSRF 防护 | 已限制 `http:80`/`https:443`、解析后 IP、实际拨号和逐跳重定向，并拒绝回环/私网/链路本地/元数据等地址；明文 HTTP 默认拒绝并返回 `MODEL_POOL_CONNECTIVITY_INSECURE_HTTP`，仅显式配置允许 | 保持回归测试；若后续开放开发期 HTTP 或自定义端口，必须增加显式环境准入和同等出站策略 |
| BE-004 | P0 | 生产管理员与启动就绪 | PostgreSQL 首次启动强制校验非占位管理员配置并写入 bcrypt 哈希；本地管理员专用密码轮换入口会撤销全部管理员会话；轮换后允许省略静态管理员密码环境变量，启动前检查持久化管理员和 `/readyz` | 持久化管理员存在、启用且有凭证哈希时可无静态密码重启；初始化/Repository/密码哈希失败主进程非零退出；普通用户管理入口不能修改本地管理员 |
| BE-005 | P0 | PostgreSQL 规范化唯一事实源 | 单行 JSONB 快照加影子 Upsert；0012 已为调用摘要增加数据库唯一事实约束，用户/设备/激活码/会话/租约/用量/审计和模型账号创建/编辑/禁用已有领域写入第一切片，但运行时仍保留快照过渡层 | 用户、设备、激活码、幂等、审计、模型账号、租约、用量均通过领域 SQL Repository 读写；跨实例结果一致 |
| BE-006 | P0 | 事务原子性和跨实例幂等 | PostgreSQL `TransactionalSessionBinder` 已将激活/心跳的会话绑定、业务状态和幂等写入置于同一 Repository 事务；激活码、用户/授权、设备、租约、用量、审计 Outbox、Session、清理和模型账号/Secret 全部事务出口已统一区分 Context 与 `COMMIT_OUTCOME_UNKNOWN`，COMMIT 发出后保留 staged Secret/不执行不安全删除；本地 Docker 已证明提交前连接终止、提交后响应丢失、Session 冲突和真实锁等待取消均不残留或能核对持久化事实 | 业务、幂等、会话绑定和必要审计在同一事务，Secret 采用可证明可靠的 staged/Outbox 状态机；完成跨存储补偿和规范化领域退出 |
| BE-007 | P0 | 数据库/请求超时和就绪检查 | 已有 `/livez`/`/readyz`，请求预算通过 `APP_REQUEST_TIMEOUT` 注入 Context；Repository、normalized backfill、SQL Session Store 和 Secret Store 已使用有界数据库预算；迁移命令已使用独立 `APP_MIGRATION_TIMEOUT` 并记录成功/失败 `duration_ms`，健康探测/保留清理遥测区分 `timeout`/`cancelled`，API 启动 Ping/版本检查/管理员引导使用请求预算，ready 检查 Repository、Secret Store 和管理员初始化；`/metrics` 已提供连接池聚合/等待首切片和后台超时状态；HTTP `Shutdown` 在本地测试证明会等待在途请求，CI 与本地 Docker Compose 均验证 readyz→SIGTERM→clean-exit；迁移命令可选输出原子 text exposition 快照，发布失败不伪装成功 | 继续补齐数据库关闭/后台调度器/指标导出器故障矩阵、迁移指标文件真实 collector 消费/权限/重启验证；数据库、迁移、Secret Store、管理员初始化失败时 ready/启动失败 |
| BE-008 | P0 | 外部契约收口 | 请求 ID、统一 JSON 错误结构、OpenAPI 路由/方法、本地 `$ref`、Go 错误码状态、显式 DTO required/nullable、共享错误响应示例/代表性运行时矩阵、40 个操作的 405/408/500 声明和管理端/桌面端 `openapi-typescript --check` 生成门禁已接入；真实运行时状态与未接入生成类型的边界仍缺 | OpenAPI/共享类型/错误码文档/Go DTO 一致；Context、数据库、上游超时有稳定状态；请求 ID 规则唯一；CI 自动检查路径、Schema、DTO、生成类型、示例、运行时状态和错误码漂移，并由管理端/桌面端共同消费生成类型 |
| BE-009 | P0 | 发布传输安全 | 已增加生产 HTTPS 配置校验、回环 HTTP hop、Nginx TLS 反向代理模板、发布脚本和 Tauri 明确 HTTPS capability；真实证书与线上冒烟未验证 | 公网/生产发布使用 HTTPS 和明确域名；Tauri 权限只允许目标 HTTPS；记录内网 HTTP 仅限联调的边界，并完成真实证书/DNS/HTTPS 冒烟 |
| BE-024 | P0 | 会话变更不得假成功 | Logout 已传播撤销错误；PostgreSQL 激活/心跳/设备禁用/解绑锁定会话并与设备/幂等状态及成功审计 Outbox 同事务提交，Memory/兼容路径有精确补偿；0018 记录 `device_bound_at`，后台在 5 分钟保护窗口后清理不存在有效设备的孤立绑定；本地 Docker 已验证提交前连接终止与提交后响应丢失时的会话一致性 | 所有会话写入错误都传播为稳定失败；完成跨存储补偿，并让其余会话/设备操作审计进入同事务或可靠 Outbox |
| BE-025 | P0 | 模型账号状态转换单一入口 | normalized 模式的专用禁用和通用更新均路由到 `ModelPoolRepository`，锁定账号并检查活动租约/并发上限；Memory/快照保留同一状态不变量回归 | 继续确保所有新增状态转换复用同一领域命令；补齐真实 PostgreSQL 并发、连接故障和未知提交证据 |
| BE-026 | P0 | 升级受支持的 Go 构建工具链 | 模块语言版本保持 `go 1.22`，工具链已固定 Go `1.26.6` | 本地/CI/Docker builder 固定 Go `1.26.6`；执行测试、Race、`govulncheck` 和镜像冒烟；模块语言版本仅在代码需要时调整 |
| BE-027 | P0 | 高风险接口限流 | 已使用 `golang.org/x/time/rate` 按客户端 IP/接口策略限流登录、Refresh、模型测试、密钥轮换和管理员密码轮换；固定桶上限/空闲淘汰、`Retry-After` 与 `429/RATE_LIMITED` 已接入，明确为单实例策略 | 持续维护限流参数与监控；若扩展多实例，再设计有界分布式策略，不把当前进程内限流误称为全局限流 |
| BE-010 | P1 | 用户编辑和密码管理 | 已实现编辑、密码重置、启用/禁用和最后管理员保护；修改后撤销用户会话 | 补齐用户设备/额度摘要和全链路语义审计 |
| BE-011 | P1 | 用户设备与用户级授权策略 | 已提供按用户设备分页、授权摘要和幂等策略 PATCH；normalized 授权摘要直接聚合规范化用户/设备/租约/策略/用量表，allowlist 与服务端记录用量门禁已在租约/摘要写入前校验，管理端可配置 | 供应商权威硬额度、短期凭证、未来用量预占、语义化审计和真实 PostgreSQL 跨实例额度验收 |
| BE-012 | P1 | 设备运行状态完整保存 | 心跳当前媒体/播放状态、在线派生、详情接口、管理端详情抽屉已完成 | 真实 PostgreSQL 读写、历史状态保留和重启恢复验收 |
| BE-013 | P1 | 设备禁用/解绑会话联动 | normalized PostgreSQL 已在设备生命周期事务内释放活动租约、撤销设备 Session 并更新禁用/待激活状态，HTTP normalized 路径跳过重复撤销；Memory/快照保留 Session Store 补偿，跨存储故障和真实 PostgreSQL 故障注入仍缺失 | 禁用/解绑后旧会话不能继续访问客户端受保护能力；允许安全重新激活；真实存储故障可恢复 |
| BE-014 | P1 | 激活码核销详情 | 已返回脱敏前缀、核销用户、设备和时间；当前明文仅创建响应返回，激活/作废后内存状态清除明文；内存并发单次核销和幂等回放已有回归；本地 Docker PostgreSQL 已覆盖过期/作废、重复核销、重启式幂等冲突、Session 冲突/锁等待取消、提交后响应丢失和无残留/持久化核对边界 | 继续覆盖语义化核销审计；后续明文重看、完整绑定详情、容量修改和设备切换转入 BE-031，且不得把明文写回 `activation_codes` |
| BE-015 | P1 | 模型密钥轮换 | 已增加独立幂等轮换接口；normalized PostgreSQL 候选密文、账号引用、探测结果、幂等记录、旧密文删除和成功审计 Outbox 通过 `TransactionalSecretWriter.PutTx` 同事务提交；Memory/快照在业务状态切换事务中预写旧引用到持久化 `PendingSecretCleanup` 队列，删除成功后移除，删除失败或提交未知时由有界清理重试，失败不回显密钥 | 完成 Secret Store 进程外故障/跨进程恢复、旧密钥归档和全模式故障矩阵；所有支持模式均有可恢复语义 |
| BE-016 | P1 | 自动健康、冷却和日切恢复 | 失败连通性测试会写入带 `cooldown_until` 的有界冷却，成功测试恢复；`exhausted` 在新的 UTC 日按同一服务端时钟恢复；后台探测具备账号/并发上限、normalized 可探测账号过滤后的有界读取、超时、取消、失败计数和抖动退避，并由服务器优雅关闭等待；PostgreSQL 会话级 advisory lock 已防止多实例重复探测，锁忙时有固定低基数跳过指标，Memory/快照保留回退；租约集成已覆盖懒过期、冷却/禁用跳过及冷却到期恢复 | 补齐供应商权威额度和真实 PostgreSQL 长时故障矩阵，并明确手工测试覆盖后台状态的优先级 |
| BE-017 | P1 | 额度与并发语义 | 用户可配置服务端记录用量门禁，达到门禁拒绝新的租约/摘要；normalized 调用摘要 Repository 已在事务内核对调用 ID、锁定账号并达到日额度时更新 `exhausted`；新增 PostgreSQL 16 双 Repository 同账号 `concurrency_limit=1` 并发租约验收和 UTC 日额度跨日恢复验收，确认只产生一个活动租约且新 UTC 日恢复可用；另有懒过期/释放槽位以及冷却/禁用筛选集成验收；账号额度与用户门禁仍不做未来预占，客户端上报不等于权威计费 | 文档明确“租约并发”和“供应商请求并发”的区别；补齐供应商可验证短期额度、真实跨实例原子预占和专用指标 |
| BE-018 | P1 | Provider、租约和审计查询能力 | 用户/设备/激活码/调用摘要/审计/模型账号/租约列表已具备 normalized SQL 分页第一切片；激活码列表已使用脱敏、有界 `COUNT + LIMIT/OFFSET` 查询并派生过期状态；模型调用摘要已支持 provider/model/user/device/request_id、RFC3339 时间窗口和创建时间升降序，Memory/normalized SQL/HTTP/OpenAPI/管理端 URL 状态一致；模型账号页派生活动租约、UTC 日用量和最近测试结果，管理员租约页展示脱敏归属/生命周期，已支持单租约详情和管理员幂等回收；0017/0021 已补齐首轮筛选索引并有 PostgreSQL `EXPLAIN` 计划可见性测试；Provider 只有账号字段、无独立管理边界 | 先确认 Provider 是否需要独立实体；以真实数据量 `EXPLAIN (ANALYZE, BUFFERS)` 复核全部筛选索引，补齐真实跨实例额度/故障验收 |
| BE-019 | P1 | 审计完整性 | 已认证写请求和登录成功/失败记录主体、目标类型/ID、结果、状态码、统一错误码并持久化；normalized PostgreSQL 通过 `audit_outbox` 先落盘，request_id 去重后确定性投递 `audit_logs`，失败保留 pending 并由 Retention Scheduler 有界重试；激活/心跳/设备生命周期/租约/客户端调用摘要/normalized Secret 轮换成功事件已在业务事务内 Outbox；关键 API 写响应采用缓冲后审计，失败统一 `503/AUDIT_UNAVAILABLE`；全部 PostgreSQL 提交出口已映射 `503/COMMIT_OUTCOME_UNKNOWN`，审计日志已有 cutoff、1～1000 批次和取消清理接口 | 记录主体、动作、资源、结果、状态码、request_id；敏感正文不入库；完成真实跨进程恢复、Memory/快照 fallback、TTL/归档和 PostgreSQL 连接故障矩阵 |
| BE-020 | P1 | 真实 PostgreSQL 集成测试 | CI 与本地 Docker PostgreSQL 16 均已执行迁移、重新构造 Repository 的 normalized round-trip、外键约束失败回滚、SQL Session Store 重启/Refresh 单次消费、双实例幂等竞争、激活码/租约并发、UTC 日额度、清理、筛选索引、锁等待取消、提交前连接终止和提交后响应丢失；所有事务出口已有 SQLMock 提交分类；Secret 进程外故障和跨存储补偿仍未覆盖 | 使用隔离 PostgreSQL 执行 Secret 密文、跨实例补偿和跨存储故障测试 |
| BE-021 | P2 | 数据保留和清理 | 会话过期/撤销和孤立设备绑定、幂等、测试结果、审计已有 cutoff、1～1000 批次上限、取消路径；Memory、SQL Session Store 和 normalized PostgreSQL 清理实现已覆盖，并由 `APP_RETENTION_CLEANUP_*`/TTL 配置驱动后台调度；同步快照或明确拒绝快照读源，清理运行/数据集/删除数量指标、连接池饱和度观测和 0017/0018/0021 索引已接通，新增筛选计划可见性测试 | 补齐容量告警规则、以真实数据量复核索引并确定归档策略 |
| BE-022 | P2 | 指标和告警 | 已有 `/metrics` HTTP 请求量、状态码、延迟、在途请求、PostgreSQL 连接池等待/饱和度、`autolive_database_pool_saturation_ratio`/`autolive_database_pool_saturated`、限流/审计失败，以及模型账号状态、活动租约、UTC 日用量、用户授权策略、健康探测事件和保留清理运行/数据集/删除数量聚合首切片；连接池饱和度使用固定 0.8 阈值、Memory 不伪造，迁移命令可选输出 `APP_MIGRATION_METRICS_FILE` text exposition 快照，包含固定 status、耗时、超时预算和目标版本；标签固定且未知值归一化，不暴露高基数或秘密 | 提供供应商额度、跨实例租约、后台任务细分指标、容量告警规则；完成迁移指标文件的真实 collector 挂载/权限/重启验证；继续保持禁止高基数标签和秘密 |
| BE-023 | P2 | 发布可复现性 | Go/Alpine 基础镜像已固定 manifest digest；CI 已声明 Docker/OCI 输出、SBOM、Provenance，并记录 BuildKit canonical digest、本地镜像 ID 与产物 SHA-256 artifact；`tools/check-backend-release.mjs` 阻止声明回退；镜像签名、迁移版本和回滚清单仍缺 | 固定基础镜像摘要/SBOM/镜像签名策略；CI 产物、canonical digest 和迁移版本可追溯；服务器不构建源码 |

### 4.2 商业化与生产就绪扩展（规划，未实施）

以下条目来自 [`服务端商业化与生产就绪扩展 PRD`](./tasks/prd-server-commercial-production-readiness.md)。它们尚未进入 OpenAPI、数据库迁移、服务代码、React 管理页或服务器，不能按当前功能验收。

| ID | 优先级 | 规划项 | 目标边界 | 计划阶段 |
| --- | --- | --- | --- | --- |
| BE-028 | P1 | 管理查询 API | 订阅、订单、套餐版本列表/详情；设备扩展白名单筛选、排序、分页、索引和真实数据量计划验证 | Phase 1、5 |
| BE-029 | P1 | 商业核心与微信支付 | 不可变套餐版本、订单价格/权益快照、订阅履约；微信支付 Native 回调验签/解密、幂等处理和主动查单 | Phase 4、6 |
| BE-030 | P1 | 密码重置投递 | 哈希化一次性 Token、中性响应、Outbox/Job Worker、有界重试和重置后会话撤销 | Phase 7 |
| BE-031 | P0 | 激活码安全管理 | 新码明文进入独立 `activation_code_secrets` 加密表；二次验证后重新查看；独立设备槽位、容量调整和设备切换 | Phase 3 |
| BE-032 | P0 | 可信制品分发 | 私有对象存储、授权后短时下载、签名清单/文件哈希校验、CI SBOM/签名/Provenance 和发布真实性门禁 | Phase 8 |
| BE-033 | P1 | namespace 配置 Schema | JSON Schema Draft 2020-12、不可变版本、受限 `$ref`、复杂度上限、dry-run、revision/ETag 和敏感字段二次扫描 | Phase 9 |
| BE-034 | P0 | 生产运维与供应链 | 数据保留/归档、加密备份、隔离恢复、Prometheus + OpenTelemetry Trace/OTLP、CI 签名和发布策略验证 | Phase 11 |
| BE-035 | P0 | 管理员 RBAC | 服务端固定权限目录、自定义角色、用户多角色并集、无用户级覆盖、`super_admin`/本地管理员/最后超管保护、即时鉴权和防越权委派 | Phase 2 |
| BE-036 | P1 | React 权限化商业管理面 | 角色与产品范围用户角色页；套餐、订单、订阅、席位、支付、重置投递、激活码安全、制品、公共配置、错误反馈和运维页；权限化导航/路由/操作/403 | Phase 2、3、5～11 |
| BE-037 | P0 | 多产品硬隔离 | `products`/`user_products`、产品会话、设备/激活码/租约/用量/审计约束与产品范围 RBAC；筛选不得替代鉴权 | P0 Phase 1 |
| BE-038 | P0 | Profile 授权新鲜度 | 服务端时间、6 小时建议复核、24 小时签名离线硬截止、权益 revision、回拨与安全降级 | P0 Phase 2 |
| BE-039 | P0 | 模型认证闭环 | douyin-desktop 默认本地 BYOK；仅供应商支持可撤销、受限、绑定租约短期凭证时开放托管直连 | P0 Phase 3 |
| BE-040 | P0 | 固定契约与跨仓库 E2E | OpenAPI/sha256/metadata 三件套固定 commit/digest，覆盖产品/授权/401/409/429/超时/取消矩阵 | P0 Phase 4 |
| BE-041 | P1 | 错误报告与反馈 | 用户同意、客户端/服务端双重脱敏、有界批量/采样/限流/TTL/删除；最小反馈状态流与产品范围管理页 | Phase 10 |

专项共同约束：保持模块化单体；P0 product 是设备、激活码、商业资源、配置、制品、报告和管理授权的共同硬隔离事实，筛选参数不能扩大授权；服务端是业务和授权唯一权威，React 隐藏不替代 API 鉴权；优先 PostgreSQL Outbox/Job Worker，不先引入外部消息队列；对象存储只承载软件发布制品，不承载用户本地媒体；构建只在本地开发机或 CI 完成；任何密钥、支付报文、密码重置 Token、激活码明文、BYOK 或模型正文不得进入日志、普通审计、快照或前端构建产物。

## 5. 分阶段实施计划

### Phase 0：事实校准与绿色基线

目标：先让测试、契约和文档描述可信，禁止在错误基线上继续扩展功能。

任务：

- [x] 为 Router/ControlPlane 注入统一可控时钟，替换 HTTP 测试中的过期固定时间；测试不得使用任意长睡眠。
- [x] 修复失败测试的连带 Panic：前置 HTTP 状态不满足时立即失败，不能继续对空响应做强制类型转换。
- [x] `APP_MODEL_READ_SOURCE=normalized` 不再“接受但忽略”：仅 PostgreSQL 可启用，迁移 0013 补齐规范化字段，Repository 读取规范化表并以事务级 advisory lock 串行化；真实 PostgreSQL 数据回填和跨进程证据仍列为后续任务。
- [x] 生成路由/OpenAPI 对照检查；`contract_test.go` 从 OpenAPI 读取路径和方法并请求真实 Router，已收口 404/405 路由漂移，并校验 paths/components 的本地 `$ref`、Go 错误码状态、共享错误响应示例/代表性运行时矩阵、显式 DTO properties/required/nullable 和 40 个操作的 405/408/500 通用 response；解绑设备 `user_id`、模型账号创建 `status`、Logout 可选字段、激活码默认值和 Direct LLM 可选统计字段已同步，管理端 `openapi-typescript --check` 与 `frontend-contract` CI 作业已锁定生成类型差异，真实运行时状态与桌面端更广泛生成类型消费仍待继续收口，手写契约仍以 OpenAPI 为唯一事实源。
- [x] 升级本地、CI 和 Docker builder 到 Go `1.26.6`；保留 `go.mod` 的 `go 1.22` 语言版本，`govulncheck` 通过，Race 和镜像冒烟纳入 Linux/amd64 CI。
- [x] 更新 `管理系统架构.md`、`数据库迁移与密钥存储约束.md` 和总计划中与代码事实冲突的完成状态。
- [ ] 删除本阶段暴露的无调用代码、空目录、过期测试夹具和无效配置；不触碰历史迁移。当前仅剩待单独审计的历史兼容代码，未在本批次扩大清理范围。

退出标准：

- `go test ./...`、`go vet ./...`、`go build ./...` 全部通过。
- `go test -race ./...` 在支持 Race Detector 的 `linux/amd64` 或 `windows/amd64 + CGO` 环境通过。
- OpenAPI 路径、方法、错误码和实现无未解释漂移。
- Go 构建工具链处于官方支持窗口，版本证据进入 CI 和发布清单。
- 项目文档不再宣称未接通的规范化读源已经完成。

### Phase 1：安全边界、启动与健康检查

目标：关闭可直接阻止生产发布的安全和就绪缺口。

任务：

- [x] 为模型账号 `base_url` 建立完整出站策略：限制 `http:80`/`https:443`、解析后 IP、回环/私网/链路本地/组播/未指定地址/保留网段/常见云元数据和逐跳重定向；默认 HTTPS，`APP_ALLOW_INSECURE_HTTP=true` 通过服务启动配置显式传入模型测试边界，未配置时返回稳定 `MODEL_POOL_CONNECTIVITY_INSECURE_HTTP`。
- [x] 自定义 `http.Transport.DialContext` 和 `CheckRedirect`，在实际连接与每次跳转前重新校验目标；限制重定向次数和响应体大小。
- [x] 保持模型测试的 Context/超时可取消，不在数据库事务中执行网络请求；测试使用本地 httptest 与受控解析器，不访问真实公网。
- [x] 将管理员初始化改为显式可失败流程；PostgreSQL 配置拒绝缺失/占位管理员，启动前 Repository/密码哈希失败会以非零状态退出，`/readyz` 校验管理员存在、启用且有凭证哈希。
- [x] 增加一次性 bootstrap 凭证轮换和本地管理员专用密码修改入口，密码写入 bcrypt、操作要求 `Idempotency-Key`，成功后撤销本地管理员全部会话；轮换完成后 PostgreSQL 可仅依赖持久化凭证启动，避免生产长期依赖静态环境密码。
- [x] Logout、Refresh 撤销、设备绑定等 Session Store 写入不得丢弃错误；PostgreSQL 设备激活/心跳通过同事务绑定实现同成同败，Memory/兼容路径通过精确补偿保持可恢复失败语义。
- [x] 将模型账号状态转换收口为单一领域命令；通用 PATCH 不能绕过活动租约、额度、禁用和审计不变量。
- [x] 增加 `/livez` 与 `/readyz` 或等价版本化接口：live 只检查进程，ready 检查 Repository、Secret Store 和认证初始化；迁移版本在进程启动阶段强制校验。
- [x] 增加 PostgreSQL Repository/规范化回填、SQL Session Store 和 Secret Store 的数据库语句与事务预算；服务启动将 `APP_REQUEST_TIMEOUT` 透传为 operation timeout，超时/取消已映射为结构化 Context 错误，`/metrics` 已暴露连接池等待计数/时长和连接数首切片；迁移命令使用独立预算并记录耗时，并可选生成原子 text exposition 文件，健康/清理后台任务输出固定 `timeout`/`cancelled` 细分。真实 SIGTERM/在途事务、数据库关闭和迁移指标文件 collector 挂载/权限/重启故障矩阵仍待补齐。
- [x] 对登录、Refresh、模型连通性测试和密钥轮换等高风险接口增加客户端 IP 维度的有界限流；使用 `golang.org/x/time/rate`，设置固定容量、空闲淘汰、`Retry-After` 和 `429/RATE_LIMITED` 契约，不自制分布式限流器。
- [x] 生产入口的配置与发布检查切片：`APP_DEPLOYMENT_ENV=production` 必须配 `APP_PUBLIC_BASE_URL=https://...` 且禁止不安全 HTTP；Compose 管理端 HTTP hop 绑定回环地址，`deploy/reverse-proxy/nginx.conf` 负责 80→443 与 TLS，`tools/check-https-publish.mjs` 校验环境、代理和 Tauri capability。真实证书、DNS、Nginx reload、网络 ACL 和 HTTPS 统一入口冒烟仍待发布环境执行。

退出标准：

- SSRF 单元测试覆盖 IPv4/IPv6、域名解析、重定向、私网、回环、元数据地址和允许目标。
- 数据库/Secret Store/管理员初始化任一失败时 readiness 非 200，且不会泄露连接串或密钥。
- Session Store 故障和活动租约场景不会产生假成功、部分成功或 PATCH 绕过。
- 登录暴力请求和模型测试风暴受到限流，正常请求不受无界等待影响。

### Phase 2：PostgreSQL 规范化唯一事实源

目标：移除业务运行时对单行 JSONB 大快照的依赖，建立按领域、可分页、可跨实例验证的事务 Repository。

任务：

- [ ] 由调用方定义最小 `UserRepository`、`UserAuthorizationRepository`、`DeviceRepository`、`ActivationRepository`、`IdempotencyRepository`、`AuditRepository`、`ModelPoolRepository` 和 `SessionRepository` 接口；本轮已落地并接入用户身份、授权策略、激活码创建/作废、激活核销+设备注册+会话绑定、心跳+设备+会话绑定、设备禁用/解绑+租约释放+会话撤销、租约生命周期、客户端调用摘要、模型账号编辑/禁用以及审计追加写路径，其余接口仍按真实调用边界拆分；禁止创建大而全的通用 Repository。
- [x] 新增 0013/0014 前向迁移，补齐 `devices.device_name/platform`、`model_pool_test_results` 和模型账号 `cooldown_until`；0011 已新增 `activation_codes.used_by_user_id`、0012 已新增调用摘要非空唯一约束，前滚边界已落在迁移文件中。
- [ ] 继续补齐账号冷却、状态约束和保留期字段，并为规范化表建立独立领域 Repository。
- [ ] 迁移用户、设备、激活码和幂等写入；用户创建/编辑/密码重置/禁用及授权策略、激活码创建/作废、激活核销+设备注册+会话绑定、心跳+设备+会话绑定和设备禁用/解绑+租约释放+会话撤销已在 normalized PostgreSQL 同一事务完成用户/设备/激活码/会话/租约/幂等写入并覆盖冲突语义，激活/心跳/设备生命周期成功审计已进入同事务 Outbox，其他管理写入仍需接入。
- [ ] 迁移模型账号、租约和用量；normalized PostgreSQL 已将模型账号创建（事务型 SecretStore 与账号行同事务）、编辑/禁用、租约创建/续期/释放/管理员回收和客户端调用摘要按领域事务写入规范化表，并使用账号/用户/设备/租约行锁、过期条件更新、账号选择、allowlist、服务端日额度、调用 ID 核对、软额度和幂等记录；租约四类、客户端调用摘要和 normalized Secret 轮换成功审计已进入同事务 Outbox，Memory/快照 Secret fallback、真实跨实例并发验收和故障矩阵仍待完成。
- [ ] 为调用摘要建立 `(lease_id, client_call_id)` 数据库唯一约束；冲突必须验证请求指纹一致，不得用无条件 `ON CONFLICT DO UPDATE` 覆盖既有事实。
- [ ] Secret Store 已支持 PostgreSQL 模型账号创建和 normalized 密钥轮换复用业务事务（`TransactionalSecretWriter`）；Memory/快照保留 staged fallback，仍需补齐真实提交失败/进程崩溃/连接中断的确定回收和故障补偿，不使用无超时 `context.Background()` 静默补偿。
- [ ] 列表查询在 SQL 层完成分页、白名单排序和筛选，不再加载完整控制面状态后切片；本轮完成用户、设备、按用户设备、调用摘要、审计、模型账号和租约的 normalized SQL 分页第一切片，并完成租约、模型用量与审计筛选/排序白名单第一切片；模型用量的 provider/model/user/device/request_id、时间窗口和 URL 状态已接入，租约详情/回收第一切片已完成，其余列表筛选和完整索引策略仍待迁移。
- [x] 提供显式一次性回填工具 `go run ./cmd/backfill-normalized`：要求迁移已到最新版本、`APP_NORMALIZED_BACKFILL_CONFIRM=YES`，使用 advisory lock、单事务写入并按表计数校验；当前 normalized 读源不会静默回退快照，工具失败会回滚。
- [x] 增加只读 normalized 切换准入命令 `go run ./cmd/verify-normalized-cutover`：要求显式确认，校验迁移版本、回填完成标记、关键规范化表可读性和模型账号 Secret 引用完整性；失败不修改数据库，并纳入 PostgreSQL CI。
- [x] 增加生产读源安全门禁：`APP_DEPLOYMENT_ENV=production` 拒绝 `APP_MODEL_READ_SOURCE=snapshot`；生产示例改为 normalized，并以真实 Docker PostgreSQL 验证“快照拒绝→迁移→回填→准入→API readyz”。
- [ ] 过渡期只允许“规范化写入 + 受控兼容读取”或等价的单一事实方向；禁止两个事实源双向写入。
- [ ] 完成后删除运行时快照写入路径和 `APP_MODEL_READ_SOURCE` 过渡开关；`control_plane_state` 历史表保留只读审计或另立迁移清理任务。

退出标准：

- 两个 Go 实例并发创建同一幂等请求只产生一个资源。
- 用户、设备、激活码、租约、用量、会话撤销在进程重启后可恢复。
- 故障注入覆盖 Secret 写成功/业务提交失败、事务超时、连接断开和重复请求。
- 业务请求不再每次重写全部用户、设备、租约、用量、幂等和审计行。

### Phase 3：用户、设备与激活码功能收口

目标：补齐当前 PRD 已声明的后台控制能力，并让禁用/解绑真正影响会话。

任务：

- [x] 用户：增加编辑、密码重置、必要的启用/禁用状态转换；防止删除或锁死最后一个有效管理员。
- [x] 用户：增加当前授权和额度摘要；新增按用户授权摘要接口与幂等策略 PATCH，支持模型 allowlist 和服务端已接收用量门禁，明确客户端自报软额度与供应商权威硬额度的边界；按用户查询设备已完成，所有资源 ID 访问仍需在真实 Repository 中校验主体权限，跨实例额度预占另列后续任务。
- [x] 会话：增加按用户、设备批量撤销；密码重置、用户禁用、设备禁用和解绑均会撤销相关会话；跨存储补偿和激活绑定原子性仍待完成。
- [x] 会话：Logout 和设备绑定持久化失败返回稳定错误；PostgreSQL 激活/心跳将绑定与业务写入同一事务，normalized 禁用/解绑同时撤销设备会话并释放租约，Memory/兼容路径按新增绑定执行有界补偿，覆盖“绑定失败不消费激活码”和“业务失败清除新绑定”；激活/心跳成功审计事件已在同一事务写入 Outbox，重复 request_id 由 Outbox 去重。
- [x] 会话：增加 `device_bound_at` 绑定时间标记和孤立绑定协调清理；超过固定 5 分钟保护窗口且不存在同用户有效设备的绑定会按 1～1000 批次、`SKIP LOCKED` 清理，Memory/SQL 路径均有取消与错误回传。
- [ ] 会话：补充真实 PostgreSQL 连接断开/提交未知结果故障注入，并将其他会话/设备业务审计写入收敛到同事务或可靠 Outbox；模型账号/Secret/快照首批已能在提交未知时保留候选密文并返回稳定错误。
- [x] 设备：保存 `current_media_name`、`playback_state`、运行系统摘要和最后心跳；增加详情接口或完善列表 DTO。
- [x] 设备：由服务端时间派生 `online/offline`，不信任客户端时钟直接声明在线；定义阈值和恢复语义。
- [x] 激活码：返回脱敏前缀、核销用户、设备和时间；仍只允许创建成功的首次响应包含明文。
- [ ] 激活码：覆盖解绑后新码重激活、真实 PostgreSQL 并发核销和核销审计；过期/作废/重复核销/重启式幂等冲突及无残留集成测试、内存并发单次核销与终态明文清除、normalized SQLMock 核销+设备+会话同事务和服务路由已有回归。
- [x] 同步更新 OpenAPI、管理端类型和页面；不手工修改可再生成产物。

退出标准：

- 用户管理满足创建、编辑、禁用、密码重置和查询设备的当前需求。
- 后台能显示设备在线状态、客户端版本、系统/资源摘要、当前媒体、播放状态和最后心跳。
- 激活码明文不进入列表、日志、审计 payload、数据库或测试快照。

### Phase 4：模型号池生产控制面

目标：把模型账号管理从手工开发闭环收口为安全、可解释的控制面；仍不启用本版本已排除的实时话术调用。

任务：

- [x] 增加基础模型密钥幂等轮换；候选密钥通过受限模型列表测试后才写入 Secret Store，失败保留旧密钥且重复幂等键不重复探测。
- [ ] 补齐 Secret Store 与业务 Repository 的真实故障矩阵、旧密钥归档、并发轮换和故障补偿；normalized PostgreSQL 已使用 `ModelPoolSecretRotator` 同事务提交，Memory/快照仍是 staged fallback。
- [x] 强化连通性测试：限制响应体，解析 `/models` JSON，并验证目标模型确实存在；任意 2xx 不能直接视为账号可用。
- [x] 定义账号健康状态机第一切片：失败/超时连通性测试进入 5 分钟 `cooldown`，成功测试恢复；状态和 `cooldown_until` 通过快照/规范化账号字段持久化。
- [x] 明确 `exhausted` 的 UTC 日切恢复规则，并与用量统计窗口使用同一 Repository 时钟；后台恢复探测已接入，PostgreSQL 跨实例调度锁另有 advisory-lock 边界，Memory/单进程使用无锁回退。
- [x] 增加有上限的后台健康探测调度，具备所有者、取消、超时、退避、确定性抖动和优雅关闭等待；健康专用事件指标已接入固定低基数 Prometheus 标签；PostgreSQL 使用会话级 advisory lock 防止多实例重复探测。
- [x] 健康探测候选查询在 normalized PostgreSQL 先按可探测条件过滤再应用 LIMIT；缺少专用 Reader 时 fail-closed，避免不可探测账号占满首批分页。
- [ ] 明确租约并发代表“活动租约数”还是“供应商活动请求数”；当前无供应商回执时不得把前者包装成后者。
- [ ] 将 `client_reported` 用量标记为软额度依据；没有供应商权威账单时不得宣称精确计费。若业务需要硬额度，必须先建立供应商可验证的短期凭证/预算协议。
- [x] 增加账号、租约、用量按 provider/model/status/time/user/device/request_id 的分页筛选第一切片；迁移 0017/0021 已补齐首轮复合索引，PostgreSQL `EXPLAIN` 计划可见性有集成测试。
- [ ] 以真实数据量执行 `EXPLAIN (ANALYZE, BUFFERS)`，补齐其余筛选/排序索引并形成容量基线。
- [ ] 对照管理系统需求决定 Provider 是否独立建模；如果只是字符串维度，修正文档并删除“供应商管理”入口；如果是独立实体，补齐 CRUD、启停、引用约束和审计。
- [x] 增加管理端租约列表、详情和回收第一切片：新增 `/api/v1/admin/model-leases`、`/api/v1/admin/model-leases/{lease_id}` 和 `/api/v1/admin/model-leases/{lease_id}/reclaim`，区分活动、过期、释放状态并只返回归属/生命周期摘要；支持状态、供应商、模型、用户、设备、账号筛选和四种白名单排序，详情显示创建/回收时间，回收要求幂等键且审计；内存/快照回退与 PostgreSQL SQL 结果保持一致，不返回租约凭证或模型密钥。
- [x] 覆盖租约懒过期回收、冷却/禁用账号跳过、冷却到期恢复、释放后并发槽位恢复、并发竞争和 UTC 日切测试。
- [ ] 补齐额度耗尽、账号禁用冲突、真实连接故障和跨实例长时矩阵。

退出标准：

- 管理员可安全登记、测试、轮换、禁用和恢复账号。
- 分配逻辑在真实 PostgreSQL 并发下不超过定义的租约上限。
- 用量来源、可信级别和额度影响在 API/管理端中可见，不混淆客户端自报与权威账单。

说明：供应商短期凭证委托、Rust 真实直连和自动调用摘要属于未来启用模型消费时的独立版本门槛；本计划不得用下发长期 API Key 的方式“补齐”该能力。

### Phase 5：审计、可观测性与数据治理

目标：让关键失败可定位、关键操作可追溯，同时避免日志和指标泄密。

任务：

- [x] 审计登录成功/失败、用户、设备、激活码、模型账号、租约和密钥轮换操作；已认证写请求和登录事件第一切片记录 actor、device、action、resource、outcome、status_code、error_code、request_id 和时间。
- [x] 审计管理列表第一切片：按操作者、设备、动作、目标类型、结果、错误码、request_id、RFC3339 时间窗口筛选，并按创建时间白名单排序；normalized SQL 与内存/快照回退保持一致，管理端和 OpenAPI 已同步。
- [x] 审计不保存密码、Token、API Key、明文激活码、请求正文、模型正文或音视频内容；中间件只提取已校验主体/目标和错误上下文，审计 payload 固定为空对象，并由登录、激活和审计列表回归覆盖。
- [ ] 关键业务写入与审计采用同事务或可靠 Outbox；已明确 API 写请求在审计不可用时 fail-closed 并返回 `AUDIT_UNAVAILABLE`，激活/心跳/设备生命周期/租约/客户端调用摘要/normalized Secret 轮换成功审计已在业务事务内入 Outbox，提交未知结果首批返回 `COMMIT_OUTCOME_UNKNOWN` 并保留 staged candidate，仍需完成其余领域、重复投递、Memory/快照 fallback 语义和归档验证。
- [x] 日志统一使用注入的 `slog.Logger`，避免生产中间件绕过应用 Logger 使用全局默认 Logger；错误重复记录仍按新增边界逐项复核。
- [x] 接入成熟 Prometheus 方案第一切片：`github.com/prometheus/client_golang v1.20.5`（Apache-2.0、Go 1.20+，官方维护的 Go 客户端）提供独立 registry、HTTP 请求量/状态码/延迟/在途请求、PostgreSQL `sql.DBStats` 连接池等待与 0.8 饱和度、限流/审计失败、模型账号/活动租约/UTC 日用量/用户授权策略聚合、健康探测事件和保留清理运行/数据集/删除数量指标；迁移命令另通过可选 `APP_MIGRATION_METRICS_FILE` 生成固定低基数 text exposition 快照；真实调用点为 `backend/internal/httpapi/metrics.go`、`backend/internal/httpapi/retention_metrics.go` 的 `/metrics` 抓取、Router 中间件和服务遥测适配器、`backend/internal/store/operational_metrics.go` 的 Memory/normalized SQL 聚合读取以及 `backend/cmd/migrate/metrics.go`，替代方案 OpenTelemetry 暂不引入以避免重复采集链路；使用 `/metrics` 或 textfile 标准格式，路由标签只使用固定模板或 `unmatched`，健康/清理/迁移标签只使用固定状态和数据集，业务聚合不写入用户 ID、完整 URL、request_id、错误全文或秘密。供应商额度、跨实例租约、容量告警规则和迁移文件真实 collector 消费/权限故障矩阵仍待补齐；后续若统一迁移到 OTel，可删除相关文件、注册器和对应 Go 依赖。
- [x] 指标标签禁止用户 ID、完整 URL、request_id、错误全文等高基数字段；聚合指标已用固定 `status` 维度或无标签输出，并以回归测试阻止账号/用户/租约 ID泄露。
- [x] 建立保留清理接口第一切片：会话、幂等记录、模型测试结果和审计日志均接受 cutoff 与 1～1000 批次上限，支持 Context 取消；normalized PostgreSQL 使用 `SKIP LOCKED` 并在事务中同步快照，快照读源明确拒绝清理，Memory 提供确定性旧到新清理（遗留内存 State 没有幂等 `created_at`，因此按首次观测时间索引；PostgreSQL 才是生产保留事实源）。
- [x] 为过期会话、幂等记录、账号测试结果和审计日志定义 TTL/保留期并接入后台调度：`APP_RETENTION_CLEANUP_INTERVAL/TIMEOUT/BATCH` 控制执行边界，四类 `APP_*_RETENTION_TTL` 控制 cutoff，单轮失败按数据集隔离并在优雅关闭时等待。
- [ ] 补齐容量告警规则和归档策略；连接池饱和度指标第一切片已落地，保留清理/管理筛选的第一轮索引评估已由迁移 0017/0021 落地，计划可见性有 PostgreSQL 集成测试，仍需以真实数据量执行 `EXPLAIN (ANALYZE, BUFFERS)` 复核。

退出标准：

- 可通过 request_id 从请求日志关联到审计资源和结果。
- 关键告警能够回答“慢、错、堵塞、容量”中的具体问题，不因单次错误制造噪声。
- 清理任务可取消、有批次上限，不长时间锁表，不影响在线请求。

### Phase 6：集成、性能与发布验收

目标：用真实依赖和故障场景证明服务端可以发布，而不是只证明单元测试通过。

任务：

- [x] CI 与本地 Docker PostgreSQL 16 均执行 0001～0021 迁移、`autolive-backfill-normalized`、normalized round-trip、约束回滚和 SQL Session Store 重启/Refresh 单次消费；本地回填 marker 已验证为 `completed`。
- [ ] 覆盖重启恢复、双实例幂等、租约并发、额度日切、会话撤销、审计原子性和 Secret Store 故障（激活码双 Repository 并发/过期/作废/幂等边界、Session 冲突/锁等待取消、提交前 `pg_terminate_backend` 强制断开、提交后响应丢失并核对旁路持久化、兼容模式旧引用删除失败队列、租约懒过期/冷却/释放槽位、全部事务提交分类和 0021 查询计划已加入并在本地 Docker PostgreSQL 通过；Secret 进程外故障、跨进程恢复和旧密钥归档仍待完成）。
- [ ] 使用 Benchmark/pprof 或代表性负载验证列表分页、心跳、租约和审计写入；无证据不增加缓存。
- [ ] 验证优雅关闭：HTTP 层已由 `TestServerGracefulShutdownWaitsForInFlightRequest` 验证停止接收新请求并等待在途请求，CI 与本地真实 Compose 已覆盖 readyz→SIGTERM→clean-exit，日志出现 `shutdown signal received` 和 `http server stopped cleanly`；仍需验证后台健康任务、数据库连接、日志/指标导出器和关闭超时的深层故障矩阵。
- [x] 为迁移、API 和管理容器配置与服务关闭预算一致的 `stop_grace_period`（默认 `AUTOLIVE_STOP_GRACE_PERIOD=60s`），并将 API `APP_SHUTDOWN_TIMEOUT` 默认设为 45 秒；收到终止信号后的真实事务不截断仍需环境冒烟。
- [x] 在本地/CI 构建 Go 二进制和 `linux/amd64` 镜像；本地 Docker 已完成 backend/admin 镜像构建、迁移→回填→normalized readyz/metrics→Compose 健康和 SIGTERM clean shutdown；CI 已记录 Go 版本、镜像内容摘要、Docker/OCI 产物 SHA-256、SBOM/Provenance，迁移版本、签名和回滚步骤仍待补齐。
- [ ] 服务器只执行镜像加载、迁移、启动和验证，不执行 Go、pnpm 或 Docker 构建。
- [ ] 通过 HTTPS 统一入口完成管理端真实冒烟：登录、用户、设备、激活码、模型账号测试、租约、用量和审计。

退出标准：

- 所有自动检查通过，真实 PostgreSQL 和跨实例用例有可复现证据。
- 发布包不含源码、真实 `.env`、数据库密码、主密钥、API Key 或管理员密码。
- 回滚不删除历史数据；不兼容迁移有前滚/回滚说明和备份恢复演练。

## 6. 测试矩阵

每个新增或修复行为必须先有失败测试，并至少覆盖适用场景：

| 领域 | 必测场景 |
| --- | --- |
| Auth | 登录成功/失败、限流、Refresh 单次轮换、旧 Token 失效、Logout、用户/设备批量撤销、存储故障 |
| Users | 创建、重复用户名、编辑、角色边界、密码重置、最后管理员保护、幂等冲突 |
| Devices | 激活、心跳、播放状态、禁用、解绑、旧会话、重激活、归属越权、离线派生 |
| Activation | 首次明文、列表脱敏、过期、作废、重复/并发核销、幂等、重启恢复 |
| Model Pool | URL/SSRF、密钥轮换、测试超时、重定向、健康/冷却、禁用、恢复 |
| Leases/Usage | 申请、续租、释放、过期、跨主体、并发上限、额度日切、重复摘要、来源可信级别 |
| Persistence | 迁移、事务回滚、跨实例幂等、连接中断、快照回填、Secret 补偿、重启恢复 |
| Audit | 成功/失败结果、资源 ID、request_id、敏感信息排除、审计存储故障 |
| Operations | live/ready、数据库不可用、迁移 dirty、优雅关闭、后台任务取消、指标标签 |

测试禁止依赖真实公网、真实供应商密钥、固定未来日期或任意长睡眠。

## 7. 统一验收命令

```bash
cd backend && gofmt -l .
cd backend && go vet ./...
cd backend && go test ./...
cd backend && go test -race ./...
cd backend && go build ./...
cd backend && GOTOOLCHAIN=go1.26.6+auto go run golang.org/x/vuln/cmd/govulncheck@v1.7.0 ./...

npx --yes js-yaml 接口契约/openapi.yaml
bash tools/check-document-references.sh
node tools/check-https-publish.mjs --env-file deploy/.env.example --reverse-proxy deploy/reverse-proxy/nginx.conf --capability desktop/src-tauri/capabilities/default.json --admin-nginx admin-web/nginx.conf

docker compose --env-file deploy/.env.example -f deploy/docker-compose.yml config
```

真实 PostgreSQL 集成测试命令由 Phase 2 固化为仓库脚本；脚本必须创建隔离数据库、执行迁移、运行测试并安全清理自身资源，不得连接未明确指定的生产数据库。

## 8. 代码总监复核清单

- [ ] 当前范围没有重新引入实时话术、旧媒体任务、服务端模型代理或请求预占。
- [ ] OpenAPI 是唯一外部契约，生成代码和手写实现没有双重事实源。
- [ ] 所有输入在 HTTP 边界和领域边界校验；URL、分页、排序、正文和路径都有上限/白名单。
- [ ] 所有资源操作校验主体、动作和资源归属；认证成功不替代授权。
- [ ] SQL 参数化；事务短小；事务内无供应商网络请求；并发和幂等由数据库约束证明。
- [ ] Goroutine 有所有者、取消、上限和等待；Channel 有明确关闭方和背压。
- [ ] Secret、Token、密码、明文激活码、连接串和模型正文不进入日志、指标、审计或响应。
- [ ] 没有 `context.Background()` 绕过上游取消；独立补偿/清理任务使用有所有者和超时的进程 Context。
- [ ] 列表查询不加载无界结果；会话、幂等、审计和测试结果有保留策略。
- [ ] 未使用代码、导入、依赖、空目录、调试日志和重复实现已删除。
- [ ] 本地/CI 完成格式化、静态检查、测试、竞态检测和构建；未验证项如实记录。
- [ ] 文档状态与代码证据一致，没有把配置字段、影子表或兼容代码写成已完成功能。

## 9. 发布门槛

只有同时满足以下条件，服务端才可从“开发期最小闭环”改为“当前版本完成”：

1. Phase 0～Phase 6 的退出标准全部有证据。
2. P0 全部关闭；P1 若延期，必须经用户确认并从当前 PRD 验收范围移出。
3. PostgreSQL 规范化表是唯一业务事实源，跨实例幂等和租约并发测试通过。
4. 模型测试 SSRF 防护、Secret Store、会话撤销、审计和 HTTPS 发布门槛通过。
5. `go test`、Race Detector、`go vet`、`go build`、OpenAPI/文档检查全部通过。
6. 管理端通过 HTTPS 统一入口完成真实 PostgreSQL 冒烟，服务器没有执行源码构建。

## 10. 延期项记录

以下能力不是本计划未完成项，不能混入当前阶段：

- 实时话术幻化、speech-to-speech、VAD/ASR/LLM/TTS。
- Rust 真实模型直连、供应商短期凭证委托和实时话术调用摘要自动上报。
- 视频/音频上传、服务端媒体处理、变体任务和版本队列。
- RTMP/OBS、直播平台接入、口型同步、研究分析和检测规避能力。

如后续恢复任何延期项，必须先更新 PRD、系统架构、OpenAPI、安全模型和独立验收计划，不能直接复用历史兼容字段或表作为完成证据。
