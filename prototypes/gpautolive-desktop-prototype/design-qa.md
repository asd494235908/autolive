# 设计视觉回归记录

## 2026-08-26 · 声音状态信息横向布局

- 用户反馈：音频状态区的“实际出口、处理状态、当前预设”不仅整体横向排列，每项名称和值也必须左右同一行；同时去掉插话区额外的“插话音频”和“独立声音周期与预设参数变化”文字。
- 实现：普通声音与插话状态共用组件在宽屏采用三列布局，各列内部以紧凑 Flex 保持名称和值左右排列；小于 480px 时只把三列回落为单列，列内仍保持左右排列。插话周期直接作为该区第一块内容，不再渲染额外分区标题。

## 2026-08-26 · 标题栏处理开关与独立插话预设 Card

- 用户反馈：仅修改原型 HTML；“高级设置”入口改为“模式修改”，“插话声音预设”作为音频下方独立 Card，普通声音处理和视频处理控件移入各自 Card 标题栏。
- 实现：插话周期、出口、状态和波形仍留在音频 Card 的普通声音上方；35 项当前插话预设单值移动到左列下方独立 Card。声音功能入口继续打开原高级声音抽屉，视频处理字段、周期和恢复默认行为不变。

## 2026-08-26 · 媒体双卡与声音入口调整

- 用户反馈：仅修改原型 HTML；高级设置与随机插话移到最右侧最终效果窗口下方的新 Card，“视频”顶层标题改为“画面”，插话声音周期和其下方状态/预设参数区并入音频 Card 且置于普通声音之前。
- 实现：中间区域顶层 Card 从音频/插话音频/视频三张收口为音频/画面两张；插话仍保留独立子区、错误就地恢复、实际出口、当前预设、最终混音波形及 35 项当前值。右栏新增“声音功能”Card，复用原高级设置和随机插话抽屉，不删除任何功能或字段。
- 验证：`node --test tests/prototype-contract.test.mjs` 25/25 通过，`pnpm build` 通过；浏览器确认 DOM 顺序为插话音频→普通声音、最终效果窗口→声音功能→PortAudio→话术功能，高级设置与随机插话按钮均能打开原抽屉。

## 对照基准

- 视觉事实源：`C:\Users\asd49\xwechat_files\wxid_rud357apqrci12_129a\temp\RWTemp\2026-08\615eea485d41d697d0197cc66da82d52\aeba2acf0be8f4c32b3e6cd4d18cf1d5.jpg`
- 事实源像素：1920 × 1040；其中图片查看器工具栏、红色分区线、蓝色框、黄色波形和红色说明文字均视为审阅标注，不属于产品界面。
- 实现截图：`artifacts/prototype-desktop-1728-final.png`
- 实现像素与浏览器视口：1728 × 1044。
- 并排对照：`artifacts/design-comparison.png`，1600 × 540。
- 抽屉截图：`artifacts/advanced-audio-drawer.png`、`artifacts/interruption-drawer.png`。
- 状态：3 项播放池、首项播放中、视频处理开启、普通声音处理开启、最终效果窗口已连接。

## 归一化说明

- 对事实源使用 `(80, 42, 1760, 960)` 的产品区域裁切，排除外部图片查看器界面。
- 并排图仅为等宽缩放，不改变实现的布局比例；事实源中的审阅标注没有被复刻。
- 实现遵循当前桌面端功能范围，保留参考图的暗色高密度三栏结构，不复刻已退出当前版本的实时话术与模型租约区域。

## 对照历史

> 2026-08-25 范围更新：下列“播放池只读”是当轮历史审阅结论，当前正式桌面端已改为支持拖放导入、追加、单项替换、拖拽/键盘排序、删除和清空；原型若继续演进应以最新产品与桌面架构为准。

### 第 1 轮

- P2：状态卡顺序与本地桌面端事实源不一致，源规格信息不够明确。
- P2：播放池条目表现为可点击，但当前版本不支持重排、删除或点选切换。
- P2：高级声音、随机插话、固定话术抽屉出现了超出当前范围或与现有字段不一致的配置项。
- P2：右侧处理链只用颜色圆点表达状态，信息不够独立于颜色。
- 修复：按真实状态顺序调整六张状态卡；播放池改为只读并补充原子替换策略；按现有桌面端字段重做三个抽屉；处理链增加完成、处理中、等待图标。

### 第 2 轮

- P2：React 19 与 Ant Design 5 产生兼容性警告。
- 修复：将 React 与 React DOM 固定到 18.3.1，并在全新浏览器页复验，控制台无错误和警告。

### 第 3 轮

- 用户反馈：参数卡只需要显示“亮度 +0.6%”等文字信息，不需要数值下方的装饰性进度条。
- 修复：移除普通视频、高级视觉与普通声音参数卡的进度条及其无用进度数据；播放进度与视频/声音周期进度仍保留。

### 第 4 轮

- 用户反馈：参数卡不显示“已接入”，参数名称与参数值需要在同一行。
- 修复：删除参数状态标签、顶部参数状态图例及无用状态数据；普通参数卡压缩为单行，频段卡标题与“12 段”同一行并保留频段明细。

### 第 5 轮

- 用户反馈：播放进度与循环次数应归入播放控制，中间不需要六张综合状态卡，只保留视频与声音两张卡。
- 修复：左侧播放控制新增播放进度与循环次数摘要；删除中间六张综合状态卡及无用数据、属性和样式；中间顶部仅保留视频周期、声音周期两张卡。

### 第 6 轮

- 用户反馈：中间需要两个独立功能卡，视频相关内容归入视频卡，音频相关内容归入音频卡。
- 修复：将视频周期、普通视频、高级视觉组合进独立视频卡；将声音周期、普通声音组合进独立音频卡；参数项改为卡内单行信息行，避免继续形成第三层卡片。

### 第 7 轮

- 用户反馈：取消跨音视频同时应用入口，视频处理和恢复默认应归入视频卡。
- 修复：删除顶部“音视频同时应用/应用视频”按钮及无用回调；将视频处理开关和视频参数恢复按钮移入视频卡，顶部只保留工作区标题说明。

### 第 8 轮

- 用户反馈：删除“音视频处理 · 实时参数”顶部块，普通声音处理、高级设置和随机插话应归入音频卡。
- 修复：删除中栏顶部标题块；将普通声音开关、出口状态、波形、诊断值、高级设置和随机插话入口迁入音频卡；右栏删除重复声音面板与对应入口，仅保留固定话术。

### 第 9 轮

- 用户反馈：“实时 · 混音后 PCM”下方不显示诊断参数，普通声音开关、高级设置和随机插话应参照视频操作栏放到音频卡顶部。
- 修复：删除采样率、RMS、峰值等诊断数值及其无用数据和样式；音频状态区只保留出口、状态、预设和波形；三个声音操作统一迁入音频卡顶部操作栏。

### 第 10 轮

- 用户反馈：本地运行状态应放到最右栏，取消声音与视频周期联动按钮。
- 修复：将四项本地运行状态完整迁入右栏；删除联动按钮及其状态、属性、组件导入和专用样式，两个独立周期范围保持不变。

### 第 11 轮

- 用户反馈：参数方框需要更小，并根据项目展示当前全部参数。
- 修复：以 Rust `VideoEffectParams`、`AudioEffectParams`、`AdvancedEffectParams` 为事实源，将参数补齐为 32、37、42 项，共 111 项；普通参数框由 44px 压缩为 34px，网格最小宽度由 174px 压缩为 148px，长文本通过单行省略与原生悬停标题保留完整信息。

### 第 12 轮

- 用户反馈：高级设置和插话弹窗没有参照当前桌面端。
- 修复：高级声音抽屉按桌面端重组为处理与输出、PortAudio 设备、多轨与预设、37 项音频参数状态和缓存管理，并使用固定 440 Hz / 400 ms 测试音与条件化应用按钮；随机插话抽屉按桌面端重组为启用状态、音频来源、独立声音轨、触发与混音，补齐固定/随机、多轨、22 项预设、周期抽样和 ducking 包络字段。
- 清理：两个抽屉均删除不属于桌面端的“取消”按钮；插话删除试听按钮；高级声音删除恢复默认和可编辑测试音字段。
- 浏览器验收：高级声音抽屉显示 5 个分组且底部为“重新生成本周期参数 / 音视频同时应用”；随机插话抽屉显示 4 个分组且底部只有“保存插话配置”。

### 第 13 轮

- 用户反馈：声音周期需要位于音频设置按钮下方，随机插话弹窗视觉未同步当前项目桌面端。
- 修复：音频卡顺序调整为“操作栏 → 声音周期 → 普通声音状态 → 普通声音参数”；随机插话抽屉同步正式桌面端双行标题、状态摘要、Card 标题说明、四列预设、三组混音参数、主体独立滚动和固定底部保存区。
- 约束：随机插话业务字段集合、默认值和校验范围保持不变，未新增依赖。

### 第 14 轮

- 用户反馈：随机插话也需要加入声音周期；普通声音参数下方需要独立展示插话音频的周期、实际出口、处理状态、当前预设和混音后波形。
- 修复：在音频卡的普通声音参数后增加“插话音频”区；固定预设显示不轮换，逐次随机显示每次插话更新，按周期随机读取 8–15 秒声音变化周期；处理状态区分未启用、等待音频、等待播放和已就绪；固定模式显示具体预设，随机模式显示预设池与混轨范围并明确等待本段抽样；波形沿用同一诊断组件并标注为插话混入后的最终 PCM。
- 约束：主界面只读取现有随机插话配置，没有新增业务字段、处理路径或依赖；正式的触发间隔、音量与 ducking 参数仍在随机插话抽屉维护。
- 视觉复验阻塞：现有 `artifacts/prototype-desktop-1728-final.png` 早于本轮改动，本轮未获得同视口的浏览器渲染截图，因此不能把旧截图当作新区域的视觉证据。源码契约、Sites 测试和生产构建均通过，但字体、间距、颜色、图像质量与新增文案仍需在浏览器中与参考图同屏复验。

### 第 15 轮

> 本轮的“随机候选范围”展示语义已被 Round 16 的“当前运行单值”要求取代，仅保留为历史审阅记录。

- 用户反馈：“插话音频”需要成为音频卡下方的独立 Card，并展示“插话声音预设”内部参数的变化。
- 修复：中栏改为两列独立堆叠，左列为视频卡，右列依次为音频卡和插话音频卡，两张右栏卡的实际间距为 8px；插话卡保留周期、出口、状态、当前预设和最终混音波形，并增加 35 项预设参数区。
- 数据事实源：参数值直接读取 `desktop/ui/src/audio-value-presets.ts` 的 `AUDIO_PRESET_FIELDS` 与 `AUDIO_VALUE_PRESETS`。固定模式显示单套精确值；随机模式没有主页可读的真实抽样结果，因此显示“候选池 20 项 · 等待首轮抽样”和 20 套候选的可能变化范围，不以 p01 冒充当前预设。
- 浏览器证据：`artifacts/interruption-audio-card.png`，截图 1061 × 900；CSS 视口 1073 × 912，`devicePixelRatio=1`。控制台无 error/warning。Card 几何为音频卡底部 842.56px、插话卡顶部 850.56px，间距 8px。
- 聚焦对照：`artifacts/interruption-audio-comparison.png`，1052 × 852；左侧为用户参考状态卡，右侧为实现中的插话独立卡。字体沿用 Inter/Segoe UI/微软雅黑回退链，表面、边框、圆角、状态色、周期条、三行状态和波形结构与参考一致；新增参数区复用现有 34px 紧凑参数行。
- 状态差异：参考图处于 PortAudio 运行状态，本轮实现截图保留原型默认“随机插话未启用”状态；该差异属于数据状态，不是视觉漂移。自动浏览器已验证随机候选池范围，固定模式的 Ant Design 下拉自动点击受可见边界限制，未完成交互切换复验。
- 剩余 P3：35 项范围在窄卡内采用三列，少数长标签会省略；原生 `title` 保留完整文本，符合此前“小参数框”要求。

## Round 16：插话当前预设单值语义修正

- 用户纠正：“插话声音预设”必须展示当前插话变换正在使用的参数值，不展示候选取值范围。
- 修复：候选预设池继续只作为抽样配置；新增与配置分离的 `interruptionRuntime.currentAudioPresetId` 运行快照，插话卡根据当前预设 ID 读取正式 35 字段定义并展示精确单值。
- 空状态：运行快照没有有效当前预设时显示“等待首轮插话抽样”和 `—`，不再静默回退 p01，也不展示候选最小值或最大值。
- 原型默认验收状态：随机插话已启用，当前运行快照为 `p13`，便于直接检查单值参数；抽屉配置仍只影响候选池和下一轮应用。
- 浏览器证据：`artifacts/interruption-audio-current-values.png`；实际 DOM 为 35 个参数值，未出现候选范围符号，示例值为高质量音高 `-0.03 半音`、输入增益 `-0.06 dB`、目标采样率 `44100 Hz`。

## Round 17：声音左置、紧凑参数行与视觉频段拆分

- 用户调整：声音与插话放到左列，视频放到右列；参数行进一步压缩；固定视觉频段权重拆成独立参数。
- 布局：直接调整 DOM 顺序为“音频→插话音频→视频”，保证视觉、键盘和读屏顺序一致，不使用 CSS 反向排序。
- 密度：普通参数行由 34px 压缩到 28px，内边距改为 4px × 6px，网格最小宽度由 148px 收紧到 126px；保留 10px 文字、单行省略和完整 `title`，让常用桌面半栏稳定容纳两列参数。
- 频段：`AdvancedEffectParams.band_weights` 仍是唯一正式字段；12 个 Map 键使用 `band_weights.<Hz>` 视图路径分别展示，删除旧的跨栏汇总渲染器和专用样式。
- 浏览器证据：`artifacts/layout-audio-left-compact-rows.png`；音频列 X=344、视频列 X=670，参数行实测均为 28px，普通声音参数网格实测为两列，12 个固定视觉频段均作为独立参数行存在。

## Round 18：参数值字号、抽屉统一与主页功能差异审计

- 用户调整：参数变化值需要稍微更大；设计稿中的抽屉弹窗统一参照随机插话；同时对照正式桌面端主页梳理遗漏功能。
- 字号：参数名称保持 10px，参数值从 10px 调整为 11px；参数行仍为 28px，未牺牲单屏信息密度。
- 统一抽屉：抽取公共 `FeatureDrawerShell` 与 `FeatureDrawerSection`，高级声音、随机插话、固定话术和播放池管理统一使用双行标题、实时状态摘要、Card 分区、独立滚动主体、暗色遮罩和固定底栏；固定话术独立为组件，删除重复的取消底栏；高级声音按既定要求移除“音视频同时应用”，只保留“应用声音参数”。
- 主页审计（历史，已由 Round 19 覆盖）：当轮曾把播放池增量管理、普通声音四项可编辑基线、固定话术预制 CRUD、本周期真实声音预设/支路查看列为 P1；当前以 Round 19 和 `desktop-home-gap-audit.md` 最新结论为准。
- 浏览器证据：`artifacts/audit-home-gap/02-random-interruption-reference.png`、`03-advanced-audio-unified.png`、`04-fixed-speech-unified.png`、`05-playback-pool-unified.png`。四个抽屉均显示相同 header/body/footer 表面、状态摘要和 Card 边界，固定底栏没有遮挡正文；`06-prototype-home-final.png` 的浏览器计算样式为参数名称 10px、参数值 11px、参数行 28px。

### 最终对照

- 全局：三栏分区、暗色表面层级、顶部 36px 标题栏与 34px 导航、中心高密度卡片和右侧输出状态栏与参考视觉一致。
- 左栏：播放池与播放控制完整；播放进度与循环次数集中在播放控制区域，不显示周期联动按钮，视频/声音周期输入归入各自媒体 Card。
- 中栏：仅保留视频与音频两个独立功能卡；两类操作均位于各自卡片顶部，音频状态区不显示混音后的诊断参数；111 项正式媒体参数完整展示在 34px 紧凑单行参数框中。
- 右栏：最终效果窗口、固定话术入口、四步处理链与本地运行状态完整，不再重复普通声音处理与声音设置入口。
- 交互：播放/暂停、视频与声音独立开关、画中画/独立窗口，以及高级声音、随机插话、固定话术抽屉均已在浏览器中验证；高级声音和随机插话的分组、字段及底部操作已按当前桌面端复验。
- 剩余 P3：原型加载了完整 Ant Design，生产构建主包约 953 kB；作为独立评审原型可接受，若进入正式产品再按路由与功能拆包。

final result: passed

## Round 19：播放池完整编辑、PortAudio 右栏状态与版本边界

> 本轮的“播放池管理抽屉”和“PortAudio 只读状态”已被 Round 20 的主页内联管理与设备配置要求覆盖，仅保留为历史审阅记录。

- 播放池：补齐整批替换、追加视频、文件拖放追加、单项替换、拖拽排序、键盘可达的上移/下移、删除和清空。所有浏览器候选先校验格式、空文件、重复项和数量，再一次性提交；失败或取消保持旧池，成功编辑后重置到首项 `Ready`。
- PortAudio：新增右栏独立只读状态 Card，放在“话术功能”上方，展示实际出口、Host API、输出设备、内存缓冲和处理状态；高级声音抽屉删除 Host API、设备、缓冲输入和测试音。
- 普通声音：取消此前审计中误列的“四项可编辑基线”，参数继续只读展示；高级声音只应用声音参数，不联动视频。
- 固定话术：本轮保留基础本地系统朗读原型；最多 10 条预制的增删改、停止朗读和独立状态明确推迟到下个版本。
- 文档：同步 README、主页差异审计、产品需求、系统架构、桌面客户端架构、媒体参数契约、功能流程和长任务计划；历史方案保留并标注被最新决策覆盖。

## Round 20：播放池主页内联管理与 PortAudio 可配置设备

- 播放池：移除播放池管理抽屉；追加视频、文件拖放追加、整批替换、单项替换、拖拽排序、上移/下移、删除和清空全部直接放在主页播放池 Card。外部文件拖放只识别 `Files` 载荷，条目内部排序只在存在当前拖拽条目时拦截事件，避免两种拖放相互吞掉。
- PortAudio：话术功能上方的独立 Card 改为可配置状态卡，提供 Host API 筛选、输出设备选择、`128–2048 KiB` 内存缓冲和显式“应用设置”；编辑草稿与已应用配置分离，未应用时显示“待应用”，恢复为已应用值后按钮重新禁用。
- 边界：高级声音抽屉继续不重复 PortAudio 配置；不恢复测试音、手动输出开关或 ASIO 假能力。普通声音参数仍只读，固定话术扩展仍属于下个版本。

## Round 21：仅压缩截图标记的周期与声音状态块

- 范围：只调整声音周期、视频周期、插话声音周期三张 `cycle-status-card`，以及普通声音、插话音频两张 `audio-processing-panel`；外层 Card、参数行、播放池、PortAudio、处理链和其他状态区均保持原样。
- 密度：周期卡内边距改为 `6px × 8px`、底部间距改为 `6px`、信息行间距改为 `2px`，进度条使用 `4px` 线宽并清除额外行高；声音状态卡同样使用 `6px × 8px` 内边距，状态行距为 `2px`，波形高度由 `72px` 收紧到 `48px`。
- 信息：周期范围、变化次数、实际出口、处理状态、当前预设和波形说明全部保留，未删除字段、缩小参数值字号或改变交互行为。

### Round 21 视觉复验

- 参考图：`C:/Users/asd49/AppData/Local/Temp/codex-clipboard-2422352a-430f-496c-9c3b-904f33052cf5.png`、`codex-clipboard-c2e3e56a-8aab-4b28-b5e5-6c7f5758678f.png`、`codex-clipboard-13d6845d-d297-4b87-92e6-4e27c378467f.png`。
- 实现证据：`artifacts/compact-status-blocks/05-compact-targets-final-full-page.png`，完整页面截图为 `924 × 3179`；三处聚焦截图分别为 `06-audio-final.png`（`448 × 255`）、`07-video-final.png`（`448 × 140`）、`08-interruption-final.png`（`448 × 190`）。
- 对照结论：三组参考图与实现聚焦截图逐组同屏检查；标题、图标、状态标签、周期范围、变化次数、出口、处理状态、当前预设、进度色和波形语义保持一致，仅按用户要求降低纵向留白。参考图是局部裁切，实现图保留了紧邻区域用于证明下一模块未被错误压缩，因此裁切边界不做像素级一致要求。
- 影响面：普通参数行仍为既定 `28px`，外层 `panel-section`、播放池、PortAudio、处理链及右栏状态没有使用本轮紧凑规则；契约测试专门检查这些选择器未被全局覆盖。
- 浏览器：`http://127.0.0.1:4173/` 重新加载后无新增 error/warning；Ant Design 进度条使用公开 `size={["100%", 4]}` 属性，不覆盖 `.ant-*` 内部样式。
- 缺陷分级：P0/P1/P2/P3 均无新增项。字体、颜色和文案沿用原组件；本轮不涉及图片资源替换。
- 回归：`npm test` 20/20、`npm run test:sites` 4/4、`npm run build` 通过，`git diff --check` 通过；构建仍有既存的主包大于 500 kB 警告，与本轮局部样式无关。

final result: passed

## Round 22：声音默认周期、插话秒输入与桌面功能复核

- 普通声音：播放控制中的声音周期默认值和音频卡周期摘要统一改为 `3–5 秒`，当前参数变化周期示例改为 `4.0 秒`；视频周期及插话周期默认值不变。
- 随机插话：变化周期最小值、最大值统一以 `1–60 秒` 输入和校验；保存到原型状态时在输入边界换算为毫秒，避免改变既有字段语义。
- 桌面端复核：新增记录正式桌面端仍待同步的 `3–5 秒` 默认值和插话秒输入；当轮发现的错误恢复与逐播放动作 loading 后续已分别由 Round 24、Round 23 补齐，本周期真实声音预设/支路仍未完成。
- 版本边界：固定话术最多 10 条预制 CRUD、停止朗读和独立状态虽已出现在正式桌面端源码，但继续遵守用户此前“下个版本再弄”的决定，不在本轮原型中增加。
- 浏览器证据：`artifacts/audit-cycle-seconds/01-home.png` 显示声音周期输入和音频卡摘要均为 `3–5 秒`；`02-interruption-seconds.png` 与 `03-interruption-period-seconds.png` 显示随机插话抽屉仍采用统一样式，变化周期字段以 `8/15 秒` 展示。控制台无 error/warning。
- 审计结论：主页和抽屉状态健康，数值、单位和紧邻文案一致；本轮没有改变布局、交互层级、插话内部毫秒字段或其他周期默认值。源码对比发现的未覆盖功能已写入 `desktop-home-gap-audit.md`，截图无法证明真实 FFprobe、PortAudio 枚举、错误恢复或键盘/读屏完整性，这些仍需正式桌面端运行时验证。

final result: passed

## Round 23：独立播放动作与单一主页导航

- 播放控制：移除按状态切换文案的单个上下文主按钮，改为独立“播放 / 暂停 / 继续 / 停止”按钮；按钮可用性由当前播放状态决定，每个动作有独立 loading，动作执行时禁用其他播放入口与画中画，防止重复触发。
- 导航：删除未实现独立页面的“设置”和“状态”入口及其选中态状态，只保留当前“主页”，避免可点击但不导航的伪入口。
- 语义复核：当前“当前预设 13 · 轻弹”只是单套演示值，不代表多轨时本周期实际抽中的 1–4 套预设及支路详情；真实运行快照接入仍列为 P1 缺口。
- 错误恢复：本轮只确定条件式就近告警方案；该方案已在 Round 24 实现，右侧本地运行状态继续只做聚合摘要。
- 浏览器证据：`artifacts/playback-nav-update/01-pause-loading.png`、`02-play-loading.png`、`03-home-final.png` 和 `04-home-nav-only.png`。DOM 复核显示顶部仅剩主页，播放、暂停、继续和停止四个独立按钮均存在；并发捕获确认继续按钮在执行期间显示 `loading`，其他动作与画中画不可用。
- 回归：契约测试新增独立动作/loading 与导航范围检查，`npm test` 24/24、`npm run test:sites` 4/4、`npm run build` 和 `git diff --check` 均通过；构建仍有既存的约 1,010 kB 主包大于 500 kB 警告，本轮未新增依赖或扩大打包范围。

final result: passed

## Round 24：错误就近展示与恢复路径

- 展示规则：媒体引擎不可用同时出现在音频与视频 Card；视频处理失败、声音处理失败、随机插话失败只出现在各自 Card。健康状态不渲染 Alert，也不预留错误高度。
- 恢复路径：每条 Alert 同时展示错误原因、主要恢复动作和必要的次要动作。点击恢复后标题、按钮、周期标签、实际出口、处理状态、右侧处理链和本地运行状态统一进入“恢复中”；成功后移除 Alert 并显示短暂成功消息。
- 状态一致性：媒体引擎不可用时声音周期和视频周期显示“引擎不可用”，普通声音实际出口显示“原声”、处理状态显示“失败回退”；插话失败时显示“本次已跳过/未输出”，主视频和普通声音保持运行。
- 可访问性：错误使用 `role="alert"`，不只依赖颜色；恢复按钮使用 Ant Design `loading` 和 `disabled`，避免重复提交。
- 演示入口：`?issue=engine`、`?issue=video`、`?issue=audio`、`?issue=interruption`。查询参数只初始化原型状态，正常地址保持健康。
- 浏览器证据：`artifacts/runtime-recovery/02-engine-error.png`、`03-engine-recovering.png`、`05-interruption-error-focused.png`、`04-healthy-no-placeholder.png`。四类错误均逐项验证，恢复 loading 和恢复后 Alert 消失已验证。
- 回归：`npm test` 25/25、`npm run test:sites` 4/4、`npm run build` 和 `git diff --check` 均通过；健康页控制台无 error/warning。生产构建仍有既存的约 1,017 kB 主包体积警告，本轮未增加依赖。

final result: passed

## Round 25：周期输入归属媒体 Card

- 结构：删除左侧播放列独立“周期范围”Card，将声音周期与视频周期的最小/最大秒数输入分别放入“音频”和“画面”Card 内的周期状态块。
- 状态：输入继续复用 `App` 中唯一的 `cycleRange` 状态与 `onCycleRangeChange` 更新路径；默认值保持声音 `3–5 秒`、视频 `8–15 秒`，卡内显示会随输入同步变化。
- 插话：插话声音周期保持只读，继续由随机插话配置中的固定/每次随机/周期更新模式决定，不接入普通声音或视频周期输入。
- 可访问性：四个输入继续提供明确的“声音/视频周期最小/最大秒数”标签；播放池、播放控制和媒体处理开关行为不变。
- 浏览器复验：本地预览确认左栏周期 Card 数量为 0，音频/画面 Card 内四个周期输入分别显示 `3/5` 与 `8/15`；将声音最小值改为 `4` 后状态立即更新，插话区仍无周期输入，控制台无 error/warning。
- 回归：`pnpm test` 26/26、`pnpm build` 与 `git diff --check` 通过；构建仍只有既存的约 1,018 kB 主包体积警告，本轮未增加依赖。

## Round 26：PortAudio 测试音与桌面同步收口

- 功能：PortAudio Card 在“应用设置”旁补回与正式桌面端一致的固定 `440 Hz / 400 ms` 本地测试音入口；仅在模拟 PortAudio 运行状态可用，不把测试音放回高级声音抽屉。
- 状态：编辑草稿、已应用配置、实际出口和处理状态继续分离；测试音不提交配置，也不改变 dirty 状态。
- 契约：产品需求与桌面架构的测试音冲突按 2026-08-26 顶部最新口径统一为“主页保留、抽屉不重复”。
