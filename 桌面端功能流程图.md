# 桌面端当前功能流程图

## 1. 产品边界

桌面端只处理一个本地源视频、一个持续存在的播放窗口和当前播放音轨。不存在 N 版本离线生成、版本队列、多视频顺序播放或批量 MP4 合成。

## 2. 当前主链路

```mermaid
flowchart TD
    Start[启动桌面端] --> Auth{登录并激活}
    Auth -->|失败| AuthPage[登录/激活页面]
    AuthPage --> Auth
    Auth -->|成功| Import[选择一个本地 MP4]
    Import --> Probe[探测媒体元数据与可读性]
    Probe -->|失败| Error[显示结构化错误]
    Probe -->|成功| Config[配置三个独立开关]
    Probe --> Hash[异步计算整个 MP4 文件 SHA-256]
    Config --> Original[同一窗口立即原声播放]
    Original --> Loop[播放结束回到源视频 0 秒]
    Loop --> Round[播放轮次 +1]
    Round --> Original

    Config --> VideoSwitch{视频处理开关}
    VideoSwitch -->|关| OriginalVideo[原始画面]
    VideoSwitch -->|开| VideoProcess[视频处理模块]
    VideoProcess --> VideoWorker[后台 FFmpeg/FFprobe Worker]
    VideoWorker --> Verify[可读性探测 + 整个 MP4 SHA-256]
    Verify --> Pending[待切换视频缓存]
    Pending -->|当前视频结束| OriginalVideo
    VideoWorker -->|不可用/失败/取消| OriginalVideo
    OriginalVideo --> Window[同一个最终效果窗口]

    Config --> BaseAudio[源视频基础音轨]
    BaseAudio --> Immediate[首项立即原声播放]

    Config --> RealtimeSwitch{话术实时幻化开关}
    RealtimeSwitch -->|关| Keep[不启动 Worker，沿用基础音轨]
    RealtimeSwitch -->|开| Input[输入基础音轨：文件/受控实时流]
    Input --> S2S[speech-to-speech Worker]
    S2S --> VAD[VAD]
    VAD --> ASR[ASR 转写]
    ASR --> LLM[LLM 决策：沿用或改写]
    LLM -->|沿用| Keep
    LLM -->|改写| TTS[TTS 生成当前音频候选]
    TTS --> Gate[音频候选门禁：代际/轮次/采样率/声道/时长/延迟/哈希]
    Gate -->|通过| Replace[替换当前播放视频的音轨]
    Gate -->|失败或超时| Fallback[保持上一条可用音轨]
    Replace --> Keep
    Fallback --> Keep

    Keep --> AudioSwitch{声音处理开关}
    AudioSwitch -->|关| FinalAudio[输出当前基础音轨]
    AudioSwitch -->|开| AudioProcess[普通声音处理模块]
    AudioProcess --> FinalAudio

    OriginalVideo --> Window
    Immediate --> Window
    FinalAudio --> Window
    Window --> Loop
    Loop -.若有待切换缓存.-> Pending
```

## 3. 三个开关的职责

| 开关 | 控制范围 | 关闭时行为 |
|---|---|---|
| 视频处理 | 画面效果、视觉调制频段、视频挂件、后台处理缓存；经过 FFprobe 和整文件 SHA-256 后在循环边界切换 | 使用当前可用画面 |
| 声音处理 | 作用于最终当前音轨的音量、音高、均衡、混响、底噪、音频分析等普通音频能力 | 不执行普通声音处理 |
| 话术实时幻化 | VAD、ASR、LLM 沿用/改写、TTS 和当前音轨替换 | 不启动 speech-to-speech Worker |

三个开关互不隐式联动。首项源音轨立即播放；实时话术幻化只替换当前基础音轨，普通声音处理作用于最终当前音轨；候选失败、过期或来不及时保持上一条可用音轨，不生成新的 MP4，不暂停视频，不重新弹出窗口。

## 4. 实时幻化失败链路

```mermaid
flowchart LR
    Candidate[音频候选] --> Check{候选门禁}
    Check -->|通过| Switch[当前窗口安全换轨]
    Check -->|失败/超时/过期| Fallback[保持上一条可用音轨]
    WorkerError[Worker/LLM/TTS 不可用] --> Fallback
    Slow[生成速度慢于播放安全余量] --> Fallback
    Switch --> Continue[视频继续播放]
    Fallback --> Continue
```

## 5. 管理系统与桌面端边界

```mermaid
flowchart LR
    Admin[React 管理系统] --> Go[Go 控制面]
    Go --> User[用户/角色]
    Go --> Device[设备/心跳/基础信息]
    Go --> Activation[激活码]
    Go --> Pool[大模型号池/租约/用量]
    Desktop[Rust/Tauri 桌面端] --> Go
    Desktop --> Local[本地视频、音频、缓存和研究报告]
    Desktop --> Popup[单一最终效果窗口]
    Go --> Provider[大模型供应商]
```

后台不接收视频文件，不创建媒体任务，不展示版本数量或版本进度；Go 负责 OpenAI-compatible 模型账号登记、连通性测试、租约和调用摘要，桌面端通过单账号直连租约直接调用模型，视频和音频内容保留在本地。

## 6. 研究分析支线

研究分析是独立的本地支线，不参与实时播放决策：

```text
当前源视频/当前处理缓存/当前音频候选
    ↓
完整文件 SHA-256（每个阶段之前计算）
    ↓
内容相似度分析
媒体鲁棒性实验
隐形标记研究
随机扰动实验
内容指纹对比
    ↓
本地研究报告
```

研究支线不连接直播平台，不提供平台审核或检测规避策略。
