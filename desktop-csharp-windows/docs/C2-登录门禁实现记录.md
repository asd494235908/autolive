# Phase C2：WPF 登录门禁

状态：代码已接入·登录错误边界已加固·待真实控制面联调

## 已实现

- `LoginViewModel` 只维护账号、登录状态和短期密码输入；密码不会写入 INI、JSON、日志或 Credential Manager。
- 初始状态为 `SignedOut`，工作台和播放栏保持禁用并由登录门禁覆盖。
- 只有 `Activated` 才能进入工作台；`Authenticated` 仍等待服务端设备授权确认。
- `Offline` 只能从已授权会话进入，表示已进入工作台后的断网续播；冷启动不能用离线状态绕过登录或激活。
- `Disabled`、`ActivationRequired`、未授权 `Offline` 始终 fail-closed。
- 未配置控制面时仍只做本地输入/状态投影，明确提示配置要求，不创建 `HttpClient`；正式环境显式配置并通过 HTTPS 校验后，由 `ControlPlaneAuthCoordinator` 执行真实登录/自动激活/Refresh/Logout/心跳。测试联调使用 `AUTOLIVE_CONTROL_PLANE_ENV=test`，或本地开发使用 `AUTOLIVE_CONTROL_PLANE_ENV=development` 时，允许固定测试地址 `http://101.96.208.132:9090` 或 loopback HTTP；其他远程 HTTP 地址仍拒绝。
- 登录尝试、取消、刷新、退出和状态切换都会清理密码控件与 ViewModel 的内存副本。
- 登录/Refresh 的永久失败会执行状态机返回的删除凭据动作，避免旧 Refresh Token 在失败后继续留存；串行门等待阶段取消会返回受控的 `CONTROL_PLANE_CANCELLED` 结果，不再向 WPF 冒泡 `TaskCanceledException`。
- Credential Manager 异常、控制面客户端异常和用户取消分别映射到脱敏错误；WPF `LoginViewModel` 对控制面调用做最后异常收口，退出时发生未知客户端异常也会 fail-closed 清理本地会话。
- 启动恢复只读取当前 Windows 用户 Credential Manager 中的 Refresh Token；恢复响应仍必须完成设备激活，返回禁用/撤销或无效凭据时保持未授权并清理持久凭据。
- 进入 `Activated` 后由单一后台调度器发送 30 秒心跳；调度器可取消、只保留一个任务，退出会等待该任务 Join，断网补发只保存一条不含 Token 的心跳请求。

## 待真实验收

- 真实控制面地址、TLS、设备绑定、Credential Manager 权限、Refresh Token 失效/禁用响应矩阵和 Windows 实机联调仍待完成；当前不宣称恢复或心跳已通过真实服务端验收。
- 未配置控制面时仍不会发出网络请求；正式登录必须提供通过地址校验的 HTTPS 控制面地址。测试联调或本地开发可分别设置 `AUTOLIVE_CONTROL_PLANE_ENV=test|development` 与 `AUTOLIVE_CONTROL_PLANE_BASE_URI=http://101.96.208.132:9090`；未填写地址时两个环境都默认使用该固定测试地址。2026-09-04 只读复查远程服务的 `/`、`/api/v1/health` 和 `/api/v1/readyz` 均返回 `200`；未使用真实账号执行登录，因此设备绑定、激活、Refresh Token 和心跳仍待真实服务端验收。
- `launchSettings.json` 只在 `dotnet run` 启动时注入环境变量，直接双击 EXE 不会读取它；本地开发/测试应使用 `tools/start-csharp-development.cmd` 或对应 `dotnet run --launch-profile`，否则客户端会按设计保持无网络预览壳并提示“控制面未配置”。

## 验证

- `GpAutoLive.App.Tests`：登录门禁、未配置提示等测试通过；认证编排边界另由 `GpAutoLive.Windows.Tests` 覆盖。
- 本轮新增认证编排回归：登录失败清理旧凭据、Refresh 失败清理轮换凭据、串行门前取消不抛异常；固定远程测试 HTTP 仅显式放行回归；`GpAutoLive.Windows.Tests` 当前 200 项通过。
- 覆盖冷启动门禁、输入长度、未配置登录、Refresh fail-closed、未授权离线拒绝、已授权离线续播、禁用与激活状态。
