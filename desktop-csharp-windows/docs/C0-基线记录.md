# Phase C0：参考基线记录

记录日期：2026-09-02（Asia/Shanghai）

## 只读边界

- 现有 `E:/aotlve/desktop/` 仅作为 Rust/Tauri + React 的行为、状态机、DTO 和验收参考。
- 本次 C# 实施没有修改 `desktop/` 下的 Rust、Tauri、React、配置、测试或资源文件。
- 每次提交前可在仓库根目录执行 `powershell -File desktop-csharp-windows/tools/verify-scope.ps1`，检查已跟踪的 `desktop/` 文件未发生变化。

## C1 已落地的可重复事实

| 项目 | 结果 |
| --- | --- |
| 解决方案 | `GpAutoLive.Windows.slnx` |
| 生产项目 | Contracts、Core、Media、Windows、App |
| 测试项目 | Contracts.Tests、Core.Tests、Media.Tests、App.Tests、Windows.Tests |
| 目标 | .NET 10.0、Windows 10/11 x64、WPF |
| `GpAutoLive.exe` | 162,816 bytes（约 159 KiB，不含运行时、DLL、媒体资源和调试符号；win-x64 framework-dependent 发布） |
| App Release 发布目录（C1 初始记录） | 821,119 bytes（15 个文件，含托管 DLL/JSON/PDB；不含 .NET Desktop Runtime、媒体资源包和用户数据；托管 DLL 合计 448,000 bytes） |
| 依赖 | C1 生产项目无第三方运行时包；测试仅使用 MSTest |
| 外部媒体进程 | C1 不启动 mpv、FFmpeg 或任何 sidecar |

## 本阶段验收命令

在 `E:/aotlve/desktop-csharp-windows` 执行：

```powershell
& .tools\dotnet\dotnet.exe restore GpAutoLive.Windows.slnx --locked-mode
& .tools\dotnet\dotnet.exe build GpAutoLive.Windows.slnx -c Release --no-restore
& .tools\dotnet\dotnet.exe test GpAutoLive.Windows.slnx -c Release --no-build --no-restore --verbosity minimal
```

该命令当前结果（2026-09-02 v8 增量）：锁定还原成功，格式检查通过，构建 0 警告/0 错误；Contracts 9/9、Core 39/39、App 18/18、Media 50/50、Windows 56/56 通过（合计 172 项）。Core 测试包含手动上一项/下一项、四维身份门禁和固定话术状态机；App 测试包含 Windows FileDrop、唯一最终效果窗口和全屏入口边界；Media 测试包含 mpv Windows 命名管道客户端、会话—传输网关、外置运行资源清单校验、有界 mpv 启动计划、固定容量 PCM 环缓和状态帧 fail-closed；Windows 测试包含 Job Object、受管 mpv 宿主、组合生命周期、播放状态监视、控制面 HTTP 传输、冷启动恢复、心跳调度器和 SAPI 适配边界。

最新 v8 framework-dependent 发布根目录为 15 个文件、1,038,499 bytes，`GpAutoLive.exe` 为 162,816 bytes；外置媒体 staging 4 项资源合计 351,999,432 bytes，含法律材料的 staging 树为 352,053,564 bytes。使用仓库内 `.tools/dotnet` 设置 `DOTNET_ROOT`/`DOTNET_ROOT_X64` 启动发布 EXE，保持运行约 1.8 秒后正常关闭，退出码 0；一次采样私有字节为 78,163,968 bytes、工作集为 135,925,760 bytes、22 个线程、1,079 个句柄，退出后无残留 `GpAutoLive/mpv/ffmpeg/ffprobe` 进程。这不是冷启动性能基线，运行时占用未从 GUI 指标中剥离。

## 未宣称事项

- C1 仅提供可运行的 WPF 壳、严格合同和不可变状态骨架；C2/C3/C4/C5 增量也不宣称已完成真实登录、真实媒体播放、GPU83、音频、Windows SAPI、RTMP、虚拟摄像头或抖音 M1。
- 发布资源包/签名、真实 HTTP、PortAudio、捕获能力、单实例唤醒 IPC 和性能采样将在后续阶段按实施计划逐项接入与验收；当前 C3/C4 已完成 WPF 文件选择、已验证 FFprobe 导入组合、纯逻辑媒体池、导入/探测边界、外部进程适配器、Job Object 最小增强、mpv 命令/会话/命名管道边界、受管 mpv 宿主组合、运行资源清单校验和 HWND 预留，但不宣称正式 mpv 资源已通过真实播放门禁。Job Object 在真实 mpv/sidecar 上的孤儿进程、嵌套 Job 与权限矩阵仍未验收。
