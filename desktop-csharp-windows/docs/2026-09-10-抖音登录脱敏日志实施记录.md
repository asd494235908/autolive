# 抖音登录脱敏日志

状态：脱敏登录日志已实现，定向自动测试和本地构建通过；当前真实登录失败原因仍未确认；用户关闭旧客户端后，日志版已于 2026-09-11 00:10 启动。

## 范围

记录扫码启动、取码、轮询状态变化、确认后登录回跳和账号身份校验，以及失败时的阶段、固定错误类别、HTTP 状态码和平台数字错误码。只观察既有登录过程，不改变上游登录、验证码、重试、限频或 TLS 校验策略，不新增短信登录。

Python 发出固定白名单 `auth.diagnostic` 事件，C# 校验后写入当前用户 `LocalAppData/GpAutoLive/logs/douyin`。日志单文件最多 1 MiB、仅一个轮转备份；界面显示实际日志位置或保存失败，不伪造成功。

不记录异常原文、响应正文、完整 URL、Cookie、Token、验证码、二维码、用户身份、弹幕正文或签名材料。重复轮询成功不逐条写日志，未知错误不猜测为验证码或登录失效。

## 验证边界

Python 验证阶段与错误提取、敏感数据不进入诊断以及既有取消/登录行为；C# 验证协议字段白名单、落盘/轮转/失败及宿主生命周期；WPF 验证真实日志状态投影和小尺寸布局。运行受影响目标测试和本地构建，真实登录结果另行记录。

本轮不执行弹幕发送。此前单次中文问号的发送验收仍未完成，不由诊断过程触发。

## Python 实施与验证

- 新文件 `sidecars/douyin/login_diagnostics.py` 负责固定字段、阶段观察、响应码提取、错误分类及每次登录最多 64 条去重诊断；原 adapter 只接入观察器，sidecar 通过协议发布。
- 上游 checkout 不修改，既有固定提交、取消、TLS 及重试语义保留。响应体最多检查 64 KiB JSON，只选数字字段；验证码相关响应头只记录是否出现，不记录值。
- `test_login_diagnostics.py` 首次因模块尚未实现而失败；实现后 7/7。新增侧进程事件顺序检查，加既有协议、取消、登录复用、Protobuf 等，Python 模块总计 18/18，0 失败。
- 命令（仓库根目录，优先使用 `AUTOLIVE_CONDA_ENV`，未设置则 `gpautolive-douyin`）：`E:/Miniconda3/Scripts/conda.exe run -n gpautolive-douyin --no-capture-output python -m unittest discover -s desktop-csharp-windows/sidecars/douyin -p 'test_*.py' -v`。
- 自动测试不代表已经复现用户真实登录错误；本轮尚未重新扫码或发送弹幕。

## C# 整合与本地验证

- 宿主协议与落盘：Windows 抖音目标测试 81/81（0 失败、0 跳过）；WPF 日志与登录/退出/布局相关测试 37/37。加 Python 18 项，本轮相关自动测试 136 项通过。
- 命令：`.tools/dotnet/dotnet.exe test tests/GpAutoLive.Windows.Tests/GpAutoLive.Windows.Tests.csproj --artifacts-path artifacts/douyin-host-log-tests --filter FullyQualifiedName~Douyin --verbosity quiet`（在 desktop-csharp-windows 下执行，记录命令等价于主线程绝对 artifacts 路径）。
- 日志 UI 新增 5 项先红后绿；测试输出位于 `artifacts/douyin-diagnostic-ui-tests`。宿主全部 fake sidecar 使用各自临时日志目录，不污染用户日志。
- 本地构建：`.tools/dotnet/dotnet.exe build src/GpAutoLive.App/GpAutoLive.App.csproj -c Release -p:Platform=x64 --artifacts-path artifacts/douyin-diagnostic-build --verbosity minimal`，8.24 秒，0 警告、0 错误。首次加 `--no-restore` 因新隔离目录缺少 assets 失败，正常还原后成功，无依赖升级。
- 源码/测试定向 `dotnet format whitespace --verify-no-changes` 及 `git diff --check` 通过。未运行无关全量测试、全量发布或安装包构建；改动限定在抖音登录诊断链。

## 消融实验

- 移除开发过程中未使用的诊断终态字段；不新增第三方依赖、异步日志队列、后台轮询或通用日志框架。
- 保留白名单协议、每次登录诊断条数上限、1 MiB 文件与单备份、路径重解析点拒绝、写失败提示：分别保证敏感数据边界、容量有界及实际保存状态准确。
- 不扩展本轮清理范围；登录、取消、退出和手动/自动发送边界仍沿用既有实现。
- 代码检查、136 项自动测试及本地应用构建通过；真实页面点击、二维码登录后日志复现仍未验证。窗口控制反复返回 `failed to activate captured window`，没有强杀旧应用。容器健康与外部模型调用不涉及；没有发送弹幕，也没有把模拟测试视为真实抖音业务成功。

## 日志版启动检查（2026-09-11）

- 用户确认旧客户端已关闭，进程检查确认无残留后运行 `tools/start-csharp-development.cmd`。本地 Release x64 构建 4.58 秒，0 警告、0 错误；启动日志 `artifacts/douyin-diagnostic-start-20260911-001039.log`，stderr 为空。
- 新进程 PID 28904，启动 00:10:44；真实窗口读取确认存在“诊断日志：尚未创建，开始连接后记录”。此时尚未发起登录，因此日志文件尚不存在，符合懒创建规则。
- 下一次连接将写入 `C:/Users/asd49/AppData/Local/GpAutoLive/logs/douyin/login.ndjson`；真实扫码失败复现与落盘内容仍待验证。本轮只更新启动，没有发送弹幕。

## 用户失败复现与直接调用对照

- 00:15:46 客户端日志收到 `qr_poll/scanned`；00:15:51 收到 HTTP 200、平台业务码 2046，继而上游异常、宿主失败。尚未进入 `login_finalize` 或 `self_identity`，不能把这次错误说成二维码过期或确定需要验证码。
- 用户要求直接调用开源仓库。通过 Conda 环境在独立 Python 进程直接调用固定 checkout 的 `DouyinAuth.from_qrcode_login(timeout=120, show_qr=False, on_qrcode=..., bootstrap_creator=False)`，绕过 C#、sidecar 和 UpstreamAdapter。仅保留 TLS 证书验证、20 秒请求超时和凭据/原始日志保护；关闭创作者初始化，不发送弹幕。
- 临时运行入口 `artifacts/douyin-native-login-20260911/run.py`；结果 `result.json`。正常生成及刷新二维码，120 秒等待后返回 timeout、无平台数字错误码、登录未完成。此对照没有成功登录证据，也没有复现 2046，不能据此判定宿主或上游已修复。
- 初次 Conda `python -c` 多行参数被 Conda 拒绝，未发起网络；改为临时脚本执行后进入真实扫码等待。没有修改开源 checkout 或产品代码，没有保存登录凭据。

### 直接调用重试结果（00:32）

用户要求二维码过期后重试。再次通过 Conda 运行相同独立入口，新二维码生成并展示后，上游直接返回 `login_returned=false, platform_code=2046, error_category=upstream_error`，进程正常结束。本次绕过 C# 宿主、sidecar 和 UpstreamAdapter 仍复现 2046，说明错误并非仅由这些客户端封装触发；平台错误码具体原因尚未证实，不能据此声称短信验证、账号限制或任何特定风控原因。没有发送弹幕、保存凭据或修改登录协议。
