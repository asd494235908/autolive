# 参考图参数对比与正式化清单

> 更新日期：2026-08-24

## 1. 结论与口径

参考图 1–5 中可辨识的音频、视频、高级视觉、挂件、切片、检测和画面修复参数全部是正式产品需求，不再使用“研究字段”“实验参数”或 `research_*` 命名。当前统一契约为：

- `MediaEffectParams`
- `AudioEffectParams`
- `VideoEffectParams`
- `AdvancedEffectParams`
- `get_default_media_effect_params`
- `validate_media_effect_params`

本清单使用三种状态：

| 状态 | 判定标准 |
| --- | --- |
| 已实现并生效 | 当前 Rust Worker 已把非默认值映射到实际 FFmpeg/播放处理链；是否处于 `runtime` 仍以当前输出快照为准 |
| 已有模型待接入 | Rust 正式类型、默认值或校验已存在，但 Worker 尚未映射算法；保存/校验成功不代表生效 |
| 正式需求但缺失 | 参考图可辨识，当前正式类型或执行算法尚缺；需要补字段、范围、默认值、映射和测试 |

标有 `〔OCR待核〕` 的名称或语义无法从截图稳定辨认。开发前必须对照原始高清图或由产品确认，不能按猜测实现。

## 2. 已实现并生效

| 模块 | 参考图可辨识参数 | 当前实现说明 |
| --- | --- | --- |
| 视频基础 | 亮度、对比度、饱和度、色相旋转、模糊、锐化、细节增强、噪点 | 已进入 FFmpeg 视频滤镜链 |
| 视频色彩/修复 | 高光、阴影、暗角、红色通道保护、色彩空间转换、图像修复 | 实时 GPU 主链中的两项色彩空间字段定义为 libplacebo 感知色域映射开关与强度；启动固定并核验 BT.709 primaries、sRGB TRC、full range、203 nit 输出合同，不再使用旧 BT.601→BT.709 RGB 强度混合语义。离线/历史 Worker 描述不作为当前入口。 |
| 视频几何 | 动态裁剪、裁剪插值、X/Y 空间偏移、像素级扰动/缩放、水平/垂直翻转、画面旋转 | 已有实际 Worker 映射；只有真实处理输出提交后才标记运行态 |
| 视频时序 | 源帧率动态锁定、帧率扰动百分比/频率/幅度、帧内/帧间扰动 | 锁定时按源探测帧率建立 CFR；扰动时进入 VFR 时间戳、`noise/tblend` 链并显式使用 `-fps_mode vfr` |
| 音频电平 | 输入增益、输出增益、响度调整 | 已进入 FFmpeg 音频滤镜链 |
| 音频 EQ | 低/中/高频 EQ、滤波 Q 值 | 已进入 FFmpeg 均衡链；参考图的 12 个固定频段不是本三段 EQ |
| 音高与共振峰 | 音高微移、共振峰偏移 | Signalsmith Stretch 在 FFmpeg 多支路混音后统一执行；各支路共享主配置的音高与共振峰 |
| 音频时序 | 播放速度、淡入、有限输入淡出 | 播放速度由 FFmpeg `atempo` 执行；无限实时流安全跳过需要完整反向缓存的淡出实现 |
| 声音效果 | 自然动态、本地音色 ID、轻混响、降噪、相位扰动、颤音频率/深度、环境噪声比例/电平、环境声素材混合、干湿比 | 自然动态使用慢速增益包络，音色 ID 使用确定性轻量 EQ；环境声经真实第二输入循环解码，完整处理声建立短反射湿声支路后混合 |
| 音频频域与特征 | 频谱扰动、高频音频扰动、频谱盲区、MFCC 偏移/维数、SNR 目标/波动、当前共振峰 | FFmpeg 执行频谱/高频/盲区滤波；realFFT/rustdct 执行 1–40 维 MFCC 分析与原相位重建；滚动 RMS 执行可实现方向的 SNR 增量噪声；LPC 输出 F1/F2/F3，当前共振峰只读投影 F1 |
| 音频输出 | 显式采样率、输出码率 | 采样率进入运行链；输出码率只在实际编码输出时生效 |
| 高级波频 | 波频强度/电平/颗粒数、通道偏移、空间维度、频率空间 X/Y 偏移、帧扰动概率、12 个 `65–20000Hz` 权重、目标/核心频率、动态均衡阈值 | 已进入 `geq/noise`；`65–20000Hz` 明确为视频空间频段，按对数映射到每幅画面 1–32 个空间周期，不进入音频频谱 |
| 挂件/切片 | 随机几何挂件数量/透明度/大小、抽象几何脸数量/大小/透明度、画面偏移、切片长度/最小长度/触发间隔 | 已进入确定性 `geq` 叠加与时间门控；最小长度约束解锁画中画的源片段窗口，“抽象脸”不检测或合成真人 |
| 画中画/局部效果 | 同源画中画比例/旋转/像素扰动/时间轴锁定、局部模糊、边缘填充/羽化、高光扰动、异步旋转、变换平滑 | 已进入单一 FFmpeg complex graph；关闭时间轴锁定时使用有界延迟源片段，平滑时长控制 smoothstep 过渡 |
| 视频边界 | 画面边缘柔化 | 使用边缘掩码、模糊支路和 `maskedmerge`，只作用于边缘区域 |

## 3. 已有模型待接入

当前 `AudioEffectParams`、`VideoEffectParams` 和 `AdvancedEffectParams` 中语义已确认的字段均已有真实执行或只读分析边界，本节暂无条目。后续新增字段仍必须先补范围、默认值、执行语义和测试，不能把“已有结构”当作“已经生效”。

## 4. 正式需求但缺失

### 4.1 图 1

- 主体保护、人脸色彩保护需要检测模型、模型许可、性能基线和可验证输入输出，尚未引入。
- 边缘外扩/填充比例 `〔OCR待核〕`；已确认的边缘柔化、边缘填充开关和边缘羽化已接入。
- 目标频率之外的频率动态展示、空间扰动频率 `〔OCR待核〕`。
- 信号抖动输入/输出增益、信号抖动比率/干湿比 `〔OCR待核〕` 的独立语义。

### 4.2 图 2

- 浮点/漂浮点类型 `〔OCR待核〕`；已确认的随机几何挂件开关、数量、透明度和尺寸已接入。
- 抽象人脸避让，以及眼、鼻、嘴、轮廓样式字段。
- 画中画高频嵌入 `〔OCR待核〕`；同源画中画切片、旋转、像素扰动、锁定/解锁时间轴和间隔已接入。
- 空间不透明度、抗震/抗扰级别 `〔OCR待核〕`；局部模糊启用/范围/半径/间隔和边缘羽化已接入。
- 变换平滑启用与时长、边缘填充启用已接入。
- 旋转间隔、偏移间隔、缩放间隔、局部色彩空间间隔。
- 微扰 X 偏移最大/最小、缩放最小/最大、亮度范围、饱和度范围、扰动时长；高光扰动启用/间隔和异步旋转最小/最大已接入。
- 静态/冻结检测启用、阈值、回退/返回偏移。

### 4.3 图 3

- 音频高频扰动最小/最大步长、特征掩码/随机化强度 `〔OCR待核〕`；已确认的启用、间隔、强度、电平已接入。
- 红纹/红色补偿 `〔OCR待核〕`；帧率动态锁定、动态均衡、图像修复和帧率扰动已接入。
- 高级去重启用、去重间隔、最小持续时长、空间不透明度、随机形状大小/形状、抽象人脸避让。
- 特征相似度阈值、回退偏移。
- 处理日志清理启用、时间变速/时伸启用与间隔、软件性能检测启用 `〔OCR待核〕`。

### 4.4 图 4–5

- 相位实时诊断卡及下一次声音周期倒计时；波形、频谱、响度、环境底噪、MFCC、SNR 和 LPC 共振峰诊断已来自最终混音 PCM，配置值与只读测量值保持拆开。
- 防重/防重模块状态 `〔OCR待核〕`、不可见水印强度、随机图形和抽象人脸的完整渲染语义。
- 画中画独立窗口语义；当前已实现的是最终输出内的同源画中画叠加，不创建第二播放窗口。
- 图中“声纹每 3–5 秒自动切换”“音色库数量”等属于普通声音预设/音色资源需求；不得解释为实时话术幻化。

## 5. OCR 待核规则

- 上述待核标签只登记“正式需求但缺失”，不进入字段名、默认值或 IPC。
- 产品确认后使用清晰业务名和带单位后缀的字段名；不直接采用截图中的模糊简称。
- 截图数值是运行时示例，不是本项目默认值。范围和默认值以《媒体参数范围与默认值》及后续确认记录为准。

## 6. 依赖建议

以下记录当前依赖决策及尚未实施能力的选型建议。

| 能力 | 建议 | 当前结论 |
| --- | --- | --- |
| 基础视频/音频、切片、叠加、冻结检测 | 继续使用现有 [FFmpeg Filters](https://ffmpeg.org/ffmpeg-filters.html) + Rust 有界调度，不增加依赖 | 仓库实测 FFmpeg 为 `n8.1.2-44-g7c533d0f86`（2026-08-15），已包含 `afftdn`、`arnndn`、`anequalizer`、`vibrato`、`crop`、`drawtext`（freetype/harfbuzz/fontconfig/fribidi）、`freezedetect`、`overlay`、`libvmaf`；已编译 OpenCL/Vulkan/CUDA/AMF/QSV/VAAPI，但编译包含不等于目标机器可用，必须运行时探测硬件、驱动和具体滤镜能力 |
| 人脸/主体检测与避让 | 优先 [ONNX Runtime](https://onnxruntime.ai/docs/) + 明确版本/许可证/输入输出的模型；只有大量传统 CV 操作确有需要时再评估 [OpenCV](https://opencv.org/license/) | 尚未引入；先确定模型、准确率/性能基线和安装包体积 |
| 高质量变调、普通变速、共振峰偏移 | 使用 MIT [Signalsmith Stretch](https://github.com/Signalsmith-Audio/signalsmith-stretch) 处理混音后音高/共振峰，普通变速继续使用 FFmpeg `atempo` | 已进入实现；不依赖 Rubber Band 商业许可。Cargo/`Cargo.lock` 固定版本，商业发布必须携带 Signalsmith Stretch 与 Signalsmith Linear 的 MIT 许可文本，并完成目标平台 C++ 构建、静态链接、延迟、包体、CPU/内存和音质验收 |
| 降噪 | 先使用现有 FFmpeg `afftdn`；需要神经降噪时优先复用现有 `arnndn` 并对模型来源、许可证和分发权逐项核验，不必先引入 RNNoise 原生库；质量仍不满足时再评估 [RNNoise](https://github.com/xiph/rnnoise) | RNNoise 为 BSD-3-Clause；当前未引入 RNNoise 原生库，也未新增模型资源 |
| MFCC/SNR/共振峰分析与 MFCC 重建 | 使用 `realfft 3.5.0`（MIT）+ `rustdct 0.7.1`（MIT OR Apache-2.0），LPC 与滚动 RMS 保持项目内小型有界实现 | 已进入实现并由 `Cargo.lock` 固定；两者为本地纯 Rust 计算依赖，无模型和运行时下载，未引入 Essentia 的 AGPL/商业授权负担。正式发布前仍须做依赖安全审计、release 体积和长时间 CPU/内存验收；移除依赖时必须同步撤下非零 MFCC 重建与对应诊断入口 |

任何新依赖都必须在开发前补齐真实调用字段、替代方案、版本锁定、许可证、安全记录、包体/性能预算、模型来源和移除路径。

## 7. 保持不变的非目标

- 不开发实时话术幻化、speech-to-speech、VAD/ASR/LLM/TTS、候选话术音轨换轨或对应模型租约直连。
- 不开发 OBS/RTMP 推流、直播平台接入、自动发布或断线重连。
- 不提供面向平台审核/检测的规避、平台评分、内容相似度、媒体鲁棒性或内容指纹结论。
- 不恢复旧 `research_*` 参数类型、独立报告 Worker、状态轮询或报告产物链路。

## 8. 变更记录

- 2026-08-24：补齐当前正式模型中剩余 21 项真实执行/诊断边界；明确 `65–20000Hz` 是视频空间频段，接入 12 段权重和相关高级视觉参数；音频接入真实第二输入、干湿支路、MFCC/SNR/LPC 分析处理等，并采用 `realfft 3.5.0`、`rustdct 0.7.1`。模型内“已有字段待接入”清零，外部检测模型和 OCR 待核字段继续保持缺失状态。
- 2026-08-24（历史分类，已由同日正式参数完整接入覆盖）：当时固定视觉频段、分析字段和第二音频输入仍保持待实现；现已接入，外部模型和 OCR 待核项继续保持缺失。
- 2026-08-24：高质量音高与共振峰偏移改用 MIT Signalsmith Stretch，在 FFmpeg `amix` 后统一处理；普通变速保留 FFmpeg `atempo`。多支路共享主配置的音高/共振峰，失败保持旧轨或 WebView 原声。
- 2026-08-24：首次建立参考图 1–5 的正式参数三态清单；记录 OCR 待核项、当前真实映射和依赖建议。
