# 项目 AI 开发约束

本文件适用于当前仓库的 Go 后端、React 管理系统和 Rust/Tauri 桌面客户端。默认使用简体中文沟通、解释和交付总结。

## 1. 规则优先级与当前范围

- 用户当前明确要求、系统安全要求和验收条件优先于本文件。
- 本文件优先于通用个人偏好；现有仓库约定、依赖版本和官方文档用于解决具体实现细节。
- 开始开发前必须阅读：
  - [`产品需求文档.md`](./产品需求文档.md)
  - [`系统架构总览.md`](./系统架构总览.md)
  - [`管理系统架构.md`](./管理系统架构.md)
  - [`桌面客户端架构.md`](./桌面客户端架构.md)
  - [`媒体参数范围与默认值.md`](./媒体参数范围与默认值.md)
  - [`长任务开发总计划.md`](./长任务开发总计划.md)
- 涉及具体技术栈时，必须同时阅读对应专项约束：
  - React/TypeScript 或管理端 UI：[`开发约束/前端开发约束.md`](./开发约束/前端开发约束.md)
  - Go 后端：[`开发约束/Go开发约束.md`](./开发约束/Go开发约束.md)
  - Rust/Tauri 核心：[`开发约束/Rust开发约束.md`](./开发约束/Rust开发约束.md)
  - Tauri 的 React 界面同时遵守前端和 Rust/Tauri 相关约束。
- 当前首版范围包括后台账号/设备/激活码/大模型号池管理、Rust 客户端单视频循环播放、独立的视频处理开关、独立的声音处理开关、独立的实时音频幻化开关和本地研究分析模式。
- 当前单视频循环不需要管理系统创建媒体任务；管理系统只负责用户、设备、激活码、OpenAI-compatible 模型账号登记/测试、模型号池租约和调用摘要。
- 当前产品导入视频后不生成 N 个 MP4、不生成离线话术版本、不创建版本队列；同一个源视频在同一个窗口内循环播放。
- 仓库中仍可能保留早期 `VariantTask`、阶段 Worker、管理端页面和历史迁移；这些只用于迁移审计，不属于当前产品入口。新代码不得继续调用或扩展它们，彻底删除属于单独的代码清理任务。
- 当前不做 RTMP/OBS 推流、断线重连、直播平台接入、口型同步、滤镜美颜、弹幕互动、多平台一键分发和面向平台的检测规避；实时音频幻化、内容相似度分析、媒体鲁棒性实验、隐形标记研究、随机扰动实验和内容指纹对比仅作为受控本地预览/研究能力。
- 后续长任务必须按 [`长任务开发总计划.md`](./长任务开发总计划.md) 的阶段、清理边界和验收命令执行；旧 PRD 的媒体任务、版本队列、服务端模型代理和 `speech_preview_task` 不得重新引入。

## 2. 开发前分析

编码或改行为前必须完成：

1. 阅读相关需求、目录说明、依赖清单、入口、配置、测试和相邻实现。
2. 明确目标、非目标、输入输出、成功/失败/取消/恢复路径和可验证验收条件。
3. 搜索所有调用方、生产方、路由、事件、API 类型、数据库表、配置和测试，确认影响面。
4. 说明新文件职责、调用方、依赖方向和放置理由；无法说明时先收窄职责。
5. 涉及网络、并发、持久化或桌面资源时，先定义超时、取消、重试、幂等和释放策略。

禁止只看文件名猜架构，禁止只修复一个可见页面而忽略复用点，禁止借机重构无关区域。

## 3. 架构和模块边界

- Go 后端是控制面，React 是管理界面，Rust 客户端是本地执行面。
- React、Go、Rust 通过 `/api/v1` 和 OpenAPI 共享契约；契约必须有唯一事实源，生成代码与手写代码分离，禁止手工修改生成产物。
- Rust 客户端不直接访问 PostgreSQL，不直接读取服务端密钥配置；本地视频不经过 Go API 转发。
- 实时音频幻化不做运行时音频重复检测，不接入 Chromaprint/landmark 历史库；由桌面端把播放轮次、音频片段/转写、上一轮输出和约束交给 `speech-to-speech` Worker，由 LLM 决定沿用或改写，再由 TTS 生成候选音轨。Go 负责 OpenAI-compatible 模型账号登记、连通性测试、短期单账号租约、额度/使用记录和审计；Rust 使用当前租约直接调用供应商，Go 不处于模型请求数据路径。
- 模型凭证、Token 和设备凭证必须使用安全存储；最终效果通过桌面端弹窗展示，不经过 Go 服务转发视频内容。
- 最终效果预览窗口采用单实例生命周期；切换视频、音轨或话术时只更新窗口内部媒体源，不重新创建或闪烁弹窗。
- 导入视频后首项通过可读性校验即可在同一窗口原声播放，并持续循环同一个源视频；循环不能被误写成 N 版视频队列。
- 视频处理开关、声音处理开关和实时音频幻化开关必须彼此独立，分别控制视频效果、普通声音效果和“LLM 决策/改写 → TTS → 当前音轨替换”。
- 实时音频幻化不生成 N 个 MP4；变体以实时音频帧、受控流或临时音频缓冲形式替换当前播放视频的基础音轨，失败时保持上一条可用音轨。
- 实时音频幻化的音频候选需要预加载、同步校验、取消和资源释放，但不需要为视频准备下一版本。
- 播放引擎实现不固定为 A/B；可以使用双音频槽、单引擎预加载或成熟媒体引擎的单实例候选切换能力，验收标准是同一窗口无黑屏、无重新弹窗、无明显停顿。
- 视频效果输出必须解码、处理并重新编码，不使用视频流 `copy`；FFmpeg 等工具只能作为本地媒体处理能力，不承担 RTMP/OBS 推流。
- 研究分析参数必须记录算法版本、参数单位/范围、参数快照、随机种子、每个产物的阶段输入/输出 SHA-256；MP4 必须对整个文件字节流计算，确保实验可复现。
- 内容相似度分析、媒体鲁棒性实验、隐形标记研究、随机扰动实验和内容指纹对比只能运行在本地研究模式，不连接平台、不自动发布、不以规避平台审核或检测为目标。
- 65Hz～20000Hz 参数归属视频模块，表示视觉调制频率；必须同时展示目标频率和按输出帧率/处理链计算的实际生效频率，不能把它当成音频频段或承诺超采样上限的可见效果。
- 媒体参数范围、单位、默认值和自动读取源素材规则以 [`媒体参数范围与默认值.md`](./媒体参数范围与默认值.md) 为唯一契约。
- 业务模块必须通过清晰的公共入口通信，下层不得反向依赖具体页面或上层业务。
- 不创建没有准入规则的 `utils`、`common`、`helpers`、`shared` 或 `misc` 大杂烩目录。
- 接口、Trait、Hook、组件和公共函数只在存在真实调用边界、替换实现或测试隔离需求时创建；禁止为假想复用预留抽象。
- 共享状态优先使用单一所有者、不可变数据或消息传递，避免全局可变状态和重复事实源。

## 4. 依赖、技术选型和清理

- 前端、Go 后端和 Rust/Tauri 桌面端遇到成熟第三方库已经可靠解决的能力时，必须优先使用成熟库或官方 SDK，不得重新编写同类工具。
- 先检查项目已安装依赖和当前框架；只有没有合适的成熟方案，或现有方案明确不满足需求时，才允许自行实现，并记录原因。
- 新依赖必须说明真实调用点、替代方案、版本/维护状态、许可证、安全记录、体积和移除路径，并使用可复现的版本策略。
- 禁止只因流行度、旧经验或 AI 记忆选择依赖；技术信息变化时查阅当前官方文档。
- 同类能力原则上只保留一种主方案；确需并存时明确边界和迁移条件。
- 写代码时必须检查并删除本次改动产生或暴露的未使用代码、导入、类型、导出、依赖、配置、资源和调试日志。

### React 管理端组件库

- React 管理端统一使用 Ant Design。
- Ant Design 已有对应能力时，必须直接使用 Ant Design 组件，不得重新实现按钮、表单、表格、弹窗、抽屉、分页、通知等重复组件。
- 不得覆盖或修改 Ant Design 组件的内部样式，不得通过 `.ant-*` 选择器强行改写组件行为和视觉。
- 需要调整布局时使用页面布局、容器、组件官方 Props 和公开配置；如果官方能力无法满足，先说明限制和替代方案，再向用户确认。

## 5. API、输入和数据

- 所有来自 API、路由、表单、文件、IPC、客户端、第三方服务和本地存储的数据都视为不可信输入。
- 在信任边界校验类型、长度、范围、格式、数量和业务组合；客户端校验只能改善体验，不能代替服务端校验。
- HTTP 状态码表达传输结果，业务错误码表达稳定业务语义；禁止把所有错误返回为 HTTP 200。
- 写操作必须使用幂等键、唯一约束、状态机或等价防重机制；不能假设用户只会提交一次。
- 分页、排序、筛选、请求体、上传文件、下载文件、路径和 URL 必须有上限、允许范围和安全处理。
- 路径必须规范化并限制在允许目录；出站 URL 必须限制协议、主机、端口、重定向和解析后的目标，防止路径穿越与 SSRF。
- API DTO、领域模型、数据库模型、表单状态和视图模型在职责不同的时候必须分离，并在边界转换、校验和测试。

## 6. 身份、权限和敏感信息

- 认证成功不等于有资源权限；每个受保护操作都必须在服务端校验主体、动作和资源归属。
- 密码、Token、Refresh Token、激活码、API Key、连接串和签名不得进入源码、普通配置、日志、Tracing、错误正文、测试快照、前端静态资源或提交记录。
- Go 服务端只保存模型 API Key 的安全引用/密文，不向 Rust 下发完整号池；Rust 只能获得当前单账号、短时、限额、可回收的直连租约凭证，并在内存中使用。供应商不支持短期/可撤销凭证时，该账号不能启用客户端直连。
- Rust 的模型直连只发送当前话术上下文，不上传视频或完整音频文件；调用摘要、usage、延迟和错误通过受保护接口回报 Go，服务端标记数据来源，不能把客户端自报 usage 伪装成供应商权威账单。
- 激活码只允许一次性核销、过期和作废；数据库保存哈希，不保存可重复使用的明文。
- Rust/Tauri IPC 使用允许列表、明确命令、输入校验、权限检查和最小返回值，禁止暴露任意命令执行或任意文件访问。
- 设备信息遵循最小采集原则，不默认采集 MAC 地址、硬盘序列号、摄像头、麦克风或用户目录文件列表。

## 7. 错误、超时、并发和资源生命周期

### Go

- 可预期失败通过返回值传递；按错误类型分类，使用错误链保留上下文，禁止吞错或用错误字符串控制流程。
- 普通输入、网络、数据库和业务失败不得 Panic；Panic/Recover 只能位于明确的进程、请求或任务边界，并且不能伪装成成功。
- `context.Context` 作为首个参数显式传递；下层不得用 `context.Background()` 绕过上游取消。
- 外部调用必须支持取消并设置超时；Goroutine 必须有所有者、退出条件、取消路径和等待机制；并发必须有上限。
- Channel 必须明确发送方、接收方、关闭方和容量；禁止用无限缓冲掩盖背压、泄漏或消费过慢。
- 文件、Response Body、Rows、事务、Ticker、连接等资源获取后立即安排释放，并检查影响数据完整性的关闭、提交和刷盘错误。

### Rust

- `Option` 表达合理缺失，`Result` 表达可失败操作；正常错误路径禁止 `unwrap`、`expect`、`panic!` 或丢弃 `Result`。
- 先明确所有者、借用范围和生命周期，禁止为绕过编译器无条件增加 `clone`、`Arc`、`Mutex`、`Box::leak` 或 `'static`。
- 异步任务必须有所有者、取消路径、超时策略、Join/监督和错误传播；禁止丢弃 `JoinHandle` 创建无人管理任务。
- 阻塞 I/O 和 CPU 密集工作不得阻塞异步执行器；锁临界区应短，禁止跨 `.await` 持有同步锁。
- Channel 必须有容量、关闭和背压策略；`Send`/`Sync` 必须来自真实线程边界，禁止用不安全实现伪造线程安全。
- `unsafe` 仅在安全 Rust 或成熟依赖不能合理满足需求时使用，必须最小封装、写明不变量并补充边界验证。

### React/Tauri UI

- React 渲染保持纯净；Effect 只用于同步外部系统，依赖、取消、竞态和清理必须完整。
- 服务端状态、URL 状态、组件状态和外部运行时状态分开管理；禁止把服务端缓存复制到多个 Store 手工同步。
- 页面和桌面窗口必须处理 loading、empty、error、无权限、取消、离线和恢复状态。
- 外部输入进入 DOM、URL、IPC 或文件系统前必须按上下文校验；禁止危险 HTML、危险 URL 和任意代码执行。
- 组件、Hook、窗口事件、定时器、媒体资源、对象 URL、IPC 订阅和长连接必须有对称清理。
- 可访问性和桌面环境按实际运行时验证：语义、键盘、焦点、缩放、窗口尺寸、多屏、主题和权限拒绝。

## 8. 数据库、缓存和消息

- SQL 必须参数化；动态列名、排序字段和表名只能从固定白名单选择。
- 事务必须短小，只覆盖必须原子提交的数据库操作；禁止在事务或数据库锁内做网络调用、等待用户或长时间 CPU 工作。
- 连接池、查询、请求体和结果集必须有上限与超时；检查 N+1、无界查询和循环逐条写入。
- 迁移必须可审查，并说明兼容发布顺序、回滚/前滚、锁表和大数据量影响。
- 缓存不是默认事实源；引入前必须定义 Key、TTL、容量、失效、一致性和恢复策略。
- 消息消费默认可能重复，必须定义幂等键、确认时机、重试上限、退避、死信和结果未知时的核对/补偿语义。

## 9. 日志、可观测性和性能

- 使用结构化日志；请求 ID/Trace ID 应跨请求、异步任务、数据库、消息和出站调用传播。
- 记录操作、结果、耗时和必要上下文，但不得记录 Secret、完整请求体、个人数据或高基数指标标签。
- 错误在最有行动价值的边界记录一次并保留错误链，禁止每层重复打印同一错误。
- 服务和客户端应能观察请求量、错误率、延迟、资源、连接池、队列积压、取消和超时。
- 性能优化前必须有 Benchmark、Profiler、Trace 或指标基线；禁止凭感觉增加缓存、Memo、复杂并发或自定义内存策略。

## 10. 测试、构建和交付

- 新增行为或修复缺陷，先写能失败的测试；文档/配置任务必须定义等效的静态或行为验证。
- 测试按风险分层：纯逻辑用单元测试，跨模块用集成测试，关键用户流程用少量 E2E/手工冒烟。
- 测试必须覆盖适用的成功、错误、空值、超时、取消、并发、竞态、权限和恢复路径；禁止依赖任意长睡眠掩盖竞态。
- 交付前按实际项目执行格式化、Lint、类型检查、测试、竞态检查和本地构建；未执行或环境不支持的检查必须如实报告。
- 构建在本地开发机或 CI 完成，禁止把源码放到服务器构建；服务器只接收发布产物。
- 不使用 Git worktree；开发工作直接在当前分支和工作区进行。
- 开发任务先整理功能边界；存在两个以上相互独立的开发任务时可并行使用子线程，主线程必须在子线程完成后检查 diff、审查设计并统一验证。
- 子线程执行期间，除非明确需要停止，否则不得主动发送停止指令。
- 交付报告必须列出改动文件、实际执行的命令和结果、未验证项、剩余风险和必要后续动作。
