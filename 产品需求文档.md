# PRD：本地录播视频处理与最终效果展示平台

> 视频周期参数连续展示（2026-08-31，代码与聚焦测试完成）：普通视频参数和高级视觉面板在正式参数配置加载后必须始终完整显示。Rust `RealtimeVideoRuntime` 仍只在完整 shader/CPU4 提交已经回读、mpv PTS 越过提交前位置且 N 晋级 `Active` 后，原子发布 `active_cycle_snapshot={sequence,fingerprint,video,advanced}`；该快照必须与同一 DTO 的 `N.sequence` 和 `active_plan_fingerprint` 一致。`Source`、换源、`Applying`、`ResultUnknown`、读回或呈现尚未确认、失败及无 Active N 时仍发布 `null`，但 React 不再用“当前视频周期参数待确认”替换整块面板：同一 `playback_generation` 的非 Source 确认间隙保留上一轮已确认显示值，新代次尚无已确认值以及 Source、关闭处理或非视频状态显示当前 `mediaEffectParams` 配置值；新 Active N 到达后只更新真实变化值。保留值只服务只读显示，不得冒充新周期已确认状态，不得写回配置状态或再次触发 `configure_realtime_video_cycle`。CPU4 显示仍结合对应已确认状态的 `parameter_support`，只有亮度、对比度、饱和度和色相可标为已执行。本项不改变普通声音周期。

> 视频 PTS 回退边界（2026-08-31，代码修复及 TS→MP4 联合实机复验完成）：同一媒体段自然播放时，Rust 视频周期控制器必须以 mpv 已观测最大 PTS 作为调度高水位；普通容器只允许实际 FPS 对应的两帧加 `2ms` 舍入裕量且最多 `100ms`，`.ts/.m2ts` 允许六帧加 `2ms` 且最多 `500ms`，覆盖真实 22.001848fps TS 的 227ms 回摆。Rust 音画同步主动执行 `HardSeek` 时，只有 mpv seek 成功后才把周期观察高水位 rebase 到目标 PTS，不清除 N/N+1/N+2 或在途应用。超过容差继续 fail-closed；显式用户 seek、loop、换源和新 generation 仍分别通过新 `clock_epoch` 或新媒体段身份重建队列；DTO 的原始 `presented_pts_ms` 保留 mpv 事实。本轮真实联合播放未再触发 `PresentedPtsRegressed`，但不替代 200 周期、超时故障注入和长稳门禁。

> 视频周期状态原子恢复（2026-08-31，代码修复及 TS→MP4 联合实机复验完成）：Rust runtime 发现逻辑 `Source` 仍绑定可用 GPU/CPU4 周期会话时，必须按 fail-closed 同时清除 DTO 槽、runtime current/pending/schedule 和 controller queue/apply transaction，再进入 `Available / Spawned / source_transitioning`；若只是 `parameter_support.backend` 与物理后端短暂漂移，则只以 `process_launch_mode` 恢复权威后端和支持报告，不得改写已有 Active 或在途事务。计划晋级时以物理启动模式重新规范化参数支持；GPU 重建重放最后确认 N 时必须同时恢复 Active 生命周期，CPU4 重启也必须在停止旧进程前快照最后确认 N 并在新进程真实重放，空计划和 demote 过渡必须清除旧活动槽，禁止再次产生“活动 N + 非 Active”非法 DTO。本项不改变普通声音周期、PortAudio 或 EOF 所有权。

> 视频帧健康硬降级证据（2026-08-31，代码修复及联合实机复验完成）：mpv `vo-passes` 的滚动/增量 P99 继续作为遥测和诊断，但不得在 frame-drop、decoder-drop、mistimed 或 delayed 计数均未实际增长时单独推进 GPU/CPU4 硬降级。只有真实健康计数连续三个有效窗口增长才按既有单向策略降级；健康窗口必须清零连续计数，PTS 停滞 watchdog、IPC 断链、进程退出和渲染致命错误保持原门禁。Rust 音画同步 HardSeek 成功后按 `seek → 周期 PTS rebase → 帧健康基线重建 → 恢复播放` 执行，seek 前证据不得跨边界继承。本轮真实 22fps TS + PortAudio 联合负载曾出现 P99 约 `56.6ms`、丢帧 `0/0` 且时序计数不可用的误降级，现已由上述规则覆盖；`vo-passes` 的 count/index 与逐帧总耗时精确化仍属于后续遥测精度改进。

> 视频后端发布原子性（2026-08-31，代码修复及首次代次实机复验完成）：Rust actor 的唯一 `publish_status` 边界必须在递增 `status_revision` 和复制 DTO 前校验 `backend` 与 `parameter_support.backend`。Source 必须发布 Source 空支持和 `NotApplicable`，RealtimeGpu 必须发布 GPU83 支持，CPU4 必须发布 CPU4 精确支持；同 backend 的真实 incomplete/unsupported 诊断必须保留。该收口覆盖首次 GPU 启动已认领 backend、但 prepare/configure 在写入支持报告前取消或失败的窗口，禁止非法 DTO 持续到 EOF；React 继续 fail-closed，不接受通过前端放宽校验来掩盖 Rust 发布竞态。

> shader 异步提交 IPC 独占规则（2026-08-31，代码修复，待 30 分钟联合长稳复验）：mpv 命名管道由唯一 worker 严格串行处理。shader 事务处于 `AwaitingResponse` 时，Rust actor 不得继续向同一 worker 追加 EOF、PTS、`vo-passes` 或其他同步查询，否则排队查询可能先触发短 deadline 并错误关闭整个会话。未收到 shader 响应的 tick 只轮询该在途响应；进入 `AwaitingPresentation` 后恢复 PTS 观察，以严格越过提交前 PTS 作为下一帧呈现确认。普通声音、PortAudio 和插话联合负载下，完整 GPU83 快照允许在 `500ms` 后进入 `result_unknown` 并继续等待，硬上限与提交预算统一为 `8s`；硬上限前不得关闭会话或降级，越过硬上限仍按既有同后端重建和单向降级处理。

> 换源 FPS 异步确认（2026-08-31，代码修复，待 30 分钟联合长稳复验）：`loadfile replace` 后 mpv 可以先恢复 `time-pos`，再在新视频输出建立后发布 `estimated-vf-fps`；这个属性短暂 unavailable/null 是 `source_transitioning` 的合法中间态，不得按三个观察 tick 直接降级。Rust actor 必须以唯一在途异步请求读取新源 FPS，软等待期间禁止追加其他 IPC，5 秒硬上限前保留同一物理后端；硬超时优先按当前 GPU/CPU4/Original 启动模式同后端重建。首个有效 FPS 到达后缓存到当前媒体段，后续周期只读取 mpv PTS 并复用该 FPS，换源、loop、新 generation、GPU 同后端重建或 GPU→CPU4 切换时都必须清空旧进程的 FPS 与旧观察事务并重新确认；`Available` 和已重放最后确认 N 的 `Active` 会话在 FPS 未确认前统一发布 `source_transitioning`，不得让到期计划进入 GPU/CPU4 提交或因此继续降级。提交路径不得自行同步补查 FPS 或吞掉会使会话失效的超时。

> mpv 播放观察异步事务与 actor 独占（2026-08-31，代码修复，待 30 分钟联合长稳复验）：40ms actor tick 不得用递减的同步 deadline 串行读取 `eof-reached`、`time-pos`、`pause`、`seeking` 与 `paused-for-cache`，否则普通声音、PortAudio、插话或 shader 负载可把合法迟到的只读响应误判为硬超时并关闭会话。五项事实必须组成单一异步观察事务：先确认 EOF，非 EOF 再读取 PTS，随后依次确认暂停、seek 与缓存状态，最后复核 EOF 未在事务中途翻转；整笔事务共享一个 5 秒硬上限并绑定 cursor、backend epoch 与源路径。shader、换源 FPS 或播放观察任一事务在途时，可能访问 mpv 的播放、配置、换源和处理开关命令必须由 actor 有界 FIFO 延后，actor 即使邮箱繁忙也优先推进在途事务；停止和挂起可以终止会话并拒绝旧延后命令，已暂停且等待 shader 呈现时只允许 Play 解锁下一帧。任一 IPC 硬超时或断管立即撤销旧有效状态并优先同后端重建，不得继续两轮宣传旧 Active。EOF 发布、周期到期、呈现确认和音画纠偏只能消费同一事务已经确认的 mpv 事实，不能在热路径同步补查，也不能回退到 WebView 时钟。

> 播放池编辑后的时钟隔离（2026-08-31，代码修复，待 30 分钟联合长稳复验）：播放池任意成功编辑都使首项回到 `Ready@0ms`。最终效果窗口可能被隐藏后复用，因此其迟到位置上报必须携带 `playback_generation + loop_index`，Rust 只在身份匹配且处于 `Playing/Paused` 时接受；`Ready/Stopped` 一律拒绝更新位置。位置上限统一为 `duration-1ms`，最近位置观测墙钟同样绑定 generation/loop，前端换代时清空旧 `mediaState`、seek 草稿和复用媒体元素的旧位置。首次 `start_playback` 还必须在 `PlaybackCore` 再次把位置归零，禁止旧媒体位置把新源直接带到 EOF，继而引发 mpv `vo-configured=false`、视频周期配置超时和 PortAudio 媒体段身份错误。

> 视频周期控制权后移（2026-08-31，代码、自动化及 TS→MP4 联合实机复验完成）：视频处理开启时，`RealtimeVideoRuntime` 是周期配置、确定性 N/N+1/N+2、到期判断和物理提交的唯一所有者，只以同一媒体段身份下 mpv 实际 `presented_pts_ms` 与实际 FPS 推进；React/WebView 只设置参数基线与 `5–8s` 范围、发送播放意图并展示 Rust 状态，不再以 100ms 定时器、页面可见性、`sourceAudio.currentTime`、`prepare/commit` 或 WebView `clock_epoch` 推动视频周期。换源、loop 或新 generation 必须先发布 `source_transitioning`，中性化物理参数并清除旧 N/N+1/N+2、旧 FPS 和旧指纹，待 mpv 回读新路径、递增 PTS 与实际 FPS 后才建立新队列。GPU shader 提交按 `Applying → ReadbackConfirmed → PresentedConfirmed → Active` 确认，SHA-256 指纹和两个 24-bit 标记必须匹配，且 mpv PTS 必须严格越过提交前 PTS 才能晋级 N；软超时保持 `result_unknown`，硬超时优先重建同一后端并只重放最后已确认 N。Rust 状态携带单调 `status_revision`，任何身份、revision、DTO 或应用确认拒绝都必须 fail-closed。详细实施与验收以 [`Rust 视频周期唯一所有者实施方案`](./docs/superpowers/plans/2026-08-31-Rust视频周期唯一所有者实施方案.md) 为准；本项不改变普通声音 DSP、PortAudio、插话、固定话术、GPU `79/83`、CPU4 `4/83` 或 4K 边界。本轮已验证主窗口后台时视频和普通声音周期持续推进、22.001848fps TS 换至 30fps MP4 后新代次独立生效；30 分钟/200 周期长稳、软硬超时故障注入及 CPU4/Original 实机链路仍未验收。本文更早的 WebView 视频候选、`prepare/commit/sync` 描述仅作历史审计，不再代表生产边界。

> 视频周期健康误判与流水线恢复整改（2026-08-30，代码及非媒体门禁完成、待用户真实媒体验收）：帧预算门禁不得把 mpv 256 帧滚动统计中的同一个历史尖峰重复视为多个连续异常窗口；用户可见完整 P99 只作诊断，自动降级必须基于相邻观察中新产生的渲染样本和实际新增的丢帧/时序计数。seek、循环、换源、参数提交和后端切换必须重建观察基线。GPU/CPU4 物理 owner 发生变化后，视频周期仍开启且 N 已生效时，若 N+1 丢失但 N+2 仍计划中，客户端必须在稳定观察后自动释放旧候选并恢复预准备；每个新 owner 只允许一次在途恢复，避免 100ms 状态轮询造成候选抖动。该整改不放宽真实连续超预算的 GPU→CPU4→Original 降级，不改变 GPU `79/83`、CPU4 `4/83`、普通声音功能、`5–8s` 周期或 4K 范围；真实连续变换、多文件循环和长稳无卡顿仍须用户验收。

> 多文件 EOF 与视频候选自愈整改（2026-08-30，代码及非媒体门禁完成、待用户真实媒体验收）：Rust runtime actor 在观察到物理 EOF 后必须先把包含同一 `playback_generation/backend_epoch/clock_epoch/loop_index` 的共享状态完整发布，再向容量 `1` 的监督通道发送 EOF 事件；通道背压时后续 tick 重试，成功后不重复通知。监督线程继续严格核对 `status.eof`、进程和物理 EOF，不得放宽过期事件门禁。React 的视频 N+1 owner 同时绑定当前 plan、源路径/索引、播放代次、source revision、目标 loop 和视频开关 revision；源切换后发现旧 active/pending/resource-wait owner 必须作废旧 token、对称释放并立即允许当前计划重新准备，旧异步回调不得恢复旧 owner。Source 兜底且已有当前计划时显示“源画面播放中 · 正在恢复视频候选”，不能永久停在模糊的“等待候选准备”。本整改不修改普通声音功能、GPU `79/83`、CPU4 `4/83`、`5–8s` 周期随机、4K 范围或严格 DTO；真实多文件轮播、周期连续变化和长稳无卡顿仍由用户验收。

> 视频周期状态与 EOF 幂等整改（2026-08-30，代码及非媒体门禁完成、待用户真实媒体验收）：主界面的周期主状态以当前已呈现的 N 为准；N 已生效时必须显示“当前周期已生效”，N+1 的准备或待提交状态只能作为次级流水线描述，不能把持续预热误报成当前画面一直未生效。受管 mpv 活跃及其 EOF 身份过渡期间，WebView 画面和隐藏原声音轨均不得调用 `complete_playback_item`；只有兼容 WebView 画面才保留原有结束推进。Rust EOF 监督对同一播放代次、单一视频源、播放中且播放状态与 mpv runtime 都恰好已推进到 `eof.loop_index + 1` 的迟到事件只记录 `already_advanced` 并幂等忽略，不得再次推进；单边推进、旧 runtime EOF、跨代次或相隔多轮仍 fail-closed。本批不修改 GPU `79/83`、CPU4 `4/83`、周期随机、声音功能或 4K 范围；尚未运行真实媒体和长稳门禁。

> ZLMediaKit RTMP GPU 直推范围（2026-08-30，方案已确认、尚未实施）：桌面端新增面向用户指定 ZLMediaKit 的 RTMP/RTMPS 直接发布正式需求。用户输入完整发布地址，默认同时输出画面和声音，也可只选其中一种；视频直接读取当前活动源并使用 FFmpeg/libplacebo/GPU83 与 GPU 优先、CPU 回退的 H.264 编码，不捕获桌面或最终效果窗口，声音从最终 PCM 总线分流为 AAC。Go 后端和管理系统不接收、转发或保存媒体正文，不接入 OBS、直播平台账号或多平台分发。唯一实施入口为 [`ZLMediaKit RTMP GPU 直推实施方案`](./docs/superpowers/plans/2026-08-30-ZLMediaKit-RTMP-GPU直推实施方案.md)；完成代码和真实服务器门禁前必须标记“正式需求·待实施/未接入”。

> 视频后端物理状态 DTO 整改（2026-08-30，代码及非媒体门禁完成、待用户真实媒体验收）：`process_id` 与 `physical_paused` 必须同时存在或同时为空。GPU、CPU4、Original、同进程换源及中性旁路只有在 mpv 串行 IPC 确认真实暂停和 EOF 状态后才可发布 PID、PTS 和物理事实；停止、降级探测和失败状态必须同时清除 PID、PTS、物理暂停、物理 EOF 与逻辑 EOF。前端保持严格拒绝非法 DTO，不得通过容忍残缺状态掩盖后端竞态。本修复不改变视频参数、GPU `79/83`、CPU4 `4/83`、周期时序、声音功能或 4K 范围；真实启动、开关、降级和循环仍需用户复验。

> 视频周期长稳冻结整改（2026-08-30，代码及非媒体门禁完成、待用户真实媒体验收）：视频处理开启后，每个 `5–8s` 周期只能通过受限 IPC 提交一条完整参数快照；约 `40ms` 的后端观察 tick 只读物理状态，不得逐帧重写 `glsl-shader-opts`。GPU `79/83` 中需要周期内动态变化的视觉调度参数，由生产 shader 使用自动媒体 `PTS`、周期起点、实际 source fps、确定性 seed、间隔和幅度推导；帧率调度继续只使用受限 speed 入口。受管 GPU/CPU4/Original 在存活、播放、非 EOF 时必须监测物理呈现 PTS，按源帧率使用 `1.5–5s` 有界停滞阈值；先执行一次同源软恢复，仍不前进再进入既有降级/Original 路径。暂停、EOF、seek、循环、换源、新会话和 PTS 恢复必须重置看门狗。自然 EOF 必须先预演下一身份并完成 mpv 物理 seek/loadfile 验收，再在最新权威 `PlaybackCore` 上恰好提交一次；物理失败不能推进 generation、loop、媒体索引或位置。CPU4 仍仅 4 项，剩余 GPU 4 项继续 fail-closed；本整改不修改声音功能，不包含 4K，也未完成真实 1080p60、多显卡或 30 分钟门禁，因此不能宣称所有周期边界绝对无卡顿。

> mpv EOF 后端原子恢复（2026-08-30，代码及非媒体门禁完成、待真实媒体验收）：自然 EOF 后的单文件循环与多文件连续性由 Rust runtime/命令独占。runtime actor 每 `40ms` 有界观察物理 EOF，并通过容量 `1` 的非阻塞去重事件通道唤醒 Rust 监督事务；React/WebView 不再为受管 mpv 发送完成触发，只冻结视频候选并展示后端推进状态。后端在一个可取消、幂等、有界超时事务中先用 clone 预演下一身份，再执行合法源内 seek 或受限 `loadfile replace`、恢复正确暂停态，并以 mpv 物理 `pause/eof/seeking/VO/source-local PTS` 回读及连续 PTS 前进确认成功；随后重新锁定最新权威状态、复核起始身份并恰好提交一次 `complete_item()`。只有 generation/epoch/loop 匹配不能视为物理恢复成功，物理失败时逻辑身份不得推进。非 stale 恢复失败时先记录诊断再无条件释放失效 mpv，由既有兼容画面接管；stale/superseded 不得误杀新会话。跨轮 `presentation_pts_ms` 与文件内 `source_pts_ms` 必须分离，mpv 不得接收 presentation PTS。本整改不改变普通声音效果、DSP、候选格式、N/N+1、PortAudio、插话、固定话术或混音语义。仍须真实 1080p 单/多文件、声音开关组合和长稳验收，未经门禁不得宣称无卡顿或音画同步已保证。

> 音画统一时间轴与同步调度整改（2026-08-30，代码及聚焦测试完成、待用户真实媒体验收）：桌面端统一以 `playback_generation + source_path + loop_index + source_duration_ms` 标识媒体段，明确区分跨循环 `presentation_pts_ms` 与单文件 `source_pts_ms`；PortAudio 可听时钟可报告两者，但 mpv 只接收合法 source-local PTS。播放态循环边界必须执行 `Seek → SetPause(false)`，暂停态执行 `SetPause(true) → Seek`；无效身份、换算失败或越界 PTS 只停用同步纠偏，不得触发 GPU/CPU4 降级。React 视频同步采用一个在途 IPC 与 latest-wins pending，`busy` 按 `100/250/500ms` 有界退避，`result_unknown` 先查状态，`stale` 刷新最新 desired；Rust 提供稳定错误分类和健康 GPU/CPU4 的 Original 防覆盖。同步命令的 mpv `IpcTimeout` 只返回 `result_unknown` 并保留会话，`IpcDisconnected`、断管和进程退出仍按原有单向降级。本轮未执行 1080p 真实媒体、多文件、1080p60、4K 或长稳验收，不能宣称无卡顿或音画同步指标已保证。

> 普通声音候选调度整改（2026-08-30，代码及聚焦检查完成、待用户真实媒体验收）：普通声音候选 `prepare` 的校验、路径/滤镜准备与候选启动已移出 Tauri 主事件线程，并保持既有单飞、取消、超时和候选所有权。该项只改变调度位置，不改变音频 DSP、参数语义、AAC/M4A 候选格式、N/N+1 提交、PortAudio、插话、固定话术或混音；真实联合媒体流畅性仍待用户验收。

> 视频周期单一循环事实源（2026-08-30，代码及非媒体自动化完成、待用户实测）：播放代次、当前媒体索引和 `loop_index` 只由 Rust `PlaybackCore` 维护。WebView 原声音轨可以为单项池保持原生连续循环，但不得自行递增循环或用媒体 `currentTime` 推算循环；检测到末尾回到开头后只幂等确认一次 Rust 播放完成，确认在途期间不发布倒退时钟。视频 N/N+1/N+2 按目标绝对时间绑定所属循环，只有最终效果时钟、Rust 快照和目标循环一致时才准备或提交；跨循环计划进入权威循环后再准备。mpv 同步只接受当前 Rust 循环，暂停状态也只服从 Rust 播放状态；循环边界清理旧候选但保留已生效参数。本修复不改变普通声音 DSP、声音候选、PortAudio、插话、固定话术或混音，尚未运行用户媒体，因此周期持续变化、循环边界流畅性和长稳仍需用户实测。

> 播放声音连续性修复（2026-08-30，代码及非媒体自动化完成、待用户实测）：带音轨视频在 WebView 原声元素可用时，以该连续媒体时钟作为前端源时间轴；受管 mpv 的 `presented_pts_ms` 只在源音轨不可用时兜底，禁止用 2 秒轮询得到的陈旧视频 PTS 每 250ms 把原声硬拉回。普通漂移不再改变原声倍速或触发 seek，只有用户显式 seek、真实循环边界或媒体 ended 才允许对齐。播放时钟恢复必须重载实际卡住的媒体元素；原生后端停止或失败时，WebView 兼容画面立即恢复并由连续原声位置对齐，不能停留在 Rust `playing`、后端 `stopped`、画面和声音均不推进的矛盾状态。PortAudio 启用时仍以 Rust 可听 PTS 为主时钟，本修复不改变普通声音 DSP、声音候选、插话、固定话术或混音。尚未运行用户媒体，因此不宣称现场卡顿或长稳验收通过。

> 多文件同进程换源修复（2026-08-30，代码、聚焦测试及用户 TS/MP4 开发端复核完成）：播放池发生视频→视频自然 EOF 转换时，Rust 只有在旧 EOF 四维身份精确匹配、新 generation 恰好连续、宿主 HWND 与物理后端不变且 mpv 仍存活时，才保留该受管进程；新源通过结构化 JSON IPC `loadfile <path> replace -1 {start}` 载入，并在命令返回后显式恢复权威暂停态，避免 `keep-open` 遗留的 `pause=true` 令新文件停在首帧。切源等待新路径、非 EOF、非 seeking、VO 已配置及播放态首帧；超时、取消或身份不确定立即回收进程，再从当前单向降级下限按正常路径重建。使用用户提供的 TS→MP4 自然轮播实测，桌面 PID 8132 下 mpv PID 16712 全程未变化、未出现空 PID 窗口，generation 由 1 进入 2；GPU 周期继续从 N=39 推进到 N=52，稳定观察 GPU pass P99 约 3.30ms，VO/解码/延迟丢帧均为 0，界面未再出现“响应必须是对象”“受管 Original 管线信息无效”或“视频后端状态不可用”。该证据只覆盖本开发机与这两份素材，不替代 1080p60、多显卡及 30 分钟长稳门禁，也不构成所有硬件绝对无卡顿保证；普通声音功能未修改，4K 不在当前范围。

> 原生视频时钟与 EOF 修复（2026-08-30，代码、聚焦测试及用户 TS/MP4 开发端复核完成）：WebView2 不支持源视频封装时，桌面端必须使用同一 `playback_generation + clock_epoch + loop_index` 的受管 mpv 已呈现 PTS 驱动视频周期；runtime 每次真实 PTS 前进都必须发布状态，不能只在后端状态转换时更新。首轮周期提交前的 `Available + Spawned` 原生进程可以上报同身份 EOF，由 Rust EOF 监督事务推进播放池；前端只接受并展示该事实，不得误报“响应必须是对象”，也不得拥有推进权。Original 与 GPU prepare 统一从 Tauri `resource_dir` 解析受控 shader。当前两份用户素材已证明 TS 可进入 GPU 原生表面、EOF 可推进到 MP4、短周期在同一 PID 中连续提交且已映射 shader 参数真实变化；跨文件同进程切源的最新结论以上一条为准。该修复不改变声音功能或 4K 边界。

> 受管 Original 状态展示修复（2026-08-30，代码及聚焦测试完成、待真实媒体验收）：视频处理关闭时不得因同一 mpv 会话仍运行 GPU 中性 shader 或 CPU4 中性参数而显示“后端状态不可用”。桌面端必须把 `Original（中性 shader）`、`Original（CPU4 中性参数）` 和 `Original（无 shader）` 作为三种受管 Source 事实严格校验，均明确展示“未应用视频参数”，不得声称周期参数生效。候选恢复期间只有收到合法新状态才能清除旧诊断，禁止把内部空状态误报为“响应必须是对象”。该修复不改变声音功能、GPU `79/83`、CPU4 `4/83` 或 4K 边界。

> 视频处理开关同会话修复（2026-08-30，代码完成、待用户实测）：当前实现已将逻辑 `Source/Original` 与 mpv 物理启动模式分离。在同一源、同一 playback generation、同一宿主 HWND、进程存活且物理模式仍匹配当前 fallback floor 时，关闭视频处理由 runtime actor 在现有 mpv 上写入 GPU 中性 shader 或 CPU4 四项中性值并清除视频周期计划；再次开启后复用同一 PID 提交后续周期快照，不再仅因开关切换执行 surface suspend、重新 seek 或重建视频表面。首次建立会话、进程不存在/退出/丢失、不满足连续视频 EOF 复用门禁的换源或播放代次变化、宿主 HWND 变化、物理模式与 fallback floor 不匹配，以及导致会话失效的 GPU、设备或 IPC 故障降级、显式停止或关窗，仍允许先回收旧进程再创建或重启；普通周期更新不允许重启。GPU 自动周期当前仍仅准入 `79/83`（61 项 shader + 18 项受限 PTS 调度），剩余 4 项继续 fail-closed，CPU4 仍为 `4/83`。本批未运行真实视频，未实施或验收 4K，也不代表无卡顿、长稳或跨 GPU 验收通过；本修复未修改普通声音 DSP、声音候选、PortAudio、插话、固定话术或混音，最终效果和开关流畅性待用户实测确认。

> 视频健康指标与过期候选修复（2026-08-30，代码完成、待用户实测）：`mistimed-frame-count` 与 `vo-delayed-frame-count` 是 mpv display-sync 条件指标，返回 `property unavailable` 时统一记为“不适用”，不得累计观察失败或触发 GPU/CPU4 降级；`vo-passes`、VO/PTS/decoder、丢帧计数以及启动/健康观察中的真实 IPC 超时、断管和协议错误继续执行既有连续门禁。同步命令确认超时单独归类为 `result_unknown` 并保留会话，只有 `IpcDisconnected` 等已确认失联才沿原路径降级。`stale_realtime_video_plan`、`realtime_video_prepare_stale` 与 `stale_realtime_video_backend_epoch` 统一作为可恢复竞态：前端释放旧候选，刷新最新播放快照与后端身份后重新调度，旧异步响应不得覆盖新代次。本批不修改声音功能、不扩大 GPU `79/83` 或 CPU4 `4/83`、不实施 4K、不运行真实媒体。

> GPU/CPU4 首帧假失败修复（2026-08-30，代码完成、待用户实测）：mpv 仍以 `--pause=yes` 安全启动，但 IPC 连接成功后必须先恢复用户权威播放状态；用户处于播放态时，GPU/CPU4 才等待 `vo-passes` 的真实渲染样本，用户本来暂停时改为验证 VO、有效视频 PTS 和解码器，不得在暂停态等待永远为零的 pass 计数。播放后的既有 `vo-passes`、帧预算和连续故障降级监控继续有效。该修复不修改声音功能，不扩大 GPU `61/83` 或 CPU4 `4/83`，不实施 4K，也不代表无卡顿已验收。

> 视频候选终态与诊断修复（2026-08-30，代码及非媒体验证完成、待用户实测）：视频处理开启后，GPU→CPU4→Original 全部失败必须发布合法的 `Source + Failed + process_id=null` 终态，命令层不得再把它覆盖成“视频表面未就绪”。前端对 IPC 调用失败与后端 DTO 校验失败分别显示稳定诊断，瞬时轮询失败保留最后一次有效身份；prepare/commit 返回无效数据或终态时立即释放候选 owner，并只沿用既有有界退避，禁止永久显示“候选处理中”。运行时错误原因压缩、路径脱敏并限制为 256 UTF-16 单位。该修复不扩大 GPU `61/83` 或 CPU4 `4/83` 能力，不修改声音功能，不包含 4K，也未运行真实媒体；最终效果、周期持续变换和流畅性由用户实测确认。

> 原生表面 Z 序整改（2026-08-29，代码完成、待用户复验）：开发端现场确认 mpv 进程、IPC、D3D11VA、视频 PTS 和 mpv 内部截图均正常，但同矩形的 WebView2 child HWND 位于专用视频宿主之上，造成最终效果窗口全黑。Original/GPU 现在只有在 PID 对应的可见、非零 mpv 子窗口验证成功后，才在 Tauri 主事件线程用 Win32 `SetWindowPos(HWND_TOP, SWP_NOMOVE | SWP_NOSIZE | SWP_NOACTIVATE | SWP_SHOWWINDOW)` 提升宿主；启动失败不提升，resize 时仅对仍然有效的 mpv 子窗口重申 Z 序。提升失败回收会话并返回真实错误。该整改未改变任何声音功能，尚不构成无黑屏、无卡顿或 1080p60 验收通过。

> mpv/libplacebo 视频主链口径（2026-08-28，本文最高优先级）：视频由一个常驻 mpv 播放器承担解封装、解码、时钟、帧调度和最终窗口呈现；当前最高开发与验收分辨率为 `1920×1080`，4K 延后。Windows 首选 `D3D11 + D3D11VA + gpu-next`，第二路径为 `Vulkan + WinVK + gpu-next`。`gpu-next` 内置 libplacebo 加载受控自定义 shader，除水平/垂直翻转外的 `83` 个视频 UI 参数以一份快照原子参加 GPU 实时播放主链。正常周期只更新动态参数，不重新编码、落盘、切源、重启播放器或重编译 shader。GPU83 不可用时按会话单向降级到 libavfilter CPU4，再失败保持 Original；CPU4 只允许亮度、对比度、饱和度和色相。

> 音画同步口径（2026-08-28）：PortAudio 实际消费采样换算的可听 PTS 是普通声音启用时的主时钟，Rust 以 `25–50Hz` 闭环控制 mpv 视频速度、丢帧或必要时精确定位；最大漂移不得超过 `80ms`，P99 目标不超过 `40ms`。React 只显示事实状态，不承担实时同步。

> 方案入口（2026-08-28）：唯一实施方案为 [`mpv/libplacebo 实时 GPU 主链实施方案`](./docs/superpowers/plans/2026-08-28-mpv-libplacebo实时GPU主链实施方案.md)。旧 MSE、SourceBuffer、视频 period、逐周期 FFmpeg 重编码、视频 A/B 和 WebView2 CSS 视频效果已从当前文档上下文及生产实现删除，不得恢复为当前入口。
>
> 实施进度（2026-08-30）：已落地受管 mpv 持久 IPC、受控 libplacebo hook、精确 GPU83 映射和正式周期入口，并删除旧视频 MSE/period/逐周期 FFmpeg 生产代码、权限和测试。Phase 3A 的 D3D11/Vulkan `1920×1080@60fps` v6 门禁已通过 61 项 shader，另有 18 项受限 PTS 调度进入开发生产链，当前为 `79/83`。两项颜色候选因帧级原子提交、完整开关语义及 HDR/驱动合同未通过继续 fail-closed；`slice_min_length_ms` 与画中画时间轴解锁两项跨帧历史也未接入，不能宣传为完整 GPU83。GPU 重建在暂停态重放最后活动 shader 快照后再恢复播放。Phase 4/6 的降级与原生表面代码已接线，但仍待一次 1080p 综合实机门禁，不表示无卡顿。Phase 5 已新增独立 PortAudio 可听时钟快照，以 generation/audio epoch/source/loop 隔离身份并按 25Hz 发布；mpv 严格观察 pause/seeking/cache 状态，以“实际音频播放速率 × 视频调度速度 × `±1%/±2%` 同步修正”的唯一受限速度控制视频。该改动不改变普通声音 DSP、N/N+1、插话、固定话术或混音语义；真实联合媒体与长稳验收尚未完成，4K 不在范围。
>
> 当前测试交付状态（2026-08-30）：视频读取侧会把新鲜 PortAudio 快照按实际播放速率外推到读取时刻，并拒绝乱序观测污染同步身份。GPU 自动周期当前只随机并提交已准入的 `79/83`，剩余四项保持基线。独立测试安装包和 portable 已能构建、安装和启动，并固定连接测试控制面；该状态仅表示可供功能测试，不表示完整 GPU83、无卡顿、音画同步长稳或正式发布通过。
>
> mpv IPC/首帧修复（2026-08-29 基线，2026-08-30 同步超时语义补充）：Windows 命名管道由单一 worker 独占，命令严格串行执行 `write + flush + read response`；不再克隆同一个同步管道句柄并让读写线程并发。事件可穿插，但响应必须匹配当前 `request_id`。启动、健康观察和普通控制超时仍毒化会话并触发回收；同步请求的命令确认超时返回 `result_unknown`、保留当前会话并先查状态/幂等重试。`IpcDisconnected`、断管和协议错误不得被首帧循环吞掉，仍按原路径处理。启动查询共享 `5s` 总预算，不再为首个 HEVC 解码/VO 查询固定截断为 `500ms`；运行态仍保持独立短预算。该批不修改声音功能、不运行真实媒体、不构成无卡顿验收。
>
> 原生视频宿主修复（2026-08-29，代码完成、待用户实测）：`final-effect` 顶层 WebView HWND 不再直接作为 mpv `--wid`；Rust 在 Tauri 主事件线程创建、调整和销毁一个专用 Win32 child HWND。传给 mpv 的值严格收窄为其 Windows 合同要求的 `uint32_t`，禁止 64 位值被静默截断。Original/GPU 准备返回前还必须按受管 mpv PID 枚举窗口树，确认 mpv 真正创建了挂在该宿主下、可见且客户区非零的视频子窗口；失败先回收会话，并返回枚举数、PID 所属窗口数、可见数和非零客户区数。`open_final_effect_window` 仍只有在宿主就绪后才返回 `video_host_ready=true`。该修复不改声音功能、不涉及 4K，也未运行真实媒体，流畅性和重复 `get time-pos` 超时是否消失由用户开发端实测确认。
>
> 视频开关 epoch 竞态修复（2026-08-29，待用户实测）：视频处理开关仍可立即呈现用户意图，但 `prepare_realtime_video_plan` 必须等待 Rust 串行开关命令返回权威播放快照和视频后端状态，随后再次校验播放代次、当前源、开关值及本次开关 revision；切换期间提前创建或晚返回的候选必须静默丢弃。Rust 保留严格 backend epoch 门禁，同播放代次的旧 epoch 以专属可恢复错误返回，且在任何 mpv、降级或状态变更前拒绝；前端刷新权威状态并有界重建候选，不把该竞态显示为 mpv 进程故障。新播放代次仍先重置会话 epoch，再按新代次校验。开关 IPC 失败时 UI 回滚到最后一个权威快照并解除调度栅栏。该修复不修改声音处理功能，未启动开发端或执行真实媒体测试。
>
> 下一版本实施边界（2026-08-28）：按“GPU83 逐项语义与证据 → D3D11/Vulkan/CPU4/Original 生产单向降级 → PortAudio 可听 PTS 驱动 mpv → 联合长稳”串行实施。普通周期不得切后端；设备丢失属于异常有界恢复，不承诺物理故障时绝对零停顿。音频侧只增加只读可听时钟和同步 epoch 接线，不改变普通声音效果、候选、插话、固定话术或混音语义；4K 继续不进入本版本。
>
> 运行态修复补充（2026-08-27）：已有 N 生效时，准备 N+1 必须继续报告 `active`，不得把顶部状态降为“尚未生效”；周期提交必须原子更新参数事实源并立即广播到最终效果窗口，避免旧默认值覆盖或定时广播延迟。PortAudio 正式同步链不得被前端常量关闭；系统默认设备必须可显式选择，回退后允许使用同一配置重试，设备枚举与启动失败必须显示真实原因，WebView 回退不得伪装成 PortAudio 已应用。
>
> 音视频双开一致性（2026-08-27）：声音处理和视频处理必须保持独立开关语义。用户快速连续开启两项时，客户端以同步最新值串行提交完整开关快照，并只接受最新响应；同一媒体时钟内的声音与画面参数提交必须基于同一参数事实源合并。Rust 只重置实际发生变化的域，声音开关不得清除视频 current/pending，视频开关也不得清除声音 current/pending。
>
> 周期调度恢复（2026-08-27）：实时画面 N 提交推进下一轮时，即使上一轮前端 apply 仍在收尾，也必须把最新 N+1 请求保存在 latest-wins pending 槽，并由 apply 或实时 commit 的完成路径主动再次排空，不能依赖 React 展示状态变化触发。普通声音候选的非 busy 失败保持当前 N，按 `1/2/4/8/15s` 有界退避重建新的 N+1/N+2；连续第 6 次失败后才等待人工重试。busy 不计入失败次数，成功、人工重试、停止、换源或播放身份变化清零重试状态。播放中修改声音预设、多轨数量或周期范围时，已在处理或待提交的 N+1 必须保留前端 owner，不得与 Rust pending 并发准备新候选；最新设置从尚未准备的 N+2 起生效。
>
> 源音轨有效窗口（2026-08-27）：导入探测必须记录相对容器时间轴的有效音轨区间 `[audio_start_ms, audio_end_ms)`。普通声音 N+1 与该区间完全无交集时属于可预期的容器无声头尾，必须在启动 FFmpeg 前静默跳过，不能生成补零静音文件后报“疑似静音”；部分相交仍保留原候选。多项播放池在当前项无声尾部等待真实换源，只有单项池可按源时长周期绕回。Rust 命令必须重复执行同一门禁，未知或不一致的旧元数据沿用既有校验路径。
>
> 主页性能口径（2026-08-27，优先于下方全部参数变化高亮历史）：主页所有媒体参数卡和“插话声音预设”参数行均改为静态显示；参数值变化只立即更新数值或进度，保留逐卡强调色和真实状态文字，不再执行闪烁、光晕、发光阴影或关键帧动画。最终效果窗口的原始诊断采样仍可保持最多 `4Hz`，主页主波形可按该节拍直接绘制；进入 React 状态树的诊断摘要最多每秒提交一次。参数面板和单卡必须使用稳定输入与渲染边界，诊断消息、播放时钟或其他无关主页状态变化不得使未变化的参数卡重复渲染。

> 当前版本范围声明（2026-08-30）：本版本不开发实时话术幻化。speech-to-speech、ASR/LLM/TTS 话术沿用或改写、实时候选音轨换轨、模型租约直连和对应 UI/验收均不属于本版本；相关内容仅保留为历史方案或后续版本预留。唯一例外是“麦克风插话”正式需求可在桌面本地使用 VAD 判断用户是否正在说话，只用于主媒体数字静音门控，不做语义识别、不上传音频、不改写话术。现有随机文件插话的用户入口统一命名为“插话文件”；麦克风插话仍为“正式需求·待实施”，实施边界以 [`麦克风插话与插话文件入口实施方案`](./docs/superpowers/plans/2026-08-30-麦克风插话与插话文件入口实施方案.md) 为准。当前播放基线是最多 100 项、支持文件选择/系统拖放导入以及成员原子 CRUD 的本地有序播放池；单一窗口以一个活动媒体源执行 `A→B→…→A`，单项池自循环，播放池只存在当前进程且不是生成版本队列。

> 主页同步口径（2026-08-26，优先于本文后续历史布局描述）：桌面主页保持“播放区 / 媒体双卡工作区 / 输出与本地状态区”三列。媒体工作区左侧为“音频”及其下方独立“插话声音预设” Card，右侧为“画面” Card；声音、插话和视频周期分别放在所属 Card 内。所有正式媒体参数继续可见且功能不删除；每个领域的只读参数占满所属 Card，按可用宽度自动形成约四列的连续紧凑网格，单行高度 `28px`，只显示“名称 + 当前值”，不显示装饰进度条，也不显示已实现参数的“已接入”文字；只有待实现或待确认项在原位置显示真实状态。分类摘要按实际可见行计数，`advanced.band_weights` 虽仍是一个契约字段，但按 12 个固定频率拆为独立显示行，因此高级视觉摘要为 53 行。声音周期和视频周期均允许用户输入 `1–60 秒`，下限不得大于上限；声音新配置默认 `3–5 秒`，视频新配置默认 `5–8 秒`，默认值不是输入范围。每轮在闭区间重新随机，区间相等时使用固定周期；既有合法本地值继续读取。随机插话周期在 UI 使用秒，IPC/Rust 继续使用毫秒。设计稿中的账号、媒体、波形、运行次数和设备名称均为演示数据，正式端必须继续读取真实会话与运行状态，不得硬编码。

> 插话预设变化反馈（2026-08-26，历史口径，已由 2026-08-27 主页性能口径覆盖）：主页“插话声音预设”仍以最终效果窗口上报的本段实际预设为事实源；当时格式化显示值变化会复用正式参数卡 A/B 代次和约 `900ms` 单次高亮。该历史实现事实保留，当前参数行只静态更新值，不再维护闪烁代次或执行动画。

> 主页能力与恢复口径（2026-08-27）：播放池的追加、拖放、整批替换、单项替换、排序、删除和清空都保留在主页；播放、暂停、继续、停止为独立按钮并分别呈现进行中状态。“设置/状态”兼容路由保留，但顶部导航只展示主页。输出区展示 N/N+1 候选阶段、FFmpeg PID/编码器、线程预算、当前/候选/待删文件数和最近清理结果。高级声音、随机插话、固定话术的原有功能均保留。媒体引擎、普通声音、视频或插话失败采用“错误出现在哪里，就在哪里展示”。

> 播放时钟口径（2026-08-28）：视频周期和声音周期的计划、参数和计数互不共享，但共享同一源时间轴。PortAudio 启用时以实际可听 PTS 为主时钟，Rust 闭环校正 mpv 已呈现 PTS；视频周期只更新参数快照，不生成临时视频文件。

> 播放控制与普通声音切换口径（2026-08-26）：媒体播放中开启或关闭普通声音处理只更新下一候选快照，不停止、暂停或重载当前 active。候选文件未就绪时继续当前音画；A/B 媒体槽完成预加载、对时和音画交接后才提交新参数。媒体元素内部 `pause/load` 只属于切换与句柄释放，不得反向写成用户暂停。

> 插话原声压低口径（2026-08-27）：WebView 最终效果窗口中的当前 N 音轨经过共享主轨增益节点，插话音轨绕过该节点单独混入；插话开始和结束分别按 `ducking_attack_ms`、`ducking_release_ms` 对主轨执行压低和恢复。该规则不得并行启动第二份普通声音主轨。

> 插话默认静音口径（2026-08-26）：新建或缺失字段的插话配置将主轨 `ducking_depth_db` 默认设为 `-60dB`；WebView 与 PortAudio 均把该下限转换为增益 `0`，因此插话实际出声期间默认静音主节目音轨。已有明确保存值继续保留，不执行强制迁移。

> 插话周期与小窗保存口径（2026-08-26）：主页“插话声音周期”必须显示随机插话已保存的 `interval_min_ms / interval_max_ms`，不得误用插话声音预设的变化周期；半秒步进值按真实小数展示。随机插话抽屉以同名“插话声音周期”区块提供最小周期和最大周期数字输入框。抽屉保留底部保存按钮，并在窗口高度不超过 `720px` 时于顶部状态概览提供保存入口，确保桌面端最小窗口高度下可保存配置。

> 统一媒体池口径（2026-08-29）：播放池允许视频与纯音频混排，最多 `100` 项并保持原子 CRUD。纯音频不创建 mpv 视频会话；新导入视频只通过 FFprobe 做有界元数据/真实流探测，随后由 mpv 直接读取只读 `source_path`，`playback_reference` 固定等于 `source_path` 且 `compatibility_mode=direct`。导入阶段不得整段转码或创建兼容视频；历史快照中的兼容字段仅为向后读取保留。

> 虚拟摄像头口径（2026-08-30）：正式方案采用 AkVirtualCamera，首版固定 `GpAutoLive Camera` 和 YUY2 `1280×720@30fps`。WGC/D3D11 在 GPU 上捕获唯一最终效果 HWND、缩放并转换；AkVirtualCamera 上游只接受 CPU 原始帧，因此允许一次有界 staging readback，必须明确 `zero_copy=false`。Windows 10/11 默认 DirectShow，Windows 11 Media Foundation 仍为实验门禁；完成 GPL-3.0、安全、签名和兼容矩阵前统一标记“正式需求·待实施/未接入”。

## 文档状态

- 状态：Draft
- 文档模式：Single（按用户要求放在仓库根目录）
- 当前阶段：按 [`长任务开发总计划.md`](./长任务开发总计划.md) 执行，当前为 Phase 4 媒体处理最小闭环实施
- 最后更新：2026-08-27
- PRD 文件：`产品需求文档.md`
- 技术架构总览：[`系统架构总览.md`](./系统架构总览.md)
- 桌面端功能流程图：[`桌面端功能流程图.md`](./桌面端功能流程图.md)
- 管理系统架构：[`管理系统架构.md`](./管理系统架构.md)
- 桌面客户端架构：[`桌面客户端架构.md`](./桌面客户端架构.md)
- 媒体参数契约：[`媒体参数范围与默认值.md`](./媒体参数范围与默认值.md)
- 数据库迁移与密钥存储约束：[`数据库迁移与密钥存储约束.md`](./数据库迁移与密钥存储约束.md)
- 服务端与桌面端登录安全加固实施计划（主体实现、本地自动化与 Docker PostgreSQL 16 真库验证已完成）：[`docs/superpowers/plans/2026-08-26-服务端与桌面端登录安全加固实施计划.md`](./docs/superpowers/plans/2026-08-26-服务端与桌面端登录安全加固实施计划.md)
- TS 视频与纯音频统一播放实施计划（本批实施、待验收）：[`docs/superpowers/plans/2026-08-26-TS视频与纯音频统一播放实施计划.md`](./docs/superpowers/plans/2026-08-26-TS视频与纯音频统一播放实施计划.md)
- N/N+1/N+2 临时文件滚动处理实施计划（仅普通声音部分继续有效，视频部分已废止）：[`docs/superpowers/plans/2026-08-26-N1-N2音视频临时文件滚动处理实施计划.md`](./docs/superpowers/plans/2026-08-26-N1-N2音视频临时文件滚动处理实施计划.md)
- mpv/libplacebo 实时 GPU 主链实施方案（实施中，里程碑 1 已完成）：[`docs/superpowers/plans/2026-08-28-mpv-libplacebo实时GPU主链实施方案.md`](./docs/superpowers/plans/2026-08-28-mpv-libplacebo实时GPU主链实施方案.md)
- AkVirtualCamera GPU 虚拟摄像头实施方案（正式需求·待实施）：[`docs/superpowers/plans/2026-08-30-AkVirtualCamera-GPU虚拟摄像头实施方案.md`](./docs/superpowers/plans/2026-08-30-AkVirtualCamera-GPU虚拟摄像头实施方案.md)
- douyin-desktop 接入 autoLive 多产品控制面 PRD（Phase 1 服务端基线已实现，后续阶段未实施）：[`tasks/prd-douyin-desktop-control-plane-integration.md`](./tasks/prd-douyin-desktop-control-plane-integration.md)
- 服务端商业化与生产就绪扩展 PRD（规划、未实施）：[`tasks/prd-server-commercial-production-readiness.md`](./tasks/prd-server-commercial-production-readiness.md)
- 目的：作为首版产品范围、技术边界和后续执行依据

桌面端用户可见品牌统一为 `GpAutoLive`，紧凑位置使用缩写 `GPAL`。该命名只适用于界面品牌、启动页、窗口标题和面向用户的替代文本；内部产品代码 `autolive`、Rust/前端包名、Tauri 应用标识符、可执行文件名、环境变量与本地存储键保持兼容，不因品牌展示调整而重命名。

当前实现状态：Go 控制面已拆分管理端与桌面端登录入口，签发 `admin/desktop` audience 会话，并提供 15 分钟 Access、30 天绝对期 Refresh family 单次轮换/重放撤销和按 Refresh 幂等 Logout；新密码写入 Argon2id，旧 bcrypt 成功登录后渐进升级。登录失败接入防枚举和分层限流：账号摘要从第 5 次失败起持久退避，可信来源地址摘要从第 50 次失败起承担聚合滥用退避；进程短突发分别限制单账号和来源地址，避免同一 NAT 下不同账号共享低阈值。React 管理端已接入 audience 校验、401 自动刷新和退出。Rust/Tauri 桌面端串行管理登录、刷新、恢复、退出和系统 Keychain/Credential Manager 凭据更新；Refresh Token 和稳定 `device_id` 均不由 WebView 作为事实源，旧 WebView 设备标识仅作为首次迁移候选。桌面端只输入账号和密码；登录成功后由客户端自动提交当前设备信息，Go 遍历该账号的有效激活授权，只要存在 `bound_devices < max_devices` 的授权就为新设备占用一个槽位，同设备重登不重复占位；跨账号已绑定设备仍需管理员先解绑。内存模式使用进程内索引，PostgreSQL 模式使用 `auth_sessions` 持久化并支持重启后查询；HTTP Server 已配置请求头、读、写和空闲超时；独立 `cmd/migrate` 已提供版本化 PostgreSQL 迁移入口。Go 已接入第一阶段 PostgreSQL JSONB 快照 repository、规范化读源切片（迁移 0013、显式回填工具、advisory lock）和 AES-256-GCM 服务端 Secret Store；用户/设备/激活码/会话/模型账号/租约/调用摘要/审计已有首批规范化领域事务和 SQL 分页，快照退出、真实跨进程一致性、额度权威性、故障矩阵和生产部署验收仍未完成。

2026-08-26 登录安全主体已实现，且仍不开发用户注册。管理端 `admin` 与桌面端 `desktop` 会话 audience 硬隔离，桌面登录拒绝管理员账号；Access Token 有效期为 15 分钟，Refresh Token 为 30 天绝对有效期并按 family 单次轮换，检测到旧 Refresh Token 重放时撤销整个 family。注销使用 Refresh Token 定位并幂等撤销当前会话，即使 Access Token 已过期也可完成。登录失败防账号枚举；账号和可信来源地址继续使用带领域前缀的 PostgreSQL 摘要桶，但分别采用 5 次与 50 次失败阈值，成功只清账号桶，来源地址保留较高阈值的聚合滥用保护。新建/重置密码使用 Argon2id，既有 bcrypt 凭证在成功登录后渐进升级。桌面端不再保存登录密码；Refresh Token 的读取、轮换、恢复和删除收口到 Rust，不返回 WebView；主窗口与最终效果窗口已拆分 Tauri capability。本地自动化和 Docker PostgreSQL 16 真库迁移/Store/认证 HTTP 验收已通过；Windows Credential Manager、正式 HTTPS/DNS/TLS、多主机与 Race 仍待环境验收。

多产品控制面 P0 的 Phase 1 服务端基线已实现：迁移 0023 建立 `products`/`user_products`，登录会话、设备、激活码、Profile、租约、用量、审计和管理分页查询已具备产品字段或产品边界；普通管理员的设备详情和全局用户授权/管理写操作在固定产品角色尚未实施前分别按产品收窄或限制为内建管理员。迁移仍保留旧设备全局唯一键，产品范围 RBAC/React 权限壳、Profile 6 小时建议复核/24 小时签名离线授权、BYOK 优先的模型认证、固定 OpenAPI commit/digest 制品和跨仓库 E2E 均未实现。详细范围以接入 PRD 为准，不能把旧 douyin-desktop 契约或当前仅含 `direct_base_url` 的模型租约描述为已完成能力。

服务端商业化与生产就绪扩展仍以分阶段规划为主：当前只提前落地了激活授权的账号归属与逐设备当前绑定事实；按产品的订阅、订单、套餐版本、微信支付、商业设备席位、密码重置投递 Worker、激活码独立加密明文表、容量在线调整/安全重显/设备切换、对象存储/外部下载、签名真实性校验、product/environment/namespace JSON Schema、双重脱敏错误报告与反馈、备份恢复、OpenTelemetry 追踪和 CI 签名/Provenance 发布门禁仍未实现。当前 OpenAPI、数据库迁移和部署状态不得据此宣称已具备未交付能力。该专项包含 Go 服务端、PostgreSQL/Secret Store、服务端 Worker、供应商适配器、服务端 CI/运维链路，以及 React 角色权限和商业/安全/运维管理页；Rust/Tauri 客户端业务改动不在该专项范围。

桌面代码核对结论：当前核心播放、基础视频/普通声音处理、插话播放器、固定话术系统朗读和单一最终效果窗口与本 PRD 的当前范围一致；实时话术候选、speech-to-speech 和模型直连仍不属于本版本。参考图中的音频、视频、高级视觉、挂件和切片参数全部转为正式产品需求，统一模型为 `MediaEffectParams`、`AudioEffectParams`、`VideoEffectParams`、`AdvancedEffectParams`；当前参数面板只使用“已接入 / 正式需求待实现 / 待确认”三态。旧研究报告 Worker 不是参数事实源，也不得成为当前播放依赖。

普通声音处理的 1～4 个预设支路在独立声音候选 FFmpeg 内等权混成一条音轨，写入临时 AAC/M4A 并通过声音 A/B 提交。视频由常驻 mpv 连续播放，周期只更新 GPU83 快照。只有视频真实帧呈现或声音候选真实出声并提交成功后，UI 才更新对应领域参数和周期次数。

普通声音 N+1 的 FFmpeg 已退出且 FFprobe 已确认 `.partial` 可读后，若 `volumedetect` 暂未返回有效 `max_volume`，同一暂存产物最多自动重试内容探测 `2` 次（总尝试最多 `3` 次），并共享本次媒体任务的原超时与取消令牌；不得为此重复编码整个 N+1。stderr 必须保留末尾摘要，避免截断 `max_volume` 后误判。静音、取消、超时、参数错误、FFmpeg 非零退出及其他不可读错误不自动重试；最终失败时清理暂存。重试期间继续播放当前声音 N，不更新已生效参数或变化次数，也不得取消、清空、重建或延后视频 N/N+1/N+2 周期。

视频与声音分别预选 N+1/N+2 完整快照，分别生成候选、预载、到期提交并计数，不再按最早目标合并候选。声音由普通声音与随机插话共用的 22 套完整预设驱动，每套精确包含 35 个可写效果字段；p01–p20 为默认池，p21/p22 仅由用户显式选择。`ambient_sound_mix_percent = 0` 时不加载环境声第二输入，非零时优先使用已校验的用户素材，否则使用随包内置真实环境声；失败只保持对应当前 N 并报告可恢复错误。

## 问题

需要一个轻量后台管理系统和一个 Rust 桌面客户端。管理员能够管理用户、桌面设备、激活码和大模型账号池；用户使用同一套登录信息登录后台和桌面客户端。桌面客户端负责采集基础设备信息、在当前进程内维护最多 100 项的本地有序播放池，并在单一窗口、一个活动媒体源内顺序循环，同时按开关执行视频处理和普通声音处理；插话与固定话术系统朗读是当前声音能力，实时话术幻化不在本版本开发范围。

当前产品包含“本地有序播放池顺序循环、正式媒体效果参数、视频/普通声音处理预览、插话和固定话术系统朗读”链路，并新增尚未实施的 ZLMediaKit RTMP/RTMPS GPU 直推正式需求；播放池项是用户选择的本地源文件，不是离线生成版本。本版本不开发实时话术幻化，不恢复独立研究报告工作流，不做 OBS、直播平台账号接入、平台自动发布或多平台分发，也不做离线 N 版口播生成。

## 目标

- G-1：后台和桌面客户端共用一套用户账号与登录接口。
- G-2：管理员能够生成、查看和禁用激活码；创建时必须选择绑定账号并设置最多登录设备数。
- G-3：桌面端使用账号密码登录后，服务端按该账号的有效激活授权自动绑定当前设备并上报基础设备信息，桌面端不输入激活码。
- G-4：客户端能够从视频 `.mp4`、`.mov`、`.mkv`、`.avi`、`.webm`、`.m4v`、`.ts`、`.m2ts`、`.flv`、`.wmv`、`.3gp` 与纯音频 `.mp3`、`.wav`、`.m4a`、`.aac`、`.ogg`、`.flac` 中一次多选 `1～100` 个本地源媒体，按文件选择或系统拖放返回顺序形成统一播放池，并在独立窗口中执行 `A→B→…→A`；视频和音频可混排，单项池自循环，不生成 N 个视频版本。
- G-5：管理员能够维护 OpenAI-compatible 大模型供应商、模型账号、连通性测试结果和使用状态。
- G-6：客户端能够向后台申请单账号、短期、限额、可回收的直连模型租约，并由 Rust 直接调用当前租约对应的模型。
- G-7：保持模块边界清晰，确保视频处理和普通声音处理可以独立开关和独立降级；实时话术幻化本版本不开发，仅保留后续版本边界。
- G-8：桌面端提供素材库、播放控制、画中画预览、合规的音视频效果调整和波形/频谱诊断面板；PortAudio 可用时，实时低频震动线必须来自 `audio_cycle_output` 的混音后 PCM 快照。
- G-9：只播放用户整批导入的本地有序播放池；不计算 N、不生成离线版本或生成版本队列，当前项结束后在同一窗口进入下一项，末项回首项。
- G-10：视频画面处理开关和普通声音处理开关相互独立，用户可以分别启用或关闭；实时话术幻化开关本版本不提供产品入口。
- G-11（后续版本，不纳入当前验收）：实时音频幻化输入文件或受控实时流，按播放轮次和音频片段交给 speech-to-speech 执行 VAD/ASR/LLM/TTS，由大模型决定沿用或改写话术，并在当前视频播放期间替换基础音轨；失败保持上一条可用音轨。
- G-12：参考图中的音频、视频、高级视觉、挂件、切片、调制和微扰参数全部作为正式产品需求纳入参数契约；不得因旧名称含“研究/实验”而删除参数。独立研究报告、内容相似度、媒体鲁棒性和内容指纹对比工作流不属于当前产品目标，效果参数也不得用于或宣传为平台检测规避。
- G-13（后续版本，不纳入当前验收）：支持本地预览场景下的实时话术幻化，不连接直播平台，不进行实时推流。
- G-14：默认采用连续播放模式，确保有序播放池换源或单项自循环时始终复用同一窗口、一个活动媒体源，不黑屏、不重新弹窗。
- G-15（后续版本，不纳入当前验收）：实时音频幻化不阻塞首项视频播放；变体候选准备好并通过同步门禁后，才替换当前音轨；候选来不及或失败时保持上一条可用音轨。
- G-16（后续服务端专项，未实施）：补齐订阅、订单、套餐版本和设备的管理查询、白名单筛选、排序、分页与详情能力。
- G-17（后续服务端专项，未实施）：使用微信支付生产渠道完成订单支付、回调验签、幂等履约和主动查单对账，并通过投递 Worker 完成自助密码重置通知。
- G-18（后续服务端专项，未实施）：以私有对象存储、短时授权下载、服务端签名/Provenance 校验和可供消费端验证的签名清单/digest 建立可信制品分发链路；该制品链路不承载用户本地音视频。
- G-19（后续服务端专项，未实施）：建立数据保留、加密备份、隔离恢复演练、Prometheus 指标、OpenTelemetry Trace 和 CI 签名/Provenance 发布门禁。
- G-20（后续服务端专项，未实施）：公共配置按 namespace 和不可变 Schema 版本执行 JSON Schema Draft 2020-12 校验，并继续保留敏感字段扫描作为第二层防线。
- G-21（后续服务端专项，未实施）：新生成激活码的可恢复明文存入独立加密表，不进入 `activation_codes`、日志或普通审计；管理员通过二次验证、限流和审计重新查看，并可管理设备槽位、容量和设备切换。
- G-22（后续 Go + React 专项，未实施）：使用自定义角色与服务端固定权限点实现管理员最小权限，并在 React 管理端提供角色配置、用户多角色分配、权限化导航/路由/操作和对应业务管理页。
- G-23（正式需求·待实施）：桌面端直接向用户输入的 RTMP/RTMPS 地址发布当前媒体，默认 H.264 + AAC 且可选择仅画面或仅声音。视频不捕获窗口，直接读取活动源并复用 FFmpeg/libplacebo/GPU83 与硬件编码器探测；声音从最终 PCM 总线分流。ZLMediaKit 位于用户指定的流媒体服务器，Go 控制面不进入媒体数据路径。
- G-24（正式需求·待实施）：使用 AkVirtualCamera 将唯一最终效果窗口输出为 `GpAutoLive Camera`，由用户在下游软件中选择。WGC/D3D11 完成 GPU 捕获、缩放和 YUY2 转换，AkVirtualCamera 仅承担一次 GPU→CPU 回读后的帧传输和 DirectShow 设备暴露；不写入物理摄像头、不经过 Go、不启动第二播放器，也不宣称 GPU 零拷贝。

## 非目标

- NG-1：首版不做口型同步、滤镜美颜、弹幕互动、直播间互动和实时话术幻化；实时话术幻化如后续恢复，也只限本地预览，不扩展为研究分析平台。
- NG-2：除用户指定 ZLMediaKit 的 RTMP/RTMPS 直接发布外，不做 OBS、直播平台账号接入、平台自动发布和多平台一键分发；RTMP 断线只按专项方案有界重试，不提供无限重连。
- NG-3：首版不做复杂的视频云端处理；视频、ASR/TTS 中间产物和合成产物默认在桌面端本地处理。
- NG-4：首版不做微服务、消息队列和 Kubernetes。
- NG-5：不做面向平台审核/检测的规避、自动发布或平台接入；不提供内容相似度、媒体鲁棒性、内容指纹或平台评分结论。不可见水印和随机扰动仅作为正式普通媒体效果需求。
- NG-6：本次登录安全加固不开发用户注册、邀请注册、MFA/WebAuthn、邮箱/短信找回密码和设备密钥对；用户继续只由管理员创建。
- NG-7：虚拟摄像头不采集或写入物理摄像头，不捕获桌面/第三方窗口，不提供虚拟麦克风、录制、上传、4K、60fps、多输出槽或端到端 GPU 零拷贝承诺。

## 成功标准

- SC-1：管理员创建普通用户后，普通用户可以使用账号密码登录 Rust 客户端；管理端与桌面端复用同一身份事实源，但会话 audience 和可访问路由必须隔离，桌面端不得接受管理员账号，普通用户也不得凭桌面会话访问管理接口。
- SC-2：没有有效账号激活授权、授权已过期/作废或已达到设备上限的客户端不能进入完整使用状态；同一设备重复登录不重复占用额度，管理员解绑后释放对应额度。
- SC-3：后台能够显示设备在线状态、客户端版本、操作系统、磁盘剩余空间、内存摘要、逻辑 CPU 数和最后心跳时间；GPU 等更细硬件信息不作为首版必采集项。
- SC-4：客户端一次多选 `1～100` 个白名单视频或纯音频，按返回顺序逐项通过 FFprobe 实际探测并分类；全部成功后才原子替换或追加到旧池。视频项由生产 mpv 直接解码，导入不准备兼容引用。播放池在单一窗口按序循环，不生成离线版本。
- SC-5：客户端断网时，已选择的本地视频和纯音频仍可继续播放；恢复网络后能够补发状态。
- SC-6：管理员禁用用户或设备后，客户端在下一次鉴权/心跳时停止受保护能力。
- SC-7：模型账号异常、额度耗尽或冷却时，后台不会继续分配该账号；客户端能收到可理解的失败原因。
- SC-8：所有模型密钥不以明文写入数据库、日志或前端构建产物。
- SC-9：客户端可以在素材库查看播放池各项文件名、时长、状态和当前项，并通过文件选择或系统文件拖放新增/追加素材、替换单项、拖拽或键盘调整顺序、删除单项及清空播放池。新增或替换素材必须先完成实际探测，超过 100 项、重复路径或任一探测失败都保持旧池并显示错误，不提交部分成功项；路径和探测元数据不得手工编辑。
- SC-10：客户端可以对视频项启用/关闭普通声音和视频效果，修改参数并恢复默认参数，且不覆盖原始文件；当前项为纯音频时视频处理与画中画显示“不适用”，全局视频开关值保留到后续视频项，普通声音处理继续适用。
- SC-11：客户端提供独立画中画预览、播放进度、音量、静音控制以及波形和频谱等基础分析视图；播放速度只读展示并由普通声音预设/周期随机化更新。诊断卡同时展示候选编码前混音 PCM 的低频震动线、RMS、峰值和采样率，并在候选未就绪或快照过期时明确提示。
- SC-12：音视频效果应用失败、格式不兼容或资源不足时，客户端保留原视频可播放，并给出可操作的错误提示。
- SC-13：视频画面处理开关和普通声音处理开关可以独立打开/关闭，关闭任一开关不会关闭其他功能或停止原视频播放；实时话术幻化开关本版本不提供。
- SC-14（后续版本，不纳入当前验收）：实时音频幻化可以接收本地音频文件或受控实时流，调用 speech-to-speech 完成 VAD/ASR/LLM/TTS，并输出沿用原文或改写后的实时变体音轨；不依赖运行时重复检测。
- SC-15：变体音轨通过时长、同步、播放代际和资源门禁后，才能替换当前播放视频的基础音轨；门禁失败继续上一条可用音轨，不生成新 MP4。
- SC-16：当前项结束后在同一个窗口进入下一项，末项结束回到首项，单项池回到自身；`source_media_index`、`playback_pool_cycle` 和换源时递增的 `playback_generation` 可观测。
- SC-17：生成使用用户拥有或获授权的素材、音色和内容，不提供平台审核/检测规避能力。
- SC-18：普通声音处理面板提供已接入的自然动态、音色 ID、音高、输入/输出增益、响度、播放速度、均衡、降噪/环境声、真实环境声第二输入、干湿比、淡入淡出、混响、环境底噪、MFCC/SNR、采样率和输出码率；波形、频谱、响度、噪声底、SNR、MFCC 和 LPC 共振峰属于只读诊断，不作为处理参数。
- SC-19：参考图中可辨识的媒体效果参数全部进入正式清单，并逐项标注“已接入 / 正式需求待实现 / 待确认”；参数存在、校验通过或 UI 可见均不能替代真实输出证据，三态也不决定主面板编辑权限。
- SC-20：正式参数快照必须保留参数版本、算法版本和可复现调度所需的内部种子；内部种子不作为普通用户输入。旧“研究哈希/阶段报告”不再作为当前参数模型名称。
- SC-21：水印、微扰、视觉调制、挂件和切片只按普通媒体效果及可见版权标识验收；不提供内容相似度、媒体鲁棒性、内容指纹或平台检测规避结论。
- SC-22：本地预览中按当前音频片段生成并播放话术变体；Rust 通过单账号直连租约调用 OpenAI-compatible 模型，服务异常时回退到原话术或上一条可用音轨。
- SC-23（后续版本，不纳入当前验收）：实时音频幻化的检测、变体生成和换轨任务异步执行，不阻塞源视频首帧和循环播放。
- SC-24：普通声音处理只作用于普通源音轨，走 FFmpeg、可选环境声、Signalsmith Stretch、realFFT/rustdct MFCC 与滚动 RMS SNR 链路，并写入独立声音 N+1 临时文件。它与 mpv 视频参数计划彼此独立；候选失败时保持当前音频 N。
- SC-25：变体生成超过当前音频安全余量时保持上一条可用音轨，不暂停视频、不静音等待、不切换未完成候选。
- SC-26：一次多选的全部条目通过素材可读性和安全校验后才原子替换 `source_media_pool`，首项保持 `Ready`；用户点击主页“播放”才打开/聚焦最终效果窗口并以当前项原声开始播放。取消、超限或任一失败保留旧池；视频处理和普通声音处理是否生效由各自开关控制，实时话术幻化不属于本版本。
- SC-27（后续版本，不纳入当前验收）：同一个源视频可以连续循环；每轮音频都携带播放轮次和片段上下文交给 speech-to-speech 决策，不把重复的源视频误认为错误；LLM 调用通过当前直连租约完成。
- SC-28：所有带数值的媒体参数均有明确单位、范围和默认值；默认值优先从源视频/音频元数据读取，任何进入调度或媒体引擎的值都必须经过范围校验。全量展示卡和周期状态保持只读；主页声音参数列不得把“音高、变速与共振峰”“特征、信噪比与空间混合”整体标为编辑区，其中 MFCC 偏移、MFCC 维度、SNR 浮动、频谱盲区、干湿比、环境声混合、高质量音高、播放速度和共振峰偏移只展示当前值，不接收手动输入。
- SC-29：桌面 UI 不展示独立“运行资源”卡片，也不提供 FFmpeg/FFprobe 的安装、取消、选择目录或清理按钮；FFmpeg/FFprobe 仍由安装包和内部媒体能力准备链路管理，缺失或失败通过导入/处理错误提示呈现。
- SC-30：普通声音处理提供独立的本地 DSP 参数预览；22 套预设逐套精确覆盖 35 个可写字段。增益/EQ/空间字段可分支，音高、播放速度、共振峰、MFCC、SNR、环境声、采样率和码率等共同总线字段由本轮主预设统一执行。p01–p20 为默认池，p21/p22 仅在用户显式选择时参与。普通声音写入 N+1 临时媒体产物，Web Audio 只承担 A/B 槽音量交接、插话 duck 和诊断；该链路与实时话术幻化完全独立。
- SC-31：用户可以配置本地插话目录、随机间隔、音量、主音轨 duck 参数，以及与普通声音配置隔离但共用 22 套参数定义的插话音轨选择。插话目录最多纳入 `1000` 个受支持的媒体文件，抽屉状态摘要展示已保存目录的实际文件数与 `1000` 上限；尚未保存的目录草稿不得冒充新扫描结果。触发间隔 UI 范围为 `0.5–60s`、默认 `8–13s`；IPC/Rust 使用 `500–60000ms`。`fixed` 可显式选择 `p01–p22`；`random` 默认从 `[p01–p20]` 低感知池抽样，p21/p22 只有显式加入后才参与。多轨每次最多四支路；预设变化周期为预设外的全局调度配置，周期到期或保存配置只更新下一段，不中断当前插话。`ambient_sound_mix_percent = 0` 时不加载第二输入；值大于 `0` 时用户本地素材可选且优先覆盖，未选择时使用随包内置真实环境声。两种来源任一验证失败都保持原声并显示可恢复原因。
- SC-32：固定话术使用本机 WebView2 `localService` 系统声音朗读，朗读期间临时静音原视频声音并暂停插话，结束/失败/取消后恢复原状态；不依赖在线语音或语音克隆模型。
- SC-33：右栏 PortAudio 设备卡位于话术功能上方，提供 Host API 筛选、输出设备选择、`128–2048 KiB` 内存缓冲设置和显式应用，同时分开展示运行时实际出口、已应用配置与处理状态。界面不提供独立“刷新设备”按钮；用户每次打开输出设备选择框时都自动请求最新设备列表，并在选择框内显示加载状态。刷新使用独立加载状态并禁止重复触发，刷新期间不得应用设置或播放测试音；只有完整枚举成功后才原子替换设备列表，失败保留旧列表并显示真实原因，不得自动应用草稿、重启活动流或改变实际出口。编辑草稿不得在应用成功前冒充运行配置；Host API 只筛选真实枚举设备，未枚举到的 ASIO 必须禁用或不显示。保留固定 `440 Hz / 400 ms` 本地测试音，且仅在 PortAudio 已运行、没有其他应用操作进行时可触发。默认输出策略仍为系统默认/WASAPI，启动失败继续按现有规则回退 WebView。
- SC-34：桌面主窗口默认以 `1280×800` 客户区启动，最小尺寸保持 `960×680`；桌面主界面使用深色高密度三栏工作台。`1728×1044` 继续作为视觉对比基准，同视口下为 `36px` 顶栏、`320px / 1fr / 272px` 三列、`12px` 外边距和栏间距，六张顶部状态卡只显示播放、循环、视频处理、普通声音处理、源规格和最终效果窗口的真实状态。主窗口的 `36px` 自定义顶栏由唯一窗口框架承载，在 React 加载、启动错误、登录、会话校验和业务页面中始终保留且不参与页面滚动。三栏独立滚动，DOM/键盘顺序为素材 → 视频/参数 → 声音/输出；窗口宽度不超过 `1199px` 时，外层工作区必须按该顺序重排，确保 Tauri `960px` 最小窗口与系统缩放下仍可完成全部关键操作。
- SC-35：参考图同时作为视觉结构与正式媒体参数需求来源。当前已实现 UI 仍不出现实时话术幻化、speech-to-speech、旧研究报告 Worker、研究/变体 MP4 产物、内容指纹、OBS、平台推流或检测规避指标；ZLMediaKit RTMP/RTMPS 卡片仅在专项实施时加入，并在真实发布门禁完成前显示“正式需求·待实施/未接入”。`65Hz–20000Hz` 明确为视频空间频段并已接入，按对数映射到每幅画面 1–32 个空间周期，不进入声音频谱。只有尚无正式字段、依赖外部模型或 OCR 语义未确认的项目继续显示“正式需求待实现/待确认”。冷启动到 React 首次可交互帧保持深色背景，不出现白色闪烁。
- SC-36：高级声音、随机插话和固定话术使用统一的深色中性抽屉结构；桌面目标宽度分别为 `760px`、`560px`、`560px`，且均不超过 `100vw`。三个抽屉具备双行标题、真实状态摘要、卡片分区、独立滚动正文和固定底部主操作；所有表单标签持续可见，窄屏字段单列，删除用户预设必须确认。抽屉重做不改变普通声音、插话和 WebView2 本地系统固定话术的功能边界。
- SC-37：桌面全量正式参数区必须与 Rust `AudioEffectParams/VideoEffectParams/AdvancedEffectParams` 逐字段一致。参数内容区填满桌面中栏并保持“左：普通视频 + 高级视觉 / 右：普通声音”的响应式双主栏；极窄时按 DOM 顺序降为单列且不横向裁剪。普通参数卡与周期状态仍是只读展示，继续使用紧凑高度、完整频段卡、三态文字和逐卡静态强调色，不执行参数变化动效。右侧声音栏可保留“音高、变速与共振峰”“特征、信噪比与空间混合”的具名展示分组，但不得把分组整体定义为编辑区；上述 9 项只读展示，高级声音抽屉也不得提供其重复输入。目标 SNR 为 `null` 时可见显示“自动”。
- SC-37a：主参数区必须同时显示“视频周期”和“声音周期”两个独立状态模块。两个模块展示各自配置范围、当前真实变化次数，以及本领域计划的目标时间、生成/校验/呈现状态和进度；不得使用前端计时器伪造。两者始终独立推进，不提供联动目标。只有本领域真实呈现或出声并提交成功后才递增变化次数；处理关闭、暂停、周期未启用或无真实计划时保持非活动。
- SC-37b：双周期的共同事实源必须是最终效果窗口当前 `playback_generation + source` 的真实绝对媒体位置。`playback_state=playing` 不得单独证明时钟健康；排除暂停、seek、切源和加载宽限后，连续约 `1500ms` 无真实推进必须进入“播放时钟停滞/恢复中”状态并冻结周期进度。同一源代次只允许一次自动重载、复位位置并续播的恢复尝试；恢复失败必须保留真实位置、展示明确的时钟失败状态并继续本地续播尝试，不得调用后端暂停或把内部失败伪装成用户操作。任何路径都不得改用墙钟补走周期进度。
- SC-37c：播放中打开或关闭普通声音处理时，当前媒体继续从原位置推进；新设置进入下一声音候选。声音 A/B 的内部 `pause/load` 和 mpv 的内部降级/缓冲都不得改写用户播放意图。
- SC-37d：视频和声音周期均显示 `1–60s` 区间、本轮随机值与下次变化媒体时间。视频状态投影会话生命周期、真实后端 `gpu83_d3d11/gpu83_vulkan/cpu4/original` 和快照 `pending/applied/rejected`；只有真实呈现或出声并提交成功后才递增次数。
- SC-38：插话在 WebView 和 PortAudio 两种实际声音出口下都必须可听并共用同一完整预设、参数校验和环境声解析。PortAudio 接管时在单个 FFmpeg 图内处理最多四支路并混入唯一 `audio_cycle_output`。WebView 路径为当前插话生成短期处理缓存后播放，处理失败才明确回退插话原声；不得与 PortAudio 双播。短期缓存绑定当前 generation，段落结束、失败、停止、切换素材或窗口卸载时删除；配置变化只影响下一段且不抢停当前段。任何失败都必须保留主播放链路、恢复 duck 并展示结构化原因。
- SC-39：桌面端登录门禁只包含账号和密码，并可在用户显式选择后记住账号及保持登录。账号只作为普通本地偏好保存；登录密码不得持久化到 Web Storage、普通配置、本地数据库或操作系统凭据库。Refresh Token 只能由 Rust 经系统 Keychain/Credential Manager 管理且不得返回 WebView；稳定 `device_id` 同样由 Rust 持久化并作为唯一事实源，WebView 只接收其受控投影用于设备注册，不得覆盖，合法旧 WebView 设备标识只允许作为首次迁移候选。Rust 串行执行登录、启动恢复、Refresh、Logout 和对应凭据更新，Logout 完成前界面禁止再次登录。单次提交严格执行“账号密码登录 → 查询当前设备 → 必要时按账号授权自动绑定 → 服务端确认 Ready”；只要当前账号存在未过期、未作废且 `bound_devices < max_devices` 的激活授权，新设备就必须完成自动绑定，同设备重登不得重复占位。任何本地偏好、Refresh 恢复或缓存都不得直接放行工作台。账号无有效激活授权、授权过期、所有有效授权设备额度均已满、跨账号绑定冲突或设备被禁用时必须停留在同一门禁并显示稳定原因。
- SC-40：工作台的账号、退出、激活有效期和会话提示属于 `36px` 顶栏下方的页面级 fixed 状态层，任何高级声音、随机插话或固定话术抽屉打开时都必须位于 Ant Design 遮罩与抽屉面板之下，不得覆盖标题、关闭按钮、正文或底部操作。全部抽屉的遮罩和面板从自定义顶栏下缘开始，不能遮挡或拦截最小化、最大化/还原和关闭按钮；抽屉保持固定头尾、正文独立滚动、窄屏单列、打开后聚焦及 Esc 关闭，关闭后焦点返回原触发入口。
- SC-41：随机插话有效配置保存后只更新同一份 playback snapshot，并标记下一段应用新固定选择或重新抽样；不得因配置变化调用 `StopInterlude`、使当前 generation 失效、重建 FFmpeg 或改变当前插话的预设组、音量和 duck。当前插话继续使用启动时快照直至自然结束，新配置从下一段开始生效。包括 `fixed` 模式保留的 `audio_preset_ids` 在内，目录/字段范围、唯一性、asset scope 或锁损坏等真实校验失败仍拒绝保存并保留当前活动插话，前端必须原样展示 Tauri 结构化错误中的 `message`。
- SC-42：登录、保持登录和清除本地会话的成功提示展示 `4` 秒后自动消失；发起新的登录、保存或清除操作时先清理旧成功提示，且旧计时器不得清除后续提示。凭据读写失败、钥匙串不可用、认证或账号授权失败等警告和错误不参与自动隐藏，必须持续显示到用户采取新操作或状态被明确解决。
- SC-42a：管理端会话必须标记为 `admin` audience，桌面会话必须标记为 `desktop` audience；Refresh 只能继承原 user、product、device 与 audience。Access Token 有效期固定为 15 分钟，Refresh Token 绝对有效期为 30 天并启用 family 重放检测。用户不存在、密码错误、用户禁用、无产品权限及桌面尝试管理员账号对外返回相同的 `401 UNAUTHENTICATED` 语义和近似工作量；多实例共享账号摘要失败计数，来源地址只在可信代理边界解析。最终效果窗口不得拥有控制面 HTTP 或认证凭据 IPC 权限。
- SC-43：Windows release 中只有一个受管常驻 mpv 视频进程和最多一个普通声音 Worker。两者纳入 Windows Job Object，取消、换源、停止和退出执行有界等待、进程树兜底与线程 Join；mpv 使用持久有界 JSON IPC 和 stderr 尾缓冲。
- SC-43a：视频只保留一个 mpv 会话和轻量 N/N+1/N+2 参数计划，不保留视频 `.partial/.ready`。声音最多保留当前 N、一个 `.partial/.ready` N+1 和轻量 N+2。
- SC-43b：视频参数周期和媒体导入都不生成 `.ready`、视频块或兼容视频。声音旧槽停止、移除 `src`、调用 `load()`、撤销对象 URL 并回报释放后删除。停止、换源、关闭窗口和正常退出按各自所有权清理，异常退出由下次启动清理。
- SC-44：Windows 10/11 x64 的正式跨机交付物是 NSIS 安装包。安装包必须包含 FFmpeg/FFprobe、PortAudio、运行资源清单和 WebView2 离线安装器，不依赖目标电脑预装系统 PATH、PortAudio、FFmpeg 或联网下载 WebView2。便携 ZIP 只作为内部测试产物，不宣称免依赖跨机交付；对外发布前仍须完成 Authenticode 代码签名、干净系统安装/卸载和登录、导入、播放冒烟。
- SC-44a（本批实施、待验收）：所有视频源文件保持只读并由生产 mpv 直接播放；加入播放池时只执行有界 FFprobe，不启动 FFmpeg、不生成 `.partial.mp4` 或兼容引用。FFprobe 失败、取消或被新导入取代时不得改变旧池；mpv 后续真实启动失败必须如实显示并保持源文件不变，不得在导入动作中隐式重编码。纯音频项继续直通。
- SC-44b（本批实施、待验收）：纯音频只有音频流或音频流加 `attached_pic` 时均按 `audio` 分类；最终效果窗口显示纯黑并禁止封面、上一视频残帧、视频处理和画中画。`.mp3`、`.wav`、`.m4a`、`.aac`、`.ogg`、`.flac` 需分别完成导入、播放和循环验收；普通声音处理、插话、固定话术、音量、静音、暂停、继续和停止继续工作。
- SC-44c（代码及聚焦测试完成、真实媒体验收待执行）：音画同步只接受完整一致的 `playback_generation + source_path + loop_index + source_duration_ms`。presentation PTS 只用于跨循环状态/计划，mpv 只接收小于源时长的 source-local PTS；播放态边界按 `Seek → SetPause(false)`，暂停态按 `SetPause(true) → Seek`。无效或越界音频时钟只停用纠偏，不触发视频降级。
- SC-44d（代码及聚焦测试完成、真实媒体验收待执行）：视频同步同一时刻最多一个 IPC 在途，pending 采用 latest-wins；busy 仅 `100/250/500ms` 有界退避，result_unknown 先查状态，stale 刷新最新 desired，旧响应不得覆盖新 revision。同步确认 `IpcTimeout` 保留会话并返回 result_unknown，明确断链才进入既有降级；健康 GPU/CPU4 不得被迟到 Original 请求覆盖。
- SC-44e（实施中）：普通声音候选 prepare 的同步前置工作不得阻塞 Tauri 主事件线程；迁移后仍须保持现有单飞、取消、超时、候选格式、DSP、提交和播放语义。该项完成前不得作为声音不卡顿验收证据。
- SC-45（后续服务端专项）：管理员可按固定白名单筛选订阅、订单、设备和套餐版本，分页/排序稳定且查询计划经过真实数据量验证。
- SC-46（后续服务端专项）：微信回调必须先对原始报文验签和解密，再按商户订单号、平台交易号与金额幂等履约；重复回调、未知结果和主动查单不会重复授予订阅。
- SC-47（后续服务端专项）：密码重置请求不暴露账号是否存在，Token 只保存哈希、一次性且短期有效，投递失败可有界重试，成功重置后撤销相关会话。
- SC-48（后续服务端专项）：管理员重新查看激活码明文必须完成管理员密码二次验证，响应禁止缓存并留下不含明文的语义审计；历史哈希-only 激活码不伪造可恢复明文。
- SC-49（后续服务端专项）：在当前 `1～100` 台创建容量和逐设备绑定事实之上，管理员可在线调整容量（不得低于已绑定数量）、查看完整历史槽位并切换绑定设备；切换时撤销旧设备会话和租约且不转移会话。
- SC-50（后续服务端专项）：下载前由服务端校验用户、订阅、设备和制品状态并签发短时地址，同时返回已验证签名清单和文件 SHA-256；错误 signer、issuer、仓库、工作流、ref 或 digest 必须被服务端拒绝，客户端实际验签另立任务。
- SC-51（后续服务端专项）：每个公共配置 namespace 绑定不可变 Schema 版本；非法结构、受限远程 `$ref`、超限文档和敏感字段均被拒绝，并支持 dry-run 与版本化回滚。
- SC-52（后续服务端专项）：备份恢复达到规划目标 RPO 不高于 24 小时、RTO 不高于 4 小时；发布制品具备可验证签名、SBOM 和 Provenance，服务端只接收本地或 CI 构建的发布产物。
- SC-53（后续 Go + React 专项）：管理员可持有多个自定义角色，权限取固定权限点并集且不存在用户级 allow/deny 覆盖；服务端即时鉴权，React 菜单、路由、按钮和 403 与有效权限一致，直接请求 API 无法绕过。
- SC-54（正式需求·待实施）：桌面端能够向用户输入的合法 RTMP/RTMPS 地址直接发布 H.264 + AAC、仅 H.264 或仅 AAC，默认音画双轨且至少选择一轨。发布不依赖最终效果窗口，不捕获桌面或 HWND，不经过 Go；GPU 可用时使用实际硬件编码器，失败按会话单向降级到 OpenH264。RTMP 故障不改变本地播放状态，停止、换源、失败和退出无 FFmpeg、线程、管道或秘密残留。第一阶段换源重新发布和视频静态参数快照的限制必须可见，不得冒充跨条目无断流或逐周期 GPU83 已接通。
- SC-55（正式需求·待实施）：Windows 10/11 的 32 位和 64 位 DirectShow 客户端均可枚举并选择 `GpAutoLive Camera`，其 YUY2 `1280×720@30fps` 画面与最终效果窗口一致。捕获、缩放和颜色转换必须由非 WARP 的 D3D11 GPU 完成，状态公开 adapter、`transport=akvcam_mmap_cpu` 和 `zero_copy=false`；暂停、停止、纯音频、锁屏和无有效帧时两帧内转黑，故障不改变本地播放，退出/卸载无进程、句柄、服务或注册残留。Windows 11 MF 未通过独立门禁前不得标记支持。

## 关键场景

### 场景 1：用户登录桌面客户端

- 操作者：普通用户
- 触发：在桌面门禁输入与后台一致的账号密码；账号密码提供显式记住选择
- 结果：客户端登录后查询当前设备；未绑定设备由客户端自动请求服务端按账号激活授权占用一个设备额度。只有服务端确认当前设备授权有效才进入工作台，已记住内容只用于自动填充，不自动提交或放行

### 场景 2：管理员发放激活码

- 操作者：管理员
- 触发：在后台选择绑定账号、有效期和最多登录设备数后生成激活码
- 结果：激活码作为该账号的服务端授权记录；同一账号可在额度内登录不同设备，桌面端无需接收或输入激活码明文

### 场景 3：客户端自动绑定设备

- 操作者：普通用户
- 触发：账号密码验证成功后，客户端自动提交当前设备标识和最小设备信息
- 结果：后台只从当前登录账号的有效激活授权中分配设备额度，并在同一事务中绑定设备与会话；成功后开始上报心跳。无授权、授权过期、设备额度已满、绑定冲突或设备禁用均停留登录门禁，客户端不持有激活码明文

### 场景 4：本地有序播放池循环展示

- 操作者：普通用户
- 触发：在客户端一次多选 `1～100` 个本地视频，整批探测成功后点击开始播放
- 结果：弹出或聚焦一个独立播放窗口，以一个活动媒体源按 `A→B→…→A` 顺序循环；单项池自循环，窗口支持暂停、继续和停止
- 默认行为：一次多选按文件对话框返回顺序逐项探测，只有全部成功才原子替换旧池；取消、超限或任一失败保持旧池。新池首项 `Ready`，用户点击“播放”才使用当前项原声播放，不等待实时音频变体处理。

### 场景 5：桌面端素材预览与效果调整

- 操作者：普通用户
- 触发：导入视频并选中素材
- 结果：主界面按深色高密度三栏组织；左侧是素材与播放控制，中间是六张真实状态卡及填满中栏可用宽度的正式参数区；参数卡、诊断和周期状态只读，主页声音列中的两个具名分组不整体开放编辑，上述 9 项与其他参数一样只展示，右侧是普通声音运行摘要、实际出口、诊断及最终效果窗口入口。参数区内部为双主栏，左侧展示普通视频与高级视觉，右侧展示普通声音；用户可播放、暂停、调节音量和重置参数，播放速度由普通声音预设/周期随机化更新并只读展示。外层三栏与参数区双主栏不继承参考产品的实时话术、OBS、平台推流或检测规避功能；ZLMediaKit RTMP/RTMPS 只按独立专项接入，尚未实现时显示明确状态

### 场景 6：音频分析与视频分析

- 操作者：普通用户
- 触发：播放或预览视频
- 结果：客户端展示音量/响度、波形、频谱、采样率、分辨率、帧率等诊断信息；分析失败不影响播放

### 场景 7：挂件和版权标识

- 操作者：普通用户
- 触发：启用挂件或叠加层
- 结果：用户可以添加可见的文字、图片、Logo、贴纸或光效，设置数量、位置、透明度和出现区间；效果可预览、关闭和清除

### 场景 8：管理员查看设备信息

- 操作者：管理员
- 触发：打开设备列表或设备详情
- 结果：看到设备基础信息、在线状态、客户端版本、当前播放状态和最近错误

### 场景 9：客户端申请大模型账号

- 操作者：客户端业务模块
- 触发：需要调用大模型
- 结果：客户端申请一个短期模型租约；后台按账号状态、额度、并发和优先级分配资源，并记录使用情况

### 场景 10：弹窗展示最终效果

- 操作者：普通用户
- 触发：播放池整批通过本地预检、首项 `Ready` 且用户开始播放
- 结果：客户端只创建一个独立最终效果窗口。视频项由单一受管常驻 mpv 会话呈现，声音继续使用独立 A/B/PortAudio；纯音频项不创建 mpv 视频会话。任一链失败只回退本领域。

### 场景 11：正式媒体效果参数配置

- 操作者：普通用户
- 触发：打开音频、视频、高级视觉、挂件或切片参数区
- 结果：全部参考图参数按统一正式模型展示并可查询范围、默认值和能力状态；每项必须明确标注“已接入 / 正式需求待实现 / 待确认”之一。已接入项只有在真实输出链成功应用后才可报告运行态，未接入和语义待确认项不得伪造为运行中；三态只说明算法能力，不代表该字段可在主面板编辑

### 场景 12（后续版本）：独立实时音频幻化预览（当前不开发）

- 操作者：普通用户
- 触发：后续版本用户单独开启“实时幻化”开关，输入本地音轨或受控实时流；当前版本没有此入口
- 结果：后续版本才允许客户端把当前音频片段、播放轮次、原文/转写、上一轮输出和锁定字段交给 speech-to-speech；当前版本不调用 LLM/TTS、不生成候选音轨、不进行实时换轨、不申请对应模型租约

### 场景 13：输出到虚拟摄像头（正式需求·待实施）

- 操作者：普通用户
- 触发：安装或修复产品拥有的 `GpAutoLive Camera`，开始本地播放并开启虚拟摄像头输出
- 结果：用户在会议、浏览器、录制或直播伴侣软件中选择 `GpAutoLive Camera`；UI 如实显示 D3D11 GPU 捕获/转换、YUY2 `720p30`、CPU 边界投递、客户端数量和错误，不提供“写入物理摄像头”的选择框

## 发现摘要

- 已检查：当前工作区文件、目录、源码、接口、数据库迁移和测试；仓库存在早期媒体任务/版本实现，当前仅作为历史审计，不能作为当前本地有序播放池入口。
- 已确认的用户范围：Go + React 后台管理系统；Rust 桌面客户端；共用登录；激活码；基础设备信息；大模型号池。
- 已分析参考截图：桌面端需要素材库、播放控制、音视频/高级视觉/挂件/切片效果参数、重置、画中画和波形/频谱诊断；截图中的运行时示例值不直接作为默认值，参数以正式契约和实现状态为准。
- 新增竞品截图证据：截图展示的音色库、固定频段权重、波频强度、动态均衡阈值及其他可辨识媒体参数全部进入 [`参考图参数对比与正式化清单.md`](./参考图参数对比与正式化清单.md)，未完成算法映射的项目不宣称已实现。
- 当前产品决策：视频继续在桌面端本地处理和预览；新增尚未实施的 ZLMediaKit RTMP/RTMPS 直接发布，桌面端读取当前活动源并编码到用户地址，不捕获窗口、不经过 Go 媒体中转，也不接入 OBS 或直播平台账号。
- 当前产品决策：虚拟摄像头采用 AkVirtualCamera，只捕获本应用最终效果 HWND；WGC/D3D11 执行 GPU 捕获、缩放与 YUY2 转换，AkVirtualCamera CPU raw 边界允许一次受控回读。该能力不经过 Go，与 RTMP“不捕获窗口”的独立链边界不得混淆。
- 新增产品决策：本版本不开发实时音频幻化；speech-to-speech、VAD/ASR/LLM/TTS、候选音轨换轨和模型租约直连仅保留为后续版本参考。媒体效果参数全部正式化，旧独立研究报告流程不进入当前播放链；后台只负责账号、设备、激活码、OpenAI-compatible 模型账号登记/测试、号池租约和调用摘要。
- 新增产品决策（后续版本参考）：A/B 不是对外架构契约；如后续恢复实时音频，可使用双音频槽、单引擎预加载或成熟媒体引擎，只对基础音轨安全替换负责，不以离线视频版本作为前置条件。本版本不实施。
- 新增产品决策：本地预览的视频效果仍由 mpv/libplacebo 解码后实时处理并呈现，周期边界和媒体导入都不重新编码。FFprobe 负责导入探测；FFmpeg 除普通声音与插话等既有边界外，后续只按专项方案承担独立 RTMP/RTMPS 发布，不把推流重新编码混入 mpv 预览周期，也不承担 OBS 集成。
- 新增产品决策：本地源视频导入白名单统一为 `.mp4`、`.mov`、`.mkv`、`.avi`、`.webm`、`.m4v`、`.ts`、`.m2ts`、`.flv`、`.wmv`、`.3gp`；扩展名只做文件选择过滤，最终以 FFprobe 对实际容器和音视频流的探测结果为准。
- 新增产品决策：普通声音处理区分用户配置、只读诊断和真实临时文件状态；字段可保存或校验不等于 DSP 已运行。普通声音使用源音轨与可选环境声 → FFmpeg → Signalsmith Stretch → realFFT/rustdct 处理，并写入 N+1 临时媒体；失败保持当前音画。
- 新增产品决策：桌面端取消“运行资源”产品展示，不提供独立卡片及 FFmpeg/FFprobe 安装、取消、目录选择、清理按钮；后台资源准备、安装包分发和内部能力探测仍保留。
- 新增产品决策：普通声音处理统一使用本地 DSP 参数预览；全部可写字段由完整预设参加每轮变化，共同总线字段由主预设执行。处理后音频写入独立 AAC/M4A 候选，不与处理视频合并；声音候选失败保持当前音频 N。本版本不开发实时话术幻化。
- 新增文档：参数范围、单位和“自动读取源素材默认值”的规则集中维护在 `媒体参数范围与默认值.md`。
- 技术影响：后台是控制面，桌面端是执行面；视频播放不经过 Go 服务转发。
- 验证面：后续需要 Go 单元/接口测试、React 类型检查与页面冒烟、Rust 单元测试和桌面端手工播放检查。
- 已确认模型调用边界：Go 记录、管理并测试 OpenAI-compatible 模型账号；Rust 申请单账号短期直连租约并直接调用模型。Go 不转发实时话术内容，不向客户端下发完整号池或长期 API Key。
- 已确认播放与音频处理边界：文件选择或系统拖放候选按返回顺序整批探测，只有全部成功才原子替换、追加或替换单项；成功编辑停止当前资源。用户点击播放后，视频由受管 mpv 会话连续播放，普通声音独立生成 AAC/M4A 候选。
- 当前唯一媒体边界是 `选择/拖入候选 → 按返回顺序整批探测 → 原子替换/追加/替换单项` 或 `基于稳定 source_path 原子重排/删除/清空`；非空池首项 `Ready` 后由用户点击播放打开窗口，再由 `complete_playback_item` 单飞推进到下一项或末项回首项。单项池自循环，取消、超限、重复路径或任一失败保持旧池。播放池只在当前进程，不持久化、不上传，不是生成版本队列；导入不默认计算完整文件哈希，未映射参数不能启动伪处理，独立研究报告不在此链路内。

## 需求

### 功能需求

- FR-1：React 管理端和 Rust 客户端必须复用同一用户身份事实源，但使用独立登录入口和会话 audience。管理端只签发 `admin` 会话；桌面端只签发 `desktop` 会话并拒绝管理员账号。
- FR-1a：Access Token 有效期必须为 15 分钟。Access Token 过期后，客户端和管理端必须使用 Refresh Token 单飞刷新并只重试原请求一次；Refresh Token 保持 30 天绝对有效期，轮换不得延长该期限。每次 Refresh 在同一事务消费上一代并创建下一代，继承原 user、product、device、audience 与 family；旧 Token 重放必须撤销整个 family。
- FR-1b：桌面端不得把 Refresh Token 写入 Web Storage、普通配置、本地数据库、日志或错误正文；必须由 Rust 使用系统 Keychain/Credential Manager 完成保存、读取、轮换、启动恢复和退出删除，原始 Refresh Token 不得返回 WebView。稳定 `device_id` 同样由 Rust 管理；合法旧 WebView 标识只作为首次迁移候选，稳定存储读取或写入失败时停留门禁，不得以临时新 ID 继续注册。钥匙串不可用时应提示用户，不得伪造已持久化状态。
- FR-1c：桌面端门禁只提供账号和密码；可显式记住账号并保持登录，但不得持久化登录密码。账号可作为非敏感界面偏好保存到 LocalStore；密码只在本次手动登录请求的必要生命周期内存在，不得进入桌面端 Web Storage、普通配置、本地数据库、操作系统凭据库、日志、埋点、错误正文、截图或测试快照。升级后必须尽力删除旧版本遗留的登录密码和激活码系统凭据。
- FR-1d：启动恢复、登录后跳转、深链和路由返回进入工作台前，必须同时满足“当前 `desktop` 会话有效”和“服务端对当前 `user_id + device_id` 的账号激活授权校验成功”；登录后桌面端只提交当前设备信息，由服务端按到期时间顺序选择该账号任一未过期、未作废且 `bound_devices < max_devices` 的授权并幂等占用设备槽位，满额授权不得阻止继续选择同账号的其他未满授权。Refresh 恢复成功但授权状态未确认、网络错误或历史缓存存在都不能代替本次门禁校验。认证失败必须回到登录门禁；账号授权失败必须停留在登录门禁并保留服务端稳定错误码及可操作说明。已经进入工作台后发生网络中断时，既有本地播放仍按离线规则继续，但不得把该规则用于冷启动或重新进入工作台。
- FR-1e：登录、保持登录和清除本地会话的成功反馈使用同一临时提示生命周期，展示 `4` 秒后自动清除。任何新的登录、保存或清除操作开始时必须清理旧成功提示；每次成功提示拥有独立代际，旧计时器回调只能清理创建它的提示，不得清除后来产生的成功、警告或错误。警告和错误不得使用该自动清理计时器；组件卸载或提示替换时必须取消对应计时器。
- FR-1f：`/api/v1/admin/**` 必须拒绝非 `admin` audience；`/api/v1/client/**` 必须拒绝非 `desktop` audience。不能只依赖角色、导航隐藏或客户端路由隔离；服务端每次请求都必须从会话事实读取并校验 audience。
- FR-1g：主动退出必须按 Refresh Token 哈希定位并幂等撤销当前会话，不要求 Access Token 仍有效，不泄露 Token 是否曾存在。桌面端先尝试服务端注销再清理本机凭据；网络或服务端失败时必须清理本机状态并明确提示“本机已退出，但服务端注销未确认”，不得伪装成远端撤销成功。Rust 必须串行登录、启动恢复、Refresh、Logout 及其 Keychain 写删，界面在 Logout 完成前禁止提交新登录，旧退出流程不得删除新会话凭据。
- FR-1h：用户不存在、密码错误、用户禁用、无产品权限和桌面尝试管理员账号必须统一返回 `401 UNAUTHENTICATED` 与中性提示；不存在用户也必须执行预生成 dummy hash 校验。登录限流同时按账号和来源 IP 的带领域前缀 HMAC 摘要生效，账号与地址状态均持久化到 PostgreSQL 并由多实例共享，但不得共用同一低阈值：账号第 5 次失败起退避，来源地址第 50 次失败起承担聚合滥用退避。进程短突发分别限制单账号和整个来源地址；同一 NAT 中一个账号的普通失败不得让其他账号进入账号级冷却。数据库不得保存明文用户名或完整 IP。只有请求来自配置的可信代理 CIDR 时才能解析转发地址；`429 RATE_LIMITED` 必须携带 `Retry-After`。
- FR-1i：新建用户、管理员重置密码和管理员自身换密统一使用 Argon2id PHC 格式；既有 bcrypt 哈希只作兼容读取，并在成功登录后以受控写入渐进升级。新密码要求 15～128 个 Unicode 字符且不静默 trim、截断或强制字符组合；登录入口继续兼容既有 8 字符以上 bcrypt 账号，直到凭证成功升级或被重置。
- FR-2：管理员必须能够创建、编辑、禁用和查询用户。
- FR-3：系统必须区分管理员和普通用户权限。
- FR-4：管理员必须能够创建激活授权，创建时选择绑定账号、设置有效期和最多登录设备数，并可查看状态和手动作废。
- FR-5：客户端首次使用必须完成账号密码登录；服务端按绑定账号的有效激活授权自动占用当前设备槽位。进入可用状态后桌面端显示当前账号授权剩余有效期，桌面端不接收激活码明文。
- FR-6：客户端必须生成稳定的设备标识，并支持设备重新安装后的重新绑定流程。
- FR-7：客户端必须上报操作系统、客户端版本、硬件概要、磁盘剩余空间、播放状态和最后心跳。
- FR-8：管理员必须能够查看、禁用和解除绑定桌面设备；解除绑定后设备进入待授权状态并释放已知设备槽位，后续账号密码登录时由服务端按该账号仍有效且有余量的激活授权自动重新绑定。
- FR-9：客户端必须支持从文件对话框或操作系统文件拖放接收 `1～100` 个白名单本地源媒体：视频为 `.mp4`、`.mov`、`.mkv`、`.avi`、`.webm`、`.m4v`、`.ts`、`.m2ts`、`.flv`、`.wmv`、`.3gp`，纯音频为 `.mp3`、`.wav`、`.m4a`、`.aac`、`.ogg`、`.flac`；候选顺序以选择或拖放返回顺序为准。不得只按扩展名信任文件，必须逐项通过 FFprobe 探测实际容器、音视频流、时长和可读性。整批替换、追加和单项替换只有全部新增候选完成探测才原子提交，取消、超限、重复路径或任一失败保持旧池且不提交部分成功项；导入不得转码，本版本仍不提供目录导入。
- FR-9a：播放池标题区必须常驻“导入媒体”和“追加媒体”入口；空状态必须提供“选择或拖入媒体文件”主提示；已有素材时必须在同一主页 Card 常驻替换、排序、删除与清空操作，不打开播放池管理抽屉。系统文件拖放与文件选择复用同一 FFprobe 探测、原子提交、快照刷新和结构化错误链，不得形成两套导入实现。
- FR-9b：播放池管理必须支持单项替换、拖拽排序、键盘上移/下移、单项删除和清空。拖拽不得是排序的唯一操作；删除和清空必须确认。播放池成员变更由 Rust 播放状态作为唯一事实源，串行停止当前媒体资源并将新池首项重置为 `Ready`；失败保持原池、索引、代际和播放状态。`source_path`、FFprobe 元数据和哈希状态只读，所谓“编辑”仅指用另一个已探测文件替换该项。
- FR-9c（本批实施）：FFprobe 分类必须忽略 `disposition.attached_pic=1` 的封面流；存在非封面视频流时为 `video`，只有音频流或音频流加封面时为 `audio`，没有可解码音视频流时拒绝。播放池项保留受校验的 `media_kind`、`source_path`、可缺省 `playback_reference` 和真实 `compatibility_mode`。
- FR-9d：视频导入只通过受管 FFprobe 对只读 `source_path` 完成有界容器与流探测，不执行生产 mpv 预播放，也不启动 FFmpeg。成功项固定 `playback_reference=source_path`、`compatibility_mode=direct`；真实播放时由受管 mpv 完成解码确认，失败则发布结构化错误，不得回到导入期整段转码。
- FR-9e（本批实施）：纯音频项不创建 mpv 视频会话，媒体表面保持纯黑；其视频处理状态为 `not_applicable`，普通声音、插话和固定话术继续执行。视频与纯音频互相切换时递增 `playback_generation`、释放旧领域资源并建立新同步 epoch。
- FR-10：客户端必须只创建一个独立播放窗口，支持有序播放池循环、暂停、继续和停止；多项池当前项结束进入下一项、末项回首项，单项池回到自身。任意时刻只有一个活动媒体源，不创建第二个视频版本。
- FR-11：本地播放不依赖后台在线；网络恢复后客户端补发必要的状态事件。
- FR-12：管理员必须能够配置模型供应商、模型名称、账号、优先级、额度和状态。
- FR-13：客户端必须能够申请、续租和释放模型账号租约。
- FR-14：后台必须记录模型租约、调用结果、额度消耗、错误和冷却状态。
- FR-15：模型账号密钥必须只在服务端安全存储；默认不向客户端下发完整号池。
- FR-16：关键操作必须留下审计记录，包括用户、设备、激活码和模型账号变更。
- FR-17：客户端必须提供当前进程内的本地视频播放池，支持整批替换、追加、单项替换、重排、删除和清空，并查看池内名称、时长、格式、大小、状态、当前项和播放进度；本版本不跨重启持久化播放池，也不提供目录导入或路径/探测元数据手工编辑。
- FR-18：客户端必须支持播放、暂停、停止、进度拖动和 `A→B→…→A` 有序循环；单项池自循环。播放速度不提供手动调整，由普通声音预设/周期随机化更新并同步音视频。该顺序只消费用户选择的本地源文件，不是多版本或生成版本队列。
- FR-19：客户端必须提供独立画中画预览窗口，预览窗口关闭或异常不能导致主窗口和心跳服务退出。
- FR-19a：桌面主界面必须实现唯一的 `36px` 深色自定义顶栏和三栏工作区；顶栏覆盖 React 加载、启动错误、登录、会话校验及全部业务页面，不随页面或窄屏工作区滚动。`1728×1044` 视觉基准下三列宽度为 `320px / minmax(0, 1fr) / 272px`，外边距与栏间距均为 `12px`。三栏独立滚动，DOM、Tab 和读屏顺序固定为素材/播放 → 视频/参数 → 普通声音/输出；`≤1199px` 时外层工作区按该顺序降为单列，不得仅靠横向裁切隐藏关键操作。
- FR-19b：左栏播放池按当前 Rust 快照顺序展示最多 100 项，文件拖放/选择导入、追加、整批替换、单项替换、拖拽与键盘重排、删除和清空全部直接位于主页播放池 Card，不打开播放池管理抽屉；不得提供随机播放或生成版本队列。保留播放、暂停、继续、停止、当前项进度、音量和画中画入口，不提供独立倍速按钮。中栏不再设置独立顶部状态条，视频、声音与插话的真实周期、处理和恢复状态分别就近展示在所属 Card。右栏在话术功能上方独立提供 PortAudio Host API 筛选、输出设备选择、内存缓冲设置与显式应用，并分开展示实际出口、已应用配置和处理状态，同时保留固定 `440 Hz / 400 ms` 本地测试音。右栏同时提供真实处理链和本地运行状态；插话文件、麦克风插话和固定话术的详细配置通过紧凑入口/抽屉承载，其中麦克风插话在实现前必须明确显示“待实施”，不得提供伪入口。
- FR-19c：高级声音抽屉宽度为 `min(760px, 100vw)`，插话文件、麦克风插话和固定话术抽屉宽度均为 `min(560px, 100vw)`。四者统一使用深色中性主题、双行标题、真实状态摘要和卡片分区；正文独立滚动，标题区与底部主操作保持固定。表单不得用 placeholder 代替持续可见标签，窄屏字段必须单列。删除用户预设前必须确认，取消后不得修改当前预设、参数或播放状态。高级声音抽屉只保留多轨与预设、声音周期、状态和缓存管理；PortAudio 设备配置只在右栏独立 Card 提供，不在抽屉重复。播放池不属于抽屉体系，其完整编辑操作保留在主页播放池 Card。两个具名声音分组位于主页声音列且不重复，上述 9 项在两处都不得出现手动输入控件。插话文件编辑本地目录、间隔、音量、duck、固定/随机选择、`p01–p22` 预设池、多轨和变化策略；固定预设默认 `p01`，默认随机池为 `p01–p20`，`p21/p22` 仅显式参与。麦克风插话编辑输入设备、输入增益、VAD 灵敏度、起声确认和尾音保持，默认开启回声消除、降噪与自动增益。固定话术只使用 `localService` 系统声音；四个抽屉均不得加入实时话术幻化、speech-to-speech 或已删除的旧报告链路。
- FR-19d：全局账号、退出、激活有效期和会话提示只能处于自定义顶栏下方的页面层级，必须低于 Ant Design Drawer/Modal 等弹层；不得通过提高单个抽屉标题的局部层级掩盖冲突。所有 Drawer 使用同一 `36px` 顶部安全区，遮罩和面板不得覆盖窗口控制栏；四个功能抽屉继续使用 Ant Design 公开 `Drawer` 属性和 `styles` 配置，标题/底部不随正文滚动，正文可独立滚动，并保留自动聚焦、Esc 关闭、焦点归还和遮罩阻止背景交互。不得使用 `.ant-*` 内部选择器，也不得改变插话文件保存逻辑。
- FR-19e：主页声音参数列可按“音高、变速与共振峰”和“特征、信噪比与空间混合”持续可见分组展示；MFCC 偏移、MFCC 维度、SNR 浮动、频谱盲区、干湿比、环境声混合、高质量音高、播放速度和共振峰偏移只显示当前值，不提供滑块、输入框、步进器或其他手动修改入口，高级声音抽屉也不得重复提供这些输入。普通声音不存在“自然动态、音色库、目标 SNR、环境声素材四项可编辑基线”的例外，参数仍由默认值、预设、周期调度和运行时状态更新。上述 9 项继续由普通声音预设和周期随机化生成；随机化提交后必须刷新展示值，并进入既有参数范围校验、N+1 候选准备/提交和运行状态链路。目标 SNR 的 `null` 必须显示为“自动”；环境声来源显示“用户素材”“随包内置真实环境声”或在混合值为 `0` 时显示“未加载”。
- FR-19f：高级声音抽屉的底部只提交声音参数，视频处理由视频卡片内的独立开关负责。视频和声音分别进入自己的 N+1/N+2 计划、提交和计数，不提供联动周期；视频计划只包含参数快照和目标 PTS，不包含媒体文件。
- FR-19g：插话文件保存、麦克风插话保存和高级声音人工应用成功后必须使用 Ant Design `message` 提示“保存成功”；失败继续沿用现有错误展示。高级声音的自动周期处理不得弹出该提示。
- FR-20：客户端必须支持音频效果开关、视频效果开关和一键恢复默认参数。
- FR-21：普通声音处理必须提供声音处理开关和本地 DSP 参数预览；35 个预设可写效果字段逐套存在于 p01–p22，并由本轮主预设参加变化。随机化后的值先通过范围与组合校验，再写入 N+1 候选临时媒体，并在切换成功后刷新只读展示。环境声来源必须通过路径、格式、可读性和 FFmpeg 解码验证；失败保持当前音画。Web Audio 不执行声音效果。
- FR-22：参考图中的 MFCC 偏移、SNR 波动/目标、共振峰配置/诊断、频谱扰动、相位扰动、音色库、滤波 Q 值和高频扰动等声音字段全部属于正式 `AudioEffectParams` 需求，当前语义已确认的字段均已进入实际处理或只读诊断边界。`current_formant_hz` 只读投影 LPC F1，MFCC/SNR/噪声底/F1/F2/F3 必须来自最终混音 PCM；自然动态和音色 ID 只代表本地轻量动态/EQ，不宣传为语音克隆或实时话术幻化。旧报告 Worker、研究报告和独立分析任务已移除且不得重新引入。
- FR-22a：每个当前声音字段必须标记为用户配置、只读诊断或资源统计；处理状态使用 `disabled`、`configured`、`runtime`、`unavailable` 区分。只读测量值不得作为 DSP 参数提交；仅完成配置保存或校验时不得显示为 `runtime`，未接入 DSP 时必须显示 `unavailable`。
- FR-22b：本地声音处理预览必须与实时音频幻化独立；除麦克风插话在桌面本地执行的回声消除、降噪、自动增益和非语义 VAD 门控外，不得启动或读取实时音频幻化的 speech-to-speech、VAD、ASR、LLM、TTS、模型租约、话术上下文或候选音轨状态。UI 展示全部已支持的 FFmpeg/Signalsmith 声音参数、处理进度、当前 PortAudio/WebView 出口和 `disabled/configured/processing/runtime/unavailable` 状态。
- FR-22c：客户端以“插话文件”作为用户可见名称，支持配置本地插话目录、随机间隔、音量、duck 和独立选择状态，但普通声音与插话文件共用同一套 p01–p22 完整参数定义。目录流式扫描最多接受 `1000` 个受支持文件，第 `1001` 个使整个配置被拒绝；React 必须从已提交的插话快照读取并显示文件数。`fixed` 可显式选择任一预设；`random` 默认池为 `[p01–p20]`，p21/p22 只有用户显式加入才参与，每次最多四支路。周期是预设外的全局配置，周期到期或配置变化只影响下一段。PortAudio 直接处理 PCM；WebView 使用同一参数生成短期处理缓存，失败才回退原声。目录、预设、环境声解析或缓存处理失败不得伪造成功。内部类型和兼容字段可暂时保留 `interlude`/`random_interlude`，但不得继续把“随机插话”显示给用户。
- FR-22d：固定话术只使用 WebView2 `speechSynthesis` 的本地系统声音朗读 1～500 个 Unicode 字符；朗读期间临时静音原视频声音并暂停插话，结束、失败或取消后恢复原状态，不调用 ASR/TTS/语音克隆 Worker，不生成音频或视频文件。最多 10 条预制话术的增删改、停止朗读和独立运行状态管理属于下个版本，本版本不实现也不作为验收条件。
- FR-22d1：客户端必须提供用户显式开启的麦克风插话。音频只在本机处理；SpeexDSP 依次执行回声消除、降噪和自动增益，再以 VAD 产生 `Idle / MaybeSpeaking / Speaking / Hangover / Error` 状态。进入 `Speaking` 后，主媒体、普通声音处理、插话文件和固定话术全部通过同一最终数字增益门控到 `0`，最终混音只保留麦克风；尾音保持结束后按短释放恢复此前可听状态。麦克风优先级高于固定话术、插话文件和主媒体。运行时必须支持权限拒绝、设备拔出、无 PortAudio 输入、DSP 初始化失败、暂停、停止、切源和窗口退出的结构化恢复；不得以物理扬声器音量变化代替数字静音，不得录音落盘或上传。该需求当前为“正式需求·待实施”，不计入现有版本完成率。
- FR-22e：普通声音与视频必须分别提前预选 N+1/N+2 周期和完整参数，所有请求携带 `playback_generation + source_revision + sequence + duration_ms + position_ms + absolute_position_ms`。每个领域的 N+1 是该领域唯一可执行候选，N+2 不创建文件、FFmpeg、PID、线程或后台任务。两个 target 永不合并，不提供联动模式；两条链分别校验身份、提交和重建，任一领域迟到、过期或失败只回退该领域。
- FR-22f：普通源同步和 PCM 生产恢复从创建到提交必须持有同一个独立占位；占位期间 N+1 prepare 和无匹配的旧候选 cancel 不得推进全局音轨 token。播放代次、媒体源或声音 revision 变化只表示同步候选已被新状态取代，后端必须返回结构化 `reason_code + retryable`、销毁旧候选并保留当前轨、PortAudio 流和偏好；准备期间被更新的停止、暂停或切换请求抢占所产生的 `audio_mixer_candidate_stale/audio_mixer_start_stale` 同样属于可重试控制竞争，不得关闭 PortAudio 偏好或硬件流。暂停、停止仍可立即取消。前端按最新快照最多立即重试一次，用户主动暂停或停止产生的旧同步取消不得包装成“音频源不可用”或“已回退 WebView”；最新同步成功后只清除已解决的 PortAudio 回退提示，不得清除无关播放错误。声音处理配置实际变化时必须递增 `audio_stream_revision`，重复提交相同配置不得制造新 revision。
- FR-22g：普通声音处理开启时，输入身份固定为播放池当前项源音轨，处理结果写入独立声音 N+1 临时产物。视频由无声 mpv 表面呈现，处理音频继续使用独立 A/B/PortAudio；两条链共享源时间轴并禁止双声或静音。
- FR-22h：Rust 命令/runtime 是自然 EOF、单项自循环与多文件顺序推进的唯一连续性所有者。mpv EOF 必须携带并精确匹配 `playback_generation + source_path + loop_index + source_duration_ms + backend_epoch + clock_epoch`；同一身份仅允许一个原子恢复事务，重复事件幂等，旧身份事件丢弃。runtime actor 必须直接通知 Rust EOF 监督事务；React/WebView 不得为受管 mpv 发送 `complete_playback_item`，不得在本地递增 `loop_index`、seek、解除暂停或成为提交/回滚事实源；EOF 待处理期间只允许冻结视频候选并展示状态。Rust 事务内先推进权威 `PlaybackCore`，再对单文件执行合法源内 `seek → pause=false`，或对满足门禁的视频→视频执行受限 `loadfile replace → 恢复权威暂停态`；失败时释放失效原生会话并暴露兼容画面，不得留下“逻辑 Playing、物理 pause/EOF”的半提交表面。结构化结果和失败原因必须传到主操作窗口，不得以笼统错误覆盖，也不得显示在面向客户的最终效果窗口上。
- FR-22i：若物理环缓与旧轨 PCM 均已耗尽，预热完成的新生产者必须允许从静音执行 30ms 淡入并接管，不能永久等待不存在的旧轨淡出样本；旧轨仍可用时继续执行正常双向交叉淡化。
- FR-22j：视频循环完成时，最终效果窗口必须先单飞提交新 `loop_index`，后端确认后再按新循环的绝对媒体时间显式重锚 PortAudio，禁止继续沿用上一轮音频时间轴。PortAudio 输出状态必须以绝对呈现锚点和实际 callback 消费帧连续计算；若同源 PCM 已物理越过段尾而显式 N+1 尚未晋级，输出线程必须按绝对时间重建新 `loop_index`、源内 PTS 和 `LoopBoundary` 身份，不能把合法跨段时间永久判为 `None` 并反复触发恢复。实时 PCM 解码每个 FFmpeg 子进程只处理一遍有限输入；成功到达 EOF 后由同一受管解码线程等待旧进程退出并从源文件 0ms 顺序续接，禁止依赖单个 `-stream_loop -1` 进程无限跨越边界。任何时刻仍只允许一个当前 FFmpeg 和一个候选 FFmpeg。
- FR-22k：PortAudio 设备卡位于右栏话术功能上方，基于 `list_audio_output_devices` 的真实枚举提供 Host API 筛选与输出设备选择，允许设置 `128–2048 KiB` 内存缓冲并显式应用；编辑值、已应用配置、运行时实际出口和处理状态必须分离。选中特定 Host API 后只显示该 API 的真实设备，无有效设备、缓冲越界、配置未变化、应用进行中或能力不可用时禁用应用；当前随包 PortAudio 运行库未编译 ASIO，不得报告 ASIO 已启用。默认仍为系统默认/WASAPI；固定 `440 Hz / 400 ms` 本地测试音仅在运行中且非忙碌状态可用。
- FR-22l：桌面中栏的参数展示层必须完整呈现 `VideoEffectParams`、`AdvancedEffectParams` 和 `AudioEffectParams`，TypeScript 字段定义与 Rust 三个正式结构逐字段一致。参数内容容器必须填满桌面中栏的实际可用宽度，不设置约 `880px`、`860–900px` 或其他固定最大宽度；外层为双主栏，左栏依次展示普通视频与高级视觉，右栏展示普通声音。每个主栏内部的参数卡网格按该栏实际可用宽度自动计算列数并自然换行，不限制每排卡片数量，不设置固定列数，也不得再把 `270–290px` 作为固定宽度或目标宽度；窗口尺寸变化时列数随可用空间自动调整。参数区极窄时，再将外层双主栏按“普通视频 → 高级视觉 → 普通声音”的 DOM 顺序降为单列。所有层级按可用宽度伸缩，布局切换前后均不得横向裁剪。普通参数卡固定为统一 `80px` 紧凑高度，仅展示参数名、当前值或无手柄紧凑分段进度和三态标签；布尔值与枚举只展示当前值。卡片底部不得显示范围、单位、接入情况或待实现原因组成的说明句。固定视觉频段权重使用独立 `196px` 固定高度，跨满所属栏，并按三列完整展示频段名称与当前权重，继续使用卡级三态标签，不得通过裁剪维持高度。参数范围、单位、默认值与能力状态继续由正式契约定义并参与进度归一化、输入校验和媒体边界判断，删除卡内说明不等于删除这些事实。每张参数卡或独立参数模块在面板挂载时分别从《媒体参数范围与默认值》的青蓝/橙/紫/粉紫/黄参考图五色色板分配自己的颜色，不得按普通视频、高级视觉、普通声音大分类共用固定色；五色可循环复用，分配顺序应尽量避免视觉相邻项同色，既有逐卡映射在挂载期内保持稳定。所有卡片只静态呈现逐卡强调色、当前值/进度和状态文字；值变化时立即更新内容，但不得维护闪烁代次，也不得执行透明度闪烁、光晕、发光阴影或关键帧动画。面板、分区和单卡必须使用稳定输入与渲染边界，只有实际消费值或状态变化的卡片重新渲染；原始诊断消息、播放时钟及其他无关主页状态不得带动全量参数卡。面板只允许“已接入 / 正式需求待实现 / 待确认”三态；颜色不表达算法状态，三态只表达算法能力且不决定编辑权限，三种状态均不可在参数卡中修改；主页声音列的两个具名分组按 FR-19e 处理，上述 9 项只读展示且不属于诊断输入。参数继续由默认值加载、普通声音预设、周期随机化和媒体引擎状态更新；随机化后的显示刷新不绕过既有校验、候选提交或运行状态链路，任何未接入非默认值都由 Rust 媒体边界明确拒绝，前端不得静默改回默认值。`audio_stream_params/audio_stream_variants` 继续服务于已提交普通声音支路的独立状态区，不是全量参数面板的唯一事实源，也不得用于从参数值反推预设身份。
- FR-22m：插话文件启用前必须规范化并校验目录、普通文件、格式、可读性和根目录边界。目录白名单包括音频 `.mp3/.wav/.m4a/.aac/.ogg/.flac` 与视频 `.mp4/.mov/.mkv/.avi/.webm/.m4v/.ts/.m2ts/.flv/.wmv/.3gp`；视频只解码音频流。普通声音与插话文件共用 22 套精确 35 字段预设定义但分别保存选择状态。`interval_min_ms/max_ms` 只决定一个插话文件结束后何时随机下一文件；随机模式在每个文件开始时先抽样一次，`audio_variation_period_min_ms/max_ms=1000–600000ms` 再按当前插话媒体时间决定同一文件内的后续切换。短文件未达到首个边界时只使用开始时抽到的预设；长文件在每个边界保持文件和播放位置不变并继续切换，最多持有 current 与 pending 两个预设候选，候选失败继续 current。`audio_variation_mode` 仅作兼容字段，当前 UI 统一写入 `periodic`，旧 `each_playback` 快照也执行该统一规则。同一活动文件只随机一次路径。PortAudio 插话解码到当前文件 EOF 后必须结束本段并释放 duck，短文件不足预缓冲水位时使用已解码的有限尾部，不得从文件开头补齐或在同一段内循环。PortAudio 使用原始音频文件作为静音结束时钟时，时钟必须按当前插话预设播放速度推进；使用已处理缓存作为时钟时保持缓存原速。自然结束必须发布 idle 运行状态，使当前文件显示“无”、插话波纹立即清空并开始下一文件的随机间隔；播放中波形只接受不早于当前插话 `started_at_ms` 的新鲜最终混音诊断，周期进度消息更新 `sent_at_ms` 时不得移动该开始门槛或让波形持续消失。“插话声音预设”卡片的当前预设和完整参数行必须持续显示，空闲时回退到已保存固定预设或随机池第一项。停止、换源、出口切换或窗口卸载使 generation 失效并释放任务、缓存和 duck。运行状态只上报当前文件基本名称，不暴露完整路径；主页“插话声音预设”卡片在正文参数网格之前居中显示当前文件基本名称和本段全部实际预设名称/编号，多轨不得只显示第一项。
- FR-22n：保存插话文件配置必须完成目录、字段范围、`audio_preset_ids` 唯一合法项、asset scope 和持久化锁完整性校验；真实失败拒绝提交并展示结构化原因。Tauri 校验成功后，React 必须把同一份非敏感配置写入本机 LocalStore；重新登录、关闭后重开或重启软件时，在首个 playback snapshot 到达后自动恢复并再次交给 Tauri 校验，避免用户重复输入。损坏、越界或目录已失效的本地值不得伪造为已恢复，界面保留可编辑输入并显示恢复失败原因。插话文件抽屉必须把启用状态、媒体目录、插话声音周期、声音预设、多轨数量、预设变化周期和 duck 过渡输入集中在同一个“插话配置”区块，并按上述顺序使用可见分组标题，不能拆成多个相隔的配置卡导致用户遗漏。新预设池和变化周期从当前活动文件的下一个媒体时间边界消费，不调用 `StopInterlude`、不重新随机文件；插话音量允许从主页“插话声音周期”卡片实时独立更新，界面只显示和输入整数 `0%–100%`（`0%=-60dB`、`100%=0dB`），历史正增益值回显为 `100%`，当前 UI 不产生超过 `100%` 的值，IPC 与持久化继续使用 dB。播放控制中的媒体音量与静音只控制主媒体，WebView 与 PortAudio 都不得因此改变或静音插话。主页运行计数必须拆分：`插话声音周期` 只累计当前播放会话成功开始的新插话文件次数；`插话预设变化周期` 只显示当前文件内已完成的预设切换次数。预设切换不得增加文件计数，新文件成功开始时文件计数加一且预设切换计数归零，停止播放时两者归零。“插话声音周期”进度条按上一文件结束后本次已等待时间占当前随机触发间隔的比例独立推进，触发时到达 `100%`，文件结束后归零并开始下一轮；不得固定为 `0`，也不得读取或改写当前文件内部的预设变化进度。
- FR-22o：全量正式参数主面板必须在同一视图中持续显示视频周期与声音周期两个状态模块。每个模块分别展示自己的配置范围、真实变化次数，以及由本领域 N+1 计划的目标时间和周期长度计算出的进度；没有真实下一计划时进度必须为 `0` 且保持非活动。声音模块的状态标签直接读取现有 `audio processing status` 事实源。视频和声音始终各自推进，不提供联动模式或共享目标。处理关闭、播放暂停、对应周期未启用或真实计划缺失时，两条状态按现有调度器次数和处理状态展示，不伪造进度。
- FR-22p：视频和声音周期输入范围统一为 `1–60s` 且下限不大于上限；每轮按真实媒体时钟从闭区间重新随机，区间相等时固定，暂停或确认停滞时冻结。前端保存当前 N、N+1 和 N+2 计划；只有 N+1 可以进入 Rust，N+2 不创建文件或任务。
- FR-22q：视频快照必须经过 `planned → validated → pending → applied/rejected`，声音候选继续经过 `rendering → verifying → ready → preloading → switching → active`。GPU83 快照以一条受限 IPC 命令原子提交；只有命令成功、下一帧真实呈现且 stderr 无禁用错误时才更新视频参数与次数。
- FR-22r：Rust 允许同时一个受管常驻 mpv 会话和最多一个普通声音 Worker。mpv 使用单一持久 JSON IPC、唯一 `request_id`、有界读写队列、命令 deadline、stderr 尾缓冲、Job Object 和对称 Join；声音 AAC/M4A 继续进入独立 A/B/PortAudio。
- FR-22s：EOF 恢复成功必须基于 mpv 物理事实，不得只比较 generation、backend epoch、clock epoch 或 loop。播放态至少确认源路径/媒体身份正确、`pause=false`、`eof-reached=false`、`seeking=false`、VO 可用、`0 <= source_pts_ms < source_duration_ms`，并连续两次观察到 PTS 前进；用户主动暂停时必须保持 `pause=true`。命令结果未知时先查物理状态并按同一事务身份幂等确认，未生效最多执行一次同会话重试；断管、进程退出、身份不确定或重试后仍无 PTS 前进，才进入受管重建和既有单向降级。`presentation_pts_ms` 只用于跨循环计划、诊断和 PortAudio 可听时钟，mpv 的 `time-pos`、seek 和恢复验收只使用 `source_pts_ms`，禁止取模、夹紧或猜测。该整改不得改变普通声音 DSP、效果参数、候选格式、N/N+1、PortAudio 设备/混音、插话和固定话术语义；音频准备不得长期持有阻塞 EOF 事务的播放切换锁。
- FR-23：桌面参数面板完整展示 `32` 个普通视频项和 `53` 个高级视觉 UI 项。自动视频周期必须让除水平/垂直翻转外的 `83` 项在每个成功提交并呈现的 GPU 参数快照中真实参加同一份低感知变换；水平/垂直翻转固定关闭且不计入生效。任何字段映射缺失、父效果未启用、量化为零或验证失败时，整份快照拒绝提交并保持上一份已呈现快照或 Original，不得以字段有值或只写入 `applied_fields` 冒充生效。
- FR-23a：最终效果窗口的视频事实源为受管 mpv 会话的已呈现帧和真实后端状态，禁止叠加 CSS Filter/Transform。完整 `83` 项只允许由 `gpu-next/libplacebo` 及 mpv 帧调度原子链执行；GPU 失败时 CPU 只执行 4 个基础字段，再失败保持 Original。
- FR-24：视频固定频段、核心频率、视觉波频、挂件、切片、抽象几何脸、局部模糊、冻结检测、画面修复等均是正式 `VideoEffectParams`/`AdvancedEffectParams` 需求；具体字段与状态以《参考图参数对比与正式化清单》为准。真人脸/主体保护需要明确许可的检测模型，冻结回退需要播放控制策略，不可见水印需要载荷和密钥契约；这些能力未定义前不得用近似滤镜冒充。OCR 不确定标签必须标记待核，不得猜测为已实现。
- FR-25：当前产品内的正式效果参数必须可被发现并查看当前值和能力状态；参数卡、诊断和周期状态保持只读，主页声音列按 FR-19e 展示两个具名分组，但不得开放上述 9 项的手动输入。处理开关、重置、预设、调度与引擎更新是独立控制边界；随机种子可作为内部调度字段，但不得伪造生效状态或包装为平台检测规避结论。
- FR-26：客户端必须提供来自最终混音 PCM 的实时波形、频谱、基础音量/响度、噪声底、SNR、1–40 维 MFCC 和 LPC F1/F2/F3 展示；`current_formant_hz` 只读投影 F1。分析模块样本不足或异常时显示不可用但不阻塞播放，不能以静态数值冒充实测结果。
- FR-27：客户端当前支持随机几何挂件、抽象几何脸、同源画中画、锁定/解锁时间轴、透明度、大小、位置偏移、切片长度、源片段最小长度和切片触发间隔；外部文字/图片/Logo/贴纸素材、真人脸检测避让和层级编辑仍需后续素材输入与模型契约，未接入时必须禁用。
- FR-28：客户端必须支持保存和恢复效果参数配置；配置只引用本地素材，不复制或上传原始视频。
- FR-29：当前播放和媒体处理只在明确启用对应哈希链路时计算完整文件 SHA-256；旧阶段报告版本不属于当前产品契约，正式参数调度所需随机种子只保留为内部可复现信息。
- FR-30：不可见水印和随机扰动可作为普通媒体效果正式需求，但不得提供内容相似度、媒体鲁棒性、内容指纹、平台评分或检测规避结论。
- FR-31（后续版本，不纳入当前验收）：客户端可提供独立的实时音频幻化开关；开关开启后，输入本地音频文件或受控实时流，按播放轮次和片段上下文调用 speech-to-speech 的 VAD/ASR/LLM/TTS，由 LLM 决定沿用或改写，生成实时音频变体并在当前播放视频继续运行时替换基础音轨。本版本不得实现或开放该开关。
- FR-32：当前版本不提供独立算法报告、平台检测报告或变体版本导出；正式媒体参数保存、校验和运行状态属于当前契约，`source_media_index`、`playback_pool_cycle` 和 `playback_generation` 只属于当前进程播放状态。
- FR-33：效果预览和处理必须使用成熟媒体库或官方能力，不自行实现音视频编解码、滤镜算法、频谱计算、哈希算法或桌面窗口工具。
- FR-34：用户原始媒体必须只读保留。只有普通声音候选和插话缓存写入受控缓存；视频导入与 GPU83 周期都不生成视频文件。使用完成、停止、换源、关窗或退出时按所有权安全清理。
- FR-35：FFmpeg/FFprobe 是安装包和内部媒体执行依赖，不作为桌面 UI 独立功能展示；产品界面不得保留“运行资源”卡片以及安装、取消、选择目录、清理资源按钮，能力缺失、探测失败或处理失败统一通过导入/处理错误提示和可操作原因呈现。
- FR-35a：Windows release 中所有 FFmpeg/FFprobe 子进程必须经统一后台命令边界设置 `CREATE_NO_WINDOW`，覆盖能力探测、素材探测、视频处理、普通声音、插话及条目 EOF 后按播放池推进结果启动的新源进程，不得弹出终端窗口。该标志只改变窗口创建方式，不改变退出、取消和错误语义；spawn 失败、stderr、非零退出码、超时及资源缺失仍须沿现有结构化错误链传播，不得吞错或伪造成功。
- FR-45：模型凭证、Token、设备凭证、用户密码和服务端激活码必须按各自契约使用安全存储，不进入普通日志、截图、前端构建产物或提交记录；桌面端用户名可作为普通本地偏好，但不得与密码合并写入同一普通配置记录，桌面端不得持有激活码明文。
- FR-46：客户端必须提供本地有序播放池状态，快照使用 `source_media_pool`、`source_media_index`、`playback_pool_cycle`、`playback_generation`，并记录视频处理开关和声音处理开关；多项池换源时递增 generation 并按新媒体重建绝对时钟，单项池自循环保持 generation。播放池只在当前进程，不创建离线版本或生成版本队列。实时幻化字段如在代码中保留，只作为非当前兼容字段。
- FR-47（后续版本，不纳入当前验收）：实时音频幻化必须提供 Worker 状态、当前片段、播放轮次、LLM 决策、候选变体状态、音频延迟、换轨结果和回退原因；不再展示运行时指纹相似度。
- FR-48（后续版本，不纳入当前验收）：实时音频变体必须按“文件/流输入 → VAD/ASR → LLM 沿用或改写 → TTS → 同步门禁 → 基础音轨替换”执行；不能阻塞视频播放。本版本不实现 `realtime_variant` 候选替换。
- FR-49（后续服务端专项，未实施）：提供订阅、订单、设备和套餐版本的管理列表/详情接口；所有筛选、排序、时间范围和分页字段使用固定白名单、明确上限及相应数据库索引。
- FR-50（后续服务端专项，未实施）：套餐版本发布后不可原地修改价格与权益；订单以人民币分为整数金额并保存下单时的套餐、价格和权益快照，支付履约与订阅变更在同一事务边界完成。
- FR-51（后续服务端专项，未实施）：首个生产支付渠道采用微信支付 Native；使用官方 SDK，回调对原始报文验签/解密，重复通知幂等，未知结果进入主动查单对账，不实施退款、JSAPI、H5 或 App 支付。
- FR-52（后续服务端专项，未实施）：自助密码重置使用哈希化一次性 Token 和 PostgreSQL Outbox/Job Worker 投递；外部响应保持中性，投递正文不发送明文密码，重置成功后撤销现有会话。
- FR-53（后续服务端专项，未实施）：新激活码在 `activation_codes` 继续只保存哈希/前缀，并在独立 `activation_code_secrets` 表保存带 `format_version`、`key_id`、AAD 的 AES-GCM 密文；明文重看采用 POST、管理员密码二次验证、限流、`Cache-Control: no-store` 和脱敏审计。
- FR-54：当前版本通过独立设备绑定表保存账号激活授权的设备槽位，并保证同一设备重试不重复占位、解绑释放槽位、禁用设备不释放槽位。后续服务端专项再实现绑定详情管理、在 1～100 范围调整容量且不得低于当前占用、切换绑定设备并撤销旧设备会话/租约；过期或已作废授权不可绑定新设备，历史未知绑定按安全迁移规则保留审计。
- FR-55（后续服务端专项，未实施）：发布制品和桌面运行资源存入私有对象存储；服务端完成授权后只签发短时 GET 地址，CI 生成 SBOM、签名和 Provenance，发布入口校验可信身份与 digest，响应提供签名清单和文件哈希。客户端实际校验另立集成任务，用户本地媒体不得进入该链路。
- FR-56（后续服务端专项，未实施）：公共配置必须按 namespace、Schema ID 和不可变版本执行 JSON Schema Draft 2020-12 校验；禁止任意远程 `$ref`，限制文档大小、深度和复杂度，并提供 dry-run、revision/ETag 与审计。
- FR-57（后续服务端专项，未实施）：按数据集定义保留期、批次、归档与删除策略；数据库和对象存储备份加密且定期在隔离环境恢复，保留 Prometheus 指标并增加 OpenTelemetry Trace/OTLP，不把高基数或秘密写入遥测。
- FR-58（后续服务端专项，未实施）：CI/发布链路必须生成并验证签名、SBOM 和 SLSA 风格 Provenance；部署门禁校验 signer、issuer、repository、workflow、ref 和 digest，服务器不接收源码构建。
- FR-59（后续 Go + React 专项，未实施）：管理权限目录为服务端固定、版本化权限点集合；自定义角色只能组合已登记权限，不允许管理员创建任意权限代码。
- FR-60（后续 Go + React 专项，未实施）：管理员可绑定多个自定义角色，有效权限取并集；首期不提供用户级 allow/deny 覆盖或通用策略引擎。
- FR-61（后续 Go + React 专项，未实施）：内建 `super_admin` 拥有全部权限，本地管理员永久绑定；现有 `role=admin` 通过前向迁移保持兼容，禁止移除最后可用超级管理员或越权授予操作者不拥有的权限。
- FR-62（后续 Go + React 专项，未实施）：服务端对每个受保护操作按当前有效权限点鉴权，角色撤销在后续请求立即生效，不把长期权限快照作为 Token 中唯一依据。
- FR-63（后续 Go + React 专项，未实施）：提供当前管理员权限、权限目录、角色 CRUD 和用户角色分配 API，写操作幂等、防提权且记录语义化审计。
- FR-64（后续 Go + React 专项，未实施）：React 管理端使用 Ant Design 和 OpenAPI 生成契约，提供角色/权限、用户角色、套餐、订单、订阅、支付、重置投递、激活码安全、制品、公共配置和运维页，按权限控制导航/路由/操作并完整处理 403。
- FR-65（正式需求·待实施）：桌面主窗口提供 RTMP 推流配置和状态。用户输入完整 `rtmp://`/`rtmps://` 地址，选择画面、声音或双轨，默认双轨且至少一项；运行中草稿不得覆盖实际会话。Rust 校验并脱敏地址，直接从活动源生成 H.264 画面、从最终 PCM 总线生成 AAC 声音，以 FLV 发布到 ZLMediaKit。视频优先 GPU 解码/libplacebo 处理/硬件编码，失败按能力单向降级到 OpenH264；所有参数由固定字段和白名单生成，不接收自由 FFmpeg 参数。
- FR-66（正式需求·待实施）：桌面主窗口提供 AkVirtualCamera 安装/修复、启停和真实状态；首版只有产品固定设备 `GpAutoLive Camera` 和 YUY2 `1280×720@30fps`。Rust 独占 WGC/D3D11、三槽 staging、latest-wins 投递和独立 GPL sidecar 生命周期，D3D11 完成 GPU 捕获/缩放/转换，AkVirtualCamera direct mode 禁止二次 CPU 转换。所有输入、路径、命令、CLSID 和 IPC 均用固定允许列表，失败不得切换到 CPU 转换或反压 mpv。

### 非功能需求

- NFR-1：Go、React 和 Rust 代码在本地开发机或 CI 构建；服务器只部署构建产物，不在服务器上编译源码。
- NFR-2：所有服务端接口使用 HTTPS；客户端凭证和模型密钥不得写入普通日志。
- NFR-3：客户端网络异常、后台短暂不可用或单个模型账号失败时，不能导致本地视频播放器崩溃。
- NFR-4：后台 API 使用版本化路径，例如 `/api/v1`，并以 OpenAPI 作为跨语言契约。
- NFR-5：客户端状态更新应幂等；重复心跳、重复释放租约和重复上报不能破坏数据状态。
- NFR-6：首版采用单体 Go 服务，模块化组织，不引入没有实际需求的分布式组件。
- NFR-7：设备信息遵循最小采集原则，不默认采集 MAC 地址、硬盘序列号、摄像头、麦克风或用户目录文件列表。
- NFR-8：音视频处理任务必须异步执行并显示进度、取消和失败状态，不得阻塞 UI、心跳或播放控制。
- NFR-8a：视频渲染进度必须来自 Worker 的实际媒体处理位置、帧计数、输出字节或等价事实，并带最近一次推进时间；总超时和无进展超时必须有限且分开配置。运行中、停滞、取消、失败和校验阶段必须可区分，不能以最长总超时掩盖已停滞子进程。
- NFR-8b：临时媒体链必须暴露当前/候选 sequence、FFmpeg PID、处理速度、输出大小、最近推进时间、文件数、缓存字节、删除重试和当前参数。候选 FFmpeg 并发始终 `<=1`；停止、换源、seek、页面卸载和失败必须有界终止进程树、Join 线程、释放媒体元素及删除失效文件，不得保留无人管理任务。
- NFR-9：效果参数必须具备版本号和默认值；应用升级后旧配置无法兼容时必须安全回退，不得静默产生错误媒体。
- NFR-10：媒体处理不能覆盖原始文件。声音 N/N+1、插话缓存、可选视频兼容引用和用户显式导出必须使用独立目录、文件所有权、数量/字节上限和可验证清理；视频效果周期不产生缓存文件。
- NFR-11：本地 AI/媒体 Worker 必须支持 GPU/CPU 资源上限、并发上限、取消、超时、失败恢复和磁盘空间检查。
- NFR-12：LLM、ASR、TTS 和本地媒体处理都必须通过可替换适配边界接入，优先复用成熟工具，不自行实现模型或编解码能力。
- NFR-13：本地媒体 Worker 和弹窗预览必须可观测、可取消、可恢复；Worker 失败不得阻塞登录、主窗口和本地播放。
- NFR-14：正式媒体参数必须有稳定类型、单位、范围、默认值、版本和三态能力标识；旧报告任务与报告产物不得成为当前播放依赖，内部随机种子不得被当作平台检测规避能力。
- NFR-15：不可见水印和随机扰动必须按普通媒体效果执行可关闭、可重置和真实状态规则；不得生成平台检测规避结论。
- NFR-16（后续版本，不纳入当前验收）：实时话术幻化必须有超时、取消、并发和回退策略，模型或 TTS 失败时不能阻塞现有预览播放。
- NFR-17（后续版本，不纳入当前验收）：连续优先模式的播放线程、实时音频变体线程和 UI 线程必须隔离；话术候选必须有预加载、取消和资源释放策略。
- NFR-18：参数 DTO、领域模型和 UI 配置必须显式携带数值、单位、范围和默认来源（源素材/系统默认/用户调整）；禁止用裸整数表达毫秒、秒、Hz、dB、像素、百分比和数量。
- NFR-19：SHA-256 计算使用整个源文件或产物文件的字节流，不能只计算视频流、音频流或抽样片段；MP4 输入/产物必须额外展示完整 MP4 SHA-256，非 MP4 使用通用产物哈希字段，阶段哈希和产物清单必须可恢复、可审计。
- NFR-20：低可感知实时参数预览必须按全局变换周期平滑过渡，具有变化幅度上限；播放暂停或停止时必须立即停止计时器、清理运行时预览并发布停用状态，恢复播放后重新初始化调度器；取消、切换媒体或关闭声音处理时同样清理；不得因预览调度阻塞播放或产生预设快照以外的额外倍速变化；播放速度字段仍由本轮主预设参加真实处理。实时话术幻化不属于本版本依赖。
- NFR-21（后续 Go + React 专项）：服务端是管理授权唯一权威；React 隐藏菜单或按钮只改善体验，不替代 API 鉴权。管理页必须覆盖 loading、empty、error、403、取消/竞态与基本键盘/焦点可访问性。
- NFR-22：Windows 正式发布基线为 Windows 10/11 x64 的 NSIS 安装包；WebView2 使用随包离线安装模式，FFmpeg/FFprobe、PortAudio 和许可文案随包交付。便携 ZIP 不作为正式免依赖承诺。对外发布必须使用受信任的 Authenticode 证书签名安装器和主 EXE；无证书的测试包必须明确标记为未签名，不得描述为已完成生产发布。
- NFR-23（RTMP 正式需求·待实施）：RTMP/RTMPS 发布必须有独立所有者、取消、启动超时、无进展检测、有界 stderr、至多五次可取消退避重试、线程 Join 和 Windows Job Object 退出兜底。FFmpeg 存活不能单独证明发布成功，只有输出时间/字节持续推进才可显示推流中。地址 userinfo、path/query 中的 stream key 或 token 不得进入普通配置、日志、错误、状态、截图或测试快照；推流失败不得阻塞或改写本地 mpv/PortAudio 播放事实。
- NFR-24（虚拟摄像头正式需求·待实施）：AkVirtualCamera 必须作为固定提交的独立 GPL sidecar/安装组件接入，发布前完成法律审核、完整对应源码/补丁/构建脚本、SBOM、x86/x64 组件和 Authenticode；本机控制通道不得绑定非回环地址且必须有会话认证，命名共享内存使用最小 ACL。NV12 路径在上游平面高度边界修复和内存安全测试完成前禁止生产；首版使用 YUY2。GPU 必须是非 WARP D3D11、adapter LUID 一致，回读和丢帧可观测，关闭后两帧内黑屏。

## 假设

- A-1：用户拥有扩展名为 `.mp4`、`.mov`、`.mkv`、`.avi`、`.webm`、`.m4v`、`.ts`、`.m2ts`、`.flv`、`.wmv` 或 `.3gp` 且可被 FFmpeg/FFprobe 正确探测的本地视频；容器在白名单内不代表任意内部编码都必然可解码。
- A-2：用户登录后台和客户端需要网络；已经打开的本地播放窗口可以离线继续播放。
- A-3：每条激活授权绑定一个账号，管理员在创建时显式设置 `1～100` 的最多登录设备数；同一设备重复登录不重复占位，管理员解绑后释放该设备槽位。
- A-4：首版模型池是服务端统一管理的资源池，不向客户端批量返回全部密钥。
- A-5：后台部署方式可以是局域网或云服务器，但构建动作在本地或 CI 完成。
- A-6：桌面端的编解码、滤镜、音频分析和窗口能力采用成熟第三方库或官方 SDK；具体库在实施阶段按目标平台验证后确定。
- A-7：用户对输入视频、原主播音色/参考音和输出内容拥有必要权利或授权。
- A-8：本地 GPU/CPU 资源能够满足基础播放、视频处理和普通声音预览；本版本不把 speech-to-speech、LLM、ASR 或 TTS 纳入运行时依赖。
- A-9：Go 可管理和测试 OpenAI-compatible 模型账号并签发短期直连租约，但本版本桌面播放不使用实时话术模型租约；相关能力仅为后续版本预留。

## 约束与依赖

- Rust 桌面端建议采用 Tauri 2，Rust 负责系统能力和业务核心，React 负责桌面界面。
- React 管理端与 Tauri 桌面端可以共享接口类型和基础 UI 规范，但不共享与平台强绑定的运行逻辑。
- PostgreSQL 保存业务元数据；模型密钥需要加密存储并限制读取权限。
- 首版视频不上传后台，不把大视频经过 Go API 代理。
- 最终效果由桌面端独立窗口展示；Go 服务只管理授权元数据和状态，不转发视频内容。

## 风险与边界情况

- 激活码泄露：使用一次性、可过期、可作废的激活码，并记录核销设备。
- 设备重装：允许管理员解绑旧设备后重新激活，不自动无限扩展设备数。
- 客户端时钟异常：激活码和租约有效性以服务端时间为准。
- 设备离线：本地播放器继续工作；后台显示最后心跳，不伪造在线状态。
- 模型账号耗尽：标记为 exhausted/cooldown，后台切换其他可用账号并记录原因。
- 客户端直连模型泄密：只返回短期、限额、单账号、可撤销租约凭证，不下发完整号池或长期 API Key；凭证仅驻留 Rust 内存，供应商不支持该能力时禁用直连。
- 视频格式不兼容：文件选择白名单覆盖 `.mp4`、`.mov`、`.mkv`、`.avi`、`.webm`、`.m4v`、`.ts`、`.m2ts`、`.flv`、`.wmv`、`.3gp`，但扩展名不代表真实可解码。加入时必须通过 FFprobe 验证容器和真实音视频流；播放时再由受管 mpv 确认实际解码。任一阶段失败都保留源文件且不得隐式整段转码；导入探测失败时旧池保持不变。
- 音视频效果资源占用：长视频、高清素材和复杂滤镜可能占用较多 CPU/GPU；首版提供异步处理、取消、进度和缓存上限。
- 配置与原片不一致：效果配置绑定素材格式和算法版本，失效时提示并回退默认参数，不修改原片。
- LLM 话术改写、speech-to-speech 音频质量和候选时长漂移：均属于后续版本风险，本版本不启动相关链路。
- 本地预览资源占用：长视频、高清素材和视频处理可能占用较多 CPU/GPU/磁盘；当前版本提供必要的异步处理、取消、进度和缓存上限。
- 平台合规和版权风险：只使用有权素材和音色，不实现平台审核/检测规避能力。
- 多客户端同时操作：服务端以设备状态和命令版本号做幂等控制，客户端拒绝过期命令。

## 执行规则

- 按阶段顺序推进；如果实现发现范围变化，先更新本 PRD，再修改后续阶段。
- 先建立接口契约和状态模型，再实现 Go、React 与 Rust 客户端。
- 每个阶段都必须包含验证、错误状态、权限检查和清理无用代码/依赖的检查。
- RTMP 只按 [`ZLMediaKit RTMP GPU 直推实施方案`](./docs/superpowers/plans/2026-08-30-ZLMediaKit-RTMP-GPU直推实施方案.md) 实施，不擅自扩展到 OBS、平台账号自动登录、平台审核/检测规避、口型同步、多平台分发、离线版本预生成或实时话术幻化；当前进程内最多 100 项的本地有序播放池、单窗口/一个活动源顺序循环、视频/普通声音处理、插话、固定话术系统朗读、本地弹窗展示和待实施的 ZLMediaKit 直推属于正式范围。
- 固定话术播放只朗读用户输入文字：使用 Windows/WebView2 本地系统声音（仅 `localService`），不识别人声、不采集麦克风、不做人声分离、不克隆音色、不生成音频/视频版本、不下载语音模型；朗读期间原视频声音临时静音，结束或取消后恢复原状态。实时话术幻化不属于本版本，不得与固定话术共用当前实现。
- 号池密钥、用户凭证和设备 Token 不进入日志、截图、前端静态资源或提交记录。

## 阶段计划

### Phase 1：接口契约与基础数据模型

- [x] 确认 `产品需求文档.md`、`管理系统架构.md` 与 `桌面客户端架构.md` 中的用户、设备、激活码和号池边界。
- [x] 建立 OpenAPI `/api/v1` 契约草案。
- [x] 定义用户、设备、激活码、模型账号、租约和审计日志数据模型。
- [x] 明确会话 Token、设备 Token 和模型租约的状态转换；Refresh Token 轮换、旧凭证失效和 Logout 已支持 PostgreSQL `auth_sessions` 持久化，用户/设备、号池、租约、用量和审计仍处于控制面快照事实源加同事务影子写入的过渡阶段。
- [x] 验证：OpenAPI 文档可解析；关键状态转换有示例；没有把完整模型密钥返回客户端的接口。

### Phase 2：Go 后端与 React 管理端

- [x] 实现统一登录、用户管理和角色权限。
- [x] 实现设备注册、设备查询、心跳和禁用。
- [x] 实现激活码生成、核销、过期和作废。
- [x] 实现模型供应商、模型账号、额度和状态管理。
- [x] 实现开发期审计日志和敏感字段脱敏，并在 PostgreSQL 模式做同事务影子写入：仅记录动作、操作者、设备、目标上下文和 request_id，不读取请求体。
- [x] 实现 React 用户、设备、激活码和号池页面。
- [x] 验证：API 集成测试覆盖登录、激活、设备禁用和号池分配；React 完成类型检查、构建和主要页面冒烟。

### Phase 3：Rust 桌面客户端

- [x] 实现客户端登录和激活流程；Refresh Token 轮换、退出和 Keychain 恢复已接通，PostgreSQL 模式支持服务端会话重启恢复。
- [x] 历史能力：已实现显式记住账号密码与清除流程；该能力将由 2026-08-26 登录安全加固替换，不作为目标态继续保留。
- [x] 移除登录密码持久化与密码读取 IPC，迁移为“记住账号并保持登录”：账号只写普通偏好，Refresh Token 由 Rust 独占系统 Keychain/Credential Manager，不返回 WebView；升级时尽力删除旧登录密码和激活码凭据。
- [ ] 在 Windows 真机完成 Credential Manager 写入、重启读取、取消勾选删除与凭据服务拒绝权限的手工矩阵。
- [x] 实现本地设备标识、基础信息采集和心跳上报；已采集磁盘、内存、逻辑 CPU 和系统版本摘要，网络失败时会按用户/设备保存最新心跳并在下次发送时补发。
- [x] 扩展本地视频文件选择和 FFprobe 实际媒体探测至 `.mp4`、`.mov`、`.mkv`、`.avi`、`.webm`、`.m4v`、`.ts`、`.m2ts`、`.flv`、`.wmv`、`.3gp`；异步源文件 SHA-256、各格式实机直放兼容性和自动预览转码仍待后续验收。
- [x] 实现独立播放窗口和单源循环播放；Tauri 只创建一个 `final-effect` 窗口，视频结束后回到同一源视频的 0 秒，不创建第二个视频版本或窗口。
- [x] 实现暂停、继续、停止和错误恢复的最小播放状态闭环。
- [ ] 实现本地状态保存和离线播放。
- [ ] 实现本地播放池和素材元数据；当前播放状态只在进程内维护 `source_media_pool`、`source_media_index`、`playback_pool_cycle`、`playback_generation`，不跨重启持久化。
- [x] 删除旧 MSE/视频 period 入口并将正式周期收口到单一受管常驻 mpv 会话；声音继续 AAC/M4A A/B/PortAudio，并保留真实时钟、资源所有权和退出/启动清理。
- [x] 实现最终效果窗口内的实时波形、频谱分析和基础音视频诊断信息；画中画独立布局仍待补齐。
- [ ] 实现可见挂件/Logo/文字/贴纸/光效叠加层，不实现平台检测规避能力。
- [ ] 实现效果配置保存、恢复；[x] 已实现媒体缓存清理、当前播放缓存保护和原始文件不覆盖，配置持久化仍待补齐。
- [ ] 按《参考图参数对比与正式化清单》继续实现正式类型之外的缺失项；本轮已经完成当前正式模型中全部语义明确字段的真实执行或诊断接入。剩余项仅包括真人脸/主体模型、冻结检测回退、特征相似度、不可见水印载荷、外部挂件素材和 OCR 待确认字段；旧 `research_*` 类型、Worker 和 IPC 已删除，禁止重新引入。
- [ ] （后续版本，不纳入当前）实现独立实时音频幻化的本地调度契约：文件输入、speech-to-speech Worker 能力探测、沿用/改写决策、同步门禁、候选槽和基础音轨替换。
- [ ] 验证：Rust/桌面测试覆盖账号密码登录、账号授权自动设备绑定、账号偏好、Rust 独占 Refresh Token、钥匙串失败、工作台双门禁、`admin/desktop` audience 拒绝和稳定错误码映射；确认登录密码与 Refresh Token 均不进入 WebView、localStorage、日志或错误。服务端测试覆盖防枚举、可信代理、账号/IP 双维度限流、Argon2id/bcrypt 渐进迁移、并发 Refresh、family 重放撤销和 Access 过期后的幂等注销。媒体范围继续覆盖播放池原子 CRUD、`A→B→…→A`、单项自循环、重复结束事件幂等、视频/普通声音处理开关、参数重置和重试逻辑；实时话术决策不属于本版本验收。桌面端逐一手工验证 `.mp4`、`.mov`、`.mkv`、`.avi`、`.webm`、`.m4v`、`.ts`、`.m2ts`、`.flv`、`.wmv`、`.3gp` 的文件选择与系统拖放、CRUD、探测、目标 WebView 直放能力或明确错误链路、画中画、效果开关、重置、顺序循环和断网；重启后不得恢复播放池。

### Phase 4：大模型号池客户端使用

- [x] 完成开发期最小闭环：管理员新增/查询/禁用号池摘要，客户端申请、续租、释放租约，校验用户/设备归属、并发上限、过期回收、幂等和密钥脱敏；禁用仍有活动租约的账号会返回冲突，不中断运行中的模型请求。
- [x] 管理员可按用户配置 `provider/model` allowlist 和服务端已接收用量门禁；空 allowlist 表示允许全部已登记模型，门禁仅依据 `client_reported` 摘要记录，不作为供应商权威账单或未来用量预占。
- [x] 客户端和管理端对同一次用户操作复用同一 `Idempotency-Key`；参数或租约 ID 变化时换 key，成功/取消后清理；桌面端登录切换与租约操作互斥，已有活动模型租约时必须先释放后才能切换账号，并丢弃旧会话响应。
- [ ] （后续版本，不纳入当前）在真实模型调用前完成实时话术幻化的模型租约绑定接口：服务端校验当前会话设备、租约用途、provider/model、active 状态和有效期；客户端支持幂等绑定和失败重试。
- [ ] 实现模型账号完整自动健康状态、额度扣减和冷却规则；当前已落地失败测试 5 分钟 cooldown、成功恢复、UTC 日切 exhausted 恢复，以及带并发上限/取消/退避/advisory lock 的后台探测；供应商权威额度、严格扣减和长时故障矩阵仍待补齐。
- [ ] 将租约、用量和请求预占从快照影子写入切换为规范化 PostgreSQL 唯一事实源，并补齐生产级供应商状态核对、unknown 最终核销和完整额度扣减；当前租约/用量已有规范化领域事务、分页和幂等第一切片，仍需全量回填、快照退出、真实跨实例并发与故障验收，不能作为完整生产能力。模型请求超时/取消仍在服务启动及请求前的独立 State 控制面事务内恢复，释放并发槽位但保留每日预占，且即使原请求上下文已取消也先提交状态，同一幂等键不自动重试。
- [x] 完成开发期 Go OpenAI-compatible 模型账号登记/租约/使用记录最小闭环；服务端保留历史代理实现，但当前目标改为账号连通性测试和单账号直连租约。
- [ ] 完成 Rust 客户端直连模型的生产闭环：当前已实现 OpenAI-compatible 请求/响应校验和 `direct_model_chat` IPC，但 Go 供应商凭证委托、可信租约注入、调用摘要上报和失败切换仍待完成；无凭证时必须保持不可用。
- [x] 完成开发期模型用量与错误记录，并提供管理端查询接口；PostgreSQL 模式已做用量/请求预占同事务影子写入，模型规范化读源已完成受控验证，完整审计筛选和账号健康状态仍待后续实现。
- [ ] 验证完整能力：集成测试覆盖 OpenAI-compatible 账号连通性测试、直连租约、账号耗尽、冷却、租约过期、客户端调用摘要、真实供应商响应和密钥脱敏。

### Phase 5：单视频循环与当前声音效果展示

> 本节保留此前单源能力的历史完成事实；当前最多 100 项播放池增量及验收以 Phase 6 为准，不把旧勾选项改写为新功能已完成。

- [x] 实现单个源视频本地预检和最终效果弹窗入口。
- [x] 支持播放、暂停、停止和单源循环播放；视频处理、声音处理两个当前开关独立生效；实时话术幻化不属于本版本。
- [x] 实现弹窗打开、关闭、失败提示和主窗口状态同步。
- [x] 将普通声音主输出收口为独立 N/N+1 AAC/M4A 并使用声音 A/B/PortAudio 执行短交接；视频只使用受管 mpv，不保留视频 A/B 或 MSE。插话叠加和固定话术朗读保持独立；实时话术变体不纳入验收。
- [x] Windows release 的 FFmpeg/FFprobe 已统一通过 `CREATE_NO_WINDOW` 后台启动且保留错误传播；普通声音周期候选使用 `max(3, playback_speed + 1)`、1 秒初始突发的有界 CatchUp 和随准备时间增长的动态覆盖水位，队列容量为 `5000ms + 130–750ms` 提交安全尾部；未覆盖时保持 current 并可重试，`1500ms` 真断粮恢复/回退保持不变。
- [x] 统一高级声音、随机插话和固定话术三个抽屉的宽度、深色中性主题、双行标题/真实状态摘要、卡片分区、正文独立滚动、固定底部主操作、持续可见标签和窄屏单列；删除用户预设增加确认，不扩展既有声音能力边界。
- [x] 将账号/退出、激活有效期和会话提示 fixed 层降回页面层级，确保 Ant Design 抽屉遮罩与面板完整覆盖背景状态；显式固定抽屉头尾、正文滚动和键盘行为，不修改插话配置保存链路。
- [ ] 按 [`麦克风插话与插话文件入口实施方案`](./docs/superpowers/plans/2026-08-30-麦克风插话与插话文件入口实施方案.md) 将现有用户入口“随机插话”改名为“插话文件”，并新增本地麦克风插话。该项不得反向改写上述历史完成事实；在实现、自动测试和真实硬件验收完成前持续标记为“正式需求·待实施”。
- [ ] 验证：几分钟、30 分钟和 1 小时素材均可在同一个独立窗口循环播放；`.mp4`、`.mov`、`.mkv`、`.avi`、`.webm`、`.m4v`、`.ts`、`.m2ts`、`.flv`、`.wmv`、`.3gp` 逐一通过导入、探测并验证目标 WebView 的直放能力或明确错误链路；探测成功后保持 `Ready`，用户点击播放才开始原声；普通声音处理耗时不会造成黑屏、空音轨或重新弹窗。实时话术处理不属于本版本验收；桌面 UI 不出现独立“运行资源”卡片或资源安装/取消/目录选择/清理按钮。补充 `1728×1044` 同视口截图对比，验证 `36px` 顶栏、三栏尺寸/间距、六张真实状态卡、三栏独立滚动、深色启动壳无白闪，以及 DOM/键盘顺序、可见焦点、系统缩放和窄屏降级。
- [ ] 验证主页右栏“普通声音处理”持续显示实际多轨支路摘要与当前周期预设名称：首轮尚未提交时显示明确等待态，单轨显示一套，多轨显示全部本周期预设并自然换行；N+1/N+2 预选不得提前改写当前名称，周期提交成功后才更新。
- [ ] 验证三个声音抽屉在桌面与窄屏下的 `760px/560px/560px` 宽度上限、正文滚动/固定标题与底部操作、持续可见标签、单列降级、真实状态摘要和删除预设确认；确认抽屉内不存在实时话术幻化、speech-to-speech 或已删除的旧报告入口，正式 `AudioEffectParams` 按三态展示。
- [ ] 验证插话文件抽屉打开时账号/退出、激活有效期和会话提示均位于遮罩之后，标题、关闭按钮、正文和底部操作无遮挡；覆盖正文滚动、窄屏单列、Tab 焦点约束、Esc 关闭及关闭后焦点返回触发入口。
- [x] 历史基线已验证全量正式参数卡逐字段覆盖、三态与编辑权限分离，并删除当时无可见消费方的旧闪动死代码；当时主页两个声音编辑区可编辑的口径已由 2026-08-25 最新决策覆盖。
- [ ] 按 SC-37/FR-19e 验证上述 9 项只展示且没有滑块、输入框、步进器或抽屉重复入口；普通声音预设或周期随机化改变任一项后刷新显示，并继续经过既有范围/组合校验、N+1 候选准备与提交、失败回退和运行状态更新。
- [ ] 按当前契约验证参数内容区填满桌面中栏实际可用宽度，不受约 `880px`、`860–900px` 或其他最大宽度限制，宽中栏两侧没有由参数容器上限造成的大面积空白；外层双主栏为“左：普通视频 + 高级视觉 / 右：普通声音”。每栏内部卡网格根据实际可用宽度自动决定列数并自然换行，不限制每排数量；逐步放大与缩小窗口时验证列数同步增加或减少，极窄时外层双主栏按 DOM 顺序降为单列。任何尺寸不出现横向裁剪、固定列数或固定/目标 `270–290px` 卡宽。验证所有普通参数卡固定为统一 `80px` 高度，只显示参数名、当前值/进度和“已接入 / 正式需求待实现 / 待确认”三态标签，不存在底部范围/单位/接入或待实现原因说明句；固定视觉频段权重卡固定为独立 `196px` 高度、跨满所属栏并完整显示三列，不能裁剪。验证范围、默认值和能力状态契约仍参与进度、校验及媒体边界；同时验证每张参数卡或独立参数模块分别从参考图五色色板分配自身颜色，不按普通视频、高级视觉、普通声音大分类固定色，允许五色循环且分配顺序尽量避免视觉相邻项同色，单次挂载期内逐卡映射保持稳定。参数卡与插话预设参数行只静态更新值，逐卡强调色和三态文字保持可见；DOM/CSS 中不存在 A/B 闪烁类、发光伪元素或关键帧。通过渲染计数验证只有实际消费值变化的卡片重新渲染，`4Hz` 原始诊断、播放时钟和其他无关主页状态不会带动全量参数卡。
- [ ] 验证最终效果窗口的原始诊断消息仍可最多 `4Hz` 到达，主页主波形可按该节拍直接绘制且没有丢失插话/普通声音互斥语义；React 诊断摘要最多每秒提交一次，连接状态在诊断停止后按既有新鲜度边界切换，卸载、页面隐藏和通道关闭时无残留定时器。该节流不得降低原始采样率，也不得改变媒体播放时钟或 N/N+1 调度节拍。
- [ ] 验证主参数区同屏持续显示视频周期与声音周期两条状态；两者准确展示各自配置范围、真实变化次数，并以各自计划的目标时间和周期长度计算进度，声音状态标签与真实 `audio processing status` 一致。确认不存在联动模式、共享目标或区间求交，修改任一领域周期不重建另一领域计划；无计划时进度为 `0` 且保持非活动。
- [ ] 验证保存账号密码及清除凭据的成功提示均在 `4` 秒后消失，新操作立即清理旧成功提示，连续操作下旧计时器不清除后续提示；警告/错误保持显示，组件卸载后无残留计时器。
- [ ] 验证随机插话在 WebView 与 PortAudio 两种出口均应用同一完整预设和环境声解析；默认随机池为 p01–p20，p21/p22 只显式参与。逐一覆盖 6 种音频与 11 种视频扩展名，视频至少包含一个有音轨样本、一个无音轨样本及一个损坏/不可解码样本；确认只输出音频，不显示画面、不触发视频处理。PortAudio 单图单任务最多四支路，视频来源先生成静音结束/暂停时钟缓存；WebView 优先播放短期处理缓存，只有音频来源处理失败时才允许原声回退。验证缓存按 generation 在段落结束、失败、停止、切素材或卸载时删除，配置变化不抢停当前段；无音轨与所有其他失败均恢复 duck、保留主轨并显示结构化原因。确认不存在实时话术幻化入口。
- [ ] 验证 `ambient_sound_mix_percent=0` 时不加载环境声第二输入；非零时覆盖“用户未选择 → 随包内置真实环境声”和“用户选择 → 本地素材优先覆盖”两条路径。两种来源都覆盖路径/资源越界、格式不支持、不可读和 FFmpeg 解码失败，失败均保持原声并返回可恢复错误。
- [ ] 验证保存插话文件有效配置只提交一次并进入同一 playback snapshot，当前活动插话不中断、不调用 `StopInterlude`、不改变预设组/音量/duck，下一段按新配置应用固定选择或重新抽样；两种模式都保存并校验 `audio_preset_ids`，目录、字段范围、池为空/重复/非法项、asset scope 和锁损坏必须拒绝保存并保持当前插话，前端展示 Tauri 结构化 `message`。验证保存成功后的全部插话文件输入可在重新登录、关闭重开和软件重启后自动恢复，损坏或失效目录重新校验失败时不伪造成功；同时验证文件触发计数与当前文件内预设切换计数彼此独立。验证只有停止播放、切换素材或窗口卸载主动取消当前任务并有界回收资源。
- [ ] 验证随机插话“触发间隔”只在 UI 以秒显示/输入，接受 `0.5–60s` 且默认回显 `8–13s`，提交后 Rust 快照仍为 `interval_min_ms/interval_max_ms=8000/13000`；验证过渡时间接受 `0`，攻击 `0–1000ms`、释放 `0–3000ms`，`0` 分别立即压低/恢复主轨，默认值仍为 `50/250ms`，前后端边界值和次序校验一致。
- [ ] 在 Windows release 安装包连续循环跨越多个声音 EOF，确认所有 FFmpeg/FFprobe 子进程均不弹终端、PortAudio 不出现“PCM 生产已停止”且声音连续；注入真实 PCM 停产后仍在 `1500ms` 门槛触发一次恢复，恢复失败时回退 WebView 并保留原始错误原因。

### Phase 6：本地统一媒体池增量

- [ ] 以当前播放池编辑 IPC 和 Rust EOF 原子状态机契约验收文件对话框/系统拖放整批替换与追加、单项替换、拖拽和键盘重排、单项删除及清空。自然 EOF 必须由 Rust runtime 的物理观察直接触发后端监督事务，前端不得为受管 mpv 调用 `complete_playback_item`；循环推进和 mpv 恢复必须在同一个 Rust 后端事务内完成。新增候选逐项探测，取消、超限、重复路径或任一失败必须保留旧池且不提交部分成功项。成功编辑停止当前资源并让新池首项进入 `Ready`；空池回到无源状态。
- [ ] 验收 `source_media_pool`、`source_media_index`、`playback_pool_cycle`、`playback_generation`：多项池执行 `A→B→…→A` 并在换源时递增 generation、按新媒体重建绝对时钟；单项池自循环、递增池循环轮次并保持 generation；覆盖 `ended/timeupdate/pause` 的不同触发顺序，重复完成事件不得跳项，内部 `pause` 不得把后端改成 `paused`，且自然结束提交仍须完成 `A→B`。
- [ ] 验收整个会话始终复用单一最终效果窗口和一个活动媒体源，换源时旧媒体/音频资源有界释放；播放池不写入 LocalStore、不跨重启恢复、不上传 Go，也不创建服务端媒体任务或生成版本队列。
- [ ] 验收生产 mpv 对白名单容器/编码的直接播放能力；覆盖损坏文件、FFprobe 超时、取消和原子播放池，并确认导入期间没有 FFmpeg 视频编码进程、`.partial` 或新兼容缓存。
- [ ] 验收 `.mp3`、`.wav`、`.m4a`、`.aac`、`.ogg`、`.flac` 与带 `attached_pic` 的音频：纯音频期间最终效果窗口纯黑、无上一帧、无封面、无 PiP、视频处理为“不适用”，普通声音、插话、固定话术及播放控制可用；视频 → 音频 → 视频连续两轮混排不新建窗口、不跳项。
- [ ] 在最终效果窗口完成 mpv/libplacebo 验收：周期边界不切源、不重启 mpv、不重编 shader；分别验证 D3D11、Vulkan、CPU4 和 Original，并完成 GPU83 逐字段输出证据、帧预算、200 周期和音画同步门禁。
- [ ] 完成 EOF 后端原子恢复真实门禁：真实 1080p 单文件循环至少 `100` 次，真实 1080p 多文件 `A→B→…→A` 自然边界至少 `100` 次；同一 EOF 身份不得重复推进或跳项，正常边界保持单一受管 mpv，满足同进程换源门禁时 PID 不变。每次边界在 `1s` 内回读确认正确源、播放态 `pause=false`、`eof-reached=false`、合法 source-local PTS 和连续两次 PTS 前进；另覆盖用户暂停、显式 seek、声音处理关闭、PortAudio 普通声音开启及故障恢复。保存跨轮 presentation/source PTS、VO/decoder 丢帧、延迟帧、音画漂移、PID 与失败原因，执行 30 分钟长稳。代码和自动化通过只能标记“已实施、待真实媒体验收”；上述证据未完成前不得写无卡顿或音画同步已保证，4K 不在本轮范围。

### Phase 7：稳定性与发布

- [ ] 增加服务端结构化日志、错误码和基础指标。
- [ ] 增加客户端日志轮转、崩溃恢复和版本信息。
- [ ] 增加用户/设备/租约关键操作审计查询。
- [ ] 定义本地/CI 构建、桌面安装包和服务端发布流程。
- [ ] 验证：完成端到端冒烟；构建过程不依赖服务器源码编译；无未使用依赖和无效代码。

## 最终多轮检查

- [ ] 1. 需求覆盖：每个 FR、NFR 和成功标准都已实现、验证或明确延期。
- [ ] 2. 集成检查：Go、React、Rust 的接口、状态和权限没有断点。
- [ ] 3. 正确性检查：登录、记住/清除凭据、自动填充不提交、激活双门禁、禁用、离线、有序播放池原子 CRUD 与顺序循环、单项自循环、视频/普通声音处理开关、插话和固定话术朗读、弹窗预览失败及租约异常均有处理；实时话术幻化不列入本版本验收。
- [ ] 4. 简化检查：没有提前引入微服务、消息队列、视频云转发或完整转码平台；媒体能力通过成熟工具接入。
- [ ] 5. 清理检查：删除未使用代码、依赖、临时文件和调试日志。
- [ ] 6. 安全检查：Token、模型密钥、激活码、设备信息和审计权限均已检查。
- [ ] 7. 性能检查：视频不经 Go API 转发；ASR/TTS/媒体处理和全文件 SHA-256 有资源上限；心跳、查询和租约接口没有无意义高频请求。
- [ ] 8. 验证检查：后端、前端、Rust 客户端和手工桌面流程均有证据。
- [ ] 9. 运维检查：日志、错误码、发布、回滚和客户端版本检查已记录。
- [ ] 10. PRD 收口：更新状态、变更记录和延期事项。

## 待确认事项

- Q-1：已确认 Rust 直接访问供应商；Go 负责 OpenAI-compatible 账号登记/测试、租约、额度和调用摘要，必须先完成直连租约凭证协议。
- Q-2：客户端是否需要后台远程下发播放文件/播放计划；当前默认视频由用户在客户端本地选择。
- Q-3：第一版部署在局域网还是云服务器；该选择会影响文件存储、HTTPS、设备发现和网络策略。

## 变更记录

- 2026-08-30：虚拟摄像头方案统一改为 AkVirtualCamera。首版冻结为 Windows 10/11 DirectShow、`GpAutoLive Camera`、YUY2 `1280×720@30fps`；WGC/D3D11 负责 GPU 捕获、缩放和转换，AkVirtualCamera CPU raw 边界允许一次受控回读并明确 `zero_copy=false`。新增 GPL-3.0、安全、签名、MF 实验和兼容门禁，当前未实施。
- 2026-08-30：新增“ZLMediaKit RTMP GPU 直推”正式需求和唯一实施方案。桌面端直接发布到用户输入的 RTMP/RTMPS 地址，默认音画双轨且可选择单轨；视频直接读取活动源、复用 FFmpeg/libplacebo/GPU83 和 GPU 优先/CPU 回退编码，声音从最终 PCM 总线分流，不捕获窗口、不经过 Go、不接 OBS 或平台账号。本项尚未实施；第一阶段换源重新发布、静态视频快照和固定话术 PCM 缺口必须明确显示。
- 2026-08-30：新增“麦克风插话”正式需求并建立专项实施方案；允许其在桌面本地使用 SpeexDSP 回声消除、降噪、自动增益和非语义 VAD，仅用于说话期间将主媒体及其他话术源数字静音到 `0`。同时确定用户可见入口由“随机插话”改名为“插话文件”。本项尚未实施，不恢复 speech-to-speech、ASR/LLM/TTS、模型租约或实时话术幻化范围。
- 2026-08-27：为降低主页 WebView 峰值与持续 CPU，取消全部媒体参数卡和插话预设参数行的闪烁、光晕、发光阴影与关键帧，保留静态逐卡强调色、当前值和状态文字；原始诊断仍最多 `4Hz`，主波形可直接绘制，React 诊断摘要限制为最多每秒一次，并以稳定组件输入避免无关主页状态使全量参数卡重渲染。2026-08-24/26 的高亮实现完成事实改列为历史，不删除。
- 2026-08-27：取消最终效果窗口 CSS 视频叠加；FFmpeg 自动周期以厂商无关 Vulkan 硬解与 Vulkan/libplacebo GPU 滤镜为主链，并按 NVIDIA NVENC、AMD AMF、Intel QSV 的真实探测结果编码。自动周期只激活已验证 GPU 子集，其他正式参数保留软件滤镜回退；运行状态分别显示实际解码、滤镜、编码，完整 GPU 成功链不得继续标注为“回退”或附带伪降级原因。普通声音仍独立多支路处理后混为一条输出。
- 2026-08-27：删除高级声音抽屉的“应用声音参数”按钮及前端手动声音处理链。普通声音只由自动声音 N/N+1/N+2 调度器生成 AAC/M4A；停止播放释放声音 A/B 槽后，再删除其受管临时文件。

- 2026-08-26：双周期继续使用最终窗口真实绝对媒体时钟；`playing` 不等于位置推进，连续约 `1500ms` 无推进时冻结计划并进入恢复。该时钟直接驱动 N/N+1 临时媒体候选生成、预加载和切换。
- 2026-08-26（设计稿与真实桌面二次对照）：修复单领域参数仍占双泳道半宽的问题，音频、普通视频和高级视觉恢复全宽四列紧凑行；周期范围输入与变化次数同层显示，标题补齐图标和副说明，顶部仅对真实已确认会话显示设备授权状态。空池显示“等待导入/等待媒体”，不再冒充纯音频不适用或 PortAudio PCM 故障；声音关闭或清空池时清除上轮运行预设身份。右栏保留所有真实功能，原型演示数据不进入正式端。
- 2026-08-26（TS 视频与纯音频统一播放，代码完成、实机待验收）：播放池已扩展为最多 100 项的视频/纯音频混排统一媒体池，增加六种纯音频格式、`attached_pic` 忽略规则、纯音频黑屏及视频处理/PiP 不适用语义；TS/M2TS 通过应用缓存快速封装或必要 H.264/AAC 转码，使用 `.partial`、FFprobe 校验和原子提交且不修改源文件。失败/取消保留旧池；Rust/UI 自动化已通过，Windows WebView 与真实媒体样本仍待验收，不标记为已发布。

- 2026-08-26：确认现阶段不开发注册，用户继续由管理员创建；先同步文档，随后实施服务端与桌面端登录安全加固。目标包括 `admin/desktop` audience 隔离、桌面拒绝管理员账号、15 分钟 Access、30 天 Refresh family 重放检测、按 Refresh 幂等注销、防枚举与 PostgreSQL 持久化限流、Argon2id 渐进迁移、桌面不保存密码、Refresh 不进入 WebView，以及主窗口/最终效果窗口 Tauri 权限隔离。注册、MFA、邮箱找回和设备密钥不在本次范围。
- 2026-08-28：修复同一 NAT 多设备偶发登录失败：账号持久失败阈值保持 5，来源地址聚合滥用阈值提升为 50，进程短突发拆分为单账号与来源地址两层；Rust 串行认证与 Keychain 操作，稳定 `device_id` 由 Rust 凭据库持有并迁移旧 WebView 候选，Logout 完成前禁止重登。设备自动绑定明确遍历账号全部有效授权，只要任一授权 `bound_devices < max_devices` 就允许新设备占位；跨账号绑定和禁用设备仍拒绝。
- 2026-08-25 历史口径，已被 2026-08-26 目标覆盖（视频部分）：视频 N+1/N+2 周期曾预选完整快照，到点只保留 latest-wins 待应用值；完整 FFmpeg 重编码仅在首次开启、人工应用、换源或播放轮次边界执行，成功切换后才更新参数与次数。PortAudio/FFmpeg 缓冲和所有权、PiP/Web Audio 生命周期、诊断 4Hz 节拍及 IPC DTO 边界的非视频缓存部分继续有效。
- 2026-08-25（环境声来源最终澄清）：`ambient_sound_mix_percent = 0` 时不加载第二输入；值大于 `0` 时默认解析并验证随包内置真实环境声，用户本地素材可选且优先覆盖。两种来源都必须通过路径、格式、可读性和 FFmpeg 解码验证，失败保持原声并报可恢复错误。
- 2026-08-25（已被“环境声来源最终澄清”覆盖）：上一轮曾要求 `ambient_sound_mix_percent > 0` 时必须由用户选择本地素材，并废止随包素材；该要求现已撤回。其路径/格式/FFmpeg 解码验证、失败保持原声和值为 `0` 时不加载第二输入的要求继续有效。
- 2026-08-25（历史声音设置保存反馈，已于 2026-08-27 删除）：曾在高级声音人工应用成功后提示“保存成功”；当前普通声音只走自动周期候选，不再存在该人工入口。随机插话的独立保存反馈不变。
- 2026-08-25（历史高级声音应用范围，已于 2026-08-27 删除）：曾根据开关在 `audio/both` 之间切换手动处理范围。当前 UI 不再调用两者；声音由独立 AAC/M4A 自动候选负责，视频只请求 `video` 处理。
- 2026-08-25（主页多轨与预设摘要）：主页右栏普通声音处理区常驻显示快照上报的实际多轨支路摘要和当前周期已提交的预设名称；首轮前显示等待态，多轨名称自然换行。展示复用现有支路与预设事实源，不新增前端调度、音频处理或插话状态。
- 2026-08-25（声音参数只读随机化）：MFCC 偏移、MFCC 维度、SNR 浮动、频谱盲区、干湿比、环境声混合、高质量音高、播放速度和共振峰偏移改为只读展示，废止将两个具名分组整体定义为可编辑区的口径；9 项继续参加普通声音预设与周期随机化，变化后刷新展示并进入既有校验、候选准备/提交和运行状态链路。
- 2026-08-25（历史环境声口径，后续由同日“环境声来源最终澄清”收口）：落地视频与全声音预设自动变换时允许未选择用户素材时解析随包环境声；最终声音口径明确为随包内置的真实环境声音频，且必须与用户本地素材一样通过完整验证。22×35 声音预设、WebView 插话短期缓存及失败原声回退继续有效；旧视频完整快照属于 2026-08-25 历史口径，已被 2026-08-26 目标覆盖。实时话术幻化范围不变。
- 2026-08-25：桌面端用户可见品牌统一为 `GpAutoLive`，缩写统一为 `GPAL`；内部产品代码 `autolive`、包名、应用标识符、可执行文件名、环境变量和本地存储键保持不变。
- 2026-08-24（正式参数完整接入）：将 `65–20000Hz` 明确定义为视频空间频段并完成 12 段权重、目标/核心频率、动态均衡、边缘柔化、源帧率锁定、画中画独立时间轴、源片段最小长度、边缘羽化和变换平滑接入；音频补齐自然动态、本地音色 ID、真实环境声第二输入、干湿支路、频谱盲区、MFCC/SNR 处理及 MFCC/SNR/LPC 诊断。当前正式模型中语义已确认字段均有真实执行或只读分析边界，剩余仅为外部模型、外部素材和 OCR 待核的缺失需求；实时话术范围保持不变。
- 2026-08-24（历史逐卡高亮动效完成事实，已由 2026-08-27 主页性能口径覆盖）：参数显示 `value` 变化后曾按该卡自己的颜色执行一次约 `900ms` 高亮，并在 `prefers-reduced-motion: reduce` 下禁用。该实现及验证事实保留；当前已取消全部参数变化闪烁、光晕、发光阴影和关键帧。
- 2026-08-24（历史双周期状态决策，联动部分已由 2026-08-27 覆盖）：主参数区同时显示视频周期与声音周期两个状态模块，各自展示配置范围、真实变化次数和本领域 N+1 进度。原联动模式已取消，当前始终独立推进、分别显示；处理关闭、播放暂停、周期未启用或真实计划缺失时进度为 `0` 且保持非活动。
- 2026-08-24（现行逐卡静态颜色分配；历史动效已由 2026-08-27 主页性能口径覆盖）：每张参数卡或独立参数模块分别从参考图五色色板分配自己的颜色，不再按普通视频、高级视觉、普通声音大分类固定一种颜色；五色可循环复用，分配顺序尽量避免视觉相邻项同色，且单次挂载期内逐卡映射稳定。历史 `290ms/900ms` 高亮事实保留，但当前颜色仅作静态强调。算法状态仍由三态文字表达，颜色不表达算法状态；满宽、自动网格、`80px`/`196px` 高度和无底部说明规则不变，也不扩展实时话术幻化。
- 2026-08-24（当前最新满宽决策）：参数内容区填满桌面中栏实际可用宽度，删除约 `880px`、`860–900px` 及其他最大宽度上限；栏内自动网格随窗口增减列数，外层双主栏与极窄单栏降级保持。普通参数卡固定为 `80px` 高，固定视觉频段权重卡固定为 `196px` 高并跨满所属栏；无底部说明、逐卡静态颜色、三态及正式参数事实规则不变。该决策覆盖此前约 `880px` 居中宽度口径，不扩展实时话术幻化。
- 2026-08-24（现行卡片内容决策；高度由上一条明确）：普通参数卡只显示参数名、当前值/进度和三态标签，删除底部范围、单位、接入情况及待实现原因说明句；固定视觉频段权重卡跨满所属栏且三列内容必须完整、不得裁剪。范围、默认值和能力状态事实仍保留在正式契约、校验与媒体边界中。该决策覆盖此前“说明文字至少 `12px` 且显示状态原因”的卡内展示口径，不扩展实时话术幻化。
- 2026-08-24（现行自动网格布局；旧宽度子句已由第一条覆盖）：全量正式参数预览使用填满中栏可用宽度的外层双主栏，左栏合并普通视频与高级视觉、右栏为普通声音；每个主栏内部卡网格根据实际可用宽度自动决定列数并自然换行，不限制每排卡片数量。极窄时外层双主栏按 DOM 顺序降为单列，全程不设置固定列数或卡宽且不横向裁剪。逐卡静态颜色和三态规则保持不变；参数变化动效已由 2026-08-27 主页性能口径取消。该布局决策覆盖下一条“每栏两卡、整行最多四卡、先固定降栏内单列”口径，不扩展实时话术幻化。
- 2026-08-24（历史宽度口径，已由同日最新满宽决策覆盖）：参数内容区此前以约 `880px` 为桌面目标宽度，并允许在 `860–900px` 内取整后居中；该最大宽度和由此产生的中栏两侧留白不再属于当前验收。
- 2026-08-24（历史卡内说明口径，已由同日最新卡片内容决策覆盖）：普通卡当时保持紧凑且显示至少 `12px` 的范围、接入情况或待实现原因说明；该说明句与内容自适应高度要求不再作为当前验收，参数范围、默认值和能力状态事实本身仍保留。
- 2026-08-24（历史响应式澄清，已由同日最新自动网格决策覆盖）：全量正式参数预览继续使用约 `880px` 居中的外层双主栏，左栏合并普通视频与高级视觉、右栏为普通声音；宽窗下每个主栏内部卡网格一排两张，参数区同一横排最多四张。空间不足时先把栏内卡网格降为单列，参数区进一步变窄时再把外层双主栏按 DOM 顺序降为单列，全程按可用宽度伸缩且不横向裁剪。紧凑高度、说明文字至少 `12px`、固定视觉频段三列、分类色与约 `290ms` 反馈、减少动态效果和三态规则均不变。该澄清覆盖下一条“每卡占满整栏、每横排左右两张”口径，不扩展实时话术幻化。
- 2026-08-24（历史响应式口径，已由同日最新澄清覆盖）：全量正式参数预览继续使用约 `880px` 居中的双主栏，左栏合并普通视频与高级视觉、右栏为普通声音；可用宽度足够时两栏等分伸缩、参数卡填满所属栏且每横排左右两张，窄窗按 DOM 顺序降为单列，任何尺寸不得横向裁剪。卡片继续保持紧凑、说明文字至少 `12px`，固定视觉频段权重三列、分类色随机分配与挂载期稳定、显示值变化约 `290ms` 高亮、减少动态效果和三态标注规则均不变。该决策覆盖下一条固定/目标 `270–290px` 卡宽口径，不扩展实时话术幻化。
- 2026-08-24（历史布局，已由同日最新响应式澄清覆盖）：全量正式参数预览采用约 `880px` 居中的双主栏，左栏合并普通视频与高级视觉、右栏为普通声音；卡片保持紧凑、目标宽度约 `270–290px`、说明文字至少 `12px`，固定视觉频段权重按三列展示。三个大分类从《媒体参数范围与默认值》的参考图五色色板随机且不重复分配颜色并在挂载期稳定；挂载后显示 `value` 实际变化按所属分类色执行一次约 `290ms` 高亮，初始挂载、相同值、仅重排和普通重渲染不触发，减少动态效果时禁用。状态继续明确标注“已接入 / 正式需求待实现 / 待确认”，且不等于编辑权限。该决策覆盖下方同日“三分区同页三栏/删除闪烁”和旧 `720ms` 参数卡反馈口径，不扩展实时话术幻化。
- 2026-08-24：随机插话触发间隔的 UI 单位改为秒，显示/输入范围为 `0.5–60s`、默认 `8–13s`；IPC/Rust 继续使用 `interval_min_ms/interval_max_ms` 和 `500–60000ms` 边界。过渡时间允许 `0`：`ducking_attack_ms` 为 `0–1000ms`、`ducking_release_ms` 为 `0–3000ms`，`0` 分别表示立即压低和立即恢复，默认值保持 `50/250ms`。
- 2026-08-25：播放池范围扩展为文件选择/系统文件拖放整批替换或追加、单项替换、拖拽/键盘重排、删除和清空。新增候选仍须整批探测并原子提交；任何失败保持旧池。成功编辑停止当前资源并重置为非空池首项 `Ready` 或空池无源状态；路径和探测元数据不可手工编辑。本决策覆盖 2026-08-24“不增加追加、重排或删除”的范围限制，不改变最多 100 项、当前进程内存、单窗口/单活动源和非生成队列边界。
- 2026-08-24（历史基线，已由 2026-08-25 播放池 CRUD 范围覆盖）：当前播放范围由单源循环更新为当前进程内最多 `100` 项的本地有序播放池。一次多选按文件对话框返回顺序逐项探测，只有全部成功才原子替换旧池；取消、超限或任一失败保留旧池，不提交部分成功项，也不增加追加、重排或删除。首项 `Ready` 后由用户点击播放，在单一最终效果窗口、一个活动媒体源内执行 `A→B→…→A`，单项池自循环。快照沿用实际字段 `source_media_pool`、`source_media_index`、`playback_pool_cycle`、`playback_generation`；播放池不跨进程持久化、不上传，不创建服务端媒体任务或生成版本队列。历史单源完成记录和历史决策保持原样，新增 Phase 6 作为当前增量验收。
- 2026-08-21：新增服务端商业化与生产就绪专项规划，覆盖管理查询 API、套餐/订单/订阅、微信支付、密码重置投递、激活码独立加密表与设备槽位、可信制品分发、namespace JSON Schema、数据保留/备份恢复/追踪和 CI 签名/Provenance；当前仅写入文档，未修改代码、OpenAPI、迁移或部署。
- 2026-08-22：商业化与生产就绪专项纳入 React 管理页和“自定义角色 + 固定权限点”：多角色权限并集、无用户级覆盖、内建超管保护、服务端即时鉴权和 React 导航/路由/操作/403；当前仍只更新文档，未实施代码、OpenAPI、迁移或部署。
- 2026-08-22：新增分层双 PRD：P0 负责多产品硬隔离、Profile 授权新鲜度、BYOK/短期凭证闭环、固定 OpenAPI 制品与跨仓库 E2E；P1/P2 商业生产 PRD 扩展为 11 阶段，纳入产品席位、产品范围配置/制品、错误报告与反馈。随后已落地 P0 Phase 1 服务端基线，其余阶段仍未实施。
- 2026-08-24（历史参数卡布局；其主页声音编辑口径已由 2026-08-25 最新只读随机化决策覆盖）：当时完成全量参数卡、无手柄分段进度和三态展示；当时两个具名声音配置区可编辑的口径不再属于当前验收。
- 2026-08-24（历史实现事实）：桌面端改为覆盖 Rust 正式类型全部字段的音频/视频/高级三分区参数面板；新增频谱/高频音频扰动、高光/阴影/暗角、色彩空间转换、图像修复、翻转/旋转、帧率/帧内/帧间扰动、波频/通道/空间调制、随机几何与抽象几何脸、同源画中画、切片、局部模糊、边缘填充、高光扰动和异步旋转的实际 FFmpeg 映射。缺少算法语义、第二输入、模型或 OCR 确认的参数保持“正式需求待实现/待确认”状态并由 Rust 明确拒绝非默认提交；当时删除旧参数卡闪动和预设参与闪动死代码。本次新反馈为后续独立契约，不改写该清理事实，也不得复用已删除的旧 token。
- 2026-08-24：参考图 1–5 中可辨识的音频、视频、视觉频段、挂件、切片、检测和画面修复参数全部正式化；统一类型/IPC 为 `MediaEffectParams`、`AudioEffectParams`、`VideoEffectParams`、`AdvancedEffectParams`、`get_default_media_effect_params`、`validate_media_effect_params`。未映射算法按“已有模型待接入/正式需求但缺失”管理，不声明已生效；旧 `research_*` 类型、报告 Worker 和 IPC 已移除且禁止重新引入。实时话术幻化、OBS/RTMP、平台检测规避仍不在范围。
- 2026-08-24（历史插话选择模型，已由 2026-08-25 当前边界覆盖）：建立固定/随机选择、最多四支路、下一段生效和有界清理规则；当前默认池、共享预设、环境声解析、WebView 短期处理缓存及失败原声回退以 2026-08-25 记录为准。

- 2026-08-12：根据用户确认，创建首版 PRD；初始范围为后台用户/设备/激活码/模型号池和 Rust 本地视频展示。
- 2026-08-12：按管理系统与桌面客户端拆分技术架构文档，`系统架构总览.md` 调整为总览索引。
- 2026-08-12：根据参考截图补充桌面端素材库、效果预览、画中画、音视频分析和可见叠加层；明确不做面向平台的检测规避。
- 2026-08-12：阶段性方案曾纳入离线录播变体生成与 RTMP 推流功能，新增 M1–M7 流水线；该推流范围已在下一条变更中撤销。
- 2026-08-12：根据最新范围调整，取消远程直播输出、断线重连和相关进程管理，最终效果改为桌面端独立窗口本地展示。
- 2026-08-12（历史决策，已废止）：曾把部分媒体参数归为独立分析模式；2026-08-24 起这些参数已正式化，但旧报告任务、报告产物和独立 Worker 不恢复。
- 2026-08-12（历史决策，已废止）：曾明确按目标展示时长计算 N；该离线 N 版本方案已被后续“单源单窗口循环播放”决策替代，不属于当前产品链路。
- 2026-08-12（历史决策，部分废止）：视频输出重新编码和 `65Hz–20000Hz` 归属视频参数的判断继续有效；旧媒体阶段、任务创建和报告闭环已废止，频段参数自 2026-08-24 起按正式需求待接入。
- 2026-08-12：根据竞品截图补充音频诊断值/倒计时、视频固定频段独立权重、核心频率及视觉调制参数的数据模型，明确截图实时值不等于产品默认值。
- 2026-08-12：完成模型号池与客户端租约最小开发闭环；密钥暂存 Go 进程内存，真实模型代理、生产持久化和完整用量审计仍延期。
- 2026-08-13：完成客户端/管理端操作级幂等键复用、桌面端会话代际保护，以及开发期 Refresh Token 轮换、401 自动刷新和 Logout；未把生产会话持久化、Keychain 或真实媒体适配器提前伪装成已完成。
- 2026-08-13：桌面端接入受限 Tauri Keychain 命令和成熟 `keyring` 库，完成 Refresh Token 的登录/轮换更新、启动恢复、退出/失效清理；本地任务 checkpoint 改为按用户命名空间读取和写入。PostgreSQL、服务端 Secret Store、跨设备恢复和真实媒体适配器仍延期。
- 2026-08-13：开发期 Go 会话索引改为只保存 Access Token SHA-256 哈希，不在会话记录或索引中保存原始 Access Token；HTTP Server 增加请求头、读取、写入和空闲超时，避免默认无限等待。该批次不等同于生产会话持久化，Refresh Token 仍需迁移到服务端安全存储。
- 2026-08-13：完成任务与 rewrite 模型租约的延迟绑定闭环；创建/start/prepare/transcribe 不占用租约，进入 rewrite 前绑定并在 rewrite 阶段事件再次校验有效期；真实模型代理和用量扣减继续延期。
- 2026-08-13：补齐管理端号池账号禁用闭环；增加幂等禁用接口、活动租约冲突保护、Ant Design 确认操作和 OpenAPI 契约，账号编辑/冷却恢复、审计和生产持久化仍延期。
- 2026-08-13（历史方案）：曾将视频处理、声音处理和实时音频幻化定义为三个独立开关；该实时话术范围已由 2026-08-19 决策移出当前版本。
- 2026-08-17：本地源视频导入契约扩展为 `.mp4`、`.mov`、`.mkv`、`.avi`、`.webm`、`.m4v`、`.ts`、`.m2ts`、`.flv`、`.wmv`、`.3gp`，由文件选择器和 FFprobe 实际探测执行；通用完整文件哈希和自动兼容预览仍待后续实现，只有实际 MP4 记录 MP4 专用哈希；补齐普通声音处理和只读诊断/运行状态区分，实时音频幻化参数不扩展；取消桌面 UI 独立“运行资源”卡片及安装、取消、目录选择、清理资源按钮，后台 FFmpeg/FFprobe 资源准备能力保留。本地声音/视频研究分析不再属于当前产品。
- 2026-08-17（历史实现事实，已由 2026-08-24 正式参数完整接入覆盖）：普通声音处理当时仅映射 21 项 FFmpeg 参数，环境声素材混合、干湿比及高级分析字段仍为 `unavailable`；现已补齐真实第二输入、湿声支路和混音后分析/处理，但仍与实时音频幻化完全独立。
- 2026-08-19：按当前桌面代码重新核对 PRD；本版本确定为单源播放、视频/普通声音处理、插话播放器、固定话术系统朗读和单一最终效果窗口，不开发实时话术幻化，也移除本地声音/视频研究分析的当前产品范围。桌面代码中仍注册的实时话术/研究参数、Worker、IPC 和 UI 标记为历史兼容或后续预留，后续代码清理另立任务。
- 2026-08-20（历史视觉基线；声音入口编辑口径已由 2026-08-25 最新只读随机化决策覆盖）：建立三栏、状态卡和参数卡信息密度；具名声音分组仍可位于主页声音列，但上述 9 项只读展示，不扩大到实时话术或研究能力。
- 2026-08-20：统一高级声音、随机插话和固定话术三个抽屉：目标宽度分别为 `760px/560px/560px` 且不超过 `100vw`，采用深色中性主题、双行标题/真实状态摘要、卡片分区、正文独立滚动和固定底部主操作；补齐持续可见标签、窄屏单列与删除预设确认，不增加实时话术或研究能力。
- 2026-08-21：补充真实输出驱动的轻微视觉反馈契约：视频/声音参数相同值不反馈，声音预设只有真实参与当前输出音轨时反馈；同时明确随机插话在 WebView 与 PortAudio 双出口均可听，PortAudio 路径混入唯一输出流并持续 duck 主轨，补齐目录/文件校验、暂停停止、固定话术抢占、失败回退与结构化状态原因。本版本仍不开发实时话术幻化或研究能力。
- 2026-08-21（历史方案，已由 2026-08-26 登录安全目标覆盖）：曾新增桌面登录/激活记住信息契约，包含激活码凭据；该交互先由 2026-08-24 的账号密码登录和服务端自动设备绑定替代，登录密码持久化又将在本次安全加固中删除。目标态只保存账号偏好并由 Rust 独占 Refresh Token 保持登录；工作台入口仍必须同时通过有效 `desktop` 会话和当前设备服务端授权校验。
- 2026-08-21：修复页面 fixed 会话状态层与声音抽屉的层级职责：账号/退出、激活有效期和会话提示保持在页面层，Ant Design 遮罩与抽屉面板完整位于其上；补充固定头尾、正文独立滚动、窄屏和键盘焦点验收，不改变随机插话保存或播放业务。
- 2026-08-21 历史口径，已由 2026-08-28 插话预设变化周期覆盖：保存仍不调用 `StopInterlude`；随机插话在每个文件开始时抽样一次，长文件在同一活动文件的后续媒体时间边界继续切换预设，文件随机周期与预设变化周期保持独立。
- 2026-08-21（历史参数卡反馈口径，已由 2026-08-24 当前后续决策覆盖）：细化真实输出反馈视觉契约为单次约 `720ms`、继承各参数/预设 box 自身强调色且不统一为绿色，并在 `prefers-reduced-motion: reduce` 下完全禁用；凭据提示生命周期仍保持 `4` 秒自动消失、代际保护与卸载清理，但激活码凭据提示已由 2026-08-24 的账号密码登录方案移除。
- 2026-08-21（历史参数卡反馈口径，已由 2026-08-24 当前后续决策覆盖）：将“普通声音 · 参数状态”和“声音预设池 · 当前选择”区块容器纳入真实输出变化反馈：前者只响应实际输出参数集合变化，后者只响应实际参与预设集合变化；两者沿用约 `720ms`、自身强调色和减少动态效果禁用，相同值/集合、轮询、预热与失败保持静止。
- 2026-08-21：Windows release 的 FFmpeg/FFprobe 统一经 `CREATE_NO_WINDOW` 后台启动且错误继续传播；修复周期声音跨 EOF 断粮，保持 `750ms` Scheduled 提交 readiness，将 current 有界 PCM 水位明确为 `5130ms`，不改变 `1500ms` 真停产检测、一次恢复与失败回退，也不扩展当前产品范围。
- 2026-08-24：修复普通声音 `3–5s` 短周期偶发整轮静音：周期 N+1 不再沿用固定 `750ms`/RealTime 候选，而是使用 `max(3, playback_speed + 1)`、1 秒初始突发的有界 CatchUp，并按“准备已用时间 + 提交安全尾部”动态确认 PCM 覆盖；不足时保留 current 并可重试。用户 MP4 的合法四支路 3 秒周期压力测试为 30/30 就绪；即时插话的固定窗口策略、声音周期范围/默认值和 `1500ms` 真停产恢复边界不变。
- 2026-08-21（历史参数卡反馈口径，已由 2026-08-24 当前后续决策覆盖）：将 Rust `PlaybackSnapshot`/DTO 的 `audio_stream_params`、`audio_stream_variants` 确立为已提交普通声音参数的唯一事实源；运行态按有效混合支路为每字段建立顺序无关的多重集合签名，真实值或支路增删才触发参数卡与区块约 `720ms` 的自身色反馈，等权重排、相同快照、预热、失败和非运行态不触发。Rust 本轮不承载预设 ID/权重，预设参与状态不得由参数支路反推。
> `vo-passes` 暂态遥测与换源门禁（2026-08-31，代码修复及 30 分钟联合长稳完成）：mpv 明确允许 `vo-passes` 的 pass 数量逐帧变化，EOF、VO 重配置、seek、缓存或 shader 应用期间取得的 `fresh/samples` 可能暂不可用于形成完整帧预算证据。Rust 只在非 EOF、非暂停、非 seek、非缓存、无待处理边界且应用状态为 `Active` 时采样；空、缺失或非法样本统一作为“本次遥测不可用”，清除旧 P99、丢帧计数和增量基线但不得单独触发 GPU/CPU4 降级。IPC 硬超时/断管仍立即同后端重建，真实 frame/decoder/mistimed/delayed 计数连续增长仍按既有单向降级。2026-08-31 07:15:56–07:46:04 使用指定 22fps TS、26fps MP4、普通声音、PortAudio 与四个插话 WAV 完成 30 分 08 秒连续后台播放：播放池进入第 7 次循环，D3D11/gpu-next 保持 Active，普通声音累计变化 347 次、插话 161 次，未出现 fallback、IPC 超时、watchdog 或 `vo-passes` 误降级。该实测只覆盖当前机器和本组媒体，不替代 200 周期、故障注入、CPU4/Original 与跨硬件发布门禁。
