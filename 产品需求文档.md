# PRD：本地录播视频处理与最终效果展示平台

> 当前版本范围声明（2026-08-19）：本版本不开发实时话术幻化。speech-to-speech、VAD/ASR/LLM/TTS 话术沿用或改写、实时候选音轨换轨、模型租约直连和对应 UI/验收均不属于本版本；相关内容仅保留为历史方案或后续版本预留。当前声音能力以普通声音处理、插话和固定话术系统朗读为准。

## 文档状态

- 状态：Draft
- 文档模式：Single（按用户要求放在仓库根目录）
- 当前阶段：按 [`长任务开发总计划.md`](./长任务开发总计划.md) 执行，当前为 Phase 4 媒体处理最小闭环实施
- 最后更新：2026-08-20
- PRD 文件：`产品需求文档.md`
- 技术架构总览：[`系统架构总览.md`](./系统架构总览.md)
- 桌面端功能流程图：[`桌面端功能流程图.md`](./桌面端功能流程图.md)
- 管理系统架构：[`管理系统架构.md`](./管理系统架构.md)
- 桌面客户端架构：[`桌面客户端架构.md`](./桌面客户端架构.md)
- 媒体参数契约：[`媒体参数范围与默认值.md`](./媒体参数范围与默认值.md)
- 数据库迁移与密钥存储约束：[`数据库迁移与密钥存储约束.md`](./数据库迁移与密钥存储约束.md)
- 目的：作为首版产品范围、技术边界和后续执行依据

当前实现状态：Go 控制面已提供 Refresh Token 轮换和 Logout；React 管理端已接入 401 自动刷新和退出，Rust/Tauri 桌面端已接入受保护请求自动刷新、手动刷新、退出清理和系统 Keychain Refresh Token 持久化。桌面端启动可从 Keychain 轮换恢复会话，Access Token 只以哈希形式保存；内存模式使用进程内索引，PostgreSQL 模式使用 `auth_sessions` 持久化并支持重启后查询；HTTP Server 已配置请求头、读、写和空闲超时；独立 `cmd/migrate` 已提供版本化 PostgreSQL 迁移入口。Go 已接入第一阶段 PostgreSQL JSONB 快照 repository、users/devices 影子 upsert、迁移版本检查和 AES-256-GCM 服务端 Secret Store；其他领域 repository、跨进程完整一致性和生产部署验收仍未完成。

桌面代码核对结论：当前核心播放、视频/普通声音处理、插话播放器、固定话术系统朗读和单一最终效果窗口与本 PRD 的当前范围一致；实时话术候选、speech-to-speech、模型直连和研究功能均不属于本版本。`desktop/ui/src/App.tsx` 以及 `desktop/src-tauri/src/main.rs` 仍保留并注册部分实时话术/研究参数、Worker、IPC 和界面，它们只能视为待清理的历史兼容或后续预留实现，不能继续作为当前产品能力扩展。

普通声音处理的 1～4 个预设支路仍在 FFmpeg 内等权混成一条输出。周期随机不在切换点临时创建全部支路：N+1 完成预选后立即启动唯一候选 FFmpeg，N+2 只保存参数，任意时刻最多一个 current 和一个 pending FFmpeg 任务。候选失败或超时必须保持当前声音；只有 Rust 确认 commit 成功后，UI 才更新当前预设和周期次数。内部运行快照仍可保存调度所需的 seed/weights 以复现普通声音周期，但当前 UI 不展示这些内部字段，不能把它们包装成研究参数。

## 问题

需要一个轻量后台管理系统和一个 Rust 桌面客户端。管理员能够管理用户、桌面设备、激活码和大模型账号池；用户使用同一套登录信息登录后台和桌面客户端。桌面客户端负责采集基础设备信息、导入一个本地视频、在单一窗口内循环播放，并按开关执行视频处理和普通声音处理；插话与固定话术系统朗读是当前声音能力，实时话术幻化不在本版本开发范围。

当前产品包含“本地单视频循环播放、视频/普通声音处理预览、插话和固定话术系统朗读”链路；本版本不开发实时话术幻化，也不包含本地声音/视频研究分析，不做 RTMP/OBS 推流、直播平台接入或自动发布，也不做离线 N 版口播生成。

## 目标

- G-1：后台和桌面客户端共用一套用户账号与登录接口。
- G-2：管理员能够生成、查看、禁用和核销客户端激活码。
- G-3：客户端激活后能够绑定到用户，并上报基础设备信息。
- G-4：客户端能够从 `.mp4`、`.mov`、`.mkv`、`.avi`、`.webm`、`.m4v`、`.ts`、`.m2ts`、`.flv`、`.wmv`、`.3gp` 中选择一个本地源视频，在独立窗口中持续循环播放，不生成 N 个视频版本。
- G-5：管理员能够维护 OpenAI-compatible 大模型供应商、模型账号、连通性测试结果和使用状态。
- G-6：客户端能够向后台申请单账号、短期、限额、可回收的直连模型租约，并由 Rust 直接调用当前租约对应的模型。
- G-7：保持模块边界清晰，确保视频处理和普通声音处理可以独立开关和独立降级；实时话术幻化本版本不开发，仅保留后续版本边界。
- G-8：桌面端提供素材库、播放控制、画中画预览、合规的音视频效果调整和波形/频谱诊断面板；PortAudio 可用时，实时低频震动线必须来自 `audio_cycle_output` 的混音后 PCM 快照。
- G-9：只播放导入的单个源视频；不计算 N、不生成离线版本，播放结束后在同一窗口从头循环。
- G-10：视频画面处理开关和普通声音处理开关相互独立，用户可以分别启用或关闭；实时话术幻化开关本版本不提供产品入口。
- G-11（后续版本，不纳入当前验收）：实时音频幻化输入文件或受控实时流，按播放轮次和音频片段交给 speech-to-speech 执行 VAD/ASR/LLM/TTS，由大模型决定沿用或改写话术，并在当前视频播放期间替换基础音轨；失败保持上一条可用音轨。
- G-12：本地声音/视频研究分析、实验报告、隐形标记研究、内容相似度分析、媒体鲁棒性实验、随机扰动实验和内容指纹对比不属于当前产品目标；仓库中遗留的相关代码只用于迁移审计。
- G-13（后续版本，不纳入当前验收）：支持本地预览场景下的实时话术幻化，不连接直播平台，不进行实时推流。
- G-14：默认采用连续播放模式，确保单个源视频循环时同一窗口内不中断、不黑屏、不重新弹窗。
- G-15（后续版本，不纳入当前验收）：实时音频幻化不阻塞首项视频播放；变体候选准备好并通过同步门禁后，才替换当前音轨；候选来不及或失败时保持上一条可用音轨。

## 非目标

- NG-1：首版不做口型同步、滤镜美颜、弹幕互动、直播间互动和实时话术幻化；实时话术幻化如后续恢复，也只限本地预览，不扩展为研究分析平台。
- NG-2：首版不做 RTMP/OBS 推流、直播平台接入、断线重连和多平台一键分发。
- NG-3：首版不做复杂的视频云端处理；视频、ASR/TTS 中间产物和合成产物默认在桌面端本地处理。
- NG-4：首版不做微服务、消息队列和 Kubernetes。
- NG-5：不做面向平台审核/检测的规避、自动发布或平台接入；内容相似度分析、媒体鲁棒性实验、隐形标记研究、随机扰动实验和内容指纹对比均不提供。

## 成功标准

- SC-1：管理员创建用户后，用户可以使用同一用户名和密码登录 React 后台与 Rust 客户端。
- SC-2：未激活的客户端不能进入完整使用状态；有效激活码只能按规则绑定设备。
- SC-3：后台能够显示设备在线状态、客户端版本、操作系统、磁盘剩余空间、内存摘要、逻辑 CPU 数和最后心跳时间；GPU 等更细硬件信息不作为首版必采集项。
- SC-4：客户端选择一个 `.mp4`、`.mov`、`.mkv`、`.avi`、`.webm`、`.m4v`、`.ts`、`.m2ts`、`.flv`、`.wmv` 或 `.3gp` 源视频并通过实际媒体探测后，能够弹出一个播放窗口并持续循环同一个源视频，不生成离线版本，不依赖抖音或 RTMP。
- SC-5：客户端断网时，已选择的本地视频仍可继续播放；恢复网络后能够补发状态。
- SC-6：管理员禁用用户或设备后，客户端在下一次鉴权/心跳时停止受保护能力。
- SC-7：模型账号异常、额度耗尽或冷却时，后台不会继续分配该账号；客户端能收到可理解的失败原因。
- SC-8：所有模型密钥不以明文写入数据库、日志或前端构建产物。
- SC-9：客户端可以导入本地视频，在素材库中查看文件名、时长、状态和当前选中项；一个播放会话只选择一个源视频进行循环。
- SC-10：客户端可以对预览视频启用/关闭音频和视频效果，修改参数，随时恢复默认参数，且不覆盖原始文件。
- SC-11：客户端提供独立画中画预览、播放进度/倍速控制、实时波形和频谱等基础分析视图；实时诊断卡同时展示混音 PCM 的低频震动线、RMS、峰值和采样率，并在 PortAudio 无输出或快照过期时明确提示。
- SC-12：音视频效果应用失败、格式不兼容或资源不足时，客户端保留原视频可播放，并给出可操作的错误提示。
- SC-13：视频画面处理开关和普通声音处理开关可以独立打开/关闭，关闭任一开关不会关闭其他功能或停止原视频播放；实时话术幻化开关本版本不提供。
- SC-14（后续版本，不纳入当前验收）：实时音频幻化可以接收本地音频文件或受控实时流，调用 speech-to-speech 完成 VAD/ASR/LLM/TTS，并输出沿用原文或改写后的实时变体音轨；不依赖运行时重复检测。
- SC-15：变体音轨通过时长、同步、播放代际和资源门禁后，才能替换当前播放视频的基础音轨；门禁失败继续上一条可用音轨，不生成新 MP4。
- SC-16：视频播放结束后在同一个窗口从 0 秒重新播放同一个源视频，播放轮次可观测。
- SC-17：生成使用用户拥有或获授权的素材、音色和内容，不提供平台审核/检测规避能力。
- SC-18：普通声音处理面板提供已接入的自然真人模式、音高、输入/输出增益、响度、播放速度、均衡、降噪/环境声、淡入淡出、混响、环境底噪、采样率和输出码率；波形、频谱和响度属于只读诊断，不作为处理参数。
- SC-19：声音/视频研究分析面板、研究报告和实验参数不属于当前产品，保留本编号仅用于历史追踪，不纳入验收。
- SC-20：研究哈希、随机种子、算法版本和阶段报告不属于当前播放契约，保留本编号仅用于历史追踪，不纳入验收。
- SC-21：隐形标记、媒体鲁棒性、内容相似度、随机扰动和内容指纹研究不属于当前产品，保留本编号仅用于历史追踪，不纳入验收。
- SC-22：本地预览中按当前音频片段生成并播放话术变体；Rust 通过单账号直连租约调用 OpenAI-compatible 模型，服务异常时回退到原话术或上一条可用音轨。
- SC-23（后续版本，不纳入当前验收）：实时音频幻化的检测、变体生成和换轨任务异步执行，不阻塞源视频首帧和循环播放。
- SC-24（当前验收仅保留普通声音部分）：普通声音处理只作用于普通源音轨，走 FFmpeg 源音频流和 PortAudio，PortAudio 关闭/失败时回退 WebView 源音频；高级 DSP 参数未接入时必须显示不可用。实时话术候选预加载、换轨和 `realtime_variant` 不属于本版本。
- SC-25：变体生成超过当前音频安全余量时保持上一条可用音轨，不暂停视频、不静音等待、不切换未完成候选。
- SC-26：首项通过素材可读性和安全校验后保持 `Ready`；用户点击主页“播放”才打开/聚焦最终效果窗口并以源视频原声开始播放。视频处理和普通声音处理是否生效由各自开关控制；实时话术幻化不属于本版本。
- SC-27（后续版本，不纳入当前验收）：同一个源视频可以连续循环；每轮音频都携带播放轮次和片段上下文交给 speech-to-speech 决策，不把重复的源视频误认为错误；LLM 调用通过当前直连租约完成。
- SC-28：所有带数值的媒体参数均有明确单位、范围和默认值；默认值优先从源视频/音频元数据读取，用户调整值必须经过范围校验。
- SC-29：桌面 UI 不展示独立“运行资源”卡片，也不提供 FFmpeg/FFprobe 的安装、取消、选择目录或清理按钮；FFmpeg/FFprobe 仍由安装包和内部媒体能力准备链路管理，缺失或失败通过导入/处理错误提示呈现。
- SC-30：普通声音处理提供独立的 FFmpeg 参数预览；系统将用户配置的增益/响度、三段 EQ、音高、变速、淡入淡出、轻混响、降噪、相位、颤音、采样率和输出码率交给 FFmpeg 源音频流处理，并在 PortAudio 可用时输出处理 PCM；PortAudio 关闭/失败时回退 WebView 源音频。播放速度按用户配置生效但不参加周期变化；不使用 Web Audio 作为声音效果处理器；不生成普通声音处理 MP4。该链路与实时音频幻化完全独立。
- SC-31：用户可以配置本地插话目录、随机间隔、音量和主音轨 duck 参数；最终效果窗口按配置播放插话，插话不可用时保持主音轨并显示原因。
- SC-32：固定话术使用本机 WebView2 `localService` 系统声音朗读，朗读期间临时静音原视频声音并暂停插话，结束/失败/取消后恢复原状态；不依赖在线语音或语音克隆模型。
- SC-33：PortAudio Host API 选择器始终显示 `ASIO`。只有运行时真实枚举到 ASIO 输出设备时才允许选择；未枚举到设备时显示明确禁用态，不得隐藏或伪装成可用。默认输出策略仍为系统默认/WASAPI，ASIO 启动失败继续按现有规则回退 WebView。
- SC-34：桌面主界面使用深色高密度三栏工作台；`1728×1044` 同视口下为 `36px` 顶栏、`320px / 1fr / 272px` 三列、`12px` 外边距和栏间距，六张顶部状态卡只显示播放、循环、视频处理、普通声音处理、源规格和最终效果窗口的真实状态。三栏独立滚动，DOM/键盘顺序为素材 → 视频/参数 → 声音/输出，窄屏和系统缩放下仍可完成全部关键操作。
- SC-35：参考图只作为视觉结构依据，不扩展产品范围。当前 UI 不出现实时话术幻化、speech-to-speech、研究 Worker、研究/变体 MP4 产物、内容指纹、OBS、RTMP、`65Hz–20000Hz` 视觉调制、像素/帧率微扰或检测规避指标；冷启动到 React 首次可交互帧保持深色背景，不出现白色闪烁。
- SC-36：高级声音、随机插话和固定话术使用统一的深色中性抽屉结构；桌面目标宽度分别为 `760px`、`560px`、`560px`，且均不超过 `100vw`。三个抽屉具备双行标题、真实状态摘要、卡片分区、独立滚动正文和固定底部主操作；所有表单标签持续可见，窄屏字段单列，删除用户预设必须确认。抽屉重做不改变普通声音、插话和 WebView2 本地系统固定话术的功能边界。

## 关键场景

### 场景 1：用户登录桌面客户端

- 操作者：普通用户
- 触发：输入与后台一致的账号密码
- 结果：客户端获得用户会话；未激活设备进入激活引导，已激活设备进入可用状态

### 场景 2：管理员发放激活码

- 操作者：管理员
- 触发：在后台生成激活码并发送给用户
- 结果：激活码有状态、有效期和绑定关系；核销后不能被其他设备重复使用

### 场景 3：客户端激活设备

- 操作者：普通用户
- 触发：登录后输入激活码
- 结果：后台绑定用户和设备，客户端保存设备凭证并开始上报心跳

### 场景 4：本地单视频循环展示

- 操作者：普通用户
- 触发：在客户端选择一个本地视频并点击开始播放
- 结果：弹出一个独立播放窗口，导入的单个源视频循环播放；窗口支持暂停、继续和停止
- 默认行为：导入探测成功后保持 `Ready`；用户点击“播放”才打开最终效果窗口并使用源视频原声播放，不等待实时音频变体处理。

### 场景 5：桌面端素材预览与效果调整

- 操作者：普通用户
- 触发：导入视频并选中素材
- 结果：主界面按深色高密度三栏组织；左侧素材与播放控制、中间视频参数和六张真实状态卡、右侧普通声音/实际出口/诊断及最终效果窗口入口；可播放、暂停、调整倍速、音量和重置参数。三栏布局只映射参考图的视觉结构，不继承其研究、变声或推流功能

### 场景 6：音频分析与视频分析

- 操作者：普通用户
- 触发：播放或预览视频
- 结果：客户端展示音量/响度、波形、频谱、采样率、分辨率、帧率等诊断信息；分析失败不影响播放

### 场景 7：挂件和版权标识

- 操作者：普通用户
- 触发：启用挂件或叠加层
- 结果：用户可以添加可见的文字、图片、Logo、贴纸或光效，设置数量、位置、透明度和出现区间；效果可预览、关闭和清除

### 场景 8：管理员查看设备信息

- 操作者：管理员
- 触发：打开设备列表或设备详情
- 结果：看到设备基础信息、在线状态、客户端版本、当前播放状态和最近错误

### 场景 9：客户端申请大模型账号

- 操作者：客户端业务模块
- 触发：需要调用大模型
- 结果：客户端申请一个短期模型租约；后台按账号状态、额度、并发和优先级分配资源，并记录使用情况

### 场景 10：弹窗展示最终效果

- 操作者：普通用户
- 触发：当前源视频通过本地预检并开始播放
- 结果：客户端只创建一个独立窗口循环展示源视频；视频处理开关控制画面缓存，普通声音处理开关控制普通音频；插话和固定话术系统朗读在当前窗口内按各自规则工作，实时话术幻化不属于本版本

### 场景 11：历史研究分析链路（非当前产品）

- 说明：该链路已从当前产品范围移除。仓库中仍可能存在历史参数、Worker、IPC 和报告类型，但当前桌面端不以此作为产品入口、验收项或发布能力；是否删除代码另立清理任务。

### 场景 12（后续版本）：独立实时音频幻化预览（当前不开发）

- 操作者：普通用户
- 触发：后续版本用户单独开启“实时幻化”开关，输入本地音轨或受控实时流；当前版本没有此入口
- 结果：后续版本才允许客户端把当前音频片段、播放轮次、原文/转写、上一轮输出和锁定字段交给 speech-to-speech；当前版本不调用 LLM/TTS、不生成候选音轨、不进行实时换轨、不申请对应模型租约

## 发现摘要

- 已检查：当前工作区文件、目录、源码、接口、数据库迁移和测试；仓库存在早期媒体任务/版本实现，当前仅作为历史审计，不能作为当前单源播放入口。
- 已确认的用户范围：Go + React 后台管理系统；Rust 桌面客户端；共用登录；激活码；基础设备信息；大模型号池。
- 已分析参考截图：桌面端需要素材库、播放控制、音视频效果参数、重置、画中画和波形/频谱诊断；研究模式及研究报告不纳入当前桌面产品。
- 新增竞品截图证据（历史参考，当前不纳入）：截图曾展示音色库数量、固定频段权重、波频强度和动态均衡阈值；当前桌面端只保留可接入的声音参数及波形/频谱诊断。
- 当前产品决策：视频只在桌面端本地处理、生成和展示；最终效果通过独立窗口呈现，不接入 RTMP/OBS 或直播平台。
- 新增产品决策：本版本不开发实时音频幻化；speech-to-speech、VAD/ASR/LLM/TTS、候选音轨换轨和模型租约直连仅保留为后续版本参考。本地声音/视频研究分析不纳入当前产品；后台只负责账号、设备、激活码、OpenAI-compatible 模型账号登记/测试、号池租约和调用摘要。
- 新增产品决策（后续版本参考）：A/B 不是对外架构契约；如后续恢复实时音频，可使用双音频槽、单引擎预加载或成熟媒体引擎，只对基础音轨安全替换负责，不以离线视频版本作为前置条件。本版本不实施。
- 新增产品决策：输出视频必须经过视频处理链并重新编码，不使用视频流 `copy`；本地媒体处理仍可使用 FFmpeg 等成熟工具，但不承担 RTMP/OBS 推流。
- 新增产品决策：本地源视频导入白名单统一为 `.mp4`、`.mov`、`.mkv`、`.avi`、`.webm`、`.m4v`、`.ts`、`.m2ts`、`.flv`、`.wmv`、`.3gp`；扩展名只做文件选择过滤，最终以 FFprobe 对实际容器和音视频流的探测结果为准。
- 新增产品决策：普通声音处理只区分用户配置、只读诊断和 `disabled/configured/runtime/unavailable` 生效状态；字段可保存或可校验不等于 DSP 已运行，未接入能力继续报告 `unavailable`。普通声音处理使用源音频流 → FFmpeg → PortAudio，失败回退 WebView，不生成音频 MP4；实时音频幻化参数本次不扩展。
- 新增产品决策：桌面端取消“运行资源”产品展示，不提供独立卡片及 FFmpeg/FFprobe 安装、取消、目录选择、清理按钮；后台资源准备、安装包分发和内部能力探测仍保留。
- 新增产品决策：普通声音处理统一使用 FFmpeg 参数预览；输入/输出增益、响度、三段 EQ/Q 值、音高、变速、淡入淡出、轻混响、降噪、相位、颤音、环境噪声、采样率和输出码率都进入源音频流处理，PortAudio 可用时输出 f32 PCM，失败回退 WebView；不生成音频处理 MP4，播放速度不参加周期调度，Web Audio 只保留只读诊断。本版本不开发实时话术幻化，普通声音链路不与任何实时话术链路耦合。
- 新增文档：参数范围、单位和“自动读取源素材默认值”的规则集中维护在 `媒体参数范围与默认值.md`。
- 技术影响：后台是控制面，桌面端是执行面；视频播放不经过 Go 服务转发。
- 验证面：后续需要 Go 单元/接口测试、React 类型检查与页面冒烟、Rust 单元测试和桌面端手工播放检查。
- 已确认模型调用边界：Go 记录、管理并测试 OpenAI-compatible 模型账号；Rust 申请单账号短期直连租约并直接调用模型。Go 不转发实时话术内容，不向客户端下发完整号池或长期 API Key。
- 已确认播放与音频处理边界：导入探测成功后保持 `Ready`，用户点击播放才打开唯一窗口并以源视频原声开始；普通声音处理只作用于普通源音轨，走 FFmpeg/PortAudio，失败回退 WebView，不生成音频 MP4。普通媒体 Worker 已映射 21 项 FFmpeg 声音参数；Web Audio 不处理声音效果，只提供波形/频谱诊断。其余未接入 DSP/分析器的参数必须显示 `unavailable`。实时话术候选和换轨不属于本版本。
- 当前唯一媒体边界是 `导入探测 → Ready → 用户点击播放打开窗口 → 源视频播放 → 视频处理（可选，校验后立即切换）→ 普通源音频 FFmpeg/PortAudio（可选）或实时变体候选独立换轨 → 单源循环`；导入不默认计算完整文件哈希，研究分析和研究报告不在此链路内。

## 需求

### 功能需求

- FR-1：系统必须提供统一登录接口，React 后台和 Rust 客户端使用同一用户账号体系。
- FR-1a：Access Token 过期后，客户端和管理端必须使用 Refresh Token 单飞刷新并只重试原请求一次；服务端必须轮换 Refresh/Access Token，旧 Refresh Token 不得复用；主动退出只撤销当前会话，失败时清理本地会话状态。
- FR-1b：桌面端不得把 Refresh Token 写入普通本地存储、配置或日志；必须通过受限 Tauri 命令使用系统 Keychain/Credential Manager 保存、轮换、启动恢复和退出删除。钥匙串不可用时应提示用户，不得伪造已持久化状态。
- FR-2：管理员必须能够创建、编辑、禁用和查询用户。
- FR-3：系统必须区分管理员和普通用户权限。
- FR-4：管理员必须能够生成激活码、设置有效期、查看状态和手动作废。
- FR-5：客户端首次使用必须完成登录和激活，激活码必须绑定用户与设备。
- FR-6：客户端必须生成稳定的设备标识，并支持设备重新安装后的重新绑定流程。
- FR-7：客户端必须上报操作系统、客户端版本、硬件概要、磁盘剩余空间、播放状态和最后心跳。
- FR-8：管理员必须能够查看、禁用和解除绑定桌面设备；解除绑定后设备进入待激活状态，重新绑定需要新的激活码。
- FR-9：客户端必须支持从文件或目录导入 `.mp4`、`.mov`、`.mkv`、`.avi`、`.webm`、`.m4v`、`.ts`、`.m2ts`、`.flv`、`.wmv`、`.3gp` 本地源视频；不得只按扩展名信任文件，必须探测实际容器、音视频流、时长和可读性。
- FR-10：客户端必须只创建一个独立播放窗口，支持单个源视频循环、暂停、继续和停止；播放结束回到同一源视频 0 秒，不创建第二个视频版本。
- FR-11：本地播放不依赖后台在线；网络恢复后客户端补发必要的状态事件。
- FR-12：管理员必须能够配置模型供应商、模型名称、账号、优先级、额度和状态。
- FR-13：客户端必须能够申请、续租和释放模型账号租约。
- FR-14：后台必须记录模型租约、调用结果、额度消耗、错误和冷却状态。
- FR-15：模型账号密钥必须只在服务端安全存储；默认不向客户端下发完整号池。
- FR-16：关键操作必须留下审计记录，包括用户、设备、激活码和模型账号变更。
- FR-17：客户端必须提供本地视频素材库，支持导入文件/目录、移除素材、查看名称、时长、格式、大小、状态和播放进度。
- FR-18：客户端必须支持播放、暂停、停止、进度拖动、倍速调整和单源循环播放；不提供多版本顺序播放。
- FR-19：客户端必须提供独立画中画预览窗口，预览窗口关闭或异常不能导致主窗口和心跳服务退出。
- FR-19a：桌面主界面必须实现 `36px` 深色顶栏和三栏工作区；`1728×1044` 视觉基准下三列宽度为 `320px / minmax(0, 1fr) / 272px`，外边距与栏间距均为 `12px`。三栏独立滚动，DOM、Tab 和读屏顺序固定为素材/播放 → 视频/参数 → 普通声音/输出；窄屏按该顺序降级，不得仅靠横向裁切隐藏关键操作。
- FR-19b：左栏素材池不得提供随机队列、多视频顺序播放或版本队列；保留播放、暂停、继续、停止、进度、`0.5–2.0` 倍速、音量和画中画入口。中栏顶部六张状态卡只读取并显示播放、循环、视频处理、普通声音处理、源规格和最终效果窗口的真实状态。右栏提供最终效果窗口、普通声音实际出口与只读诊断、真实处理链状态；插话和固定话术的详细配置通过紧凑入口/抽屉承载。
- FR-19c：高级声音抽屉宽度为 `min(760px, 100vw)`，随机插话和固定话术抽屉宽度为 `min(560px, 100vw)`。三者统一使用深色中性主题、双行标题、真实状态摘要和卡片分区；正文独立滚动，标题区与底部主操作保持固定。表单不得用 placeholder 代替持续可见标签，窄屏字段必须单列。删除用户预设前必须确认，取消后不得修改当前预设、参数或播放状态。高级声音只编辑普通声音 FFmpeg/PortAudio 配置，随机插话只编辑本地目录/间隔/音量/duck，固定话术只使用 `localService` 系统声音；不得加入实时话术幻化、speech-to-speech、研究 Worker 或研究分析入口。
- FR-20：客户端必须支持音频效果开关、视频效果开关和一键恢复默认参数。
- FR-21：普通声音处理必须提供声音处理开关和 FFmpeg 参数预览；已映射的增益/响度、三段 EQ、音高、变速、淡入淡出、轻混响、降噪、相位、颤音、环境噪声和采样率进入 FFmpeg 参数数组并报告实际状态，输出码率只在存在编码输出的媒体处理链中生效。播放速度按用户配置生效但不参加周期变化；不使用 Web Audio 处理声音效果。环境声素材、干湿比、压缩及其他未接入 DSP 的技术参数必须显示 `unavailable`，不得伪造为已接入能力。
- FR-22：当前版本不提供本地声音研究分析参数、研究 Worker 或研究报告；MFCC、SNR、共振峰、频谱扰动、音色库等历史字段不得作为当前 UI 或播放处理能力宣传。
- FR-22a：每个当前声音字段必须标记为用户配置、只读诊断或资源统计；处理状态使用 `disabled`、`configured`、`runtime`、`unavailable` 区分。只读测量值不得作为 DSP 参数提交；仅完成配置保存或校验时不得显示为 `runtime`，未接入 DSP 时必须显示 `unavailable`。
- FR-22b：FFmpeg 声音处理预览必须与实时音频幻化独立；不得启动或读取实时音频幻化的 speech-to-speech、VAD、ASR、LLM、TTS、模型租约、话术上下文或候选音轨状态。UI 展示全部已支持的 FFmpeg 声音参数、处理进度、当前 PortAudio/WebView 出口和 `disabled/configured/processing/runtime/unavailable` 状态。
- FR-22c：客户端支持配置本地插话目录、随机插话间隔、插话音量、主音轨 duck 的深度/攻击/释放，并在最终效果窗口播放；目录无可用音频、播放器未连接、暂停或停止时不得伪造插话成功。
- FR-22d：固定话术只使用 WebView2 `speechSynthesis` 的本地系统声音朗读 1～500 个 Unicode 字符；朗读期间临时静音原视频声音并暂停插话，结束、失败或取消后恢复原状态，不调用 ASR/TTS/语音克隆 Worker，不生成音频或视频文件。
- FR-22e：普通声音与视频随机周期必须提前预选 N+1/N+2 的周期和参数，所有同步、准备和提交请求都携带最终播放窗口的 `playback_generation + loop_index + duration_ms + position_ms + absolute_position_ms`，目标以未回绕视频绝对媒体时间为准，不能只传单轮 `video.currentTime`。只有声音 N+1 可以成为候选，并在预选完成后立即启动 Scheduled FFmpeg 处理；资源上限仍为“一个当前 FFmpeg 任务 + 一个下一周期候选 + 一个 PortAudio 流”。N+2 不得创建 FFmpeg、PCM、PID 或后台任务，也不得常驻创建全部预设音轨。独立模式分别按声音/视频目标变化；联动模式取两个配置区间的交集并共享同一目标，无交集时拒绝启用。后端必须先校验播放代次和声音 revision，再取消或替换已有候选；旧 revision 报错后彻底丢弃旧计划，读取最新快照并用新 candidate ID 重建，禁止重试旧 revision。到点前应扣除环缓实际待播时长和硬件输出延迟，由唯一输出线程执行 30ms 双向交叉淡化；尚未真正到点时保留已预热候选并重试。候选失败、过期或未覆盖当前画面时继续旧轨，不清空环缓、不把可恢复候选错误误报为硬件回退。声音处理在播放期间自动尝试 PortAudio，不提供手动硬件出口开关；WebView 在 PCM 覆盖当前绝对画面时间及动态安全水位前持续出声，满足后自动接管。Scheduled 生产速率按 `playback_speed × 1.1` 保留约 10% PCM 余量，避免严格 1× 在调度抖动或加速播放时耗尽队列；固定候选窗和有界队列仍限制资源。CatchUp 预热和提交共用同一个单调时钟，短暂覆盖不足每 10ms 动态复核并在总计 5 秒内继续预热；超时返回可重试错误并保留 WebView/旧轨，不关闭 PortAudio。PortAudio 只有在硬件 callback、当前 PCM 生产任务、有效音频时间轴和实际 PCM frame 都持续推进时才标记为运行；当前生产者停产时先恢复 WebView，并在保留旧生产者和旧环缓的同时预热新生产者，预热成功后才原子替换，真正的 FFmpeg/硬件失败才关闭 PortAudio 偏好并保持 WebView。HTML 视频自然结束产生的内部 `pause` 不得调用后端暂停或取消候选；只有用户暂停才执行该路径。该候选属于普通声音 DSP，不是实时话术候选。
- FR-22f：普通源同步和 PCM 生产恢复从创建到提交必须持有同一个独立占位；占位期间 N+1 prepare 和无匹配的旧候选 cancel 不得推进全局音轨 token。播放代次、媒体源或声音 revision 变化只表示同步候选已被新状态取代，后端必须返回结构化 `reason_code + retryable`、销毁旧候选并保留当前轨、PortAudio 流和偏好；准备期间被更新的停止、暂停或切换请求抢占所产生的 `audio_mixer_candidate_stale/audio_mixer_start_stale` 同样属于可重试控制竞争，不得关闭 PortAudio 偏好或硬件流。暂停、停止仍可立即取消。前端按最新快照最多立即重试一次，用户主动暂停或停止产生的旧同步取消不得包装成“音频源不可用”或“已回退 WebView”；最新同步成功后只清除已解决的 PortAudio 回退提示，不得清除无关播放错误。声音处理配置实际变化时必须递增 `audio_stream_revision`，重复提交相同配置不得制造新 revision。
- FR-22g：活动 PortAudio 流的普通源同步和 PCM 恢复只能由掌握真实绝对媒体时钟的最终效果窗口发起；主窗口只负责硬件未启动时的创建或重启，不得与最终效果窗口并发替换候选。普通声音处理开启时，音频输入身份固定为源素材音轨；视频处理缓存路径变化和循环次数推进不得触发音频源重建。`preferred_portaudio=true`、硬件仍为 `active` 但 `running=false` 时，最终效果窗口必须先恢复 WebView 出声，并在冷却期内串行执行一次普通同步或生产者恢复，不能停留在“已选择但尚未输出”。
- FR-22h：最终效果窗口的 `timeupdate` 与 `ended` 必须共用单飞循环提交门禁；上一条 `complete_playback_loop` 未完成时不得再次递增本地 `loop_index`。重复播放命令必须按目标状态幂等跳过，Tauri 返回的结构化错误原因必须原样展示，不能被笼统的“更新播放状态失败”替代。
- FR-22i：若物理环缓与旧轨 PCM 均已耗尽，预热完成的新生产者必须允许从静音执行 30ms 淡入并接管，不能永久等待不存在的旧轨淡出样本；旧轨仍可用时继续执行正常双向交叉淡化。
- FR-22j：视频循环完成时，最终效果窗口必须先单飞提交新 `loop_index`，后端确认后再按新循环的绝对媒体时间显式重锚 PortAudio，禁止继续沿用上一轮音频时间轴。实时 PCM 解码每个 FFmpeg 子进程只处理一遍有限输入；成功到达 EOF 后由同一受管解码线程等待旧进程退出并从源文件 0ms 顺序续接，禁止依赖单个 `-stream_loop -1` 进程无限跨越边界。任何时刻仍只允许一个当前 FFmpeg 和一个候选 FFmpeg。
- FR-22k：PortAudio Host API 筛选器必须保留 `ASIO` 可见入口。ASIO 可选性只以 `list_audio_output_devices` 的实时设备枚举为准：存在 `host_api=asio` 的输出设备时启用并允许按设备 ID 选择；不存在时显示“运行时未检测到设备”并禁用。当前随包 PortAudio 运行库未编译 ASIO，不得仅因 UI 有入口就报告 ASIO 已启用，也不得改变系统默认/WASAPI 的默认出口。
- FR-23：当前桌面参数网格只开放已映射的亮度、饱和度、模糊、对比度、色相旋转、锐化、噪点、细节增强、X/Y 像素偏移、动态裁剪和像素级缩放；像素扰动、裁剪边缘平滑度、帧内/帧间微扰、帧率微扰、色域转换、`65Hz–20000Hz` 视觉调制及其他研究/检测规避参数不得出现在当前 UI 或被宣传为已生效。
- FR-24：当前版本不提供本地视频研究分析、视觉调制频段、实验挂件或研究 Worker；历史字段不纳入桌面端当前 UI 和验收。
- FR-25：当前产品内的效果参数必须在 UI 中可见、可关闭、可重置；研究模式、随机种子和实验配置版本不属于当前 UI。
- FR-26：客户端必须提供实时波形、频谱和基础音量/响度展示；分析模块异常时不阻塞播放。诊断数据只能来自当前真实媒体/混音快照，不提供研究频段或检测规避联动指标。
- FR-27：客户端必须支持文字、图片、Logo、贴纸、光效、抽象人脸和随机图形等挂件，支持透明度、大小、位置、画面 X/Y 偏移、层级、切片长度、切片最小长度和切片触发间隔。
- FR-28：客户端必须支持保存和恢复效果参数配置；配置只引用本地素材，不复制或上传原始视频。
- FR-29：当前播放和媒体处理只在明确启用对应哈希链路时计算完整文件 SHA-256；研究阶段哈希、随机种子和报告版本不属于当前产品契约。
- FR-30：隐形标记、内容相似度、媒体鲁棒性、随机扰动和内容指纹研究不提供当前产品入口。
- FR-31（后续版本，不纳入当前验收）：客户端可提供独立的实时音频幻化开关；开关开启后，输入本地音频文件或受控实时流，按播放轮次和片段上下文调用 speech-to-speech 的 VAD/ASR/LLM/TTS，由 LLM 决定沿用或改写，生成实时音频变体并在当前播放视频继续运行时替换基础音轨。本版本不得实现或开放该开关。
- FR-32：当前版本不提供研究分析可复现报告或导出能力；单源循环的播放轮次只属于播放状态，不生成研究版本。
- FR-33：效果预览和处理必须使用成熟媒体库或官方能力，不自行实现音视频编解码、滤镜算法、频谱计算、哈希算法或桌面窗口工具。
- FR-34：处理产物必须写入应用缓存/导出目录，原始视频只读保留；处理失败时可以回退到原始视频播放。
- FR-35：FFmpeg/FFprobe 是安装包和内部媒体执行依赖，不作为桌面 UI 独立功能展示；产品界面不得保留“运行资源”卡片以及安装、取消、选择目录、清理资源按钮，能力缺失、探测失败或处理失败统一通过导入/处理错误提示和可操作原因呈现。
- FR-45：模型凭证、Token 和设备凭证必须使用安全存储，不进入普通日志、截图、前端构建产物或提交记录。
- FR-46：客户端必须提供单源连续循环状态，记录当前源文件、播放轮次、视频处理开关和声音处理开关；不创建离线版本或版本队列。实时幻化字段如在代码中保留，只作为非当前兼容字段。
- FR-47（后续版本，不纳入当前验收）：实时音频幻化必须提供 Worker 状态、当前片段、播放轮次、LLM 决策、候选变体状态、音频延迟、换轨结果和回退原因；不再展示运行时指纹相似度。
- FR-48（后续版本，不纳入当前验收）：实时音频变体必须按“文件/流输入 → VAD/ASR → LLM 沿用或改写 → TTS → 同步门禁 → 基础音轨替换”执行；不能阻塞视频播放。本版本不实现 `realtime_variant` 候选替换。

### 非功能需求

- NFR-1：Go、React 和 Rust 代码在本地开发机或 CI 构建；服务器只部署构建产物，不在服务器上编译源码。
- NFR-2：所有服务端接口使用 HTTPS；客户端凭证和模型密钥不得写入普通日志。
- NFR-3：客户端网络异常、后台短暂不可用或单个模型账号失败时，不能导致本地视频播放器崩溃。
- NFR-4：后台 API 使用版本化路径，例如 `/api/v1`，并以 OpenAPI 作为跨语言契约。
- NFR-5：客户端状态更新应幂等；重复心跳、重复释放租约和重复上报不能破坏数据状态。
- NFR-6：首版采用单体 Go 服务，模块化组织，不引入没有实际需求的分布式组件。
- NFR-7：设备信息遵循最小采集原则，不默认采集 MAC 地址、硬盘序列号、摄像头、麦克风或用户目录文件列表。
- NFR-8：音视频处理任务必须异步执行并显示进度、取消和失败状态，不得阻塞 UI、心跳或播放控制。
- NFR-9：效果参数必须具备版本号和默认值；应用升级后旧配置无法兼容时必须安全回退，不得静默产生错误媒体。
- NFR-10：视频处理不能无提示覆盖原始文件；缓存和临时文件必须可清理并有大小上限。
- NFR-11：本地 AI/媒体 Worker 必须支持 GPU/CPU 资源上限、并发上限、取消、超时、失败恢复和磁盘空间检查。
- NFR-12：LLM、ASR、TTS 和本地媒体处理都必须通过可替换适配边界接入，优先复用成熟工具，不自行实现模型或编解码能力。
- NFR-13：本地媒体 Worker 和弹窗预览必须可观测、可取消、可恢复；Worker 失败不得阻塞登录、主窗口和本地播放。
- NFR-14：研究分析、研究报告、实验随机种子和研究产物不属于当前非功能要求；相关历史代码不能成为当前播放依赖。
- NFR-15：当前产品不提供隐形标记研究和随机扰动实验入口。
- NFR-16（后续版本，不纳入当前验收）：实时话术幻化必须有超时、取消、并发和回退策略，模型或 TTS 失败时不能阻塞现有预览播放。
- NFR-17（后续版本，不纳入当前验收）：连续优先模式的播放线程、实时音频变体线程和 UI 线程必须隔离；话术候选必须有预加载、取消和资源释放策略。
- NFR-18：参数 DTO、领域模型和 UI 配置必须显式携带数值、单位、范围和默认来源（源素材/系统默认/用户调整）；禁止用裸整数表达毫秒、秒、Hz、dB、像素、百分比和数量。
- NFR-19：SHA-256 计算使用整个源文件或产物文件的字节流，不能只计算视频流、音频流或抽样片段；MP4 输入/产物必须额外展示完整 MP4 SHA-256，非 MP4 使用通用产物哈希字段，阶段哈希和产物清单必须可恢复、可审计。
- NFR-20：低可感知实时参数预览必须按全局变换周期平滑过渡，具有变化幅度上限；播放暂停或停止时必须立即停止计时器、清理运行时预览并发布停用状态，恢复播放后重新初始化调度器；取消、切换媒体或关闭声音处理时同样清理；不得因预览调度阻塞播放或改变播放速度。实时话术幻化不属于本版本依赖。

## 假设

- A-1：用户拥有扩展名为 `.mp4`、`.mov`、`.mkv`、`.avi`、`.webm`、`.m4v`、`.ts`、`.m2ts`、`.flv`、`.wmv` 或 `.3gp` 且可被 FFmpeg/FFprobe 正确探测的本地视频；容器在白名单内不代表任意内部编码都必然可解码。
- A-2：用户登录后台和客户端需要网络；已经打开的本地播放窗口可以离线继续播放。
- A-3：一个激活码默认绑定一个用户的一台设备；管理员可以手工解绑和重新激活。
- A-4：首版模型池是服务端统一管理的资源池，不向客户端批量返回全部密钥。
- A-5：后台部署方式可以是局域网或云服务器，但构建动作在本地或 CI 完成。
- A-6：桌面端的编解码、滤镜、音频分析和窗口能力采用成熟第三方库或官方 SDK；具体库在实施阶段按目标平台验证后确定。
- A-7：用户对输入视频、原主播音色/参考音和输出内容拥有必要权利或授权。
- A-8：本地 GPU/CPU 资源能够满足基础播放、视频处理和普通声音预览；本版本不把 speech-to-speech、LLM、ASR 或 TTS 纳入运行时依赖。
- A-9：Go 可管理和测试 OpenAI-compatible 模型账号并签发短期直连租约，但本版本桌面播放不使用实时话术模型租约；相关能力仅为后续版本预留。

## 约束与依赖

- Rust 桌面端建议采用 Tauri 2，Rust 负责系统能力和业务核心，React 负责桌面界面。
- React 管理端与 Tauri 桌面端可以共享接口类型和基础 UI 规范，但不共享与平台强绑定的运行逻辑。
- PostgreSQL 保存业务元数据；模型密钥需要加密存储并限制读取权限。
- 首版视频不上传后台，不把大视频经过 Go API 代理。
- 最终效果由桌面端独立窗口展示；Go 服务只管理授权元数据和状态，不转发视频内容。

## 风险与边界情况

- 激活码泄露：使用一次性、可过期、可作废的激活码，并记录核销设备。
- 设备重装：允许管理员解绑旧设备后重新激活，不自动无限扩展设备数。
- 客户端时钟异常：激活码和租约有效性以服务端时间为准。
- 设备离线：本地播放器继续工作；后台显示最后心跳，不伪造在线状态。
- 模型账号耗尽：标记为 exhausted/cooldown，后台切换其他可用账号并记录原因。
- 客户端直连模型泄密：只返回短期、限额、单账号、可撤销租约凭证，不下发完整号池或长期 API Key；凭证仅驻留 Rust 内存，供应商不支持该能力时禁用直连。
- 视频格式不兼容：文件选择白名单覆盖 `.mp4`、`.mov`、`.mkv`、`.avi`、`.webm`、`.m4v`、`.ts`、`.m2ts`、`.flv`、`.wmv`、`.3gp`，但扩展名与实际容器或内部编码可能不一致；必须先用 FFprobe 探测，损坏文件或不受支持的编码返回明确导入/处理错误且不修改源文件。当前实现不承诺自动转码或兼容预览缓存，直放能力以目标 WebView/媒体引擎实际支持为准。
- 音视频效果资源占用：长视频、高清素材和复杂滤镜可能占用较多 CPU/GPU；首版提供异步处理、取消、进度和缓存上限。
- 配置与原片不一致：效果配置绑定素材格式和算法版本，失效时提示并回退默认参数，不修改原片。
- LLM 话术改写、speech-to-speech 音频质量和候选时长漂移：均属于后续版本风险，本版本不启动相关链路。
- 本地预览资源占用：长视频、高清素材和视频处理可能占用较多 CPU/GPU/磁盘；当前版本提供必要的异步处理、取消、进度和缓存上限。
- 平台合规和版权风险：只使用有权素材和音色，不实现平台审核/检测规避能力。
- 多客户端同时操作：服务端以设备状态和命令版本号做幂等控制，客户端拒绝过期命令。

## 执行规则

- 按阶段顺序推进；如果实现发现范围变化，先更新本 PRD，再修改后续阶段。
- 先建立接口契约和状态模型，再实现 Go、React 与 Rust 客户端。
- 每个阶段都必须包含验证、错误状态、权限检查和清理无用代码/依赖的检查。
- 不擅自扩展到 RTMP/OBS 推流、平台账号自动登录、平台审核/检测规避、口型同步、实时视频边播边合成、多平台分发、离线版本预生成或实时话术幻化；本地单视频循环、视频/普通声音处理、插话、固定话术系统朗读和本地弹窗展示属于当前范围。
- 固定话术播放只朗读用户输入文字：使用 Windows/WebView2 本地系统声音（仅 `localService`），不识别人声、不采集麦克风、不做人声分离、不克隆音色、不生成音频/视频版本、不下载语音模型；朗读期间原视频声音临时静音，结束或取消后恢复原状态。实时话术幻化不属于本版本，不得与固定话术共用当前实现。
- 号池密钥、用户凭证和设备 Token 不进入日志、截图、前端静态资源或提交记录。

## 阶段计划

### Phase 1：接口契约与基础数据模型

- [x] 确认 `产品需求文档.md`、`管理系统架构.md` 与 `桌面客户端架构.md` 中的用户、设备、激活码和号池边界。
- [x] 建立 OpenAPI `/api/v1` 契约草案。
- [x] 定义用户、设备、激活码、模型账号、租约和审计日志数据模型。
- [x] 明确会话 Token、设备 Token 和模型租约的状态转换；Refresh Token 轮换、旧凭证失效和 Logout 已支持 PostgreSQL `auth_sessions` 持久化，用户/设备、号池、租约、用量和审计仍处于控制面快照事实源加同事务影子写入的过渡阶段。
- [x] 验证：OpenAPI 文档可解析；关键状态转换有示例；没有把完整模型密钥返回客户端的接口。

### Phase 2：Go 后端与 React 管理端

- [x] 实现统一登录、用户管理和角色权限。
- [x] 实现设备注册、设备查询、心跳和禁用。
- [x] 实现激活码生成、核销、过期和作废。
- [x] 实现模型供应商、模型账号、额度和状态管理。
- [x] 实现开发期审计日志和敏感字段脱敏，并在 PostgreSQL 模式做同事务影子写入：仅记录动作、操作者、设备、目标上下文和 request_id，不读取请求体。
- [x] 实现 React 用户、设备、激活码和号池页面。
- [x] 验证：API 集成测试覆盖登录、激活、设备禁用和号池分配；React 完成类型检查、构建和主要页面冒烟。

### Phase 3：Rust 桌面客户端

- [x] 实现客户端登录和激活流程；Refresh Token 轮换、退出和 Keychain 恢复已接通，PostgreSQL 模式支持服务端会话重启恢复。
- [x] 实现本地设备标识、基础信息采集和心跳上报；已采集磁盘、内存、逻辑 CPU 和系统版本摘要，网络失败时会按用户/设备保存最新心跳并在下次发送时补发。
- [x] 扩展本地视频文件选择和 FFprobe 实际媒体探测至 `.mp4`、`.mov`、`.mkv`、`.avi`、`.webm`、`.m4v`、`.ts`、`.m2ts`、`.flv`、`.wmv`、`.3gp`；异步源文件 SHA-256、各格式实机直放兼容性和自动预览转码仍待后续验收。
- [x] 实现独立播放窗口和单源循环播放；Tauri 只创建一个 `final-effect` 窗口，视频结束后回到同一源视频的 0 秒，不创建第二个视频版本或窗口。
- [x] 实现暂停、继续、停止和错误恢复的最小播放状态闭环。
- [ ] 实现本地状态保存和离线播放。
- [ ] 实现本地素材库和素材元数据；当前播放状态只维护一个源视频与播放轮次。
- [x] 接入本地 FFmpeg/FFprobe 受控媒体 Worker 最小闭环，实现视频/声音处理开关、参数校验、非破坏性缓存预览、取消/超时/失败回退；基础声音输出已映射增益/响度、三段 EQ、音高、变速、淡入淡出、轻混响、采样率和输出码率；完整高级参数映射和真实安装环境验收仍延期。
- [x] 实现最终效果窗口内的实时波形、频谱分析和基础音视频诊断信息；画中画独立布局仍待补齐。
- [ ] 实现可见挂件/Logo/文字/贴纸/光效叠加层，不实现平台检测规避能力。
- [ ] 实现效果配置保存、恢复；[x] 已实现媒体缓存清理、当前播放缓存保护和原始文件不覆盖，配置持久化仍待补齐。
- [ ] 本地声音/视频研究分析、研究报告和实验算法不再作为当前版本 backlog；遗留 `research_*` 代码的删除或迁移另立代码清理任务。
- [ ] （后续版本，不纳入当前）实现独立实时音频幻化的本地调度契约：文件输入、speech-to-speech Worker 能力探测、沿用/改写决策、同步门禁、候选槽和基础音轨替换。
- [ ] 验证：Rust 单元测试覆盖激活状态、单源播放轮次、视频/普通声音处理开关、参数重置和重试逻辑；实时话术决策不属于本版本验收。桌面端逐一手工验证 `.mp4`、`.mov`、`.mkv`、`.avi`、`.webm`、`.m4v`、`.ts`、`.m2ts`、`.flv`、`.wmv`、`.3gp` 的导入、探测、目标 WebView 直放能力或明确错误链路、画中画、效果开关、重置、循环、断网和重启恢复。

### Phase 4：大模型号池客户端使用

- [x] 完成开发期最小闭环：管理员新增/查询/禁用号池摘要，客户端申请、续租、释放租约，校验用户/设备归属、并发上限、过期回收、幂等和密钥脱敏；禁用仍有活动租约的账号会返回冲突，不中断运行中的模型请求。
- [x] 客户端和管理端对同一次用户操作复用同一 `Idempotency-Key`；参数或租约 ID 变化时换 key，成功/取消后清理；桌面端登录切换与租约操作互斥，已有活动模型租约时必须先释放后才能切换账号，并丢弃旧会话响应。
- [ ] （后续版本，不纳入当前）在真实模型调用前完成实时话术幻化的模型租约绑定接口：服务端校验当前会话设备、租约用途、provider/model、active 状态和有效期；客户端支持幂等绑定和失败重试。
- [ ] 实现模型账号自动健康状态、额度扣减和冷却规则；当前仅支持管理员手动编辑状态/额度/并发配置。
- [ ] 将租约、用量和请求预占从快照影子写入切换为规范化 PostgreSQL 唯一事实源，并补齐生产级供应商状态核对、unknown 最终核销和完整额度扣减；当前已提供受控 `APP_MODEL_READ_SOURCE=normalized` 读源验证，加载时 fail-closed 校验账号/租约/usage/请求预占的一致性，但写入仍由快照事务重建影子表。模型请求超时/取消仍在服务启动及请求前的独立 State 控制面事务内恢复，释放并发槽位但保留每日预占，且即使原请求上下文已取消也先提交状态，同一幂等键不自动重试。
- [x] 完成开发期 Go OpenAI-compatible 模型账号登记/租约/使用记录最小闭环；服务端保留历史代理实现，但当前目标改为账号连通性测试和单账号直连租约。
- [ ] 完成 Rust 客户端直连模型的生产闭环：当前已实现 OpenAI-compatible 请求/响应校验和 `direct_model_chat` IPC，但 Go 供应商凭证委托、可信租约注入、调用摘要上报和失败切换仍待完成；无凭证时必须保持不可用。
- [x] 完成开发期模型用量与错误记录，并提供管理端查询接口；PostgreSQL 模式已做用量/请求预占同事务影子写入，模型规范化读源已完成受控验证，完整审计筛选和账号健康状态仍待后续实现。
- [ ] 验证完整能力：集成测试覆盖 OpenAI-compatible 账号连通性测试、直连租约、账号耗尽、冷却、租约过期、客户端调用摘要、真实供应商响应和密钥脱敏。

### Phase 5：单视频循环与当前声音效果展示

- [x] 实现单个源视频本地预检和最终效果弹窗入口。
- [x] 支持播放、暂停、停止和单源循环播放；视频处理、声音处理两个当前开关独立生效；实时话术幻化不属于本版本。
- [x] 实现弹窗打开、关闭、失败提示和主窗口状态同步。
- [x] 实现普通声音处理的 current/next 滚动预热、单一 PortAudio 输出所有权、30ms 样本域交叉淡化、PortAudio/WebView 回退、插话叠加和固定话术朗读；实时话术变体候选槽仍属于后续版本，不纳入当前验收。
- [x] 统一高级声音、随机插话和固定话术三个抽屉的宽度、深色中性主题、双行标题/真实状态摘要、卡片分区、正文独立滚动、固定底部主操作、持续可见标签和窄屏单列；删除用户预设增加确认，不扩展既有声音能力边界。
- [ ] 验证：几分钟、30 分钟和 1 小时素材均可在同一个独立窗口循环播放；`.mp4`、`.mov`、`.mkv`、`.avi`、`.webm`、`.m4v`、`.ts`、`.m2ts`、`.flv`、`.wmv`、`.3gp` 逐一通过导入、探测并验证目标 WebView 的直放能力或明确错误链路；探测成功后保持 `Ready`，用户点击播放才开始原声；普通声音处理耗时不会造成黑屏、空音轨或重新弹窗。实时话术处理不属于本版本验收；桌面 UI 不出现独立“运行资源”卡片或资源安装/取消/目录选择/清理按钮。补充 `1728×1044` 同视口截图对比，验证 `36px` 顶栏、三栏尺寸/间距、六张真实状态卡、三栏独立滚动、深色启动壳无白闪，以及 DOM/键盘顺序、可见焦点、系统缩放和窄屏降级。
- [ ] 验证三个声音抽屉在桌面与窄屏下的 `760px/560px/560px` 宽度上限、正文滚动/固定标题与底部操作、持续可见标签、单列降级、真实状态摘要和删除预设确认；确认抽屉内不存在实时话术幻化、speech-to-speech 或研究能力入口。

### Phase 7：稳定性与发布

- [ ] 增加服务端结构化日志、错误码和基础指标。
- [ ] 增加客户端日志轮转、崩溃恢复和版本信息。
- [ ] 增加用户/设备/租约关键操作审计查询。
- [ ] 定义本地/CI 构建、桌面安装包和服务端发布流程。
- [ ] 验证：完成端到端冒烟；构建过程不依赖服务器源码编译；无未使用依赖和无效代码。

## 最终多轮检查

- [ ] 1. 需求覆盖：每个 FR、NFR 和成功标准都已实现、验证或明确延期。
- [ ] 2. 集成检查：Go、React、Rust 的接口、状态和权限没有断点。
- [ ] 3. 正确性检查：登录、激活、禁用、离线、单源循环播放、视频/普通声音处理开关、插话和固定话术朗读、弹窗预览失败及租约异常均有处理；实时话术幻化不列入本版本验收。
- [ ] 4. 简化检查：没有提前引入微服务、消息队列、视频云转发或完整转码平台；媒体能力通过成熟工具接入。
- [ ] 5. 清理检查：删除未使用代码、依赖、临时文件和调试日志。
- [ ] 6. 安全检查：Token、模型密钥、激活码、设备信息和审计权限均已检查。
- [ ] 7. 性能检查：视频不经 Go API 转发；ASR/TTS/媒体处理和全文件 SHA-256 有资源上限；心跳、查询和租约接口没有无意义高频请求。
- [ ] 8. 验证检查：后端、前端、Rust 客户端和手工桌面流程均有证据。
- [ ] 9. 运维检查：日志、错误码、发布、回滚和客户端版本检查已记录。
- [ ] 10. PRD 收口：更新状态、变更记录和延期事项。

## 待确认事项

- Q-1：已确认 Rust 直接访问供应商；Go 负责 OpenAI-compatible 账号登记/测试、租约、额度和调用摘要，必须先完成直连租约凭证协议。
- Q-2：客户端是否需要后台远程下发播放文件/播放计划；当前默认视频由用户在客户端本地选择。
- Q-3：第一版部署在局域网还是云服务器；该选择会影响文件存储、HTTPS、设备发现和网络策略。

## 变更记录

- 2026-08-12：根据用户确认，创建首版 PRD；初始范围为后台用户/设备/激活码/模型号池和 Rust 本地视频展示。
- 2026-08-12：按管理系统与桌面客户端拆分技术架构文档，`系统架构总览.md` 调整为总览索引。
- 2026-08-12：根据参考截图补充桌面端素材库、效果预览、画中画、音视频分析和可见叠加层；明确不做面向平台的检测规避。
- 2026-08-12：阶段性方案曾纳入离线录播变体生成与 RTMP 推流功能，新增 M1–M7 流水线；该推流范围已在下一条变更中撤销。
- 2026-08-12：根据最新范围调整，取消远程直播输出、断线重连和相关进程管理，最终效果改为桌面端独立窗口本地展示。
- 2026-08-12（历史决策，已废止）：曾新增本地研究分析模式；该范围已由当前版本移除，遗留代码仅用于迁移审计。
- 2026-08-12（历史决策，已废止）：曾明确按目标展示时长计算 N；该离线 N 版本方案已被后续“单源单窗口循环播放”决策替代，不属于当前产品链路。
- 2026-08-12（历史决策，已废止）：曾明确视频输出必须重新编码、65Hz～20000Hz 归属视频模块、每阶段全文件 SHA-256、任务创建闭环和统一参数范围契约；其中研究参数和研究阶段不属于当前产品。
- 2026-08-12：根据竞品截图补充音频诊断值/倒计时、视频固定频段独立权重、核心频率及视觉调制参数的数据模型，明确截图实时值不等于产品默认值。
- 2026-08-12：完成模型号池与客户端租约最小开发闭环；密钥暂存 Go 进程内存，真实模型代理、生产持久化和完整用量审计仍延期。
- 2026-08-13：完成客户端/管理端操作级幂等键复用、桌面端会话代际保护，以及开发期 Refresh Token 轮换、401 自动刷新和 Logout；未把生产会话持久化、Keychain 或真实媒体适配器提前伪装成已完成。
- 2026-08-13：桌面端接入受限 Tauri Keychain 命令和成熟 `keyring` 库，完成 Refresh Token 的登录/轮换更新、启动恢复、退出/失效清理；本地任务 checkpoint 改为按用户命名空间读取和写入。PostgreSQL、服务端 Secret Store、跨设备恢复和真实媒体适配器仍延期。
- 2026-08-13：开发期 Go 会话索引改为只保存 Access Token SHA-256 哈希，不在会话记录或索引中保存原始 Access Token；HTTP Server 增加请求头、读取、写入和空闲超时，避免默认无限等待。该批次不等同于生产会话持久化，Refresh Token 仍需迁移到服务端安全存储。
- 2026-08-13：完成任务与 rewrite 模型租约的延迟绑定闭环；创建/start/prepare/transcribe 不占用租约，进入 rewrite 前绑定并在 rewrite 阶段事件再次校验有效期；真实模型代理和用量扣减继续延期。
- 2026-08-13：补齐管理端号池账号禁用闭环；增加幂等禁用接口、活动租约冲突保护、Ant Design 确认操作和 OpenAPI 契约，账号编辑/冷却恢复、审计和生产持久化仍延期。
- 2026-08-13（历史方案）：曾将视频处理、声音处理和实时音频幻化定义为三个独立开关；该实时话术范围已由 2026-08-19 决策移出当前版本。
- 2026-08-17：本地源视频导入契约扩展为 `.mp4`、`.mov`、`.mkv`、`.avi`、`.webm`、`.m4v`、`.ts`、`.m2ts`、`.flv`、`.wmv`、`.3gp`，由文件选择器和 FFprobe 实际探测执行；通用完整文件哈希和自动兼容预览仍待后续实现，只有实际 MP4 记录 MP4 专用哈希；补齐普通声音处理和只读诊断/运行状态区分，实时音频幻化参数不扩展；取消桌面 UI 独立“运行资源”卡片及安装、取消、目录选择、清理资源按钮，后台 FFmpeg/FFprobe 资源准备能力保留。本地声音/视频研究分析不再属于当前产品。
- 2026-08-17：普通声音处理改为 FFmpeg-only 参数预览：21 项可映射声音参数进入 FFmpeg 滤镜/编码链，处理完成后安全切换；Web Audio 仅保留只读诊断，播放速度不参加周期调度；环境声素材混合、干湿比、压缩及高级分析字段继续保持 `unavailable`，且与实时音频幻化完全独立。
- 2026-08-19：按当前桌面代码重新核对 PRD；本版本确定为单源播放、视频/普通声音处理、插话播放器、固定话术系统朗读和单一最终效果窗口，不开发实时话术幻化，也移除本地声音/视频研究分析的当前产品范围。桌面代码中仍注册的实时话术/研究参数、Worker、IPC 和 UI 标记为历史兼容或后续预留，后续代码清理另立任务。
- 2026-08-20：按用户提供的参考图确定桌面端深色高密度三栏重做范围；新增 `1728×1044` 视觉基线、六张真实状态卡、三栏滚动/键盘/窄屏规则和基础视频参数可见白名单；中栏使用同尺寸只读卡片展示普通声音参数/能力状态和已选预设以维持信息密度，但不扩大可编辑参数范围。明确只复刻视觉结构，不继承参考图的实时话术、研究/检测规避、内容指纹或 OBS/RTMP 功能。
- 2026-08-20：统一高级声音、随机插话和固定话术三个抽屉：目标宽度分别为 `760px/560px/560px` 且不超过 `100vw`，采用深色中性主题、双行标题/真实状态摘要、卡片分区、正文独立滚动和固定底部主操作；补齐持续可见标签、窄屏单列与删除预设确认，不增加实时话术或研究能力。
