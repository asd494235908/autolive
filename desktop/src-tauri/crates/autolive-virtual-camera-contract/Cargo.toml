[package]
name = "autolive-virtual-camera-contract"
version = "0.1.0"
edition = "2021"
publish = false
license = "MIT"
description = "Fail-closed contract and bounded scheduler for the GpAutoLive AkVirtualCamera output."

[dependencies]
serde = { version = "1.0.229", features = ["derive"] }

[dev-dependencies]
serde_json = "1.0.151"

[lints.rust]
unsafe_code = "forbid"
