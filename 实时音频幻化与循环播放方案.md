# 实时音频幻化与循环播放方案

## 1. 文档目的

本方案定义桌面端“单窗口循环播放 + 独立实时音频幻化”的边界、数据流、延迟目标和降级规则。

本方案只处理本地授权素材的播放、speech-to-speech 话术变体和研究分析，不连接平台、不推流、不上传音视频，也不提供平台检测规避功能。

功能开关边界：

- `video_processing_enabled`：视频画面处理总开关，控制亮度、裁剪、像素、帧率、视觉调制和视频挂件等视频模块能力。
- `audio_processing_enabled`：普通声音处理总开关，控制自然真人模式、随机周期、音高、增益/响度、播放速度、三段均衡、降噪/环境声、淡入淡出、干湿比、混响、底噪、采样率、码率和其他已配置的常规音频参数；研究分析参数另按《媒体参数范围与默认值.md》管理。
- `realtime_audio_variant_enabled`：实时幻化独立开关，控制本方案的“speech-to-speech（VAD/ASR/LLM/TTS）→ 话术决策 → 当前音轨替换”链路。
- 三个开关互不隐式联动；关闭实时幻化不关闭普通声音处理，关闭声音处理不关闭视频处理，关闭视频处理不影响音频链路。
- 所有开关关闭时，仍然可以播放原始视频和原始音轨。

固定音频处理顺序：`源视频基础音轨 → 实时话术幻化（可选）→ 普通声音处理（可选）→ 当前最终音轨输出`。首项必须先以源视频原声播放；实时候选未就绪、失败、过期或同步门禁失败时，继续上一条可用音轨。

当前代码状态：

- 当前首版只载入一个源视频；音频候选可以使用双音频槽、单引擎预加载或成熟媒体引擎的安全切换能力，但不准备下一项视频。
- Rust 已提供按播放代际、轮次和片段提交上下文的受控 Worker 调度入口，支持能力探测、超时、取消、子进程回收和结构化结果校验。
- 当前桌面端已经通过同一窗口的隐藏音频槽和源视频音轨门控实现候选音轨切换；真实 `speech-to-speech serve`、供应商直连租约凭证、浏览器媒体时钟误差和长时间连续性仍需环境验收，不能宣称整条链路已通过实机验收。实时调度使用约 6 秒安全余量、约 5 秒 Worker 超时和约 5 秒片段，候选元素真正触发 `playing` 前继续输出源视频音频；缺少音频采样率/声道元数据时不启动 Worker。
- 本文是后续实现的契约，未配置真实 speech-to-speech、LLM 或 TTS Worker 时不得伪造成功状态。

## 2. 播放器边界

### 2.1 单窗口循环播放

- 只允许一个播放器窗口实例，循环播放不创建第二个视频窗口。
- 当前首版只载入一个源视频；播放结束后回到该视频的 0 秒继续播放。不会为了循环生成 N 个 MP4，也不会在当前播放器主流程计算 N。
- 多视频队列和版本顺序播放不属于当前产品范围，不得覆盖当前单视频循环语义。
- 首项可以是源视频原声，必须立即尝试播放，不等待 LLM、TTS 或变体生成。
- 当前首版由 `loop_playback_enabled` 控制是否持续循环，并记录当前播放轮次；固定循环次数和播放计划不属于当前产品范围。
- 每轮重新进入视频起点或音频分段边界时，实时幻化开关开启才向 speech-to-speech Worker 提交当前上下文；当前桌面端以播放位置向前约 6 秒的安全余量调度约 5 秒片段，由 LLM 决定沿用原文或生成改写，候选准备好后在 `start_at_ms` 边界替换当前视频正在播放的音轨。候选未真正进入 `playing` 前不静音源视频，避免单音频槽加载期间出现空播。该调度是最小纵切片，真实模型耗时和长视频连续性仍需实机验收。
- 变体未准备好时继续当前媒体或保留当前画面/上一条可用音轨，不切换到空媒体源、不暂停等待、不重新创建窗口。
- 只有用户停止、播放计划完成或明确不可恢复失败，播放器才进入停止状态。

### 2.2 视频与音频解耦

视频画面保持当前源媒体连续播放；音频采用候选缓冲/预加载桥接，最终输出顺序固定为“基础音轨 → 实时话术幻化（可选）→ 普通声音处理（可选）”：

```text
视频槽：负责同一源视频的持续循环；视频处理开关开启时使用处理缓存/临时预览结果
音频候选缓冲层：负责基础音轨、变体音轨的预加载和安全边界换入；具体实现不锁定为 A/B 播放引擎
播放代际 + 轮次 + segment_id：拒绝旧异步结果
```

音频变体只能在以下条件同时满足时换入：

1. LLM 决策要求使用变体；
2. 变体音频文件或实时流已就绪；
3. 时长、起始偏移和音画同步误差通过门禁；
4. 候选属于当前播放代际和当前播放轮次。

否则保持上一条可用音轨。换轨失败不得影响视频继续播放。

## 3. 实时音频处理数据流

```text
输入音轨（本地文件 / 受控实时流）
        ↓
音频分帧、VAD 与时间轴切片
        ↓
实时幻化开关是否开启？
  ├─ 否 → 输出当前基础音轨，随后按声音处理开关决定是否执行普通声音处理
  └─ 是
        ↓
构造 speech-to-speech 上下文
（播放轮次、片段文本、上一轮结果、锁定字段、目标时长）
        ↓
speech-to-speech：VAD / ASR / LLM / TTS
        ↓
LLM 决策：沿用原文 or 改写话术
  ├─ 沿用 → 基础音轨输出
  └─ 改写 → 生成实时音频变体
        ↓
时长/同步门禁与候选音频预加载
        ↓
同一播放器窗口替换正在播放视频的基础音轨
        ↓
按声音处理开关对最终当前音轨执行普通声音处理
        ↓
写入本轮决策、候选和处理元数据
```

这里的“实时”表示按片段和播放边界调度，并尽量在安全余量内完成候选音轨；大模型决策不是相似度证明，也不要求维护运行时历史指纹库。内容相似度分析和内容指纹对比仅用于研究报告，不参与播放决策。

## 4. speech-to-speech Worker

### 4.1 输入上下文

支持两类音频输入：

| 输入 | 说明 |
| --- | --- |
| 本地音频文件 | WAV、MP3、AAC，以及 11 种受支持视频容器的内嵌音轨，由媒体库解码成统一 PCM 流 |
| 受控实时流 | 仅限本地播放器或明确授权的输入流；必须有流 ID、采样率、声道和时间戳 |

统一上下文字段：`track_id`、`source_kind`、`playback_generation`、`loop_index`、`segment_id`、`audio_path_or_stream_ref`、`transcript_text`、`previous_variant_text`、`locked_fields`、`locked_field_values`、`target_duration_ms`、`max_chars`、`language`、`rewrite_policy`、`timeout_ms`。`locked_fields` 与 `locked_field_values` 按相同下标对应。

### 4.2 Worker 输出

Worker 返回结构化结果，不把模型日志直接当作播放结果：

| 字段 | 说明 |
| --- | --- |
| `decision` | `keep_original` 或 `rewrite` |
| `text` | 沿用或改写后的最终话术文本 |
| `audio_path_or_stream_ref` | 候选音频文件或受控音频流引用 |
| `audio_sha256` | 候选音频完整 SHA-256；仅用于完整性和阶段追踪 |
| `duration_ms` | 候选音频时长 |
| `sync_offset_ms` | 候选音频相对当前片段的同步偏移 |
| `sample_rate_hz` | 候选音频采样率 |
| `channel_count` | 候选音频声道数 |
| `latency_ms` | 从提交到候选可用的耗时 |
| `model` | STT、LLM、TTS 模型标识和版本 |
| `fallback_reason` | 未生成候选时的原因 |

推荐接入方式：

- 使用 Hugging Face `speech-to-speech` 作为 VAD、ASR、LLM、TTS 的组合 Worker；桌面端通过本地进程、WebSocket 或 loopback 服务调用。
- LLM 只有在 Go 签发的单账号短期直连租约包含供应商委托凭证时，才由 Rust 直接调用 OpenAI-compatible 接口；当前无委托凭证时 Worker 必须报告不可用并回退。Go 负责账号登记、连通性测试、租约、额度和调用摘要，完整 API Key 不进入桌面端。
- TTS 使用 Worker 已支持且经本机能力探测通过的模型；中文素材必须先做中文 ASR/TTS 样片验证。
- Worker 不负责播放器窗口、音频槽切换、连续性门禁和当前轮次状态，这些由 Rust 播放协调器负责。

### 4.3 LLM 约束

LLM 必须接收并遵守：

- 品名、价格、规格、优惠等锁定字段不得删除、改写或编造；
- 只输出结构化的 `keep_original` 或 `rewrite` 结果，不输出无法解析的说明文本；
- 改写文本必须满足 `max_chars` 和 `target_duration_ms`，并保持原语义和卖点；
- Rust 提交前再次检查锁定值存在、字符数不超过 `max_chars`，以及候选音频时长位于目标时长容差内；检查失败不得换入；
- `previous_variant_text` 用于避免连续轮次机械复用，但不作为重复检测结果；
- 生成不确定、字段校验失败或上下文不足时返回 `keep_original`，由播放器继续使用上一条可用音轨。

## 5. 实时变体与安全换轨

实时话术变体仅通过内容级链路生成：

```text
VAD → ASR → LLM 沿用/改写决策 → TTS → 时长对齐 → 音频槽预加载
```

音频信号级参数（音高、共振峰、响度、频谱、混响等）仍属于普通声音处理模块；它们不再作为“重复后自动选择”的实时决策档位。实时音频幻化参数本轮不扩展；speech-to-speech 生成耗时超过当前播放安全余量时，继续上一条可用音轨，不等待、不空播。

## 6. 音频换轨与连续性门禁

- 原音频槽始终保留，变体音频先加载到非激活槽。
- 变体音频必须先完成文件可读性、采样率、声道、时长和哈希校验。
- 默认音画同步误差阈值为 `±120ms`，候选目标时长门禁为 `±3%`；实际阈值进入播放计划并可按素材调整。
- 变体音频覆盖失败的时间段不得静音，上一条可用音轨作为兜底继续输出。
- 只允许当前播放代际的候选提交；旧任务、旧轮次和旧 `segment_id` 的回调直接丢弃。
- 视频自然播放与音频换轨都在当前窗口完成；实时变体只替换当前播放视频的音轨，不生成新 MP4，不通过重新弹窗、不通过暂停后重新加载来“伪造连续”。
- 记录每次决策：输入哈希、播放轮次、片段 ID、LLM 决策、模型版本、预计/实际延迟、是否换入、回退原因和输出哈希。普通声音处理可以生成当前播放所需的音频缓存，但不创建离线话术版本或视频队列。

## 7. 关键延迟目标

| 阶段 | 目标 | 超时处理 |
| --- | --- | --- |
| 分帧与 VAD 调度 | 毫秒级更新 | 暂用上一次稳定结果 |
| ASR 转写 | 由 Worker 和片段长度决定 | 沿用当前基础音轨 |
| LLM 沿用/改写决策 | 由模型和上下文决定 | 沿用原文 |
| TTS 内容变体 | 由模型、片段长度和设备决定 | 回退原声 |
| 音频槽切换 | 在安全边界完成 | 不满足门禁则不切换 |

以上是工程目标，不是对任何第三方模型的保证；真实结果必须写入本地能力探测和运行报告。

## 8. 首版实施顺序

1. 循环播放计划和当前轮次状态，保持单窗口。
2. 建立 `SpeechToSpeechContext`、`SpeechToSpeechResult` 和 Worker 能力探测契约。
3. 接入 Hugging Face `speech-to-speech` 的 VAD/ASR/LLM/TTS 本地 Worker；没有 Worker 时返回真实不可用。
4. 将 Worker 调度入口接入实际片段来源，按片段、播放轮次提交后台生成，不直接阻塞播放器；当前桌面端已按源视频音轨元数据调度约 5 秒片段，真实服务和中文样片仍待环境验收。
5. 加入真实候选音频产物、候选缓冲、时长对齐、同步门禁、超时取消和上一条可用音轨回退；当前状态层门禁已完成，媒体音轨切换待接入。
6. 完成几分钟、30 分钟、1 小时素材和短素材“生成慢于播放”的连续性测试，并验证同一窗口实际无缝换轨。

## 9. 明确不做

- 不接 RTMP、OBS 或任何平台推流。
- 不创建第二个播放器窗口播放下一项。
- 不在运行时做重复检测，也不维护 Chromaprint/landmark 历史库；内容指纹对比只属于研究报告。
- 不把未配置的 speech-to-speech、LLM 或 TTS Worker 标记为可用。
- 不通过静音、空音频或未完成音频伪造连续；同一源视频循环是显式播放策略，不等同于多个视频版本顺序播放。
