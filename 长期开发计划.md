# 本地录播视频处理与桌面展示平台：长期开发计划

> 本文件保留为历史阶段记录；后续新开发统一以根目录 [`长任务开发总计划.md`](./长任务开发总计划.md) 为唯一执行计划。

> 本文件只保留历史记录，不作为当前实现清单；其中旧批次中的 N 版本、变体任务、播放队列和 speech preview 描述均已废止。

> 适用于当前工作区的长期实施计划。所有阶段都必须先完成边界、接口、测试和验收，再进入下一阶段。

## 1. 目标

构建一个由 Go + React 管理系统和 Rust/Tauri 桌面客户端组成的本地媒体处理平台：用户登录并激活桌面端后，选择一个本地录制视频，在同一个窗口内持续循环播放；视频处理、声音处理和实时音频幻化为三个独立开关，实时幻化只在当前播放过程中按需替换音轨。

当前产品只支持单个源视频在同一窗口内循环播放，并提供视频处理、声音处理、实时话术幻化三个独立开关。当前不计算版本数量、不创建离线媒体任务、不生成多个 MP4、不维护视频版本队列。

首版不接入 RTMP/OBS、抖音或其他直播平台，不做平台账号自动登录、弹幕、口型同步和面向平台检测的规避。

## 2. 总体架构

```text
React 管理端
    │ HTTPS / OpenAPI /api/v1
    ▼
Go 模块化单体控制面 ─── PostgreSQL
    │                  └── 模型密钥安全存储
    │ 模型账号测试/直连租约/调用摘要
    ▼
Rust/Tauri 桌面端
    ├── 本地登录、激活、设备信息和心跳
    ├── 本地素材库与单一最终效果窗口
    ├── 单源循环播放与当前音轨替换协调器
    ├── 视频处理 / 声音处理 / 实时音频幻化三个独立模块
    ├── speech-to-speech 实时音频幻化 Worker
    ├── 音频/视频研究分析与参数报告
    └── 本地缓存、阶段产物和完整文件 SHA-256
```

### 2.1 当前首版核心业务规则

- 导入一个源视频后，只创建一个最终效果窗口并从原声开始播放；播放结束后回到同一源视频 0 秒，记录播放轮次。
- 视频处理开关只控制视频画面处理；声音处理开关只控制普通音频处理；实时音频幻化是第三个独立开关。
- 实时幻化开启后执行“文件/受控实时流输入 → speech-to-speech（VAD/ASR/LLM/TTS）→ LLM 沿用/改写决策 → 同步门禁 → 当前基础音轨替换 → 普通声音处理（可选）”；失败或超时保持上一条可用音轨。
- 当前首版不生成离线视频版本、不追加多个视频、不因音频变体重新弹窗。
- 启用视频效果后必须解码、处理并重新编码，不使用视频流 `copy`。
- 65Hz～20000Hz 归属视频模块，表示视觉调制频率；每个固定频段保存独立 `band_weight`，同时展示目标频率、核心频率和实际生效频率。
- MP4 必须对整个文件字节流计算 SHA-256；其他阶段产物也计算完整字节流 SHA-256，但不得伪装成 MP4 哈希。
- 内容相似度分析、媒体鲁棒性实验、隐形标记研究、随机扰动实验和内容指纹对比只在本地研究模式运行，不连接平台。

### 2.2 当前连续播放规则

- 首个源视频通过可读性校验后立即原声播放。
- 播放结束后在同一窗口回到源视频 0 秒，并递增播放轮次。
- 实时话术幻化只替换当前基础音轨，普通声音处理作用于最终当前音轨；候选生成失败或来不及时，继续当前视频和上一条可用音轨。

## 3. 全局开发约束

- 默认使用简体中文沟通、文档和用户界面文案。
- Go、React、Rust 优先使用成熟第三方库或官方 SDK，不重新实现编解码、滤镜、频谱、MFCC、哈希、窗口和通用 UI 工具。
- React 管理端和 Tauri React 界面统一使用 Ant Design；能用组件库解决的功能直接使用组件，不修改 `.ant-*` 内部样式。
- Go 是控制面，Rust 是本地执行面；本地视频不经过 Go API 转发。
- Go 只保存模型 API Key 的安全引用/密文，负责 OpenAI-compatible 账号登记、连通性测试、租约和调用摘要；Rust 只获取当前单账号短期直连租约，不获取完整号池或长期 API Key。
- 所有本地 Worker、模型请求、心跳、事件和释放操作必须定义超时、取消、幂等、重试和资源回收策略。
- 本地媒体处理和研究 Worker 按阶段保存输入、输出、状态、日志、参数版本和哈希，失败可从失败阶段重跑，不静默覆盖源文件或已验收产物。
- 播放、生成、分析、UI 和心跳线程分离；缓冲不足时不能切换空媒体源。
- 参数必须带单位、范围、默认值来源和启用状态；禁止使用裸整数混淆毫秒、秒、Hz、dB、像素、百分比和数量。
- 构建只在本地开发机或 CI 执行，服务器只接收发布产物；不使用 Git worktree。
- 当前工作区存在 Go、React、Rust/Tauri 源码、接口契约、数据库迁移和测试；早期媒体任务/版本代码保留作历史审计，本阶段不得将其扩展为当前入口。

## 4. 长期阶段计划

### Phase 0：基线、工具链和接口契约

目标：让三端有统一的目录、错误格式、状态枚举和 OpenAPI 契约。

主要文件：

- `接口契约/openapi.yaml`
- `接口契约/错误码.md`
- [`接口契约/任务状态.md`](./接口契约/任务状态.md)：播放状态与实时音频状态契约
- `接口契约/共享类型.md`
- `工具/检查文档引用.sh`
- `工具/检查参数契约.sh`

交付内容：

- `/api/v1` 统一错误格式、分页、请求 ID 和幂等键约定。
- 登录、刷新、激活、心跳、模型租约、模型账号测试和直连调用摘要接口。
- 播放状态：`ready → loading → playing/paused/stopped/error`。
- 实时音频状态：`disabled → idle → capturing → processing → candidate_ready → switched/fallback`。
- OpenAPI 可被 Go、React、Rust 的生成或类型约束流程消费。

验收：OpenAPI 文档可解析；错误码和状态机有示例；接口不存在完整模型密钥和大视频上传字段。

### Phase 1：Go 后端控制面基础

目标：启动一个可测试的模块化 Go 单体服务。

目录：

```text
backend/
  go.mod
  cmd/server/main.go
  internal/
    auth/
    users/
    devices/
    activation_codes/
    model_pool/
    audit_logs/
    httpapi/
    storage/
    config/
```

实施顺序：

1. 建立 Go module、配置加载、结构化日志、HTTP server 和优雅关闭。
2. 实现 `/api/v1/health`，包含服务状态、版本和请求 ID。
3. 实现统一错误响应、输入校验和鉴权中间件。
4. 实现用户登录、Refresh Token、角色权限和禁用状态。
5. 实现设备注册、激活码核销、心跳和设备禁用。
6. 实现模型供应商、账号池、租约和用量记录。
7. 接入 PostgreSQL repository；所有迁移可审查，敏感字段加密或哈希。

验收：Go 单元测试和 HTTP 集成测试覆盖登录、激活、设备禁用、模型租约、OpenAI-compatible 账号测试、直连调用摘要幂等和租约过期。

### Phase 2：React 管理系统

目标：提供最小可运营后台，不处理视频文件。

页面：

- `/login`
- `/dashboard`
- `/users`
- `/devices`
- `/activation-codes`
- `/model-pool`
- `/audit-logs`

实施顺序：

1. 建立 React + TypeScript + Ant Design + React Query 工程。
2. 接入 OpenAPI 类型和统一 API client。
3. 实现登录、刷新、退出和权限路由。
4. 实现用户、设备、激活码和模型号池页面。
5. 为禁用、作废和释放租约等操作增加确认弹窗、成功提示和错误状态。

验收：`pnpm typecheck`、`pnpm build` 和页面冒烟通过；每个列表页面覆盖 loading、empty、error、无权限和重试状态。

### Phase 3：Rust/Tauri 桌面客户端骨架

目标：建立单窗口桌面端和安全 IPC，不实现完整媒体算法。

目录：

```text
desktop/
  src-tauri/
    src/
      auth/
      activation/
      device/
      playback/
      media_library/
      research/
      model_client/
      local_store/
      commands/
      errors/
      main.rs
  ui/
    login/
    activation/
    library/
    player/
    research/
    final-preview/
```

实施顺序：

1. 建立 Tauri 2 + React 工程和单窗口生命周期。
2. 定义 Rust/React IPC 命令和结构化错误。
3. 实现登录、激活、设备信息和心跳状态机。
4. 实现本地文件/目录选择、媒体元数据探测和单源播放状态。
5. 实现 `PlayerWindow` 单实例和播放状态机。
6. 实现原声立即播放、单源循环、暂停、继续、停止和当前音轨安全替换的最小可测试流程。
7. 当前首版不创建媒体任务；桌面端只调用登录、激活、心跳、模型租约和直连调用摘要接口，模型请求本体直达供应商。

验收：能选择标准 MP4，在同一个窗口播放、暂停、停止和从 0 秒循环；三个处理开关独立生效，实时音频变体失败时保持上一条可用音轨；断网不影响已打开的本地播放；IPC 不暴露任意命令执行和任意文件访问。

### Phase 4：本地媒体基础能力

目标：确定目标平台可用的播放、探测、转码和滤镜引擎。

实施顺序：

1. 评估并固定播放内核：HTML5 Video、libmpv、GStreamer 或其他成熟方案。
2. 评估并固定本地媒体处理 Worker；当前环境未检测到 FFmpeg 命令，必须把依赖安装/打包/能力探测写入方案。
3. 实现媒体元数据探测、容器可读性校验、音视频流探测和完整文件 SHA-256。
4. 实现非破坏性预览缓存和导出目录；源文件只读。
5. 实现基础亮度、对比度、饱和度、色相、锐化、模糊、裁剪、缩放和色域处理。
6. 所有启用视频效果的输出强制重新编码，保留编码参数和处理链版本。

验收：标准 MP4 可探测、预览、导出；原片不被覆盖；媒体处理失败时回退原片；MP4 哈希对整个文件计算。

### Phase 5：声音模块和视频模块参数面板

目标：将竞品截图中的字段落成明确的配置/测量/状态模型。

声音模块：

- 自然真人模式、随机变声周期、下一次变声倒计时。
- 音高微移、频谱扰动、环境底噪、MFCC 偏移、相位扰动、滤波 Q 值。
- SNR 浮动与当前 SNR 分离。
- 共振峰偏移与当前共振峰分离。
- 音色库 ID 与可用音色数量分离。
- 实时波形、频谱、响度和音频状态。

视频模块：

- 亮度、饱和度、模糊、对比度、色相、锐化、噪点、细节增强。
- 帧率微扰频率与幅度分离。
- 像素级缩放、像素/帧内/帧间扰动、动态裁剪、空间偏移、色域转换强度。
- 65Hz～20000Hz 固定频段及 `band_weight` 独立权重。
- 目标频率、核心频率、波频强度、波频电平、波频颗粒数、动态均衡阈值。
- 抽象人脸、随机图形、切片长度、最小长度和触发间隔。

验收：所有参数有单位、范围、默认值来源和启用状态；测量值为只读；参数可重置；参数越界在 UI 和 Rust 侧均被拒绝。

### Phase 6：实时音频幻化与单源连续播放

目标：单个源视频在同一个窗口内持续循环；实时幻化独立处理音频，并在安全边界替换当前播放音轨。当前不生成其他视频、不创建版本队列。

流程：

```text
源视频 + 原声立即播放
    ↓
（实时幻化开关开启）读取输入音轨：文件 / 受控实时流
    ↓
speech-to-speech：VAD / ASR / LLM / TTS
    ↓
LLM 决定沿用原文或生成改写话术
    ↓
沿用当前基础音轨；改写结果进入时长对齐和播放器候选缓冲
    ↓
当前播放视频继续运行，在安全边界替换当前基础音轨；失败保持上一条可用音轨
```

实施顺序：

1. 建立 `ContinuousPlaybackCoordinator` 单源循环状态机和播放轮次。
2. 建立 `SpeechToSpeechContext`、`SpeechToSpeechResult` 和 Worker 能力探测契约。
3. 接入 Hugging Face `speech-to-speech` 的 VAD/ASR/LLM/TTS 本地 Worker；未配置时返回真实不可用。
4. 实现 LLM 沿用/改写决策、锁定字段校验、时长约束和播放器候选缓冲/安全换轨；底层不锁定 A/B 播放引擎。
5. 实现超时、取消、失败、过期候选和上一条可用音轨回退，不阻塞视频循环。
6. 验证几分钟、30 分钟和 1 小时素材在同一个窗口内持续循环，验证三个开关互不联动。

验收：不重新创建窗口、不黑屏、不切换空媒体源；实时变体生成速度不足时继续当前视频和上一条可用音轨，并显示 Worker/候选/回退状态。

实时音频幻化补充边界：播放器循环必须显式记录播放轮次；每次输入音轨（本地文件或受控实时流）在实时幻化开启时交给 speech-to-speech Worker，由 LLM 基于当前轮次、片段文本、上一轮结果和锁定字段决定沿用或改写。改写候选完成时长对齐和安全门禁后才允许换轨；Worker、LLM、TTS 或换轨失败时保持原声，不能暂停、静音、切换空媒体源或新建窗口。运行时不做 Chromaprint/landmark 重复检测；内容指纹对比仅用于研究报告。当前已落地候选契约、锁定字段/时长校验和候选槽状态机，真实 Worker 与媒体换轨仍待后续阶段，详见[实时音频幻化与循环播放方案](./实时音频幻化与循环播放方案.md)。

### Phase 7：研究分析、哈希和实验报告

目标：建立可复现的本地研究分析能力。

- 内容相似度分析：比较源视频与当前处理缓存/音轨的音频、视频特征；当前单源循环允许重复同一源 MP4。
- 媒体鲁棒性实验：对授权素材进行压缩、缩放、裁剪、重采样等实验并记录结果。
- 隐形标记研究：本地嵌入、检测、提取和鲁棒性报告，默认关闭。
- 随机扰动实验：参数、算法版本和随机种子可复现。
- 内容指纹对比：原始/当前产物间指纹对比，不连接平台。
- 报告记录源文件、阶段产物和实验产物的完整字节流 SHA-256。

验收：同一输入、参数和随机种子可以复现实验结果；报告明确区分配置值、测量值、目标频率和实际生效频率。

### Phase 8：可见挂件与最终效果展示

目标：完成截图中可见的桌面工作台和单窗口最终展示。当前只展示一个源视频循环和当前音轨状态。

- 素材库、导入视频、当前源视频、播放控制和播放轮次状态。
- 音频参数卡片、下一次变声倒计时、波形和频谱。
- 视频参数卡片、频段权重表和处理状态。
- 文字、图片、Logo、贴纸、光效、抽象人脸和随机图形。
- 研究模式显式标记；研究产物与普通预览分目录。
- 最终效果窗口单实例、单源循环、当前音轨状态和三个独立处理开关。

验收：普通预览和研究预览入口清晰区分；关闭效果或窗口不会导致主界面、登录或心跳退出。

### Phase 9：稳定性、性能、打包和发布

目标：形成可重复的本地开发、测试、打包和发布流程。

- Go：单元、集成、竞态检测、数据库迁移和 OpenAPI 校验。
- React：类型检查、Lint、构建、页面冒烟和可访问性检查。
- Rust：格式化、Clippy、单元测试、IPC 测试、实时 Worker 取消和资源释放测试。
- 长视频 CPU/GPU/内存/磁盘监控、并发上限、缓存清理和本地 Worker 取消。
- 客户端崩溃恢复、重启恢复、断网恢复和后台禁用处理。
- 本地或 CI 构建 Go、React 和 Rust；服务器只部署产物。
- 发布前删除未使用代码、依赖、导入、类型、配置、资源和调试日志。

验收：完成一条当前首版流程：创建用户 → 登录客户端 → 激活设备 → 导入单个素材 → 原声立即播放 → 三个独立开关 → 同一窗口循环 → 实时音频变体失败回退。

## 5. 当前实施状态与批次记录

以下批次记录保留历史审计事实，不代表当前产品功能范围；其中涉及多视频队列、变体任务、N 版生成和双视频槽的内容均已废止。当前批次按“先打通三端最小可联调骨架，再进入媒体能力”的顺序实施，暂不宣称完整业务已完成：

- [x] Phase 0：OpenAPI、错误码、任务状态、共享类型和文档检查脚本已建立。
- [x] Phase 1 基础：Go 配置、结构化日志、健康检查、请求 ID、统一错误响应、开发阶段环境变量认证桩和优雅关闭已建立。
- [x] Phase 2 骨架：React + TypeScript + Ant Design + React Query 管理端导航、登录请求壳和健康状态页已建立。
- [x] Phase 3 核心骨架：Rust 播放状态机、单窗口绑定、单源循环和首项原声策略已建立；旧版本唯一校验不属于当前产品。
- [x] Phase 1 控制面第二批：历史实现曾包含用户/设备/激活码/媒体任务闭环；该媒体任务入口已从当前产品契约移除，不作为新功能基础。
- [x] Phase 1 凭证补强：创建用户时只保存 bcrypt 密码哈希，已创建的普通用户可以使用同一控制面登录；Refresh Token 仍是后续工作。
- [x] Phase 2 管理端第二批：历史实现曾包含用户、设备、激活码和媒体任务列表；当前管理端只保留用户、设备、激活码和模型号池相关入口。
- [x] Phase 3 本地基础第二批：Rust 本地 MP4 探测、完整文件 SHA-256、取消令牌和可序列化媒体 DTO 已建立。
- [x] Phase 3 Tauri IPC 第三批：单窗口 Tauri 2 壳、Ant Design 最小素材页、MP4 选择、受限探测命令和 MP4 整文件 SHA-256 命令已接通；仅主窗口可调用，非 MP4 哈希在 Rust 侧拒绝。
- [x] Phase 3 播放状态第四批：Tauri 单窗口已接通 `start/pause/resume/stop/advance/get_snapshot`，复用 `PlaybackCore/PlaybackQueue`，UI 明确显示媒体内核尚未接入。
- [x] Phase 3 真实预览第五批：通过 Tauri asset protocol 与 `convertFileSrc` 在同一窗口加载探测成功的 MP4；静态 asset scope 不开放整个用户目录，浏览器模式和媒体解码失败均显示真实错误。
- [x] Phase 1 客户端状态第四批：已接通 `client/profile` 和 `client/heartbeat`，支持设备归属、禁用校验、心跳幂等与设备状态更新时间。
- [x] Phase 3 客户端登录激活第六批：Tauri 桌面端已接通登录、激活码核销、Profile 刷新和一次性 Heartbeat；Access Token 只保存在 React 运行时内存，控制面 HTTP 通过官方插件并限制到本地开发地址；心跳磁盘空间由 Rust 使用 `sysinfo` 读取，读取失败不发送伪造心跳。
- [x] Phase 3 运行时遥测第二十七批：Rust 使用 `sysinfo` 和标准库采集磁盘可用空间、总/可用内存、逻辑 CPU 数、系统名、系统版本和内核版本；Go Heartbeat 校验并保存这些字段，Profile/Admin 设备列表可展示；缺失的系统可选字段不阻断心跳，内存可用量超过总量会被拒绝。
- [x] 历史实施记录：早期曾增加 `set_playback_queue`、多素材队列和离线版本任务创建；这些能力已废止并仅保留审计记录，不属于当前单视频循环流程。
- [x] Phase 3 本地任务第十批：已接通 `create → start → 本地 prepare → events` 最小闭环；Rust 只读校验源 MP4、记录完整文件 SHA-256、生成幂等 `prepare.json` 和符合控制面契约的产物摘要，React 上报 `prepare running/succeeded/failed` 并刷新服务端任务状态；事件 ID 符合控制面格式，失败任务可用新 start 幂等键重跑，取消只有控制面明确确认后才清理 staging；本批次不调用 `complete`，不宣称已生成 N 个视频。
- [x] Phase 3/4 本地媒体纵切片第十一批：已接通固定 `ffmpeg/ffprobe` 能力探测、基础参数校验、参数数组重新编码、staging 产物不覆盖提交、输出 MP4 完整 SHA-256 和结构化不可用/失败错误；当前环境没有 FFmpeg 时只展示真实不可用状态，不生成假视频。本批次仅是本地重新编码试验，不代表 ASR、LLM 改写、TTS、时长对齐或完整 mux 已完成。
- [x] 历史实施记录：早期曾接入连续优先、双媒体槽和下一项缓冲策略；这些方案已废止。当前单源循环不等待下一项，实时幻化只对当前音轨执行换轨。
- [x] Phase 4/5 本地阶段能力第十三批：新增 Rust 阶段状态与能力契约；固定 `prepare → transcribe → rewrite → tts → align → mux → research` 顺序，失败阶段可重试，成功/取消为终态；Tauri `get_local_stage_capabilities` 按本地环境真实报告 ASR、模型直连/账号租约、TTS、FFmpeg 和研究算法是否可用，不产生伪成功事件。
- [x] Phase 5 参数契约第十四批：新增 Rust 音频、视频、65Hz～20000Hz 视觉频段、挂件与切片参数模型，所有字段带单位语义、默认值和范围校验；新增 React Ant Design 参数面板，区分配置值、只读测量值、原始/当前 SHA-256、算法版本和研究开关。当前只完成契约与展示，完整媒体算法和参数映射仍未完成。
- [x] Phase 6 本地转写纵切片第十五批：新增真实本地 ASR Worker 契约；Tauri `transcribe_variant_task` 从受控的 `AUTOLIVE_ASR_COMMAND` 读取 ASR 可执行程序，使用参数数组传入规范化源 MP4 和临时 JSON 输出路径，轮询取消/超时并回收子进程；转写段落按时间轴、文本、ID、字数和目标时长校验，生成幂等 `transcript.json`，记录源 MP4 SHA-256 和产物完整 `artifact_sha256`；React 已接入 `prepare → transcribe → StageEvent` 同一任务、同一窗口状态展示。未配置 ASR 时明确失败，不生成假转写；rewrite、TTS、align、mux 尚未实现。
- [x] Phase 1/2 控制面与客户端第十六批：新增模型号池摘要/新增接口和客户端模型租约申请、续租、释放闭环；Go 在开发期内存中保存密钥并只返回 `secret_configured`，租约绑定已激活用户与设备，执行并发上限、过期惰性回收、幂等和稳定错误码；管理端使用 Ant Design Table/Form/Modal 接入 loading、empty、error、重试和密钥不回显，桌面端增加租约状态与操作展示，但明确不把租约当成 rewrite 成功。当前未实现真实模型代理、用量扣减、账号编辑/禁用、PostgreSQL 和生产 Secret Store。
- [x] Phase 1 会话生命周期：Go 已实现 Refresh Token 轮换、旧凭证撤销和 Logout；管理端接入 401 自动刷新/退出，桌面端接入受保护请求自动刷新、手动刷新和退出清理。
- [x] Phase 1 凭证安全存储：桌面端 Refresh Token 已使用成熟 `keyring` 库接入系统 Keychain/Credential Manager，启动轮换恢复、登录/轮换更新、退出/失效清理均已闭环；Access Token 仍只保存在运行时。
- [ ] Phase 1 生产持久化：PostgreSQL、服务端 Secret Store、号池/租约/审计持久化与完整权限闭环。第一阶段已接入 PostgreSQL JSONB 快照 repository、迁移版本检查、AES-256-GCM Secret Store、持久化会话，以及 users/devices、幂等记录、激活码、号池、租约、用量、审计和任务/阶段事件的同事务影子写入；模型域和任务域均已增加受控规范化读源验证，但规范化写入仍为影子重建，不能视为完整生产模式。
- [ ] Phase 2 完整页面：号池编辑/冷却与恢复、审计、权限细化和任务详情；号池摘要/新增/禁用、激活码作废、设备解绑页面操作已完成最小版本。
- [ ] Phase 3 桌面业务剩余：补齐 GPU/更细硬件摘要；最终效果窗口、断网心跳最新状态补发已完成，登录、激活、Profile、Heartbeat、Refresh/退出、Keychain、基础运行时遥测、Tauri IPC、单 MP4 选择/探测和单窗口基础播放已完成；旧多 MP4 选择/探测/队列仅保留历史代码审计，不属于当前目标。
- [ ] Phase 4 以后：当前首版需要 FFmpeg/真实媒体引擎依赖打包策略、speech-to-speech Worker、单源循环和当前音轨换轨实测；跨设备恢复、阶段清单自动修复和研究分析算法仍待完成。旧的 LLM rewrite、TTS、align、mux、多版本队列和离线媒体任务纵切片已废止，不再作为后续实施目标。
- [x] 第十七批：为桌面端申请/续租/释放和管理端新增账号建立“单次用户操作生命周期内复用同一 Idempotency-Key”的重试状态；参数/租约 ID 变化时生成新 key，成功或取消后清理；桌面端将登录、Profile、激活、Heartbeat 和租约操作统一互斥，并增加会话代际校验，避免旧响应串入新会话；已有活动模型租约时禁止切换登录账号，必须先释放租约。后续 rewrite 接入前的任务与有效模型租约绑定、真实用量扣减和生产 Secret Store 仍未实现。
- [x] 第十八批前半：定义并实现任务与 `rewrite` 租约的延迟绑定契约；创建/start/prepare/transcribe 不占用租约，桌面端在进入 rewrite 前通过幂等绑定接口提交任务、租约、provider、model；Go 校验当前会话设备、任务/租约用户设备归属、用途、模型匹配、active 状态和过期时间，并强制要求 `prepare/transcribe` 已成功；旧租约过期/释放后可换绑新租约，rewrite 阶段事件再次校验有效租约，运行中任务占用的租约不可直接释放。真实模型代理、用量扣减和生产 Secret Store 仍未实现。
- [x] 第十八批后半最小闭环：接入开发期 OpenAI-compatible Go 模型代理、服务端 API Key 隔离、账号并发/每日 token 额度原子预占、超时/失败后的槽位释放、usage 错误记录、管理端 usage 查询和模型请求幂等重放；rewrite 成功事件必须关联服务端已确认的 `proxy_request_id`。通过 httptest 供应商覆盖成功、幂等、失败回收、额度耗尽、并发上限、HTTP 路由和成功证明校验；该批次当时桌面端只接入转写段落、一版 rewrite 请求和阶段事件上报，批量 N 版、TTS、align、mux 和连续预生成尚未接入，后续状态由第二十批更新。
- 说明：第十八批前半行中的“未实现”仅指该半批当时的验收范围；模型代理和开发期用量闭环已由本批后半完成，当前剩余的是生产化和批量媒体流水线。
- [ ] 第十八批后续生产化：把账号、租约、usage、幂等状态迁移到 PostgreSQL/Secret Store；补账号健康状态、冷却、刷新/取消/未知状态查询、真实供应商集成测试和 Rust 客户端失败切换。
- [x] 第十九批媒体 Worker 与 N 版计划基础：新增 Rust `tts_worker`、`align_worker` 和媒体 `mux` 计划；三者均通过受控外部第三方命令执行，使用参数数组、不经过 Shell，具备输入/输出文件校验、完整字节流哈希、超时、取消、子进程回收、staging/拒绝覆盖和结构化对齐报告校验。新增 Tauri `tts_variant_task`、`align_variant_task`、`mux_variant_task` 及统一阶段取消命令，能力探测改为验证可执行文件真实存在；新增按源时长计算 N、手动覆盖、同窗口顺序调度和失败版本重试的 `variant_plan` 核心，并增加当前队列追加命令和等待时不重播相同 MP4 的前端行为。该批次仅完成 Worker 契约与命令边界，尚未配置真实 TTS/对齐适配器，尚未将这些阶段串入 React 的批量任务流程，也未完成 Go 版本子任务、阶段清单、连续预生成和真实短样片端到端验收；后续状态由第二十批更新。

本批次验收记录（2026-08-12）：Rust `transcribe_worker` 定向测试 8 项通过，Rust 工作区测试 79 项、Tauri 命令测试 8 项通过；`cargo fmt --check`、`cargo clippy --workspace --all-targets -- -D warnings` 和 `cargo build --release` 通过；桌面 UI `pnpm exec tsc --noEmit --noUnusedLocals --noUnusedParameters` 与 `pnpm run build`、管理端 `pnpm exec tsc --noEmit` 与 `pnpm run build`、Go `go test -race ./...`、`go vet ./...`、`go build ./...`、文档引用与媒体参数契约检查均通过。Worker 测试覆盖段落规范化、空段/重叠/时长越界、请求前置校验、取消抢占、可选字段 JSON、取消期间哈希和能力探测规则；ASR 子进程 stdout/stderr 使用空设备避免管道阻塞，任务级取消令牌可由 Tauri 取消命令触发；尚未配置真实 ASR 可执行程序，故未声称真实长视频转写成功。当前未检测到 FFmpeg/FFprobe，真实编码成功路径仍未验证；桌面端与管理端 Vite 均有大包体积 warning，后续性能阶段处理。构建产生的 `node_modules`、`dist`、`target` 属于本地生成物，不作为源代码交付。

第十六批补充验收记录（2026-08-12）：Go 号池与租约定向测试、竞态测试、vet 和 build 通过；管理端与桌面端 TypeScript 检查和构建通过；OpenAPI YAML 可解析。主线程复核并修正了释放租约响应类型缺失 `lease_id`、每日额度 UI 与契约下限不一致、无 body/分块请求的可选 JSON 解析、JSON `null` 误当空请求、未绑定设备申请租约、登录会话被心跳改绑和任务阶段提示遗漏 `transcribe`。本批次仍未配置真实模型供应商代理，API Key 仅为开发期进程内存状态，重启丢失；`daily_limit` 仅保存配置、尚未扣减，不得视为生产安全实现。

第十七批补充验收记录（2026-08-13）：桌面端和管理端均增加纯逻辑幂等键测试，不引入新的测试框架；同一操作失败重试复用同一 key，表单参数或租约 ID 变化生成新 key，成功/取消清理状态。桌面端新增 Node 原生测试命令，6 项测试通过；管理端 4 项测试通过；两端 TypeScript 检查和 Vite 构建通过。代码总监复核发现并修复登录与租约请求并发、Profile/激活/Heartbeat 与登录切换并发导致的竞态，以及活动租约切换账号造成服务端租约泄漏：五类会话相关操作统一互斥，响应检查会话代际，活动租约存在时登录被拒绝，旧响应不能覆盖新会话。当前未做浏览器 DOM 点击测试和真实控制面网络超时集成测试；模型任务与租约绑定、真实模型代理、用量扣减和生产 Secret Store 继续延期。

第十八批前半验收记录（2026-08-13）：Go 新增任务 rewrite 租约绑定服务和 HTTP 接口；创建/start/prepare/transcribe 保持不占用模型租约，只有 `prepare/transcribe` 成功后才允许进入绑定；rewrite 阶段事件要求有效绑定。服务端覆盖缺失绑定、用户/设备归属、provider/model 不匹配、重复绑定、过期/释放后换绑、幂等重放和运行中任务占用租约时禁止释放；桌面端新增绑定 API、操作幂等键、任务操作互斥、绑定状态展示、阶段完成前置提示、登录切换清理和失败可重试提示。真实模型代理、真实 rewrite、用量扣减、租约自动释放和生产 Secret Store 未纳入本半批。

第十八批前半补充验收记录（2026-08-13）：代码总监复核发现并修复绑定前置阶段门禁、过期/释放租约换绑路径、登录切换残留任务状态和桌面端接口清单遗漏；Go `go test -race ./...`、`go vet ./...`、`go build ./...`，Rust 79 项工作区测试、8 项 Tauri 命令测试、fmt/clippy/release build，桌面端 8 项 Node 测试、TypeScript 检查和构建，管理端 4 项 Node 测试、typecheck 和构建，以及 OpenAPI/文档/参数契约检查全部通过。两端构建仍有既有大包体积 warning；当前未配置真实模型代理、ASR/TTS 和 FFmpeg，不宣称真实媒体生成已完成。

第十八批后半最小闭环验收记录（2026-08-13）：Go 增加 OpenAI-compatible `/chat/completions` 服务端代理、API Key 隔离、账号并发槽位、每日 token 预占/结算、失败/超时回收、usage 记录和管理端查询；rewrite 成功事件必须关联已完成的 `proxy_request_id`。Go 单元与 HTTP 集成测试覆盖代理成功、幂等重放、上游失败回收、额度耗尽、并发上限、鉴权路由和成功证明；`go test -race ./...`、`go vet ./...`、`go build ./...` 通过。Rust 转写结果携带规范化段落，桌面端在同一任务和同一窗口增加一版 rewrite 调用，支持 running/succeeded/failed 上报和模型结果展示；Rust 79+8 项测试、fmt/clippy、桌面端 Node 8 项测试、TypeScript/build、管理端测试/typecheck/build、OpenAPI 引用检查通过。模型账号/租约/usage 仍是开发期内存实现，桌面端批量 N 版、TTS、align、mux、连续预生成、取消/未知状态查询和生产 Secret Store 未完成；前端构建保留既有大包体积 warning。

第十九批补充验收记录（2026-08-13）：Rust 新增 TTS/align/mux Worker 契约、真实可执行文件能力探测、参数数组外部命令调用、阶段取消注册、超时/取消后的 kill-wait 清理、唯一 partial 和拒绝覆盖提交；Mux 明确使用第二音频输入并重新编码视频，未使用 `-c:v copy`。新增 `variant_plan` 自动/手动 N 计算、失败版本同 ID 重试、播放队列追加以及连续等待时不重播相同 MP4；Tauri 注册 `tts_variant_task`、`align_variant_task`、`mux_variant_task`、`cancel_variant_stage_task`、`append_playback_queue`。本轮 Rust 核心 90 项测试、Tauri 命令 8 项测试、fmt/clippy、桌面端 Node 8 项测试/TypeScript/build、管理端 4 项测试/typecheck/build、Go `go test -race ./...`/`go vet ./...`/`go build ./...`、文档引用/媒体参数契约/OpenAPI 解析均通过；前端仍保留既有 Vite 大包体积 warning。未配置真实 TTS/对齐适配器和 FFmpeg，未宣称真实音频生成、长视频合成或端到端连续播放验收；下一批进入 Go 版本子任务、结构化脚本/阶段清单和 React 批量编排。

第二十批实施记录（2026-08-13，进行中）：Go 控制面已增加服务端权威版本计划摘要（`plan_revision`、`plan_sha256`、`variants[v01...vN]`）和单版本查询、版本阶段事件、单版本完成接口；版本子任务从 `rewrite` 开始，`prepare/transcribe` 作为父任务共享阶段只执行一次；版本事件指纹按任务/版本隔离，rewrite 模型代理请求增加 `variant_id` 归属，父任务 `complete` 不能绕过 N 个版本的最终验收，父任务取消会级联标记未完成版本。Rust 新增统一 `variant_manifest` 阶段清单，记录 attempt、operation、输入/输出哈希、参数哈希、引擎版本、随机种子、时长偏差和质量检查，并采用不可覆盖提交；新增结构化 `script_vNN.json` 写入命令，拒绝自由文本进入 TTS。React 已把原先一版 rewrite 调用改为按 Go 返回的版本顺序逐版上报 `rewrite running/succeeded/failed`，并保存每版结构化脚本，同一窗口和同一任务内处理；TTS/align/mux 的批量编排、完成产物追加队列和重启恢复仍未完成。代码总监复核修正了版本事件 `operation_id` 测试夹具，并完成 Go 竞态/vet/build、Rust 核心 92 项 + Tauri 9 项测试、桌面端 Node 8 项测试/TypeScript/build、管理端测试/typecheck/build、文档引用/媒体参数契约/OpenAPI 解析验证；仅保留既有前端大包体积 warning。该批次明确为部分完成，不宣称 N 版视频已生成。

第二十一批实施记录（2026-08-13，已完成）：Rust 修复 TTS/align 首次执行时输出目录未创建的问题，并新增目录幂等回归测试；React 增加授权参考音频选择，按 `v01...vN` 顺序把结构化脚本接入 `tts_variant_task → align_variant_task → mux_variant_task`，每个阶段均上报版本级 running/succeeded/failed 事件，成功后重新探测输出 MP4 并校验完整 SHA-256、可读性和时长偏差不超过 3%。真实 TTS/FFmpeg/对齐适配器仍需配置后进行样片验证。

第二十二批实施记录（2026-08-13，已完成）：React 增加版本最终质量门禁：校验 N 个输出 MP4 完整 SHA-256 唯一、相邻脚本文本相似度低于 70%、本地可播放性和时长偏差不超过 3%，并要求用户明确确认锁定字段、敏感词和适用规则；随后按 `v01...vN` 顺序调用版本 `complete`，全部完成后将“原声首项 + N 个版本”建立或将 N 个版本追加到当前同一播放窗口，重复追加会跳过已存在版本，不创建新窗口。若产物追加失败，控制面完成状态保留，界面允许重试追加；跨重启恢复、阶段断点续跑和真实适配器端到端验证仍未完成。

第二十三批实施记录（2026-08-13，已完成）：桌面端连续优先编排改为“先原声、后逐版追加”：生成任务开始先建立并尝试启动原声首项；每个版本在 `rewrite → TTS → align → mux → MP4 探测 → 相邻话术差异/哈希唯一性门禁 → variant complete` 成功后立即追加到同一播放窗口，不等待 N 个版本全部完成。播放队列保持当前项和播放代际不变；源视频先结束而下一版尚未生成时通过 `wait_for_buffer` 保留当前末帧，下一项就绪后再切换，自动播放失败保留显式 Start 兜底。Rust 92 个核心测试、11 个 Tauri 测试、桌面端 8 个测试/TypeScript/build、管理端 4 个测试/typecheck/build、Go 竞态/vet/build、文档引用/参数契约/OpenAPI 解析均通过；Vite 仍保留既有大包体积 warning。跨重启恢复、TTS/对齐/媒体引擎真实适配器和分段实时幻化仍未完成。

第二十四批实施记录（2026-08-13，已完成）：桌面端增加单任务本地 checkpoint，保存任务摘要、源路径和完整 SHA-256、参考音频、转写、结构化脚本、TTS/align/mux 产物摘要、质量结果、人工确认项和同一窗口播放队列。应用启动时重新探测源 MP4 并复核完整 SHA-256，拒绝把旧任务恢复到已变化的素材；登录后刷新控制面任务状态。连续优先任务可从控制面已成功且本地已有结果的 rewrite/TTS/align/mux 阶段跳过重复执行，失败阶段继续重跑，输出仍必须重新 probe、校验时长和质量门禁；播放队列恢复到 Ready，不自动播放。已修复恢复失败任务时被 UI 清空的问题，并避免 checkpoint 版本产物在重复验收时被自身哈希误判为重复。Go `go test -race ./...`、`go vet ./...`、`go build ./...`，Rust 92 个核心测试 + 11 个 Tauri 测试、fmt/clippy/release build，桌面端 8 项 Node 测试/TypeScript/build，管理端 4 项测试/typecheck/build，以及文档引用和参数契约检查全部通过；Vite 保留既有大包体积 warning。Refresh Token/Keychain、跨设备恢复、阶段清单自动修复、真实 TTS/对齐/媒体适配器和分段实时幻化仍未完成。

第二十五批实施记录（2026-08-13，已完成）：补齐控制面会话生命周期。Go 新增 `POST /api/v1/auth/refresh` 的 Refresh Token 单次轮换，旧 Access/Refresh 凭证立即失效；新增受 Bearer 保护的 `POST /api/v1/auth/logout`，同时撤销当前访问和刷新凭证。管理端 API Client 在受保护请求收到 401 时复用单个刷新请求并重试一次，失败后清理会话；主布局增加退出登录。桌面端 Tauri HTTP 客户端同样提供并发合并的 401 自动刷新，App 增加手动刷新和退出清理，活动模型租约存在时禁止退出以避免租约丢失。新增 Go 刷新轮换/复用/退出测试和管理端会话 Token 更新测试；Go `go test -race ./...` 与两端类型检查通过。该批次当时凭证仍只在内存/浏览器会话中保存，Keychain/Secret Store、PostgreSQL 持久化、跨设备恢复和真实媒体适配器仍未完成。

第二十六批实施记录（2026-08-13，已完成）：桌面端新增受限 Tauri Keychain 命令，仅允许按稳定设备标识读写/删除 Refresh Token，不提供任意服务名、账号名或文件路径接口；引入成熟 `keyring` 库使用系统凭据存储，不把令牌写入 localStorage、sessionStorage、普通配置或日志。登录成功和 Refresh Token 轮换后更新钥匙串；应用启动读取并调用控制面 refresh，再获取 profile 恢复会话；主动退出、Refresh 失效和自动刷新竞态清理钥匙串。钥匙串不可用时本次运行仍可继续，但提示重启需重新登录。桌面 checkpoint 改为按 `user_id` 命名空间并要求版本 2，登录后才读取，避免账号切换恢复其他用户任务。新增 Rust 账户命名空间/非法标识测试；Rust 全量 92 个核心测试 + 13 个 Tauri 测试、fmt，通过桌面 UI TypeScript 检查和 8 项 Node 测试；PostgreSQL、服务端 Secret Store、跨设备恢复和真实媒体适配器仍未完成。

第二十七批实施记录（2026-08-13，已完成）：扩展桌面运行时信息采集：在已有磁盘可用空间基础上，增加总内存、可用内存、逻辑 CPU 数、系统名、系统版本和内核版本；Tauri 返回结构化 DTO，React Heartbeat 发送，Go 服务端校验并保存到设备摘要，管理端设备列表展示运行时信息。新增 Rust DTO 序列化测试、Go 服务层持久化/异常范围测试和 HTTP Profile 回读测试；同步更新 OpenAPI、管理系统架构、桌面架构和客户端状态文案。GPU 信息、断网补发和 PostgreSQL 持久化仍未完成。

第二十八批实施记录（2026-08-13，已完成）：完成两项开发期生产安全边界加固：Go 认证会话 map 只以 Access Token 的 SHA-256 哈希索引和保存，原始令牌不落入服务端会话记录；HTTP Server 显式设置 `ReadHeaderTimeout=5s`、`ReadTimeout=30s`、`WriteTimeout=60s`、`IdleTimeout=120s`，并新增会话存储不含原始 Access Token 的回归测试和超时配置测试。同时加入 `APP_STORAGE_MODE`/`APP_DATABASE_URL` 配置校验、PostgreSQL 模式未实现时的 fail-closed 启动保护、首个核心控制面 SQL 迁移和 Secret Store 字段边界文档。该批次没有伪装成 PostgreSQL/Secret Store，生产 repository、迁移执行器和服务端密钥存储仍未完成。

第二十九批实施记录（2026-08-13，已完成）：桌面端新增按 `user_id + device_id` 隔离的单条 Heartbeat 本地补发记录，网络错误、超时、429 和 5xx 只保留最新状态，不积累无限历史；补发记录复用原 `Idempotency-Key`，成功后按 key 清理，鉴权/参数类错误不入队。补发数据不含任何 Token、激活码或模型密钥；新增 3 项 Node 原生测试，验证隔离、覆盖、按 key 清理和临时错误判定。桌面端 UI 显示待补发时间，构建和 11 项测试通过；尚未完成真实网络断开/恢复的 Tauri 手工验收。

第三十批实施记录（2026-08-13，已完成）：新增 Tauri `open_final_effect_window`/`close_final_effect_window`，最终效果窗口使用固定 `final-effect` 标签和 `index.html?view=final-effect`，重复打开只显示并聚焦，不重新创建窗口；React 主窗口通过 Tauri 事件同步当前/下一媒体 URL、激活槽、播放状态、当前时间和媒体名，最终效果窗口内部继续使用双媒体槽预加载与切换，不因第二个视频重新弹窗。新增最终效果入口、自动播放失败时的手动开始提示和关闭按钮；Rust 14 项测试、Clippy、桌面端 TypeScript/Vite 构建通过。几分钟/30 分钟/1 小时真实素材的手工连续播放验收仍待真实媒体引擎和样片完成后执行。

第三十一批实施记录（2026-08-13，已完成）：接入成熟 `golang-migrate/migrate` v4.17.1，迁移文件改为标准 `.up.sql` 命名并通过 `embed.FS` 固定随版本发布；新增独立 `backend/cmd/migrate` 命令，要求 `APP_STORAGE_MODE=postgres` 和 `APP_DATABASE_URL`，执行前 Ping 数据库，迁移不会在 API 服务启动时隐式执行。新增迁移目录嵌入测试、配置边界测试和命令编译/默认 fail-closed 验证；Go 全量测试通过。当前仅完成可执行迁移入口，PostgreSQL repository、Secret Store 和 API 服务生产模式仍未开放。

第三十二批实施记录（2026-08-13，已完成）：补齐管理端号池账号禁用闭环。Go 新增 `POST /api/v1/admin/model-pool/{account_id}/disable`，服务端按幂等键返回同一禁用结果，账号不存在返回稳定 404；禁用前惰性回收过期租约，仍有活动租约时返回 `MODEL_POOL_ACCOUNT_IN_USE`，避免中断正在执行的模型请求。React 号池页面增加 Ant Design 确认弹窗、禁用操作、操作中状态和错误 request_id 展示；OpenAPI、管理系统架构和号池设计文档同步更新。Go 服务层/HTTP 集成测试覆盖成功、重复提交、幂等冲突、缺失账号和管理端响应；账号编辑、冷却/恢复、审计记录和 PostgreSQL/Secret Store 仍未完成。

第三十三批实施记录（2026-08-13，已完成）：将 Go 控制面全部状态读写从具体 `MemoryStore` 收口到 `store.Repository`，新增可注入的 `NewControlPlaneWithRepository` 和 `NewRouterWithRepository`；保留现有内存实现作为本地开发/测试实现，并用 context 检查和受保护状态操作测试固定边界。该批次只完成替换点和依赖注入，不宣称 PostgreSQL repository 已接通；API 仍按配置 fail-closed，迁移仍由独立命令执行。

第三十四批实施记录（2026-08-13，已完成）：补齐激活码作废闭环。Go 新增 `POST /api/v1/admin/activation-codes/{code_id}/revoke`，按幂等键重放同一结果，缺失账号返回稳定 404，已核销/已过期状态返回 `ACTIVATION_CODE_STATE_CONFLICT`；管理端激活码列表增加 Ant Design 确认弹窗、操作中状态和错误 request_id 展示。OpenAPI、管理系统架构、服务层测试和 HTTP 集成测试同步更新；已绑定设备不受激活码作废影响。

第三十五批实施记录（2026-08-13，已完成）：补齐设备解绑闭环。Go 新增 `POST /api/v1/admin/devices/{device_id}/unbind`，解除后清空设备用户归属并将状态设为 `pending_activation`；同一设备只有使用新的激活码才能重新绑定，原设备上的受保护 Profile/Heartbeat 会因归属消失而失败。管理端设备列表增加解绑确认操作、并发状态和错误 request_id 展示；OpenAPI、PRD、架构文档及服务层/HTTP 集成测试同步更新。

第三十六批实施记录（2026-08-13，已完成）：补齐管理端变体任务详情最小展示。任务列表支持展开同一行查看源 MP4 SHA-256、计划版本摘要、每个 `vNN` 的顺序/尝试次数/状态/当前阶段/阶段摘要和失败原因，不新增视频传输或服务端媒体处理；号池编辑/冷却恢复、审计筛选和细粒度权限仍未完成。

第三十七批实施记录（2026-08-13，已完成）：补齐号池非密钥编辑与手动状态管理。Go 新增 `PATCH /api/v1/admin/model-pool/{account_id}`，支持代理地址、优先级、每日额度、并发上限和 `active/cooldown/exhausted/disabled` 状态的幂等更新；降低并发上限不得低于当前活动租约数，API Key 不经过编辑接口读取或修改。React 使用 Ant Design 编辑弹窗和状态选择器接入操作，OpenAPI、管理系统架构和测试同步更新；自动健康探测、自动冷却、生产额度持久化和密钥轮换仍未完成。

第三十八批实施记录（2026-08-13，已完成）：将模型密钥从 Go `State` 通用状态中移除，`ModelPoolAccountSummary` 只保留不可序列化到 JSON 的 `SecretRef`；新增 `store.SecretStore`、开发期 `MemorySecretStore` 和 AES-256-GCM `EncryptedSQLSecretStore`，生产候选实现将密文保存到 `model_account_secrets`，加密主密钥由 `APP_SECRET_ENCRYPTION_KEY` 注入。模型号池创建、租约申请和模型代理均通过 Secret Store 读取/写入，缺失密钥返回稳定错误；新增上下文、密文不含明文和配置边界测试，并更新迁移。PostgreSQL repository、Secret Store 生产启动接线、密钥轮换和跨进程集成测试仍未完成。

第三十九批实施记录（2026-08-13，已完成）：补齐开发期审计记录闭环。Go 在认证请求上下文中传递操作者和设备边界，审计中间件只记录已认证 `/api/v1` 非 GET 请求的 HTTP 动作、操作者、设备和 request_id，不读取请求体；新增内存审计记录、分页查询接口和管理端 Ant Design 列表，敏感字段不进入审计。新增服务层/HTTP 集成测试并同步 OpenAPI、PRD；审计持久化、筛选、跨进程一致性和数据库事务接入仍待 PostgreSQL repository。

第四十批实施记录（2026-08-13，已完成）：完成 PostgreSQL 控制面第一阶段接入。新增 `control_plane_state` 版本化单行 JSONB 快照迁移；`PostgresRepository` 通过 `SELECT ... FOR UPDATE` 包住现有服务层的读改写和提交，保持取消、失败回滚和并发事务边界；快照序列化前剥离模型密钥引用/值和明文激活码，恢复时仅按账号 ID 派生内部 SecretRef；新增 PostgreSQL 模式服务启动接线、数据库 Ping、迁移版本/dirty 检查和 `APP_SECRET_ENCRYPTION_KEY` 的 32 字节十六进制/Base64 校验，接入 `EncryptedSQLSecretStore`。新增快照脱敏、密钥格式、显式存储模式和迁移目录测试，Go 全量测试通过。该批次是过渡适配器，不宣称认证会话、规范化业务表、多实例无状态、密钥轮换和跨进程故障恢复已完成；这些内容进入后续生产化批次。

第四十一批实施记录（2026-08-13，已完成）：接入 PostgreSQL `SessionStore`。新增 `auth_sessions` 的创建、Access Token 哈希查询、Refresh Token 单次事务轮换、Logout 撤销和设备绑定接口；PostgreSQL 模式不把会话加载到进程缓存，服务重启后可直接恢复会话，旧 Refresh Token 重放只能失败；内存模式继续保留原有测试实现。为满足会话外键，`PostgresRepository` 在同一控制面事务内对 `users`、`devices` 做影子 upsert，普通用户密码哈希仍只保存哈希，本地管理员使用明确的外部配置占位值。新增会话仓储输入校验、authenticator 重建后的 Access/Refresh 恢复测试，并修正设备绑定条件更新和历史会话缓存风险。Go `go test -race ./...`、`go vet ./...`、`go build ./...` 通过。该批次尚未完成真实 PostgreSQL 回滚/并发/重启集成测试，其他领域仍未切换规范化读写，SecretStore 与快照提交的跨资源补偿也仍待后续处理。

第四十二批实施记录（2026-08-13，已完成）：增加控制面初始快照修复迁移 `0003_修复控制面初始快照.up.sql`，把旧的 `{"version":1}` 占位行升级为显式 `{"version":1,"state":{}}`；`unmarshalStateSnapshot` 不再把缺失 State 静默当作新系统，且会初始化空快照中的全部领域 Map。新增迁移目录版本断言和缺失 State 回归测试，避免数据库损坏被误判为空系统。

第四十三批实施记录（2026-08-13，已完成）：使用临时 PostgreSQL 16 容器执行真实迁移和服务冒烟验收。首次登录发现空 `device_id` 被写成空字符串并触发 `auth_sessions` 外键错误，修复 `SQLSessionStore.Create` 使用 `NULLIF` 写入 NULL；修复后验证真实登录、数据库会话哈希记录、服务重启后的 Access Token 恢复、Refresh Token 轮换、旧 Access/Refresh 立即失效和新 Access 可用。临时容器已停止且未保留持久卷。真实数据库回滚、并发 Refresh、跨实例故障恢复和其他领域规范化读写仍需后续集成测试。

第四十四批实施记录（2026-08-13，已完成）：将 `State.IdempotencyRecords` 纳入 PostgreSQL 快照事务的影子写入。每次控制面快照提交时，在同一 `sql.Tx` 中重建 `control-plane-state` 范围的 `idempotency_records`，保留完整 State 复合键、指纹和资源 ID，不改变现有服务层的幂等读取事实源；空键、空指纹或空资源 ID 会阻止提交。新增真实 SQL 形状的 sqlmock 测试，验证幂等记录与快照共用事务；激活码、号池、租约、任务和审计领域的规范化读写仍未切换。

第四十五批实施记录（2026-08-13，已完成）：将激活码元数据纳入快照事务影子写入。新增非敏感 `CodePrefix`，创建激活码时只保存前缀；PostgreSQL 写入 `activation_codes` 的 code_hash、code_prefix、状态、过期时间和已绑定设备，绝不写入 `PlainCode`，使用状态转移时记录 used_at。新增哈希索引缺失、过期时间非法和真实 SQL 参数边界测试；激活码服务层仍以 State 为事实源，号池、租约、任务和审计规范化读写仍待后续批次。

第四十六批实施记录（2026-08-13，已完成）：将开发期审计日志纳入 PostgreSQL 快照事务影子写入。写入 `audit_logs` 的动作、操作者、设备、资源上下文和 request_id，不写请求体，payload 固定为空 JSON；Actor/Device 外键在同一事务内由 users/devices 影子行保障。新增审计 SQL 参数测试，确认敏感数据不会通过审计 payload 进入数据库；号池、租约和任务领域仍待后续规范化迁移。

第四十七批实施记录（2026-08-13，已完成）：补齐 PostgreSQL 快照与独立 SQL SecretStore 的失败补偿边界。`PostgresRepository` 记录事务前后模型 SecretRef 集合；快照同步、序列化、更新或提交失败时，对本次新增引用执行 SecretStore 删除，避免正常失败路径留下孤儿密钥；补偿失败不被伪装为成功，后续需接入告警/运维核查。启动接线使用绑定了同一 SecretStore 的 repository 构造器，新增补偿测试；该方案仍不是分布式事务，号池/租约/任务规范化事务仍待后续批次。

第四十八批实施记录（2026-08-13，已完成）：补齐 `model_accounts` 影子写入所需的 `priority` 迁移字段，并在快照事务中同步模型供应商、模型名、代理地址、SecretRef、状态、优先级、并发上限、每日额度、活动请求数和当日预占数；缺失 SecretRef 或非法状态会阻止提交，密钥值仍只由 SecretStore 管理。新增迁移版本 4 和模型账号 SQL 字段映射，号池/租约/任务仍以 State 为事实源，尚未切换规范化读写。

第四十九批实施记录（2026-08-13，已完成）：使用临时 PostgreSQL 16 容器执行迁移 1～4 和 PostgreSQL 模式 API 验收；通过管理接口创建模型账号后，`model_accounts` 正确保存供应商、模型、优先级、额度和 SecretRef，`model_account_secrets` 只保存 AES-GCM 密文，数据库字节搜索未发现 API Key 明文。临时容器已停止且未保留持久卷；真实并发租约、代理额度和故障恢复集成测试仍待后续。

第五十批实施记录（2026-08-13，已完成）：新增迁移 5，补齐 `model_leases` 的 provider/model/proxy_mode/concurrency_limit 和 `model_usage_records` 的 request_id/total_tokens/latency_ms 字段；PostgresRepository 在模型账号之后影子写入租约和用量，校验租约引用的账号、用户、设备及用量引用的租约/账号，时间字段非法或引用缺失会阻止提交。号池和租约服务仍以快照为读写事实源，规范化表暂不直接服务请求。

第五十批补充验收记录（2026-08-13，已完成）：使用临时 PostgreSQL 16 容器重新执行迁移 1～5，`schema_migrations` 为版本 5 且 `dirty=false`；服务以 `APP_STORAGE_MODE=postgres` 启动并通过数据库、迁移版本和 32 字节加密主密钥检查后正常退出。Go `go test -race ./...`、`go vet ./...`、`go build ./...` 全部通过；模型租约/用量真实并发、故障回滚注入和规范化读源切换仍未纳入本批。

第五十一批实施记录（2026-08-13，已完成）：新增显式环境门控的 PostgreSQL 集成测试 `TestPostgresRepositoryRealModelLeaseAndUsage`；默认 `go test ./...` 不依赖外部数据库，设置 `AUTOLIVE_POSTGRES_TEST_URL` 后校验迁移版本为 5 且非 dirty、模型租约的 provider/model/proxy_mode/concurrency_limit、模型用量的 request_id/total_tokens/latency_ms、快照不含 SecretRef，并在测试结束后按固定测试 ID 清理状态与影子行。使用临时 PostgreSQL 16 容器实际通过该测试。该测试仍是过渡快照 repository 的验收，不代表规范化表已成为服务读源，也未覆盖双实例并发、数据库故障回滚和真实供应商额度一致性。

第五十二批实施记录（2026-08-13，已完成）：修复控制面 JSONB 快照丢失 `ModelLease` API 隐藏关联字段的问题；新增快照专用租约序列化，保留 user/device/account/purpose，同时不改变 HTTP JSON 输出。规范化影子写入的终态 `released_at` 改为只记录首次终态时间，重复同步不再污染历史。真实 PostgreSQL 集成测试新增租约关联重启恢复、终态时间稳定和 CHECK 约束失败整事务回滚；临时 PostgreSQL 16 实测通过。模型领域仍以 State 为事实源，规范化读源切换、并发额度和跨实例验收继续延期。

第五十三批实施记录（2026-08-13，已完成）：补充真实 PostgreSQL `SQLSessionStore` 生命周期集成测试；覆盖无设备会话写入与读取、撤销后访问失效、无效用户外键拒绝，以及同一 Refresh Token 的并发轮换只有一个成功、只产生一个新会话。`Rotate` 增加用户/设备身份不可变和 Refresh 绝对截止时间校验，验证失败会回滚事务；会话仍由 `auth_sessions` 作为 PostgreSQL 持久化读源，模型/任务领域的规范化读源切换仍未完成。

第五十四批实施记录（2026-08-13，已完成）：新增迁移 6 `model_request_reservations`，保存模型代理请求的 scope、幂等指纹、状态、usage/租约/账号关联、任务版本、预占 token、每日额度键、开始/结束时间、错误信息和结构化结果；Go 模型代理开始/完成路径补齐这些字段，PostgresRepository 在同一快照事务中重建请求预占影子表，并校验引用、所有者、时间和 in-flight 预占 token。真实 PostgreSQL 16 已通过迁移 1～6、请求预占结果 JSONB 写入和现有会话/租约集成测试。请求预占表仍是影子记录，模型代理尚未切换到规范化唯一事实源，崩溃未知状态恢复和多实例额度一致性继续作为下一批决策门。

第五十五批实施记录（2026-08-13，已完成）：新增迁移 7，为模型请求预占增加状态枚举、状态与结果/结束时间组合、结束时间不早于开始时间等 PostgreSQL CHECK 约束；PostgresRepository 同时校验预占与 usage 的 request_id、lease_id、owner、状态映射和终态字段，拒绝未知状态、in-flight 缺少预占 token、成功缺少结果、失败缺少错误码等数据。真实 PostgreSQL 16 已通过迁移 1～7、成功到失败状态转换和非法成功状态组合拒绝测试。请求崩溃后的 unknown 恢复动作、规范化唯一事实源和跨实例额度事务仍未完成。

第五十六批实施记录（2026-08-13，已完成）：补齐模型代理请求的安全恢复边界。控制面在下一次开始模型请求的事务内扫描超过 2 分钟安全窗口的 `in_flight` 记录，将无法确认上游结果的请求标记为 `unknown`，释放本地活动并发但保留每日预占，更新对应 usage，并保留原幂等记录；相同幂等请求不自动重试，只返回明确的 `MODEL_REQUEST_UNKNOWN`，避免把未知上游结果伪造成成功或失败或让新幂等键绕过额度保护。超时、客户端取消和无法确认的响应也进入同一 unknown 路径；最终状态先提交，再向调用方返回业务错误，即使原请求上下文已取消也不丢失状态。新增恢复单元测试、取消提交测试、PostgreSQL 真实事务提交测试；该恢复仍是请求触发式、以 State 为事实源，尚未完成启动/后台扫描、供应商幂等查询、unknown 最终核销、规范化表读源切换和跨实例额度一致性。

第五十七批实施记录（2026-08-13，已完成）：将模型请求恢复抽为独立事务 API，服务启动时执行一次，后续模型请求开始前继续执行；恢复不再和后续租约、账号、密钥或额度校验共享同一事务，后续业务失败不会回滚已经提交的 unknown。PostgreSQL 快照读取增加旧请求兼容迁移：缺少预占 token 或开始时间无法解析的历史 `in_flight` 会转为 `unknown`，补齐账号/用户/设备/每日键、usage 状态和活动并发扣减，并保留每日预占。新增旧快照迁移单元覆盖；模型请求规范化事实源、unknown 人工/供应商核销和跨实例额度事务仍未完成。

第五十八批实施记录（2026-08-13，已完成）：为 PostgreSQL 模型域增加受控 `APP_MODEL_READ_SOURCE=normalized` 读源开关，默认仍为 `snapshot`。规范化读源从 `model_accounts`、`model_leases`、`model_usage_records` 和 `model_request_reservations` 重建模型状态及 request index、活动并发、每日已用/预占额度；加载阶段 fail-closed 校验数量与快照一致、外键/所有者、状态枚举、usage/request 链接、终态字段、时间顺序、预占 token 和 request_id 唯一性，部分缺失或不一致拒绝事务。新增真实 PostgreSQL 16 仓储映射测试，以及 Go 服务层号池创建→租约申请→任务创建/start→阶段前置→rewrite 代理→幂等重放生命周期测试，确认模型密钥仍只由 SecretStore 提供。该批次只完成受控读源验证，规范化表仍由快照事务影子重建，尚未切换为唯一写入事实源；任务/阶段事件规范化 repository、供应商 unknown 核销、额度原子并发和跨实例一致性继续作为后续门槛。

第五十九批实施记录（2026-08-13，已完成）：新增迁移 8/9，为 `variant_tasks` 保存可查询主字段和完整 `task_payload`，为 `variant_task_events` 保存 `event_id`、阶段字段和完整事件 JSONB；任务创建、共享阶段事件和版本阶段事件均在同一 PostgreSQL 快照事务内影子写入，删除最后任务也会清理影子表。阶段事件唯一键改为 `(task_id, variant_id, event_id)`，允许同一 operation_id 下的 running/succeeded 等不同 event_id 共存，避免破坏事件语义。新增真实 PostgreSQL 16 任务/模型服务生命周期验收，覆盖共享事件、版本事件、模型代理幂等重放和迁移 1～9；当前任务/事件表仍是影子写入，任务规范化读源已增加严格 payload/索引/时间/事件引用校验和缺行 fail-closed 测试，但仍不是独立 repository、唯一写入事实源，跨实例并发和完整任务重启恢复仍未完成。

第六十批实施记录（2026-08-13，已完成）：新增 `backend/cmd/rebuild-task-shadow` 运维命令和 `PostgresRepository.RebuildTaskShadow`，要求迁移已到最新版本且模型/任务读源、任务写源均为 `snapshot`，在单事务内从控制面快照重建 `variant_tasks`、`variant_task_events` 的完整结构化影子数据；规范化任务读源缺载荷时不再回退快照。迁移 8/9 后可先执行该命令完成一次补齐，再切换 `APP_TASK_READ_SOURCE=normalized`。真实 PostgreSQL 16 已验证正常恢复、删除事件后规范化读源拒绝、快照命令修复后再次恢复。任务/事件仍未切换为独立写入事实源。

第六十一批实施记录（2026-08-13，已完成）：增加 `APP_TASK_WRITE_SOURCE=snapshot|dual` 配置。`snapshot` 保持兼容模式；`dual` 明确任务/阶段事件与控制面快照在同一 PostgreSQL 事务内提交，随后复用完整任务规范化 hydration 校验 payload、索引字段、时间、事件主键、版本引用和数量一致性；任何篡改或缺行都会阻止事务提交。`APP_TASK_READ_SOURCE=normalized` 必须配合 `dual`，内存模式拒绝双写。真实 PostgreSQL 16 生命周期测试改为使用 `dual + normalized` 配置，覆盖纳秒事件精度、缺行 fail-closed、同量篡改 payload 拒绝和重建修复；本批仍保留快照作为事实源，尚未切换独立任务 repository。

第六十二批实施记录（2026-08-13，已完成）：新增 Rust `research_worker` 受控研究分析适配器和 Tauri `research_variant_task`。研究 Worker 通过参数数组接收 MP4、研究参数 JSON、报告和可选输出 MP4；Rust 在执行前后计算完整文件 SHA-256，校验结构化报告中的源/当前哈希、内容相似度、媒体鲁棒性、隐形标记状态、随机种子和内容指纹，并把报告指标传递到 React 展示；支持取消、超时、子进程回收、partial 清理、双产物回滚和阶段清单。`get_local_stage_capabilities` 新增 `AUTOLIVE_RESEARCH_COMMAND` 能力探测；研究算法仍由外部 Worker 提供，未配置时返回真实不可用，不生成研究假产物。新增 5 项研究 Worker 契约测试；Rust 核心、Tauri 和研究契约测试已通过，尚未配置真实研究算法或 FFmpeg/ASR/TTS，因此未宣称真实媒体研究报告已生成。

第六十三批实施记录（2026-08-13，已完成）：新增 Rust `continuous_strategy` 策略计算器和 Tauri `recommend_continuous_strategy` 查询命令，使用源素材实际时长、当前播放剩余时间、最近最多 5 次版本生成耗时、安全系数和分段长度，返回整片预生成、后台预生成或分段预生成建议、所需提前量和当前素材是否能覆盖生成耗时；React 已在同一任务页面展示策略和历史耗时，并在版本完成后记录新样本。策略分类使用安全系数后的实际提前量，不能只比较裸生成耗时；没有实时剩余时间时才回退到源素材时长。该批只完成可测试的策略建议，不把建议当作已经分段生成，也不伪造连续播放；真正的分段 Worker、动态并发和长视频实测仍待后续批次。Rust 核心 100 项、Tauri 14 项、研究契约 7 项、桌面 UI 11 项测试通过，Go 竞态/vet/build 和文档/参数契约检查通过。

第六十四批实施记录（2026-08-13，已完成）：新增 Rust `speech_preview` 文本契约和 Tauri `speech_preview_task`/`cancel_speech_preview_task`。桌面端在当前任务页面增加同窗口实时话术预览：用户可输入文本或使用转写首段，先经 Go 模型代理生成纯文本，再复用受控 `AUTOLIVE_TTS_COMMAND` 生成 WAV，并使用当前页面 `audio` 控件试听；预览失败、TTS 未配置、文本不合法、超时或取消时，当前视频继续原声播放，不暂停、不重建窗口、不修改播放队列。预览脚本限制单段 `segments`、文本不超过 4096 字符，产物返回完整 SHA-256；同一 `preview_id` 的脚本内容不一致时拒绝覆盖；该批不写版本阶段事件、不将预览音频当作已验收 MP4，也不宣称无缝换轨完成。Rust 核心 103 项、Tauri 16 项、研究契约 7 项和桌面 UI TypeScript 检查通过；真实 TTS、ASR、FFmpeg 和模型供应商仍需配置后做样片验证。

第六十五批文档变更（2026-08-13，已完成）：新增《实时音频幻化与循环播放方案》，曾按单窗口循环、显式播放轮次、输入音轨和音频双槽换轨门禁规划运行时重复检测；该批历史方案现已被第六十六批变更废止，不能作为当前实现依据。

第六十六批文档变更（2026-08-13，已完成）：取消实时播放链路中的 Chromaprint/landmark 重复检测、历史指纹库和相似度决策。实时幻化改为按播放轮次和音频片段调用 Hugging Face `speech-to-speech` Worker，由 VAD/ASR/LLM/TTS 完成上下文理解，LLM 决定沿用原文或生成改写话术，再经时长对齐、同步门禁和当前音轨替换；Worker、LLM、TTS 或换轨失败时回退原声。内容指纹对比仍保留在研究分析报告中，但不参与播放决策。

## 6. 每阶段统一验收清单

- [ ] 功能边界与非目标没有被扩大。
- [ ] 输入、输出、成功、失败、取消、超时、重试和恢复路径有定义。
- [ ] 第三方依赖有真实调用点、版本策略、许可证和移除路径。
- [ ] 参数有单位、范围、默认值来源和序列化格式。
- [ ] 任务和资源有所有者、取消路径、超时和清理。
- [ ] 测试覆盖核心成功路径和高风险失败路径。
- [ ] 本地执行格式化、Lint、类型检查、测试和构建；未执行项明确记录。
- [ ] 检查并删除未使用代码、依赖、导入、配置和调试日志。
- [ ] 更新受影响的 PRD、架构、接口和约束文档。

## 7. 当前风险与决策门

1. 媒体引擎：当前没有检测到 FFmpeg 命令；已增加 `AUTOLIVE_FFMPEG_PATH`/`AUTOLIVE_FFPROBE_PATH` 显式路径探测，Phase 4 仍必须完成随包引擎打包策略和长视频性能验收。
2. 本地 TTS：GPT-SoVITS、CosyVoice、F5-TTS 的模型体积、许可证、GPU 和中文音色效果必须在样片上实测后确定主方案。
3. 65Hz～20000Hz：普通 30/60 fps 视频无法忠实表达所有时间频率，必须展示实际生效频率，并将超出部分降级为纹理/空间实验。
4. 首项原声是否计入目标时长：默认不计入；如果验收要求从首帧严格计算，使用播放计划字段切换。
5. 模型调用：Go 负责 OpenAI-compatible 账号登记/连通性测试、租约、额度和调用摘要；Rust 通过单账号短期直连租约直接调用模型，不下发完整号池或长期 API Key。
