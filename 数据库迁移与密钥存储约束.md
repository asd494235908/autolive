# 数据库迁移与密钥存储约束

> 产品范围同步（2026-08-19）：本版本不开发实时话术幻化。本文涉及模型账号、租约或历史媒体表结构时，仅作为控制面/迁移审计说明；不代表当前桌面端启用 speech-to-speech、LLM/TTS 或实时话术换轨。

## 当前状态

当前 Go 控制面默认使用 `MemoryStore`，仅适用于本地开发和测试。`APP_STORAGE_MODE` 默认为 `memory`；配置为 `postgres` 时必须同时提供 `APP_DATABASE_URL` 和 `APP_SECRET_ENCRYPTION_KEY`，服务端会连接 PostgreSQL、检查迁移版本并使用第一阶段 JSONB 状态快照适配器，任何检查失败都直接拒绝启动，不能静默回退到内存。

迁移目录中的 0001–0010 保留为历史重放依据；其中的 `variant_tasks`、`variant_task_events` 和 `model_request_reservations` 是旧 PRD 的历史兼容结构，当前管理系统不提供媒体任务、离线版本、服务端模型代理或请求预占功能，新代码不得读写这些表，也不擅自删除已部署数据库中的历史数据。第一阶段 `PostgresRepository` 使用 `control_plane_state` 单行 JSONB 快照和 `SELECT ... FOR UPDATE` 保证现有服务层的读改写事务边界；这是可恢复的过渡适配器，不是最终按领域拆分的规范化 repository。敏感字段不会进入快照：模型密钥引用和值、明文激活码均被剥离。

迁移使用成熟的 `golang-migrate/migrate` v4.17.1，执行入口为 `go run ./cmd/migrate`；命令读取嵌入式 `.up.sql`，要求 `APP_STORAGE_MODE=postgres` 和 `APP_DATABASE_URL`，API 服务启动不会隐式执行迁移，只检查 `schema_migrations` 的最新版本是否为应用要求版本且未处于 dirty 状态。

当前已实现 `store.SecretStore` 边界：开发期使用 `MemorySecretStore`；PostgreSQL 模式使用 `EncryptedSQLSecretStore`，以 AES-256-GCM 加密后写入 `model_account_secrets`，加密主密钥只从 `APP_SECRET_ENCRYPTION_KEY` 注入，不进入数据库。主密钥支持 64 位十六进制或 Base64，解码后必须恰好 32 字节。

当前不创建新的模型请求预占记录，也不在服务端保存模型正文。Go 控制面只负责号池账号测试、短期直连租约、额度/并发状态、客户端调用摘要和审计；Rust 客户端通过租约直接调用 OpenAI-compatible 供应商，并在调用结束后幂等上报摘要。无法确认的上游执行结果由客户端按调用失败处理并展示明确状态，不在控制面新增或恢复旧的预占链路。

当前 PostgreSQL 启动接线仍有明确限制：认证会话已接入 `auth_sessions`，Bearer 查询和 Refresh 轮换不依赖进程内缓存；`users`、`devices`、控制面快照幂等记录、激活码元数据、模型账号、模型租约和模型用量已在快照事务内做影子写入。模型读源默认仍为快照；受控 PostgreSQL 环境可设置 `APP_MODEL_READ_SOURCE=normalized`，此时模型账号、租约和用量从规范化表恢复，并对数量、外键、所有者、状态和 usage 链接做 fail-closed 校验，任何部分缺失或不一致都会拒绝本次事务。旧媒体任务和请求预占表不参与当前读源。模型规范化唯一写入事实源、真实额度并发和跨实例一致性仍需完成。在完成领域 repository、真实 PostgreSQL 并发/故障集成测试和跨进程一致性验收前，不得将该模式宣称为完整生产部署方案。

## 不可违反的存储边界

- `auth_sessions` 只保存 Access/Refresh Token 的哈希、过期时间、撤销时间和归属，不保存原始 Token。
- `activation_codes` 只保存激活码哈希和不可逆展示前缀，不保存明文激活码。
- `model_accounts.secret_ref` 只保存服务端 Secret Store 引用；`model_account_secrets.ciphertext` 只保存密文；API Key 不进入 PostgreSQL 明文列、日志、HTTP 响应或前端构建产物。
- 模型租约、用量和幂等记录必须在同一数据库事务边界内完成状态变更，不能通过先写内存再异步补库实现“伪持久化”；调用摘要不能包含模型正文、API Key 或完整音频/视频内容。
- 数据库连接、查询和事务必须配置超时；迁移失败必须阻止应用进入生产服务状态。

## 接入顺序

1. 抽象 `UserRepository`、`DeviceRepository`、`ActivationRepository`、`SessionRepository` 和 `IdempotencyRepository`，先覆盖登录、激活、设备心跳和幂等。
2. 接入成熟 PostgreSQL 驱动/迁移工具，迁移执行在发布阶段完成，应用启动只做版本检查，不隐式修改表结构。
3. 把模型号池、租约、用量和调用摘要迁移到事务 repository；并发槽位与每日额度必须使用数据库原子更新或行级锁。
4. 已接入第一阶段服务端 Secret Store 和控制面快照，并开放受迁移版本、数据库和主密钥校验保护的 `APP_STORAGE_MODE=postgres`；另提供受控的 `APP_MODEL_READ_SOURCE=normalized` 读源开关。它仍不等于完整生产持久化。
5. 接入 SessionRepository 和规范化业务 repository，移除快照过渡层；再将审计日志切换为独立写入事实源，并补跨进程/故障恢复集成测试。

## 验收门槛

- 进程重启后用户、设备、激活码、会话撤销、租约和模型用量仍可恢复。
- 两个 Go 实例并发使用同一幂等键时只能产生一个资源；租约并发和额度扣减不能超卖。
- 数据库不可用、迁移版本不匹配或 Secret Store 不可用时，服务端返回明确错误并拒绝进入生产模式。
- 审计日志能关联用户、设备、请求 ID 和资源，但不含密码、Token、API Key 或明文激活码。

已完成本地真实 PostgreSQL 16 冒烟验收：迁移版本/dirty 检查、服务启动、登录会话写入、服务重启后的 Access Token 查询、Refresh 轮换、旧凭证失效，以及模型账号元数据和加密密钥写入均通过；数据库中未发现 API Key 明文。早期任务、服务端模型代理和请求预占测试仅作为历史实现证据，不代表当前能力；当前仍需供应商直连、客户端调用摘要端到端验收、真实额度并发、规范化写入唯一事实源和跨实例一致性验收。
