# 数据库迁移与密钥存储约束

> 产品范围同步（2026-08-19）：本版本不开发实时话术幻化。本文涉及模型账号、租约或历史媒体表结构时，仅作为控制面/迁移审计说明；不代表当前桌面端启用 speech-to-speech、LLM/TTS 或实时话术换轨。

## 当前状态

当前 Go 控制面默认使用 `MemoryStore`，仅适用于本地开发和测试。`APP_STORAGE_MODE` 默认为 `memory`；配置为 `postgres` 时必须提供 `APP_DATABASE_URL`、`APP_SECRET_ENCRYPTION_KEY`，首次引导还需提供已替换的 `APP_ADMIN_USERNAME`/`APP_ADMIN_PASSWORD`（密码至少 12 个字符）。管理员完成专用密码轮换后，后续重启可省略这两个环境变量，服务端会从持久化 `users` 凭证启动前检查管理员；任何检查失败都直接拒绝启动，不能静默回退到内存。

迁移目录中的 0001–0022 保留为历史重放依据；其中的 `variant_tasks`、`variant_task_events` 和 `model_request_reservations` 是旧 PRD 的历史兼容结构，当前管理系统不提供媒体任务、离线版本、服务端模型代理或请求预占功能，新代码不得读写这些表，也不擅自删除已部署数据库中的历史数据。0012 在创建唯一索引前拒绝 `(lease_id, client_call_id)` 的历史重复摘要；重复数据必须由发布前人工核对，迁移不会静默合并或删除账单事实。0013 补齐设备展示字段并新增模型测试结果表，0014 为模型账号增加可恢复的 `cooldown_until` 字段及索引，0015 为审计记录增加结果、状态码和错误码字段，0016 新增用户模型 allowlist 与服务端已接收用量门禁表，0017 为会话/幂等/测试结果/审计/租约筛选与保留清理补齐可重复执行索引，0018 为会话设备绑定增加 `device_bound_at` 标记、历史回填和孤立绑定协调索引，并移除阻止“先绑定会话、后创建设备”的历史外键，改由主体校验与协调清理维护临时引用，0019 新增 `audit_outbox` 去重、处理状态、尝试次数、下一次重试时间和投递时间字段，支持审计事件先落盘后有界重试投递；0020 新增单例 `normalized_backfill_state` 与数据库写入门禁函数，迁移初始状态为 `pending`，只有 `autolive-backfill-normalized` 在复制快照并完成逐表校验的同一事务内才会切换为 `completed`；0021 为租约按用户/设备/provider+model 以及审计按操作者/设备/动作/资源的常用筛选增加可重复执行复合索引，并配套 PostgreSQL 计划可见性测试；0022 为激活码增加 `max_devices`/`bound_devices`、历史已核销计数回填和 1～100/占用不超容量检查约束。真实数据量 `EXPLAIN (ANALYZE, BUFFERS)` 仍需发布环境复核。第一阶段 `PostgresRepository` 默认仍使用 `control_plane_state` 单行 JSONB 快照；`APP_MODEL_READ_SOURCE=normalized` 在 PostgreSQL 上读取规范化表，并以事务级 advisory lock 保证现有服务层读改写边界，同时拒绝缺失/未完成回填标记和少于快照的规范化投影。这是规范化读源的过渡切片，不是最终按领域拆分的 repository。敏感字段不会进入快照：模型密钥引用和值、明文激活码均被剥离。

迁移使用成熟的 `golang-migrate/migrate` v4.17.1，执行入口为 `go run ./cmd/migrate`；命令读取嵌入式 `.up.sql`，要求 `APP_STORAGE_MODE=postgres` 和 `APP_DATABASE_URL`，API 服务启动不会隐式执行迁移，只检查 `schema_migrations` 的最新版本是否为应用要求版本且未处于 dirty 状态。迁移命令可选读取 `APP_MIGRATION_METRICS_FILE`，将固定低基数的 text exposition 快照以同目录临时文件写入、刷盘后原子重命名，包含 `autolive_migration_result{status}`、耗时、配置超时预算和目标 `migration_version`；不写入数据库 URL、错误全文、Secret 或 request ID。未配置时保持原有行为；已配置但指标文件无法发布时即使迁移本身成功也记录明确错误并以非零退出，避免发布门禁把“迁移成功但观测不可用”误报为完整成功。Compose 仅透传变量，生产启用时必须额外挂载可写目录供 textfile collector 读取。normalized 设备详情和客户端 profile 通过 `DeviceReader` 直接读取 `devices`/`users`，按用户校验设备归属；缺少规范化用户/设备领域能力时必须 fail-closed，不能回退 `control_plane_state`。

当前已实现 `store.SecretStore` 边界：开发期使用 `MemorySecretStore`；PostgreSQL 模式使用 `EncryptedSQLSecretStore`，以 AES-256-GCM 加密后写入 `model_account_secrets`，加密主密钥只从 `APP_SECRET_ENCRYPTION_KEY` 注入，不进入数据库。主密钥支持 64 位十六进制或 Base64，解码后必须恰好 32 字节。normalized 模式创建模型账号时必须使用 `TransactionalSecretWriter.PutTx`，在同一 SQL 事务写入账号行和密文；不支持事务写入的 SecretStore 会被明确拒绝，不能回退成部分成功。

当前不创建新的模型请求预占记录，也不在服务端保存模型正文。Go 控制面只负责号池账号测试、短期直连租约、额度/并发状态、客户端调用摘要和审计；Rust 客户端通过租约直接调用 OpenAI-compatible 供应商，并在调用结束后幂等上报摘要。无法确认的上游执行结果由客户端按调用失败处理并展示明确状态，不在控制面新增或恢复旧的预占链路。

当前 PostgreSQL 启动接线仍有明确限制：认证会话已接入 `auth_sessions`，Bearer 查询和 Refresh 轮换不依赖进程内缓存；首次启动会把管理员密码写入 `users` 关联的 bcrypt 哈希，`POST /api/v1/admin/auth/change-password` 仅允许本地管理员轮换密码并撤销其会话，后续重启不覆盖已有哈希且可不再提供静态管理员密码；Repository/密码哈希/配置失败时 API 进程非零退出；快照模式继续向 `users`、`devices`、激活码、模型账号、租约、用量、模型测试、幂等和审计规范化表同步影子行。`APP_MODEL_READ_SOURCE=normalized` 仅允许 PostgreSQL，必须先执行 `APP_NORMALIZED_BACKFILL_CONFIRM=YES go run ./cmd/backfill-normalized`；迁移 0020 的状态必须为 `completed`，否则读路径、分页、就绪检查和领域写事务都会拒绝，且回填工具在 completed 状态下拒绝重跑，避免覆盖 normalized-only 事实。该模式不会静默回退或读取快照；旧媒体任务和请求预占表不参与当前读源。规范化写入唯一事实源、真实额度并发和跨实例一致性仍需完成。在完成领域 repository、真实 PostgreSQL 并发/故障集成测试和跨进程一致性验收前，不得将该模式宣称为完整生产部署方案。

生产传输边界已单独配置：`APP_DEPLOYMENT_ENV=production` 时必须提供无凭证/无查询的 `APP_PUBLIC_BASE_URL=https://...`，并拒绝 `APP_ALLOW_INSECURE_HTTP=true`；Go 仍可在反向代理后使用内部 HTTP。Compose 的管理端 HTTP hop 绑定回环地址，宿主机 TLS 入口模板和不允许公网 HTTP 的发布检查位于 `deploy/reverse-proxy/` 与 `tools/check-https-publish.mjs`。真实证书、DNS、代理加载和 HTTPS PostgreSQL 联调仍需发布环境证据。

## 多产品控制面存储规划（未实施）

详细需求与阶段以 [`tasks/prd-douyin-desktop-control-plane-integration.md`](./tasks/prd-douyin-desktop-control-plane-integration.md) 为准。实施顺序必须先扩展、回填、验证，再收紧约束；不得只给查询 DTO 增加 `product`。

- 新增不可变 `products` 注册表并幂等种子化 `autolive`、`douyin_desktop`；全局 `users` 不复制，产品准入进入 `user_products`。
- 客户端会话绑定单一 product；设备、激活码、设备绑定、模型租约、用量、审计以及相关幂等 scope 必须包含或由受约束引用确定 product。
- 相关唯一约束和引用按产品收口，例如设备唯一性为 `(product, device_id)`；跨产品复用同一物理 ID 不能形成跨产品外键或资源访问。
- 历史数据先回填 `autolive`，校验空值、孤立引用和数量后再设置非空/产品范围约束；旧客户端默认映射必须有兼容窗口和退出门禁。
- 固定权限目录与自定义角色保持全局；用户角色绑定增加 product 范围，普通管理员查询授权范围不能由筛选参数扩大。
- Profile 离线授权仅保存签名验证/轮换所需 key id、权益 revision 和必要审计元数据；签名私钥只进入 Secret/KMS，不进入业务表、快照或客户端。
- douyin-desktop 本地 BYOK 不进入本数据库；可选委托短期凭证不得写普通列，创建/续租只允许一次性交付，持久层只保存撤销/过期所需非秘密元数据。

## 商业化与生产就绪存储规划（未实施）

详细需求与阶段以 [`tasks/prd-server-commercial-production-readiness.md`](./tasks/prd-server-commercial-production-readiness.md) 为准。下列结构尚未分配迁移编号，也未进入数据库或 OpenAPI；实施时必须先检查最新迁移目录，再顺序新增前向迁移，禁止修改 0001～0022。

- 管理 RBAC 计划使用独立的 `admin_permissions`、`admin_roles`、`admin_role_permissions` 和产品范围 `user_admin_roles` 表；权限代码来自服务端固定目录，自定义角色不得插入未知权限。用户可按产品绑定多角色且权限取并集，不新增用户级 allow/deny 表。
- 内建 `super_admin` 角色和本地管理员绑定必须由前向迁移/引导流程幂等创建，现有 `users.role='admin'` 兼容回填不得丢权；最后超管保护、角色权限更新和用户角色替换必须在短事务内锁定并校验，并同事务写审计 Outbox。
- 当前有效权限以数据库角色关系为事实源，不把长期权限快照作为 Access Token 中的唯一授权依据；角色撤销必须对后续请求立即生效。
- `activation_codes` 继续只保存哈希、前缀、状态和容量计数；新生成激活码的可恢复明文只允许写入独立 `activation_code_secrets`，密文格式必须包含 `format_version`、`key_id`、nonce/ciphertext，并使用绑定激活码 ID、用途和版本的 AAD。历史哈希-only 数据保持不可逆，不做伪回填。
- 激活码每个设备槽位写入独立绑定表，保存激活码、槽位、用户、设备、绑定/切换/释放时间与状态；历史只有汇总计数但无设备明细时生成“未知历史占用”占位，禁止静默降低已占容量。
- 套餐、不可变套餐版本、订单、支付尝试/通知、订阅和设备席位使用独立且 product 范围的领域表；订单保存人民币分金额及价格/周期/席位快照，商户订单号、平台交易号和回调事件均有数据库唯一约束，支付履约、订阅和席位变更在短事务内原子提交。
- 席位记录保存 slot/subscription/product/device、绑定/释放/转移时间与状态；并发激活不能超卖，释放/禁用/转移必须递增权益 revision 并撤销旧会话/租约/离线授权，且不能跨 product。
- 密码重置只保存 Token 哈希、用途、过期/消费时间和投递状态；明文 Token 只在生成到投递适配器的短生命周期内存在，不写日志、审计或数据库。
- 支付凭证、邮件凭证、对象存储凭证和 CI 验证策略使用专用 SecretRef，不进入业务表、JSONB 快照或普通配置。密钥轮换必须通过 `key_id` 和版本化密文支持双读/新写及可审计回收。
- 对象存储保存软件发布制品及其签名清单、SBOM、Provenance 和摘要；数据库只保存对象键、digest、签名/验证元数据、发布状态和权限条件，不保存用户本地媒体内容。
- 公共配置按 product/environment/namespace、Schema ID、不可变 Schema 版本、配置 revision 和 ETag 分表保存；任意远程 `$ref` 不得在运行时解析，Schema/配置文档均有大小、深度和复杂度上限，配置不能授予订阅权益。
- 桌面制品元数据按 product/channel/platform/arch 保存最低版本、灰度、强制升级、撤回和防降级策略；P0 OpenAPI 制品迁入对象存储时保持原 digest。
- 错误报告/反馈按 product 保存严格 allowlist 字段、指纹、同意、状态和 TTL；禁止 dump、截图、本地路径、Token/Cookie、Prompt/正文和 BYOK 入库，删除/冻结例外可审计但不复制正文。
- 数据保留策略、清理游标、归档结果、备份清单和恢复演练证据均需可审计；数据库与对象存储备份必须加密，恢复只在隔离环境执行并验证 RPO/RTO。

## 不可违反的存储边界

- `auth_sessions` 只保存 Access/Refresh Token 的哈希、过期时间、撤销时间、归属和设备绑定时间标记，不保存原始 Token；`device_bound_at` 仅用于跨存储补偿协调，超过保护窗口且找不到同用户有效设备时由有界清理任务解除绑定。
- 迁移 0018 移除 `auth_sessions.device_id` 指向 `devices` 的外键，以允许激活请求在设备业务事务提交前暂存绑定；PostgreSQL `TransactionalSessionBinder` 会在同一事务锁定已认证会话、校验用户归属并与激活/心跳状态一起提交，Memory/兼容路径才使用精确补偿；写入仍要求已认证会话与设备 ID 校验，进程崩溃遗留引用由保护窗口后的协调清理回收。
- `activation_codes` 只保存激活码哈希和不可逆展示前缀，不保存明文激活码；后续可恢复明文也只能进入独立加密表，不得写回该表或 `control_plane_state`。
- 0011 新增 `activation_codes.used_by_user_id`，保存首次核销用户归属；由 0001–0010 已有的设备核销关系回填，设备解绑后仍可审计原核销用户。
- 0012 为 `model_usage_records` 增加部分唯一索引 `(lease_id, client_call_id) WHERE client_call_id <> ''`；空调用 ID 不参与唯一性约束，客户端仍必须通过 `Idempotency-Key` 提交摘要。
- `model_accounts.secret_ref` 只保存服务端 Secret Store 引用；`model_account_secrets.ciphertext` 只保存密文；API Key 不进入 PostgreSQL 明文列、日志、HTTP 响应或前端构建产物。
- 模型账号创建使用事务型 SecretStore：`model_accounts` 与 `model_account_secrets` 在同一 PostgreSQL 事务提交，幂等记录同时落库；normalized PostgreSQL 密钥轮换通过 `ModelPoolSecretRotator` 和 `TransactionalSecretWriter.PutTx` 在同一事务切换 `model_accounts.secret_ref`、写入探测结果、删除旧密文并记录审计，失败整体回滚；Memory/快照保留 staged fallback。JSONB 快照不包含引用，PostgreSQL 事务加载时从 `model_accounts` 受保护列恢复当前引用。后台清理只删除 `model-account/%/rotation_%`、超过 1 小时且不在当前账号引用集合中的 staged 引用，并受批次和 `SKIP LOCKED` 限制；真实连接中断/提交未知结果、旧密钥归档和跨存储故障补偿仍需故障注入验收。
- normalized 模式的模型租约创建、续租、释放、回收、用量和幂等记录必须在同一数据库事务边界内完成状态变更；租约创建还必须在该事务内完成用户/设备归属、模型授权、服务端日额度、账号容量和过期派生检查，不能通过先写内存再异步补库实现“伪持久化”；调用摘要不能包含模型正文、API Key 或完整音频/视频内容。
- normalized 审计事件必须先写入 0019 的 `audit_outbox`；`dedupe_key` 以 request_id 为主，投递使用确定性 `audit_logs.id` 并在失败时保留 pending/attempts/next_attempt_at，Retention Scheduler 只按 1～1000 批次有界重试。激活/心跳/设备禁用/解绑/租约创建/续租/释放/回收/客户端调用摘要/normalized Secret 轮换成功事件已由业务事务直接入 Outbox，Outbox 写失败会回滚对应业务状态；Memory/快照 Secret fallback、提交未知结果、跨进程恢复和归档仍需真实 PostgreSQL 验收。
- 数据库连接、查询和事务必须配置超时；当前 `PostgresRepository`、normalized backfill、SQL Session Store 和 Secret Store 已使用 `APP_REQUEST_TIMEOUT` 作为事务/语句上限，并将驱动取消统一回译为 Context 错误；迁移命令使用独立 `APP_MIGRATION_TIMEOUT`，保留清理后台任务使用 `APP_RETENTION_CLEANUP_TIMEOUT`，应用使用 `APP_SHUTDOWN_TIMEOUT` 等待请求排空和健康/清理任务退出，Compose 以 `AUTOLIVE_STOP_GRACE_PERIOD` 给容器留出更大的终止宽限。更细超时观测、容量告警、归档和真实关闭冒烟仍需继续收口。迁移失败必须阻止应用进入生产服务状态。

## 接入顺序

1. 抽象 `UserRepository`、`DeviceRepository`、`ActivationRepository`、`SessionRepository` 和 `IdempotencyRepository`，先覆盖登录、激活、设备心跳和幂等。
2. 接入成熟 PostgreSQL 驱动/迁移工具，迁移执行在发布阶段完成，应用启动只做版本检查，不隐式修改表结构。
3. 把模型号池、租约、用量和调用摘要迁移到事务 repository；并发槽位与每日额度必须使用数据库原子更新或行级锁。
4. 已接入第一阶段服务端 Secret Store、控制面快照和规范化读源切片，并开放受迁移版本、数据库和主密钥校验保护的 `APP_STORAGE_MODE=postgres`；迁移命令使用独立 `APP_MIGRATION_TIMEOUT`，API 启动的 Ping/版本检查/管理员引导使用 `APP_REQUEST_TIMEOUT`；设置 `APP_MODEL_READ_SOURCE=normalized` 时要求 PostgreSQL 已完成 0020，且 `normalized_backfill_state.status=completed`。启用前先设置 `APP_NORMALIZED_BACKFILL_CONFIRM=YES` 执行 `go run ./cmd/backfill-normalized`，该工具不会由 API 启动自动调用，completed 状态下拒绝重复回填。它仍不等于完整生产持久化。
5. 接入 SessionRepository 和规范化业务 repository，移除快照过渡层；再将审计日志切换为独立写入事实源，并补跨进程/故障恢复集成测试。

## 验收门槛

- 进程重启后用户、设备、激活码、会话撤销、租约和模型用量仍可恢复。
- 两个 Go 实例并发使用同一幂等键时只能产生一个资源；租约并发和额度扣减不能超卖。
- 数据库不可用、迁移版本不匹配或 Secret Store 不可用时，服务端返回明确错误并拒绝进入生产模式。
- 审计日志能关联用户、设备、请求 ID 和资源，但不含密码、Token、API Key 或明文激活码。

已完成本地真实 PostgreSQL 16 冒烟验收：迁移版本/dirty 检查、服务启动、登录会话写入、服务重启后的 Access Token 查询、Refresh 轮换、旧凭证失效，以及模型账号元数据和加密密钥写入均通过；数据库中未发现 API Key 明文。早期任务、服务端模型代理和请求预占测试仅作为历史实现证据，不代表当前能力；当前仍需供应商直连、客户端调用摘要端到端验收、真实额度并发、规范化写入唯一事实源和跨实例一致性验收。
