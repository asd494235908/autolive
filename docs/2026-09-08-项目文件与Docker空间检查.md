# 项目文件与 Docker 空间检查（2026-09-08）

本次是本机空间检查与 Docker 运维，没有修改产品代码。初次检查未删除项目文件；后续经用户确认，使用 Cargo 原生命令清理了下列三个 Rust 构建目录，详见末尾执行结果。工作区已有大量未提交修改，均保留。目录大小为文件逻辑长度之和，单位 GiB，不等于实际可回收磁盘块。

## 项目清理候选

| 路径（相对仓库） | 大小 | 判断 |
| --- | ---: | --- |
| `desktop/src-tauri/target` | 57.23 GiB | Rust 构建产物，可重建，待用户确认删除 |
| `desktop/src-tauri/target-douyin-test` | 6.12 GiB | 专项测试构建产物，可重建，待用户确认删除 |
| `desktop/src-tauri/target-test-package` | 5.42 GiB | 测试打包构建产物，可重建，待用户确认删除 |
| `desktop-csharp-windows/tests/GpAutoLive.App.Tests/TestResults` | 3.36 GiB | 主要是 7 个崩溃转储 `.dmp`，另有 TRX/XML；属于诊断证据，保留待确认 |
| `desktop-csharp-windows/artifacts` | 37.45 GiB | 历史发布包、安装包、运行时缓存及验收产物，不能整体判定无用 |
| `desktop/package`、`desktop/package-test` | 1.98 GiB | 旧桌面打包结果，需明确是否仍需回滚后再清理 |

前三个目录合计约 68.77 GiB，均命中 `.gitignore`，`git ls-files` 未发现跟踪文件。检查时未发现 cargo/rustc/dotnet/MSBuild 进程，正在运行的应用位于 `D:\Program Files\GpAutoLive\app\GpAutoLive.exe`。删除前仍需重新核对运行进程。

保留依据：`VideoEffectPixelFixtureTests.cs` 的 `FindRuntimeFromRepository` 会查找 `artifacts/csharp-gpu83-real-v2` 和 `artifacts/csharp-windows-controller-20260903-v90`；`test-c7-install-transaction-lock.ps1` 默认引用 v89 包；NSIS 构建脚本引用 artifacts/cache 的 .NET 运行时安装器。历史目录、未跟踪文件及 Git 忽略项都不自动等于无用文件。

本次没有进行逐符号死代码审计，不能据此认定源码、历史迁移、第三方运行时或文档可以删除。项目检查范围为目录占用、Git 跟踪与忽略状态、关键产物引用及运行进程。

## Docker 清理

- `docker builder prune -a -f`：成功，Docker 报告回收 8.462 GB，构建缓存降至 0。
- `docker image prune -f`：成功，Docker 报告回收 910.9 MB 悬空镜像。
- 清理后 `docker system df`：16 个镜像、10 个容器（8 个运行）、5 个数据卷。未删除容器或卷，也未删除有标签的镜像。
- Docker 回收统计是引擎内部空间，不能直接当作 Windows C 盘已释放空间；需要数据盘压缩或迁移后的磁盘测量确认。

## 存储位置

初始 C 盘约余 1.06 GiB，D 盘约余 15.25 GiB，E 盘约余 189.81 GiB。选择 E 盘，目录位于仓库外，避免与项目清理混淆。

原数据盘：`C:\Users\asd49\AppData\Local\Docker\wsl\disk\docker_data.vhdx`，逻辑长度约 21.80 GiB。

通过 Docker Desktop 4.35.1 的 Settings → Resources → Advanced → Disk image location 选择 `E:\Docker`；界面自动使用 `E:\Docker\DockerDesktopWSL`。应用设置后由 Docker 自身执行停机、压缩与搬移。

官方依据：https://docs.docker.com/desktop/features/wsl/

## 验证边界

只执行文件检查和 Docker 运维验证，不运行产品自动化测试、全量构建、页面业务点击或外部模型调用。容器健康不等于真实业务端到端验收。

迁移完成：设置文件 `CustomWslDistroDir` 已为 `E:\Docker\DockerDesktopWSL`，新目录内存在 `disk/docker_data.vhdx` 和 `main/ext4.vhdx`，C 盘原 `disk/docker_data.vhdx` 已不存在。Docker 自身先自动压缩数据盘，再搬移；新数据盘逻辑长度约 8.55 GiB。C 盘可用空间从约 1.06 GiB 增至约 22.98 GiB，E 盘约余 181.09 GiB。

迁移后本项目 3 个容器自动恢复；另一个本机项目的 5 个原运行容器未自动启动，使用 `docker start` 按基础服务、应用服务顺序恢复。两个原本退出成功的 migrate 容器不重跑。通过 `docker ps -a`、`docker inspect`、`docker volume ls`、`docker system df` 复核容器、健康状态、数据卷与缓存；16 个镜像、10 个容器、5 个卷保留，构建缓存为 0。

保留有标签但暂未被容器引用的镜像（包括其他项目发布镜像及基础镜像），避免误删本地发布产物。后续构建缓存与镜像层随 Docker 数据盘写入 E 盘；Docker 程序、少量设置和日志仍可能留在 C 盘。

没有新增依赖、后台定时清理器或额外迁移脚本；保留数据库卷、容器、未提交文件和真实测试依赖。项目源码与业务能力没有修改，本次真实验证仅覆盖本机存储迁移和容器运行状态。

## 用户确认后的 Rust 清理结果

用户确认删除三个 Rust 构建目录后，PowerShell `Remove-Item` 被执行策略拒绝，未产生删除。用户随后明确提出使用 Rust 清理命令。

先对每个目录运行 `cargo clean --manifest-path E:\aotlve\desktop\src-tauri\Cargo.toml --target-dir <绝对目标路径> --offline --dry-run`，确认范围后去掉 `--dry-run` 执行。三个命令均退出 0：

- `target`：Removed 64037 files, 57.2GiB total。
- `target-douyin-test`：Removed 7002 files, 6.1GiB total。
- `target-test-package`：Removed 5644 files, 5.4GiB total。

完成后通过 `Test-Path` 确认三个目录均已不存在。没有删除 C# 历史发布包、崩溃转储、运行时资源或源文件。后续 Rust 编译需要重新生成构建缓存。本轮仅做缓存清理，不运行会重新生成这些缓存的产品构建或自动化测试。
