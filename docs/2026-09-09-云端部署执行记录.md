# 2026-09-09 云端部署执行记录

## 发布前 OpenAPI 生成类型同步

发布准备基线为 `3ab614b`。管理端 `api:check` 检查发现 `admin-web/src/api/openapi.generated.ts` 未同步当前唯一契约源 `接口契约/openapi.yaml` 中的客户端资产同步接口类型。

使用项目锁定的官方 `openapi-typescript 7.13.0` 重新生成文件，没有手工编辑生成产物。生成器复用本地发布准备目录中已安装的依赖；输入和输出均为当前仓库文件，不在服务器执行构建。

生成差异仅新增 239 行 TypeScript 类型声明：`/api/v1/client/sync/items` 路由、客户端同步请求/响应与载荷 schema，以及 `listClientSyncItems`、`writeClientSyncItems` 操作类型。未修改 OpenAPI 契约、运行逻辑、鉴权、页面或接口调用。

工作目录为 `E:\aotlve`，实际执行命令：

```powershell
node E:/aotlve/artifacts/cloud-release-20260909-3ab614b/source-utf8/admin-web/node_modules/openapi-typescript/bin/cli.js 接口契约/openapi.yaml -o admin-web/src/api/openapi.generated.ts
node E:/aotlve/artifacts/cloud-release-20260909-3ab614b/source-utf8/admin-web/node_modules/openapi-typescript/bin/cli.js 接口契约/openapi.yaml -o admin-web/src/api/openapi.generated.ts --check
git diff --stat -- admin-web/src/api/openapi.generated.ts
git diff -- admin-web/src/api/openapi.generated.ts
git diff --check -- admin-web/src/api/openapi.generated.ts
```

生成和 `--check` 均退出码 0；人工检查 diff 确认为接口类型同步；`git diff --check` 退出码 0。Git 提示工作区 LF 在后续操作可能按现有配置转为 CRLF，未出现空白错误。

## 本次同步的验证边界

- 代码检查：生成器一致性检查与 diff 检查通过。
- 自动化产品测试、完整构建：本次类型同步子任务未执行，由发布主任务统一验证。
- 容器健康、页面点击、外部模型返回、真实业务结果：本次子任务未验证；类型同步不能代表部署成功。
- Git 提交与镜像构建：本次子任务未执行。

## 最小化检查

正在进行消融实验。

本次只补齐契约要求的生成类型，不新增抽象、适配层或依赖，没有需要删除或合并的不必要设计。保留契约作为唯一事实源及生成器 `--check` 门禁，避免手工类型与服务端接口漂移。同步覆盖当前发布前生成一致性检查目标，不改变产品黄金路径、安全边界或业务验收要求。

## 云端部署状态

阶段状态：发布准备与数据库备份恢复完成时曾受本机 Docker 阻塞。引擎随后恢复，镜像构建、上传及部分隔离演练已完成；最新状态见本文末尾。尚未迁移线上数据库或替换线上容器。

### 发布候选与本地验证

- 冻结源码基线：`3ab614bdf177cea5222f0634c39c31c321a3e30e`，额外包含上述 239 行生成类型补丁。候选不是纯 HEAD，补丁保存在本地 `artifacts/cloud-release-20260909-3ab614b/generated-contract.patch`。
- 有效源码暂存目录：`artifacts/cloud-release-20260909-3ab614b/source-utf8`。使用 .NET ZipFile 明确按 UTF-8 解压；上层早期暂存目录存在文件名编码问题，不得用于发布。既有 C# 未提交修改未纳入服务端候选。
- 后端：`go test ./...` 通过。以 `GOOS=linux GOARCH=amd64 CGO_ENABLED=0` 执行 `go build -trimpath -o ../../autolive-api ./cmd/server` 和 `go build -trimpath -o ../../autolive-migrate ./cmd/migrate`，均成功生成本地 Linux 二进制。
- 管理端：使用锁定的 pnpm 10.0.0 执行 `install --frozen-lockfile`、`api:check`、`typecheck`、`test`、`build`。生成类型补齐后一致性检查通过；23 项测试通过，类型检查和构建通过。构建存在大 chunk 提示，未阻止构建。
- C# 兼容单元测试：此前本轮验证的认证 HTTP 客户端、认证协调器与心跳调度器共 34 项通过；结果位于 `artifacts/server-compatibility-20260909/control-plane.trx`。这不代表真实云端升级兼容验收通过。
- `go test -race ./internal/httpapi ./internal/service ./internal/store` 未通过执行环境门禁：本机默认 windows/386、CGO 关闭；待本地 Linux 环境可用后执行。PostgreSQL 集成测试、镜像构建及镜像本地运行尚未执行。
- 已尝试启动 Docker Desktop，再次执行 `docker info --format '{{.ServerVersion}}'` 仍报告 `dockerDesktopLinuxEngine` 管道不存在；未重置或删除 Docker 数据。

### 远端只读核验与备份

- 实际 Compose：`/opt/services/autolive/docker-compose.yml`，项目名 `autolive`，管理端继续通过 9090 暴露；配置文件权限 600。
- API：`autolive-api:dev-2.0-75f5ae5-20260828-172007-authfix`，image ID `sha256:62f3f084f7bdfa432c929ab8d8d53a3e676c9415ca7980e8152a22aae696385f`。
- 管理端：`autolive-admin:dev-2.0-75f5ae5-20260828-053903-workspace`，image ID `sha256:3606e30b178f7f4f92112ea63a52714136e1d61853774adedab3cee41ffa6d2a`。
- API 使用 PostgreSQL 存储、normalized 读取模式，当前环境为 staging。数据库容器为共享的 `sub2api-postgres`，数据库 `autolive_control_plane`；未停止、重建或修改该共享容器。
- 原库迁移版本 27、dirty=false，约 99 MB，用户 4、设备 17。宿主机可用磁盘约 8.2 GB；镜像上传前须重新核对空间。
- 本次受限备份目录：`/opt/services/autolive/backups/20260909-3ab614b-predeploy`，目录权限 700，配置和备份文件权限 600。保存实际 Compose、环境配置及脱敏镜像标识；敏感文件未进入仓库。
- `pg_dump -Fc` 成功，备份大小 5,164,560 字节，SHA-256：`c523108fbeeaad9c2c379ca28fc16b9444a752ada66a44fd4a6444d97b954434`。`pg_restore --list` 成功读取 227 项。
- 新建隔离数据库 `autolive_restore_20260909`，`createdb`、`pg_restore` 均退出码 0；恢复后版本 27、dirty=false、用户 4、设备 17。该库包含敏感会话材料，仅作受限演练，不对客户端开放。

### 尚未完成的发布门禁

1. 本机 Docker 引擎恢复后，完成 Linux race 检查、相关 PostgreSQL 集成测试、两镜像本地构建和运行，记录镜像及归档摘要。
2. 在隔离库完成迁移 0028、旧会话跨版本刷新与回滚演练。代码已确认旧 API 精确要求 schema=27，新 API 要求 28，不能把“只新增表”视为旧镜像可直接回滚。尚无演练通过的回滚方案，因此禁止直接迁移线上库。
3. 确认回滚方案后再上传产物、更新发布前备份并切换 API；完成真实 C# 登录、授权、刷新和心跳验证，再切换管理端并观察。

### 分类验收结论

| 类别 | 实际结果 |
| --- | --- |
| 代码检查 | 生成一致性、管理端类型检查及相关 diff 检查通过 |
| 自动化测试 | Go 全量普通测试、管理端 23 项、C# 认证链路 34 项通过；race 与 PostgreSQL 集成未完成 |
| 构建 | Linux API/迁移二进制和管理端 dist 完成；发布镜像尚未构建 |
| 容器健康 | 旧服务已核验；候选镜像健康未验证 |
| 页面点击 | 新版本管理端未验证 |
| 外部模型返回 | 本次无外部模型调用，不适用 |
| 真实业务结果 | 数据库备份恢复成功；升级后的真实登录、刷新、心跳及会话连续性未验证 |

主任务消融检查未新增依赖、发布框架或运行时抽象。保留隔离恢复库、备份和迁移兼容门禁，因为旧程序存在明确 schema 拒绝行为；没有把生成类型同步或静态测试冒充部署成功。未进行无关未使用代码删除。

## Docker 恢复后的继续实施

用户确认已启动 Docker 后，本机 `docker info` 返回 `27.3.1 linux x86_64`。开始使用有效暂存目录内既有 Dockerfile 本地构建 `autolive-api:20260909-3ab614b-r1` 与 `autolive-admin:20260909-3ab614b-r1`，结果待后续记录。

### 重建回滚候选边界（实施前约束）

只读审查确认 `fd9b1b5` 包含旧部署记录对应的登录限流修复，但尚无源码摘要证明它与线上 authfix 二进制完全同源。线上二进制只读提取的 Go build info 确认为 linux/amd64，未提供 VCS revision。因此候选必须标为“重建回滚候选”，不得称为原镜像。

在本地独立发布暂存目录提取 `fd9b1b5` 后端，不使用 worktree、不修改当前产品后端。实际 server 启动入口是 `cmd/server/main.go` 的 `validateDatabaseVersion` / `validateAppliedMigration`，并不调用 `internal/migrationscheck`。兼容补丁仅把这个真实启动检查明确限定为 schema 28 且 clean；旧迁移目录、catalog 和独立迁移检查工具仍如实记录 27，回滚候选只运行 server，禁止运行其迁移程序。候选不得新增同步 API或放宽为接受任意高版本。新增直接边界测试覆盖 27、28、29、dirty 和数据库读取失败。差异、测试与镜像摘要全部记录后，才可进入隔离演练。

回滚保持数据库版本 28、三张同步表、密钥和会话原位，不执行 Force 27、不移动同步表、不恢复整库。必须验证旧→新→回滚候选→新版本的刷新连续性、新同步数据保留及幂等回执不变。原线上镜像继续保留，但不能直接用于 schema 28 回滚。若重建候选未通过演练，则不得切换线上。

### 已完成的镜像、测试与隔离演练

- 既有两个 Dockerfile 在本地成功构建 linux/amd64 镜像。API image ID：`sha256:c9fdf9af5ea5b92464b66eddb8aa2acc47c5e7da695c8d5d02096b375a29f296`；管理端 image ID：`sha256:f4fbd2b9f725c44e3b7e42e806b2b36ad5e4fc8bcf23b6220174b727c041f543`。实际 Nginx 基础镜像 digest：`sha256:65645c7bb6a0661892a8b03b89d0743208a18dd2f3f17a54ef4b76fb8e2f2a10`，Go/Alpine 沿用 Dockerfile 固定 digest。
- `docker save` 导出 `autolive-release-r1.tar`，SHA-256 为 `7044fc3076f6fe40c801188074bf34b248b0ed5b49d6e66623529221a45b4895`。通过 SFTP 上传到远端 `/opt/services/autolive/releases/20260909-3ab614b-r1/`，远端重新计算摘要一致，`docker load` 成功。服务器没有接收源码或执行构建。
- Linux race：基于本地 build stage 安装 GCC/musl 工具链，补入 Dockerfile `.dockerignore` 排除的测试文件及契约目录后，`CGO_ENABLED=1 go test -race ./internal/httpapi ./internal/service ./internal/store` 三包通过。最初一次 `[no test files]` 不计为通过；补入测试后因契约目录缺失失败的尝试也不计为通过。最终证据为 `linux-race-final.log`。
- 本地独立 PostgreSQL 18 容器与独立 Docker 网络完成迁移、显式 normalized backfill；`TEST_POSTGRES_URL` 指向该专用测试库，`go test -tags postgres_integration -count=1 ./migrations ./internal/store` 两包通过，证据为 `postgres-integration.log`。没有向线上库运行集成测试。
- 本地候选 API 容器为 healthy，管理端通过 `127.0.0.1:19092` 代理 `/api/v1/readyz` 返回候选版本及 `status=ok`。浏览器实测首页进入登录页，点击空表单“登录”显示“请输入账号和密码。”；尚未完成管理端真实登录页面验收。
- 远端隔离 API 只绑定 `127.0.0.1:19091`，使用恢复库，关闭模型健康探测和保留清理调度；未向外部模型发请求。原环境不保留管理员明文凭据，因此仅在恢复库将本地管理员副本改成随机演练凭据，并通过受保护 API 创建普通测试账号与一天、单设备授权；线上账号未改动。
- 初次隔离迁移失败：恢复库由 `sub2api` 拥有，应用用户没有 public schema CREATE 权限；线上原库由 `autolive_app` 拥有且具备权限。已明确确认隔离库三张新增表均不存在后，对齐隔离库所有者，并仅修复该次失败迁移账本，再执行迁移成功，耗时 21 ms。此修复不是正常回滚方案，不用于线上绕过版本门禁。
- 原线上镜像在隔离 schema 27 启动成功；隔离库升级 28 后再启动原镜像，实测退出码 1，明确报告版本 28 与要求 27 不符。随后启动新候选成功。
- 本地 C# 探针复用现有产品 `ControlPlaneHttpClient` 与 DTO 程序集，先在旧隔离 API 完成 Login、Activate、Heartbeat；保持同一进程和会话，隔离 API 升级后 Refresh、Activate、Heartbeat 全部 PASS。这是实际 HTTP/数据库及 C# 严格响应解析证据，不是 Mock；尚不代表完整桌面 UI、冷启动、媒体输出或线上切换验收。
- 同步演练的产品代码实际为 `douyin_desktop`。恢复库内为独立同步测试账号显式准备产品成员关系，然后真实发送新增资产、先创建后删除的墓碑与幂等重试，服务器 revision=3。回滚前数据指纹已保存在远端 `sync-before-rollback.json`：workspaces 1 行、items 2 行、mutations 3 行。初次删除不存在项的 409 属于被校验拒绝的测试准备错误，不计成功。

### 当前剩余门禁

重建回滚候选的真实入口边界红测已执行，待应用差异保存在本地 `rollback-schema28-pending.patch`；生产补丁尚未应用。按用户提供的 AGENTS.md 第 3 条，已询问是否清理本次将不再使用的两个 migrations 导入，等待答复；也已询问是否进行代码总监审查。尚未完成候选普通全量测试、构建及新→回滚候选→新演练，因此线上保持旧版。

上线后的真实管理员登录、新普通用户登录、C# 冷启动与设备身份、撤权/网络恢复、媒体输出及至少 20 分钟观察均未执行。原服务配置中没有管理员明文凭据，后续需使用已有受控登录方式或专用测试账号完成真实线上验收，不能通过修改原管理员密码取得凭据。

本地清单为 `artifacts/cloud-release-20260909-3ab614b/release-manifest.json`，包含源码提交、生成补丁摘要、归档摘要、架构和镜像标识。临时演练凭据保存在受限目录，禁止写入清单、源码或普通日志。当前状态是“镜像已上传、隔离演练进行中”，不是部署完成。
