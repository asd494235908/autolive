# 插话播放器与音轨优先级实施计划

> **For agentic workers:** 本计划由主线程拆分为两个不重叠写入范围并行执行；实现必须先写失败测试，再写生产代码。

**Goal:** 在桌面端最终效果窗口加入本地随机插话播放器，并统一固定话术替换、实时变体、原声和插话的优先级与回退行为。

**Architecture:** Rust/Tauri 负责插话目录规范化、音频文件扫描、配置校验和播放快照；React 最终效果窗口复用现有 Web Audio `AudioContext`，把当前基础音轨和插话音轨分成两个总线。基础音轨按 `voice_clone > realtime_variant > processed_original > original` 解析；插话永远是叠加层，插话开始时通过侧链 Gain 平滑降低当前基础音轨。

**Tech Stack:** Rust std、现有 Tauri 2、React 18、Web Audio API、Ant Design、现有 FFmpeg/FFprobe 能力探测；不新增依赖，不接入 Go API，不在服务器构建。

## Global Constraints

- 不使用 Git worktree，直接在当前分支和工作区实现。
- 不把插话文件、目录路径或视频内容上传 Go 后端。
- 不把插话音频写入 `current_audio_source`，不得破坏实时变体候选状态机。
- 固定话术替换是最高优先级；固定话术生成/播放期间不启动新的插话。
- 插话仅实时叠加，不生成新的 MP4，不重新创建最终效果窗口。
- 暂停、停止、切换源视频、进入下一轮时必须取消或复位插话和闪避状态。
- 所有路径、目录、扩展名、数量、时间和增益参数必须在 Tauri/Rust 边界校验。
- 现有未提交改动属于用户；只修改本计划和任务明确的文件。

## 文件职责与写入分区

### 后端任务

- Create: `desktop/src-tauri/src/interlude_player.rs` — 插话配置、扫描、候选选择、状态和基础参数校验。
- Modify: `desktop/src-tauri/src/lib.rs` — 注册模块、统一基础音轨优先级、保存插话配置/快照。
- Modify: `desktop/src-tauri/src/commands.rs` — Tauri DTO、配置命令和快照映射。
- Modify: `desktop/src-tauri/src/main.rs` — 注册 `set_interlude_config` Tauri 命令。
- Create: `desktop/src-tauri/tests/interlude_player_contract.rs` — Rust 插话扫描、参数校验和优先级契约测试。

### 前端任务

- Create: `desktop/ui/src/插话播放器.ts` — 随机间隔、候选选择、基础音轨优先级和插话调度的纯函数。
- Create: `desktop/ui/src/插话播放器.test.mjs` — 前端调度和优先级失败测试。
- Modify: `desktop/ui/src/App.tsx` — 最终窗口 Web Audio 插话总线、主界面配置面板、快照控制和冲突处理。

## Task 1: Rust 插话状态和统一基础音轨优先级

**Interfaces:**

```rust
pub struct InterludeConfig {
    pub enabled: bool,
    pub directory: Option<String>,
    pub interval_min_ms: u64,
    pub interval_max_ms: u64,
    pub volume_db: f64,
    pub ducking_depth_db: f64,
    pub ducking_attack_ms: u64,
    pub ducking_release_ms: u64,
}

pub struct InterludeSnapshot {
    pub enabled: bool,
    pub directory: Option<String>,
    pub audio_files: Vec<String>,
    pub audio_count: u32,
    pub status: String,
    pub error: Option<String>,
}
```

- [ ] Write `interlude_player_contract.rs` tests for empty directory rejection, unsupported extensions being ignored, min interval not exceeding max interval, invalid gain/range rejection, and stable `effective_audio_source` priority.
- [ ] Run `cargo test --manifest-path desktop/src-tauri/Cargo.toml --test interlude_player_contract` and confirm it fails because the module and commands do not exist.
- [ ] Implement directory canonicalization and non-recursive scan using `std::fs::read_dir`; allow only common local audio extensions already playable by the WebView (`mp3`, `wav`, `m4a`, `aac`, `ogg`, `flac`). Ignore subdirectories and unreadable files while returning an empty-catalog error when no usable file remains.
- [ ] Add `InterludeConfig::validate`, `InterludeCatalog`, and `InterludeSnapshot` with bounded intervals, dB values and attack/release values. Keep constants local to the module and do not add a generic utility module.
- [ ] Add `PlaybackCore` fields for `interlude_config` and `interlude_snapshot`, expose `effective_audio_source` as `voice_clone`, `realtime_variant`, `processed_original` or `original`, and keep `current_audio_source` restricted to the existing base audio state.
- [ ] Add a Tauri `set_interlude_config` command that accepts the directory and configuration, canonicalizes/scans it, updates the playback snapshot, and returns a clear error when the directory has no usable files.
- [ ] Run the focused Rust test again and then `cargo test --manifest-path desktop/src-tauri/Cargo.toml --lib`.

## Task 2: React Web Audio 插话混音、配置界面和调度

**Interfaces:**

```ts
export type BaseAudioSource = 'voice_clone' | 'realtime_variant' | 'processed_original' | 'original';

export function resolveBaseAudioSource(input: {
  voiceCloneActive: boolean;
  realtimeVariantActive: boolean;
  processedOriginalActive: boolean;
}): BaseAudioSource;

export function randomIntervalMs(minMs: number, maxMs: number, random?: () => number): number;
export function chooseInterludeIndex(count: number, previousIndex: number | null, random?: () => number): number | null;
export function shouldPauseInterlude(input: {
  playbackState: string;
  voiceCloneStatus: string;
}): boolean;
```

- [ ] Write `插话播放器.test.mjs` tests for the four-level base track priority, bounded random interval, no immediate repeat when multiple files exist, empty catalog returning `null`, and pausing insertion while fixed voice clone is generating or playing.
- [ ] Run `node --test desktop/ui/src/插话播放器.test.mjs` and confirm it fails before the helper exists.
- [ ] Implement the pure helper module with no DOM or Tauri dependencies.
- [ ] In `FinalEffectWindow`, add one hidden interlude `<audio>` element and one `interludeGain` plus `baseBus`, `duckGain` and `interludeBus` nodes. Route only the resolved base source through `baseBus → duckGain → analyser/destination`; route interlude through `interludeGain → interludeBus → analyser/destination`.
- [ ] Apply `AudioParam` ramps for `ducking_depth_db`, `ducking_attack_ms` and `ducking_release_ms`; do not use a normal compressor as a substitute for sidechain ducking.
- [ ] Drive random insertion from the video playback clock and current playback generation/loop index. On pause, stop/pause the interlude and preserve the next due time; on stop, source switch or loop boundary, cancel the current interlude and reset duck gain to unity.
- [ ] When fixed voice clone status is `generating` or `playing`, stop/fade the current interlude and suppress new scheduling. When realtime variant or original audio is active, allow insertion and duck the resolved base bus.
- [ ] Ensure interlude completion never calls `restore_original_audio` and never changes `current_audio_source`.
- [ ] Add an Ant Design panel to the main window with enable switch, folder chooser, interval min/max, interlude volume, duck depth, file count, current status and error state. Use the existing Tauri dialog directory picker and `set_interlude_config` command.
- [ ] Run focused frontend tests, then `pnpm --dir desktop/ui build` and the existing frontend test command.

## Main-thread review and verification

- [ ] Inspect the two task diffs and remove unused imports, types, commands, state fields and dead branches exposed by the change.
- [ ] Verify no backend API, database migration, server build or worktree was introduced.
- [ ] Add or adjust tests for voice clone priority, realtime variant fallback, interlude suppression, loop reset and invalid folder handling.
- [ ] Run the complete local checks supported by this repository: Rust format/check/test, frontend typecheck/build/test, and inspect `git diff --check`.
- [ ] Perform a manual desktop smoke test when the local Tauri runtime is available: select a folder, trigger random insertion, pause/resume, start fixed voice replacement, complete a loop, stop, and confirm no stuck ducking or silent base track.
