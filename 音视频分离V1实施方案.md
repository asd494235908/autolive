# 音视频分离 V1 实施方案

## 1. 目标与边界

本版本将视频处理、普通声音处理和实时话术幻化明确拆成三条独立链路。视频开关只改变画面，声音开关只改变普通音频，实时话术幻化只负责候选话术音轨；任一链路失败都必须保留其他链路和源视频可播放。

本版本纳入现有 FFmpeg、Tauri 和 Windows 系统语音可以稳定完成的能力；Web Audio 只保留只读波形/频谱观测，不参与声音效果处理。不引入浏览器插件，不要求用户手动安装 Python、FFmpeg 或模型。

不在 V1 承诺：AI 降噪、语音克隆、MFCC/共振峰实时修改、异步视频帧修复、抽象人脸生成、GPU 重建和平台接入。上述能力保留参数契约，但在 Worker 未接入时必须显示 `unavailable`，不得伪造 `runtime`。

## 2. 功能范围

### 2.1 视频处理

- 已接入 FFmpeg 的亮度、对比度、饱和度、色相、模糊、锐化、细节增强、噪点；
- 已接入像素级缩放、动态裁剪、X/Y 空间偏移和基础像素帧间微扰；
- 视频处理开启时重新编码视频，关闭时保持视频流 `copy`；
- 旋转、水平/垂直翻转、目标帧率、帧率微扰、边缘平滑、色域转换、文字/图片叠加和研究挂件暂未接入，必须保持 `unavailable`，不能只在界面显示为已生效；
- 处理成功后在播放轮次边界切换，不覆盖源文件。

### 2.2 普通声音处理

- 已接入 FFmpeg 媒体输出链的输入增益、输出增益、响度、低/中/高频 EQ、滤波 Q、音高微移、播放速度、淡入、淡出、轻混响、降噪、相位扰动、颤音、环境噪声、采样率和输出码率；这些参数表示处理产物的实际编码/滤镜能力；
- 视频关闭且声音开启时保持视频流 `copy`，只重新编码音频；
- FFmpeg 原生 `afftdn`、`aphaser`、`vibrato`、`anoisesrc` + `amix` 和 EQ 的 Q 值已接入声音处理；环境声素材混合、干湿比、压缩、MFCC、共振峰和频谱扰动暂未接入，必须保持 `unavailable`；
- 普通声音效果统一由 FFmpeg 媒体输出链处理；UI 的音频参数预览展示用户配置、FFmpeg 处理状态和已生效参数，不使用 Web Audio 伪造处理结果；播放速度按用户配置生效但不参与周期调度，所有已支持参数在点击应用后由 FFmpeg 生效；
- 普通声音处理不依赖 Web Audio；波形、频谱和响度诊断若启用，仅作为只读观测，不改变 FFmpeg 处理结果；
- 处理失败时保持原音轨。

### 2.3 独立开关

| 视频处理 | 普通声音处理 | 结果 |
| --- | --- | --- |
| 关 | 关 | 原视频 + 原音频 |
| 开 | 关 | 处理视频 + 原音频 |
| 关 | 开 | 原视频 + 处理音频 |
| 开 | 开 | 处理视频 + 处理音频 |

实时话术幻化不被以上两个开关隐式开启或关闭；其候选失败时回退原音轨或上一条可用音轨。

## 3. 模块职责

### Rust/Tauri

- `research_params.rs`：保存带单位、范围、默认值和版本的音视频参数；
- `media_engine.rs`：分别构建视频滤镜和音频滤镜；未启用的流使用 `copy`；
- `commands.rs`：按当前处理开关执行视频-only、音频-only 或音视频同时处理；UI 的三个应用动作负责显式选择范围，同时保持现有 IPC 请求兼容；
- `lib.rs`：维护独立的 `video_processing_status` 和 `audio_processing_status`；
- `media_engine_contract.rs`：验证视频-only、音频-only、音视频同时处理和失败回退。

### React/Tauri UI

- 视频和声音分栏展示；
- 每个模块独立开关、参数、应用按钮、进度和错误；
- 显示 `disabled/configured/processing/ready/runtime/unavailable/failed`；
- 实时预览只消费对应模块的运行参数；
- 不展示已移除的运行资源下载/安装页面。

### 本地资源

- FFmpeg/FFprobe 随安装包分发；
- 子进程使用隐藏窗口策略；
- 系统固定话术使用 Windows `speechSynthesis`；
- 语音 Worker、模型和高级分析器暂不作为 V1 的强制依赖。

## 4. 处理链规则

```text
源视频
 ├─ 视频开关 → FFmpeg 视频滤镜 → 下一轮切换
 ├─ 声音开关 → FFmpeg 音频滤镜（全部已支持声音参数）→ 处理缓存/当前音轨
 └─ 话术开关 → speech-to-speech Worker → 候选音轨安全换轨
```

- 只开视频时，FFmpeg 使用 `-c:a copy`；
- 只开音频时，FFmpeg 使用 `-c:v copy`；
- 两条处理链共享源文件但不共享可变状态；
- 每个产物使用 `.partial` 临时文件、FFprobe 可读性校验、完整 SHA-256 和原子提交；
- 任何处理失败不得暂停、静音、黑屏或覆盖源文件。

## 5. 实施阶段

### Phase 1：参数和命令契约

1. 由 UI 应用动作和当前处理开关确定明确处理范围，保持现有 IPC 请求兼容；
2. 将视频/音频参数的运行状态分开维护；
3. 更新参数校验和未映射参数错误；
4. 先补充失败测试。

### Phase 2：视频链路

1. 扩展 FFmpeg 视频滤镜；
2. 当前 V1 已完成基础色彩、画质、缩放、裁剪、空间偏移和像素扰动映射；
3. 旋转、翻转、帧率和基础叠加等待参数契约与处理链同时具备后再实施；
4. 验证视频-only 不改音频。

### Phase 3：音频链路

1. 当前 V1 已完成 FFmpeg 媒体输出的增益、响度、三段 EQ、音高、变速、淡入淡出、轻混响、降噪、相位扰动、颤音、环境噪声、采样率和输出码率映射；
2. 当前 V1 的声音参数预览全部以 FFmpeg 处理状态为准，用户修改参数后重新生成音频处理缓存并在安全边界切换；播放速度只按用户配置生效，不参加周期变化；
3. 音高、采样率和输出码率在应用后由 FFmpeg 媒体输出链生效，不标记为 Web Audio `runtime`；
4. 环境声素材混合、干湿比、压缩和高级分析等待独立 DSP/分析器，当前保持 `unavailable`；环境噪声是 FFmpeg 生成的受控噪声混入，不等同于外部环境音素材；
5. 验证音频-only 不重编码视频。

### Phase 4：界面和错误恢复

1. 拆分视频/音频应用按钮；
2. 补充每个模块的 loading、error、unavailable 和 recovery；
3. 明确“配置值”和“实际生效值”；
4. 保持实时话术幻化独立。

### Phase 5：打包与验收

1. 检查 FFmpeg/FFprobe 是否位于安装包资源目录；
2. 验证 Worker 和 FFmpeg 不弹终端窗口；
3. 新安装环境不从网络重复下载内置资源；
4. 生成便携版和 Windows 安装包；
5. 执行桌面 UI、Rust、媒体和打包测试。

## 6. 依赖策略

V1 不增加用户侧插件。已有依赖足够完成基础能力：FFmpeg/FFprobe、Tauri、Windows WebView2 和系统语音；Web Audio 仅用于只读诊断。

后续可选依赖：

- RNNoise/DeepFilterNet：AI 降噪；
- Rubber Band：高质量变速/变调；
- librosa/torchaudio/ONNX Runtime：MFCC、共振峰和 SNR；
- OpenCV/ONNX/WebGPU：高级视频修复和推理。

可选依赖必须作为随包 Worker 或资源分发，不能要求用户手动安装浏览器插件。

## 7. 验收标准

- 视频、声音、实时话术三个开关互不串联；
- 视频-only、音频-only、音视频同时处理均能生成可读 MP4；
- 只开视频时音频流保持 copy；
- 只开音频时视频流保持 copy；
- FFmpeg 已映射的声音参数在媒体输出处理成功后标记为 `ready`；音频预览展示全部 21 项已支持参数及当前处理缓存状态；
- 播放速度按用户配置由 FFmpeg 生效，不参与周期调度；Web Audio 不处理增益、EQ、变速、混响或淡入淡出；
- 未接入的环境声素材混合、干湿比、压缩和高级分析参数显示 `unavailable`；
- 处理失败保持原视频和原音频；
- 不出现终端窗口；
- 不从 C 盘下载或解包内置资源；
- 新电脑不需要手动安装 FFmpeg、Python 或浏览器插件；
- 关闭任一开关不会停止另一条链路；
- 不接入 RTMP、OBS、平台发布或检测规避逻辑。

## 8. 交付记录

每个阶段交付时记录：改动文件、测试命令、构建产物、未验证项、剩余依赖和回滚方式。参数契约、桌面架构和长任务计划必须与实际生效状态同步更新。

- 2026-08-17：统一普通声音处理口径：增益/响度、三段 EQ、音高、变速、淡入淡出、轻混响、降噪、相位扰动、颤音、环境噪声、采样率和输出码率全部进入 FFmpeg 音频链；Web Audio 仅保留只读诊断，播放速度不参加周期调度；环境声素材混合、干湿比、压缩及高级分析保持 `unavailable`。
