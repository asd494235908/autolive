# 音视频分离 V1 实施方案

> 当前版本范围声明（2026-08-24）：本版本不开发实时话术幻化。本 V1 实施视频处理、普通声音处理、插话和固定话术系统朗读；参考图媒体参数全部是正式需求，但尚未映射算法的参数保持“待实现/未接入”。speech-to-speech、VAD/ASR/LLM/TTS、候选话术音轨和实时换轨仍不属于当前实施和验收范围。

## 1. 目标与边界

本版本只实施视频处理和普通声音处理两条链路，并保留插话与固定话术系统朗读。视频开关只改变画面，声音开关只改变普通音频；实时话术幻化不在本版本开发范围，相关链路不启动。任一当前链路失败都必须保留其他链路和源视频可播放。

> 当前实现事实源：`desktop/src-tauri/src/commands.rs`、`desktop/src-tauri/src/lib.rs`、`desktop/ui/src/App.tsx` 及其自动化测试。导入探测成功后保持 `Ready`，用户点击主页“播放”才打开最终效果窗口；普通声音处理是源视频音频流经 FFmpeg 滤镜后输出 PortAudio，PortAudio 关闭/不可用/失败时回退 WebView 源音频，不生成普通声音处理 MP4。

本版本纳入现有 FFmpeg、Tauri 和 Windows 系统语音可以稳定完成的能力；Web Audio 只保留只读波形/频谱观测，不参与声音效果处理。不引入浏览器插件，不要求用户手动安装 Python、FFmpeg 或模型。

当前正式参数模型中语义已确认的字段均已接入；MFCC/SNR 与 `current_formant_hz` 已进入最终混音 PCM 处理/只读诊断，`formant_shift_percent` 由 Signalsmith Stretch 执行。仍未实现的是依赖外部模型、外部素材或 OCR 语义确认的异步冻结回退、真人脸/主体保护、GPU 加速重建等缺失需求。语音克隆、实时话术幻化、独立算法报告、平台接入和检测规避不在 V1 范围。

## 2. 功能范围

### 2.1 视频处理

- 已接入 FFmpeg 的亮度、对比度、饱和度、色相、模糊、锐化、细节增强、噪点；
- 已接入像素级缩放、动态裁剪、X/Y 空间偏移和基础像素帧间微扰；
- 视频处理开启时重新编码视频，关闭时保持视频流 `copy`；
- 旋转、水平/垂直翻转、目标帧率、帧率扰动、边缘平滑、色域转换、文字/图片叠加和高级效果均为正式需求；暂未接入的必须保持 `unavailable`，不能只在界面显示为已生效；
- 处理成功并通过可读性校验后立即切换当前播放视频引用，不覆盖源文件，也不等待下一轮循环边界。

### 2.2 普通声音处理

- 已接入 FFmpeg 源音频流的输入增益、输出增益、响度、低/中/高频 EQ、滤波 Q、音高微移、播放速度、淡入、淡出、轻混响、降噪、相位扰动、颤音、环境噪声、采样率和输出码率；这些参数由 PortAudio 实时出口消费，不以处理后音频 MP4 作为播放前置条件；
- 视频关闭且声音开启时保持源视频播放，只对当前源音频流做实时处理；
- FFmpeg 原生 `afftdn`、`aphaser`、`vibrato`、`anoisesrc` + `amix`、EQ 的 Q 值、频谱扰动、频谱盲区、6kHz 以上高频扰动、真实环境声第二输入和干湿支路已接入声音处理；Signalsmith Stretch 执行 `formant_shift_percent`，realFFT/rustdct 与滚动 RMS/LPC 执行 MFCC、SNR 和 `current_formant_hz` 只读诊断。独立压缩因当前没有正式字段保持 `unavailable`；
- 普通声音效果统一由 FFmpeg 源音频流处理；UI 的音频参数预览展示用户配置、FFmpeg/PortAudio 运行状态和已生效参数，不使用 Web Audio 伪造处理结果；播放速度按用户配置生效但不参与周期调度，所有已支持参数在点击应用后由 FFmpeg 生效；当前实时变体音轨绕过普通声音滤镜；
- 普通声音处理不依赖 Web Audio；波形、频谱和响度诊断若启用，仅作为只读观测，不改变 FFmpeg 处理结果；
- 处理失败时保持原音轨。

### 2.3 独立开关

| 视频处理 | 普通声音处理 | 结果 |
| --- | --- | --- |
| 关 | 关 | 原视频 + 原音频 |
| 开 | 关 | 处理视频 + 原音频 |
| 关 | 开 | 原视频 + FFmpeg/PortAudio 处理源音频；PortAudio 失败时回退 WebView 源音频 |
| 开 | 开 | 处理视频 + FFmpeg/PortAudio 处理源音频；视频缓存内音频只复制源音轨 |

实时话术幻化本版本不提供开关、入口或候选音轨；代码中的相关字段如仍存在，只作兼容审计。普通声音处理失败时回退源音轨或 WebView 源音频。

## 3. 模块职责

### Rust/Tauri

- `media_effect_params.rs`：保存正式 `MediaEffectParams`、`AudioEffectParams`、`VideoEffectParams`、`AdvancedEffectParams` 的单位、范围、默认值和版本；旧 `research_params.rs`、报告 Worker 及其 IPC 已删除，禁止重新引入；
- `media_engine.rs`：构建视频缓存滤镜；未启用的视频音频流使用 `copy`；普通声音滤镜由 `commands.rs` 的流式混音路径调用；
- `commands.rs`：按当前处理开关配置视频缓存和普通声音流式出口；UI 的视频/普通声音应用动作负责显式选择范围，同时保持现有 IPC 请求兼容；实时话术 IPC 仅作历史兼容。
- `lib.rs`：维护独立的 `video_processing_status` 和 `audio_processing_status`；
- Rust/UI 测试：验证视频-only、声音-only、音视频同时处理、PortAudio 回退和失败恢复。

### React/Tauri UI

- 视频和声音分栏展示；
- 每个模块独立开关、参数、应用按钮、进度和错误；
- 显示 `disabled/configured/processing/ready/runtime/unavailable/failed`；
- 实时预览只消费对应模块的运行参数；
- 不展示已移除的运行资源下载/安装页面。

### 本地资源

- FFmpeg/FFprobe 优先随 Tauri 安装包内置；内置资源不完整时由 Rust 使用已校验的应用数据目录下载/安装路径兜底；
- PortAudio 运行库及许可证随桌面包分发；
- 子进程使用隐藏窗口策略；
- 系统固定话术使用 Windows `speechSynthesis`；
- 语音 Worker、模型和高级分析器暂不作为 V1 的强制依赖。

## 4. 处理链规则

```text
源视频
 ├─ 视频开关 → FFmpeg 视频滤镜 → 视频缓存 → 校验成功后立即切换当前视频引用
 ├─ 声音开关 → FFmpeg 源音频流滤镜 → PortAudio；关闭/失败回退 WebView 源音频
  └─ 固定话术 → WebView2 localService 系统声音朗读（不调用 speech-to-speech）
```

- 只开视频时，FFmpeg 使用 `-c:a copy`；
- 只开声音时不生成普通声音处理 MP4，源视频继续由 WebView 播放，处理后的 PCM 由 PortAudio 消费；
- 两条处理链共享源文件但不共享可变状态；
- 视频产物使用 `.partial` 临时文件、FFprobe 可读性校验、完整 SHA-256 和原子提交；声音运行时使用有界 PCM 缓冲和可取消的候选任务；
- 任何处理失败不得暂停、静音、黑屏或覆盖源文件。

## 5. 实施阶段

### Phase 1：参数和命令契约

1. 由 UI 应用动作和当前处理开关确定明确处理范围，默认值/校验命令统一使用 `get_default_media_effect_params`、`validate_media_effect_params`；
2. 将视频/音频参数的运行状态分开维护；
3. 更新参数校验和未映射参数错误；
4. 先补充失败测试。

### Phase 2：视频链路

1. 扩展 FFmpeg 视频滤镜；
2. 当前 V1 已完成基础色彩、画质、缩放、裁剪、空间偏移和像素扰动映射；
3. 旋转、翻转、帧率和基础叠加等待参数契约与处理链同时具备后再实施；
4. 验证视频-only 不改音频。

### Phase 3：音频链路

1. 当前 V1 已完成 FFmpeg 源音频流的增益、响度、三段 EQ、音高、变速、淡入淡出、轻混响、降噪、相位扰动、颤音、环境噪声、采样率和输出码率映射；
2. 当前 V1 的声音参数预览以 FFmpeg/PortAudio 运行状态为准，用户修改参数后预热候选并在播放安全边界切换；播放速度只按用户配置生效，不参加周期变化；
3. 音高、采样率和输出码率在应用后由 FFmpeg 流式处理生效，不标记为 Web Audio `runtime`；
4. 环境声素材混合通过已校验的第二输入循环解码，干湿比通过短反射湿声支路执行；MFCC/SNR/LPC 分析来自最终混音 PCM。独立压缩仍因缺少正式字段保持 `unavailable`；环境噪声是 FFmpeg 生成的受控噪声混入，与外部环境音素材保持不同输入语义；
5. 验证声音-only 不生成音频处理 MP4，视频保持源素材播放，PortAudio 不可用时回退 WebView。

### Phase 4：界面和错误恢复

1. 拆分视频/音频应用按钮；
2. 补充每个模块的 loading、error、unavailable 和 recovery；
3. 明确“配置值”和“实际生效值”；
4. 明确实时话术幻化本版本不开发；speech-to-speech、VAD/ASR/LLM/TTS 和候选换轨只进入后续版本评估，不得写入当前 V1 入口或验收。

### Phase 5：打包与验收

1. 检查 FFmpeg/FFprobe 是否位于安装包资源目录；
2. 验证 Worker 和 FFmpeg 不弹终端窗口；
3. 新安装环境优先使用安装包内置资源；内置资源不完整时才从受控应用数据路径下载并校验；
4. 生成便携版和 Windows 安装包；
5. 执行桌面 UI、Rust、媒体和打包测试。

## 6. 依赖策略

V1 不增加用户侧插件。已有依赖足够完成基础能力：FFmpeg/FFprobe、Tauri、Windows WebView2 和系统语音；Web Audio 仅用于只读诊断。

后续可选依赖与许可证门禁以《参考图参数对比与正式化清单》的依赖建议为准；本轮未引入新依赖。基础效果继续优先使用现有 FFmpeg + Rust 调度。

可选依赖必须作为随包 Worker 或资源分发，不能要求用户手动安装浏览器插件。

## 7. 验收标准

- 视频和普通声音两个当前开关互不串联；实时话术幻化本版本不提供开关。
- 视频处理成功生成可读视频缓存；声音-only 不生成音频处理 MP4，PortAudio 可用时输出处理 PCM，不可用时回退 WebView；
- 只开视频时音频流保持 copy；
- 只开声音时源视频不被改写，普通声音效果不写入视频缓存；
- FFmpeg 已映射的声音参数在流式处理运行时标记为 `runtime`，候选失败时回退；音频预览展示全部已支持参数及当前运行出口状态；
- 播放速度按用户配置由 FFmpeg 生效，不参与周期调度；Web Audio 不处理增益、EQ、变速、混响或淡入淡出；
- 已接入的环境声素材混合、干湿比和高级分析必须报告真实处理/诊断状态；独立压缩等尚无正式字段的能力显示 `unavailable`；
- 处理失败保持原视频和原音频；
- 不出现终端窗口；
- 不从任意未校验位置下载或解包资源；运行时下载仅使用受控应用数据路径；
- 新电脑不需要手动安装 FFmpeg、Python 或浏览器插件；
- 关闭任一开关不会停止另一条链路；
- 不接入 RTMP、OBS、平台发布或检测规避逻辑。

## 8. 交付记录

每个阶段交付时记录：改动文件、测试命令、构建产物、未验证项、剩余依赖和回滚方式。参数契约、桌面架构和长任务计划必须与实际生效状态同步更新。

- 2026-08-25：完成双视频换源、视频链性能/PIP/最小有效强度以及声音空 PCM/固定响度归一化修复。窗口按媒体代次拒绝迟到事件并保持全尺寸单一渲染表面；视频正式链减少高成本全帧遍历，PIP 时间窗相交且低感知叠加至少产生 `2/255` alpha；声音保留源相对响度，Signalsmith warmup/flush 空块不再进入 MFCC/SNR，PortAudio 未就绪时继续 WebView 原声。
- 2026-08-17（历史状态，已由 2026-08-24 正式参数完整接入覆盖）：当时环境声素材混合、干湿比及高级分析保持 `unavailable`；现已接入真实第二输入、湿声支路和最终混音 PCM 分析，独立压缩仍因无正式字段保持 `unavailable`。
- 2026-08-24：参考图参数全部正式化，当前类型/IPC 切换为 `MediaEffectParams` 系列和新默认值/校验命令；未映射字段继续保持 `unavailable`。旧 `research_*` 参数模块、报告 Worker 和 IPC 已移除，实时话术幻化、OBS/RTMP 和平台检测规避边界不变。
